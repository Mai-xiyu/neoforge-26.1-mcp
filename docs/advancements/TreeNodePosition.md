# TreeNodePosition

**Package:** `net.minecraft.advancements`
**Type:** class

## Methods

### TreeNodePosition

```java
public TreeNodePosition(AdvancementNode p_301306_, TreeNodePosition p_16568_, TreeNodePosition p_16569_, int p_16570_, int p_16571_)
```

**Parameters:**

- `p_301306_` (`AdvancementNode`)
- `p_16568_` (`TreeNodePosition`)
- `p_16569_` (`TreeNodePosition`)
- `p_16570_` (`int`)
- `p_16571_` (`int`)

**Returns:** `public`

### IllegalArgumentException

```java
throw new IllegalArgumentException("Can't position an invisible advancement!")
```

**Parameters:**

- `advancement!"` (`"Can't position an invisible`)

**Returns:** `throw new`

### firstWalk

```java
private void firstWalk()
```

**Returns:** `private void`

### secondWalk

```java
private float secondWalk(float p_16576_, int p_16577_, float p_16578_)
```

**Parameters:**

- `p_16576_` (`float`)
- `p_16577_` (`int`)
- `p_16578_` (`float`)

**Returns:** `private float`

### thirdWalk

```java
private void thirdWalk(float p_16574_)
```

**Parameters:**

- `p_16574_` (`float`)

**Returns:** `private void`

### executeShifts

```java
private void executeShifts()
```

**Returns:** `private void`

### apportion

```java
private TreeNodePosition apportion(TreeNodePosition p_16580_)
```

**Parameters:**

- `p_16580_` (`TreeNodePosition`)

**Returns:** `private TreeNodePosition`

### moveSubtree

```java
private void moveSubtree(TreeNodePosition p_16582_, float p_16583_)
```

**Parameters:**

- `p_16582_` (`TreeNodePosition`)
- `p_16583_` (`float`)

**Returns:** `private void`

### getAncestor

```java
private TreeNodePosition getAncestor(TreeNodePosition p_16585_, TreeNodePosition p_16586_)
```

**Parameters:**

- `p_16585_` (`TreeNodePosition`)
- `p_16586_` (`TreeNodePosition`)

**Returns:** `private TreeNodePosition`

### finalizePosition

```java
private void finalizePosition()
```

**Returns:** `private void`

### run

```java
public static void run(AdvancementNode p_301111_)
```

**Parameters:**

- `p_301111_` (`AdvancementNode`)

**Returns:** `public static void`

### IllegalArgumentException

```java
throw new IllegalArgumentException("Can't position children of an invisible root!")
```

**Parameters:**

- `root!"` (`"Can't position children of an invisible`)

**Returns:** `throw new`
