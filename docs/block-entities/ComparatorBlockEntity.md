# ComparatorBlockEntity

**Package:** `net.minecraft.world.level.block.entity`
**Type:** class
**Extends:** `BlockEntity`

## Methods

### ComparatorBlockEntity

```java
public ComparatorBlockEntity(BlockPos p_155386_, BlockState p_155387_)
```

**Parameters:**

- `p_155386_` (`BlockPos`)
- `p_155387_` (`BlockState`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### saveAdditional

```java
protected void saveAdditional(CompoundTag p_187493_, HolderLookup.Provider p_323979_)
```

**Parameters:**

- `p_187493_` (`CompoundTag`)
- `p_323979_` (`HolderLookup.Provider`)

### loadAdditional

```java
protected void loadAdditional(CompoundTag p_338778_, HolderLookup.Provider p_338355_)
```

**Parameters:**

- `p_338778_` (`CompoundTag`)
- `p_338355_` (`HolderLookup.Provider`)

### getOutputSignal

```java
public int getOutputSignal()
```

**Returns:** `public int`

### setOutputSignal

```java
public void setOutputSignal(int p_59176_)
```

**Parameters:**

- `p_59176_` (`int`)

**Returns:** `public void`
