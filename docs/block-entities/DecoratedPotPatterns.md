# DecoratedPotPatterns

**Package:** `net.minecraft.world.level.block.entity`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `BLANK` | `ResourceKey<DecoratedPotPattern>` |  |
| `ANGLER` | `ResourceKey<DecoratedPotPattern>` |  |
| `ARCHER` | `ResourceKey<DecoratedPotPattern>` |  |
| `ARMS_UP` | `ResourceKey<DecoratedPotPattern>` |  |
| `BLADE` | `ResourceKey<DecoratedPotPattern>` |  |
| `BREWER` | `ResourceKey<DecoratedPotPattern>` |  |
| `BURN` | `ResourceKey<DecoratedPotPattern>` |  |
| `DANGER` | `ResourceKey<DecoratedPotPattern>` |  |
| `EXPLORER` | `ResourceKey<DecoratedPotPattern>` |  |
| `FLOW` | `ResourceKey<DecoratedPotPattern>` |  |
| `FRIEND` | `ResourceKey<DecoratedPotPattern>` |  |
| `GUSTER` | `ResourceKey<DecoratedPotPattern>` |  |
| `HEART` | `ResourceKey<DecoratedPotPattern>` |  |
| `HEARTBREAK` | `ResourceKey<DecoratedPotPattern>` |  |
| `HOWL` | `ResourceKey<DecoratedPotPattern>` |  |
| `MINER` | `ResourceKey<DecoratedPotPattern>` |  |
| `MOURNER` | `ResourceKey<DecoratedPotPattern>` |  |
| `PLENTY` | `ResourceKey<DecoratedPotPattern>` |  |
| `PRIZE` | `ResourceKey<DecoratedPotPattern>` |  |
| `SCRAPE` | `ResourceKey<DecoratedPotPattern>` |  |
| `SHEAF` | `ResourceKey<DecoratedPotPattern>` |  |
| `SHELTER` | `ResourceKey<DecoratedPotPattern>` |  |
| `SKULL` | `ResourceKey<DecoratedPotPattern>` |  |
| `SNORT` | `ResourceKey<DecoratedPotPattern>` |  |

## Methods

### getPatternFromItem

```java
public static ResourceKey<DecoratedPotPattern> getPatternFromItem(Item p_273094_)
```

**Parameters:**

- `p_273094_` (`Item`)

**Returns:** `ResourceKey<DecoratedPotPattern>`

### create

```java
private static ResourceKey<DecoratedPotPattern> create(String p_272919_)
```

**Parameters:**

- `p_272919_` (`String`)

**Returns:** `private static ResourceKey<DecoratedPotPattern>`

### bootstrap

```java
public static DecoratedPotPattern bootstrap(Registry<DecoratedPotPattern> p_273479_)
```

**Parameters:**

- `p_273479_` (`Registry<DecoratedPotPattern>`)

**Returns:** `public static DecoratedPotPattern`

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
return register()
```

**Returns:** `return`

### register

```java
private static DecoratedPotPattern register(Registry<DecoratedPotPattern> p_347598_, ResourceKey<DecoratedPotPattern> p_347620_, String p_347616_)
```

**Parameters:**

- `p_347598_` (`Registry<DecoratedPotPattern>`)
- `p_347620_` (`ResourceKey<DecoratedPotPattern>`)
- `p_347616_` (`String`)

**Returns:** `private static DecoratedPotPattern`
