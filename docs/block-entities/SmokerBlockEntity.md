# SmokerBlockEntity

**Package:** `net.minecraft.world.level.block.entity`
**Type:** class
**Extends:** `AbstractFurnaceBlockEntity`

## Methods

### SmokerBlockEntity

```java
public SmokerBlockEntity(BlockPos p_155749_, BlockState p_155750_)
```

**Parameters:**

- `p_155749_` (`BlockPos`)
- `p_155750_` (`BlockState`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getDefaultName

```java
protected Component getDefaultName()
```

**Returns:** `Component`

### getBurnDuration

```java
protected int getBurnDuration(ItemStack p_59786_)
```

**Parameters:**

- `p_59786_` (`ItemStack`)

**Returns:** `int`

### createMenu

```java
protected AbstractContainerMenu createMenu(int p_59783_, Inventory p_59784_)
```

**Parameters:**

- `p_59783_` (`int`)
- `p_59784_` (`Inventory`)

**Returns:** `AbstractContainerMenu`

### SmokerMenu

```java
return new SmokerMenu()
```

**Returns:** `return new`
