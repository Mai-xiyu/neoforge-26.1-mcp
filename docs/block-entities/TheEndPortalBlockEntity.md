# TheEndPortalBlockEntity

**Package:** `net.minecraft.world.level.block.entity`
**Type:** class
**Extends:** `BlockEntity`

## Methods

### TheEndPortalBlockEntity

```java
protected TheEndPortalBlockEntity(BlockEntityType<?> p_155855_, BlockPos p_155856_, BlockState p_155857_)
```

**Parameters:**

- `p_155855_` (`BlockEntityType<?>`)
- `p_155856_` (`BlockPos`)
- `p_155857_` (`BlockState`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### TheEndPortalBlockEntity

```java
public TheEndPortalBlockEntity(BlockPos p_155859_, BlockState p_155860_)
```

**Parameters:**

- `p_155859_` (`BlockPos`)
- `p_155860_` (`BlockState`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### shouldRenderFace

```java
public boolean shouldRenderFace(Direction p_59980_)
```

**Parameters:**

- `p_59980_` (`Direction`)

**Returns:** `public boolean`
