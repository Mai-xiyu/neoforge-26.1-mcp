# TrappedChestBlockEntity

**Package:** `net.minecraft.world.level.block.entity`
**Type:** class
**Extends:** `ChestBlockEntity`

## Methods

### TrappedChestBlockEntity

```java
public TrappedChestBlockEntity(BlockPos p_155862_, BlockState p_155863_)
```

**Parameters:**

- `p_155862_` (`BlockPos`)
- `p_155863_` (`BlockState`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### signalOpenCount

```java
protected void signalOpenCount(Level p_155865_, BlockPos p_155866_, BlockState p_155867_, int p_155868_, int p_155869_)
```

**Parameters:**

- `p_155865_` (`Level`)
- `p_155866_` (`BlockPos`)
- `p_155867_` (`BlockState`)
- `p_155868_` (`int`)
- `p_155869_` (`int`)
