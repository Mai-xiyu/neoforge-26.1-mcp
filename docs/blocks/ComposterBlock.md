# ComposterBlock

**Package:** `net.minecraft.world.level.block`
**Type:** class
**Extends:** `Block`
**Implements:** `WorldlyContainerHolder`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<ComposterBlock>` |  |
| `READY` | `int` |  |
| `MIN_LEVEL` | `int` |  |
| `MAX_LEVEL` | `int` |  |
| `LEVEL` | `IntegerProperty` |  |

## Methods

### codec

```java
public MapCodec<ComposterBlock> codec()
```

**Returns:** `MapCodec<ComposterBlock>`

### bootStrap

```java
public static void bootStrap()
```

**Returns:** `public static void`

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
 add()
```

**Returns:** ``

### add

```java
private static void add(float p_51921_, ItemLike p_51922_)
```

**Parameters:**

- `p_51921_` (`float`)
- `p_51922_` (`ItemLike`)

**Returns:** `private static void`

### ComposterBlock

```java
public ComposterBlock(BlockBehaviour.Properties p_51919_)
```

**Parameters:**

- `p_51919_` (`BlockBehaviour.Properties`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### handleFill

```java
public static void handleFill(Level p_51924_, BlockPos p_51925_, boolean p_51926_)
```

**Parameters:**

- `p_51924_` (`Level`)
- `p_51925_` (`BlockPos`)
- `p_51926_` (`boolean`)

**Returns:** `public static void`

### getShape

```java
protected VoxelShape getShape(BlockState p_51973_, BlockGetter p_51974_, BlockPos p_51975_, CollisionContext p_51976_)
```

**Parameters:**

- `p_51973_` (`BlockState`)
- `p_51974_` (`BlockGetter`)
- `p_51975_` (`BlockPos`)
- `p_51976_` (`CollisionContext`)

**Returns:** `VoxelShape`

### getInteractionShape

```java
protected VoxelShape getInteractionShape(BlockState p_51969_, BlockGetter p_51970_, BlockPos p_51971_)
```

**Parameters:**

- `p_51969_` (`BlockState`)
- `p_51970_` (`BlockGetter`)
- `p_51971_` (`BlockPos`)

**Returns:** `VoxelShape`

### getCollisionShape

```java
protected VoxelShape getCollisionShape(BlockState p_51990_, BlockGetter p_51991_, BlockPos p_51992_, CollisionContext p_51993_)
```

**Parameters:**

- `p_51990_` (`BlockState`)
- `p_51991_` (`BlockGetter`)
- `p_51992_` (`BlockPos`)
- `p_51993_` (`CollisionContext`)

**Returns:** `VoxelShape`

### onPlace

```java
protected void onPlace(BlockState p_51978_, Level p_51979_, BlockPos p_51980_, BlockState p_51981_, boolean p_51982_)
```

**Parameters:**

- `p_51978_` (`BlockState`)
- `p_51979_` (`Level`)
- `p_51980_` (`BlockPos`)
- `p_51981_` (`BlockState`)
- `p_51982_` (`boolean`)

### onRemove

```java
protected void onRemove(BlockState p_60515_, Level p_60516_, BlockPos p_60517_, BlockState p_60518_, boolean p_60519_)
```

**Parameters:**

- `p_60515_` (`BlockState`)
- `p_60516_` (`Level`)
- `p_60517_` (`BlockPos`)
- `p_60518_` (`BlockState`)
- `p_60519_` (`boolean`)

### useItemOn

```java
protected ItemInteractionResult useItemOn(ItemStack p_316332_, BlockState p_316118_, Level p_316624_, BlockPos p_316660_, Player p_316715_, InteractionHand p_316472_, BlockHitResult p_316606_)
```

**Parameters:**

- `p_316332_` (`ItemStack`)
- `p_316118_` (`BlockState`)
- `p_316624_` (`Level`)
- `p_316660_` (`BlockPos`)
- `p_316715_` (`Player`)
- `p_316472_` (`InteractionHand`)
- `p_316606_` (`BlockHitResult`)

**Returns:** `ItemInteractionResult`

### useWithoutItem

```java
protected InteractionResult useWithoutItem(BlockState p_316361_, Level p_316271_, BlockPos p_316647_, Player p_316633_, BlockHitResult p_316555_)
```

**Parameters:**

- `p_316361_` (`BlockState`)
- `p_316271_` (`Level`)
- `p_316647_` (`BlockPos`)
- `p_316633_` (`Player`)
- `p_316555_` (`BlockHitResult`)

**Returns:** `InteractionResult`

### extractProduce

```java
 extractProduce()
```

**Returns:** ``

### insertItem

```java
public static BlockState insertItem(Entity p_270919_, BlockState p_270087_, ServerLevel p_270284_, ItemStack p_270253_, BlockPos p_270678_)
```

**Parameters:**

- `p_270919_` (`Entity`)
- `p_270087_` (`BlockState`)
- `p_270284_` (`ServerLevel`)
- `p_270253_` (`ItemStack`)
- `p_270678_` (`BlockPos`)

**Returns:** `public static BlockState`

### extractProduce

```java
public static BlockState extractProduce(Entity p_270467_, BlockState p_51999_, Level p_52000_, BlockPos p_52001_)
```

**Parameters:**

- `p_270467_` (`Entity`)
- `p_51999_` (`BlockState`)
- `p_52000_` (`Level`)
- `p_52001_` (`BlockPos`)

**Returns:** `public static BlockState`

### empty

```java
static BlockState empty(Entity p_270236_, BlockState p_270873_, LevelAccessor p_270963_, BlockPos p_270211_)
```

**Parameters:**

- `p_270236_` (`Entity`)
- `p_270873_` (`BlockState`)
- `p_270963_` (`LevelAccessor`)
- `p_270211_` (`BlockPos`)

**Returns:** `static BlockState`

### addItem

```java
static BlockState addItem(Entity p_270464_, BlockState p_270603_, LevelAccessor p_270151_, BlockPos p_270547_, ItemStack p_270354_)
```

**Parameters:**

- `p_270464_` (`Entity`)
- `p_270603_` (`BlockState`)
- `p_270151_` (`LevelAccessor`)
- `p_270547_` (`BlockPos`)
- `p_270354_` (`ItemStack`)

**Returns:** `static BlockState`

### tick

```java
protected void tick(BlockState p_221015_, ServerLevel p_221016_, BlockPos p_221017_, RandomSource p_221018_)
```

**Parameters:**

- `p_221015_` (`BlockState`)
- `p_221016_` (`ServerLevel`)
- `p_221017_` (`BlockPos`)
- `p_221018_` (`RandomSource`)

### hasAnalogOutputSignal

```java
protected boolean hasAnalogOutputSignal(BlockState p_51928_)
```

**Parameters:**

- `p_51928_` (`BlockState`)

**Returns:** `boolean`

### getAnalogOutputSignal

```java
protected int getAnalogOutputSignal(BlockState p_51945_, Level p_51946_, BlockPos p_51947_)
```

**Parameters:**

- `p_51945_` (`BlockState`)
- `p_51946_` (`Level`)
- `p_51947_` (`BlockPos`)

**Returns:** `int`

### createBlockStateDefinition

```java
protected void createBlockStateDefinition(StateDefinition.Builder<Block, BlockState> p_51965_)
```

**Parameters:**

- `p_51965_` (`StateDefinition.Builder<Block, BlockState>`)

### isPathfindable

```java
protected boolean isPathfindable(BlockState p_51940_, PathComputationType p_51943_)
```

**Parameters:**

- `p_51940_` (`BlockState`)
- `p_51943_` (`PathComputationType`)

**Returns:** `boolean`

### getContainer

```java
public WorldlyContainer getContainer(BlockState p_51956_, LevelAccessor p_51957_, BlockPos p_51958_)
```

**Parameters:**

- `p_51956_` (`BlockState`)
- `p_51957_` (`LevelAccessor`)
- `p_51958_` (`BlockPos`)

**Returns:** `WorldlyContainer`

### EmptyContainer

```java
public EmptyContainer()
```

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getSlotsForFace

```java
public int[] getSlotsForFace(Direction p_52012_)
```

**Parameters:**

- `p_52012_` (`Direction`)

**Returns:** `int[]`

### canPlaceItemThroughFace

```java
public boolean canPlaceItemThroughFace(int p_52008_, ItemStack p_52009_, Direction p_52010_)
```

**Parameters:**

- `p_52008_` (`int`)
- `p_52009_` (`ItemStack`)
- `p_52010_` (`Direction`)

**Returns:** `boolean`

### canTakeItemThroughFace

```java
public boolean canTakeItemThroughFace(int p_52014_, ItemStack p_52015_, Direction p_52016_)
```

**Parameters:**

- `p_52014_` (`int`)
- `p_52015_` (`ItemStack`)
- `p_52016_` (`Direction`)

**Returns:** `boolean`

### InputContainer

```java
public InputContainer(BlockState p_52022_, LevelAccessor p_52023_, BlockPos p_52024_)
```

**Parameters:**

- `p_52022_` (`BlockState`)
- `p_52023_` (`LevelAccessor`)
- `p_52024_` (`BlockPos`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getMaxStackSize

```java
public int getMaxStackSize()
```

**Returns:** `int`

### getSlotsForFace

```java
public int[] getSlotsForFace(Direction p_52032_)
```

**Parameters:**

- `p_52032_` (`Direction`)

**Returns:** `int[]`

### canPlaceItemThroughFace

```java
public boolean canPlaceItemThroughFace(int p_52028_, ItemStack p_52029_, Direction p_52030_)
```

**Parameters:**

- `p_52028_` (`int`)
- `p_52029_` (`ItemStack`)
- `p_52030_` (`Direction`)

**Returns:** `boolean`

### canTakeItemThroughFace

```java
public boolean canTakeItemThroughFace(int p_52034_, ItemStack p_52035_, Direction p_52036_)
```

**Parameters:**

- `p_52034_` (`int`)
- `p_52035_` (`ItemStack`)
- `p_52036_` (`Direction`)

**Returns:** `boolean`

### setChanged

```java
public void setChanged()
```

### OutputContainer

```java
public OutputContainer(BlockState p_52042_, LevelAccessor p_52043_, BlockPos p_52044_, ItemStack p_52045_)
```

**Parameters:**

- `p_52042_` (`BlockState`)
- `p_52043_` (`LevelAccessor`)
- `p_52044_` (`BlockPos`)
- `p_52045_` (`ItemStack`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getMaxStackSize

```java
public int getMaxStackSize()
```

**Returns:** `int`

### getSlotsForFace

```java
public int[] getSlotsForFace(Direction p_52053_)
```

**Parameters:**

- `p_52053_` (`Direction`)

**Returns:** `int[]`

### canPlaceItemThroughFace

```java
public boolean canPlaceItemThroughFace(int p_52049_, ItemStack p_52050_, Direction p_52051_)
```

**Parameters:**

- `p_52049_` (`int`)
- `p_52050_` (`ItemStack`)
- `p_52051_` (`Direction`)

**Returns:** `boolean`

### canTakeItemThroughFace

```java
public boolean canTakeItemThroughFace(int p_52055_, ItemStack p_52056_, Direction p_52057_)
```

**Parameters:**

- `p_52055_` (`int`)
- `p_52056_` (`ItemStack`)
- `p_52057_` (`Direction`)

**Returns:** `boolean`

### setChanged

```java
public void setChanged()
```

### getValue

```java
public static float getValue(ItemStack item)
```

**Parameters:**

- `item` (`ItemStack`)

**Returns:** `public static float`
