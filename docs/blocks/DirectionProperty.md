# DirectionProperty

**Package:** `net.minecraft.world.level.block.state.properties`
**Type:** class
**Extends:** `EnumProperty<Direction>`

## Methods

### DirectionProperty

```java
protected DirectionProperty(String p_61541_, Collection<Direction> p_61542_)
```

**Parameters:**

- `p_61541_` (`String`)
- `p_61542_` (`Collection<Direction>`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### create

```java
public static DirectionProperty create(String p_156004_)
```

**Parameters:**

- `p_156004_` (`String`)

**Returns:** `public static DirectionProperty`

### create

```java
return create(p_187558_ -> true)
```

**Parameters:**

- `true` (`p_187558_ ->`)

**Returns:** `return`

### create

```java
public static DirectionProperty create(String p_61547_, Predicate<Direction> p_61548_)
```

**Parameters:**

- `p_61547_` (`String`)
- `p_61548_` (`Predicate<Direction>`)

**Returns:** `public static DirectionProperty`

### create

```java
public static DirectionProperty create(String p_61550_, Direction[]... p_61551_)
```

**Parameters:**

- `p_61550_` (`String`)
- `p_61551_` (`Direction[]...`)

**Returns:** `public static DirectionProperty`

### create

```java
public static DirectionProperty create(String p_61544_, Collection<Direction> p_61545_)
```

**Parameters:**

- `p_61544_` (`String`)
- `p_61545_` (`Collection<Direction>`)

**Returns:** `public static DirectionProperty`

### DirectionProperty

```java
return new DirectionProperty()
```

**Returns:** `return new`
