# EndRodBlock

**Package:** `net.minecraft.world.level.block`
**Type:** class
**Extends:** `RodBlock`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<EndRodBlock>` |  |

## Methods

### codec

```java
public MapCodec<EndRodBlock> codec()
```

**Returns:** `MapCodec<EndRodBlock>`

### EndRodBlock

```java
public EndRodBlock(BlockBehaviour.Properties p_53085_)
```

**Parameters:**

- `p_53085_` (`BlockBehaviour.Properties`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getStateForPlacement

```java
public BlockState getStateForPlacement(BlockPlaceContext p_53087_)
```

**Parameters:**

- `p_53087_` (`BlockPlaceContext`)

**Returns:** `BlockState`

### animateTick

```java
public void animateTick(BlockState p_221107_, Level p_221108_, BlockPos p_221109_, RandomSource p_221110_)
```

**Parameters:**

- `p_221107_` (`BlockState`)
- `p_221108_` (`Level`)
- `p_221109_` (`BlockPos`)
- `p_221110_` (`RandomSource`)

### createBlockStateDefinition

```java
protected void createBlockStateDefinition(StateDefinition.Builder<Block, BlockState> p_53105_)
```

**Parameters:**

- `p_53105_` (`StateDefinition.Builder<Block, BlockState>`)
