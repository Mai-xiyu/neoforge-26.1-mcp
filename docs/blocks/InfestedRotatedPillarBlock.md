# InfestedRotatedPillarBlock

**Package:** `net.minecraft.world.level.block`
**Type:** class
**Extends:** `InfestedBlock`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<InfestedRotatedPillarBlock>` |  |

## Methods

### codec

```java
public MapCodec<InfestedRotatedPillarBlock> codec()
```

**Returns:** `MapCodec<InfestedRotatedPillarBlock>`

### InfestedRotatedPillarBlock

```java
public InfestedRotatedPillarBlock(Block p_153438_, BlockBehaviour.Properties p_153439_)
```

**Parameters:**

- `p_153438_` (`Block`)
- `p_153439_` (`BlockBehaviour.Properties`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### rotate

```java
protected BlockState rotate(BlockState p_153443_, Rotation p_153444_)
```

**Parameters:**

- `p_153443_` (`BlockState`)
- `p_153444_` (`Rotation`)

**Returns:** `BlockState`

### createBlockStateDefinition

```java
protected void createBlockStateDefinition(StateDefinition.Builder<Block, BlockState> p_153446_)
```

**Parameters:**

- `p_153446_` (`StateDefinition.Builder<Block, BlockState>`)

### getStateForPlacement

```java
public BlockState getStateForPlacement(BlockPlaceContext p_153441_)
```

**Parameters:**

- `p_153441_` (`BlockPlaceContext`)

**Returns:** `BlockState`
