# LavaCauldronBlock

**Package:** `net.minecraft.world.level.block`
**Type:** class
**Extends:** `AbstractCauldronBlock`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<LavaCauldronBlock>` |  |

## Methods

### codec

```java
public MapCodec<LavaCauldronBlock> codec()
```

**Returns:** `MapCodec<LavaCauldronBlock>`

### LavaCauldronBlock

```java
public LavaCauldronBlock(BlockBehaviour.Properties p_153498_)
```

**Parameters:**

- `p_153498_` (`BlockBehaviour.Properties`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getContentHeight

```java
protected double getContentHeight(BlockState p_153500_)
```

**Parameters:**

- `p_153500_` (`BlockState`)

**Returns:** `double`

### isFull

```java
public boolean isFull(BlockState p_153511_)
```

**Parameters:**

- `p_153511_` (`BlockState`)

**Returns:** `boolean`

### entityInside

```java
protected void entityInside(BlockState p_153506_, Level p_153507_, BlockPos p_153508_, Entity p_153509_)
```

**Parameters:**

- `p_153506_` (`BlockState`)
- `p_153507_` (`Level`)
- `p_153508_` (`BlockPos`)
- `p_153509_` (`Entity`)

### getAnalogOutputSignal

```java
protected int getAnalogOutputSignal(BlockState p_153502_, Level p_153503_, BlockPos p_153504_)
```

**Parameters:**

- `p_153502_` (`BlockState`)
- `p_153503_` (`Level`)
- `p_153504_` (`BlockPos`)

**Returns:** `int`
