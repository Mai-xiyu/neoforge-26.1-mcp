# PistonMath

**Package:** `net.minecraft.world.level.block.piston`
**Type:** class

## Methods

### getMovementArea

```java
public static AABB getMovementArea(AABB p_60329_, Direction p_60330_, double p_60331_)
```

**Parameters:**

- `p_60329_` (`AABB`)
- `p_60330_` (`Direction`)
- `p_60331_` (`double`)

**Returns:** `public static AABB`

### AABB

```java
return new AABB(p_60329_.minX + d1, p_60329_.minX + d2)
```

**Parameters:**

- `d1` (`p_60329_.minX +`)
- `d2` (`p_60329_.minX +`)

**Returns:** `return new`

### AABB

```java
return new AABB(p_60329_.maxX + d1, p_60329_.maxX + d2)
```

**Parameters:**

- `d1` (`p_60329_.maxX +`)
- `d2` (`p_60329_.maxX +`)

**Returns:** `return new`

### AABB

```java
return new AABB(p_60329_.minY + d1, p_60329_.minY + d2)
```

**Parameters:**

- `d1` (`p_60329_.minY +`)
- `d2` (`p_60329_.minY +`)

**Returns:** `return new`

### AABB

```java
return new AABB(p_60329_.maxY + d1, p_60329_.maxY + d2)
```

**Parameters:**

- `d1` (`p_60329_.maxY +`)
- `d2` (`p_60329_.maxY +`)

**Returns:** `return new`

### AABB

```java
return new AABB(p_60329_.minZ + d1, p_60329_.minZ + d2)
```

**Parameters:**

- `d1` (`p_60329_.minZ +`)
- `d2` (`p_60329_.minZ +`)

**Returns:** `return new`

### AABB

```java
return new AABB(p_60329_.maxZ + d1, p_60329_.maxZ + d2)
```

**Parameters:**

- `d1` (`p_60329_.maxZ +`)
- `d2` (`p_60329_.maxZ +`)

**Returns:** `return new`
