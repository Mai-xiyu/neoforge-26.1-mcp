# PoweredBlock

**Package:** `net.minecraft.world.level.block`
**Type:** class
**Extends:** `Block`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<PoweredBlock>` |  |

## Methods

### codec

```java
public MapCodec<PoweredBlock> codec()
```

**Returns:** `MapCodec<PoweredBlock>`

### PoweredBlock

```java
public PoweredBlock(BlockBehaviour.Properties p_55206_)
```

**Parameters:**

- `p_55206_` (`BlockBehaviour.Properties`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### isSignalSource

```java
protected boolean isSignalSource(BlockState p_55213_)
```

**Parameters:**

- `p_55213_` (`BlockState`)

**Returns:** `boolean`

### getSignal

```java
protected int getSignal(BlockState p_55208_, BlockGetter p_55209_, BlockPos p_55210_, Direction p_55211_)
```

**Parameters:**

- `p_55208_` (`BlockState`)
- `p_55209_` (`BlockGetter`)
- `p_55210_` (`BlockPos`)
- `p_55211_` (`Direction`)

**Returns:** `int`
