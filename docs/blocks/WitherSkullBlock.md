# WitherSkullBlock

**Package:** `net.minecraft.world.level.block`
**Type:** class
**Extends:** `SkullBlock`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<WitherSkullBlock>` |  |

## Methods

### codec

```java
public MapCodec<WitherSkullBlock> codec()
```

**Returns:** `MapCodec<WitherSkullBlock>`

### WitherSkullBlock

```java
public WitherSkullBlock(BlockBehaviour.Properties p_58254_)
```

**Parameters:**

- `p_58254_` (`BlockBehaviour.Properties`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### setPlacedBy

```java
public void setPlacedBy(Level p_58260_, BlockPos p_58261_, BlockState p_58262_, LivingEntity p_58263_, ItemStack p_58264_)
```

**Parameters:**

- `p_58260_` (`Level`)
- `p_58261_` (`BlockPos`)
- `p_58262_` (`BlockState`)
- `p_58263_` (`LivingEntity`)
- `p_58264_` (`ItemStack`)

### checkSpawn

```java
 checkSpawn()
```

**Returns:** ``

### checkSpawn

```java
public static void checkSpawn(Level p_320011_, BlockPos p_320602_)
```

**Parameters:**

- `p_320011_` (`Level`)
- `p_320602_` (`BlockPos`)

**Returns:** `public static void`

### checkSpawn

```java
 checkSpawn()
```

**Returns:** ``

### checkSpawn

```java
public static void checkSpawn(Level p_58256_, BlockPos p_58257_, SkullBlockEntity p_58258_)
```

**Parameters:**

- `p_58256_` (`Level`)
- `p_58257_` (`BlockPos`)
- `p_58258_` (`SkullBlockEntity`)

**Returns:** `public static void`

### canSpawnMob

```java
public static boolean canSpawnMob(Level p_58268_, BlockPos p_58269_, ItemStack p_58270_)
```

**Parameters:**

- `p_58268_` (`Level`)
- `p_58269_` (`BlockPos`)
- `p_58270_` (`ItemStack`)

**Returns:** `public static boolean`

### getOrCreateWitherFull

```java
private static BlockPattern getOrCreateWitherFull()
```

**Returns:** `private static BlockPattern`

### getOrCreateWitherBase

```java
private static BlockPattern getOrCreateWitherBase()
```

**Returns:** `private static BlockPattern`
