# ICapabilityProvider

**Package:** `net.neoforged.neoforge.capabilities`
**Type:** interface<O, C extends @Nullable Object, T>
**Annotations:** `@FunctionalInterface`

## Methods

### getCapability

```java
T getCapability(O object, C context)
```

**Parameters:**

- `object` (`O`)
- `context` (`C`)

**Returns:** `T`
