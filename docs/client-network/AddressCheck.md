# AddressCheck

**Package:** `net.minecraft.client.multiplayer.resolver`
**Type:** interface
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### isAllowed

```java
boolean isAllowed(ResolvedServerAddress p_171829_)
```

**Parameters:**

- `p_171829_` (`ResolvedServerAddress`)

**Returns:** `boolean`

### isAllowed

```java
boolean isAllowed(ServerAddress p_171830_)
```

**Parameters:**

- `p_171830_` (`ServerAddress`)

**Returns:** `boolean`

### createFromService

```java
static AddressCheck createFromService()
```

**Returns:** `static AddressCheck`

### AddressCheck

```java
return new AddressCheck()
```

**Returns:** `return new`

### isAllowed

```java
public boolean isAllowed(ResolvedServerAddress p_171835_)
```

**Parameters:**

- `p_171835_` (`ResolvedServerAddress`)

**Returns:** `boolean`

### isAllowed

```java
public boolean isAllowed(ServerAddress p_171837_)
```

**Parameters:**

- `p_171837_` (`ServerAddress`)

**Returns:** `boolean`
