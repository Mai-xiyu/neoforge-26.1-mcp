# SimpleSoundInstance

**Package:** `net.minecraft.client.resources.sounds`
**Type:** class
**Extends:** `AbstractSoundInstance`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### SimpleSoundInstance

```java
public SimpleSoundInstance(SoundEvent p_235109_, SoundSource p_235110_, float p_235111_, float p_235112_, RandomSource p_235113_, BlockPos p_235114_)
```

**Parameters:**

- `p_235109_` (`SoundEvent`)
- `p_235110_` (`SoundSource`)
- `p_235111_` (`float`)
- `p_235112_` (`float`)
- `p_235113_` (`RandomSource`)
- `p_235114_` (`BlockPos`)

**Returns:** `public`

### forUI

```java
public static SimpleSoundInstance forUI(SoundEvent p_119753_, float p_119754_)
```

**Parameters:**

- `p_119753_` (`SoundEvent`)
- `p_119754_` (`float`)

**Returns:** `public static SimpleSoundInstance`

### forUI

```java
return forUI()
```

**Returns:** `return`

### forUI

```java
public static SimpleSoundInstance forUI(Holder<SoundEvent> p_263418_, float p_263405_)
```

**Parameters:**

- `p_263418_` (`Holder<SoundEvent>`)
- `p_263405_` (`float`)

**Returns:** `public static SimpleSoundInstance`

### forUI

```java
public static SimpleSoundInstance forUI(SoundEvent p_119756_, float p_119757_, float p_119758_)
```

**Parameters:**

- `p_119756_` (`SoundEvent`)
- `p_119757_` (`float`)
- `p_119758_` (`float`)

**Returns:** `public static SimpleSoundInstance`

### forMusic

```java
public static SimpleSoundInstance forMusic(SoundEvent p_119746_)
```

**Parameters:**

- `p_119746_` (`SoundEvent`)

**Returns:** `public static SimpleSoundInstance`

### forJukeboxSong

```java
public static SimpleSoundInstance forJukeboxSong(SoundEvent p_249575_, Vec3 p_249600_)
```

**Parameters:**

- `p_249575_` (`SoundEvent`)
- `p_249600_` (`Vec3`)

**Returns:** `public static SimpleSoundInstance`

### forLocalAmbience

```java
public static SimpleSoundInstance forLocalAmbience(SoundEvent p_119767_, float p_119768_, float p_119769_)
```

**Parameters:**

- `p_119767_` (`SoundEvent`)
- `p_119768_` (`float`)
- `p_119769_` (`float`)

**Returns:** `public static SimpleSoundInstance`

### forAmbientAddition

```java
public static SimpleSoundInstance forAmbientAddition(SoundEvent p_119760_)
```

**Parameters:**

- `p_119760_` (`SoundEvent`)

**Returns:** `public static SimpleSoundInstance`

### forLocalAmbience

```java
return forLocalAmbience()
```

**Returns:** `return`

### forAmbientMood

```java
public static SimpleSoundInstance forAmbientMood(SoundEvent p_235128_, RandomSource p_235129_, double p_235130_, double p_235131_, double p_235132_)
```

**Parameters:**

- `p_235128_` (`SoundEvent`)
- `p_235129_` (`RandomSource`)
- `p_235130_` (`double`)
- `p_235131_` (`double`)
- `p_235132_` (`double`)

**Returns:** `public static SimpleSoundInstance`

### SimpleSoundInstance

```java
return new SimpleSoundInstance()
```

**Returns:** `return new`

### SimpleSoundInstance

```java
public SimpleSoundInstance(SoundEvent p_235100_, SoundSource p_235101_, float p_235102_, float p_235103_, RandomSource p_235104_, double p_235105_, double p_235106_, double p_235107_)
```

**Parameters:**

- `p_235100_` (`SoundEvent`)
- `p_235101_` (`SoundSource`)
- `p_235102_` (`float`)
- `p_235103_` (`float`)
- `p_235104_` (`RandomSource`)
- `p_235105_` (`double`)
- `p_235106_` (`double`)
- `p_235107_` (`double`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### SimpleSoundInstance

```java
private SimpleSoundInstance(SoundEvent p_235116_, SoundSource p_235117_, float p_235118_, float p_235119_, RandomSource p_235120_, boolean p_235121_, int p_235122_, SoundInstance.Attenuation p_235123_, double p_235124_, double p_235125_, double p_235126_)
```

**Parameters:**

- `p_235116_` (`SoundEvent`)
- `p_235117_` (`SoundSource`)
- `p_235118_` (`float`)
- `p_235119_` (`float`)
- `p_235120_` (`RandomSource`)
- `p_235121_` (`boolean`)
- `p_235122_` (`int`)
- `p_235123_` (`SoundInstance.Attenuation`)
- `p_235124_` (`double`)
- `p_235125_` (`double`)
- `p_235126_` (`double`)

**Returns:** `private`

### SimpleSoundInstance

```java
public SimpleSoundInstance(ResourceLocation p_235087_, SoundSource p_235088_, float p_235089_, float p_235090_, RandomSource p_235091_, boolean p_235092_, int p_235093_, SoundInstance.Attenuation p_235094_, double p_235095_, double p_235096_, double p_235097_, boolean p_235098_)
```

**Parameters:**

- `p_235087_` (`ResourceLocation`)
- `p_235088_` (`SoundSource`)
- `p_235089_` (`float`)
- `p_235090_` (`float`)
- `p_235091_` (`RandomSource`)
- `p_235092_` (`boolean`)
- `p_235093_` (`int`)
- `p_235094_` (`SoundInstance.Attenuation`)
- `p_235095_` (`double`)
- `p_235096_` (`double`)
- `p_235097_` (`double`)
- `p_235098_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``
