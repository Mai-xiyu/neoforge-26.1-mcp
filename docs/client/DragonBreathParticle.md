# DragonBreathParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `TextureSheetParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### DragonBreathParticle

```java
protected DragonBreathParticle(ClientLevel p_106005_, double p_106006_, double p_106007_, double p_106008_, double p_106009_, double p_106010_, double p_106011_, SpriteSet p_106012_)
```

**Parameters:**

- `p_106005_` (`ClientLevel`)
- `p_106006_` (`double`)
- `p_106007_` (`double`)
- `p_106008_` (`double`)
- `p_106009_` (`double`)
- `p_106010_` (`double`)
- `p_106011_` (`double`)
- `p_106012_` (`SpriteSet`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### tick

```java
public void tick()
```

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`

### getQuadSize

```java
public float getQuadSize(float p_106026_)
```

**Parameters:**

- `p_106026_` (`float`)

**Returns:** `float`

### Provider

```java
public Provider(SpriteSet p_106029_)
```

**Parameters:**

- `p_106029_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_106040_, ClientLevel p_106041_, double p_106042_, double p_106043_, double p_106044_, double p_106045_, double p_106046_, double p_106047_)
```

**Parameters:**

- `p_106040_` (`SimpleParticleType`)
- `p_106041_` (`ClientLevel`)
- `p_106042_` (`double`)
- `p_106043_` (`double`)
- `p_106044_` (`double`)
- `p_106045_` (`double`)
- `p_106046_` (`double`)
- `p_106047_` (`double`)

**Returns:** `public Particle`

### DragonBreathParticle

```java
return new DragonBreathParticle()
```

**Returns:** `return new`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Provider` | class |  |
