# DustParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `DustParticleBase<DustParticleOptions>`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### DustParticle

```java
protected DustParticle(ClientLevel p_106415_, double p_106416_, double p_106417_, double p_106418_, double p_106419_, double p_106420_, double p_106421_, DustParticleOptions p_106422_, SpriteSet p_106423_)
```

**Parameters:**

- `p_106415_` (`ClientLevel`)
- `p_106416_` (`double`)
- `p_106417_` (`double`)
- `p_106418_` (`double`)
- `p_106419_` (`double`)
- `p_106420_` (`double`)
- `p_106421_` (`double`)
- `p_106422_` (`DustParticleOptions`)
- `p_106423_` (`SpriteSet`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### Provider

```java
public Provider(SpriteSet p_106441_)
```

**Parameters:**

- `p_106441_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(DustParticleOptions p_106443_, ClientLevel p_106444_, double p_106445_, double p_106446_, double p_106447_, double p_106448_, double p_106449_, double p_106450_)
```

**Parameters:**

- `p_106443_` (`DustParticleOptions`)
- `p_106444_` (`ClientLevel`)
- `p_106445_` (`double`)
- `p_106446_` (`double`)
- `p_106447_` (`double`)
- `p_106448_` (`double`)
- `p_106449_` (`double`)
- `p_106450_` (`double`)

**Returns:** `public Particle`

### DustParticle

```java
return new DustParticle()
```

**Returns:** `return new`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Provider` | class |  |
