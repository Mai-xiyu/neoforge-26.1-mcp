# DustParticleBase

**Package:** `net.minecraft.client.particle`
**Type:** class<T extends ScalableParticleOptionsBase>
**Extends:** `TextureSheetParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### DustParticleBase

```java
protected DustParticleBase(ClientLevel p_172094_, double p_172095_, double p_172096_, double p_172097_, double p_172098_, double p_172099_, double p_172100_, T p_341669_, SpriteSet p_172102_)
```

**Parameters:**

- `p_172094_` (`ClientLevel`)
- `p_172095_` (`double`)
- `p_172096_` (`double`)
- `p_172097_` (`double`)
- `p_172098_` (`double`)
- `p_172099_` (`double`)
- `p_172100_` (`double`)
- `p_341669_` (`T`)
- `p_172102_` (`SpriteSet`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### randomizeColor

```java
protected float randomizeColor(float p_172105_, float p_172106_)
```

**Parameters:**

- `p_172105_` (`float`)
- `p_172106_` (`float`)

**Returns:** `protected float`

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`

### getQuadSize

```java
public float getQuadSize(float p_172109_)
```

**Parameters:**

- `p_172109_` (`float`)

**Returns:** `float`

### tick

```java
public void tick()
```
