# EndRodParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `SimpleAnimatedParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### EndRodParticle

```java
protected EndRodParticle(ClientLevel p_106531_, double p_106532_, double p_106533_, double p_106534_, double p_106535_, double p_106536_, double p_106537_, SpriteSet p_106538_)
```

**Parameters:**

- `p_106531_` (`ClientLevel`)
- `p_106532_` (`double`)
- `p_106533_` (`double`)
- `p_106534_` (`double`)
- `p_106535_` (`double`)
- `p_106536_` (`double`)
- `p_106537_` (`double`)
- `p_106538_` (`SpriteSet`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### move

```java
public void move(double p_106550_, double p_106551_, double p_106552_)
```

**Parameters:**

- `p_106550_` (`double`)
- `p_106551_` (`double`)
- `p_106552_` (`double`)

### Provider

```java
public Provider(SpriteSet p_106555_)
```

**Parameters:**

- `p_106555_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_106566_, ClientLevel p_106567_, double p_106568_, double p_106569_, double p_106570_, double p_106571_, double p_106572_, double p_106573_)
```

**Parameters:**

- `p_106566_` (`SimpleParticleType`)
- `p_106567_` (`ClientLevel`)
- `p_106568_` (`double`)
- `p_106569_` (`double`)
- `p_106570_` (`double`)
- `p_106571_` (`double`)
- `p_106572_` (`double`)
- `p_106573_` (`double`)

**Returns:** `public Particle`

### EndRodParticle

```java
return new EndRodParticle()
```

**Returns:** `return new`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Provider` | class |  |
