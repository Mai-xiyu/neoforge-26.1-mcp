# FallingDustParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `TextureSheetParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### FallingDustParticle

```java
protected FallingDustParticle(ClientLevel p_106610_, double p_106611_, double p_106612_, double p_106613_, float p_106614_, float p_106615_, float p_106616_, SpriteSet p_106617_)
```

**Parameters:**

- `p_106610_` (`ClientLevel`)
- `p_106611_` (`double`)
- `p_106612_` (`double`)
- `p_106613_` (`double`)
- `p_106614_` (`float`)
- `p_106615_` (`float`)
- `p_106616_` (`float`)
- `p_106617_` (`SpriteSet`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`

### getQuadSize

```java
public float getQuadSize(float p_106631_)
```

**Parameters:**

- `p_106631_` (`float`)

**Returns:** `float`

### tick

```java
public void tick()
```

### Provider

```java
public Provider(SpriteSet p_106634_)
```

**Parameters:**

- `p_106634_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(BlockParticleOption p_106636_, ClientLevel p_106637_, double p_106638_, double p_106639_, double p_106640_, double p_106641_, double p_106642_, double p_106643_)
```

**Parameters:**

- `p_106636_` (`BlockParticleOption`)
- `p_106637_` (`ClientLevel`)
- `p_106638_` (`double`)
- `p_106639_` (`double`)
- `p_106640_` (`double`)
- `p_106641_` (`double`)
- `p_106642_` (`double`)
- `p_106643_` (`double`)

**Returns:** `Particle`

### FallingDustParticle

```java
return new FallingDustParticle()
```

**Returns:** `return new`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Provider` | class |  |
