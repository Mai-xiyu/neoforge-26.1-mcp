# FlyStraightTowardsParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `TextureSheetParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### FlyStraightTowardsParticle

```java
protected FlyStraightTowardsParticle(ClientLevel p_338359_, double p_338512_, double p_338787_, double p_338665_, double p_338833_, double p_338537_, double p_338840_, int p_338764_, int p_338316_)
```

**Parameters:**

- `p_338359_` (`ClientLevel`)
- `p_338512_` (`double`)
- `p_338787_` (`double`)
- `p_338665_` (`double`)
- `p_338833_` (`double`)
- `p_338537_` (`double`)
- `p_338840_` (`double`)
- `p_338764_` (`int`)
- `p_338316_` (`int`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`

### move

```java
public void move(double p_338805_, double p_338843_, double p_338720_)
```

**Parameters:**

- `p_338805_` (`double`)
- `p_338843_` (`double`)
- `p_338720_` (`double`)

### getLightColor

```java
public int getLightColor(float p_338732_)
```

**Parameters:**

- `p_338732_` (`float`)

**Returns:** `int`

### tick

```java
public void tick()
```

### OminousSpawnProvider

```java
public OminousSpawnProvider(SpriteSet p_338292_)
```

**Parameters:**

- `p_338292_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_338365_, ClientLevel p_338448_, double p_338829_, double p_338561_, double p_338765_, double p_338694_, double p_338802_, double p_338768_)
```

**Parameters:**

- `p_338365_` (`SimpleParticleType`)
- `p_338448_` (`ClientLevel`)
- `p_338829_` (`double`)
- `p_338561_` (`double`)
- `p_338765_` (`double`)
- `p_338694_` (`double`)
- `p_338802_` (`double`)
- `p_338768_` (`double`)

**Returns:** `public Particle`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `OminousSpawnProvider` | class |  |
