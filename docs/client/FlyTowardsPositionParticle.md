# FlyTowardsPositionParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `TextureSheetParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### FlyTowardsPositionParticle

```java
protected FlyTowardsPositionParticle(ClientLevel p_323658_, double p_324523_, double p_324079_, double p_324377_, double p_324144_, double p_324286_, double p_323511_)
```

**Parameters:**

- `p_323658_` (`ClientLevel`)
- `p_324523_` (`double`)
- `p_324079_` (`double`)
- `p_324377_` (`double`)
- `p_324144_` (`double`)
- `p_324286_` (`double`)
- `p_323511_` (`double`)

**Returns:** `protected`

### this

```java
 this()
```

**Returns:** ``

### FlyTowardsPositionParticle

```java
 FlyTowardsPositionParticle(ClientLevel p_323938_, double p_323720_, double p_324407_, double p_324020_, double p_323737_, double p_323883_, double p_324615_, boolean p_323911_, Particle.LifetimeAlpha p_324427_)
```

**Parameters:**

- `p_323938_` (`ClientLevel`)
- `p_323720_` (`double`)
- `p_324407_` (`double`)
- `p_324020_` (`double`)
- `p_323737_` (`double`)
- `p_323883_` (`double`)
- `p_324615_` (`double`)
- `p_323911_` (`boolean`)
- `p_324427_` (`Particle.LifetimeAlpha`)

**Returns:** ``

### super

```java
 super()
```

**Returns:** ``

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`

### move

```java
public void move(double p_324487_, double p_323538_, double p_324364_)
```

**Parameters:**

- `p_324487_` (`double`)
- `p_323538_` (`double`)
- `p_324364_` (`double`)

### getLightColor

```java
public int getLightColor(float p_323664_)
```

**Parameters:**

- `p_323664_` (`float`)

**Returns:** `int`

### tick

```java
public void tick()
```

### render

```java
public void render(VertexConsumer p_324177_, Camera p_323683_, float p_323936_)
```

**Parameters:**

- `p_324177_` (`VertexConsumer`)
- `p_323683_` (`Camera`)
- `p_323936_` (`float`)

### EnchantProvider

```java
public EnchantProvider(SpriteSet p_324184_)
```

**Parameters:**

- `p_324184_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_323913_, ClientLevel p_323933_, double p_324281_, double p_323543_, double p_324051_, double p_323907_, double p_324082_, double p_323993_)
```

**Parameters:**

- `p_323913_` (`SimpleParticleType`)
- `p_323933_` (`ClientLevel`)
- `p_324281_` (`double`)
- `p_323543_` (`double`)
- `p_324051_` (`double`)
- `p_323907_` (`double`)
- `p_324082_` (`double`)
- `p_323993_` (`double`)

**Returns:** `public Particle`

### NautilusProvider

```java
public NautilusProvider(SpriteSet p_323905_)
```

**Parameters:**

- `p_323905_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_323965_, ClientLevel p_324479_, double p_323514_, double p_323507_, double p_324033_, double p_323555_, double p_323611_, double p_323980_)
```

**Parameters:**

- `p_323965_` (`SimpleParticleType`)
- `p_324479_` (`ClientLevel`)
- `p_323514_` (`double`)
- `p_323507_` (`double`)
- `p_324033_` (`double`)
- `p_323555_` (`double`)
- `p_323611_` (`double`)
- `p_323980_` (`double`)

**Returns:** `public Particle`

### VaultConnectionProvider

```java
public VaultConnectionProvider(SpriteSet p_324409_)
```

**Parameters:**

- `p_324409_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_324345_, ClientLevel p_324607_, double p_324458_, double p_324613_, double p_324276_, double p_323805_, double p_324556_, double p_323945_)
```

**Parameters:**

- `p_324345_` (`SimpleParticleType`)
- `p_324607_` (`ClientLevel`)
- `p_324458_` (`double`)
- `p_324613_` (`double`)
- `p_324276_` (`double`)
- `p_323805_` (`double`)
- `p_324556_` (`double`)
- `p_323945_` (`double`)

**Returns:** `public Particle`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `EnchantProvider` | class |  |
| `NautilusProvider` | class |  |
| `VaultConnectionProvider` | class |  |
