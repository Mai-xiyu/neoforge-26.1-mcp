# GustParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `TextureSheetParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### GustParticle

```java
protected GustParticle(ClientLevel p_312171_, double p_312015_, double p_312462_, double p_312867_, SpriteSet p_312279_)
```

**Parameters:**

- `p_312171_` (`ClientLevel`)
- `p_312015_` (`double`)
- `p_312462_` (`double`)
- `p_312867_` (`double`)
- `p_312279_` (`SpriteSet`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`

### getLightColor

```java
public int getLightColor(float p_312697_)
```

**Parameters:**

- `p_312697_` (`float`)

**Returns:** `int`

### tick

```java
public void tick()
```

### Provider

```java
public Provider(SpriteSet p_312644_)
```

**Parameters:**

- `p_312644_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_311849_, ClientLevel p_312008_, double p_312498_, double p_312520_, double p_311822_, double p_312595_, double p_312181_, double p_312468_)
```

**Parameters:**

- `p_311849_` (`SimpleParticleType`)
- `p_312008_` (`ClientLevel`)
- `p_312498_` (`double`)
- `p_312520_` (`double`)
- `p_311822_` (`double`)
- `p_312595_` (`double`)
- `p_312181_` (`double`)
- `p_312468_` (`double`)

**Returns:** `public Particle`

### GustParticle

```java
return new GustParticle()
```

**Returns:** `return new`

### SmallProvider

```java
public SmallProvider(SpriteSet p_338457_)
```

**Parameters:**

- `p_338457_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_338464_, ClientLevel p_338628_, double p_338495_, double p_338874_, double p_338214_, double p_338609_, double p_338492_, double p_338605_)
```

**Parameters:**

- `p_338464_` (`SimpleParticleType`)
- `p_338628_` (`ClientLevel`)
- `p_338495_` (`double`)
- `p_338874_` (`double`)
- `p_338214_` (`double`)
- `p_338609_` (`double`)
- `p_338492_` (`double`)
- `p_338605_` (`double`)

**Returns:** `public Particle`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Provider` | class |  |
| `SmallProvider` | class |  |
