# HeartParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `TextureSheetParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### HeartParticle

```java
protected HeartParticle(ClientLevel p_106847_, double p_106848_, double p_106849_, double p_106850_)
```

**Parameters:**

- `p_106847_` (`ClientLevel`)
- `p_106848_` (`double`)
- `p_106849_` (`double`)
- `p_106850_` (`double`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`

### getQuadSize

```java
public float getQuadSize(float p_106860_)
```

**Parameters:**

- `p_106860_` (`float`)

**Returns:** `float`

### AngryVillagerProvider

```java
public AngryVillagerProvider(SpriteSet p_106863_)
```

**Parameters:**

- `p_106863_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_106874_, ClientLevel p_106875_, double p_106876_, double p_106877_, double p_106878_, double p_106879_, double p_106880_, double p_106881_)
```

**Parameters:**

- `p_106874_` (`SimpleParticleType`)
- `p_106875_` (`ClientLevel`)
- `p_106876_` (`double`)
- `p_106877_` (`double`)
- `p_106878_` (`double`)
- `p_106879_` (`double`)
- `p_106880_` (`double`)
- `p_106881_` (`double`)

**Returns:** `public Particle`

### Provider

```java
public Provider(SpriteSet p_106884_)
```

**Parameters:**

- `p_106884_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_106895_, ClientLevel p_106896_, double p_106897_, double p_106898_, double p_106899_, double p_106900_, double p_106901_, double p_106902_)
```

**Parameters:**

- `p_106895_` (`SimpleParticleType`)
- `p_106896_` (`ClientLevel`)
- `p_106897_` (`double`)
- `p_106898_` (`double`)
- `p_106899_` (`double`)
- `p_106900_` (`double`)
- `p_106901_` (`double`)
- `p_106902_` (`double`)

**Returns:** `public Particle`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `AngryVillagerProvider` | class |  |
| `Provider` | class |  |
