# Hotbar

**Package:** `net.minecraft.client.player.inventory`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `Codec<Hotbar>` |  |

## Methods

### Hotbar

```java
private Hotbar(List<Dynamic<?>> p_324385_)
```

**Parameters:**

- `p_324385_` (`List<Dynamic<?>>`)

**Returns:** `private`

### Hotbar

```java
public Hotbar()
```

**Returns:** `public`

### load

```java
public List<ItemStack> load(HolderLookup.Provider p_323741_)
```

**Parameters:**

- `p_323741_` (`HolderLookup.Provider`)

**Returns:** `public List<ItemStack>`

### storeFrom

```java
public void storeFrom(Inventory p_324559_, RegistryAccess p_324514_)
```

**Parameters:**

- `p_324559_` (`Inventory`)
- `p_324514_` (`RegistryAccess`)

**Returns:** `public void`

### isEmpty

```java
public boolean isEmpty()
```

**Returns:** `public boolean`

### isEmpty

```java
private static boolean isEmpty(Dynamic<?> p_324499_)
```

**Parameters:**

- `p_324499_` (`Dynamic<?>`)

**Returns:** `private static boolean`
