# HugeExplosionParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `TextureSheetParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### HugeExplosionParticle

```java
protected HugeExplosionParticle(ClientLevel p_106905_, double p_106906_, double p_106907_, double p_106908_, double p_106909_, SpriteSet p_106910_)
```

**Parameters:**

- `p_106905_` (`ClientLevel`)
- `p_106906_` (`double`)
- `p_106907_` (`double`)
- `p_106908_` (`double`)
- `p_106909_` (`double`)
- `p_106910_` (`SpriteSet`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### getLightColor

```java
public int getLightColor(float p_106921_)
```

**Parameters:**

- `p_106921_` (`float`)

**Returns:** `int`

### tick

```java
public void tick()
```

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`

### Provider

```java
public Provider(SpriteSet p_106925_)
```

**Parameters:**

- `p_106925_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_106936_, ClientLevel p_106937_, double p_106938_, double p_106939_, double p_106940_, double p_106941_, double p_106942_, double p_106943_)
```

**Parameters:**

- `p_106936_` (`SimpleParticleType`)
- `p_106937_` (`ClientLevel`)
- `p_106938_` (`double`)
- `p_106939_` (`double`)
- `p_106940_` (`double`)
- `p_106941_` (`double`)
- `p_106942_` (`double`)
- `p_106943_` (`double`)

**Returns:** `public Particle`

### HugeExplosionParticle

```java
return new HugeExplosionParticle()
```

**Returns:** `return new`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Provider` | class |  |
