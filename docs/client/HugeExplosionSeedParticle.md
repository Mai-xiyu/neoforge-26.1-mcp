# HugeExplosionSeedParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `NoRenderParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### HugeExplosionSeedParticle

```java
protected HugeExplosionSeedParticle(ClientLevel p_106947_, double p_106948_, double p_106949_, double p_106950_)
```

**Parameters:**

- `p_106947_` (`ClientLevel`)
- `p_106948_` (`double`)
- `p_106949_` (`double`)
- `p_106950_` (`double`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### tick

```java
public void tick()
```

### createParticle

```java
public Particle createParticle(SimpleParticleType p_106969_, ClientLevel p_106970_, double p_106971_, double p_106972_, double p_106973_, double p_106974_, double p_106975_, double p_106976_)
```

**Parameters:**

- `p_106969_` (`SimpleParticleType`)
- `p_106970_` (`ClientLevel`)
- `p_106971_` (`double`)
- `p_106972_` (`double`)
- `p_106973_` (`double`)
- `p_106974_` (`double`)
- `p_106975_` (`double`)
- `p_106976_` (`double`)

**Returns:** `public Particle`

### HugeExplosionSeedParticle

```java
return new HugeExplosionSeedParticle()
```

**Returns:** `return new`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Provider` | class |  |
