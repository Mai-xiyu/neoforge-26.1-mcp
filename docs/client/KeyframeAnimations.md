# KeyframeAnimations

**Package:** `net.minecraft.client.animation`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### animate

```java
public static void animate(HierarchicalModel<?> p_232320_, AnimationDefinition p_232321_, long p_232322_, float p_232323_, Vector3f p_253861_)
```

**Parameters:**

- `p_232320_` (`HierarchicalModel<?>`)
- `p_232321_` (`AnimationDefinition`)
- `p_232322_` (`long`)
- `p_232323_` (`float`)
- `p_253861_` (`Vector3f`)

**Returns:** `public static void`

### getElapsedSeconds

```java
private static float getElapsedSeconds(AnimationDefinition p_232317_, long p_232318_)
```

**Parameters:**

- `p_232317_` (`AnimationDefinition`)
- `p_232318_` (`long`)

**Returns:** `private static float`

### posVec

```java
public static Vector3f posVec(float p_253691_, float p_254046_, float p_254461_)
```

**Parameters:**

- `p_253691_` (`float`)
- `p_254046_` (`float`)
- `p_254461_` (`float`)

**Returns:** `public static Vector3f`

### Vector3f

```java
return new Vector3f()
```

**Returns:** `return new`

### degreeVec

```java
public static Vector3f degreeVec(float p_254402_, float p_253917_, float p_254397_)
```

**Parameters:**

- `p_254402_` (`float`)
- `p_253917_` (`float`)
- `p_254397_` (`float`)

**Returns:** `public static Vector3f`

### scaleVec

```java
public static Vector3f scaleVec(double p_253806_, double p_253647_, double p_254396_)
```

**Parameters:**

- `p_253806_` (`double`)
- `p_253647_` (`double`)
- `p_254396_` (`double`)

**Returns:** `public static Vector3f`
