# ModListScreen

**Package:** `net.neoforged.neoforge.client.gui`
**Type:** class
**Extends:** `Screen`
**Side:** 🖥️ Client

## Methods

### stripControlCodes

```java
private static String stripControlCodes(String value)
```

**Parameters:**

- `value` (`String`)

**Returns:** `private static String`

### compare

```java
protected int compare(String name1, String name2)
```

**Parameters:**

- `name1` (`String`)
- `name2` (`String`)

**Returns:** `int`

### compare

```java
protected int compare(String name1, String name2)
```

**Parameters:**

- `name1` (`String`)
- `name2` (`String`)

**Returns:** `int`

### compare

```java
protected int compare(String name1, String name2)
```

**Parameters:**

- `name1` (`String`)
- `name2` (`String`)

**Returns:** `protected int`

### compare

```java
public int compare(ModContainer o1, ModContainer o2)
```

**Parameters:**

- `o1` (`ModContainer`)
- `o2` (`ModContainer`)

**Returns:** `int`

### compare

```java
return compare()
```

**Returns:** `return`

### getButtonText

```java
Component getButtonText()
```

**Returns:** `Component`

### ModListScreen

```java
public ModListScreen(Screen parentScreen)
```

**Parameters:**

- `parentScreen` (`Screen`)

**Returns:** `public`

### InfoPanel

```java
 InfoPanel(Minecraft mcIn, int widthIn, int heightIn, int topIn)
```

**Parameters:**

- `mcIn` (`Minecraft`)
- `widthIn` (`int`)
- `heightIn` (`int`)
- `topIn` (`int`)

**Returns:** ``

### setInfo

```java
void setInfo(List<String> lines, ResourceLocation logoPath, Size2i logoDims)
```

**Parameters:**

- `lines` (`List<String>`)
- `logoPath` (`ResourceLocation`)
- `logoDims` (`Size2i`)

### clearInfo

```java
void clearInfo()
```

### resizeContent

```java
private List<FormattedCharSequence> resizeContent(List<String> lines)
```

**Parameters:**

- `lines` (`List<String>`)

**Returns:** `private List<FormattedCharSequence>`

### getContentHeight

```java
public int getContentHeight()
```

**Returns:** `int`

### getScrollAmount

```java
protected int getScrollAmount()
```

**Returns:** `int`

### drawPanel

```java
protected void drawPanel(GuiGraphics guiGraphics, int entryRight, int relativeY, Tesselator tess, int mouseX, int mouseY)
```

**Parameters:**

- `guiGraphics` (`GuiGraphics`)
- `entryRight` (`int`)
- `relativeY` (`int`)
- `tess` (`Tesselator`)
- `mouseX` (`int`)
- `mouseY` (`int`)

### findTextLine

```java
private Style findTextLine(int mouseX, int mouseY)
```

**Parameters:**

- `mouseX` (`int`)
- `mouseY` (`int`)

**Returns:** `private Style`

### mouseClicked

```java
public boolean mouseClicked(double mouseX, double mouseY, int button)
```

**Parameters:**

- `mouseX` (`double`)
- `mouseY` (`double`)
- `button` (`int`)

**Returns:** `boolean`

### narrationPriority

```java
public NarrationPriority narrationPriority()
```

**Returns:** `NarrationPriority`

### updateNarration

```java
public void updateNarration(NarrationElementOutput p_169152_)
```

**Parameters:**

- `p_169152_` (`NarrationElementOutput`)

### init

```java
public void init()
```

### resortMods

```java
 resortMods()
```

**Returns:** ``

### updateCache

```java
 updateCache()
```

**Returns:** ``

### displayModConfig

```java
private void displayModConfig()
```

**Returns:** `private void`

### tick

```java
public void tick()
```

### reloadMods

```java
 reloadMods()
```

**Returns:** ``

### reloadMods

```java
 reloadMods()
```

**Returns:** ``

### updateCache

```java
 updateCache()
```

**Returns:** ``

### buildModList

```java
public <T extends ObjectSelectionList.Entry<T>> void buildModList(Consumer<T> modListViewConsumer, Function<ModContainer, T> newEntry)
```

**Parameters:**

- `modListViewConsumer` (`Consumer<T>`)
- `newEntry` (`Function<ModContainer, T>`)

**Returns:** `public <T extends ObjectSelectionList.Entry<T>> void`

### reloadMods

```java
private void reloadMods()
```

**Returns:** `private void`

### resortMods

```java
private void resortMods(SortType newSort)
```

**Parameters:**

- `newSort` (`SortType`)

**Returns:** `private void`

### render

```java
public void render(GuiGraphics guiGraphics, int mouseX, int mouseY, float partialTick)
```

**Parameters:**

- `guiGraphics` (`GuiGraphics`)
- `mouseX` (`int`)
- `mouseY` (`int`)
- `partialTick` (`float`)

### getMinecraftInstance

```java
public Minecraft getMinecraftInstance()
```

**Returns:** `public Minecraft`

### getFontRenderer

```java
public Font getFontRenderer()
```

**Returns:** `public Font`

### setSelected

```java
public void setSelected(ModListWidget.ModEntry entry)
```

**Parameters:**

- `entry` (`ModListWidget.ModEntry`)

**Returns:** `public void`

### updateCache

```java
 updateCache()
```

**Returns:** ``

### updateCache

```java
private void updateCache()
```

**Returns:** `private void`

### DynamicTexture

```java
, new DynamicTexture()
```

**Returns:** `, new`

### upload

```java
public void upload()
```

### resize

```java
public void resize(Minecraft mc, int width, int height)
```

**Parameters:**

- `mc` (`Minecraft`)
- `width` (`int`)
- `height` (`int`)

### reloadMods

```java
 reloadMods()
```

**Returns:** ``

### resortMods

```java
 resortMods()
```

**Returns:** ``

### updateCache

```java
 updateCache()
```

**Returns:** ``

### onClose

```java
public void onClose()
```
