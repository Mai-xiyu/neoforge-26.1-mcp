# NoRenderParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `Particle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### NoRenderParticle

```java
protected NoRenderParticle(ClientLevel p_107149_, double p_107150_, double p_107151_, double p_107152_)
```

**Parameters:**

- `p_107149_` (`ClientLevel`)
- `p_107150_` (`double`)
- `p_107151_` (`double`)
- `p_107152_` (`double`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### NoRenderParticle

```java
protected NoRenderParticle(ClientLevel p_107154_, double p_107155_, double p_107156_, double p_107157_, double p_107158_, double p_107159_, double p_107160_)
```

**Parameters:**

- `p_107154_` (`ClientLevel`)
- `p_107155_` (`double`)
- `p_107156_` (`double`)
- `p_107157_` (`double`)
- `p_107158_` (`double`)
- `p_107159_` (`double`)
- `p_107160_` (`double`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### render

```java
public final void render(VertexConsumer p_107162_, Camera p_107163_, float p_107164_)
```

**Parameters:**

- `p_107162_` (`VertexConsumer`)
- `p_107163_` (`Camera`)
- `p_107164_` (`float`)

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`
