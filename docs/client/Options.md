# Options

**Package:** `net.minecraft.client`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `RENDER_DISTANCE_TINY` | `int` |  |
| `RENDER_DISTANCE_SHORT` | `int` |  |
| `RENDER_DISTANCE_NORMAL` | `int` |  |
| `RENDER_DISTANCE_FAR` | `int` |  |
| `RENDER_DISTANCE_REALLY_FAR` | `int` |  |
| `RENDER_DISTANCE_EXTREME` | `int` |  |
| `DEFAULT_SOUND_DEVICE` | `String` |  |
| `UNLIMITED_FRAMERATE_CUTOFF` | `int` |  |

## Methods

### genericValueLabel

```java
> genericValueLabel()
```

**Returns:** `>`

### darkMojangStudiosBackground

```java
public OptionInstance<Boolean> darkMojangStudiosBackground()
```

**Returns:** `public OptionInstance<Boolean>`

### hideLightningFlash

```java
public OptionInstance<Boolean> hideLightningFlash()
```

**Returns:** `public OptionInstance<Boolean>`

### hideSplashTexts

```java
public OptionInstance<Boolean> hideSplashTexts()
```

**Returns:** `public OptionInstance<Boolean>`

### sensitivity

```java
public OptionInstance<Double> sensitivity()
```

**Returns:** `public OptionInstance<Double>`

### renderDistance

```java
public OptionInstance<Integer> renderDistance()
```

**Returns:** `public OptionInstance<Integer>`

### simulationDistance

```java
public OptionInstance<Integer> simulationDistance()
```

**Returns:** `public OptionInstance<Integer>`

### entityDistanceScaling

```java
public OptionInstance<Double> entityDistanceScaling()
```

**Returns:** `public OptionInstance<Double>`

### framerateLimit

```java
public OptionInstance<Integer> framerateLimit()
```

**Returns:** `public OptionInstance<Integer>`

### cloudStatus

```java
public OptionInstance<CloudStatus> cloudStatus()
```

**Returns:** `public OptionInstance<CloudStatus>`

### graphicsMode

```java
public OptionInstance<GraphicsStatus> graphicsMode()
```

**Returns:** `public OptionInstance<GraphicsStatus>`

### ambientOcclusion

```java
public OptionInstance<Boolean> ambientOcclusion()
```

**Returns:** `public OptionInstance<Boolean>`

### prioritizeChunkUpdates

```java
public OptionInstance<PrioritizeChunkUpdates> prioritizeChunkUpdates()
```

**Returns:** `public OptionInstance<PrioritizeChunkUpdates>`

### updateResourcePacks

```java
public void updateResourcePacks(PackRepository p_275268_)
```

**Parameters:**

- `p_275268_` (`PackRepository`)

**Returns:** `public void`

### chatVisibility

```java
public OptionInstance<ChatVisiblity> chatVisibility()
```

**Returns:** `public OptionInstance<ChatVisiblity>`

### chatOpacity

```java
public OptionInstance<Double> chatOpacity()
```

**Returns:** `public OptionInstance<Double>`

### chatLineSpacing

```java
public OptionInstance<Double> chatLineSpacing()
```

**Returns:** `public OptionInstance<Double>`

### menuBackgroundBlurriness

```java
public OptionInstance<Integer> menuBackgroundBlurriness()
```

**Returns:** `public OptionInstance<Integer>`

### getMenuBackgroundBlurriness

```java
public int getMenuBackgroundBlurriness()
```

**Returns:** `public int`

### textBackgroundOpacity

```java
public OptionInstance<Double> textBackgroundOpacity()
```

**Returns:** `public OptionInstance<Double>`

### panoramaSpeed

```java
public OptionInstance<Double> panoramaSpeed()
```

**Returns:** `public OptionInstance<Double>`

### highContrast

```java
public OptionInstance<Boolean> highContrast()
```

**Returns:** `public OptionInstance<Boolean>`

### narratorHotkey

```java
public OptionInstance<Boolean> narratorHotkey()
```

**Returns:** `public OptionInstance<Boolean>`

### mainHand

```java
public OptionInstance<HumanoidArm> mainHand()
```

**Returns:** `public OptionInstance<HumanoidArm>`

### chatScale

```java
public OptionInstance<Double> chatScale()
```

**Returns:** `public OptionInstance<Double>`

### chatWidth

```java
public OptionInstance<Double> chatWidth()
```

**Returns:** `public OptionInstance<Double>`

### chatHeightUnfocused

```java
public OptionInstance<Double> chatHeightUnfocused()
```

**Returns:** `public OptionInstance<Double>`

### chatHeightFocused

```java
public OptionInstance<Double> chatHeightFocused()
```

**Returns:** `public OptionInstance<Double>`

### chatDelay

```java
public OptionInstance<Double> chatDelay()
```

**Returns:** `public OptionInstance<Double>`

### notificationDisplayTime

```java
public OptionInstance<Double> notificationDisplayTime()
```

**Returns:** `public OptionInstance<Double>`

### mipmapLevels

```java
public OptionInstance<Integer> mipmapLevels()
```

**Returns:** `public OptionInstance<Integer>`

### attackIndicator

```java
public OptionInstance<AttackIndicatorStatus> attackIndicator()
```

**Returns:** `public OptionInstance<AttackIndicatorStatus>`

### biomeBlendRadius

```java
public OptionInstance<Integer> biomeBlendRadius()
```

**Returns:** `public OptionInstance<Integer>`

### logMouse

```java
private static double logMouse(int p_231966_)
```

**Parameters:**

- `p_231966_` (`int`)

**Returns:** `private static double`

### unlogMouse

```java
private static int unlogMouse(double p_231840_)
```

**Parameters:**

- `p_231840_` (`double`)

**Returns:** `private static int`

### mouseWheelSensitivity

```java
public OptionInstance<Double> mouseWheelSensitivity()
```

**Returns:** `public OptionInstance<Double>`

### rawMouseInput

```java
public OptionInstance<Boolean> rawMouseInput()
```

**Returns:** `public OptionInstance<Boolean>`

### autoJump

```java
public OptionInstance<Boolean> autoJump()
```

**Returns:** `public OptionInstance<Boolean>`

### operatorItemsTab

```java
public OptionInstance<Boolean> operatorItemsTab()
```

**Returns:** `public OptionInstance<Boolean>`

### autoSuggestions

```java
public OptionInstance<Boolean> autoSuggestions()
```

**Returns:** `public OptionInstance<Boolean>`

### chatColors

```java
public OptionInstance<Boolean> chatColors()
```

**Returns:** `public OptionInstance<Boolean>`

### chatLinks

```java
public OptionInstance<Boolean> chatLinks()
```

**Returns:** `public OptionInstance<Boolean>`

### chatLinksPrompt

```java
public OptionInstance<Boolean> chatLinksPrompt()
```

**Returns:** `public OptionInstance<Boolean>`

### enableVsync

```java
public OptionInstance<Boolean> enableVsync()
```

**Returns:** `public OptionInstance<Boolean>`

### entityShadows

```java
public OptionInstance<Boolean> entityShadows()
```

**Returns:** `public OptionInstance<Boolean>`

### updateFontOptions

```java
private static void updateFontOptions()
```

**Returns:** `private static void`

### forceUnicodeFont

```java
public OptionInstance<Boolean> forceUnicodeFont()
```

**Returns:** `public OptionInstance<Boolean>`

### japaneseGlyphVariantsDefault

```java
private static boolean japaneseGlyphVariantsDefault()
```

**Returns:** `private static boolean`

### japaneseGlyphVariants

```java
public OptionInstance<Boolean> japaneseGlyphVariants()
```

**Returns:** `public OptionInstance<Boolean>`

### invertYMouse

```java
public OptionInstance<Boolean> invertYMouse()
```

**Returns:** `public OptionInstance<Boolean>`

### discreteMouseScroll

```java
public OptionInstance<Boolean> discreteMouseScroll()
```

**Returns:** `public OptionInstance<Boolean>`

### realmsNotifications

```java
public OptionInstance<Boolean> realmsNotifications()
```

**Returns:** `public OptionInstance<Boolean>`

### allowServerListing

```java
public OptionInstance<Boolean> allowServerListing()
```

**Returns:** `public OptionInstance<Boolean>`

### reducedDebugInfo

```java
public OptionInstance<Boolean> reducedDebugInfo()
```

**Returns:** `public OptionInstance<Boolean>`

### getSoundSourceVolume

```java
public final float getSoundSourceVolume(SoundSource p_92148_)
```

**Parameters:**

- `p_92148_` (`SoundSource`)

**Returns:** `public final float`

### getSoundSourceOptionInstance

```java
public final OptionInstance<Double> getSoundSourceOptionInstance(SoundSource p_251574_)
```

**Parameters:**

- `p_251574_` (`SoundSource`)

**Returns:** `public final OptionInstance<Double>`

### createSoundSliderOptionInstance

```java
private OptionInstance<Double> createSoundSliderOptionInstance(String p_250353_, SoundSource p_249262_)
```

**Parameters:**

- `p_250353_` (`String`)
- `p_249262_` (`SoundSource`)

**Returns:** `private OptionInstance<Double>`

### showSubtitles

```java
public OptionInstance<Boolean> showSubtitles()
```

**Returns:** `public OptionInstance<Boolean>`

### directionalAudio

```java
public OptionInstance<Boolean> directionalAudio()
```

**Returns:** `public OptionInstance<Boolean>`

### backgroundForChatOnly

```java
public OptionInstance<Boolean> backgroundForChatOnly()
```

**Returns:** `public OptionInstance<Boolean>`

### touchscreen

```java
public OptionInstance<Boolean> touchscreen()
```

**Returns:** `public OptionInstance<Boolean>`

### fullscreen

```java
public OptionInstance<Boolean> fullscreen()
```

**Returns:** `public OptionInstance<Boolean>`

### bobView

```java
public OptionInstance<Boolean> bobView()
```

**Returns:** `public OptionInstance<Boolean>`

### toggleCrouch

```java
public OptionInstance<Boolean> toggleCrouch()
```

**Returns:** `public OptionInstance<Boolean>`

### toggleSprint

```java
public OptionInstance<Boolean> toggleSprint()
```

**Returns:** `public OptionInstance<Boolean>`

### hideMatchedNames

```java
public OptionInstance<Boolean> hideMatchedNames()
```

**Returns:** `public OptionInstance<Boolean>`

### showAutosaveIndicator

```java
public OptionInstance<Boolean> showAutosaveIndicator()
```

**Returns:** `public OptionInstance<Boolean>`

### onlyShowSecureChat

```java
public OptionInstance<Boolean> onlyShowSecureChat()
```

**Returns:** `public OptionInstance<Boolean>`

### fov

```java
public OptionInstance<Integer> fov()
```

**Returns:** `public OptionInstance<Integer>`

### telemetryOptInExtra

```java
public OptionInstance<Boolean> telemetryOptInExtra()
```

**Returns:** `public OptionInstance<Boolean>`

### screenEffectScale

```java
public OptionInstance<Double> screenEffectScale()
```

**Returns:** `public OptionInstance<Double>`

### fovEffectScale

```java
public OptionInstance<Double> fovEffectScale()
```

**Returns:** `public OptionInstance<Double>`

### darknessEffectScale

```java
public OptionInstance<Double> darknessEffectScale()
```

**Returns:** `public OptionInstance<Double>`

### glintSpeed

```java
public OptionInstance<Double> glintSpeed()
```

**Returns:** `public OptionInstance<Double>`

### glintStrength

```java
public OptionInstance<Double> glintStrength()
```

**Returns:** `public OptionInstance<Double>`

### damageTiltStrength

```java
public OptionInstance<Double> damageTiltStrength()
```

**Returns:** `public OptionInstance<Double>`

### gamma

```java
public OptionInstance<Double> gamma()
```

**Returns:** `public OptionInstance<Double>`

### guiScale

```java
public OptionInstance<Integer> guiScale()
```

**Returns:** `public OptionInstance<Integer>`

### particles

```java
public OptionInstance<ParticleStatus> particles()
```

**Returns:** `public OptionInstance<ParticleStatus>`

### narrator

```java
public OptionInstance<NarratorStatus> narrator()
```

**Returns:** `public OptionInstance<NarratorStatus>`

### soundDevice

```java
public OptionInstance<String> soundDevice()
```

**Returns:** `public OptionInstance<String>`

### onboardingAccessibilityFinished

```java
public void onboardingAccessibilityFinished()
```

**Returns:** `public void`

### Options

```java
public Options(Minecraft p_92138_, File p_92139_)
```

**Parameters:**

- `p_92138_` (`Minecraft`)
- `p_92139_` (`File`)

**Returns:** `public`

### setForgeKeybindProperties

```java
 setForgeKeybindProperties()
```

**Returns:** ``

### getBackgroundOpacity

```java
public float getBackgroundOpacity(float p_92142_)
```

**Parameters:**

- `p_92142_` (`float`)

**Returns:** `public float`

### getBackgroundColor

```java
public int getBackgroundColor(float p_92171_)
```

**Parameters:**

- `p_92171_` (`float`)

**Returns:** `public int`

### getBackgroundColor

```java
public int getBackgroundColor(int p_92144_)
```

**Parameters:**

- `p_92144_` (`int`)

**Returns:** `public int`

### setKey

```java
public void setKey(KeyMapping p_92160_, InputConstants.Key p_92161_)
```

**Parameters:**

- `p_92160_` (`KeyMapping`)
- `p_92161_` (`InputConstants.Key`)

**Returns:** `public void`

### processDumpedOptions

```java
private void processDumpedOptions(Options.OptionAccess p_326142_)
```

**Parameters:**

- `p_326142_` (`Options.OptionAccess`)

**Returns:** `private void`

### processOptions

```java
private void processOptions(Options.FieldAccess p_168428_)
```

**Parameters:**

- `p_168428_` (`Options.FieldAccess`)

**Returns:** `private void`

### processOptionsForge

```java
 processOptionsForge()
```

**Returns:** ``

### processOptionsForge

```java
split off to allow reloading options after mod loading is done
    private void processOptionsForge(Options.FieldAccess p_168428_)
```

**Parameters:**

- `p_168428_` (`Options.FieldAccess`)

**Returns:** `split off to allow reloading options after mod loading is done
    private void`

### load

```java
public void load()
```

**Returns:** `public void`

### load

```java
public void load(boolean limited)
```

**Parameters:**

- `limited` (`boolean`)

**Returns:** `public void`

### process

```java
public <T> void process(String p_232125_, OptionInstance<T> p_232126_)
```

**Parameters:**

- `p_232125_` (`String`)
- `p_232126_` (`OptionInstance<T>`)

### process

```java
public int process(String p_168467_, int p_168468_)
```

**Parameters:**

- `p_168467_` (`String`)
- `p_168468_` (`int`)

**Returns:** `int`

### process

```java
public boolean process(String p_168483_, boolean p_168484_)
```

**Parameters:**

- `p_168483_` (`String`)
- `p_168484_` (`boolean`)

**Returns:** `boolean`

### process

```java
public String process(String p_168480_, String p_168481_)
```

**Parameters:**

- `p_168480_` (`String`)
- `p_168481_` (`String`)

**Returns:** `String`

### process

```java
public float process(String p_168464_, float p_168465_)
```

**Parameters:**

- `p_168464_` (`String`)
- `p_168465_` (`float`)

**Returns:** `float`

### process

```java
public <T> T process(String p_168470_, T p_168471_, Function<String, T> p_168472_, Function<T, String> p_168473_)
```

**Parameters:**

- `p_168470_` (`String`)
- `p_168471_` (`T`)
- `p_168472_` (`Function<String, T>`)
- `p_168473_` (`Function<T, String>`)

**Returns:** `T`

### isTrue

```java
static boolean isTrue(String p_168436_)
```

**Parameters:**

- `p_168436_` (`String`)

**Returns:** `static boolean`

### isFalse

```java
static boolean isFalse(String p_168441_)
```

**Parameters:**

- `p_168441_` (`String`)

**Returns:** `static boolean`

### dataFix

```java
private CompoundTag dataFix(CompoundTag p_92165_)
```

**Parameters:**

- `p_92165_` (`CompoundTag`)

**Returns:** `private CompoundTag`

### save

```java
public void save()
```

**Returns:** `public void`

### writePrefix

```java
public void writePrefix(String p_168491_)
```

**Parameters:**

- `p_168491_` (`String`)

**Returns:** `public void`

### process

```java
public <T> void process(String p_232135_, OptionInstance<T> p_232136_)
```

**Parameters:**

- `p_232135_` (`String`)
- `p_232136_` (`OptionInstance<T>`)

### process

```java
public int process(String p_168499_, int p_168500_)
```

**Parameters:**

- `p_168499_` (`String`)
- `p_168500_` (`int`)

**Returns:** `int`

### process

```java
public boolean process(String p_168515_, boolean p_168516_)
```

**Parameters:**

- `p_168515_` (`String`)
- `p_168516_` (`boolean`)

**Returns:** `boolean`

### process

```java
public String process(String p_168512_, String p_168513_)
```

**Parameters:**

- `p_168512_` (`String`)
- `p_168513_` (`String`)

**Returns:** `String`

### process

```java
public float process(String p_168496_, float p_168497_)
```

**Parameters:**

- `p_168496_` (`String`)
- `p_168497_` (`float`)

**Returns:** `float`

### process

```java
public <T> T process(String p_168502_, T p_168503_, Function<String, T> p_168504_, Function<T, String> p_168505_)
```

**Parameters:**

- `p_168502_` (`String`)
- `p_168503_` (`T`)
- `p_168504_` (`Function<String, T>`)
- `p_168505_` (`Function<T, String>`)

**Returns:** `T`

### buildPlayerInformation

```java
public ClientInformation buildPlayerInformation()
```

**Returns:** `public ClientInformation`

### broadcastOptions

```java
public void broadcastOptions()
```

**Returns:** `public void`

### setModelPart

```java
private void setModelPart(PlayerModelPart p_92155_, boolean p_92156_)
```

**Parameters:**

- `p_92155_` (`PlayerModelPart`)
- `p_92156_` (`boolean`)

**Returns:** `private void`

### isModelPartEnabled

```java
public boolean isModelPartEnabled(PlayerModelPart p_168417_)
```

**Parameters:**

- `p_168417_` (`PlayerModelPart`)

**Returns:** `public boolean`

### toggleModelPart

```java
public void toggleModelPart(PlayerModelPart p_168419_, boolean p_168420_)
```

**Parameters:**

- `p_168419_` (`PlayerModelPart`)
- `p_168420_` (`boolean`)

**Returns:** `public void`

### getCloudsType

```java
public CloudStatus getCloudsType()
```

**Returns:** `public CloudStatus`

### useNativeTransport

```java
public boolean useNativeTransport()
```

**Returns:** `public boolean`

### loadSelectedResourcePacks

```java
public void loadSelectedResourcePacks(PackRepository p_92146_)
```

**Parameters:**

- `p_92146_` (`PackRepository`)

**Returns:** `public void`

### setForgeKeybindProperties

```java
private void setForgeKeybindProperties()
```

**Returns:** `private void`

### getCameraType

```java
public CameraType getCameraType()
```

**Returns:** `public CameraType`

### setCameraType

```java
public void setCameraType(CameraType p_92158_)
```

**Parameters:**

- `p_92158_` (`CameraType`)

**Returns:** `public void`

### readListOfStrings

```java
private static List<String> readListOfStrings(String p_295764_)
```

**Parameters:**

- `p_295764_` (`String`)

**Returns:** `private static List<String>`

### getFile

```java
public File getFile()
```

**Returns:** `public File`

### dumpOptionsForReport

```java
public String dumpOptionsForReport()
```

**Returns:** `public String`

### process

```java
public <T> void process(String p_325921_, OptionInstance<T> p_326002_)
```

**Parameters:**

- `p_325921_` (`String`)
- `p_326002_` (`OptionInstance<T>`)

### setServerRenderDistance

```java
public void setServerRenderDistance(int p_193771_)
```

**Parameters:**

- `p_193771_` (`int`)

**Returns:** `public void`

### getEffectiveRenderDistance

```java
public int getEffectiveRenderDistance()
```

**Returns:** `public int`

### pixelValueLabel

```java
private static Component pixelValueLabel(Component p_231953_, int p_231954_)
```

**Parameters:**

- `p_231953_` (`Component`)
- `p_231954_` (`int`)

**Returns:** `private static Component`

### percentValueLabel

```java
private static Component percentValueLabel(Component p_231898_, double p_231899_)
```

**Parameters:**

- `p_231898_` (`Component`)
- `p_231899_` (`double`)

**Returns:** `private static Component`

### genericValueLabel

```java
public static Component genericValueLabel(Component p_231922_, Component p_231923_)
```

**Parameters:**

- `p_231922_` (`Component`)
- `p_231923_` (`Component`)

**Returns:** `public static Component`

### genericValueLabel

```java
public static Component genericValueLabel(Component p_231901_, int p_231902_)
```

**Parameters:**

- `p_231901_` (`Component`)
- `p_231902_` (`int`)

**Returns:** `public static Component`

### genericValueOrOffLabel

```java
public static Component genericValueOrOffLabel(Component p_348615_, int p_348682_)
```

**Parameters:**

- `p_348615_` (`Component`)
- `p_348682_` (`int`)

**Returns:** `public static Component`

### percentValueOrOffLabel

```java
private static Component percentValueOrOffLabel(Component p_341700_, double p_341691_)
```

**Parameters:**

- `p_341700_` (`Component`)
- `p_341691_` (`double`)

**Returns:** `private static Component`

### process

```java
int process(String p_168523_, int p_168524_)
```

**Parameters:**

- `p_168523_` (`String`)
- `p_168524_` (`int`)

**Returns:** `int`

### process

```java
boolean process(String p_168535_, boolean p_168536_)
```

**Parameters:**

- `p_168535_` (`String`)
- `p_168536_` (`boolean`)

**Returns:** `boolean`

### process

```java
String process(String p_168533_, String p_168534_)
```

**Parameters:**

- `p_168533_` (`String`)
- `p_168534_` (`String`)

**Returns:** `String`

### process

```java
float process(String p_168521_, float p_168522_)
```

**Parameters:**

- `p_168521_` (`String`)
- `p_168522_` (`float`)

**Returns:** `float`

### process

```java
<T> <T> T process(String p_168525_, T p_168526_, Function<String, T> p_168527_, Function<T, String> p_168528_)
```

**Parameters:**

- `p_168525_` (`String`)
- `p_168526_` (`T`)
- `p_168527_` (`Function<String, T>`)
- `p_168528_` (`Function<T, String>`)

**Returns:** `<T> T`

### process

```java
<T> <T> void process(String p_326392_, OptionInstance<T> p_326326_)
```

**Parameters:**

- `p_326392_` (`String`)
- `p_326326_` (`OptionInstance<T>`)

**Returns:** `<T> void`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `FieldAccess` | interface |  |
