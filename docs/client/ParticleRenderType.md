# ParticleRenderType

**Package:** `net.minecraft.client.particle`
**Type:** interface
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### begin

```java
public BufferBuilder begin(Tesselator p_350993_, TextureManager p_107442_)
```

**Parameters:**

- `p_350993_` (`Tesselator`)
- `p_107442_` (`TextureManager`)

**Returns:** `BufferBuilder`

### toString

```java
public String toString()
```

**Returns:** `String`

### begin

```java
public BufferBuilder begin(Tesselator p_350576_, TextureManager p_107449_)
```

**Parameters:**

- `p_350576_` (`Tesselator`)
- `p_107449_` (`TextureManager`)

**Returns:** `BufferBuilder`

### toString

```java
public String toString()
```

**Returns:** `String`

### isTranslucent

```java
public boolean isTranslucent()
```

**Returns:** `boolean`

### begin

```java
public BufferBuilder begin(Tesselator p_350826_, TextureManager p_107456_)
```

**Parameters:**

- `p_350826_` (`Tesselator`)
- `p_107456_` (`TextureManager`)

**Returns:** `BufferBuilder`

### toString

```java
public String toString()
```

**Returns:** `String`

### begin

```java
public BufferBuilder begin(Tesselator p_351047_, TextureManager p_107463_)
```

**Parameters:**

- `p_351047_` (`Tesselator`)
- `p_107463_` (`TextureManager`)

**Returns:** `BufferBuilder`

### toString

```java
public String toString()
```

**Returns:** `String`

### isTranslucent

```java
public boolean isTranslucent()
```

**Returns:** `boolean`

### begin

```java
public BufferBuilder begin(Tesselator p_350910_, TextureManager p_107470_)
```

**Parameters:**

- `p_350910_` (`Tesselator`)
- `p_107470_` (`TextureManager`)

**Returns:** `BufferBuilder`

### toString

```java
public String toString()
```

**Returns:** `String`

### begin

```java
public BufferBuilder begin(Tesselator p_350814_, TextureManager p_107477_)
```

**Parameters:**

- `p_350814_` (`Tesselator`)
- `p_107477_` (`TextureManager`)

**Returns:** `BufferBuilder`

### toString

```java
public String toString()
```

**Returns:** `String`

### begin

```java
BufferBuilder begin(Tesselator p_350949_, TextureManager p_107437_)
```

**Parameters:**

- `p_350949_` (`Tesselator`)
- `p_107437_` (`TextureManager`)

**Returns:** `BufferBuilder`

### isTranslucent

```java
default boolean isTranslucent()
```

**Returns:** `boolean`
