# PlayerCloudParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `TextureSheetParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### PlayerCloudParticle

```java
protected PlayerCloudParticle(ClientLevel p_107483_, double p_107484_, double p_107485_, double p_107486_, double p_107487_, double p_107488_, double p_107489_, SpriteSet p_107490_)
```

**Parameters:**

- `p_107483_` (`ClientLevel`)
- `p_107484_` (`double`)
- `p_107485_` (`double`)
- `p_107486_` (`double`)
- `p_107487_` (`double`)
- `p_107488_` (`double`)
- `p_107489_` (`double`)
- `p_107490_` (`SpriteSet`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`

### getQuadSize

```java
public float getQuadSize(float p_107504_)
```

**Parameters:**

- `p_107504_` (`float`)

**Returns:** `float`

### tick

```java
public void tick()
```

### Provider

```java
public Provider(SpriteSet p_107507_)
```

**Parameters:**

- `p_107507_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_107518_, ClientLevel p_107519_, double p_107520_, double p_107521_, double p_107522_, double p_107523_, double p_107524_, double p_107525_)
```

**Parameters:**

- `p_107518_` (`SimpleParticleType`)
- `p_107519_` (`ClientLevel`)
- `p_107520_` (`double`)
- `p_107521_` (`double`)
- `p_107522_` (`double`)
- `p_107523_` (`double`)
- `p_107524_` (`double`)
- `p_107525_` (`double`)

**Returns:** `public Particle`

### PlayerCloudParticle

```java
return new PlayerCloudParticle()
```

**Returns:** `return new`

### SneezeProvider

```java
public SneezeProvider(SpriteSet p_107528_)
```

**Parameters:**

- `p_107528_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_107539_, ClientLevel p_107540_, double p_107541_, double p_107542_, double p_107543_, double p_107544_, double p_107545_, double p_107546_)
```

**Parameters:**

- `p_107539_` (`SimpleParticleType`)
- `p_107540_` (`ClientLevel`)
- `p_107541_` (`double`)
- `p_107542_` (`double`)
- `p_107543_` (`double`)
- `p_107544_` (`double`)
- `p_107545_` (`double`)
- `p_107546_` (`double`)

**Returns:** `public Particle`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Provider` | class |  |
| `SneezeProvider` | class |  |
