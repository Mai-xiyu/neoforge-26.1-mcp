# PrioritizeChunkUpdates

**Package:** `net.minecraft.client`
**Type:** enum
**Implements:** `OptionEnum`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### NEARBY

```java
, NEARBY()
```

**Returns:** `,`

### PrioritizeChunkUpdates

```java
private PrioritizeChunkUpdates(int p_193784_, String p_193785_)
```

**Parameters:**

- `p_193784_` (`int`)
- `p_193785_` (`String`)

**Returns:** `private`

### getId

```java
public int getId()
```

**Returns:** `int`

### getKey

```java
public String getKey()
```

**Returns:** `String`

### byId

```java
public static PrioritizeChunkUpdates byId(int p_193788_)
```

**Parameters:**

- `p_193788_` (`int`)

**Returns:** `public static PrioritizeChunkUpdates`
