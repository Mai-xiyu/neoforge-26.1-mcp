# ReversePortalParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `PortalParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### ReversePortalParticle

```java
protected ReversePortalParticle(ClientLevel p_107590_, double p_107591_, double p_107592_, double p_107593_, double p_107594_, double p_107595_, double p_107596_)
```

**Parameters:**

- `p_107590_` (`ClientLevel`)
- `p_107591_` (`double`)
- `p_107592_` (`double`)
- `p_107593_` (`double`)
- `p_107594_` (`double`)
- `p_107595_` (`double`)
- `p_107596_` (`double`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### getQuadSize

```java
public float getQuadSize(float p_107608_)
```

**Parameters:**

- `p_107608_` (`float`)

**Returns:** `float`

### tick

```java
public void tick()
```

### ReversePortalProvider

```java
public ReversePortalProvider(SpriteSet p_107611_)
```

**Parameters:**

- `p_107611_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_107622_, ClientLevel p_107623_, double p_107624_, double p_107625_, double p_107626_, double p_107627_, double p_107628_, double p_107629_)
```

**Parameters:**

- `p_107622_` (`SimpleParticleType`)
- `p_107623_` (`ClientLevel`)
- `p_107624_` (`double`)
- `p_107625_` (`double`)
- `p_107626_` (`double`)
- `p_107627_` (`double`)
- `p_107628_` (`double`)
- `p_107629_` (`double`)

**Returns:** `public Particle`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `ReversePortalProvider` | class |  |
