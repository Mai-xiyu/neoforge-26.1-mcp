# SculkChargeParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `TextureSheetParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### SculkChargeParticle

```java
protected SculkChargeParticle(ClientLevel p_233892_, double p_233893_, double p_233894_, double p_233895_, double p_233896_, double p_233897_, double p_233898_, SpriteSet p_233899_)
```

**Parameters:**

- `p_233892_` (`ClientLevel`)
- `p_233893_` (`double`)
- `p_233894_` (`double`)
- `p_233895_` (`double`)
- `p_233896_` (`double`)
- `p_233897_` (`double`)
- `p_233898_` (`double`)
- `p_233899_` (`SpriteSet`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### getLightColor

```java
public int getLightColor(float p_233902_)
```

**Parameters:**

- `p_233902_` (`float`)

**Returns:** `int`

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`

### tick

```java
public void tick()
```

### createParticle

```java
public Particle createParticle(SculkChargeParticleOptions p_233918_, ClientLevel p_233919_, double p_233920_, double p_233921_, double p_233922_, double p_233923_, double p_233924_, double p_233925_)
```

**Parameters:**

- `p_233918_` (`SculkChargeParticleOptions`)
- `p_233919_` (`ClientLevel`)
- `p_233920_` (`double`)
- `p_233921_` (`double`)
- `p_233922_` (`double`)
- `p_233923_` (`double`)
- `p_233924_` (`double`)
- `p_233925_` (`double`)

**Returns:** `public Particle`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Provider` | record |  |
