# SculkChargePopParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `TextureSheetParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### SculkChargePopParticle

```java
protected SculkChargePopParticle(ClientLevel p_233932_, double p_233933_, double p_233934_, double p_233935_, double p_233936_, double p_233937_, double p_233938_, SpriteSet p_233939_)
```

**Parameters:**

- `p_233932_` (`ClientLevel`)
- `p_233933_` (`double`)
- `p_233934_` (`double`)
- `p_233935_` (`double`)
- `p_233936_` (`double`)
- `p_233937_` (`double`)
- `p_233938_` (`double`)
- `p_233939_` (`SpriteSet`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### getLightColor

```java
public int getLightColor(float p_233942_)
```

**Parameters:**

- `p_233942_` (`float`)

**Returns:** `int`

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`

### tick

```java
public void tick()
```

### createParticle

```java
public Particle createParticle(SimpleParticleType p_233958_, ClientLevel p_233959_, double p_233960_, double p_233961_, double p_233962_, double p_233963_, double p_233964_, double p_233965_)
```

**Parameters:**

- `p_233958_` (`SimpleParticleType`)
- `p_233959_` (`ClientLevel`)
- `p_233960_` (`double`)
- `p_233961_` (`double`)
- `p_233962_` (`double`)
- `p_233963_` (`double`)
- `p_233964_` (`double`)
- `p_233965_` (`double`)

**Returns:** `public Particle`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Provider` | record |  |
