# SmokeParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `BaseAshSmokeParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### SmokeParticle

```java
protected SmokeParticle(ClientLevel p_107685_, double p_107686_, double p_107687_, double p_107688_, double p_107689_, double p_107690_, double p_107691_, float p_107692_, SpriteSet p_107693_)
```

**Parameters:**

- `p_107685_` (`ClientLevel`)
- `p_107686_` (`double`)
- `p_107687_` (`double`)
- `p_107688_` (`double`)
- `p_107689_` (`double`)
- `p_107690_` (`double`)
- `p_107691_` (`double`)
- `p_107692_` (`float`)
- `p_107693_` (`SpriteSet`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### Provider

```java
public Provider(SpriteSet p_107696_)
```

**Parameters:**

- `p_107696_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_107707_, ClientLevel p_107708_, double p_107709_, double p_107710_, double p_107711_, double p_107712_, double p_107713_, double p_107714_)
```

**Parameters:**

- `p_107707_` (`SimpleParticleType`)
- `p_107708_` (`ClientLevel`)
- `p_107709_` (`double`)
- `p_107710_` (`double`)
- `p_107711_` (`double`)
- `p_107712_` (`double`)
- `p_107713_` (`double`)
- `p_107714_` (`double`)

**Returns:** `public Particle`

### SmokeParticle

```java
return new SmokeParticle()
```

**Returns:** `return new`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Provider` | class |  |
