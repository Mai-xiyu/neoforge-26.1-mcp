# SpellParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `TextureSheetParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### SpellParticle

```java
protected SpellParticle(ClientLevel p_107762_, double p_107763_, double p_107764_, double p_107765_, double p_107766_, double p_107767_, double p_107768_, SpriteSet p_107769_)
```

**Parameters:**

- `p_107762_` (`ClientLevel`)
- `p_107763_` (`double`)
- `p_107764_` (`double`)
- `p_107765_` (`double`)
- `p_107766_` (`double`)
- `p_107767_` (`double`)
- `p_107768_` (`double`)
- `p_107769_` (`SpriteSet`)

**Returns:** `protected`

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`

### tick

```java
public void tick()
```

### setAlpha

```java
protected void setAlpha(float p_340807_)
```

**Parameters:**

- `p_340807_` (`float`)

### isCloseToScopingPlayer

```java
private boolean isCloseToScopingPlayer()
```

**Returns:** `private boolean`

### InstantProvider

```java
public InstantProvider(SpriteSet p_107805_)
```

**Parameters:**

- `p_107805_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_107816_, ClientLevel p_107817_, double p_107818_, double p_107819_, double p_107820_, double p_107821_, double p_107822_, double p_107823_)
```

**Parameters:**

- `p_107816_` (`SimpleParticleType`)
- `p_107817_` (`ClientLevel`)
- `p_107818_` (`double`)
- `p_107819_` (`double`)
- `p_107820_` (`double`)
- `p_107821_` (`double`)
- `p_107822_` (`double`)
- `p_107823_` (`double`)

**Returns:** `public Particle`

### SpellParticle

```java
return new SpellParticle()
```

**Returns:** `return new`

### MobEffectProvider

```java
public MobEffectProvider(SpriteSet p_333733_)
```

**Parameters:**

- `p_333733_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(ColorParticleOption p_333710_, ClientLevel p_333861_, double p_333927_, double p_333732_, double p_333727_, double p_334019_, double p_333790_, double p_333937_)
```

**Parameters:**

- `p_333710_` (`ColorParticleOption`)
- `p_333861_` (`ClientLevel`)
- `p_333927_` (`double`)
- `p_333732_` (`double`)
- `p_333727_` (`double`)
- `p_334019_` (`double`)
- `p_333790_` (`double`)
- `p_333937_` (`double`)

**Returns:** `public Particle`

### Provider

```java
public Provider(SpriteSet p_107847_)
```

**Parameters:**

- `p_107847_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_107858_, ClientLevel p_107859_, double p_107860_, double p_107861_, double p_107862_, double p_107863_, double p_107864_, double p_107865_)
```

**Parameters:**

- `p_107858_` (`SimpleParticleType`)
- `p_107859_` (`ClientLevel`)
- `p_107860_` (`double`)
- `p_107861_` (`double`)
- `p_107862_` (`double`)
- `p_107863_` (`double`)
- `p_107864_` (`double`)
- `p_107865_` (`double`)

**Returns:** `public Particle`

### SpellParticle

```java
return new SpellParticle()
```

**Returns:** `return new`

### WitchProvider

```java
public WitchProvider(SpriteSet p_107868_)
```

**Parameters:**

- `p_107868_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_107879_, ClientLevel p_107880_, double p_107881_, double p_107882_, double p_107883_, double p_107884_, double p_107885_, double p_107886_)
```

**Parameters:**

- `p_107879_` (`SimpleParticleType`)
- `p_107880_` (`ClientLevel`)
- `p_107881_` (`double`)
- `p_107882_` (`double`)
- `p_107883_` (`double`)
- `p_107884_` (`double`)
- `p_107885_` (`double`)
- `p_107886_` (`double`)

**Returns:** `public Particle`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `InstantProvider` | class |  |
| `MobEffectProvider` | class |  |
| `Provider` | class |  |
| `WitchProvider` | class |  |
