# SuspendedParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `TextureSheetParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### SuspendedParticle

```java
protected SuspendedParticle(ClientLevel p_172403_, SpriteSet p_172404_, double p_172405_, double p_172406_, double p_172407_)
```

**Parameters:**

- `p_172403_` (`ClientLevel`)
- `p_172404_` (`SpriteSet`)
- `p_172405_` (`double`)
- `p_172406_` (`double`)
- `p_172407_` (`double`)

**Returns:** `protected`

### super

```java
 super(p_172406_ - 0.125)
```

**Parameters:**

- `0.125` (`p_172406_ -`)

**Returns:** ``

### SuspendedParticle

```java
 SuspendedParticle(ClientLevel p_172409_, SpriteSet p_172410_, double p_172411_, double p_172412_, double p_172413_, double p_172414_, double p_172415_, double p_172416_)
```

**Parameters:**

- `p_172409_` (`ClientLevel`)
- `p_172410_` (`SpriteSet`)
- `p_172411_` (`double`)
- `p_172412_` (`double`)
- `p_172413_` (`double`)
- `p_172414_` (`double`)
- `p_172415_` (`double`)
- `p_172416_` (`double`)

**Returns:** ``

### super

```java
 super(p_172412_ - 0.125)
```

**Parameters:**

- `0.125` (`p_172412_ -`)

**Returns:** ``

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`

### CrimsonSporeProvider

```java
public CrimsonSporeProvider(SpriteSet p_108042_)
```

**Parameters:**

- `p_108042_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_108053_, ClientLevel p_108054_, double p_108055_, double p_108056_, double p_108057_, double p_108058_, double p_108059_, double p_108060_)
```

**Parameters:**

- `p_108053_` (`SimpleParticleType`)
- `p_108054_` (`ClientLevel`)
- `p_108055_` (`double`)
- `p_108056_` (`double`)
- `p_108057_` (`double`)
- `p_108058_` (`double`)
- `p_108059_` (`double`)
- `p_108060_` (`double`)

**Returns:** `public Particle`

### SporeBlossomAirProvider

```java
public SporeBlossomAirProvider(SpriteSet p_172419_)
```

**Parameters:**

- `p_172419_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_172430_, ClientLevel p_172431_, double p_172432_, double p_172433_, double p_172434_, double p_172435_, double p_172436_, double p_172437_)
```

**Parameters:**

- `p_172430_` (`SimpleParticleType`)
- `p_172431_` (`ClientLevel`)
- `p_172432_` (`double`)
- `p_172433_` (`double`)
- `p_172434_` (`double`)
- `p_172435_` (`double`)
- `p_172436_` (`double`)
- `p_172437_` (`double`)

**Returns:** `public Particle`

### getParticleGroup

```java
public Optional<ParticleGroup> getParticleGroup()
```

**Returns:** `Optional<ParticleGroup>`

### UnderwaterProvider

```java
public UnderwaterProvider(SpriteSet p_108063_)
```

**Parameters:**

- `p_108063_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_108074_, ClientLevel p_108075_, double p_108076_, double p_108077_, double p_108078_, double p_108079_, double p_108080_, double p_108081_)
```

**Parameters:**

- `p_108074_` (`SimpleParticleType`)
- `p_108075_` (`ClientLevel`)
- `p_108076_` (`double`)
- `p_108077_` (`double`)
- `p_108078_` (`double`)
- `p_108079_` (`double`)
- `p_108080_` (`double`)
- `p_108081_` (`double`)

**Returns:** `public Particle`

### WarpedSporeProvider

```java
public WarpedSporeProvider(SpriteSet p_108084_)
```

**Parameters:**

- `p_108084_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_108095_, ClientLevel p_108096_, double p_108097_, double p_108098_, double p_108099_, double p_108100_, double p_108101_, double p_108102_)
```

**Parameters:**

- `p_108095_` (`SimpleParticleType`)
- `p_108096_` (`ClientLevel`)
- `p_108097_` (`double`)
- `p_108098_` (`double`)
- `p_108099_` (`double`)
- `p_108100_` (`double`)
- `p_108101_` (`double`)
- `p_108102_` (`double`)

**Returns:** `public Particle`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `CrimsonSporeProvider` | class |  |
| `SporeBlossomAirProvider` | class |  |
| `UnderwaterProvider` | class |  |
| `WarpedSporeProvider` | class |  |
