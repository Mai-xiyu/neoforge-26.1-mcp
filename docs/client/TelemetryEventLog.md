# TelemetryEventLog

**Package:** `net.minecraft.client.telemetry`
**Type:** class
**Implements:** `AutoCloseable`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### TelemetryEventLog

```java
public TelemetryEventLog(FileChannel p_261731_, Executor p_262010_)
```

**Parameters:**

- `p_261731_` (`FileChannel`)
- `p_262010_` (`Executor`)

**Returns:** `public`

### logger

```java
public TelemetryEventLogger logger()
```

**Returns:** `public TelemetryEventLogger`

### close

```java
public void close()
```
