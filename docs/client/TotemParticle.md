# TotemParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `SimpleAnimatedParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### TotemParticle

```java
protected TotemParticle(ClientLevel p_108346_, double p_108347_, double p_108348_, double p_108349_, double p_108350_, double p_108351_, double p_108352_, SpriteSet p_108353_)
```

**Parameters:**

- `p_108346_` (`ClientLevel`)
- `p_108347_` (`double`)
- `p_108348_` (`double`)
- `p_108349_` (`double`)
- `p_108350_` (`double`)
- `p_108351_` (`double`)
- `p_108352_` (`double`)
- `p_108353_` (`SpriteSet`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### Provider

```java
public Provider(SpriteSet p_108366_)
```

**Parameters:**

- `p_108366_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_108377_, ClientLevel p_108378_, double p_108379_, double p_108380_, double p_108381_, double p_108382_, double p_108383_, double p_108384_)
```

**Parameters:**

- `p_108377_` (`SimpleParticleType`)
- `p_108378_` (`ClientLevel`)
- `p_108379_` (`double`)
- `p_108380_` (`double`)
- `p_108381_` (`double`)
- `p_108382_` (`double`)
- `p_108383_` (`double`)
- `p_108384_` (`double`)

**Returns:** `public Particle`

### TotemParticle

```java
return new TotemParticle()
```

**Returns:** `return new`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Provider` | class |  |
