# TrialSpawnerDetectionParticle

**Package:** `net.minecraft.client.particle`
**Type:** class
**Extends:** `TextureSheetParticle`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### TrialSpawnerDetectionParticle

```java
protected TrialSpawnerDetectionParticle(ClientLevel p_312476_, double p_312565_, double p_312753_, double p_311963_, double p_312472_, double p_311956_, double p_312272_, float p_312057_, SpriteSet p_312002_)
```

**Parameters:**

- `p_312476_` (`ClientLevel`)
- `p_312565_` (`double`)
- `p_312753_` (`double`)
- `p_311963_` (`double`)
- `p_312472_` (`double`)
- `p_311956_` (`double`)
- `p_312272_` (`double`)
- `p_312057_` (`float`)
- `p_312002_` (`SpriteSet`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``

### getRenderType

```java
public ParticleRenderType getRenderType()
```

**Returns:** `ParticleRenderType`

### getLightColor

```java
public int getLightColor(float p_312871_)
```

**Parameters:**

- `p_312871_` (`float`)

**Returns:** `int`

### getFacingCameraMode

```java
public SingleQuadParticle.FacingCameraMode getFacingCameraMode()
```

**Returns:** `SingleQuadParticle.FacingCameraMode`

### tick

```java
public void tick()
```

### getQuadSize

```java
public float getQuadSize(float p_312246_)
```

**Parameters:**

- `p_312246_` (`float`)

**Returns:** `float`

### Provider

```java
public Provider(SpriteSet p_312414_)
```

**Parameters:**

- `p_312414_` (`SpriteSet`)

**Returns:** `public`

### createParticle

```java
public Particle createParticle(SimpleParticleType p_312488_, ClientLevel p_312627_, double p_312195_, double p_312322_, double p_312229_, double p_312548_, double p_312570_, double p_311993_)
```

**Parameters:**

- `p_312488_` (`SimpleParticleType`)
- `p_312627_` (`ClientLevel`)
- `p_312195_` (`double`)
- `p_312322_` (`double`)
- `p_312229_` (`double`)
- `p_312548_` (`double`)
- `p_312570_` (`double`)
- `p_311993_` (`double`)

**Returns:** `public Particle`

### TrialSpawnerDetectionParticle

```java
return new TrialSpawnerDetectionParticle()
```

**Returns:** `return new`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Provider` | class |  |
