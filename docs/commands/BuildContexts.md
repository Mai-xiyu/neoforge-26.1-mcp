# BuildContexts

**Package:** `net.minecraft.commands.execution.tasks`
**Type:** class<T extends ExecutionCommandSource<T>>

## Methods

### BuildContexts

```java
public BuildContexts(String p_306157_, ContextChain<T> p_305974_)
```

**Parameters:**

- `p_306157_` (`String`)
- `p_305974_` (`ContextChain<T>`)

**Returns:** `public`

### execute

```java
protected void execute(T p_309567_, List<T> p_306303_, ExecutionContext<T> p_305977_, Frame p_309603_, ChainModifiers p_309659_)
```

**Parameters:**

- `p_309567_` (`T`)
- `p_306303_` (`List<T>`)
- `p_305977_` (`ExecutionContext<T>`)
- `p_309603_` (`Frame`)
- `p_309659_` (`ChainModifiers`)

**Returns:** `protected void`

### traceCommandStart

```java
protected void traceCommandStart(ExecutionContext<T> p_306237_, Frame p_309625_)
```

**Parameters:**

- `p_306237_` (`ExecutionContext<T>`)
- `p_309625_` (`Frame`)

**Returns:** `protected void`

### toString

```java
public String toString()
```

**Returns:** `String`

### Continuation

```java
public Continuation(String p_306217_, ContextChain<T> p_305988_, ChainModifiers p_309702_, T p_309701_, List<T> p_306017_)
```

**Parameters:**

- `p_306217_` (`String`)
- `p_305988_` (`ContextChain<T>`)
- `p_309702_` (`ChainModifiers`)
- `p_309701_` (`T`)
- `p_306017_` (`List<T>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### execute

```java
public void execute(ExecutionContext<T> p_305882_, Frame p_309577_)
```

**Parameters:**

- `p_305882_` (`ExecutionContext<T>`)
- `p_309577_` (`Frame`)

### TopLevel

```java
public TopLevel(String p_306314_, ContextChain<T> p_305844_, T p_306134_)
```

**Parameters:**

- `p_306314_` (`String`)
- `p_305844_` (`ContextChain<T>`)
- `p_306134_` (`T`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### execute

```java
public void execute(ExecutionContext<T> p_306114_, Frame p_309583_)
```

**Parameters:**

- `p_306114_` (`ExecutionContext<T>`)
- `p_309583_` (`Frame`)

### Unbound

```java
public Unbound(String p_305863_, ContextChain<T> p_305842_)
```

**Parameters:**

- `p_305863_` (`String`)
- `p_305842_` (`ContextChain<T>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### execute

```java
public void execute(T p_306259_, ExecutionContext<T> p_305944_, Frame p_309714_)
```

**Parameters:**

- `p_306259_` (`T`)
- `p_305944_` (`ExecutionContext<T>`)
- `p_309714_` (`Frame`)

**Returns:** `public void`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Continuation` | class |  |
| `TopLevel` | class |  |
| `Unbound` | class |  |
