# ItemInput

**Package:** `net.minecraft.commands.arguments.item`
**Type:** class

## Methods

### ItemInput

```java
public ItemInput(Holder<Item> p_235282_, DataComponentPatch p_345942_)
```

**Parameters:**

- `p_235282_` (`Holder<Item>`)
- `p_345942_` (`DataComponentPatch`)

**Returns:** `public`

### getItem

```java
public Item getItem()
```

**Returns:** `public Item`

### createItemStack

```java
public ItemStack createItemStack(int p_120981_, boolean p_120982_)
```

**Parameters:**

- `p_120981_` (`int`)
- `p_120982_` (`boolean`)

**Returns:** `public ItemStack`

### serialize

```java
public String serialize(HolderLookup.Provider p_330692_)
```

**Parameters:**

- `p_330692_` (`HolderLookup.Provider`)

**Returns:** `public String`

### serializeComponents

```java
private String serializeComponents(HolderLookup.Provider p_331015_)
```

**Parameters:**

- `p_331015_` (`HolderLookup.Provider`)

**Returns:** `private String`

### getItemName

```java
private String getItemName()
```

**Returns:** `private String`
