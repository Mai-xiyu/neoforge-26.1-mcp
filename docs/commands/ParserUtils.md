# ParserUtils

**Package:** `net.minecraft.commands`
**Type:** class

## Methods

### IllegalStateException

```java
throw new IllegalStateException("Couldn't get field 'pos' for JsonReader")
```

**Parameters:**

- `JsonReader"` (`"Couldn't get field 'pos' for`)

**Returns:** `throw new`

### IllegalStateException

```java
throw new IllegalStateException("Couldn't get field 'lineStart' for JsonReader")
```

**Parameters:**

- `JsonReader"` (`"Couldn't get field 'lineStart' for`)

**Returns:** `throw new`

### getPos

```java
private static int getPos(JsonReader p_313929_)
```

**Parameters:**

- `p_313929_` (`JsonReader`)

**Returns:** `private static int`

### IllegalStateException

```java
throw new IllegalStateException("Couldn't read position of JsonReader")
```

**Parameters:**

- `JsonReader"` (`"Couldn't read position of`)

**Returns:** `throw new`

### parseJson

```java
<T> public static <T> T parseJson(HolderLookup.Provider p_323581_, StringReader p_313786_, Codec<T> p_313876_)
```

**Parameters:**

- `p_323581_` (`HolderLookup.Provider`)
- `p_313786_` (`StringReader`)
- `p_313876_` (`Codec<T>`)

**Returns:** `public static <T> T`

### JsonParseException

```java
throw new JsonParseException()
```

**Returns:** `throw new`

### readWhile

```java
public static String readWhile(StringReader p_332772_, CharPredicate p_332664_)
```

**Parameters:**

- `p_332772_` (`StringReader`)
- `p_332664_` (`CharPredicate`)

**Returns:** `public static String`
