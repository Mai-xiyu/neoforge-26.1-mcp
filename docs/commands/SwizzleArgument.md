# SwizzleArgument

**Package:** `net.minecraft.commands.arguments.coordinates`
**Type:** class
**Implements:** `ArgumentType<EnumSet<Direction.Axis>>`

## Methods

### swizzle

```java
public static SwizzleArgument swizzle()
```

**Returns:** `public static SwizzleArgument`

### SwizzleArgument

```java
return new SwizzleArgument()
```

**Returns:** `return new`

### getSwizzle

```java
public static EnumSet<Direction.Axis> getSwizzle(CommandContext<CommandSourceStack> p_120811_, String p_120812_)
```

**Parameters:**

- `p_120811_` (`CommandContext<CommandSourceStack>`)
- `p_120812_` (`String`)

**Returns:** `public static EnumSet<Direction.Axis>`

### parse

```java
public EnumSet<Direction.Axis> parse(StringReader p_120809_)
```

**Parameters:**

- `p_120809_` (`StringReader`)

**Returns:** `public EnumSet<Direction.Axis>`

### getExamples

```java
public Collection<String> getExamples()
```

**Returns:** `Collection<String>`
