# WorldCoordinate

**Package:** `net.minecraft.commands.arguments.coordinates`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `ERROR_EXPECTED_DOUBLE` | `SimpleCommandExceptionType` |  |
| `ERROR_EXPECTED_INT` | `SimpleCommandExceptionType` |  |

## Methods

### WorldCoordinate

```java
public WorldCoordinate(boolean p_120864_, double p_120865_)
```

**Parameters:**

- `p_120864_` (`boolean`)
- `p_120865_` (`double`)

**Returns:** `public`

### get

```java
public double get(double p_120868_)
```

**Parameters:**

- `p_120868_` (`double`)

**Returns:** `public double`

### parseDouble

```java
public static WorldCoordinate parseDouble(StringReader p_120872_, boolean p_120873_)
```

**Parameters:**

- `p_120872_` (`StringReader`)
- `p_120873_` (`boolean`)

**Returns:** `public static WorldCoordinate`

### WorldCoordinate

```java
return new WorldCoordinate()
```

**Returns:** `return new`

### WorldCoordinate

```java
return new WorldCoordinate()
```

**Returns:** `return new`

### parseInt

```java
public static WorldCoordinate parseInt(StringReader p_120870_)
```

**Parameters:**

- `p_120870_` (`StringReader`)

**Returns:** `public static WorldCoordinate`

### WorldCoordinate

```java
return new WorldCoordinate()
```

**Returns:** `return new`

### isRelative

```java
public static boolean isRelative(StringReader p_120875_)
```

**Parameters:**

- `p_120875_` (`StringReader`)

**Returns:** `public static boolean`

### equals

```java
public boolean equals(Object p_120877_)
```

**Parameters:**

- `p_120877_` (`Object`)

**Returns:** `boolean`

### hashCode

```java
public int hashCode()
```

**Returns:** `int`

### isRelative

```java
public boolean isRelative()
```

**Returns:** `public boolean`
