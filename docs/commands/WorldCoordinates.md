# WorldCoordinates

**Package:** `net.minecraft.commands.arguments.coordinates`
**Type:** class
**Implements:** `Coordinates`

## Methods

### WorldCoordinates

```java
public WorldCoordinates(WorldCoordinate p_120883_, WorldCoordinate p_120884_, WorldCoordinate p_120885_)
```

**Parameters:**

- `p_120883_` (`WorldCoordinate`)
- `p_120884_` (`WorldCoordinate`)
- `p_120885_` (`WorldCoordinate`)

**Returns:** `public`

### getPosition

```java
public Vec3 getPosition(CommandSourceStack p_120893_)
```

**Parameters:**

- `p_120893_` (`CommandSourceStack`)

**Returns:** `Vec3`

### getRotation

```java
public Vec2 getRotation(CommandSourceStack p_120896_)
```

**Parameters:**

- `p_120896_` (`CommandSourceStack`)

**Returns:** `Vec2`

### isXRelative

```java
public boolean isXRelative()
```

**Returns:** `boolean`

### isYRelative

```java
public boolean isYRelative()
```

**Returns:** `boolean`

### isZRelative

```java
public boolean isZRelative()
```

**Returns:** `boolean`

### equals

```java
public boolean equals(Object p_120900_)
```

**Parameters:**

- `p_120900_` (`Object`)

**Returns:** `boolean`

### parseInt

```java
public static WorldCoordinates parseInt(StringReader p_120888_)
```

**Parameters:**

- `p_120888_` (`StringReader`)

**Returns:** `public static WorldCoordinates`

### WorldCoordinates

```java
return new WorldCoordinates()
```

**Returns:** `return new`

### parseDouble

```java
public static WorldCoordinates parseDouble(StringReader p_120890_, boolean p_120891_)
```

**Parameters:**

- `p_120890_` (`StringReader`)
- `p_120891_` (`boolean`)

**Returns:** `public static WorldCoordinates`

### WorldCoordinates

```java
return new WorldCoordinates()
```

**Returns:** `return new`

### absolute

```java
public static WorldCoordinates absolute(double p_175086_, double p_175087_, double p_175088_)
```

**Parameters:**

- `p_175086_` (`double`)
- `p_175087_` (`double`)
- `p_175088_` (`double`)

**Returns:** `public static WorldCoordinates`

### absolute

```java
public static WorldCoordinates absolute(Vec2 p_175090_)
```

**Parameters:**

- `p_175090_` (`Vec2`)

**Returns:** `public static WorldCoordinates`

### current

```java
public static WorldCoordinates current()
```

**Returns:** `public static WorldCoordinates`

### hashCode

```java
public int hashCode()
```

**Returns:** `int`
