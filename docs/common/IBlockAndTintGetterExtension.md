# IBlockAndTintGetterExtension

**Package:** `net.neoforged.neoforge.common.extensions`
**Type:** interface

## Description

Extension interface for `BlockAndTintGetter`.

## Methods

### self

```java
private BlockAndTintGetter self()
```

**Returns:** `private BlockAndTintGetter`

### getShade

```java
default float getShade(float normalX, float normalY, float normalZ, boolean shade)
```

**Parameters:**

- `normalX` (`float`)
- `normalY` (`float`)
- `normalZ` (`float`)
- `shade` (`boolean`)

**Returns:** `float`
