# IBlockGetterExtension

**Package:** `net.neoforged.neoforge.common.extensions`
**Type:** interface

## Methods

### getAuxLightManager

```java
.NonExtendable
    default AuxiliaryLightManager getAuxLightManager(BlockPos pos)
```

**Parameters:**

- `pos` (`BlockPos`)

**Returns:** `.NonExtendable
    default AuxiliaryLightManager`

### getAuxLightManager

```java
default AuxiliaryLightManager getAuxLightManager(ChunkPos pos)
```

**Parameters:**

- `pos` (`ChunkPos`)

**Returns:** `AuxiliaryLightManager`

### getModelData

```java
default ModelData getModelData(BlockPos pos)
```

**Parameters:**

- `pos` (`BlockPos`)

**Returns:** `ModelData`
