# IBoatExtension

**Package:** `net.neoforged.neoforge.common.extensions`
**Type:** interface

## Methods

### self

```java
private Boat self()
```

**Returns:** `private Boat`

### canBoatInFluid

```java
default boolean canBoatInFluid(FluidState state)
```

**Parameters:**

- `state` (`FluidState`)

**Returns:** `boolean`

### canBoatInFluid

```java
default boolean canBoatInFluid(FluidType type)
```

**Parameters:**

- `type` (`FluidType`)

**Returns:** `boolean`
