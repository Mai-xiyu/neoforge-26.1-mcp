# PiglinNeutralArmorEntityPredicate

**Package:** `net.neoforged.neoforge.common.advancements.critereon`
**Type:** class
**Implements:** `EntitySubPredicate`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `INSTANCE` | `PiglinNeutralArmorEntityPredicate` |  |
| `CODEC` | `MapCodec<PiglinNeutralArmorEntityPredicate>` |  |

## Methods

### PiglinNeutralArmorEntityPredicate

```java
private PiglinNeutralArmorEntityPredicate()
```

**Returns:** `private`

### codec

```java
public MapCodec<PiglinNeutralArmorEntityPredicate> codec()
```

**Returns:** `MapCodec<PiglinNeutralArmorEntityPredicate>`

### matches

```java
public boolean matches(Entity entity, ServerLevel level, Vec3 position)
```

**Parameters:**

- `entity` (`Entity`)
- `level` (`ServerLevel`)
- `position` (`Vec3`)

**Returns:** `boolean`
