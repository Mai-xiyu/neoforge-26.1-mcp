# SnowBootsEntityPredicate

**Package:** `net.neoforged.neoforge.common.advancements.critereon`
**Type:** class
**Implements:** `EntitySubPredicate`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `INSTANCE` | `SnowBootsEntityPredicate` |  |
| `CODEC` | `MapCodec<SnowBootsEntityPredicate>` |  |

## Methods

### SnowBootsEntityPredicate

```java
private SnowBootsEntityPredicate()
```

**Returns:** `private`

### codec

```java
public MapCodec<SnowBootsEntityPredicate> codec()
```

**Returns:** `MapCodec<SnowBootsEntityPredicate>`

### matches

```java
public boolean matches(Entity entity, ServerLevel level, Vec3 position)
```

**Parameters:**

- `entity` (`Entity`)
- `level` (`ServerLevel`)
- `position` (`Vec3`)

**Returns:** `boolean`
