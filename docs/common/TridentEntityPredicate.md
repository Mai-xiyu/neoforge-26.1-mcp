# TridentEntityPredicate

**Package:** `net.neoforged.neoforge.common.advancements.critereon`
**Type:** class
**Implements:** `EntitySubPredicate`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `INSTANCE` | `TridentEntityPredicate` |  |
| `CODEC` | `MapCodec<TridentEntityPredicate>` |  |

## Methods

### TridentEntityPredicate

```java
private TridentEntityPredicate()
```

**Returns:** `private`

### codec

```java
public MapCodec<TridentEntityPredicate> codec()
```

**Returns:** `MapCodec<TridentEntityPredicate>`

### matches

```java
public boolean matches(Entity entity, ServerLevel level, Vec3 position)
```

**Parameters:**

- `entity` (`Entity`)
- `level` (`ServerLevel`)
- `position` (`Vec3`)

**Returns:** `boolean`
