# AxisCycle

**Package:** `net.minecraft.core`
**Type:** enum

## Fields

| Name | Type | Description |
|------|------|-------------|
| `AXIS_VALUES` | `Direction.Axis[]` |  |
| `VALUES` | `AxisCycle[]` |  |

## Methods

### cycle

```java
public int cycle(int p_121810_, int p_121811_, int p_121812_, Direction.Axis p_121813_)
```

**Parameters:**

- `p_121810_` (`int`)
- `p_121811_` (`int`)
- `p_121812_` (`int`)
- `p_121813_` (`Direction.Axis`)

**Returns:** `int`

### cycle

```java
public double cycle(double p_175242_, double p_175243_, double p_175244_, Direction.Axis p_175245_)
```

**Parameters:**

- `p_175242_` (`double`)
- `p_175243_` (`double`)
- `p_175244_` (`double`)
- `p_175245_` (`Direction.Axis`)

**Returns:** `double`

### cycle

```java
public Direction.Axis cycle(Direction.Axis p_121815_)
```

**Parameters:**

- `p_121815_` (`Direction.Axis`)

**Returns:** `Direction.Axis`

### inverse

```java
public AxisCycle inverse()
```

**Returns:** `AxisCycle`

### cycle

```java
public int cycle(int p_121821_, int p_121822_, int p_121823_, Direction.Axis p_121824_)
```

**Parameters:**

- `p_121821_` (`int`)
- `p_121822_` (`int`)
- `p_121823_` (`int`)
- `p_121824_` (`Direction.Axis`)

**Returns:** `int`

### cycle

```java
public double cycle(double p_175247_, double p_175248_, double p_175249_, Direction.Axis p_175250_)
```

**Parameters:**

- `p_175247_` (`double`)
- `p_175248_` (`double`)
- `p_175249_` (`double`)
- `p_175250_` (`Direction.Axis`)

**Returns:** `double`

### cycle

```java
public Direction.Axis cycle(Direction.Axis p_121826_)
```

**Parameters:**

- `p_121826_` (`Direction.Axis`)

**Returns:** `Direction.Axis`

### inverse

```java
public AxisCycle inverse()
```

**Returns:** `AxisCycle`

### cycle

```java
public int cycle(int p_121832_, int p_121833_, int p_121834_, Direction.Axis p_121835_)
```

**Parameters:**

- `p_121832_` (`int`)
- `p_121833_` (`int`)
- `p_121834_` (`int`)
- `p_121835_` (`Direction.Axis`)

**Returns:** `int`

### cycle

```java
public double cycle(double p_175252_, double p_175253_, double p_175254_, Direction.Axis p_175255_)
```

**Parameters:**

- `p_175252_` (`double`)
- `p_175253_` (`double`)
- `p_175254_` (`double`)
- `p_175255_` (`Direction.Axis`)

**Returns:** `double`

### cycle

```java
public Direction.Axis cycle(Direction.Axis p_121837_)
```

**Parameters:**

- `p_121837_` (`Direction.Axis`)

**Returns:** `Direction.Axis`

### inverse

```java
public AxisCycle inverse()
```

**Returns:** `AxisCycle`

### cycle

```java
public abstract int cycle(int p_121794_, int p_121795_, int p_121796_, Direction.Axis p_121797_)
```

**Parameters:**

- `p_121794_` (`int`)
- `p_121795_` (`int`)
- `p_121796_` (`int`)
- `p_121797_` (`Direction.Axis`)

**Returns:** `public abstract int`

### cycle

```java
public abstract double cycle(double p_175236_, double p_175237_, double p_175238_, Direction.Axis p_175239_)
```

**Parameters:**

- `p_175236_` (`double`)
- `p_175237_` (`double`)
- `p_175238_` (`double`)
- `p_175239_` (`Direction.Axis`)

**Returns:** `public abstract double`

### cycle

```java
public abstract Direction.Axis cycle(Direction.Axis p_121798_)
```

**Parameters:**

- `p_121798_` (`Direction.Axis`)

**Returns:** `public abstract Direction.Axis`

### inverse

```java
public abstract AxisCycle inverse()
```

**Returns:** `public abstract AxisCycle`

### between

```java
public static AxisCycle between(Direction.Axis p_121800_, Direction.Axis p_121801_)
```

**Parameters:**

- `p_121800_` (`Direction.Axis`)
- `p_121801_` (`Direction.Axis`)

**Returns:** `public static AxisCycle`
