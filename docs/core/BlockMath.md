# BlockMath

**Package:** `net.minecraft.core`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `VANILLA_UV_TRANSFORM_LOCAL_TO_GLOBAL` | `Map<Direction, Transformation>` |  |
| `VANILLA_UV_TRANSFORM_GLOBAL_TO_LOCAL` | `Map<Direction, Transformation>` |  |

## Methods

### blockCenterToCorner

```java
public static Transformation blockCenterToCorner(Transformation p_121843_)
```

**Parameters:**

- `p_121843_` (`Transformation`)

**Returns:** `public static Transformation`

### Transformation

```java
return new Transformation()
```

**Returns:** `return new`

### blockCornerToCenter

```java
public static Transformation blockCornerToCenter(Transformation p_175260_)
```

**Parameters:**

- `p_175260_` (`Transformation`)

**Returns:** `public static Transformation`

### Transformation

```java
return new Transformation()
```

**Returns:** `return new`

### getUVLockTransform

```java
public static Transformation getUVLockTransform(Transformation p_121845_, Direction p_121846_)
```

**Parameters:**

- `p_121845_` (`Transformation`)
- `p_121846_` (`Direction`)

**Returns:** `public static Transformation`

### blockCenterToCorner

```java
return blockCenterToCorner()
```

**Returns:** `return`
