# DefaultDispenseItemBehavior

**Package:** `net.minecraft.core.dispenser`
**Type:** class
**Implements:** `DispenseItemBehavior`

## Methods

### dispense

```java
public final ItemStack dispense(BlockSource p_302432_, ItemStack p_123392_)
```

**Parameters:**

- `p_302432_` (`BlockSource`)
- `p_123392_` (`ItemStack`)

**Returns:** `ItemStack`

### execute

```java
protected ItemStack execute(BlockSource p_302420_, ItemStack p_123386_)
```

**Parameters:**

- `p_302420_` (`BlockSource`)
- `p_123386_` (`ItemStack`)

**Returns:** `protected ItemStack`

### spawnItem

```java
public static void spawnItem(Level p_123379_, ItemStack p_123380_, int p_123381_, Direction p_123382_, Position p_123383_)
```

**Parameters:**

- `p_123379_` (`Level`)
- `p_123380_` (`ItemStack`)
- `p_123381_` (`int`)
- `p_123382_` (`Direction`)
- `p_123383_` (`Position`)

**Returns:** `public static void`

### playSound

```java
protected void playSound(BlockSource p_302471_)
```

**Parameters:**

- `p_302471_` (`BlockSource`)

**Returns:** `protected void`

### playDefaultSound

```java
 playDefaultSound()
```

**Returns:** ``

### playAnimation

```java
protected void playAnimation(BlockSource p_302462_, Direction p_123389_)
```

**Parameters:**

- `p_302462_` (`BlockSource`)
- `p_123389_` (`Direction`)

**Returns:** `protected void`

### playDefaultAnimation

```java
 playDefaultAnimation()
```

**Returns:** ``

### playDefaultSound

```java
private static void playDefaultSound(BlockSource p_347476_)
```

**Parameters:**

- `p_347476_` (`BlockSource`)

**Returns:** `private static void`

### playDefaultAnimation

```java
private static void playDefaultAnimation(BlockSource p_347531_, Direction p_347570_)
```

**Parameters:**

- `p_347531_` (`BlockSource`)
- `p_347570_` (`Direction`)

**Returns:** `private static void`

### consumeWithRemainder

```java
protected ItemStack consumeWithRemainder(BlockSource p_347658_, ItemStack p_347682_, ItemStack p_347670_)
```

**Parameters:**

- `p_347658_` (`BlockSource`)
- `p_347682_` (`ItemStack`)
- `p_347670_` (`ItemStack`)

**Returns:** `protected ItemStack`

### addToInventoryOrDispense

```java
private void addToInventoryOrDispense(BlockSource p_347634_, ItemStack p_347604_)
```

**Parameters:**

- `p_347634_` (`BlockSource`)
- `p_347604_` (`ItemStack`)

**Returns:** `private void`

### playDefaultSound

```java
 playDefaultSound()
```

**Returns:** ``

### playDefaultAnimation

```java
 playDefaultAnimation()
```

**Returns:** ``
