# DispenseItemBehavior

**Package:** `net.minecraft.core.dispenser`
**Type:** interface

## Methods

### dispense

```java
ItemStack dispense(BlockSource p_302445_, ItemStack p_123404_)
```

**Parameters:**

- `p_302445_` (`BlockSource`)
- `p_123404_` (`ItemStack`)

**Returns:** `ItemStack`

### bootStrap

```java
static void bootStrap()
```

**Returns:** `static void`

### execute

```java
public ItemStack execute(BlockSource p_338275_, ItemStack p_338216_)
```

**Parameters:**

- `p_338275_` (`BlockSource`)
- `p_338216_` (`ItemStack`)

**Returns:** `ItemStack`

### DefaultDispenseItemBehavior

```java
Items.ARMOR_STAND,
            new DefaultDispenseItemBehavior()
```

**Returns:** `Items.ARMOR_STAND,
            new`

### execute

```java
public ItemStack execute(BlockSource p_338813_, ItemStack p_338809_)
```

**Parameters:**

- `p_338813_` (`BlockSource`)
- `p_338809_` (`ItemStack`)

**Returns:** `ItemStack`

### OptionalDispenseItemBehavior

```java
Items.SADDLE,
            new OptionalDispenseItemBehavior()
```

**Returns:** `Items.SADDLE,
            new`

### execute

```java
public ItemStack execute(BlockSource p_338360_, ItemStack p_338306_)
```

**Parameters:**

- `p_338360_` (`BlockSource`)
- `p_338306_` (`ItemStack`)

**Returns:** `ItemStack`

### execute

```java
protected ItemStack execute(BlockSource p_338193_, ItemStack p_338600_)
```

**Parameters:**

- `p_338193_` (`BlockSource`)
- `p_338600_` (`ItemStack`)

**Returns:** `ItemStack`

### OptionalDispenseItemBehavior

```java
Items.CHEST,
            new OptionalDispenseItemBehavior()
```

**Returns:** `Items.CHEST,
            new`

### execute

```java
public ItemStack execute(BlockSource p_338297_, ItemStack p_338735_)
```

**Parameters:**

- `p_338297_` (`BlockSource`)
- `p_338735_` (`ItemStack`)

**Returns:** `ItemStack`

### execute

```java
public ItemStack execute(BlockSource p_338850_, ItemStack p_338251_)
```

**Parameters:**

- `p_338850_` (`BlockSource`)
- `p_338251_` (`ItemStack`)

**Returns:** `ItemStack`

### DefaultDispenseItemBehavior

```java
Items.BUCKET, new DefaultDispenseItemBehavior()
```

**Returns:** `Items.BUCKET, new`

### execute

```java
public ItemStack execute(BlockSource p_338386_, ItemStack p_338526_)
```

**Parameters:**

- `p_338386_` (`BlockSource`)
- `p_338526_` (`ItemStack`)

**Returns:** `ItemStack`

### OptionalDispenseItemBehavior

```java
Items.FLINT_AND_STEEL, new OptionalDispenseItemBehavior()
```

**Returns:** `Items.FLINT_AND_STEEL, new`

### execute

```java
protected ItemStack execute(BlockSource p_338494_, ItemStack p_338444_)
```

**Parameters:**

- `p_338494_` (`BlockSource`)
- `p_338444_` (`ItemStack`)

**Returns:** `ItemStack`

### OptionalDispenseItemBehavior

```java
Items.BONE_MEAL, new OptionalDispenseItemBehavior()
```

**Returns:** `Items.BONE_MEAL, new`

### execute

```java
protected ItemStack execute(BlockSource p_302450_, ItemStack p_123524_)
```

**Parameters:**

- `p_302450_` (`BlockSource`)
- `p_123524_` (`ItemStack`)

**Returns:** `ItemStack`

### DefaultDispenseItemBehavior

```java
Blocks.TNT, new DefaultDispenseItemBehavior()
```

**Returns:** `Blocks.TNT, new`

### execute

```java
protected ItemStack execute(BlockSource p_302430_, ItemStack p_123462_)
```

**Parameters:**

- `p_302430_` (`BlockSource`)
- `p_123462_` (`ItemStack`)

**Returns:** `ItemStack`

### execute

```java
protected ItemStack execute(BlockSource p_302463_, ItemStack p_123530_)
```

**Parameters:**

- `p_302463_` (`BlockSource`)
- `p_123530_` (`ItemStack`)

**Returns:** `ItemStack`

### OptionalDispenseItemBehavior

```java
Items.WITHER_SKELETON_SKULL,
            new OptionalDispenseItemBehavior()
```

**Returns:** `Items.WITHER_SKELETON_SKULL,
            new`

### execute

```java
protected ItemStack execute(BlockSource p_302425_, ItemStack p_123536_)
```

**Parameters:**

- `p_302425_` (`BlockSource`)
- `p_123536_` (`ItemStack`)

**Returns:** `ItemStack`

### OptionalDispenseItemBehavior

```java
Blocks.CARVED_PUMPKIN, new OptionalDispenseItemBehavior()
```

**Returns:** `Blocks.CARVED_PUMPKIN, new`

### execute

```java
protected ItemStack execute(BlockSource p_302452_, ItemStack p_123542_)
```

**Parameters:**

- `p_302452_` (`BlockSource`)
- `p_123542_` (`ItemStack`)

**Returns:** `ItemStack`

### OptionalDispenseItemBehavior

```java
,
            new OptionalDispenseItemBehavior()
```

**Returns:** `,
            new`

### takeLiquid

```java
private ItemStack takeLiquid(BlockSource p_338528_, ItemStack p_338340_, ItemStack p_338879_)
```

**Parameters:**

- `p_338528_` (`BlockSource`)
- `p_338340_` (`ItemStack`)
- `p_338879_` (`ItemStack`)

**Returns:** `private ItemStack`

### execute

```java
public ItemStack execute(BlockSource p_302433_, ItemStack p_123548_)
```

**Parameters:**

- `p_302433_` (`BlockSource`)
- `p_123548_` (`ItemStack`)

**Returns:** `ItemStack`

### OptionalDispenseItemBehavior

```java
Items.GLOWSTONE, new OptionalDispenseItemBehavior()
```

**Returns:** `Items.GLOWSTONE, new`

### execute

```java
public ItemStack execute(BlockSource p_302423_, ItemStack p_123557_)
```

**Parameters:**

- `p_302423_` (`BlockSource`)
- `p_123557_` (`ItemStack`)

**Returns:** `ItemStack`

### OptionalDispenseItemBehavior

```java
, new OptionalDispenseItemBehavior()
```

**Returns:** `, new`

### execute

```java
protected ItemStack execute(BlockSource p_302435_, ItemStack p_123562_)
```

**Parameters:**

- `p_302435_` (`BlockSource`)
- `p_123562_` (`ItemStack`)

**Returns:** `ItemStack`

### OptionalDispenseItemBehavior

```java
Items.HONEYCOMB, new OptionalDispenseItemBehavior()
```

**Returns:** `Items.HONEYCOMB, new`

### execute

```java
public ItemStack execute(BlockSource p_302464_, ItemStack p_123567_)
```

**Parameters:**

- `p_302464_` (`BlockSource`)
- `p_123567_` (`ItemStack`)

**Returns:** `ItemStack`

### DefaultDispenseItemBehavior

```java
Items.POTION,
            new DefaultDispenseItemBehavior()
```

**Returns:** `Items.POTION,
            new`

### execute

```java
public ItemStack execute(BlockSource p_302453_, ItemStack p_123413_)
```

**Parameters:**

- `p_302453_` (`BlockSource`)
- `p_123413_` (`ItemStack`)

**Returns:** `ItemStack`
