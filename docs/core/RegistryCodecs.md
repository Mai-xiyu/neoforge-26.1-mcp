# RegistryCodecs

**Package:** `net.minecraft.core`
**Type:** class

## Methods

### homogeneousList

```java
<E> public static <E> Codec<HolderSet<E>> homogeneousList(ResourceKey<? extends Registry<E>> p_206280_, Codec<E> p_206281_)
```

**Parameters:**

- `p_206280_` (`ResourceKey<? extends Registry<E>>`)
- `p_206281_` (`Codec<E>`)

**Returns:** `public static <E> Codec<HolderSet<E>>`

### homogeneousList

```java
return homogeneousList()
```

**Returns:** `return`

### homogeneousList

```java
<E> public static <E> Codec<HolderSet<E>> homogeneousList(ResourceKey<? extends Registry<E>> p_206288_, Codec<E> p_206289_, boolean p_206290_)
```

**Parameters:**

- `p_206288_` (`ResourceKey<? extends Registry<E>>`)
- `p_206289_` (`Codec<E>`)
- `p_206290_` (`boolean`)

**Returns:** `public static <E> Codec<HolderSet<E>>`

### homogeneousList

```java
<E> public static <E> Codec<HolderSet<E>> homogeneousList(ResourceKey<? extends Registry<E>> p_206278_)
```

**Parameters:**

- `p_206278_` (`ResourceKey<? extends Registry<E>>`)

**Returns:** `public static <E> Codec<HolderSet<E>>`

### homogeneousList

```java
return homogeneousList()
```

**Returns:** `return`

### homogeneousList

```java
<E> public static <E> Codec<HolderSet<E>> homogeneousList(ResourceKey<? extends Registry<E>> p_206311_, boolean p_206312_)
```

**Parameters:**

- `p_206311_` (`ResourceKey<? extends Registry<E>>`)
- `p_206312_` (`boolean`)

**Returns:** `public static <E> Codec<HolderSet<E>>`
