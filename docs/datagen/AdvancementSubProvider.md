# AdvancementSubProvider

**Package:** `net.minecraft.data.advancements`
**Type:** interface

## Methods

### generate

```java
void generate(HolderLookup.Provider p_255901_, Consumer<AdvancementHolder> p_250888_)
```

**Parameters:**

- `p_255901_` (`HolderLookup.Provider`)
- `p_250888_` (`Consumer<AdvancementHolder>`)

### createPlaceholder

```java
static AdvancementHolder createPlaceholder(String p_314470_)
```

**Parameters:**

- `p_314470_` (`String`)

**Returns:** `static AdvancementHolder`
