# BiomeTagsProvider

**Package:** `net.minecraft.data.tags`
**Type:** class
**Extends:** `TagsProvider<Biome>`

## Methods

### BiomeTagsProvider

```java
public public BiomeTagsProvider(PackOutput p_255800_, CompletableFuture<HolderLookup.Provider> p_256205_)
```

**Parameters:**

- `p_255800_` (`PackOutput`)
- `p_256205_` (`CompletableFuture<HolderLookup.Provider>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### BiomeTagsProvider

```java
public BiomeTagsProvider(PackOutput p_255800_, CompletableFuture<HolderLookup.Provider> p_256205_, String modId, .jetbrains.annotations.Nullable net.neoforged.neoforge.common.data.ExistingFileHelper existingFileHelper)
```

**Parameters:**

- `p_255800_` (`PackOutput`)
- `p_256205_` (`CompletableFuture<HolderLookup.Provider>`)
- `modId` (`String`)
- `existingFileHelper` (`.jetbrains.annotations.Nullable net.neoforged.neoforge.common.data.ExistingFileHelper`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### addTags

```java
protected void addTags(HolderLookup.Provider p_256485_)
```

**Parameters:**

- `p_256485_` (`HolderLookup.Provider`)
