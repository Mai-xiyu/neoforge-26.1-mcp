# BlockModelGenerators

**Package:** `net.minecraft.data.models`
**Type:** class

## Methods

### createMirroredCubeGenerator

```java
private static BlockStateGenerator createMirroredCubeGenerator(Block p_176110_, ResourceLocation p_176111_, TextureMapping p_176112_, BiConsumer<ResourceLocation, Supplier<JsonElement>> p_176113_)
```

**Parameters:**

- `p_176110_` (`Block`)
- `p_176111_` (`ResourceLocation`)
- `p_176112_` (`TextureMapping`)
- `p_176113_` (`BiConsumer<ResourceLocation, Supplier<JsonElement>>`)

**Returns:** `private static BlockStateGenerator`

### createRotatedVariant

```java
return createRotatedVariant()
```

**Returns:** `return`

### createNorthWestMirroredCubeGenerator

```java
private static BlockStateGenerator createNorthWestMirroredCubeGenerator(Block p_236317_, ResourceLocation p_236318_, TextureMapping p_236319_, BiConsumer<ResourceLocation, Supplier<JsonElement>> p_236320_)
```

**Parameters:**

- `p_236317_` (`Block`)
- `p_236318_` (`ResourceLocation`)
- `p_236319_` (`TextureMapping`)
- `p_236320_` (`BiConsumer<ResourceLocation, Supplier<JsonElement>>`)

**Returns:** `private static BlockStateGenerator`

### createSimpleBlock

```java
return createSimpleBlock()
```

**Returns:** `return`

### createMirroredColumnGenerator

```java
private static BlockStateGenerator createMirroredColumnGenerator(Block p_176180_, ResourceLocation p_176181_, TextureMapping p_176182_, BiConsumer<ResourceLocation, Supplier<JsonElement>> p_176183_)
```

**Parameters:**

- `p_176180_` (`Block`)
- `p_176181_` (`ResourceLocation`)
- `p_176182_` (`TextureMapping`)
- `p_176183_` (`BiConsumer<ResourceLocation, Supplier<JsonElement>>`)

**Returns:** `private static BlockStateGenerator`

### BlockModelGenerators

```java
public BlockModelGenerators(Consumer<BlockStateGenerator> p_124481_, BiConsumer<ResourceLocation, Supplier<JsonElement>> p_124482_, Consumer<Item> p_124483_)
```

**Parameters:**

- `p_124481_` (`Consumer<BlockStateGenerator>`)
- `p_124482_` (`BiConsumer<ResourceLocation, Supplier<JsonElement>>`)
- `p_124483_` (`Consumer<Item>`)

**Returns:** `public`

### skipAutoItemBlock

```java
void skipAutoItemBlock(Block p_124525_)
```

**Parameters:**

- `p_124525_` (`Block`)

### delegateItemModel

```java
void delegateItemModel(Block p_124798_, ResourceLocation p_124799_)
```

**Parameters:**

- `p_124798_` (`Block`)
- `p_124799_` (`ResourceLocation`)

### delegateItemModel

```java
private void delegateItemModel(Item p_124520_, ResourceLocation p_124521_)
```

**Parameters:**

- `p_124520_` (`Item`)
- `p_124521_` (`ResourceLocation`)

**Returns:** `private void`

### createSimpleFlatItemModel

```java
void createSimpleFlatItemModel(Item p_124518_)
```

**Parameters:**

- `p_124518_` (`Item`)

### createSimpleFlatItemModel

```java
private void createSimpleFlatItemModel(Block p_124729_)
```

**Parameters:**

- `p_124729_` (`Block`)

**Returns:** `private void`

### createSimpleFlatItemModel

```java
private void createSimpleFlatItemModel(Block p_124576_, String p_124577_)
```

**Parameters:**

- `p_124576_` (`Block`)
- `p_124577_` (`String`)

**Returns:** `private void`

### createHorizontalFacingDispatch

```java
private static PropertyDispatch createHorizontalFacingDispatch()
```

**Returns:** `private static PropertyDispatch`

### createHorizontalFacingDispatchAlt

```java
private static PropertyDispatch createHorizontalFacingDispatchAlt()
```

**Returns:** `private static PropertyDispatch`

### createTorchHorizontalDispatch

```java
private static PropertyDispatch createTorchHorizontalDispatch()
```

**Returns:** `private static PropertyDispatch`

### createFacingDispatch

```java
private static PropertyDispatch createFacingDispatch()
```

**Returns:** `private static PropertyDispatch`

### createRotatedVariant

```java
private static MultiVariantGenerator createRotatedVariant(Block p_124832_, ResourceLocation p_124833_)
```

**Parameters:**

- `p_124832_` (`Block`)
- `p_124833_` (`ResourceLocation`)

**Returns:** `private static MultiVariantGenerator`

### createRotatedVariants

```java
private static Variant[] createRotatedVariants(ResourceLocation p_124689_)
```

**Parameters:**

- `p_124689_` (`ResourceLocation`)

**Returns:** `private static Variant[]`

### createRotatedVariant

```java
private static MultiVariantGenerator createRotatedVariant(Block p_124863_, ResourceLocation p_124864_, ResourceLocation p_124865_)
```

**Parameters:**

- `p_124863_` (`Block`)
- `p_124864_` (`ResourceLocation`)
- `p_124865_` (`ResourceLocation`)

**Returns:** `private static MultiVariantGenerator`

### createBooleanModelDispatch

```java
private static PropertyDispatch createBooleanModelDispatch(BooleanProperty p_124623_, ResourceLocation p_124624_, ResourceLocation p_124625_)
```

**Parameters:**

- `p_124623_` (`BooleanProperty`)
- `p_124624_` (`ResourceLocation`)
- `p_124625_` (`ResourceLocation`)

**Returns:** `private static PropertyDispatch`

### createRotatedMirroredVariantBlock

```java
private void createRotatedMirroredVariantBlock(Block p_124787_)
```

**Parameters:**

- `p_124787_` (`Block`)

**Returns:** `private void`

### createRotatedVariantBlock

```java
private void createRotatedVariantBlock(Block p_124824_)
```

**Parameters:**

- `p_124824_` (`Block`)

**Returns:** `private void`

### createBrushableBlock

```java
private void createBrushableBlock(Block p_277651_)
```

**Parameters:**

- `p_277651_` (`Block`)

**Returns:** `private void`

### createButton

```java
static BlockStateGenerator createButton(Block p_124885_, ResourceLocation p_124886_, ResourceLocation p_124887_)
```

**Parameters:**

- `p_124885_` (`Block`)
- `p_124886_` (`ResourceLocation`)
- `p_124887_` (`ResourceLocation`)

**Returns:** `static BlockStateGenerator`

### configureDoorHalf

```java
private static PropertyDispatch.C4<Direction, DoubleBlockHalf, DoorHingeSide, Boolean> configureDoorHalf(PropertyDispatch.C4<Direction, DoubleBlockHalf, DoorHingeSide, Boolean> p_236305_, DoubleBlockHalf p_236306_, ResourceLocation p_236307_, ResourceLocation p_236308_, ResourceLocation p_236309_, ResourceLocation p_236310_)
```

**Parameters:**

- `p_236305_` (`PropertyDispatch.C4<Direction, DoubleBlockHalf, DoorHingeSide, Boolean>`)
- `p_236306_` (`DoubleBlockHalf`)
- `p_236307_` (`ResourceLocation`)
- `p_236308_` (`ResourceLocation`)
- `p_236309_` (`ResourceLocation`)
- `p_236310_` (`ResourceLocation`)

**Returns:** `private static PropertyDispatch.C4<Direction, DoubleBlockHalf, DoorHingeSide, Boolean>`

### createDoor

```java
private static BlockStateGenerator createDoor(Block p_236284_, ResourceLocation p_236285_, ResourceLocation p_236286_, ResourceLocation p_236287_, ResourceLocation p_236288_, ResourceLocation p_236289_, ResourceLocation p_236290_, ResourceLocation p_236291_, ResourceLocation p_236292_)
```

**Parameters:**

- `p_236284_` (`Block`)
- `p_236285_` (`ResourceLocation`)
- `p_236286_` (`ResourceLocation`)
- `p_236287_` (`ResourceLocation`)
- `p_236288_` (`ResourceLocation`)
- `p_236289_` (`ResourceLocation`)
- `p_236290_` (`ResourceLocation`)
- `p_236291_` (`ResourceLocation`)
- `p_236292_` (`ResourceLocation`)

**Returns:** `private static BlockStateGenerator`

### createCustomFence

```java
static BlockStateGenerator createCustomFence(Block p_248625_, ResourceLocation p_248654_, ResourceLocation p_249827_, ResourceLocation p_248819_, ResourceLocation p_251062_, ResourceLocation p_249076_)
```

**Parameters:**

- `p_248625_` (`Block`)
- `p_248654_` (`ResourceLocation`)
- `p_249827_` (`ResourceLocation`)
- `p_248819_` (`ResourceLocation`)
- `p_251062_` (`ResourceLocation`)
- `p_249076_` (`ResourceLocation`)

**Returns:** `static BlockStateGenerator`

### createFence

```java
static BlockStateGenerator createFence(Block p_124905_, ResourceLocation p_124906_, ResourceLocation p_124907_)
```

**Parameters:**

- `p_124905_` (`Block`)
- `p_124906_` (`ResourceLocation`)
- `p_124907_` (`ResourceLocation`)

**Returns:** `static BlockStateGenerator`

### createWall

```java
static BlockStateGenerator createWall(Block p_124839_, ResourceLocation p_124840_, ResourceLocation p_124841_, ResourceLocation p_124842_)
```

**Parameters:**

- `p_124839_` (`Block`)
- `p_124840_` (`ResourceLocation`)
- `p_124841_` (`ResourceLocation`)
- `p_124842_` (`ResourceLocation`)

**Returns:** `static BlockStateGenerator`

### createFenceGate

```java
static BlockStateGenerator createFenceGate(Block p_124810_, ResourceLocation p_124811_, ResourceLocation p_124812_, ResourceLocation p_124813_, ResourceLocation p_124814_, boolean p_251730_)
```

**Parameters:**

- `p_124810_` (`Block`)
- `p_124811_` (`ResourceLocation`)
- `p_124812_` (`ResourceLocation`)
- `p_124813_` (`ResourceLocation`)
- `p_124814_` (`ResourceLocation`)
- `p_251730_` (`boolean`)

**Returns:** `static BlockStateGenerator`

### createStairs

```java
static BlockStateGenerator createStairs(Block p_124867_, ResourceLocation p_124868_, ResourceLocation p_124869_, ResourceLocation p_124870_)
```

**Parameters:**

- `p_124867_` (`Block`)
- `p_124868_` (`ResourceLocation`)
- `p_124869_` (`ResourceLocation`)
- `p_124870_` (`ResourceLocation`)

**Returns:** `static BlockStateGenerator`

### createOrientableTrapdoor

```java
private static BlockStateGenerator createOrientableTrapdoor(Block p_124889_, ResourceLocation p_124890_, ResourceLocation p_124891_, ResourceLocation p_124892_)
```

**Parameters:**

- `p_124889_` (`Block`)
- `p_124890_` (`ResourceLocation`)
- `p_124891_` (`ResourceLocation`)
- `p_124892_` (`ResourceLocation`)

**Returns:** `private static BlockStateGenerator`

### createTrapdoor

```java
private static BlockStateGenerator createTrapdoor(Block p_124909_, ResourceLocation p_124910_, ResourceLocation p_124911_, ResourceLocation p_124912_)
```

**Parameters:**

- `p_124909_` (`Block`)
- `p_124910_` (`ResourceLocation`)
- `p_124911_` (`ResourceLocation`)
- `p_124912_` (`ResourceLocation`)

**Returns:** `private static BlockStateGenerator`

### createSimpleBlock

```java
static MultiVariantGenerator createSimpleBlock(Block p_124860_, ResourceLocation p_124861_)
```

**Parameters:**

- `p_124860_` (`Block`)
- `p_124861_` (`ResourceLocation`)

**Returns:** `static MultiVariantGenerator`

### createRotatedPillar

```java
private static PropertyDispatch createRotatedPillar()
```

**Returns:** `private static PropertyDispatch`

### createPillarBlockUVLocked

```java
static BlockStateGenerator createPillarBlockUVLocked(Block p_259670_, TextureMapping p_259852_, BiConsumer<ResourceLocation, Supplier<JsonElement>> p_259181_)
```

**Parameters:**

- `p_259670_` (`Block`)
- `p_259852_` (`TextureMapping`)
- `p_259181_` (`BiConsumer<ResourceLocation, Supplier<JsonElement>>`)

**Returns:** `static BlockStateGenerator`

### createAxisAlignedPillarBlock

```java
static BlockStateGenerator createAxisAlignedPillarBlock(Block p_124882_, ResourceLocation p_124883_)
```

**Parameters:**

- `p_124882_` (`Block`)
- `p_124883_` (`ResourceLocation`)

**Returns:** `static BlockStateGenerator`

### createAxisAlignedPillarBlockCustomModel

```java
private void createAxisAlignedPillarBlockCustomModel(Block p_124902_, ResourceLocation p_124903_)
```

**Parameters:**

- `p_124902_` (`Block`)
- `p_124903_` (`ResourceLocation`)

**Returns:** `private void`

### createAxisAlignedPillarBlock

```java
public void createAxisAlignedPillarBlock(Block p_124587_, TexturedModel.Provider p_124588_)
```

**Parameters:**

- `p_124587_` (`Block`)
- `p_124588_` (`TexturedModel.Provider`)

**Returns:** `public void`

### createHorizontallyRotatedBlock

```java
private void createHorizontallyRotatedBlock(Block p_124745_, TexturedModel.Provider p_124746_)
```

**Parameters:**

- `p_124745_` (`Block`)
- `p_124746_` (`TexturedModel.Provider`)

**Returns:** `private void`

### createRotatedPillarWithHorizontalVariant

```java
static BlockStateGenerator createRotatedPillarWithHorizontalVariant(Block p_124925_, ResourceLocation p_124926_, ResourceLocation p_124927_)
```

**Parameters:**

- `p_124925_` (`Block`)
- `p_124926_` (`ResourceLocation`)
- `p_124927_` (`ResourceLocation`)

**Returns:** `static BlockStateGenerator`

### createRotatedPillarWithHorizontalVariant

```java
private void createRotatedPillarWithHorizontalVariant(Block p_124590_, TexturedModel.Provider p_124591_, TexturedModel.Provider p_124592_)
```

**Parameters:**

- `p_124590_` (`Block`)
- `p_124591_` (`TexturedModel.Provider`)
- `p_124592_` (`TexturedModel.Provider`)

**Returns:** `private void`

### createSuffixedVariant

```java
private ResourceLocation createSuffixedVariant(Block p_124579_, String p_124580_, ModelTemplate p_124581_, Function<ResourceLocation, TextureMapping> p_124582_)
```

**Parameters:**

- `p_124579_` (`Block`)
- `p_124580_` (`String`)
- `p_124581_` (`ModelTemplate`)
- `p_124582_` (`Function<ResourceLocation, TextureMapping>`)

**Returns:** `private ResourceLocation`

### createPressurePlate

```java
static BlockStateGenerator createPressurePlate(Block p_124942_, ResourceLocation p_124943_, ResourceLocation p_124944_)
```

**Parameters:**

- `p_124942_` (`Block`)
- `p_124943_` (`ResourceLocation`)
- `p_124944_` (`ResourceLocation`)

**Returns:** `static BlockStateGenerator`

### createSlab

```java
static BlockStateGenerator createSlab(Block p_124929_, ResourceLocation p_124930_, ResourceLocation p_124931_, ResourceLocation p_124932_)
```

**Parameters:**

- `p_124929_` (`Block`)
- `p_124930_` (`ResourceLocation`)
- `p_124931_` (`ResourceLocation`)
- `p_124932_` (`ResourceLocation`)

**Returns:** `static BlockStateGenerator`

### createTrivialCube

```java
public void createTrivialCube(Block p_124852_)
```

**Parameters:**

- `p_124852_` (`Block`)

**Returns:** `public void`

### createTrivialBlock

```java
public void createTrivialBlock(Block p_124795_, TexturedModel.Provider p_124796_)
```

**Parameters:**

- `p_124795_` (`Block`)
- `p_124796_` (`TexturedModel.Provider`)

**Returns:** `public void`

### createTrivialBlock

```java
private void createTrivialBlock(Block p_124568_, TextureMapping p_124569_, ModelTemplate p_124570_)
```

**Parameters:**

- `p_124568_` (`Block`)
- `p_124569_` (`TextureMapping`)
- `p_124570_` (`ModelTemplate`)

**Returns:** `private void`

### family

```java
private BlockModelGenerators.BlockFamilyProvider family(Block p_124877_)
```

**Parameters:**

- `p_124877_` (`Block`)

**Returns:** `private BlockModelGenerators.BlockFamilyProvider`

### createHangingSign

```java
public void createHangingSign(Block p_249023_, Block p_250861_, Block p_250943_)
```

**Parameters:**

- `p_249023_` (`Block`)
- `p_250861_` (`Block`)
- `p_250943_` (`Block`)

**Returns:** `public void`

### createDoor

```java
void createDoor(Block p_124897_)
```

**Parameters:**

- `p_124897_` (`Block`)

### copyDoorModel

```java
private void copyDoorModel(Block p_308919_, Block p_308994_)
```

**Parameters:**

- `p_308919_` (`Block`)
- `p_308994_` (`Block`)

**Returns:** `private void`

### createOrientableTrapdoor

```java
void createOrientableTrapdoor(Block p_124917_)
```

**Parameters:**

- `p_124917_` (`Block`)

### createTrapdoor

```java
void createTrapdoor(Block p_124937_)
```

**Parameters:**

- `p_124937_` (`Block`)

### copyTrapdoorModel

```java
private void copyTrapdoorModel(Block p_309079_, Block p_309124_)
```

**Parameters:**

- `p_309079_` (`Block`)
- `p_309124_` (`Block`)

**Returns:** `private void`

### createBigDripLeafBlock

```java
private void createBigDripLeafBlock()
```

**Returns:** `private void`

### woodProvider

```java
private BlockModelGenerators.WoodProvider woodProvider(Block p_124949_)
```

**Parameters:**

- `p_124949_` (`Block`)

**Returns:** `private BlockModelGenerators.WoodProvider`

### createNonTemplateModelBlock

```java
private void createNonTemplateModelBlock(Block p_124961_)
```

**Parameters:**

- `p_124961_` (`Block`)

**Returns:** `private void`

### createNonTemplateModelBlock

```java
private void createNonTemplateModelBlock(Block p_124534_, Block p_124535_)
```

**Parameters:**

- `p_124534_` (`Block`)
- `p_124535_` (`Block`)

**Returns:** `private void`

### createCrossBlockWithDefaultItem

```java
private void createCrossBlockWithDefaultItem(Block p_124558_, BlockModelGenerators.TintState p_124559_)
```

**Parameters:**

- `p_124558_` (`Block`)
- `p_124559_` (`BlockModelGenerators.TintState`)

**Returns:** `private void`

### createCrossBlockWithDefaultItem

```java
private void createCrossBlockWithDefaultItem(Block p_124561_, BlockModelGenerators.TintState p_124562_, TextureMapping p_124563_)
```

**Parameters:**

- `p_124561_` (`Block`)
- `p_124562_` (`BlockModelGenerators.TintState`)
- `p_124563_` (`TextureMapping`)

**Returns:** `private void`

### createCrossBlock

```java
private void createCrossBlock(Block p_124738_, BlockModelGenerators.TintState p_124739_)
```

**Parameters:**

- `p_124738_` (`Block`)
- `p_124739_` (`BlockModelGenerators.TintState`)

**Returns:** `private void`

### createCrossBlock

```java
private void createCrossBlock(Block p_124741_, BlockModelGenerators.TintState p_124742_, TextureMapping p_124743_)
```

**Parameters:**

- `p_124741_` (`Block`)
- `p_124742_` (`BlockModelGenerators.TintState`)
- `p_124743_` (`TextureMapping`)

**Returns:** `private void`

### createCrossBlock

```java
private void createCrossBlock(Block p_273533_, BlockModelGenerators.TintState p_273521_, Property<Integer> p_273430_, int[]... p_273001_)
```

**Parameters:**

- `p_273533_` (`Block`)
- `p_273521_` (`BlockModelGenerators.TintState`)
- `p_273430_` (`Property<Integer>`)
- `p_273001_` (`int[]...`)

**Returns:** `private void`

### IllegalArgumentException

```java
throw new IllegalArgumentException("missing values for property: " + p_273430_)
```

**Parameters:**

- `p_273430_` (`"missing values for property: " +`)

**Returns:** `throw new`

### createPlant

```java
private void createPlant(Block p_124546_, Block p_124547_, BlockModelGenerators.TintState p_124548_)
```

**Parameters:**

- `p_124546_` (`Block`)
- `p_124547_` (`Block`)
- `p_124548_` (`BlockModelGenerators.TintState`)

**Returns:** `private void`

### createCoralFans

```java
private void createCoralFans(Block p_124731_, Block p_124732_)
```

**Parameters:**

- `p_124731_` (`Block`)
- `p_124732_` (`Block`)

**Returns:** `private void`

### createStems

```java
private void createStems(Block p_124789_, Block p_124790_)
```

**Parameters:**

- `p_124789_` (`Block`)
- `p_124790_` (`Block`)

**Returns:** `private void`

### createPitcherPlant

```java
private void createPitcherPlant()
```

**Returns:** `private void`

### createPitcherCrop

```java
private void createPitcherCrop()
```

**Returns:** `private void`

### createCoral

```java
private void createCoral(Block p_124537_, Block p_124538_, Block p_124539_, Block p_124540_, Block p_124541_, Block p_124542_, Block p_124543_, Block p_124544_)
```

**Parameters:**

- `p_124537_` (`Block`)
- `p_124538_` (`Block`)
- `p_124539_` (`Block`)
- `p_124540_` (`Block`)
- `p_124541_` (`Block`)
- `p_124542_` (`Block`)
- `p_124543_` (`Block`)
- `p_124544_` (`Block`)

**Returns:** `private void`

### createDoublePlant

```java
private void createDoublePlant(Block p_124792_, BlockModelGenerators.TintState p_124793_)
```

**Parameters:**

- `p_124792_` (`Block`)
- `p_124793_` (`BlockModelGenerators.TintState`)

**Returns:** `private void`

### createSunflower

```java
private void createSunflower()
```

**Returns:** `private void`

### createTallSeagrass

```java
private void createTallSeagrass()
```

**Returns:** `private void`

### createSmallDripleaf

```java
private void createSmallDripleaf()
```

**Returns:** `private void`

### createDoubleBlock

```java
private void createDoubleBlock(Block p_124954_, ResourceLocation p_124955_, ResourceLocation p_124956_)
```

**Parameters:**

- `p_124954_` (`Block`)
- `p_124955_` (`ResourceLocation`)
- `p_124956_` (`ResourceLocation`)

**Returns:** `private void`

### createPassiveRail

```java
private void createPassiveRail(Block p_124969_)
```

**Parameters:**

- `p_124969_` (`Block`)

**Returns:** `private void`

### createActiveRail

```java
private void createActiveRail(Block p_124975_)
```

**Parameters:**

- `p_124975_` (`Block`)

**Returns:** `private void`

### UnsupportedOperationException

```java
throw new UnsupportedOperationException("Fix you generator!")
```

**Parameters:**

- `generator!"` (`"Fix you`)

**Returns:** `throw new`

### blockEntityModels

```java
private BlockModelGenerators.BlockEntityModelGenerator blockEntityModels(ResourceLocation p_124691_, Block p_124692_)
```

**Parameters:**

- `p_124691_` (`ResourceLocation`)
- `p_124692_` (`Block`)

**Returns:** `private BlockModelGenerators.BlockEntityModelGenerator`

### blockEntityModels

```java
private BlockModelGenerators.BlockEntityModelGenerator blockEntityModels(Block p_124826_, Block p_124827_)
```

**Parameters:**

- `p_124826_` (`Block`)
- `p_124827_` (`Block`)

**Returns:** `private BlockModelGenerators.BlockEntityModelGenerator`

### createAirLikeBlock

```java
private void createAirLikeBlock(Block p_124531_, Item p_124532_)
```

**Parameters:**

- `p_124531_` (`Block`)
- `p_124532_` (`Item`)

**Returns:** `private void`

### createAirLikeBlock

```java
private void createAirLikeBlock(Block p_124922_, ResourceLocation p_124923_)
```

**Parameters:**

- `p_124922_` (`Block`)
- `p_124923_` (`ResourceLocation`)

**Returns:** `private void`

### createFullAndCarpetBlocks

```java
private void createFullAndCarpetBlocks(Block p_176218_, Block p_176219_)
```

**Parameters:**

- `p_176218_` (`Block`)
- `p_176219_` (`Block`)

**Returns:** `private void`

### createFlowerBed

```java
private void createFlowerBed(Block p_273441_)
```

**Parameters:**

- `p_273441_` (`Block`)

**Returns:** `private void`

### createColoredBlockWithRandomRotations

```java
private void createColoredBlockWithRandomRotations(TexturedModel.Provider p_124686_, Block[]... p_124687_)
```

**Parameters:**

- `p_124686_` (`TexturedModel.Provider`)
- `p_124687_` (`Block[]...`)

**Returns:** `private void`

### createColoredBlockWithStateRotations

```java
private void createColoredBlockWithStateRotations(TexturedModel.Provider p_124778_, Block[]... p_124779_)
```

**Parameters:**

- `p_124778_` (`TexturedModel.Provider`)
- `p_124779_` (`Block[]...`)

**Returns:** `private void`

### createGlassBlocks

```java
private void createGlassBlocks(Block p_124879_, Block p_124880_)
```

**Parameters:**

- `p_124879_` (`Block`)
- `p_124880_` (`Block`)

**Returns:** `private void`

### createCommandBlock

```java
private void createCommandBlock(Block p_124978_)
```

**Parameters:**

- `p_124978_` (`Block`)

**Returns:** `private void`

### createAnvil

```java
private void createAnvil(Block p_124981_)
```

**Parameters:**

- `p_124981_` (`Block`)

**Returns:** `private void`

### createBambooModels

```java
private List<Variant> createBambooModels(int p_124512_)
```

**Parameters:**

- `p_124512_` (`int`)

**Returns:** `private List<Variant>`

### createBamboo

```java
private void createBamboo()
```

**Returns:** `private void`

### createColumnWithFacing

```java
private PropertyDispatch createColumnWithFacing()
```

**Returns:** `private PropertyDispatch`

### createBarrel

```java
private void createBarrel()
```

**Returns:** `private void`

### createEmptyOrFullDispatch

```java
private static <T extends Comparable<T>> PropertyDispatch createEmptyOrFullDispatch(Property<T> p_124627_, T p_124628_, ResourceLocation p_124629_, ResourceLocation p_124630_)
```

**Parameters:**

- `p_124627_` (`Property<T>`)
- `p_124628_` (`T`)
- `p_124629_` (`ResourceLocation`)
- `p_124630_` (`ResourceLocation`)

**Returns:** `private static <T extends Comparable<T>> PropertyDispatch`

### createBeeNest

```java
private void createBeeNest(Block p_124584_, Function<Block, TextureMapping> p_124585_)
```

**Parameters:**

- `p_124584_` (`Block`)
- `p_124585_` (`Function<Block, TextureMapping>`)

**Returns:** `private void`

### createCropBlock

```java
private void createCropBlock(Block p_124554_, Property<Integer> p_124555_, int[]... p_124556_)
```

**Parameters:**

- `p_124554_` (`Block`)
- `p_124555_` (`Property<Integer>`)
- `p_124556_` (`int[]...`)

**Returns:** `private void`

### IllegalArgumentException

```java
throw new IllegalArgumentException()
```

**Returns:** `throw new`

### createBell

```java
private void createBell()
```

**Returns:** `private void`

### createGrindstone

```java
private void createGrindstone()
```

**Returns:** `private void`

### createFurnace

```java
private void createFurnace(Block p_124857_, TexturedModel.Provider p_124858_)
```

**Parameters:**

- `p_124857_` (`Block`)
- `p_124858_` (`TexturedModel.Provider`)

**Returns:** `private void`

### createCampfires

```java
private void createCampfires(Block[]... p_124714_)
```

**Parameters:**

- `p_124714_` (`Block[]...`)

**Returns:** `private void`

### createAzalea

```java
private void createAzalea(Block p_176248_)
```

**Parameters:**

- `p_176248_` (`Block`)

**Returns:** `private void`

### createPottedAzalea

```java
private void createPottedAzalea(Block p_176250_)
```

**Parameters:**

- `p_176250_` (`Block`)

**Returns:** `private void`

### createBookshelf

```java
private void createBookshelf()
```

**Returns:** `private void`

### createRedstoneWire

```java
private void createRedstoneWire()
```

**Returns:** `private void`

### createComparator

```java
private void createComparator()
```

**Returns:** `private void`

### createSmoothStoneSlab

```java
private void createSmoothStoneSlab()
```

**Returns:** `private void`

### createBrewingStand

```java
private void createBrewingStand()
```

**Returns:** `private void`

### createMushroomBlock

```java
private void createMushroomBlock(Block p_124984_)
```

**Parameters:**

- `p_124984_` (`Block`)

**Returns:** `private void`

### createCakeBlock

```java
private void createCakeBlock()
```

**Returns:** `private void`

### createCartographyTable

```java
private void createCartographyTable()
```

**Returns:** `private void`

### createSmithingTable

```java
private void createSmithingTable()
```

**Returns:** `private void`

### createCraftingTableLike

```java
private void createCraftingTableLike(Block p_124550_, Block p_124551_, BiFunction<Block, Block, TextureMapping> p_124552_)
```

**Parameters:**

- `p_124550_` (`Block`)
- `p_124551_` (`Block`)
- `p_124552_` (`BiFunction<Block, Block, TextureMapping>`)

**Returns:** `private void`

### createGenericCube

```java
public void createGenericCube(Block p_282830_)
```

**Parameters:**

- `p_282830_` (`Block`)

**Returns:** `public void`

### createPumpkins

```java
private void createPumpkins()
```

**Returns:** `private void`

### createPumpkinVariant

```java
private void createPumpkinVariant(Block p_124565_, TextureMapping p_124566_)
```

**Parameters:**

- `p_124565_` (`Block`)
- `p_124566_` (`TextureMapping`)

**Returns:** `private void`

### createCauldrons

```java
private void createCauldrons()
```

**Returns:** `private void`

### createChorusFlower

```java
private void createChorusFlower()
```

**Returns:** `private void`

### createCrafterBlock

```java
private void createCrafterBlock()
```

**Returns:** `private void`

### createDispenserBlock

```java
private void createDispenserBlock(Block p_124987_)
```

**Parameters:**

- `p_124987_` (`Block`)

**Returns:** `private void`

### createEndPortalFrame

```java
private void createEndPortalFrame()
```

**Returns:** `private void`

### createChorusPlant

```java
private void createChorusPlant()
```

**Returns:** `private void`

### createComposter

```java
private void createComposter()
```

**Returns:** `private void`

### createCopperBulb

```java
private void createCopperBulb(Block p_308914_)
```

**Parameters:**

- `p_308914_` (`Block`)

**Returns:** `private void`

### createCopperBulb

```java
private BlockStateGenerator createCopperBulb(Block p_309175_, ResourceLocation p_309189_, ResourceLocation p_308957_, ResourceLocation p_308948_, ResourceLocation p_309019_)
```

**Parameters:**

- `p_309175_` (`Block`)
- `p_309189_` (`ResourceLocation`)
- `p_308957_` (`ResourceLocation`)
- `p_308948_` (`ResourceLocation`)
- `p_309019_` (`ResourceLocation`)

**Returns:** `private BlockStateGenerator`

### copyCopperBulbModel

```java
private void copyCopperBulbModel(Block p_309045_, Block p_309092_)
```

**Parameters:**

- `p_309045_` (`Block`)
- `p_309092_` (`Block`)

**Returns:** `private void`

### createAmethystCluster

```java
private void createAmethystCluster(Block p_176252_)
```

**Parameters:**

- `p_176252_` (`Block`)

**Returns:** `private void`

### createAmethystClusters

```java
private void createAmethystClusters()
```

**Returns:** `private void`

### createPointedDripstone

```java
private void createPointedDripstone()
```

**Returns:** `private void`

### createPointedDripstoneVariant

```java
private Variant createPointedDripstoneVariant(Direction p_176117_, DripstoneThickness p_176118_)
```

**Parameters:**

- `p_176117_` (`Direction`)
- `p_176118_` (`DripstoneThickness`)

**Returns:** `private Variant`

### createNyliumBlock

```java
private void createNyliumBlock(Block p_124990_)
```

**Parameters:**

- `p_124990_` (`Block`)

**Returns:** `private void`

### createDaylightDetector

```java
private void createDaylightDetector()
```

**Returns:** `private void`

### createRotatableColumn

```java
private void createRotatableColumn(Block p_124993_)
```

**Parameters:**

- `p_124993_` (`Block`)

**Returns:** `private void`

### createLightningRod

```java
private void createLightningRod()
```

**Returns:** `private void`

### createFarmland

```java
private void createFarmland()
```

**Returns:** `private void`

### createFloorFireModels

```java
private List<ResourceLocation> createFloorFireModels(Block p_124996_)
```

**Parameters:**

- `p_124996_` (`Block`)

**Returns:** `private List<ResourceLocation>`

### createSideFireModels

```java
private List<ResourceLocation> createSideFireModels(Block p_124999_)
```

**Parameters:**

- `p_124999_` (`Block`)

**Returns:** `private List<ResourceLocation>`

### createTopFireModels

```java
private List<ResourceLocation> createTopFireModels(Block p_125002_)
```

**Parameters:**

- `p_125002_` (`Block`)

**Returns:** `private List<ResourceLocation>`

### wrapModels

```java
private static List<Variant> wrapModels(List<ResourceLocation> p_124683_, UnaryOperator<Variant> p_124684_)
```

**Parameters:**

- `p_124683_` (`List<ResourceLocation>`)
- `p_124684_` (`UnaryOperator<Variant>`)

**Returns:** `private static List<Variant>`

### createFire

```java
private void createFire()
```

**Returns:** `private void`

### createSoulFire

```java
private void createSoulFire()
```

**Returns:** `private void`

### createLantern

```java
private void createLantern(Block p_125005_)
```

**Parameters:**

- `p_125005_` (`Block`)

**Returns:** `private void`

### createMuddyMangroveRoots

```java
private void createMuddyMangroveRoots()
```

**Returns:** `private void`

### createMangrovePropagule

```java
private void createMangrovePropagule()
```

**Returns:** `private void`

### createFrostedIce

```java
private void createFrostedIce()
```

**Returns:** `private void`

### createGrassBlocks

```java
private void createGrassBlocks()
```

**Returns:** `private void`

### createGrassLikeBlock

```java
private void createGrassLikeBlock(Block p_124600_, ResourceLocation p_124601_, Variant p_124602_)
```

**Parameters:**

- `p_124600_` (`Block`)
- `p_124601_` (`ResourceLocation`)
- `p_124602_` (`Variant`)

**Returns:** `private void`

### createCocoa

```java
private void createCocoa()
```

**Returns:** `private void`

### createDirtPath

```java
private void createDirtPath()
```

**Returns:** `private void`

### createWeightedPressurePlate

```java
private void createWeightedPressurePlate(Block p_124919_, Block p_124920_)
```

**Parameters:**

- `p_124919_` (`Block`)
- `p_124920_` (`Block`)

**Returns:** `private void`

### createHopper

```java
private void createHopper()
```

**Returns:** `private void`

### copyModel

```java
private void copyModel(Block p_124939_, Block p_124940_)
```

**Parameters:**

- `p_124939_` (`Block`)
- `p_124940_` (`Block`)

**Returns:** `private void`

### createIronBars

```java
private void createIronBars()
```

**Returns:** `private void`

### createNonTemplateHorizontalBlock

```java
private void createNonTemplateHorizontalBlock(Block p_125008_)
```

**Parameters:**

- `p_125008_` (`Block`)

**Returns:** `private void`

### createLever

```java
private void createLever()
```

**Returns:** `private void`

### createLilyPad

```java
private void createLilyPad()
```

**Returns:** `private void`

### createFrogspawnBlock

```java
private void createFrogspawnBlock()
```

**Returns:** `private void`

### createNetherPortalBlock

```java
private void createNetherPortalBlock()
```

**Returns:** `private void`

### createNetherrack

```java
private void createNetherrack()
```

**Returns:** `private void`

### createObserver

```java
private void createObserver()
```

**Returns:** `private void`

### createPistons

```java
private void createPistons()
```

**Returns:** `private void`

### createPistonVariant

```java
private void createPistonVariant(Block p_124604_, ResourceLocation p_124605_, TextureMapping p_124606_)
```

**Parameters:**

- `p_124604_` (`Block`)
- `p_124605_` (`ResourceLocation`)
- `p_124606_` (`TextureMapping`)

**Returns:** `private void`

### createPistonHeads

```java
private void createPistonHeads()
```

**Returns:** `private void`

### createTrialSpawner

```java
private void createTrialSpawner()
```

**Returns:** `private void`

### createVault

```java
private void createVault()
```

**Returns:** `private void`

### createSculkSensor

```java
private void createSculkSensor()
```

**Returns:** `private void`

### createCalibratedSculkSensor

```java
private void createCalibratedSculkSensor()
```

**Returns:** `private void`

### createSculkShrieker

```java
private void createSculkShrieker()
```

**Returns:** `private void`

### createScaffolding

```java
private void createScaffolding()
```

**Returns:** `private void`

### createCaveVines

```java
private void createCaveVines()
```

**Returns:** `private void`

### createRedstoneLamp

```java
private void createRedstoneLamp()
```

**Returns:** `private void`

### createNormalTorch

```java
private void createNormalTorch(Block p_124951_, Block p_124952_)
```

**Parameters:**

- `p_124951_` (`Block`)
- `p_124952_` (`Block`)

**Returns:** `private void`

### createRedstoneTorch

```java
private void createRedstoneTorch()
```

**Returns:** `private void`

### createRepeater

```java
private void createRepeater()
```

**Returns:** `private void`

### createSeaPickle

```java
private void createSeaPickle()
```

**Returns:** `private void`

### createSnowBlocks

```java
private void createSnowBlocks()
```

**Returns:** `private void`

### createStonecutter

```java
private void createStonecutter()
```

**Returns:** `private void`

### createStructureBlock

```java
private void createStructureBlock()
```

**Returns:** `private void`

### createSweetBerryBush

```java
private void createSweetBerryBush()
```

**Returns:** `private void`

### createTripwire

```java
private void createTripwire()
```

**Returns:** `private void`

### createTripwireHook

```java
private void createTripwireHook()
```

**Returns:** `private void`

### createTurtleEggModel

```java
private ResourceLocation createTurtleEggModel(int p_124514_, String p_124515_, TextureMapping p_124516_)
```

**Parameters:**

- `p_124514_` (`int`)
- `p_124515_` (`String`)
- `p_124516_` (`TextureMapping`)

**Returns:** `private ResourceLocation`

### UnsupportedOperationException

```java
throw new UnsupportedOperationException()
```

**Returns:** `throw new`

### createTurtleEggModel

```java
private ResourceLocation createTurtleEggModel(Integer p_124677_, Integer p_124678_)
```

**Parameters:**

- `p_124677_` (`Integer`)
- `p_124678_` (`Integer`)

**Returns:** `private ResourceLocation`

### UnsupportedOperationException

```java
throw new UnsupportedOperationException()
```

**Returns:** `throw new`

### createTurtleEgg

```java
private void createTurtleEgg()
```

**Returns:** `private void`

### createSnifferEgg

```java
private void createSnifferEgg()
```

**Returns:** `private void`

### createMultiface

```java
private void createMultiface(Block p_176086_)
```

**Parameters:**

- `p_176086_` (`Block`)

**Returns:** `private void`

### createSculkCatalyst

```java
private void createSculkCatalyst()
```

**Returns:** `private void`

### createChiseledBookshelf

```java
private void createChiseledBookshelf()
```

**Returns:** `private void`

### addSlotStateAndRotationVariants

```java
private void addSlotStateAndRotationVariants(MultiPartGenerator p_261951_, Condition.TerminalCondition p_261482_, VariantProperties.Rotation p_262169_)
```

**Parameters:**

- `p_261951_` (`MultiPartGenerator`)
- `p_261482_` (`Condition.TerminalCondition`)
- `p_262169_` (`VariantProperties.Rotation`)

**Returns:** `private void`

### addBookSlotModel

```java
private void addBookSlotModel(MultiPartGenerator p_261839_, Condition.TerminalCondition p_261634_, VariantProperties.Rotation p_262044_, BooleanProperty p_262163_, ModelTemplate p_261986_, boolean p_261790_)
```

**Parameters:**

- `p_261839_` (`MultiPartGenerator`)
- `p_261634_` (`Condition.TerminalCondition`)
- `p_262044_` (`VariantProperties.Rotation`)
- `p_262163_` (`BooleanProperty`)
- `p_261986_` (`ModelTemplate`)
- `p_261790_` (`boolean`)

**Returns:** `private void`

### createMagmaBlock

```java
private void createMagmaBlock()
```

**Returns:** `private void`

### createShulkerBox

```java
private void createShulkerBox(Block p_125011_)
```

**Parameters:**

- `p_125011_` (`Block`)

**Returns:** `private void`

### createGrowingPlant

```java
private void createGrowingPlant(Block p_124734_, Block p_124735_, BlockModelGenerators.TintState p_124736_)
```

**Parameters:**

- `p_124734_` (`Block`)
- `p_124735_` (`Block`)
- `p_124736_` (`BlockModelGenerators.TintState`)

**Returns:** `private void`

### createBedItem

```java
private void createBedItem(Block p_124963_, Block p_124964_)
```

**Parameters:**

- `p_124963_` (`Block`)
- `p_124964_` (`Block`)

**Returns:** `private void`

### createInfestedStone

```java
private void createInfestedStone()
```

**Returns:** `private void`

### createInfestedDeepslate

```java
private void createInfestedDeepslate()
```

**Returns:** `private void`

### createNetherRoots

```java
private void createNetherRoots(Block p_124971_, Block p_124972_)
```

**Parameters:**

- `p_124971_` (`Block`)
- `p_124972_` (`Block`)

**Returns:** `private void`

### createRespawnAnchor

```java
private void createRespawnAnchor()
```

**Returns:** `private void`

### applyRotation

```java
private Variant applyRotation(FrontAndTop p_124636_, Variant p_124637_)
```

**Parameters:**

- `p_124636_` (`FrontAndTop`)
- `p_124637_` (`Variant`)

**Returns:** `private Variant`

### UnsupportedOperationException

```java
throw new UnsupportedOperationException("Rotation " + p_124636_ + " can't be expressed with existing x and y values")
```

**Parameters:**

- `values"` (`"Rotation " + p_124636_ + " can't be expressed with existing x and y`)

**Returns:** `throw new`

### createJigsaw

```java
private void createJigsaw()
```

**Returns:** `private void`

### createPetrifiedOakSlab

```java
private void createPetrifiedOakSlab()
```

**Returns:** `private void`

### run

```java
public void run()
```

**Returns:** `public void`

### createLightBlock

```java
private void createLightBlock()
```

**Returns:** `private void`

### createCandleAndCandleCake

```java
private void createCandleAndCandleCake(Block p_176245_, Block p_176246_)
```

**Parameters:**

- `p_176245_` (`Block`)
- `p_176246_` (`Block`)

**Returns:** `private void`

### BlockEntityModelGenerator

```java
public BlockEntityModelGenerator(ResourceLocation p_125020_, Block p_125021_)
```

**Parameters:**

- `p_125020_` (`ResourceLocation`)
- `p_125021_` (`Block`)

**Returns:** `public`

### create

```java
public BlockModelGenerators.BlockEntityModelGenerator create(Block[]... p_125026_)
```

**Parameters:**

- `p_125026_` (`Block[]...`)

**Returns:** `public BlockModelGenerators.BlockEntityModelGenerator`

### createWithoutBlockItem

```java
public BlockModelGenerators.BlockEntityModelGenerator createWithoutBlockItem(Block[]... p_125028_)
```

**Parameters:**

- `p_125028_` (`Block[]...`)

**Returns:** `public BlockModelGenerators.BlockEntityModelGenerator`

### createWithCustomBlockItemModel

```java
public BlockModelGenerators.BlockEntityModelGenerator createWithCustomBlockItemModel(ModelTemplate p_125023_, Block[]... p_125024_)
```

**Parameters:**

- `p_125023_` (`ModelTemplate`)
- `p_125024_` (`Block[]...`)

**Returns:** `public BlockModelGenerators.BlockEntityModelGenerator`

### BlockFamilyProvider

```java
public BlockFamilyProvider(TextureMapping p_125034_)
```

**Parameters:**

- `p_125034_` (`TextureMapping`)

**Returns:** `public`

### fullBlock

```java
public BlockModelGenerators.BlockFamilyProvider fullBlock(Block p_125041_, ModelTemplate p_125042_)
```

**Parameters:**

- `p_125041_` (`Block`)
- `p_125042_` (`ModelTemplate`)

**Returns:** `public BlockModelGenerators.BlockFamilyProvider`

### donateModelTo

```java
public BlockModelGenerators.BlockFamilyProvider donateModelTo(Block p_308961_, Block p_309136_)
```

**Parameters:**

- `p_308961_` (`Block`)
- `p_309136_` (`Block`)

**Returns:** `public BlockModelGenerators.BlockFamilyProvider`

### button

```java
public BlockModelGenerators.BlockFamilyProvider button(Block p_125036_)
```

**Parameters:**

- `p_125036_` (`Block`)

**Returns:** `public BlockModelGenerators.BlockFamilyProvider`

### wall

```java
public BlockModelGenerators.BlockFamilyProvider wall(Block p_125046_)
```

**Parameters:**

- `p_125046_` (`Block`)

**Returns:** `public BlockModelGenerators.BlockFamilyProvider`

### customFence

```java
public BlockModelGenerators.BlockFamilyProvider customFence(Block p_250333_)
```

**Parameters:**

- `p_250333_` (`Block`)

**Returns:** `public BlockModelGenerators.BlockFamilyProvider`

### fence

```java
public BlockModelGenerators.BlockFamilyProvider fence(Block p_125048_)
```

**Parameters:**

- `p_125048_` (`Block`)

**Returns:** `public BlockModelGenerators.BlockFamilyProvider`

### customFenceGate

```java
public BlockModelGenerators.BlockFamilyProvider customFenceGate(Block p_248640_)
```

**Parameters:**

- `p_248640_` (`Block`)

**Returns:** `public BlockModelGenerators.BlockFamilyProvider`

### fenceGate

```java
public BlockModelGenerators.BlockFamilyProvider fenceGate(Block p_125050_)
```

**Parameters:**

- `p_125050_` (`Block`)

**Returns:** `public BlockModelGenerators.BlockFamilyProvider`

### pressurePlate

```java
public BlockModelGenerators.BlockFamilyProvider pressurePlate(Block p_125052_)
```

**Parameters:**

- `p_125052_` (`Block`)

**Returns:** `public BlockModelGenerators.BlockFamilyProvider`

### sign

```java
public BlockModelGenerators.BlockFamilyProvider sign(Block p_176270_)
```

**Parameters:**

- `p_176270_` (`Block`)

**Returns:** `public BlockModelGenerators.BlockFamilyProvider`

### IllegalStateException

```java
throw new IllegalStateException("Family not defined")
```

**Parameters:**

- `defined"` (`"Family not`)

**Returns:** `throw new`

### slab

```java
public BlockModelGenerators.BlockFamilyProvider slab(Block p_125054_)
```

**Parameters:**

- `p_125054_` (`Block`)

**Returns:** `public BlockModelGenerators.BlockFamilyProvider`

### IllegalStateException

```java
throw new IllegalStateException("Full block not generated yet")
```

**Parameters:**

- `yet"` (`"Full block not generated`)

**Returns:** `throw new`

### stairs

```java
public BlockModelGenerators.BlockFamilyProvider stairs(Block p_125056_)
```

**Parameters:**

- `p_125056_` (`Block`)

**Returns:** `public BlockModelGenerators.BlockFamilyProvider`

### fullBlockVariant

```java
private BlockModelGenerators.BlockFamilyProvider fullBlockVariant(Block p_176272_)
```

**Parameters:**

- `p_176272_` (`Block`)

**Returns:** `private BlockModelGenerators.BlockFamilyProvider`

### door

```java
private BlockModelGenerators.BlockFamilyProvider door(Block p_176274_)
```

**Parameters:**

- `p_176274_` (`Block`)

**Returns:** `private BlockModelGenerators.BlockFamilyProvider`

### trapdoor

```java
private void trapdoor(Block p_176276_)
```

**Parameters:**

- `p_176276_` (`Block`)

**Returns:** `private void`

### getOrCreateModel

```java
private ResourceLocation getOrCreateModel(ModelTemplate p_176262_, Block p_176263_)
```

**Parameters:**

- `p_176262_` (`ModelTemplate`)
- `p_176263_` (`Block`)

**Returns:** `private ResourceLocation`

### generateFor

```java
public BlockModelGenerators.BlockFamilyProvider generateFor(BlockFamily p_176260_)
```

**Parameters:**

- `p_176260_` (`BlockFamily`)

**Returns:** `public BlockModelGenerators.BlockFamilyProvider`

### create

```java
BlockStateGenerator create(Block p_176278_, ResourceLocation p_176279_, TextureMapping p_176280_, BiConsumer<ResourceLocation, Supplier<JsonElement>> p_176281_)
```

**Parameters:**

- `p_176278_` (`Block`)
- `p_176279_` (`ResourceLocation`)
- `p_176280_` (`TextureMapping`)
- `p_176281_` (`BiConsumer<ResourceLocation, Supplier<JsonElement>>`)

**Returns:** `BlockStateGenerator`

### BookSlotModelCacheKey

```java
static record BookSlotModelCacheKey(ModelTemplate template, String modelSuffix)
```

**Parameters:**

- `template` (`ModelTemplate`)
- `modelSuffix` (`String`)

**Returns:** `static record`

### getCross

```java
public ModelTemplate getCross()
```

**Returns:** `public ModelTemplate`

### getCrossPot

```java
public ModelTemplate getCrossPot()
```

**Returns:** `public ModelTemplate`

### WoodProvider

```java
public WoodProvider(TextureMapping p_125073_)
```

**Parameters:**

- `p_125073_` (`TextureMapping`)

**Returns:** `public`

### wood

```java
public BlockModelGenerators.WoodProvider wood(Block p_125075_)
```

**Parameters:**

- `p_125075_` (`Block`)

**Returns:** `public BlockModelGenerators.WoodProvider`

### log

```java
public BlockModelGenerators.WoodProvider log(Block p_125077_)
```

**Parameters:**

- `p_125077_` (`Block`)

**Returns:** `public BlockModelGenerators.WoodProvider`

### logWithHorizontal

```java
public BlockModelGenerators.WoodProvider logWithHorizontal(Block p_125079_)
```

**Parameters:**

- `p_125079_` (`Block`)

**Returns:** `public BlockModelGenerators.WoodProvider`

### logUVLocked

```java
public BlockModelGenerators.WoodProvider logUVLocked(Block p_259915_)
```

**Parameters:**

- `p_259915_` (`Block`)

**Returns:** `public BlockModelGenerators.WoodProvider`
