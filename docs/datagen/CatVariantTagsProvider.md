# CatVariantTagsProvider

**Package:** `net.minecraft.data.tags`
**Type:** class
**Extends:** `TagsProvider<CatVariant>`

## Methods

### CatVariantTagsProvider

```java
public public CatVariantTagsProvider(PackOutput p_256547_, CompletableFuture<HolderLookup.Provider> p_256090_)
```

**Parameters:**

- `p_256547_` (`PackOutput`)
- `p_256090_` (`CompletableFuture<HolderLookup.Provider>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### CatVariantTagsProvider

```java
public CatVariantTagsProvider(PackOutput p_256547_, CompletableFuture<HolderLookup.Provider> p_256090_, String modId, .jetbrains.annotations.Nullable net.neoforged.neoforge.common.data.ExistingFileHelper existingFileHelper)
```

**Parameters:**

- `p_256547_` (`PackOutput`)
- `p_256090_` (`CompletableFuture<HolderLookup.Provider>`)
- `modId` (`String`)
- `existingFileHelper` (`.jetbrains.annotations.Nullable net.neoforged.neoforge.common.data.ExistingFileHelper`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### addTags

```java
protected void addTags(HolderLookup.Provider p_256078_)
```

**Parameters:**

- `p_256078_` (`HolderLookup.Provider`)
