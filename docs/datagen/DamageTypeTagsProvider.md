# DamageTypeTagsProvider

**Package:** `net.minecraft.data.tags`
**Type:** class
**Extends:** `TagsProvider<DamageType>`

## Methods

### DamageTypeTagsProvider

```java
public public DamageTypeTagsProvider(PackOutput p_270719_, CompletableFuture<HolderLookup.Provider> p_270256_)
```

**Parameters:**

- `p_270719_` (`PackOutput`)
- `p_270256_` (`CompletableFuture<HolderLookup.Provider>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### DamageTypeTagsProvider

```java
public DamageTypeTagsProvider(PackOutput p_270719_, CompletableFuture<HolderLookup.Provider> p_270256_, String modId, .jetbrains.annotations.Nullable net.neoforged.neoforge.common.data.ExistingFileHelper existingFileHelper)
```

**Parameters:**

- `p_270719_` (`PackOutput`)
- `p_270256_` (`CompletableFuture<HolderLookup.Provider>`)
- `modId` (`String`)
- `existingFileHelper` (`.jetbrains.annotations.Nullable net.neoforged.neoforge.common.data.ExistingFileHelper`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### addTags

```java
protected void addTags(HolderLookup.Provider p_270108_)
```

**Parameters:**

- `p_270108_` (`HolderLookup.Provider`)
