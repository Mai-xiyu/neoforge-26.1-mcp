# EndBiomes

**Package:** `net.minecraft.data.worldgen.biome`
**Type:** class

## Methods

### baseEndBiome

```java
private static Biome baseEndBiome(BiomeGenerationSettings.Builder p_194825_)
```

**Parameters:**

- `p_194825_` (`BiomeGenerationSettings.Builder`)

**Returns:** `private static Biome`

### endBarrens

```java
public static Biome endBarrens(HolderGetter<PlacedFeature> p_256510_, HolderGetter<ConfiguredWorldCarver<?>> p_256130_)
```

**Parameters:**

- `p_256510_` (`HolderGetter<PlacedFeature>`)
- `p_256130_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### baseEndBiome

```java
return baseEndBiome()
```

**Returns:** `return`

### theEnd

```java
public static Biome theEnd(HolderGetter<PlacedFeature> p_255623_, HolderGetter<ConfiguredWorldCarver<?>> p_255991_)
```

**Parameters:**

- `p_255623_` (`HolderGetter<PlacedFeature>`)
- `p_255991_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### baseEndBiome

```java
return baseEndBiome()
```

**Returns:** `return`

### endMidlands

```java
public static Biome endMidlands(HolderGetter<PlacedFeature> p_255719_, HolderGetter<ConfiguredWorldCarver<?>> p_255751_)
```

**Parameters:**

- `p_255719_` (`HolderGetter<PlacedFeature>`)
- `p_255751_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### baseEndBiome

```java
return baseEndBiome()
```

**Returns:** `return`

### endHighlands

```java
public static Biome endHighlands(HolderGetter<PlacedFeature> p_256650_, HolderGetter<ConfiguredWorldCarver<?>> p_256540_)
```

**Parameters:**

- `p_256650_` (`HolderGetter<PlacedFeature>`)
- `p_256540_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### baseEndBiome

```java
return baseEndBiome()
```

**Returns:** `return`

### smallEndIslands

```java
public static Biome smallEndIslands(HolderGetter<PlacedFeature> p_255848_, HolderGetter<ConfiguredWorldCarver<?>> p_256605_)
```

**Parameters:**

- `p_255848_` (`HolderGetter<PlacedFeature>`)
- `p_256605_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### baseEndBiome

```java
return baseEndBiome()
```

**Returns:** `return`
