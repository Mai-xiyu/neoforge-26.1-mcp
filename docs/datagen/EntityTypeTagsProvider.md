# EntityTypeTagsProvider

**Package:** `net.minecraft.data.tags`
**Type:** class
**Extends:** `IntrinsicHolderTagsProvider<EntityType<?>>`

## Methods

### EntityTypeTagsProvider

```java
public public EntityTypeTagsProvider(PackOutput p_256095_, CompletableFuture<HolderLookup.Provider> p_256572_)
```

**Parameters:**

- `p_256095_` (`PackOutput`)
- `p_256572_` (`CompletableFuture<HolderLookup.Provider>`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### EntityTypeTagsProvider

```java
public EntityTypeTagsProvider(PackOutput p_256095_, CompletableFuture<HolderLookup.Provider> p_256572_, String modId, .jetbrains.annotations.Nullable net.neoforged.neoforge.common.data.ExistingFileHelper existingFileHelper)
```

**Parameters:**

- `p_256095_` (`PackOutput`)
- `p_256572_` (`CompletableFuture<HolderLookup.Provider>`)
- `modId` (`String`)
- `existingFileHelper` (`.jetbrains.annotations.Nullable net.neoforged.neoforge.common.data.ExistingFileHelper`)

**Returns:** `public`

### addTags

```java
protected void addTags(HolderLookup.Provider p_255894_)
```

**Parameters:**

- `p_255894_` (`HolderLookup.Provider`)
