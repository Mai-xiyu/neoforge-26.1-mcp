# FluidTagsProvider

**Package:** `net.minecraft.data.tags`
**Type:** class
**Extends:** `IntrinsicHolderTagsProvider<Fluid>`

## Methods

### FluidTagsProvider

```java
public public FluidTagsProvider(PackOutput p_255941_, CompletableFuture<HolderLookup.Provider> p_256600_)
```

**Parameters:**

- `p_255941_` (`PackOutput`)
- `p_256600_` (`CompletableFuture<HolderLookup.Provider>`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### FluidTagsProvider

```java
public FluidTagsProvider(PackOutput p_255941_, CompletableFuture<HolderLookup.Provider> p_256600_, String modId, .jetbrains.annotations.Nullable net.neoforged.neoforge.common.data.ExistingFileHelper existingFileHelper)
```

**Parameters:**

- `p_255941_` (`PackOutput`)
- `p_256600_` (`CompletableFuture<HolderLookup.Provider>`)
- `modId` (`String`)
- `existingFileHelper` (`.jetbrains.annotations.Nullable net.neoforged.neoforge.common.data.ExistingFileHelper`)

**Returns:** `public`

### addTags

```java
protected void addTags(HolderLookup.Provider p_256366_)
```

**Parameters:**

- `p_256366_` (`HolderLookup.Provider`)
