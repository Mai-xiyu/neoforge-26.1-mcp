# MultiPartGenerator

**Package:** `net.minecraft.data.models.blockstates`
**Type:** class
**Implements:** `BlockStateGenerator`

## Methods

### MultiPartGenerator

```java
private MultiPartGenerator(Block p_125202_)
```

**Parameters:**

- `p_125202_` (`Block`)

**Returns:** `private`

### getBlock

```java
public Block getBlock()
```

**Returns:** `Block`

### multiPart

```java
public static MultiPartGenerator multiPart(Block p_125205_)
```

**Parameters:**

- `p_125205_` (`Block`)

**Returns:** `public static MultiPartGenerator`

### MultiPartGenerator

```java
return new MultiPartGenerator()
```

**Returns:** `return new`

### with

```java
public MultiPartGenerator with(List<Variant> p_125221_)
```

**Parameters:**

- `p_125221_` (`List<Variant>`)

**Returns:** `public MultiPartGenerator`

### with

```java
public MultiPartGenerator with(Variant p_125219_)
```

**Parameters:**

- `p_125219_` (`Variant`)

**Returns:** `public MultiPartGenerator`

### with

```java
public MultiPartGenerator with(Condition p_125213_, List<Variant> p_125214_)
```

**Parameters:**

- `p_125213_` (`Condition`)
- `p_125214_` (`List<Variant>`)

**Returns:** `public MultiPartGenerator`

### with

```java
public MultiPartGenerator with(Condition p_125216_, Variant[]... p_125217_)
```

**Parameters:**

- `p_125216_` (`Condition`)
- `p_125217_` (`Variant[]...`)

**Returns:** `public MultiPartGenerator`

### with

```java
public MultiPartGenerator with(Condition p_125210_, Variant p_125211_)
```

**Parameters:**

- `p_125210_` (`Condition`)
- `p_125211_` (`Variant`)

**Returns:** `public MultiPartGenerator`

### get

```java
public JsonElement get()
```

**Returns:** `public JsonElement`

### ConditionalEntry

```java
 ConditionalEntry(Condition p_125226_, List<Variant> p_125227_)
```

**Parameters:**

- `p_125226_` (`Condition`)
- `p_125227_` (`List<Variant>`)

**Returns:** ``

### super

```java
 super()
```

**Returns:** ``

### validate

```java
public void validate(StateDefinition<?, ?> p_125233_)
```

**Parameters:**

- `p_125233_` (`StateDefinition<?, ?>`)

### decorate

```java
public void decorate(JsonObject p_125235_)
```

**Parameters:**

- `p_125235_` (`JsonObject`)

### Entry

```java
 Entry(List<Variant> p_125238_)
```

**Parameters:**

- `p_125238_` (`List<Variant>`)

**Returns:** ``

### validate

```java
public void validate(StateDefinition<?, ?> p_125243_)
```

**Parameters:**

- `p_125243_` (`StateDefinition<?, ?>`)

**Returns:** `public void`

### decorate

```java
public void decorate(JsonObject p_125244_)
```

**Parameters:**

- `p_125244_` (`JsonObject`)

**Returns:** `public void`

### get

```java
public JsonElement get()
```

**Returns:** `public JsonElement`
