# OverworldBiomes

**Package:** `net.minecraft.data.worldgen.biome`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `NORMAL_WATER_COLOR` | `int` |  |
| `NORMAL_WATER_FOG_COLOR` | `int` |  |
| `SWAMP_SKELETON_WEIGHT` | `int` |  |

## Methods

### calculateSkyColor

```java
protected static int calculateSkyColor(float p_194844_)
```

**Parameters:**

- `p_194844_` (`float`)

**Returns:** `protected static int`

### biome

```java
private static Biome biome(boolean p_265746_, float p_265800_, float p_265276_, MobSpawnSettings.Builder p_265425_, BiomeGenerationSettings.Builder p_265371_, Music p_265636_)
```

**Parameters:**

- `p_265746_` (`boolean`)
- `p_265800_` (`float`)
- `p_265276_` (`float`)
- `p_265425_` (`MobSpawnSettings.Builder`)
- `p_265371_` (`BiomeGenerationSettings.Builder`)
- `p_265636_` (`Music`)

**Returns:** `private static Biome`

### biome

```java
return biome()
```

**Returns:** `return`

### biome

```java
private static Biome biome(boolean p_273483_, float p_272621_, float p_273588_, int p_273605_, int p_272756_, Integer p_272889_, Integer p_272657_, MobSpawnSettings.Builder p_273300_, BiomeGenerationSettings.Builder p_272700_, Music p_272996_)
```

**Parameters:**

- `p_273483_` (`boolean`)
- `p_272621_` (`float`)
- `p_273588_` (`float`)
- `p_273605_` (`int`)
- `p_272756_` (`int`)
- `p_272889_` (`Integer`)
- `p_272657_` (`Integer`)
- `p_273300_` (`MobSpawnSettings.Builder`)
- `p_272700_` (`BiomeGenerationSettings.Builder`)
- `p_272996_` (`Music`)

**Returns:** `private static Biome`

### globalOverworldGeneration

```java
private static void globalOverworldGeneration(BiomeGenerationSettings.Builder p_194870_)
```

**Parameters:**

- `p_194870_` (`BiomeGenerationSettings.Builder`)

**Returns:** `private static void`

### oldGrowthTaiga

```java
public static Biome oldGrowthTaiga(HolderGetter<PlacedFeature> p_255849_, HolderGetter<ConfiguredWorldCarver<?>> p_256578_, boolean p_194877_)
```

**Parameters:**

- `p_255849_` (`HolderGetter<PlacedFeature>`)
- `p_256578_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_194877_` (`boolean`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome(p_194877_ ? 0.25F : 0.3F)
```

**Parameters:**

- `0.3F` (`p_194877_ ? 0.25F :`)

**Returns:** `return`

### sparseJungle

```java
public static Biome sparseJungle(HolderGetter<PlacedFeature> p_255977_, HolderGetter<ConfiguredWorldCarver<?>> p_256531_)
```

**Parameters:**

- `p_255977_` (`HolderGetter<PlacedFeature>`)
- `p_256531_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### jungle

```java
public static Biome jungle(HolderGetter<PlacedFeature> p_256033_, HolderGetter<ConfiguredWorldCarver<?>> p_255651_)
```

**Parameters:**

- `p_256033_` (`HolderGetter<PlacedFeature>`)
- `p_255651_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### bambooJungle

```java
public static Biome bambooJungle(HolderGetter<PlacedFeature> p_255817_, HolderGetter<ConfiguredWorldCarver<?>> p_256096_)
```

**Parameters:**

- `p_255817_` (`HolderGetter<PlacedFeature>`)
- `p_256096_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### baseJungle

```java
private static Biome baseJungle(HolderGetter<PlacedFeature> p_285208_, HolderGetter<ConfiguredWorldCarver<?>> p_285276_, float p_285079_, boolean p_285393_, boolean p_285109_, boolean p_285122_, MobSpawnSettings.Builder p_285449_, Music p_285440_)
```

**Parameters:**

- `p_285208_` (`HolderGetter<PlacedFeature>`)
- `p_285276_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_285079_` (`float`)
- `p_285393_` (`boolean`)
- `p_285109_` (`boolean`)
- `p_285122_` (`boolean`)
- `p_285449_` (`MobSpawnSettings.Builder`)
- `p_285440_` (`Music`)

**Returns:** `private static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome()
```

**Returns:** `return`

### windsweptHills

```java
public static Biome windsweptHills(HolderGetter<PlacedFeature> p_255703_, HolderGetter<ConfiguredWorldCarver<?>> p_256239_, boolean p_194887_)
```

**Parameters:**

- `p_255703_` (`HolderGetter<PlacedFeature>`)
- `p_256239_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_194887_` (`boolean`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome()
```

**Returns:** `return`

### desert

```java
public static Biome desert(HolderGetter<PlacedFeature> p_256064_, HolderGetter<ConfiguredWorldCarver<?>> p_255852_)
```

**Parameters:**

- `p_256064_` (`HolderGetter<PlacedFeature>`)
- `p_255852_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### plains

```java
public static Biome plains(HolderGetter<PlacedFeature> p_256382_, HolderGetter<ConfiguredWorldCarver<?>> p_256173_, boolean p_194882_, boolean p_194883_, boolean p_194884_)
```

**Parameters:**

- `p_256382_` (`HolderGetter<PlacedFeature>`)
- `p_256173_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_194882_` (`boolean`)
- `p_194883_` (`boolean`)
- `p_194884_` (`boolean`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome(p_194883_ ? 0.5F : 0.4F)
```

**Parameters:**

- `0.4F` (`p_194883_ ? 0.5F :`)

**Returns:** `return`

### mushroomFields

```java
public static Biome mushroomFields(HolderGetter<PlacedFeature> p_255775_, HolderGetter<ConfiguredWorldCarver<?>> p_256480_)
```

**Parameters:**

- `p_255775_` (`HolderGetter<PlacedFeature>`)
- `p_256480_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome()
```

**Returns:** `return`

### savanna

```java
public static Biome savanna(HolderGetter<PlacedFeature> p_256294_, HolderGetter<ConfiguredWorldCarver<?>> p_256583_, boolean p_194879_, boolean p_194880_)
```

**Parameters:**

- `p_256294_` (`HolderGetter<PlacedFeature>`)
- `p_256583_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_194879_` (`boolean`)
- `p_194880_` (`boolean`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome()
```

**Returns:** `return`

### badlands

```java
public static Biome badlands(HolderGetter<PlacedFeature> p_256309_, HolderGetter<ConfiguredWorldCarver<?>> p_256430_, boolean p_194897_)
```

**Parameters:**

- `p_256309_` (`HolderGetter<PlacedFeature>`)
- `p_256430_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_194897_` (`boolean`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### baseOcean

```java
private static Biome baseOcean(MobSpawnSettings.Builder p_194872_, int p_194873_, int p_194874_, BiomeGenerationSettings.Builder p_194875_)
```

**Parameters:**

- `p_194872_` (`MobSpawnSettings.Builder`)
- `p_194873_` (`int`)
- `p_194874_` (`int`)
- `p_194875_` (`BiomeGenerationSettings.Builder`)

**Returns:** `private static Biome`

### biome

```java
return biome()
```

**Returns:** `return`

### baseOceanGeneration

```java
private static BiomeGenerationSettings.Builder baseOceanGeneration(HolderGetter<PlacedFeature> p_256289_, HolderGetter<ConfiguredWorldCarver<?>> p_256514_)
```

**Parameters:**

- `p_256289_` (`HolderGetter<PlacedFeature>`)
- `p_256514_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `private static BiomeGenerationSettings.Builder`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### coldOcean

```java
public static Biome coldOcean(HolderGetter<PlacedFeature> p_256141_, HolderGetter<ConfiguredWorldCarver<?>> p_255841_, boolean p_194900_)
```

**Parameters:**

- `p_256141_` (`HolderGetter<PlacedFeature>`)
- `p_255841_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_194900_` (`boolean`)

**Returns:** `public static Biome`

### baseOcean

```java
return baseOcean()
```

**Returns:** `return`

### ocean

```java
public static Biome ocean(HolderGetter<PlacedFeature> p_256265_, HolderGetter<ConfiguredWorldCarver<?>> p_256537_, boolean p_255752_)
```

**Parameters:**

- `p_256265_` (`HolderGetter<PlacedFeature>`)
- `p_256537_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_255752_` (`boolean`)

**Returns:** `public static Biome`

### baseOcean

```java
return baseOcean()
```

**Returns:** `return`

### lukeWarmOcean

```java
public static Biome lukeWarmOcean(HolderGetter<PlacedFeature> p_255660_, HolderGetter<ConfiguredWorldCarver<?>> p_256231_, boolean p_194906_)
```

**Parameters:**

- `p_255660_` (`HolderGetter<PlacedFeature>`)
- `p_256231_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_194906_` (`boolean`)

**Returns:** `public static Biome`

### baseOcean

```java
return baseOcean()
```

**Returns:** `return`

### warmOcean

```java
public static Biome warmOcean(HolderGetter<PlacedFeature> p_256477_, HolderGetter<ConfiguredWorldCarver<?>> p_256024_)
```

**Parameters:**

- `p_256477_` (`HolderGetter<PlacedFeature>`)
- `p_256024_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### baseOcean

```java
return baseOcean()
```

**Returns:** `return`

### frozenOcean

```java
public static Biome frozenOcean(HolderGetter<PlacedFeature> p_256482_, HolderGetter<ConfiguredWorldCarver<?>> p_256660_, boolean p_194909_)
```

**Parameters:**

- `p_256482_` (`HolderGetter<PlacedFeature>`)
- `p_256660_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_194909_` (`boolean`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### forest

```java
public static Biome forest(HolderGetter<PlacedFeature> p_255788_, HolderGetter<ConfiguredWorldCarver<?>> p_256461_, boolean p_194892_, boolean p_194893_, boolean p_194894_)
```

**Parameters:**

- `p_255788_` (`HolderGetter<PlacedFeature>`)
- `p_256461_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_194892_` (`boolean`)
- `p_194893_` (`boolean`)
- `p_194894_` (`boolean`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome(p_194892_ ? 0.6F : 0.8F)
```

**Parameters:**

- `0.8F` (`p_194892_ ? 0.6F :`)

**Returns:** `return`

### taiga

```java
public static Biome taiga(HolderGetter<PlacedFeature> p_256177_, HolderGetter<ConfiguredWorldCarver<?>> p_255727_, boolean p_194912_)
```

**Parameters:**

- `p_256177_` (`HolderGetter<PlacedFeature>`)
- `p_255727_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_194912_` (`boolean`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome(p_194912_ ? 0.4F : 0.8F, p_194912_ ? 4020182 : 4159204)
```

**Parameters:**

- `0.8F` (`p_194912_ ? 0.4F :`)
- `4159204` (`p_194912_ ? 4020182 :`)

**Returns:** `return`

### darkForest

```java
public static Biome darkForest(HolderGetter<PlacedFeature> p_256140_, HolderGetter<ConfiguredWorldCarver<?>> p_256223_)
```

**Parameters:**

- `p_256140_` (`HolderGetter<PlacedFeature>`)
- `p_256223_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### swamp

```java
public static Biome swamp(HolderGetter<PlacedFeature> p_256058_, HolderGetter<ConfiguredWorldCarver<?>> p_256016_)
```

**Parameters:**

- `p_256058_` (`HolderGetter<PlacedFeature>`)
- `p_256016_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### mangroveSwamp

```java
public static Biome mangroveSwamp(HolderGetter<PlacedFeature> p_256353_, HolderGetter<ConfiguredWorldCarver<?>> p_256103_)
```

**Parameters:**

- `p_256353_` (`HolderGetter<PlacedFeature>`)
- `p_256103_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### river

```java
public static Biome river(HolderGetter<PlacedFeature> p_256613_, HolderGetter<ConfiguredWorldCarver<?>> p_256581_, boolean p_194915_)
```

**Parameters:**

- `p_256613_` (`HolderGetter<PlacedFeature>`)
- `p_256581_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_194915_` (`boolean`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome(p_194915_ ? 3750089 : 4159204)
```

**Parameters:**

- `4159204` (`p_194915_ ? 3750089 :`)

**Returns:** `return`

### beach

```java
public static Biome beach(HolderGetter<PlacedFeature> p_256157_, HolderGetter<ConfiguredWorldCarver<?>> p_255712_, boolean p_194889_, boolean p_194890_)
```

**Parameters:**

- `p_256157_` (`HolderGetter<PlacedFeature>`)
- `p_255712_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_194889_` (`boolean`)
- `p_194890_` (`boolean`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome(flag ? 0.4F : 0.3F, p_194889_ ? 4020182 : 4159204)
```

**Parameters:**

- `0.3F` (`flag ? 0.4F :`)
- `4159204` (`p_194889_ ? 4020182 :`)

**Returns:** `return`

### theVoid

```java
public static Biome theVoid(HolderGetter<PlacedFeature> p_256509_, HolderGetter<ConfiguredWorldCarver<?>> p_256544_)
```

**Parameters:**

- `p_256509_` (`HolderGetter<PlacedFeature>`)
- `p_256544_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### meadowOrCherryGrove

```java
public static Biome meadowOrCherryGrove(HolderGetter<PlacedFeature> p_273564_, HolderGetter<ConfiguredWorldCarver<?>> p_273374_, boolean p_273710_)
```

**Parameters:**

- `p_273564_` (`HolderGetter<PlacedFeature>`)
- `p_273374_` (`HolderGetter<ConfiguredWorldCarver<?>>`)
- `p_273710_` (`boolean`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### frozenPeaks

```java
public static Biome frozenPeaks(HolderGetter<PlacedFeature> p_255713_, HolderGetter<ConfiguredWorldCarver<?>> p_256092_)
```

**Parameters:**

- `p_255713_` (`HolderGetter<PlacedFeature>`)
- `p_256092_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome()
```

**Returns:** `return`

### jaggedPeaks

```java
public static Biome jaggedPeaks(HolderGetter<PlacedFeature> p_256512_, HolderGetter<ConfiguredWorldCarver<?>> p_255908_)
```

**Parameters:**

- `p_256512_` (`HolderGetter<PlacedFeature>`)
- `p_255908_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome()
```

**Returns:** `return`

### stonyPeaks

```java
public static Biome stonyPeaks(HolderGetter<PlacedFeature> p_256490_, HolderGetter<ConfiguredWorldCarver<?>> p_255694_)
```

**Parameters:**

- `p_256490_` (`HolderGetter<PlacedFeature>`)
- `p_255694_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome()
```

**Returns:** `return`

### snowySlopes

```java
public static Biome snowySlopes(HolderGetter<PlacedFeature> p_255927_, HolderGetter<ConfiguredWorldCarver<?>> p_255982_)
```

**Parameters:**

- `p_255927_` (`HolderGetter<PlacedFeature>`)
- `p_255982_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome()
```

**Returns:** `return`

### grove

```java
public static Biome grove(HolderGetter<PlacedFeature> p_256094_, HolderGetter<ConfiguredWorldCarver<?>> p_256431_)
```

**Parameters:**

- `p_256094_` (`HolderGetter<PlacedFeature>`)
- `p_256431_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome()
```

**Returns:** `return`

### lushCaves

```java
public static Biome lushCaves(HolderGetter<PlacedFeature> p_255944_, HolderGetter<ConfiguredWorldCarver<?>> p_255654_)
```

**Parameters:**

- `p_255944_` (`HolderGetter<PlacedFeature>`)
- `p_255654_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome()
```

**Returns:** `return`

### dripstoneCaves

```java
public static Biome dripstoneCaves(HolderGetter<PlacedFeature> p_256253_, HolderGetter<ConfiguredWorldCarver<?>> p_255644_)
```

**Parameters:**

- `p_256253_` (`HolderGetter<PlacedFeature>`)
- `p_255644_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### globalOverworldGeneration

```java
 globalOverworldGeneration()
```

**Returns:** ``

### biome

```java
return biome()
```

**Returns:** `return`

### deepDark

```java
public static Biome deepDark(HolderGetter<PlacedFeature> p_256073_, HolderGetter<ConfiguredWorldCarver<?>> p_256212_)
```

**Parameters:**

- `p_256073_` (`HolderGetter<PlacedFeature>`)
- `p_256212_` (`HolderGetter<ConfiguredWorldCarver<?>>`)

**Returns:** `public static Biome`

### biome

```java
return biome()
```

**Returns:** `return`
