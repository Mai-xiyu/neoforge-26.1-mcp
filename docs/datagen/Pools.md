# Pools

**Package:** `net.minecraft.data.worldgen`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `EMPTY` | `ResourceKey<StructureTemplatePool>` |  |

## Methods

### createKey

```java
public static ResourceKey<StructureTemplatePool> createKey(String p_256439_)
```

**Parameters:**

- `p_256439_` (`String`)

**Returns:** `public static ResourceKey<StructureTemplatePool>`

### parseKey

```java
public static ResourceKey<StructureTemplatePool> parseKey(String p_350360_)
```

**Parameters:**

- `p_350360_` (`String`)

**Returns:** `public static ResourceKey<StructureTemplatePool>`

### register

```java
public static void register(BootstrapContext<StructureTemplatePool> p_321699_, String p_255837_, StructureTemplatePool p_256161_)
```

**Parameters:**

- `p_321699_` (`BootstrapContext<StructureTemplatePool>`)
- `p_255837_` (`String`)
- `p_256161_` (`StructureTemplatePool`)

**Returns:** `public static void`

### bootstrap

```java
public static void bootstrap(BootstrapContext<StructureTemplatePool> p_321867_)
```

**Parameters:**

- `p_321867_` (`BootstrapContext<StructureTemplatePool>`)

**Returns:** `public static void`
