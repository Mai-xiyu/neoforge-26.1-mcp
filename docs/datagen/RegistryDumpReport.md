# RegistryDumpReport

**Package:** `net.minecraft.data.info`
**Type:** class
**Implements:** `DataProvider`

## Methods

### RegistryDumpReport

```java
public RegistryDumpReport(PackOutput p_249862_)
```

**Parameters:**

- `p_249862_` (`PackOutput`)

**Returns:** `public`

### run

```java
public CompletableFuture<?> run(CachedOutput p_253743_)
```

**Parameters:**

- `p_253743_` (`CachedOutput`)

**Returns:** `CompletableFuture<?>`

### dumpRegistry

```java
<T> private static <T> JsonElement dumpRegistry(Registry<T> p_124059_)
```

**Parameters:**

- `p_124059_` (`Registry<T>`)

**Returns:** `private static <T> JsonElement`

### getName

```java
public final String getName()
```

**Returns:** `String`
