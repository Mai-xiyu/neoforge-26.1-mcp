# SimpleCookingRecipeBuilder

**Package:** `net.minecraft.data.recipes`
**Type:** class
**Implements:** `RecipeBuilder`

## Methods

### SimpleCookingRecipeBuilder

```java
private SimpleCookingRecipeBuilder(RecipeCategory p_251345_, CookingBookCategory p_251607_, ItemLike p_252112_, Ingredient p_250362_, float p_251204_, int p_250189_, AbstractCookingRecipe.Factory<?> p_311960_)
```

**Parameters:**

- `p_251345_` (`RecipeCategory`)
- `p_251607_` (`CookingBookCategory`)
- `p_252112_` (`ItemLike`)
- `p_250362_` (`Ingredient`)
- `p_251204_` (`float`)
- `p_250189_` (`int`)
- `p_311960_` (`AbstractCookingRecipe.Factory<?>`)

**Returns:** `private`

### SimpleCookingRecipeBuilder

```java
private SimpleCookingRecipeBuilder(RecipeCategory p_251345_, CookingBookCategory p_251607_, ItemStack result, Ingredient p_250362_, float p_251204_, int p_250189_, AbstractCookingRecipe.Factory<?> p_311960_)
```

**Parameters:**

- `p_251345_` (`RecipeCategory`)
- `p_251607_` (`CookingBookCategory`)
- `result` (`ItemStack`)
- `p_250362_` (`Ingredient`)
- `p_251204_` (`float`)
- `p_250189_` (`int`)
- `p_311960_` (`AbstractCookingRecipe.Factory<?>`)

**Returns:** `private`

### generic

```java
<T extends AbstractCookingRecipe> public static <T extends AbstractCookingRecipe> SimpleCookingRecipeBuilder generic(Ingredient p_250999_, RecipeCategory p_248815_, ItemLike p_249766_, float p_251320_, int p_248693_, RecipeSerializer<T> p_250921_, AbstractCookingRecipe.Factory<T> p_312657_)
```

**Parameters:**

- `p_250999_` (`Ingredient`)
- `p_248815_` (`RecipeCategory`)
- `p_249766_` (`ItemLike`)
- `p_251320_` (`float`)
- `p_248693_` (`int`)
- `p_250921_` (`RecipeSerializer<T>`)
- `p_312657_` (`AbstractCookingRecipe.Factory<T>`)

**Returns:** `public static <T extends AbstractCookingRecipe> SimpleCookingRecipeBuilder`

### campfireCooking

```java
public static SimpleCookingRecipeBuilder campfireCooking(Ingredient p_249393_, RecipeCategory p_249372_, ItemLike p_251516_, float p_252321_, int p_251916_)
```

**Parameters:**

- `p_249393_` (`Ingredient`)
- `p_249372_` (`RecipeCategory`)
- `p_251516_` (`ItemLike`)
- `p_252321_` (`float`)
- `p_251916_` (`int`)

**Returns:** `public static SimpleCookingRecipeBuilder`

### SimpleCookingRecipeBuilder

```java
return new SimpleCookingRecipeBuilder()
```

**Returns:** `return new`

### blasting

```java
public static SimpleCookingRecipeBuilder blasting(Ingredient p_252115_, RecipeCategory p_249421_, ItemLike p_251247_, float p_250383_, int p_250476_)
```

**Parameters:**

- `p_252115_` (`Ingredient`)
- `p_249421_` (`RecipeCategory`)
- `p_251247_` (`ItemLike`)
- `p_250383_` (`float`)
- `p_250476_` (`int`)

**Returns:** `public static SimpleCookingRecipeBuilder`

### smelting

```java
public static SimpleCookingRecipeBuilder smelting(Ingredient p_249223_, RecipeCategory p_251240_, ItemLike p_249551_, float p_249452_, int p_250496_)
```

**Parameters:**

- `p_249223_` (`Ingredient`)
- `p_251240_` (`RecipeCategory`)
- `p_249551_` (`ItemLike`)
- `p_249452_` (`float`)
- `p_250496_` (`int`)

**Returns:** `public static SimpleCookingRecipeBuilder`

### smoking

```java
public static SimpleCookingRecipeBuilder smoking(Ingredient p_248930_, RecipeCategory p_250319_, ItemLike p_250377_, float p_252329_, int p_250482_)
```

**Parameters:**

- `p_248930_` (`Ingredient`)
- `p_250319_` (`RecipeCategory`)
- `p_250377_` (`ItemLike`)
- `p_252329_` (`float`)
- `p_250482_` (`int`)

**Returns:** `public static SimpleCookingRecipeBuilder`

### SimpleCookingRecipeBuilder

```java
return new SimpleCookingRecipeBuilder()
```

**Returns:** `return new`

### generic

```java
<T extends AbstractCookingRecipe> public static <T extends AbstractCookingRecipe> SimpleCookingRecipeBuilder generic(Ingredient p_250999_, RecipeCategory p_248815_, ItemStack result, float p_251320_, int p_248693_, RecipeSerializer<T> p_250921_, AbstractCookingRecipe.Factory<T> p_312657_)
```

**Parameters:**

- `p_250999_` (`Ingredient`)
- `p_248815_` (`RecipeCategory`)
- `result` (`ItemStack`)
- `p_251320_` (`float`)
- `p_248693_` (`int`)
- `p_250921_` (`RecipeSerializer<T>`)
- `p_312657_` (`AbstractCookingRecipe.Factory<T>`)

**Returns:** `public static <T extends AbstractCookingRecipe> SimpleCookingRecipeBuilder`

### campfireCooking

```java
public static SimpleCookingRecipeBuilder campfireCooking(Ingredient p_249393_, RecipeCategory p_249372_, ItemStack result, float p_252321_, int p_251916_)
```

**Parameters:**

- `p_249393_` (`Ingredient`)
- `p_249372_` (`RecipeCategory`)
- `result` (`ItemStack`)
- `p_252321_` (`float`)
- `p_251916_` (`int`)

**Returns:** `public static SimpleCookingRecipeBuilder`

### SimpleCookingRecipeBuilder

```java
return new SimpleCookingRecipeBuilder()
```

**Returns:** `return new`

### blasting

```java
public static SimpleCookingRecipeBuilder blasting(Ingredient p_252115_, RecipeCategory p_249421_, ItemStack result, float p_250383_, int p_250476_)
```

**Parameters:**

- `p_252115_` (`Ingredient`)
- `p_249421_` (`RecipeCategory`)
- `result` (`ItemStack`)
- `p_250383_` (`float`)
- `p_250476_` (`int`)

**Returns:** `public static SimpleCookingRecipeBuilder`

### smelting

```java
public static SimpleCookingRecipeBuilder smelting(Ingredient p_249223_, RecipeCategory p_251240_, ItemStack result, float p_249452_, int p_250496_)
```

**Parameters:**

- `p_249223_` (`Ingredient`)
- `p_251240_` (`RecipeCategory`)
- `result` (`ItemStack`)
- `p_249452_` (`float`)
- `p_250496_` (`int`)

**Returns:** `public static SimpleCookingRecipeBuilder`

### smoking

```java
public static SimpleCookingRecipeBuilder smoking(Ingredient p_248930_, RecipeCategory p_250319_, ItemStack result, float p_252329_, int p_250482_)
```

**Parameters:**

- `p_248930_` (`Ingredient`)
- `p_250319_` (`RecipeCategory`)
- `result` (`ItemStack`)
- `p_252329_` (`float`)
- `p_250482_` (`int`)

**Returns:** `public static SimpleCookingRecipeBuilder`

### SimpleCookingRecipeBuilder

```java
return new SimpleCookingRecipeBuilder()
```

**Returns:** `return new`

### unlockedBy

```java
public SimpleCookingRecipeBuilder unlockedBy(String p_176792_, Criterion<?> p_300970_)
```

**Parameters:**

- `p_176792_` (`String`)
- `p_300970_` (`Criterion<?>`)

**Returns:** `public SimpleCookingRecipeBuilder`

### group

```java
public SimpleCookingRecipeBuilder group(String p_176795_)
```

**Parameters:**

- `p_176795_` (`String`)

**Returns:** `public SimpleCookingRecipeBuilder`

### getResult

```java
public Item getResult()
```

**Returns:** `Item`

### save

```java
public void save(RecipeOutput p_301266_, ResourceLocation p_126264_)
```

**Parameters:**

- `p_301266_` (`RecipeOutput`)
- `p_126264_` (`ResourceLocation`)

### determineSmeltingRecipeCategory

```java
private static CookingBookCategory determineSmeltingRecipeCategory(ItemLike p_251938_)
```

**Parameters:**

- `p_251938_` (`ItemLike`)

**Returns:** `private static CookingBookCategory`

### determineBlastingRecipeCategory

```java
private static CookingBookCategory determineBlastingRecipeCategory(ItemLike p_249047_)
```

**Parameters:**

- `p_249047_` (`ItemLike`)

**Returns:** `private static CookingBookCategory`

### determineRecipeCategory

```java
private static CookingBookCategory determineRecipeCategory(RecipeSerializer<? extends AbstractCookingRecipe> p_251261_, ItemLike p_249582_)
```

**Parameters:**

- `p_251261_` (`RecipeSerializer<? extends AbstractCookingRecipe>`)
- `p_249582_` (`ItemLike`)

**Returns:** `private static CookingBookCategory`

### determineSmeltingRecipeCategory

```java
return determineSmeltingRecipeCategory()
```

**Returns:** `return`

### determineBlastingRecipeCategory

```java
return determineBlastingRecipeCategory()
```

**Returns:** `return`

### IllegalStateException

```java
throw new IllegalStateException("Unknown cooking recipe type")
```

**Parameters:**

- `type"` (`"Unknown cooking recipe`)

**Returns:** `throw new`

### ensureValid

```java
private void ensureValid(ResourceLocation p_126266_)
```

**Parameters:**

- `p_126266_` (`ResourceLocation`)

**Returns:** `private void`

### IllegalStateException

```java
throw new IllegalStateException("No way of obtaining recipe " + p_126266_)
```

**Parameters:**

- `p_126266_` (`"No way of obtaining recipe " +`)

**Returns:** `throw new`
