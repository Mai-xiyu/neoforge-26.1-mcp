# SingleItemRecipeBuilder

**Package:** `net.minecraft.data.recipes`
**Type:** class
**Implements:** `RecipeBuilder`

## Methods

### SingleItemRecipeBuilder

```java
public SingleItemRecipeBuilder(RecipeCategory p_251425_, SingleItemRecipe.Factory<?> p_312361_, Ingredient p_251221_, ItemLike p_251302_, int p_250964_)
```

**Parameters:**

- `p_251425_` (`RecipeCategory`)
- `p_312361_` (`SingleItemRecipe.Factory<?>`)
- `p_251221_` (`Ingredient`)
- `p_251302_` (`ItemLike`)
- `p_250964_` (`int`)

**Returns:** `public`

### stonecutting

```java
public static SingleItemRecipeBuilder stonecutting(Ingredient p_248596_, RecipeCategory p_250503_, ItemLike p_250269_)
```

**Parameters:**

- `p_248596_` (`Ingredient`)
- `p_250503_` (`RecipeCategory`)
- `p_250269_` (`ItemLike`)

**Returns:** `public static SingleItemRecipeBuilder`

### SingleItemRecipeBuilder

```java
return new SingleItemRecipeBuilder()
```

**Returns:** `return new`

### stonecutting

```java
public static SingleItemRecipeBuilder stonecutting(Ingredient p_251375_, RecipeCategory p_248984_, ItemLike p_250105_, int p_249506_)
```

**Parameters:**

- `p_251375_` (`Ingredient`)
- `p_248984_` (`RecipeCategory`)
- `p_250105_` (`ItemLike`)
- `p_249506_` (`int`)

**Returns:** `public static SingleItemRecipeBuilder`

### SingleItemRecipeBuilder

```java
return new SingleItemRecipeBuilder()
```

**Returns:** `return new`

### unlockedBy

```java
public SingleItemRecipeBuilder unlockedBy(String p_176810_, Criterion<?> p_301267_)
```

**Parameters:**

- `p_176810_` (`String`)
- `p_301267_` (`Criterion<?>`)

**Returns:** `public SingleItemRecipeBuilder`

### group

```java
public SingleItemRecipeBuilder group(String p_176808_)
```

**Parameters:**

- `p_176808_` (`String`)

**Returns:** `public SingleItemRecipeBuilder`

### getResult

```java
public Item getResult()
```

**Returns:** `Item`

### save

```java
public void save(RecipeOutput p_301137_, ResourceLocation p_126328_)
```

**Parameters:**

- `p_301137_` (`RecipeOutput`)
- `p_126328_` (`ResourceLocation`)

### ensureValid

```java
private void ensureValid(ResourceLocation p_126330_)
```

**Parameters:**

- `p_126330_` (`ResourceLocation`)

**Returns:** `private void`

### IllegalStateException

```java
throw new IllegalStateException("No way of obtaining recipe " + p_126330_)
```

**Parameters:**

- `p_126330_` (`"No way of obtaining recipe " +`)

**Returns:** `throw new`
