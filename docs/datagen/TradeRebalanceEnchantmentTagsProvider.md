# TradeRebalanceEnchantmentTagsProvider

**Package:** `net.minecraft.data.tags`
**Type:** class
**Extends:** `TagsProvider<Enchantment>`

## Methods

### TradeRebalanceEnchantmentTagsProvider

```java
public TradeRebalanceEnchantmentTagsProvider(PackOutput p_345802_, CompletableFuture<HolderLookup.Provider> p_345863_)
```

**Parameters:**

- `p_345802_` (`PackOutput`)
- `p_345863_` (`CompletableFuture<HolderLookup.Provider>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### addTags

```java
protected void addTags(HolderLookup.Provider p_345111_)
```

**Parameters:**

- `p_345111_` (`HolderLookup.Provider`)
