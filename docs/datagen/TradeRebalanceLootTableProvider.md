# TradeRebalanceLootTableProvider

**Package:** `net.minecraft.data.loot.packs`
**Type:** class

## Methods

### create

```java
public static LootTableProvider create(PackOutput p_301985_, CompletableFuture<HolderLookup.Provider> p_324288_)
```

**Parameters:**

- `p_301985_` (`PackOutput`)
- `p_324288_` (`CompletableFuture<HolderLookup.Provider>`)

**Returns:** `public static LootTableProvider`
