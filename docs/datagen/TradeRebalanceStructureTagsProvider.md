# TradeRebalanceStructureTagsProvider

**Package:** `net.minecraft.data.tags`
**Type:** class
**Extends:** `TagsProvider<Structure>`

## Methods

### TradeRebalanceStructureTagsProvider

```java
public TradeRebalanceStructureTagsProvider(PackOutput p_302237_, CompletableFuture<HolderLookup.Provider> p_302233_)
```

**Parameters:**

- `p_302237_` (`PackOutput`)
- `p_302233_` (`CompletableFuture<HolderLookup.Provider>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### addTags

```java
protected void addTags(HolderLookup.Provider p_302229_)
```

**Parameters:**

- `p_302229_` (`HolderLookup.Provider`)
