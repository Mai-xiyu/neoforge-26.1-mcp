# VanillaBlockTagsProvider

**Package:** `net.minecraft.data.tags`
**Type:** class
**Extends:** `IntrinsicHolderTagsProvider<Block>`

## Methods

### VanillaBlockTagsProvider

```java
public VanillaBlockTagsProvider(PackOutput p_256406_, CompletableFuture<HolderLookup.Provider> p_256525_)
```

**Parameters:**

- `p_256406_` (`PackOutput`)
- `p_256525_` (`CompletableFuture<HolderLookup.Provider>`)

**Returns:** `public`

### addTags

```java
protected void addTags(HolderLookup.Provider p_255662_)
```

**Parameters:**

- `p_255662_` (`HolderLookup.Provider`)
