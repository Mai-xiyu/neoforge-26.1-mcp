# VanillaItemTagsProvider

**Package:** `net.minecraft.data.tags`
**Type:** class
**Extends:** `ItemTagsProvider`

## Methods

### VanillaItemTagsProvider

```java
public VanillaItemTagsProvider(PackOutput p_275657_, CompletableFuture<HolderLookup.Provider> p_275712_, CompletableFuture<TagsProvider.TagLookup<Block>> p_275572_)
```

**Parameters:**

- `p_275657_` (`PackOutput`)
- `p_275712_` (`CompletableFuture<HolderLookup.Provider>`)
- `p_275572_` (`CompletableFuture<TagsProvider.TagLookup<Block>>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### addTags

```java
protected void addTags(HolderLookup.Provider p_255639_)
```

**Parameters:**

- `p_255639_` (`HolderLookup.Provider`)
