# VanillaRegistries

**Package:** `net.minecraft.data.registries`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `DATAPACK_REGISTRY_KEYS` | `List<? extends net.minecraft.resources.ResourceKey<? extends net.minecraft.core.Registry<?>>>` |  |

## Methods

### validateThatAllBiomeFeaturesHaveBiomeFilter

```java
private static void validateThatAllBiomeFeaturesHaveBiomeFilter(HolderLookup.Provider p_256242_)
```

**Parameters:**

- `p_256242_` (`HolderLookup.Provider`)

**Returns:** `private static void`

### validateThatAllBiomeFeaturesHaveBiomeFilter

```java
public static void validateThatAllBiomeFeaturesHaveBiomeFilter(HolderGetter<PlacedFeature> p_272963_, HolderLookup<Biome> p_273693_)
```

**Parameters:**

- `p_272963_` (`HolderGetter<PlacedFeature>`)
- `p_273693_` (`HolderLookup<Biome>`)

**Returns:** `public static void`

### validatePlacedFeature

```java
private static boolean validatePlacedFeature(PlacedFeature p_255656_)
```

**Parameters:**

- `p_255656_` (`PlacedFeature`)

**Returns:** `private static boolean`

### createLookup

```java
public static HolderLookup.Provider createLookup()
```

**Returns:** `public static HolderLookup.Provider`

### validateThatAllBiomeFeaturesHaveBiomeFilter

```java
 validateThatAllBiomeFeaturesHaveBiomeFilter()
```

**Returns:** ``
