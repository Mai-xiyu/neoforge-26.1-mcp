# WorldPresetTagsProvider

**Package:** `net.minecraft.data.tags`
**Type:** class
**Extends:** `TagsProvider<WorldPreset>`

## Methods

### WorldPresetTagsProvider

```java
public public WorldPresetTagsProvider(PackOutput p_255701_, CompletableFuture<HolderLookup.Provider> p_255974_)
```

**Parameters:**

- `p_255701_` (`PackOutput`)
- `p_255974_` (`CompletableFuture<HolderLookup.Provider>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### WorldPresetTagsProvider

```java
public WorldPresetTagsProvider(PackOutput p_255701_, CompletableFuture<HolderLookup.Provider> p_255974_, String modId, .jetbrains.annotations.Nullable net.neoforged.neoforge.common.data.ExistingFileHelper existingFileHelper)
```

**Parameters:**

- `p_255701_` (`PackOutput`)
- `p_255974_` (`CompletableFuture<HolderLookup.Provider>`)
- `modId` (`String`)
- `existingFileHelper` (`.jetbrains.annotations.Nullable net.neoforged.neoforge.common.data.ExistingFileHelper`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### addTags

```java
protected void addTags(HolderLookup.Provider p_255734_)
```

**Parameters:**

- `p_255734_` (`HolderLookup.Provider`)
