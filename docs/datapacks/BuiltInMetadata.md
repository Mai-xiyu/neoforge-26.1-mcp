# BuiltInMetadata

**Package:** `net.minecraft.server.packs`
**Type:** class
**Side:** 🖧 Server

## Methods

### BuiltInMetadata

```java
private BuiltInMetadata(Map<MetadataSectionSerializer<?>, ?> p_251588_)
```

**Parameters:**

- `p_251588_` (`Map<MetadataSectionSerializer<?>, ?>`)

**Returns:** `private`

### get

```java
<T> public <T> T get(MetadataSectionSerializer<T> p_251597_)
```

**Parameters:**

- `p_251597_` (`MetadataSectionSerializer<T>`)

**Returns:** `public <T> T`

### of

```java
public static BuiltInMetadata of()
```

**Returns:** `public static BuiltInMetadata`

### of

```java
<T> public static <T> BuiltInMetadata of(MetadataSectionSerializer<T> p_248992_, T p_249997_)
```

**Parameters:**

- `p_248992_` (`MetadataSectionSerializer<T>`)
- `p_249997_` (`T`)

**Returns:** `public static <T> BuiltInMetadata`

### of

```java
<T1, T2> public static <T1, T2> BuiltInMetadata of(MetadataSectionSerializer<T1> p_252035_, T1 p_252174_, MetadataSectionSerializer<T2> p_249734_, T2 p_250020_)
```

**Parameters:**

- `p_252035_` (`MetadataSectionSerializer<T1>`)
- `p_252174_` (`T1`)
- `p_249734_` (`MetadataSectionSerializer<T2>`)
- `p_250020_` (`T2`)

**Returns:** `public static <T1, T2> BuiltInMetadata`
