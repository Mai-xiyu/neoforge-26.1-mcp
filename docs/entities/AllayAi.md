# AllayAi

**Package:** `net.minecraft.world.entity.animal.allay`
**Type:** class

## Methods

### makeBrain

```java
protected static Brain<?> makeBrain(Brain<Allay> p_218420_)
```

**Parameters:**

- `p_218420_` (`Brain<Allay>`)

**Returns:** `protected static Brain<?>`

### initCoreActivity

```java
 initCoreActivity()
```

**Returns:** ``

### initIdleActivity

```java
 initIdleActivity()
```

**Returns:** ``

### initCoreActivity

```java
private static void initCoreActivity(Brain<Allay> p_218426_)
```

**Parameters:**

- `p_218426_` (`Brain<Allay>`)

**Returns:** `private static void`

### initIdleActivity

```java
private static void initIdleActivity(Brain<Allay> p_218432_)
```

**Parameters:**

- `p_218432_` (`Brain<Allay>`)

**Returns:** `private static void`

### updateActivity

```java
public static void updateActivity(Allay p_218422_)
```

**Parameters:**

- `p_218422_` (`Allay`)

**Returns:** `public static void`

### hearNoteblock

```java
public static void hearNoteblock(LivingEntity p_218417_, BlockPos p_218418_)
```

**Parameters:**

- `p_218417_` (`LivingEntity`)
- `p_218418_` (`BlockPos`)

**Returns:** `public static void`

### getItemDepositPosition

```java
private static Optional<PositionTracker> getItemDepositPosition(LivingEntity p_218424_)
```

**Parameters:**

- `p_218424_` (`LivingEntity`)

**Returns:** `private static Optional<PositionTracker>`

### getLikedPlayerPositionTracker

```java
return getLikedPlayerPositionTracker()
```

**Returns:** `return`

### hasWantedItem

```java
private static boolean hasWantedItem(LivingEntity p_273346_)
```

**Parameters:**

- `p_273346_` (`LivingEntity`)

**Returns:** `private static boolean`

### shouldDepositItemsAtLikedNoteblock

```java
private static boolean shouldDepositItemsAtLikedNoteblock(LivingEntity p_218413_, Brain<?> p_218414_, GlobalPos p_218415_)
```

**Parameters:**

- `p_218413_` (`LivingEntity`)
- `p_218414_` (`Brain<?>`)
- `p_218415_` (`GlobalPos`)

**Returns:** `private static boolean`

### getLikedPlayerPositionTracker

```java
private static Optional<PositionTracker> getLikedPlayerPositionTracker(LivingEntity p_218430_)
```

**Parameters:**

- `p_218430_` (`LivingEntity`)

**Returns:** `private static Optional<PositionTracker>`

### getLikedPlayer

```java
public static Optional<ServerPlayer> getLikedPlayer(LivingEntity p_218411_)
```

**Parameters:**

- `p_218411_` (`LivingEntity`)

**Returns:** `public static Optional<ServerPlayer>`
