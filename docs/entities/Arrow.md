# Arrow

**Package:** `net.minecraft.world.entity.projectile`
**Type:** class
**Extends:** `AbstractArrow`

## Methods

### Arrow

```java
public Arrow(EntityType<? extends Arrow> p_36858_, Level p_36859_)
```

**Parameters:**

- `p_36858_` (`EntityType<? extends Arrow>`)
- `p_36859_` (`Level`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### Arrow

```java
public Arrow(Level p_36861_, double p_345459_, double p_346135_, double p_345618_, ItemStack p_309188_, ItemStack p_345233_)
```

**Parameters:**

- `p_36861_` (`Level`)
- `p_345459_` (`double`)
- `p_346135_` (`double`)
- `p_345618_` (`double`)
- `p_309188_` (`ItemStack`)
- `p_345233_` (`ItemStack`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### Arrow

```java
public Arrow(Level p_36866_, LivingEntity p_345746_, ItemStack p_309200_, ItemStack p_345461_)
```

**Parameters:**

- `p_36866_` (`Level`)
- `p_345746_` (`LivingEntity`)
- `p_309200_` (`ItemStack`)
- `p_345461_` (`ItemStack`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getPotionContents

```java
private PotionContents getPotionContents()
```

**Returns:** `private PotionContents`

### setPotionContents

```java
private void setPotionContents(PotionContents p_331534_)
```

**Parameters:**

- `p_331534_` (`PotionContents`)

**Returns:** `private void`

### setPickupItemStack

```java
protected void setPickupItemStack(ItemStack p_331667_)
```

**Parameters:**

- `p_331667_` (`ItemStack`)

### updateColor

```java
private void updateColor()
```

**Returns:** `private void`

### addEffect

```java
public void addEffect(MobEffectInstance p_36871_)
```

**Parameters:**

- `p_36871_` (`MobEffectInstance`)

**Returns:** `public void`

### defineSynchedData

```java
protected void defineSynchedData(SynchedEntityData.Builder p_326324_)
```

**Parameters:**

- `p_326324_` (`SynchedEntityData.Builder`)

### tick

```java
public void tick()
```

### makeParticle

```java
private void makeParticle(int p_36877_)
```

**Parameters:**

- `p_36877_` (`int`)

**Returns:** `private void`

### getColor

```java
public int getColor()
```

**Returns:** `public int`

### doPostHurtEffects

```java
protected void doPostHurtEffects(LivingEntity p_36873_)
```

**Parameters:**

- `p_36873_` (`LivingEntity`)

### getDefaultPickupItem

```java
protected ItemStack getDefaultPickupItem()
```

**Returns:** `ItemStack`

### ItemStack

```java
return new ItemStack()
```

**Returns:** `return new`

### handleEntityEvent

```java
public void handleEntityEvent(byte p_36869_)
```

**Parameters:**

- `p_36869_` (`byte`)
