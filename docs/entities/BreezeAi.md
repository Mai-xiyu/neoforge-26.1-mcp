# BreezeAi

**Package:** `net.minecraft.world.entity.monster.breeze`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `SPEED_MULTIPLIER_WHEN_SLIDING` | `float` |  |
| `JUMP_CIRCLE_INNER_RADIUS` | `float` |  |
| `JUMP_CIRCLE_MIDDLE_RADIUS` | `float` |  |
| `JUMP_CIRCLE_OUTER_RADIUS` | `float` |  |

## Methods

### makeBrain

```java
protected static Brain<?> makeBrain(Breeze p_350858_, Brain<Breeze> p_312887_)
```

**Parameters:**

- `p_350858_` (`Breeze`)
- `p_312887_` (`Brain<Breeze>`)

**Returns:** `protected static Brain<?>`

### initCoreActivity

```java
 initCoreActivity()
```

**Returns:** ``

### initIdleActivity

```java
 initIdleActivity()
```

**Returns:** ``

### initFightActivity

```java
 initFightActivity()
```

**Returns:** ``

### initCoreActivity

```java
private static void initCoreActivity(Brain<Breeze> p_312774_)
```

**Parameters:**

- `p_312774_` (`Brain<Breeze>`)

**Returns:** `private static void`

### initIdleActivity

```java
private static void initIdleActivity(Brain<Breeze> p_316741_)
```

**Parameters:**

- `p_316741_` (`Brain<Breeze>`)

**Returns:** `private static void`

### initFightActivity

```java
private static void initFightActivity(Breeze p_350515_, Brain<Breeze> p_312350_)
```

**Parameters:**

- `p_350515_` (`Breeze`)
- `p_312350_` (`Brain<Breeze>`)

**Returns:** `private static void`

### updateActivity

```java
static void updateActivity(Breeze p_316353_)
```

**Parameters:**

- `p_316353_` (`Breeze`)

**Returns:** `static void`

### SlideToTargetSink

```java
public public SlideToTargetSink(int p_311828_, int p_312532_)
```

**Parameters:**

- `p_311828_` (`int`)
- `p_312532_` (`int`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### start

```java
protected void start(ServerLevel p_312732_, Mob p_312543_, long p_312612_)
```

**Parameters:**

- `p_312732_` (`ServerLevel`)
- `p_312543_` (`Mob`)
- `p_312612_` (`long`)

### stop

```java
protected void stop(ServerLevel p_312932_, Mob p_311871_, long p_312594_)
```

**Parameters:**

- `p_312932_` (`ServerLevel`)
- `p_311871_` (`Mob`)
- `p_312594_` (`long`)

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `SlideToTargetSink` | class |  |
