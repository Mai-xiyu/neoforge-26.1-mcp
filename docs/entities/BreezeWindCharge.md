# BreezeWindCharge

**Package:** `net.minecraft.world.entity.projectile.windcharge`
**Type:** class
**Extends:** `AbstractWindCharge`

## Methods

### BreezeWindCharge

```java
public BreezeWindCharge(EntityType<? extends AbstractWindCharge> p_326366_, Level p_325976_)
```

**Parameters:**

- `p_326366_` (`EntityType<? extends AbstractWindCharge>`)
- `p_325976_` (`Level`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### BreezeWindCharge

```java
public BreezeWindCharge(Breeze p_326239_, Level p_326041_)
```

**Parameters:**

- `p_326239_` (`Breeze`)
- `p_326041_` (`Level`)

**Returns:** `public`

### explode

```java
protected void explode(Vec3 p_352274_)
```

**Parameters:**

- `p_352274_` (`Vec3`)
