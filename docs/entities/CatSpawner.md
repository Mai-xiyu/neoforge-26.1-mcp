# CatSpawner

**Package:** `net.minecraft.world.entity.npc`
**Type:** class
**Implements:** `CustomSpawner`

## Methods

### tick

```java
public int tick(ServerLevel p_35330_, boolean p_35331_, boolean p_35332_)
```

**Parameters:**

- `p_35330_` (`ServerLevel`)
- `p_35331_` (`boolean`)
- `p_35332_` (`boolean`)

**Returns:** `int`

### spawnInVillage

```java
private int spawnInVillage(ServerLevel p_35327_, BlockPos p_35328_)
```

**Parameters:**

- `p_35327_` (`ServerLevel`)
- `p_35328_` (`BlockPos`)

**Returns:** `private int`

### spawnInHut

```java
private int spawnInHut(ServerLevel p_35337_, BlockPos p_35338_)
```

**Parameters:**

- `p_35337_` (`ServerLevel`)
- `p_35338_` (`BlockPos`)

**Returns:** `private int`

### spawnCat

```java
private int spawnCat(BlockPos p_35334_, ServerLevel p_35335_)
```

**Parameters:**

- `p_35334_` (`BlockPos`)
- `p_35335_` (`ServerLevel`)

**Returns:** `private int`
