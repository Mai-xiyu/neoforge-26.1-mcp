# EnderDragonPhaseManager

**Package:** `net.minecraft.world.entity.boss.enderdragon.phases`
**Type:** class

## Methods

### EnderDragonPhaseManager

```java
public EnderDragonPhaseManager(EnderDragon p_31414_)
```

**Parameters:**

- `p_31414_` (`EnderDragon`)

**Returns:** `public`

### setPhase

```java
public void setPhase(EnderDragonPhase<?> p_31417_)
```

**Parameters:**

- `p_31417_` (`EnderDragonPhase<?>`)

**Returns:** `public void`

### getCurrentPhase

```java
public DragonPhaseInstance getCurrentPhase()
```

**Returns:** `public DragonPhaseInstance`

### getPhase

```java
<T extends DragonPhaseInstance> public <T extends DragonPhaseInstance> T getPhase(EnderDragonPhase<T> p_31419_)
```

**Parameters:**

- `p_31419_` (`EnderDragonPhase<T>`)

**Returns:** `public <T extends DragonPhaseInstance> T`
