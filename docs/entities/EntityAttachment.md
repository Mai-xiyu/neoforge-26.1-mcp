# EntityAttachment

**Package:** `net.minecraft.world.entity`
**Type:** enum

## Methods

### WARDEN_CHEST

```java
, WARDEN_CHEST()
```

**Returns:** `,`

### EntityAttachment

```java
private EntityAttachment(EntityAttachment.Fallback p_316243_)
```

**Parameters:**

- `p_316243_` (`EntityAttachment.Fallback`)

**Returns:** `private`

### createFallbackPoints

```java
public List<Vec3> createFallbackPoints(float p_316596_, float p_316153_)
```

**Parameters:**

- `p_316596_` (`float`)
- `p_316153_` (`float`)

**Returns:** `public List<Vec3>`

### create

```java
List<Vec3> create(float p_316360_, float p_316865_)
```

**Parameters:**

- `p_316360_` (`float`)
- `p_316865_` (`float`)

**Returns:** `List<Vec3>`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Fallback` | interface |  |
