# EyeOfEnder

**Package:** `net.minecraft.world.entity.projectile`
**Type:** class
**Extends:** `Entity`
**Implements:** `ItemSupplier`

## Methods

### EyeOfEnder

```java
public EyeOfEnder(EntityType<? extends EyeOfEnder> p_36957_, Level p_36958_)
```

**Parameters:**

- `p_36957_` (`EntityType<? extends EyeOfEnder>`)
- `p_36958_` (`Level`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### EyeOfEnder

```java
public EyeOfEnder(Level p_36960_, double p_36961_, double p_36962_, double p_36963_)
```

**Parameters:**

- `p_36960_` (`Level`)
- `p_36961_` (`double`)
- `p_36962_` (`double`)
- `p_36963_` (`double`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### setItem

```java
public void setItem(ItemStack p_36973_)
```

**Parameters:**

- `p_36973_` (`ItemStack`)

**Returns:** `public void`

### getItem

```java
public ItemStack getItem()
```

**Returns:** `ItemStack`

### defineSynchedData

```java
protected void defineSynchedData(SynchedEntityData.Builder p_326310_)
```

**Parameters:**

- `p_326310_` (`SynchedEntityData.Builder`)

### shouldRenderAtSqrDistance

```java
public boolean shouldRenderAtSqrDistance(double p_36966_)
```

**Parameters:**

- `p_36966_` (`double`)

**Returns:** `boolean`

### signalTo

```java
public void signalTo(BlockPos p_36968_)
```

**Parameters:**

- `p_36968_` (`BlockPos`)

**Returns:** `public void`

### lerpMotion

```java
public void lerpMotion(double p_36984_, double p_36985_, double p_36986_)
```

**Parameters:**

- `p_36984_` (`double`)
- `p_36985_` (`double`)
- `p_36986_` (`double`)

### tick

```java
public void tick()
```

### addAdditionalSaveData

```java
public void addAdditionalSaveData(CompoundTag p_36975_)
```

**Parameters:**

- `p_36975_` (`CompoundTag`)

### readAdditionalSaveData

```java
public void readAdditionalSaveData(CompoundTag p_36970_)
```

**Parameters:**

- `p_36970_` (`CompoundTag`)

### getDefaultItem

```java
private ItemStack getDefaultItem()
```

**Returns:** `private ItemStack`

### ItemStack

```java
return new ItemStack()
```

**Returns:** `return new`

### getLightLevelDependentMagicValue

```java
public float getLightLevelDependentMagicValue()
```

**Returns:** `float`

### isAttackable

```java
public boolean isAttackable()
```

**Returns:** `boolean`
