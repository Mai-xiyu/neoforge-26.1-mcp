# FrogAi

**Package:** `net.minecraft.world.entity.animal.frog`
**Type:** class

## Methods

### initMemories

```java
protected static void initMemories(Frog p_218580_, RandomSource p_218581_)
```

**Parameters:**

- `p_218580_` (`Frog`)
- `p_218581_` (`RandomSource`)

**Returns:** `protected static void`

### makeBrain

```java
protected static Brain<?> makeBrain(Brain<Frog> p_218576_)
```

**Parameters:**

- `p_218576_` (`Brain<Frog>`)

**Returns:** `protected static Brain<?>`

### initCoreActivity

```java
 initCoreActivity()
```

**Returns:** ``

### initIdleActivity

```java
 initIdleActivity()
```

**Returns:** ``

### initSwimActivity

```java
 initSwimActivity()
```

**Returns:** ``

### initLaySpawnActivity

```java
 initLaySpawnActivity()
```

**Returns:** ``

### initTongueActivity

```java
 initTongueActivity()
```

**Returns:** ``

### initJumpActivity

```java
 initJumpActivity()
```

**Returns:** ``

### initCoreActivity

```java
private static void initCoreActivity(Brain<Frog> p_218587_)
```

**Parameters:**

- `p_218587_` (`Brain<Frog>`)

**Returns:** `private static void`

### initIdleActivity

```java
private static void initIdleActivity(Brain<Frog> p_218591_)
```

**Parameters:**

- `p_218591_` (`Brain<Frog>`)

**Returns:** `private static void`

### initSwimActivity

```java
private static void initSwimActivity(Brain<Frog> p_218595_)
```

**Parameters:**

- `p_218595_` (`Brain<Frog>`)

**Returns:** `private static void`

### initLaySpawnActivity

```java
private static void initLaySpawnActivity(Brain<Frog> p_218599_)
```

**Parameters:**

- `p_218599_` (`Brain<Frog>`)

**Returns:** `private static void`

### initJumpActivity

```java
private static void initJumpActivity(Brain<Frog> p_218603_)
```

**Parameters:**

- `p_218603_` (`Brain<Frog>`)

**Returns:** `private static void`

### initTongueActivity

```java
private static void initTongueActivity(Brain<Frog> p_218607_)
```

**Parameters:**

- `p_218607_` (`Brain<Frog>`)

**Returns:** `private static void`

### isAcceptableLandingSpot

```java
<E extends Mob> private static <E extends Mob> boolean isAcceptableLandingSpot(E p_249699_, BlockPos p_250057_)
```

**Parameters:**

- `p_249699_` (`E`)
- `p_250057_` (`BlockPos`)

**Returns:** `private static <E extends Mob> boolean`

### canAttack

```java
private static boolean canAttack(Frog p_218589_)
```

**Parameters:**

- `p_218589_` (`Frog`)

**Returns:** `private static boolean`

### updateActivity

```java
public static void updateActivity(Frog p_218578_)
```

**Parameters:**

- `p_218578_` (`Frog`)

**Returns:** `public static void`

### getTemptations

```java
public static Predicate<ItemStack> getTemptations()
```

**Returns:** `public static Predicate<ItemStack>`
