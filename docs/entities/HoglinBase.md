# HoglinBase

**Package:** `net.minecraft.world.entity.monster.hoglin`
**Type:** interface

## Methods

### getAttackAnimationRemainingTicks

```java
int getAttackAnimationRemainingTicks()
```

**Returns:** `int`

### hurtAndThrowTarget

```java
static boolean hurtAndThrowTarget(LivingEntity p_34643_, LivingEntity p_34644_)
```

**Parameters:**

- `p_34643_` (`LivingEntity`)
- `p_34644_` (`LivingEntity`)

**Returns:** `static boolean`

### throwTarget

```java
 throwTarget()
```

**Returns:** ``

### throwTarget

```java
static void throwTarget(LivingEntity p_34646_, LivingEntity p_34647_)
```

**Parameters:**

- `p_34646_` (`LivingEntity`)
- `p_34647_` (`LivingEntity`)

**Returns:** `static void`
