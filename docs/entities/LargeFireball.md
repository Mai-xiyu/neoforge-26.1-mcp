# LargeFireball

**Package:** `net.minecraft.world.entity.projectile`
**Type:** class
**Extends:** `Fireball`

## Methods

### LargeFireball

```java
public LargeFireball(EntityType<? extends LargeFireball> p_37199_, Level p_37200_)
```

**Parameters:**

- `p_37199_` (`EntityType<? extends LargeFireball>`)
- `p_37200_` (`Level`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### LargeFireball

```java
public LargeFireball(Level p_181151_, LivingEntity p_181152_, Vec3 p_347580_, int p_181156_)
```

**Parameters:**

- `p_181151_` (`Level`)
- `p_181152_` (`LivingEntity`)
- `p_347580_` (`Vec3`)
- `p_181156_` (`int`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### onHit

```java
protected void onHit(HitResult p_37218_)
```

**Parameters:**

- `p_37218_` (`HitResult`)

### onHitEntity

```java
protected void onHitEntity(EntityHitResult p_37216_)
```

**Parameters:**

- `p_37216_` (`EntityHitResult`)

### addAdditionalSaveData

```java
public void addAdditionalSaveData(CompoundTag p_37222_)
```

**Parameters:**

- `p_37222_` (`CompoundTag`)

### readAdditionalSaveData

```java
public void readAdditionalSaveData(CompoundTag p_37220_)
```

**Parameters:**

- `p_37220_` (`CompoundTag`)
