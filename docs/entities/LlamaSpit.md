# LlamaSpit

**Package:** `net.minecraft.world.entity.projectile`
**Type:** class
**Extends:** `Projectile`

## Methods

### LlamaSpit

```java
public LlamaSpit(EntityType<? extends LlamaSpit> p_37224_, Level p_37225_)
```

**Parameters:**

- `p_37224_` (`EntityType<? extends LlamaSpit>`)
- `p_37225_` (`Level`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### LlamaSpit

```java
public LlamaSpit(Level p_37235_, Llama p_37236_)
```

**Parameters:**

- `p_37235_` (`Level`)
- `p_37236_` (`Llama`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### getDefaultGravity

```java
protected double getDefaultGravity()
```

**Returns:** `double`

### tick

```java
public void tick()
```

### onHitEntity

```java
protected void onHitEntity(EntityHitResult p_37241_)
```

**Parameters:**

- `p_37241_` (`EntityHitResult`)

### onHitBlock

```java
protected void onHitBlock(BlockHitResult p_37239_)
```

**Parameters:**

- `p_37239_` (`BlockHitResult`)

### defineSynchedData

```java
protected void defineSynchedData(SynchedEntityData.Builder p_326005_)
```

**Parameters:**

- `p_326005_` (`SynchedEntityData.Builder`)

### recreateFromPacket

```java
public void recreateFromPacket(ClientboundAddEntityPacket p_150162_)
```

**Parameters:**

- `p_150162_` (`ClientboundAddEntityPacket`)
