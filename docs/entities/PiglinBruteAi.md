# PiglinBruteAi

**Package:** `net.minecraft.world.entity.monster.piglin`
**Type:** class

## Methods

### makeBrain

```java
protected static Brain<?> makeBrain(PiglinBrute p_35100_, Brain<PiglinBrute> p_35101_)
```

**Parameters:**

- `p_35100_` (`PiglinBrute`)
- `p_35101_` (`Brain<PiglinBrute>`)

**Returns:** `protected static Brain<?>`

### initCoreActivity

```java
 initCoreActivity()
```

**Returns:** ``

### initIdleActivity

```java
 initIdleActivity()
```

**Returns:** ``

### initFightActivity

```java
 initFightActivity()
```

**Returns:** ``

### initMemories

```java
protected static void initMemories(PiglinBrute p_35095_)
```

**Parameters:**

- `p_35095_` (`PiglinBrute`)

**Returns:** `protected static void`

### initCoreActivity

```java
private static void initCoreActivity(PiglinBrute p_35112_, Brain<PiglinBrute> p_35113_)
```

**Parameters:**

- `p_35112_` (`PiglinBrute`)
- `p_35113_` (`Brain<PiglinBrute>`)

**Returns:** `private static void`

### initIdleActivity

```java
private static void initIdleActivity(PiglinBrute p_35120_, Brain<PiglinBrute> p_35121_)
```

**Parameters:**

- `p_35120_` (`PiglinBrute`)
- `p_35121_` (`Brain<PiglinBrute>`)

**Returns:** `private static void`

### initFightActivity

```java
private static void initFightActivity(PiglinBrute p_35125_, Brain<PiglinBrute> p_35126_)
```

**Parameters:**

- `p_35125_` (`PiglinBrute`)
- `p_35126_` (`Brain<PiglinBrute>`)

**Returns:** `private static void`

### createIdleLookBehaviors

```java
private static RunOne<PiglinBrute> createIdleLookBehaviors()
```

**Returns:** `private static RunOne<PiglinBrute>`

### createIdleMovementBehaviors

```java
private static RunOne<PiglinBrute> createIdleMovementBehaviors()
```

**Returns:** `private static RunOne<PiglinBrute>`

### updateActivity

```java
protected static void updateActivity(PiglinBrute p_35110_)
```

**Parameters:**

- `p_35110_` (`PiglinBrute`)

**Returns:** `protected static void`

### playActivitySound

```java
 playActivitySound()
```

**Returns:** ``

### isNearestValidAttackTarget

```java
private static boolean isNearestValidAttackTarget(AbstractPiglin p_35089_, LivingEntity p_35090_)
```

**Parameters:**

- `p_35089_` (`AbstractPiglin`)
- `p_35090_` (`LivingEntity`)

**Returns:** `private static boolean`

### findNearestValidAttackTarget

```java
private static Optional<? extends LivingEntity> findNearestValidAttackTarget(AbstractPiglin p_35087_)
```

**Parameters:**

- `p_35087_` (`AbstractPiglin`)

**Returns:** `private static Optional<? extends LivingEntity>`

### getTargetIfWithinRange

```java
private static Optional<? extends LivingEntity> getTargetIfWithinRange(AbstractPiglin p_35092_, MemoryModuleType<? extends LivingEntity> p_35093_)
```

**Parameters:**

- `p_35092_` (`AbstractPiglin`)
- `p_35093_` (`MemoryModuleType<? extends LivingEntity>`)

**Returns:** `private static Optional<? extends LivingEntity>`

### wasHurtBy

```java
protected static void wasHurtBy(PiglinBrute p_35097_, LivingEntity p_35098_)
```

**Parameters:**

- `p_35097_` (`PiglinBrute`)
- `p_35098_` (`LivingEntity`)

**Returns:** `protected static void`

### setAngerTarget

```java
protected static void setAngerTarget(PiglinBrute p_149989_, LivingEntity p_149990_)
```

**Parameters:**

- `p_149989_` (`PiglinBrute`)
- `p_149990_` (`LivingEntity`)

**Returns:** `protected static void`

### maybePlayActivitySound

```java
protected static void maybePlayActivitySound(PiglinBrute p_35115_)
```

**Parameters:**

- `p_35115_` (`PiglinBrute`)

**Returns:** `protected static void`

### playActivitySound

```java
 playActivitySound()
```

**Returns:** ``

### playActivitySound

```java
private static void playActivitySound(PiglinBrute p_35123_)
```

**Parameters:**

- `p_35123_` (`PiglinBrute`)

**Returns:** `private static void`
