# RelativeMovement

**Package:** `net.minecraft.world.entity`
**Type:** enum

## Fields

| Name | Type | Description |
|------|------|-------------|
| `ALL` | `Set<RelativeMovement>` |  |
| `ROTATION` | `Set<RelativeMovement>` |  |

## Methods

### X_ROT

```java
, X_ROT()
```

**Returns:** `,`

### RelativeMovement

```java
private RelativeMovement(int p_265098_)
```

**Parameters:**

- `p_265098_` (`int`)

**Returns:** `private`

### getMask

```java
private int getMask()
```

**Returns:** `private int`

### isSet

```java
private boolean isSet(int p_265420_)
```

**Parameters:**

- `p_265420_` (`int`)

**Returns:** `private boolean`

### unpack

```java
public static Set<RelativeMovement> unpack(int p_265683_)
```

**Parameters:**

- `p_265683_` (`int`)

**Returns:** `public static Set<RelativeMovement>`

### pack

```java
public static int pack(Set<RelativeMovement> p_265525_)
```

**Parameters:**

- `p_265525_` (`Set<RelativeMovement>`)

**Returns:** `public static int`
