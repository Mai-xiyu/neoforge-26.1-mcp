# Shoot

**Package:** `net.minecraft.world.entity.monster.breeze`
**Type:** class
**Extends:** `Behavior<Breeze>`

## Methods

### Shoot

```java
public public Shoot()
```

**Returns:** `public`

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_312041_, Breeze p_312169_)
```

**Parameters:**

- `p_312041_` (`ServerLevel`)
- `p_312169_` (`Breeze`)

**Returns:** `protected boolean`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_312535_, Breeze p_312174_, long p_311812_)
```

**Parameters:**

- `p_312535_` (`ServerLevel`)
- `p_312174_` (`Breeze`)
- `p_311812_` (`long`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_311932_, Breeze p_312618_, long p_311781_)
```

**Parameters:**

- `p_311932_` (`ServerLevel`)
- `p_312618_` (`Breeze`)
- `p_311781_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_312137_, Breeze p_311803_, long p_312309_)
```

**Parameters:**

- `p_312137_` (`ServerLevel`)
- `p_311803_` (`Breeze`)
- `p_312309_` (`long`)

**Returns:** `protected void`

### tick

```java
protected void tick(ServerLevel p_312907_, Breeze p_312605_, long p_312804_)
```

**Parameters:**

- `p_312907_` (`ServerLevel`)
- `p_312605_` (`Breeze`)
- `p_312804_` (`long`)

**Returns:** `protected void`

### isFacingTarget

```java
public static boolean isFacingTarget(Breeze p_311845_, LivingEntity p_312453_)
```

**Parameters:**

- `p_311845_` (`Breeze`)
- `p_312453_` (`LivingEntity`)

**Returns:** `boolean`

### isTargetWithinRange

```java
private static boolean isTargetWithinRange(Breeze p_312114_, LivingEntity p_312647_)
```

**Parameters:**

- `p_312114_` (`Breeze`)
- `p_312647_` (`LivingEntity`)

**Returns:** `private static boolean`
