# ShootWhenStuck

**Package:** `net.minecraft.world.entity.monster.breeze`
**Type:** class
**Extends:** `Behavior<Breeze>`

## Methods

### ShootWhenStuck

```java
public ShootWhenStuck()
```

**Returns:** `public`

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_314464_, Breeze p_314546_)
```

**Parameters:**

- `p_314464_` (`ServerLevel`)
- `p_314546_` (`Breeze`)

**Returns:** `protected boolean`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_314552_, Breeze p_314459_, long p_314460_)
```

**Parameters:**

- `p_314552_` (`ServerLevel`)
- `p_314459_` (`Breeze`)
- `p_314460_` (`long`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_314434_, Breeze p_314572_, long p_314431_)
```

**Parameters:**

- `p_314434_` (`ServerLevel`)
- `p_314572_` (`Breeze`)
- `p_314431_` (`long`)

**Returns:** `protected void`
