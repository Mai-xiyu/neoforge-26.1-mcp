# SkeletonTrapGoal

**Package:** `net.minecraft.world.entity.animal.horse`
**Type:** class
**Extends:** `Goal`

## Methods

### SkeletonTrapGoal

```java
public SkeletonTrapGoal(SkeletonHorse p_30927_)
```

**Parameters:**

- `p_30927_` (`SkeletonHorse`)

**Returns:** `public`

### canUse

```java
public boolean canUse()
```

**Returns:** `boolean`

### tick

```java
public void tick()
```

### enchant

```java
private void enchant(Skeleton p_345688_, EquipmentSlot p_346060_, DifficultyInstance p_348658_)
```

**Parameters:**

- `p_345688_` (`Skeleton`)
- `p_346060_` (`EquipmentSlot`)
- `p_348658_` (`DifficultyInstance`)

**Returns:** `private void`
