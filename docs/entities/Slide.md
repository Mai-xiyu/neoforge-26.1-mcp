# Slide

**Package:** `net.minecraft.world.entity.monster.breeze`
**Type:** class
**Extends:** `Behavior<Breeze>`

## Methods

### Slide

```java
public Slide()
```

**Returns:** `public`

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_311853_, Breeze p_311894_)
```

**Parameters:**

- `p_311853_` (`ServerLevel`)
- `p_311894_` (`Breeze`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_312325_, Breeze p_312534_, long p_311789_)
```

**Parameters:**

- `p_312325_` (`ServerLevel`)
- `p_312534_` (`Breeze`)
- `p_311789_` (`long`)

**Returns:** `protected void`

### randomPointInMiddleCircle

```java
private static Vec3 randomPointInMiddleCircle(Breeze p_311931_, LivingEntity p_312413_)
```

**Parameters:**

- `p_311931_` (`Breeze`)
- `p_312413_` (`LivingEntity`)

**Returns:** `private static Vec3`
