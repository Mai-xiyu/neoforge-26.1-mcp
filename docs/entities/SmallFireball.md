# SmallFireball

**Package:** `net.minecraft.world.entity.projectile`
**Type:** class
**Extends:** `Fireball`

## Methods

### SmallFireball

```java
public SmallFireball(EntityType<? extends SmallFireball> p_37364_, Level p_37365_)
```

**Parameters:**

- `p_37364_` (`EntityType<? extends SmallFireball>`)
- `p_37365_` (`Level`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### SmallFireball

```java
public SmallFireball(Level p_37375_, LivingEntity p_37376_, Vec3 p_347501_)
```

**Parameters:**

- `p_37375_` (`Level`)
- `p_37376_` (`LivingEntity`)
- `p_347501_` (`Vec3`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### SmallFireball

```java
public SmallFireball(Level p_37367_, double p_37368_, double p_37369_, double p_37370_, Vec3 p_347543_)
```

**Parameters:**

- `p_37367_` (`Level`)
- `p_37368_` (`double`)
- `p_37369_` (`double`)
- `p_37370_` (`double`)
- `p_347543_` (`Vec3`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### onHitEntity

```java
protected void onHitEntity(EntityHitResult p_37386_)
```

**Parameters:**

- `p_37386_` (`EntityHitResult`)

### onHitBlock

```java
protected void onHitBlock(BlockHitResult p_37384_)
```

**Parameters:**

- `p_37384_` (`BlockHitResult`)

### onHit

```java
protected void onHit(HitResult p_37388_)
```

**Parameters:**

- `p_37388_` (`HitResult`)

### hurt

```java
public boolean hurt(DamageSource p_37381_, float p_37382_)
```

**Parameters:**

- `p_37381_` (`DamageSource`)
- `p_37382_` (`float`)

**Returns:** `boolean`
