# SnifferAi

**Package:** `net.minecraft.world.entity.animal.sniffer`
**Type:** class

## Methods

### getTemptations

```java
public static Predicate<ItemStack> getTemptations()
```

**Returns:** `public static Predicate<ItemStack>`

### makeBrain

```java
protected static Brain<?> makeBrain(Brain<Sniffer> p_273175_)
```

**Parameters:**

- `p_273175_` (`Brain<Sniffer>`)

**Returns:** `protected static Brain<?>`

### initCoreActivity

```java
 initCoreActivity()
```

**Returns:** ``

### initIdleActivity

```java
 initIdleActivity()
```

**Returns:** ``

### initSniffingActivity

```java
 initSniffingActivity()
```

**Returns:** ``

### initDigActivity

```java
 initDigActivity()
```

**Returns:** ``

### resetSniffing

```java
static Sniffer resetSniffing(Sniffer p_279301_)
```

**Parameters:**

- `p_279301_` (`Sniffer`)

**Returns:** `static Sniffer`

### initCoreActivity

```java
private static void initCoreActivity(Brain<Sniffer> p_273185_)
```

**Parameters:**

- `p_273185_` (`Brain<Sniffer>`)

**Returns:** `private static void`

### start

```java
protected void start(ServerLevel p_326360_, Sniffer p_326298_, long p_326169_)
```

**Parameters:**

- `p_326360_` (`ServerLevel`)
- `p_326298_` (`Sniffer`)
- `p_326169_` (`long`)

**Returns:** `protected void`

### initSniffingActivity

```java
private static void initSniffingActivity(Brain<Sniffer> p_273183_)
```

**Parameters:**

- `p_273183_` (`Brain<Sniffer>`)

**Returns:** `private static void`

### initDigActivity

```java
private static void initDigActivity(Brain<Sniffer> p_273677_)
```

**Parameters:**

- `p_273677_` (`Brain<Sniffer>`)

**Returns:** `private static void`

### initIdleActivity

```java
private static void initIdleActivity(Brain<Sniffer> p_273750_)
```

**Parameters:**

- `p_273750_` (`Brain<Sniffer>`)

**Returns:** `private static void`

### AnimalMakeLove

```java
0, new AnimalMakeLove()
```

**Returns:** `0, new`

### start

```java
protected void start(ServerLevel p_279149_, Animal p_279090_, long p_279482_)
```

**Parameters:**

- `p_279149_` (`ServerLevel`)
- `p_279090_` (`Animal`)
- `p_279482_` (`long`)

### start

```java
protected void start(ServerLevel p_279230_, PathfinderMob p_279386_, long p_279139_)
```

**Parameters:**

- `p_279230_` (`ServerLevel`)
- `p_279386_` (`PathfinderMob`)
- `p_279139_` (`long`)

### updateActivity

```java
static void updateActivity(Sniffer p_273301_)
```

**Parameters:**

- `p_273301_` (`Sniffer`)

**Returns:** `static void`

### Digging

```java
 Digging(int p_272666_, int p_273420_)
```

**Parameters:**

- `p_272666_` (`int`)
- `p_273420_` (`int`)

**Returns:** ``

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_273442_, Sniffer p_273370_)
```

**Parameters:**

- `p_273442_` (`ServerLevel`)
- `p_273370_` (`Sniffer`)

**Returns:** `protected boolean`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_272686_, Sniffer p_273617_, long p_273124_)
```

**Parameters:**

- `p_272686_` (`ServerLevel`)
- `p_273617_` (`Sniffer`)
- `p_273124_` (`long`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_272951_, Sniffer p_272688_, long p_272979_)
```

**Parameters:**

- `p_272951_` (`ServerLevel`)
- `p_272688_` (`Sniffer`)
- `p_272979_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_273656_, Sniffer p_273063_, long p_272844_)
```

**Parameters:**

- `p_273656_` (`ServerLevel`)
- `p_273063_` (`Sniffer`)
- `p_272844_` (`long`)

**Returns:** `protected void`

### FeelingHappy

```java
 FeelingHappy(int p_273286_, int p_272777_)
```

**Parameters:**

- `p_273286_` (`int`)
- `p_272777_` (`int`)

**Returns:** ``

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_272660_, Sniffer p_273250_, long p_273180_)
```

**Parameters:**

- `p_272660_` (`ServerLevel`)
- `p_273250_` (`Sniffer`)
- `p_273180_` (`long`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_273624_, Sniffer p_273470_, long p_273501_)
```

**Parameters:**

- `p_273624_` (`ServerLevel`)
- `p_273470_` (`Sniffer`)
- `p_273501_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_273216_, Sniffer p_273271_, long p_273738_)
```

**Parameters:**

- `p_273216_` (`ServerLevel`)
- `p_273271_` (`Sniffer`)
- `p_273738_` (`long`)

**Returns:** `protected void`

### FinishedDigging

```java
 FinishedDigging(int p_272941_)
```

**Parameters:**

- `p_272941_` (`int`)

**Returns:** ``

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_273692_, Sniffer p_272856_)
```

**Parameters:**

- `p_273692_` (`ServerLevel`)
- `p_272856_` (`Sniffer`)

**Returns:** `protected boolean`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_273775_, Sniffer p_273131_, long p_273569_)
```

**Parameters:**

- `p_273775_` (`ServerLevel`)
- `p_273131_` (`Sniffer`)
- `p_273569_` (`long`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_272708_, Sniffer p_273502_, long p_272739_)
```

**Parameters:**

- `p_272708_` (`ServerLevel`)
- `p_273502_` (`Sniffer`)
- `p_272739_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_273210_, Sniffer p_273648_, long p_272804_)
```

**Parameters:**

- `p_273210_` (`ServerLevel`)
- `p_273648_` (`Sniffer`)
- `p_272804_` (`long`)

**Returns:** `protected void`

### Scenting

```java
 Scenting(int p_272680_, int p_273445_)
```

**Parameters:**

- `p_272680_` (`int`)
- `p_273445_` (`int`)

**Returns:** ``

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_279176_, Sniffer p_279496_)
```

**Parameters:**

- `p_279176_` (`ServerLevel`)
- `p_279496_` (`Sniffer`)

**Returns:** `protected boolean`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_273482_, Sniffer p_273724_, long p_273191_)
```

**Parameters:**

- `p_273482_` (`ServerLevel`)
- `p_273724_` (`Sniffer`)
- `p_273191_` (`long`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_272795_, Sniffer p_272788_, long p_273611_)
```

**Parameters:**

- `p_272795_` (`ServerLevel`)
- `p_272788_` (`Sniffer`)
- `p_273611_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_272816_, Sniffer p_273426_, long p_272832_)
```

**Parameters:**

- `p_272816_` (`ServerLevel`)
- `p_273426_` (`Sniffer`)
- `p_272832_` (`long`)

**Returns:** `protected void`

### Searching

```java
 Searching()
```

**Returns:** ``

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_273493_, Sniffer p_272857_)
```

**Parameters:**

- `p_273493_` (`ServerLevel`)
- `p_272857_` (`Sniffer`)

**Returns:** `protected boolean`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_273196_, Sniffer p_273769_, long p_273602_)
```

**Parameters:**

- `p_273196_` (`ServerLevel`)
- `p_273769_` (`Sniffer`)
- `p_273602_` (`long`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_273563_, Sniffer p_273394_, long p_273358_)
```

**Parameters:**

- `p_273563_` (`ServerLevel`)
- `p_273394_` (`Sniffer`)
- `p_273358_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_273705_, Sniffer p_273135_, long p_272667_)
```

**Parameters:**

- `p_273705_` (`ServerLevel`)
- `p_273135_` (`Sniffer`)
- `p_272667_` (`long`)

**Returns:** `protected void`

### Sniffing

```java
 Sniffing(int p_272703_, int p_272735_)
```

**Parameters:**

- `p_272703_` (`int`)
- `p_272735_` (`int`)

**Returns:** ``

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_272972_, Sniffer p_273676_)
```

**Parameters:**

- `p_272972_` (`ServerLevel`)
- `p_273676_` (`Sniffer`)

**Returns:** `protected boolean`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_273156_, Sniffer p_273448_, long p_272841_)
```

**Parameters:**

- `p_273156_` (`ServerLevel`)
- `p_273448_` (`Sniffer`)
- `p_272841_` (`long`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_272950_, Sniffer p_272614_, long p_273573_)
```

**Parameters:**

- `p_272950_` (`ServerLevel`)
- `p_272614_` (`Sniffer`)
- `p_273573_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_272617_, Sniffer p_273181_, long p_272635_)
```

**Parameters:**

- `p_272617_` (`ServerLevel`)
- `p_273181_` (`Sniffer`)
- `p_272635_` (`long`)

**Returns:** `protected void`
