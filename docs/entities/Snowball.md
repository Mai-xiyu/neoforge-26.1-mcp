# Snowball

**Package:** `net.minecraft.world.entity.projectile`
**Type:** class
**Extends:** `ThrowableItemProjectile`

## Methods

### Snowball

```java
public Snowball(EntityType<? extends Snowball> p_37391_, Level p_37392_)
```

**Parameters:**

- `p_37391_` (`EntityType<? extends Snowball>`)
- `p_37392_` (`Level`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### Snowball

```java
public Snowball(Level p_37399_, LivingEntity p_37400_)
```

**Parameters:**

- `p_37399_` (`Level`)
- `p_37400_` (`LivingEntity`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### Snowball

```java
public Snowball(Level p_37394_, double p_37395_, double p_37396_, double p_37397_)
```

**Parameters:**

- `p_37394_` (`Level`)
- `p_37395_` (`double`)
- `p_37396_` (`double`)
- `p_37397_` (`double`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getDefaultItem

```java
protected Item getDefaultItem()
```

**Returns:** `Item`

### getParticle

```java
private ParticleOptions getParticle()
```

**Returns:** `private ParticleOptions`

### handleEntityEvent

```java
public void handleEntityEvent(byte p_37402_)
```

**Parameters:**

- `p_37402_` (`byte`)

### onHitEntity

```java
protected void onHitEntity(EntityHitResult p_37404_)
```

**Parameters:**

- `p_37404_` (`EntityHitResult`)

### onHit

```java
protected void onHit(HitResult p_37406_)
```

**Parameters:**

- `p_37406_` (`HitResult`)
