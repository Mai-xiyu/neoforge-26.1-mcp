# SpawnPlacementTypes

**Package:** `net.minecraft.world.entity`
**Type:** interface

## Methods

### isSpawnPositionOk

```java
public boolean isSpawnPositionOk(LevelReader p_321666_, BlockPos p_321783_, EntityType<?> p_321839_)
```

**Parameters:**

- `p_321666_` (`LevelReader`)
- `p_321783_` (`BlockPos`)
- `p_321839_` (`EntityType<?>`)

**Returns:** `boolean`

### isValidEmptySpawnBlock

```java
private boolean isValidEmptySpawnBlock(LevelReader p_321512_, BlockPos p_321822_, EntityType<?> p_321785_)
```

**Parameters:**

- `p_321512_` (`LevelReader`)
- `p_321822_` (`BlockPos`)
- `p_321785_` (`EntityType<?>`)

**Returns:** `private boolean`

### adjustSpawnPosition

```java
public BlockPos adjustSpawnPosition(LevelReader p_321527_, BlockPos p_321602_)
```

**Parameters:**

- `p_321527_` (`LevelReader`)
- `p_321602_` (`BlockPos`)

**Returns:** `BlockPos`
