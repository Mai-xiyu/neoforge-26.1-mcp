# SpectralArrow

**Package:** `net.minecraft.world.entity.projectile`
**Type:** class
**Extends:** `AbstractArrow`

## Methods

### SpectralArrow

```java
public SpectralArrow(EntityType<? extends SpectralArrow> p_37411_, Level p_37412_)
```

**Parameters:**

- `p_37411_` (`EntityType<? extends SpectralArrow>`)
- `p_37412_` (`Level`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### SpectralArrow

```java
public SpectralArrow(Level p_37414_, LivingEntity p_309162_, ItemStack p_309167_, ItemStack p_346408_)
```

**Parameters:**

- `p_37414_` (`Level`)
- `p_309162_` (`LivingEntity`)
- `p_309167_` (`ItemStack`)
- `p_346408_` (`ItemStack`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### SpectralArrow

```java
public SpectralArrow(Level p_37419_, double p_309044_, double p_309099_, double p_308873_, ItemStack p_308959_, ItemStack p_345907_)
```

**Parameters:**

- `p_37419_` (`Level`)
- `p_309044_` (`double`)
- `p_309099_` (`double`)
- `p_308873_` (`double`)
- `p_308959_` (`ItemStack`)
- `p_345907_` (`ItemStack`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### tick

```java
public void tick()
```

### doPostHurtEffects

```java
protected void doPostHurtEffects(LivingEntity p_37422_)
```

**Parameters:**

- `p_37422_` (`LivingEntity`)

### readAdditionalSaveData

```java
public void readAdditionalSaveData(CompoundTag p_37424_)
```

**Parameters:**

- `p_37424_` (`CompoundTag`)

### addAdditionalSaveData

```java
public void addAdditionalSaveData(CompoundTag p_37426_)
```

**Parameters:**

- `p_37426_` (`CompoundTag`)

### getDefaultPickupItem

```java
protected ItemStack getDefaultPickupItem()
```

**Returns:** `ItemStack`

### ItemStack

```java
return new ItemStack()
```

**Returns:** `return new`
