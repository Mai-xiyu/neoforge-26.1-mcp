# TadpoleAi

**Package:** `net.minecraft.world.entity.animal.frog`
**Type:** class

## Methods

### makeBrain

```java
protected static Brain<?> makeBrain(Brain<Tadpole> p_218742_)
```

**Parameters:**

- `p_218742_` (`Brain<Tadpole>`)

**Returns:** `protected static Brain<?>`

### initCoreActivity

```java
 initCoreActivity()
```

**Returns:** ``

### initIdleActivity

```java
 initIdleActivity()
```

**Returns:** ``

### initCoreActivity

```java
private static void initCoreActivity(Brain<Tadpole> p_218746_)
```

**Parameters:**

- `p_218746_` (`Brain<Tadpole>`)

**Returns:** `private static void`

### initIdleActivity

```java
private static void initIdleActivity(Brain<Tadpole> p_218748_)
```

**Parameters:**

- `p_218748_` (`Brain<Tadpole>`)

**Returns:** `private static void`

### updateActivity

```java
public static void updateActivity(Tadpole p_218744_)
```

**Parameters:**

- `p_218744_` (`Tadpole`)

**Returns:** `public static void`
