# ThrownEgg

**Package:** `net.minecraft.world.entity.projectile`
**Type:** class
**Extends:** `ThrowableItemProjectile`

## Methods

### ThrownEgg

```java
public ThrownEgg(EntityType<? extends ThrownEgg> p_37473_, Level p_37474_)
```

**Parameters:**

- `p_37473_` (`EntityType<? extends ThrownEgg>`)
- `p_37474_` (`Level`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### ThrownEgg

```java
public ThrownEgg(Level p_37481_, LivingEntity p_37482_)
```

**Parameters:**

- `p_37481_` (`Level`)
- `p_37482_` (`LivingEntity`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### ThrownEgg

```java
public ThrownEgg(Level p_37476_, double p_37477_, double p_37478_, double p_37479_)
```

**Parameters:**

- `p_37476_` (`Level`)
- `p_37477_` (`double`)
- `p_37478_` (`double`)
- `p_37479_` (`double`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### handleEntityEvent

```java
public void handleEntityEvent(byte p_37484_)
```

**Parameters:**

- `p_37484_` (`byte`)

### onHitEntity

```java
protected void onHitEntity(EntityHitResult p_37486_)
```

**Parameters:**

- `p_37486_` (`EntityHitResult`)

### onHit

```java
protected void onHit(HitResult p_37488_)
```

**Parameters:**

- `p_37488_` (`HitResult`)

### getDefaultItem

```java
protected Item getDefaultItem()
```

**Returns:** `Item`
