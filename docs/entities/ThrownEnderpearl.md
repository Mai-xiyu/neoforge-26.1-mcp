# ThrownEnderpearl

**Package:** `net.minecraft.world.entity.projectile`
**Type:** class
**Extends:** `ThrowableItemProjectile`

## Methods

### ThrownEnderpearl

```java
public ThrownEnderpearl(EntityType<? extends ThrownEnderpearl> p_37491_, Level p_37492_)
```

**Parameters:**

- `p_37491_` (`EntityType<? extends ThrownEnderpearl>`)
- `p_37492_` (`Level`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### ThrownEnderpearl

```java
public ThrownEnderpearl(Level p_37499_, LivingEntity p_37500_)
```

**Parameters:**

- `p_37499_` (`Level`)
- `p_37500_` (`LivingEntity`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getDefaultItem

```java
protected Item getDefaultItem()
```

**Returns:** `Item`

### onHitEntity

```java
protected void onHitEntity(EntityHitResult p_37502_)
```

**Parameters:**

- `p_37502_` (`EntityHitResult`)

### onHit

```java
protected void onHit(HitResult p_37504_)
```

**Parameters:**

- `p_37504_` (`HitResult`)

### isAllowedToTeleportOwner

```java
private static boolean isAllowedToTeleportOwner(Entity p_352055_, Level p_352453_)
```

**Parameters:**

- `p_352055_` (`Entity`)
- `p_352453_` (`Level`)

**Returns:** `private static boolean`

### tick

```java
public void tick()
```

### playSound

```java
private void playSound(Level p_350706_, Vec3 p_350543_)
```

**Parameters:**

- `p_350706_` (`Level`)
- `p_350543_` (`Vec3`)

**Returns:** `private void`

### canChangeDimensions

```java
public boolean canChangeDimensions(Level p_352941_, Level p_352929_)
```

**Parameters:**

- `p_352941_` (`Level`)
- `p_352929_` (`Level`)

**Returns:** `boolean`

### onInsideBlock

```java
protected void onInsideBlock(BlockState p_353066_)
```

**Parameters:**

- `p_353066_` (`BlockState`)
