# VillagerTrades

**Package:** `net.minecraft.world.entity.npc`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `TRADES` | `Map<VillagerProfession, Int2ObjectMap<VillagerTrades.ItemListing[]>>` |  |

## Methods

### commonBooks

```java
private static VillagerTrades.ItemListing commonBooks(int p_294584_)
```

**Parameters:**

- `p_294584_` (`int`)

**Returns:** `private static VillagerTrades.ItemListing`

### specialBooks

```java
private static VillagerTrades.ItemListing specialBooks()
```

**Returns:** `private static VillagerTrades.ItemListing`

### toIntMap

```java
private static Int2ObjectMap<VillagerTrades.ItemListing[]> toIntMap(ImmutableMap<Integer, VillagerTrades.ItemListing[]> p_35631_)
```

**Parameters:**

- `p_35631_` (`ImmutableMap<Integer, VillagerTrades.ItemListing[]>`)

**Returns:** `private static Int2ObjectMap<VillagerTrades.ItemListing[]>`

### potionCost

```java
private static ItemCost potionCost(Holder<Potion> p_331255_)
```

**Parameters:**

- `p_331255_` (`Holder<Potion>`)

**Returns:** `private static ItemCost`

### potion

```java
private static ItemStack potion(Holder<Potion> p_316745_)
```

**Parameters:**

- `p_316745_` (`Holder<Potion>`)

**Returns:** `private static ItemStack`

### DyedArmorForEmeralds

```java
public DyedArmorForEmeralds(Item p_35639_, int p_35640_)
```

**Parameters:**

- `p_35639_` (`Item`)
- `p_35640_` (`int`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### DyedArmorForEmeralds

```java
public DyedArmorForEmeralds(Item p_35642_, int p_35643_, int p_35644_, int p_35645_)
```

**Parameters:**

- `p_35642_` (`Item`)
- `p_35643_` (`int`)
- `p_35644_` (`int`)
- `p_35645_` (`int`)

**Returns:** `public`

### getOffer

```java
public MerchantOffer getOffer(Entity p_219679_, RandomSource p_219680_)
```

**Parameters:**

- `p_219679_` (`Entity`)
- `p_219680_` (`RandomSource`)

**Returns:** `MerchantOffer`

### MerchantOffer

```java
return new MerchantOffer()
```

**Returns:** `return new`

### getRandomDye

```java
private static DyeItem getRandomDye(RandomSource p_219677_)
```

**Parameters:**

- `p_219677_` (`RandomSource`)

**Returns:** `private static DyeItem`

### EmeraldForItems

```java
public EmeraldForItems(ItemLike p_35657_, int p_35658_, int p_35659_, int p_35660_)
```

**Parameters:**

- `p_35657_` (`ItemLike`)
- `p_35658_` (`int`)
- `p_35659_` (`int`)
- `p_35660_` (`int`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### EmeraldForItems

```java
public EmeraldForItems(ItemLike p_295166_, int p_294790_, int p_294810_, int p_295575_, int p_294582_)
```

**Parameters:**

- `p_295166_` (`ItemLike`)
- `p_294790_` (`int`)
- `p_294810_` (`int`)
- `p_295575_` (`int`)
- `p_294582_` (`int`)

**Returns:** `public`

### EmeraldForItems

```java
public EmeraldForItems(ItemCost p_331989_, int p_294800_, int p_295111_, int p_294137_)
```

**Parameters:**

- `p_331989_` (`ItemCost`)
- `p_294800_` (`int`)
- `p_295111_` (`int`)
- `p_294137_` (`int`)

**Returns:** `public`

### getOffer

```java
public MerchantOffer getOffer(Entity p_219682_, RandomSource p_219683_)
```

**Parameters:**

- `p_219682_` (`Entity`)
- `p_219683_` (`RandomSource`)

**Returns:** `MerchantOffer`

### EmeraldsForVillagerTypeItem

```java
public EmeraldsForVillagerTypeItem(int p_35669_, int p_35670_, int p_35671_, Map<VillagerType, Item> p_35672_)
```

**Parameters:**

- `p_35669_` (`int`)
- `p_35670_` (`int`)
- `p_35671_` (`int`)
- `p_35672_` (`Map<VillagerType, Item>`)

**Returns:** `public`

### getOffer

```java
public MerchantOffer getOffer(Entity p_219685_, RandomSource p_219686_)
```

**Parameters:**

- `p_219685_` (`Entity`)
- `p_219686_` (`RandomSource`)

**Returns:** `MerchantOffer`

### EnchantBookForEmeralds

```java
public EnchantBookForEmeralds(int p_296300_, TagKey<Enchantment> p_345350_)
```

**Parameters:**

- `p_296300_` (`int`)
- `p_345350_` (`TagKey<Enchantment>`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### EnchantBookForEmeralds

```java
public EnchantBookForEmeralds(int p_296323_, int p_345122_, int p_346010_, TagKey<Enchantment> p_346031_)
```

**Parameters:**

- `p_296323_` (`int`)
- `p_345122_` (`int`)
- `p_346010_` (`int`)
- `p_346031_` (`TagKey<Enchantment>`)

**Returns:** `public`

### getOffer

```java
public MerchantOffer getOffer(Entity p_219688_, RandomSource p_219689_)
```

**Parameters:**

- `p_219688_` (`Entity`)
- `p_219689_` (`RandomSource`)

**Returns:** `MerchantOffer`

### EnchantedItemForEmeralds

```java
public EnchantedItemForEmeralds(Item p_35693_, int p_35694_, int p_35695_, int p_35696_)
```

**Parameters:**

- `p_35693_` (`Item`)
- `p_35694_` (`int`)
- `p_35695_` (`int`)
- `p_35696_` (`int`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### EnchantedItemForEmeralds

```java
public EnchantedItemForEmeralds(Item p_35698_, int p_35699_, int p_35700_, int p_35701_, float p_35702_)
```

**Parameters:**

- `p_35698_` (`Item`)
- `p_35699_` (`int`)
- `p_35700_` (`int`)
- `p_35701_` (`int`)
- `p_35702_` (`float`)

**Returns:** `public`

### getOffer

```java
public MerchantOffer getOffer(Entity p_219691_, RandomSource p_219692_)
```

**Parameters:**

- `p_219691_` (`Entity`)
- `p_219692_` (`RandomSource`)

**Returns:** `MerchantOffer`

### MerchantOffer

```java
return new MerchantOffer()
```

**Returns:** `return new`

### getOffer

```java
public MerchantOffer getOffer(Entity p_302036_, RandomSource p_301986_)
```

**Parameters:**

- `p_302036_` (`Entity`)
- `p_301986_` (`RandomSource`)

**Returns:** `MerchantOffer`

### getOffer

```java
MerchantOffer getOffer(Entity p_219693_, RandomSource p_219694_)
```

**Parameters:**

- `p_219693_` (`Entity`)
- `p_219694_` (`RandomSource`)

**Returns:** `MerchantOffer`

### ItemsAndEmeraldsToItems

```java
public ItemsAndEmeraldsToItems(ItemLike p_35717_, int p_35718_, int p_35719_, Item p_35720_, int p_35721_, int p_35722_, int p_35723_, float p_301975_)
```

**Parameters:**

- `p_35717_` (`ItemLike`)
- `p_35718_` (`int`)
- `p_35719_` (`int`)
- `p_35720_` (`Item`)
- `p_35721_` (`int`)
- `p_35722_` (`int`)
- `p_35723_` (`int`)
- `p_301975_` (`float`)

**Returns:** `public`

### ItemsAndEmeraldsToItems

```java
private ItemsAndEmeraldsToItems(ItemLike p_35725_, int p_35726_, int p_35728_, ItemStack p_302012_, int p_35729_, int p_35730_, int p_302002_, float p_302039_)
```

**Parameters:**

- `p_35725_` (`ItemLike`)
- `p_35726_` (`int`)
- `p_35728_` (`int`)
- `p_302012_` (`ItemStack`)
- `p_35729_` (`int`)
- `p_35730_` (`int`)
- `p_302002_` (`int`)
- `p_302039_` (`float`)

**Returns:** `private`

### ItemsAndEmeraldsToItems

```java
 ItemsAndEmeraldsToItems(ItemLike p_345985_, int p_320882_, int p_320089_, ItemLike p_345218_, int p_320458_, int p_345627_, int p_345902_, float p_319962_, ResourceKey<EnchantmentProvider> p_345388_)
```

**Parameters:**

- `p_345985_` (`ItemLike`)
- `p_320882_` (`int`)
- `p_320089_` (`int`)
- `p_345218_` (`ItemLike`)
- `p_320458_` (`int`)
- `p_345627_` (`int`)
- `p_345902_` (`int`)
- `p_319962_` (`float`)
- `p_345388_` (`ResourceKey<EnchantmentProvider>`)

**Returns:** ``

### ItemsAndEmeraldsToItems

```java
public ItemsAndEmeraldsToItems(ItemCost p_345481_, int p_345494_, ItemStack p_345223_, int p_345894_, int p_344894_, float p_345457_, Optional<ResourceKey<EnchantmentProvider>> p_346401_)
```

**Parameters:**

- `p_345481_` (`ItemCost`)
- `p_345494_` (`int`)
- `p_345223_` (`ItemStack`)
- `p_345894_` (`int`)
- `p_344894_` (`int`)
- `p_345457_` (`float`)
- `p_346401_` (`Optional<ResourceKey<EnchantmentProvider>>`)

**Returns:** `public`

### getOffer

```java
public MerchantOffer getOffer(Entity p_219696_, RandomSource p_219697_)
```

**Parameters:**

- `p_219696_` (`Entity`)
- `p_219697_` (`RandomSource`)

**Returns:** `MerchantOffer`

### ItemsForEmeralds

```java
public ItemsForEmeralds(Block p_35765_, int p_35766_, int p_35767_, int p_35768_, int p_35769_)
```

**Parameters:**

- `p_35765_` (`Block`)
- `p_35766_` (`int`)
- `p_35767_` (`int`)
- `p_35768_` (`int`)
- `p_35769_` (`int`)

**Returns:** `public`

### ItemsForEmeralds

```java
public ItemsForEmeralds(Item p_35741_, int p_35742_, int p_35743_, int p_35744_)
```

**Parameters:**

- `p_35741_` (`Item`)
- `p_35742_` (`int`)
- `p_35743_` (`int`)
- `p_35744_` (`int`)

**Returns:** `public`

### ItemsForEmeralds

```java
public ItemsForEmeralds(Item p_35746_, int p_35747_, int p_35748_, int p_35749_, int p_35750_)
```

**Parameters:**

- `p_35746_` (`Item`)
- `p_35747_` (`int`)
- `p_35748_` (`int`)
- `p_35749_` (`int`)
- `p_35750_` (`int`)

**Returns:** `public`

### ItemsForEmeralds

```java
public ItemsForEmeralds(ItemStack p_35752_, int p_35753_, int p_35754_, int p_35755_, int p_35756_)
```

**Parameters:**

- `p_35752_` (`ItemStack`)
- `p_35753_` (`int`)
- `p_35754_` (`int`)
- `p_35755_` (`int`)
- `p_35756_` (`int`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### ItemsForEmeralds

```java
public ItemsForEmeralds(Item p_302016_, int p_302006_, int p_302008_, int p_302010_, int p_301989_, float p_302037_)
```

**Parameters:**

- `p_302016_` (`Item`)
- `p_302006_` (`int`)
- `p_302008_` (`int`)
- `p_302010_` (`int`)
- `p_301989_` (`int`)
- `p_302037_` (`float`)

**Returns:** `public`

### ItemsForEmeralds

```java
public ItemsForEmeralds(Item p_345085_, int p_346208_, int p_345650_, int p_345298_, int p_345652_, float p_346098_, ResourceKey<EnchantmentProvider> p_345759_)
```

**Parameters:**

- `p_345085_` (`Item`)
- `p_346208_` (`int`)
- `p_345650_` (`int`)
- `p_345298_` (`int`)
- `p_345652_` (`int`)
- `p_346098_` (`float`)
- `p_345759_` (`ResourceKey<EnchantmentProvider>`)

**Returns:** `public`

### ItemsForEmeralds

```java
public ItemsForEmeralds(ItemStack p_35758_, int p_35759_, int p_35760_, int p_35761_, int p_35762_, float p_35763_)
```

**Parameters:**

- `p_35758_` (`ItemStack`)
- `p_35759_` (`int`)
- `p_35760_` (`int`)
- `p_35761_` (`int`)
- `p_35762_` (`int`)
- `p_35763_` (`float`)

**Returns:** `public`

### ItemsForEmeralds

```java
public ItemsForEmeralds(ItemStack p_344989_, int p_345411_, int p_345770_, int p_344818_, int p_345507_, float p_344802_, Optional<ResourceKey<EnchantmentProvider>> p_345806_)
```

**Parameters:**

- `p_344989_` (`ItemStack`)
- `p_345411_` (`int`)
- `p_345770_` (`int`)
- `p_344818_` (`int`)
- `p_345507_` (`int`)
- `p_344802_` (`float`)
- `p_345806_` (`Optional<ResourceKey<EnchantmentProvider>>`)

**Returns:** `public`

### getOffer

```java
public MerchantOffer getOffer(Entity p_219699_, RandomSource p_219700_)
```

**Parameters:**

- `p_219699_` (`Entity`)
- `p_219700_` (`RandomSource`)

**Returns:** `MerchantOffer`

### SuspiciousStewForEmerald

```java
public SuspiciousStewForEmerald(Holder<MobEffect> p_316230_, int p_186314_, int p_186315_)
```

**Parameters:**

- `p_316230_` (`Holder<MobEffect>`)
- `p_186314_` (`int`)
- `p_186315_` (`int`)

**Returns:** `public`

### SuspiciousStewForEmerald

```java
public SuspiciousStewForEmerald(SuspiciousStewEffects p_330918_, int p_299043_, float p_298449_)
```

**Parameters:**

- `p_330918_` (`SuspiciousStewEffects`)
- `p_299043_` (`int`)
- `p_298449_` (`float`)

**Returns:** `public`

### getOffer

```java
public MerchantOffer getOffer(Entity p_219702_, RandomSource p_219703_)
```

**Parameters:**

- `p_219702_` (`Entity`)
- `p_219703_` (`RandomSource`)

**Returns:** `MerchantOffer`

### TippedArrowForItemsAndEmeralds

```java
public TippedArrowForItemsAndEmeralds(Item p_35793_, int p_35794_, Item p_35795_, int p_35796_, int p_35797_, int p_35798_, int p_35799_)
```

**Parameters:**

- `p_35793_` (`Item`)
- `p_35794_` (`int`)
- `p_35795_` (`Item`)
- `p_35796_` (`int`)
- `p_35797_` (`int`)
- `p_35798_` (`int`)
- `p_35799_` (`int`)

**Returns:** `public`

### getOffer

```java
public MerchantOffer getOffer(Entity p_219705_, RandomSource p_219706_)
```

**Parameters:**

- `p_219705_` (`Entity`)
- `p_219706_` (`RandomSource`)

**Returns:** `MerchantOffer`

### TreasureMapForEmeralds

```java
public TreasureMapForEmeralds(int p_207767_, TagKey<Structure> p_207768_, String p_207769_, Holder<MapDecorationType> p_335969_, int p_207771_, int p_207772_)
```

**Parameters:**

- `p_207767_` (`int`)
- `p_207768_` (`TagKey<Structure>`)
- `p_207769_` (`String`)
- `p_335969_` (`Holder<MapDecorationType>`)
- `p_207771_` (`int`)
- `p_207772_` (`int`)

**Returns:** `public`

### getOffer

```java
public MerchantOffer getOffer(Entity p_219708_, RandomSource p_219709_)
```

**Parameters:**

- `p_219708_` (`Entity`)
- `p_219709_` (`RandomSource`)

**Returns:** `MerchantOffer`

### oneTradeInBiomes

```java
public static VillagerTrades.TypeSpecificTrade oneTradeInBiomes(VillagerTrades.ItemListing p_302030_, VillagerType[]... p_301996_)
```

**Parameters:**

- `p_302030_` (`VillagerTrades.ItemListing`)
- `p_301996_` (`VillagerType[]...`)

**Returns:** `public static VillagerTrades.TypeSpecificTrade`

### getOffer

```java
public MerchantOffer getOffer(Entity p_296480_, RandomSource p_295729_)
```

**Parameters:**

- `p_296480_` (`Entity`)
- `p_295729_` (`RandomSource`)

**Returns:** `MerchantOffer`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `ItemListing` | interface |  |
