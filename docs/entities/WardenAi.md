# WardenAi

**Package:** `net.minecraft.world.entity.monster.warden`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `EMERGE_DURATION` | `int` |  |
| `ROAR_DURATION` | `int` |  |
| `DIGGING_COOLDOWN` | `int` |  |

## Methods

### updateActivity

```java
public static void updateActivity(Warden p_219513_)
```

**Parameters:**

- `p_219513_` (`Warden`)

**Returns:** `public static void`

### makeBrain

```java
protected static Brain<?> makeBrain(Warden p_219521_, Dynamic<?> p_219522_)
```

**Parameters:**

- `p_219521_` (`Warden`)
- `p_219522_` (`Dynamic<?>`)

**Returns:** `protected static Brain<?>`

### initCoreActivity

```java
 initCoreActivity()
```

**Returns:** ``

### initEmergeActivity

```java
 initEmergeActivity()
```

**Returns:** ``

### initDiggingActivity

```java
 initDiggingActivity()
```

**Returns:** ``

### initIdleActivity

```java
 initIdleActivity()
```

**Returns:** ``

### initRoarActivity

```java
 initRoarActivity()
```

**Returns:** ``

### initFightActivity

```java
 initFightActivity()
```

**Returns:** ``

### initInvestigateActivity

```java
 initInvestigateActivity()
```

**Returns:** ``

### initSniffingActivity

```java
 initSniffingActivity()
```

**Returns:** ``

### initCoreActivity

```java
private static void initCoreActivity(Brain<Warden> p_219511_)
```

**Parameters:**

- `p_219511_` (`Brain<Warden>`)

**Returns:** `private static void`

### initEmergeActivity

```java
private static void initEmergeActivity(Brain<Warden> p_219527_)
```

**Parameters:**

- `p_219527_` (`Brain<Warden>`)

**Returns:** `private static void`

### initDiggingActivity

```java
private static void initDiggingActivity(Brain<Warden> p_219532_)
```

**Parameters:**

- `p_219532_` (`Brain<Warden>`)

**Returns:** `private static void`

### initIdleActivity

```java
private static void initIdleActivity(Brain<Warden> p_219537_)
```

**Parameters:**

- `p_219537_` (`Brain<Warden>`)

**Returns:** `private static void`

### initInvestigateActivity

```java
private static void initInvestigateActivity(Brain<Warden> p_219542_)
```

**Parameters:**

- `p_219542_` (`Brain<Warden>`)

**Returns:** `private static void`

### initSniffingActivity

```java
private static void initSniffingActivity(Brain<Warden> p_219544_)
```

**Parameters:**

- `p_219544_` (`Brain<Warden>`)

**Returns:** `private static void`

### initRoarActivity

```java
private static void initRoarActivity(Brain<Warden> p_219546_)
```

**Parameters:**

- `p_219546_` (`Brain<Warden>`)

**Returns:** `private static void`

### initFightActivity

```java
private static void initFightActivity(Warden p_219518_, Brain<Warden> p_219519_)
```

**Parameters:**

- `p_219518_` (`Warden`)
- `p_219519_` (`Brain<Warden>`)

**Returns:** `private static void`

### isTarget

```java
private static boolean isTarget(Warden p_219515_, LivingEntity p_219516_)
```

**Parameters:**

- `p_219515_` (`Warden`)
- `p_219516_` (`LivingEntity`)

**Returns:** `private static boolean`

### onTargetInvalid

```java
private static void onTargetInvalid(Warden p_219529_, LivingEntity p_219530_)
```

**Parameters:**

- `p_219529_` (`Warden`)
- `p_219530_` (`LivingEntity`)

**Returns:** `private static void`

### setDigCooldown

```java
 setDigCooldown()
```

**Returns:** ``

### setDigCooldown

```java
public static void setDigCooldown(LivingEntity p_219506_)
```

**Parameters:**

- `p_219506_` (`LivingEntity`)

**Returns:** `public static void`

### setDisturbanceLocation

```java
public static void setDisturbanceLocation(Warden p_219524_, BlockPos p_219525_)
```

**Parameters:**

- `p_219524_` (`Warden`)
- `p_219525_` (`BlockPos`)

**Returns:** `public static void`

### setDigCooldown

```java
 setDigCooldown()
```

**Returns:** ``
