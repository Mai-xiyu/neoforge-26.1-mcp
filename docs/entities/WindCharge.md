# WindCharge

**Package:** `net.minecraft.world.entity.projectile.windcharge`
**Type:** class
**Extends:** `AbstractWindCharge`

## Methods

### WindCharge

```java
public WindCharge(EntityType<? extends AbstractWindCharge> p_326226_, Level p_326464_)
```

**Parameters:**

- `p_326226_` (`EntityType<? extends AbstractWindCharge>`)
- `p_326464_` (`Level`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### WindCharge

```java
public WindCharge(Player p_326044_, Level p_326101_, double p_326183_, double p_326157_, double p_325928_)
```

**Parameters:**

- `p_326044_` (`Player`)
- `p_326101_` (`Level`)
- `p_326183_` (`double`)
- `p_326157_` (`double`)
- `p_325928_` (`double`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### WindCharge

```java
public WindCharge(Level p_326007_, double p_326331_, double p_326001_, double p_325990_, Vec3 p_347497_)
```

**Parameters:**

- `p_326007_` (`Level`)
- `p_326331_` (`double`)
- `p_326001_` (`double`)
- `p_325990_` (`double`)
- `p_347497_` (`Vec3`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### tick

```java
public void tick()
```

### deflect

```java
public boolean deflect(ProjectileDeflection p_350880_, Entity p_350728_, Entity p_350716_, boolean p_350553_)
```

**Parameters:**

- `p_350880_` (`ProjectileDeflection`)
- `p_350728_` (`Entity`)
- `p_350716_` (`Entity`)
- `p_350553_` (`boolean`)

**Returns:** `boolean`

### explode

```java
protected void explode(Vec3 p_352393_)
```

**Parameters:**

- `p_352393_` (`Vec3`)
