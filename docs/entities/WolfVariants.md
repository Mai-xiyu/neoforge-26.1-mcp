# WolfVariants

**Package:** `net.minecraft.world.entity.animal`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `PALE` | `ResourceKey<WolfVariant>` |  |
| `SPOTTED` | `ResourceKey<WolfVariant>` |  |
| `SNOWY` | `ResourceKey<WolfVariant>` |  |
| `BLACK` | `ResourceKey<WolfVariant>` |  |
| `ASHEN` | `ResourceKey<WolfVariant>` |  |
| `RUSTY` | `ResourceKey<WolfVariant>` |  |
| `WOODS` | `ResourceKey<WolfVariant>` |  |
| `CHESTNUT` | `ResourceKey<WolfVariant>` |  |
| `STRIPED` | `ResourceKey<WolfVariant>` |  |
| `DEFAULT` | `ResourceKey<WolfVariant>` |  |

## Methods

### createKey

```java
private static ResourceKey<WolfVariant> createKey(String p_332764_)
```

**Parameters:**

- `p_332764_` (`String`)

**Returns:** `private static ResourceKey<WolfVariant>`

### register

```java
static void register(BootstrapContext<WolfVariant> p_332703_, ResourceKey<WolfVariant> p_332747_, String p_332786_, ResourceKey<Biome> p_332693_)
```

**Parameters:**

- `p_332703_` (`BootstrapContext<WolfVariant>`)
- `p_332747_` (`ResourceKey<WolfVariant>`)
- `p_332786_` (`String`)
- `p_332693_` (`ResourceKey<Biome>`)

**Returns:** `static void`

### register

```java
static void register(BootstrapContext<WolfVariant> p_333957_, ResourceKey<WolfVariant> p_334052_, String p_333903_, TagKey<Biome> p_333877_)
```

**Parameters:**

- `p_333957_` (`BootstrapContext<WolfVariant>`)
- `p_334052_` (`ResourceKey<WolfVariant>`)
- `p_333903_` (`String`)
- `p_333877_` (`TagKey<Biome>`)

**Returns:** `static void`

### register

```java
static void register(BootstrapContext<WolfVariant> p_333853_, ResourceKey<WolfVariant> p_333874_, String p_333924_, HolderSet<Biome> p_333998_)
```

**Parameters:**

- `p_333853_` (`BootstrapContext<WolfVariant>`)
- `p_333874_` (`ResourceKey<WolfVariant>`)
- `p_333924_` (`String`)
- `p_333998_` (`HolderSet<Biome>`)

**Returns:** `static void`

### getSpawnVariant

```java
public static Holder<WolfVariant> getSpawnVariant(RegistryAccess p_332694_, Holder<Biome> p_332773_)
```

**Parameters:**

- `p_332694_` (`RegistryAccess`)
- `p_332773_` (`Holder<Biome>`)

**Returns:** `public static Holder<WolfVariant>`

### bootstrap

```java
public static void bootstrap(BootstrapContext<WolfVariant> p_332726_)
```

**Parameters:**

- `p_332726_` (`BootstrapContext<WolfVariant>`)

**Returns:** `public static void`

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``
