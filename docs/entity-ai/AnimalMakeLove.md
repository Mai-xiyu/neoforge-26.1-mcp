# AnimalMakeLove

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class
**Extends:** `Behavior<Animal>`

## Methods

### AnimalMakeLove

```java
public AnimalMakeLove(EntityType<? extends Animal> p_320473_)
```

**Parameters:**

- `p_320473_` (`EntityType<? extends Animal>`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### AnimalMakeLove

```java
public AnimalMakeLove(EntityType<? extends Animal> p_22391_, float p_22392_, int p_320576_)
```

**Parameters:**

- `p_22391_` (`EntityType<? extends Animal>`)
- `p_22392_` (`float`)
- `p_320576_` (`int`)

**Returns:** `public`

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_22401_, Animal p_22402_)
```

**Parameters:**

- `p_22401_` (`ServerLevel`)
- `p_22402_` (`Animal`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_22404_, Animal p_22405_, long p_22406_)
```

**Parameters:**

- `p_22404_` (`ServerLevel`)
- `p_22405_` (`Animal`)
- `p_22406_` (`long`)

**Returns:** `protected void`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_22416_, Animal p_22417_, long p_22418_)
```

**Parameters:**

- `p_22416_` (`ServerLevel`)
- `p_22417_` (`Animal`)
- `p_22418_` (`long`)

**Returns:** `protected boolean`

### tick

```java
protected void tick(ServerLevel p_22428_, Animal p_22429_, long p_22430_)
```

**Parameters:**

- `p_22428_` (`ServerLevel`)
- `p_22429_` (`Animal`)
- `p_22430_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_22438_, Animal p_22439_, long p_22440_)
```

**Parameters:**

- `p_22438_` (`ServerLevel`)
- `p_22439_` (`Animal`)
- `p_22440_` (`long`)

**Returns:** `protected void`

### getBreedTarget

```java
private Animal getBreedTarget(Animal p_22410_)
```

**Parameters:**

- `p_22410_` (`Animal`)

**Returns:** `private Animal`

### hasBreedTargetOfRightType

```java
private boolean hasBreedTargetOfRightType(Animal p_22422_)
```

**Parameters:**

- `p_22422_` (`Animal`)

**Returns:** `private boolean`

### findValidBreedPartner

```java
private Optional<? extends Animal> findValidBreedPartner(Animal p_22432_)
```

**Parameters:**

- `p_22432_` (`Animal`)

**Returns:** `private Optional<? extends Animal>`
