# BreathAirGoal

**Package:** `net.minecraft.world.entity.ai.goal`
**Type:** class
**Extends:** `Goal`

## Methods

### BreathAirGoal

```java
public BreathAirGoal(PathfinderMob p_25103_)
```

**Parameters:**

- `p_25103_` (`PathfinderMob`)

**Returns:** `public`

### canUse

```java
public boolean canUse()
```

**Returns:** `boolean`

### canContinueToUse

```java
public boolean canContinueToUse()
```

**Returns:** `boolean`

### isInterruptable

```java
public boolean isInterruptable()
```

**Returns:** `boolean`

### start

```java
public void start()
```

### findAirPosition

```java
private void findAirPosition()
```

**Returns:** `private void`

### tick

```java
public void tick()
```

### givesAir

```java
private boolean givesAir(LevelReader p_25107_, BlockPos p_25108_)
```

**Parameters:**

- `p_25107_` (`LevelReader`)
- `p_25108_` (`BlockPos`)

**Returns:** `private boolean`
