# CatLieOnBedGoal

**Package:** `net.minecraft.world.entity.ai.goal`
**Type:** class
**Extends:** `MoveToBlockGoal`

## Methods

### CatLieOnBedGoal

```java
public CatLieOnBedGoal(Cat p_25135_, double p_25136_, int p_25137_)
```

**Parameters:**

- `p_25135_` (`Cat`)
- `p_25136_` (`double`)
- `p_25137_` (`int`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### canUse

```java
public boolean canUse()
```

**Returns:** `boolean`

### start

```java
public void start()
```

### nextStartTick

```java
protected int nextStartTick(PathfinderMob p_25140_)
```

**Parameters:**

- `p_25140_` (`PathfinderMob`)

**Returns:** `int`

### stop

```java
public void stop()
```

### tick

```java
public void tick()
```

### isValidTarget

```java
protected boolean isValidTarget(LevelReader p_25142_, BlockPos p_25143_)
```

**Parameters:**

- `p_25142_` (`LevelReader`)
- `p_25143_` (`BlockPos`)

**Returns:** `boolean`
