# Croak

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class
**Extends:** `Behavior<Frog>`

## Methods

### Croak

```java
public Croak()
```

**Returns:** `public`

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_217151_, Frog p_217152_)
```

**Parameters:**

- `p_217151_` (`ServerLevel`)
- `p_217152_` (`Frog`)

**Returns:** `protected boolean`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_217154_, Frog p_217155_, long p_217156_)
```

**Parameters:**

- `p_217154_` (`ServerLevel`)
- `p_217155_` (`Frog`)
- `p_217156_` (`long`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_217162_, Frog p_217163_, long p_217164_)
```

**Parameters:**

- `p_217162_` (`ServerLevel`)
- `p_217163_` (`Frog`)
- `p_217164_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_217170_, Frog p_217171_, long p_217172_)
```

**Parameters:**

- `p_217170_` (`ServerLevel`)
- `p_217171_` (`Frog`)
- `p_217172_` (`long`)

**Returns:** `protected void`

### tick

```java
protected void tick(ServerLevel p_217178_, Frog p_217179_, long p_217180_)
```

**Parameters:**

- `p_217178_` (`ServerLevel`)
- `p_217179_` (`Frog`)
- `p_217180_` (`long`)

**Returns:** `protected void`
