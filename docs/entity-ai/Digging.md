# Digging

**Package:** `net.minecraft.world.entity.ai.behavior.warden`
**Type:** class<E extends Warden> extends Behavior<E>

## Methods

### Digging

```java
public Digging(int p_217515_)
```

**Parameters:**

- `p_217515_` (`int`)

**Returns:** `public`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_217527_, E p_217528_, long p_217529_)
```

**Parameters:**

- `p_217527_` (`ServerLevel`)
- `p_217528_` (`E`)
- `p_217529_` (`long`)

**Returns:** `protected boolean`

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_217524_, E p_217525_)
```

**Parameters:**

- `p_217524_` (`ServerLevel`)
- `p_217525_` (`E`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_217535_, E p_217536_, long p_217537_)
```

**Parameters:**

- `p_217535_` (`ServerLevel`)
- `p_217536_` (`E`)
- `p_217537_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_217543_, E p_217544_, long p_217545_)
```

**Parameters:**

- `p_217543_` (`ServerLevel`)
- `p_217544_` (`E`)
- `p_217545_` (`long`)

**Returns:** `protected void`
