# Emerging

**Package:** `net.minecraft.world.entity.ai.behavior.warden`
**Type:** class<E extends Warden> extends Behavior<E>

## Methods

### Emerging

```java
public Emerging(int p_217547_)
```

**Parameters:**

- `p_217547_` (`int`)

**Returns:** `public`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_217553_, E p_217554_, long p_217555_)
```

**Parameters:**

- `p_217553_` (`ServerLevel`)
- `p_217554_` (`E`)
- `p_217555_` (`long`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_217561_, E p_217562_, long p_217563_)
```

**Parameters:**

- `p_217561_` (`ServerLevel`)
- `p_217562_` (`E`)
- `p_217563_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_217569_, E p_217570_, long p_217571_)
```

**Parameters:**

- `p_217569_` (`ServerLevel`)
- `p_217570_` (`E`)
- `p_217571_` (`long`)

**Returns:** `protected void`
