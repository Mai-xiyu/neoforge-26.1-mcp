# FollowMobGoal

**Package:** `net.minecraft.world.entity.ai.goal`
**Type:** class
**Extends:** `Goal`

## Methods

### FollowMobGoal

```java
public FollowMobGoal(Mob p_25271_, double p_25272_, float p_25273_, float p_25274_)
```

**Parameters:**

- `p_25271_` (`Mob`)
- `p_25272_` (`double`)
- `p_25273_` (`float`)
- `p_25274_` (`float`)

**Returns:** `public`

### IllegalArgumentException

```java
throw new IllegalArgumentException("Unsupported mob type for FollowMobGoal")
```

**Parameters:**

- `FollowMobGoal"` (`"Unsupported mob type for`)

**Returns:** `throw new`

### canUse

```java
public boolean canUse()
```

**Returns:** `boolean`

### canContinueToUse

```java
public boolean canContinueToUse()
```

**Returns:** `boolean`

### start

```java
public void start()
```

### stop

```java
public void stop()
```

### tick

```java
public void tick()
```
