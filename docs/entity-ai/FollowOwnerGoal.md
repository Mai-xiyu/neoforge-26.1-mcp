# FollowOwnerGoal

**Package:** `net.minecraft.world.entity.ai.goal`
**Type:** class
**Extends:** `Goal`

## Methods

### FollowOwnerGoal

```java
public FollowOwnerGoal(TamableAnimal p_25294_, double p_25295_, float p_25296_, float p_25297_)
```

**Parameters:**

- `p_25294_` (`TamableAnimal`)
- `p_25295_` (`double`)
- `p_25296_` (`float`)
- `p_25297_` (`float`)

**Returns:** `public`

### IllegalArgumentException

```java
throw new IllegalArgumentException("Unsupported mob type for FollowOwnerGoal")
```

**Parameters:**

- `FollowOwnerGoal"` (`"Unsupported mob type for`)

**Returns:** `throw new`

### canUse

```java
public boolean canUse()
```

**Returns:** `boolean`

### canContinueToUse

```java
public boolean canContinueToUse()
```

**Returns:** `boolean`

### start

```java
public void start()
```

### stop

```java
public void stop()
```

### tick

```java
public void tick()
```
