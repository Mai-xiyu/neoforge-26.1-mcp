# GoToPotentialJobSite

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class
**Extends:** `Behavior<Villager>`

## Methods

### GoToPotentialJobSite

```java
public GoToPotentialJobSite(float p_23098_)
```

**Parameters:**

- `p_23098_` (`float`)

**Returns:** `public`

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_23103_, Villager p_23104_)
```

**Parameters:**

- `p_23103_` (`ServerLevel`)
- `p_23104_` (`Villager`)

**Returns:** `protected boolean`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_23106_, Villager p_23107_, long p_23108_)
```

**Parameters:**

- `p_23106_` (`ServerLevel`)
- `p_23107_` (`Villager`)
- `p_23108_` (`long`)

**Returns:** `protected boolean`

### tick

```java
protected void tick(ServerLevel p_23121_, Villager p_23122_, long p_23123_)
```

**Parameters:**

- `p_23121_` (`ServerLevel`)
- `p_23122_` (`Villager`)
- `p_23123_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_23129_, Villager p_23130_, long p_23131_)
```

**Parameters:**

- `p_23129_` (`ServerLevel`)
- `p_23130_` (`Villager`)
- `p_23131_` (`long`)

**Returns:** `protected void`
