# GoToTargetLocation

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class

## Methods

### getNearbyPos

```java
private static BlockPos getNearbyPos(Mob p_217251_, BlockPos p_217252_)
```

**Parameters:**

- `p_217251_` (`Mob`)
- `p_217252_` (`BlockPos`)

**Returns:** `private static BlockPos`

### getRandomOffset

```java
private static int getRandomOffset(RandomSource p_217247_)
```

**Parameters:**

- `p_217247_` (`RandomSource`)

**Returns:** `private static int`

### create

```java
<E extends Mob> public static <E extends Mob> OneShot<E> create(MemoryModuleType<BlockPos> p_259938_, int p_259740_, float p_259957_)
```

**Parameters:**

- `p_259938_` (`MemoryModuleType<BlockPos>`)
- `p_259740_` (`int`)
- `p_259957_` (`float`)

**Returns:** `public static <E extends Mob> OneShot<E>`
