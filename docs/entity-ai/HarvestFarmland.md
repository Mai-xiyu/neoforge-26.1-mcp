# HarvestFarmland

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class
**Extends:** `Behavior<Villager>`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `SPEED_MODIFIER` | `float` |  |

## Methods

### HarvestFarmland

```java
public HarvestFarmland()
```

**Returns:** `public`

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_23174_, Villager p_23175_)
```

**Parameters:**

- `p_23174_` (`ServerLevel`)
- `p_23175_` (`Villager`)

**Returns:** `protected boolean`

### validPos

```java
private boolean validPos(BlockPos p_23181_, ServerLevel p_23182_)
```

**Parameters:**

- `p_23181_` (`BlockPos`)
- `p_23182_` (`ServerLevel`)

**Returns:** `private boolean`

### start

```java
protected void start(ServerLevel p_23177_, Villager p_23178_, long p_23179_)
```

**Parameters:**

- `p_23177_` (`ServerLevel`)
- `p_23178_` (`Villager`)
- `p_23179_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_23188_, Villager p_23189_, long p_23190_)
```

**Parameters:**

- `p_23188_` (`ServerLevel`)
- `p_23189_` (`Villager`)
- `p_23190_` (`long`)

**Returns:** `protected void`

### tick

```java
protected void tick(ServerLevel p_23196_, Villager p_23197_, long p_23198_)
```

**Parameters:**

- `p_23196_` (`ServerLevel`)
- `p_23197_` (`Villager`)
- `p_23198_` (`long`)

**Returns:** `protected void`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_23204_, Villager p_23205_, long p_23206_)
```

**Parameters:**

- `p_23204_` (`ServerLevel`)
- `p_23205_` (`Villager`)
- `p_23206_` (`long`)

**Returns:** `protected boolean`
