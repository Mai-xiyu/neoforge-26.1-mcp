# LookAndFollowTradingPlayerSink

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class
**Extends:** `Behavior<Villager>`

## Methods

### LookAndFollowTradingPlayerSink

```java
public LookAndFollowTradingPlayerSink(float p_23434_)
```

**Parameters:**

- `p_23434_` (`float`)

**Returns:** `public`

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_23445_, Villager p_23446_)
```

**Parameters:**

- `p_23445_` (`ServerLevel`)
- `p_23446_` (`Villager`)

**Returns:** `protected boolean`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_23448_, Villager p_23449_, long p_23450_)
```

**Parameters:**

- `p_23448_` (`ServerLevel`)
- `p_23449_` (`Villager`)
- `p_23450_` (`long`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_23458_, Villager p_23459_, long p_23460_)
```

**Parameters:**

- `p_23458_` (`ServerLevel`)
- `p_23459_` (`Villager`)
- `p_23460_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_23466_, Villager p_23467_, long p_23468_)
```

**Parameters:**

- `p_23466_` (`ServerLevel`)
- `p_23467_` (`Villager`)
- `p_23468_` (`long`)

**Returns:** `protected void`

### tick

```java
protected void tick(ServerLevel p_23474_, Villager p_23475_, long p_23476_)
```

**Parameters:**

- `p_23474_` (`ServerLevel`)
- `p_23475_` (`Villager`)
- `p_23476_` (`long`)

**Returns:** `protected void`

### timedOut

```java
protected boolean timedOut(long p_23436_)
```

**Parameters:**

- `p_23436_` (`long`)

**Returns:** `boolean`

### followPlayer

```java
private void followPlayer(Villager p_23452_)
```

**Parameters:**

- `p_23452_` (`Villager`)

**Returns:** `private void`
