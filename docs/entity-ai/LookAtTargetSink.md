# LookAtTargetSink

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class
**Extends:** `Behavior<Mob>`

## Methods

### LookAtTargetSink

```java
public LookAtTargetSink(int p_23478_, int p_23479_)
```

**Parameters:**

- `p_23478_` (`int`)
- `p_23479_` (`int`)

**Returns:** `public`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_23481_, Mob p_23482_, long p_23483_)
```

**Parameters:**

- `p_23481_` (`ServerLevel`)
- `p_23482_` (`Mob`)
- `p_23483_` (`long`)

**Returns:** `protected boolean`

### stop

```java
protected void stop(ServerLevel p_23492_, Mob p_23493_, long p_23494_)
```

**Parameters:**

- `p_23492_` (`ServerLevel`)
- `p_23493_` (`Mob`)
- `p_23494_` (`long`)

**Returns:** `protected void`

### tick

```java
protected void tick(ServerLevel p_23503_, Mob p_23504_, long p_23505_)
```

**Parameters:**

- `p_23503_` (`ServerLevel`)
- `p_23504_` (`Mob`)
- `p_23505_` (`long`)

**Returns:** `protected void`
