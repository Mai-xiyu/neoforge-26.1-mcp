# MoveToTargetSink

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class
**Extends:** `Behavior<Mob>`

## Methods

### MoveToTargetSink

```java
public MoveToTargetSink()
```

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### MoveToTargetSink

```java
public MoveToTargetSink(int p_23573_, int p_23574_)
```

**Parameters:**

- `p_23573_` (`int`)
- `p_23574_` (`int`)

**Returns:** `public`

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_23583_, Mob p_23584_)
```

**Parameters:**

- `p_23583_` (`ServerLevel`)
- `p_23584_` (`Mob`)

**Returns:** `protected boolean`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_23586_, Mob p_23587_, long p_23588_)
```

**Parameters:**

- `p_23586_` (`ServerLevel`)
- `p_23587_` (`Mob`)
- `p_23588_` (`long`)

**Returns:** `protected boolean`

### stop

```java
protected void stop(ServerLevel p_23601_, Mob p_23602_, long p_23603_)
```

**Parameters:**

- `p_23601_` (`ServerLevel`)
- `p_23602_` (`Mob`)
- `p_23603_` (`long`)

**Returns:** `protected void`

### start

```java
protected void start(ServerLevel p_23609_, Mob p_23610_, long p_23611_)
```

**Parameters:**

- `p_23609_` (`ServerLevel`)
- `p_23610_` (`Mob`)
- `p_23611_` (`long`)

**Returns:** `protected void`

### tick

```java
protected void tick(ServerLevel p_23617_, Mob p_23618_, long p_23619_)
```

**Parameters:**

- `p_23617_` (`ServerLevel`)
- `p_23618_` (`Mob`)
- `p_23619_` (`long`)

**Returns:** `protected void`

### tryComputePath

```java
private boolean tryComputePath(Mob p_23593_, WalkTarget p_23594_, long p_23595_)
```

**Parameters:**

- `p_23593_` (`Mob`)
- `p_23594_` (`WalkTarget`)
- `p_23595_` (`long`)

**Returns:** `private boolean`

### reachedTarget

```java
private boolean reachedTarget(Mob p_23590_, WalkTarget p_23591_)
```

**Parameters:**

- `p_23590_` (`Mob`)
- `p_23591_` (`WalkTarget`)

**Returns:** `private boolean`

### isWalkTargetSpectator

```java
private static boolean isWalkTargetSpectator(WalkTarget p_277420_)
```

**Parameters:**

- `p_277420_` (`WalkTarget`)

**Returns:** `private static boolean`
