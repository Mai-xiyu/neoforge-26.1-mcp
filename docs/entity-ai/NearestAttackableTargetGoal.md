# NearestAttackableTargetGoal

**Package:** `net.minecraft.world.entity.ai.goal.target`
**Type:** class<T extends LivingEntity>
**Extends:** `TargetGoal`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `targetType` | `Class<T>` |  |
| `randomInterval` | `int` |  |
| `targetConditions` | `TargetingConditions` |  |

## Methods

### NearestAttackableTargetGoal

```java
public NearestAttackableTargetGoal(Mob p_26060_, Class<T> p_26061_, boolean p_26062_)
```

**Parameters:**

- `p_26060_` (`Mob`)
- `p_26061_` (`Class<T>`)
- `p_26062_` (`boolean`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### NearestAttackableTargetGoal

```java
public NearestAttackableTargetGoal(Mob p_199891_, Class<T> p_199892_, boolean p_199893_, Predicate<LivingEntity> p_199894_)
```

**Parameters:**

- `p_199891_` (`Mob`)
- `p_199892_` (`Class<T>`)
- `p_199893_` (`boolean`)
- `p_199894_` (`Predicate<LivingEntity>`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### NearestAttackableTargetGoal

```java
public NearestAttackableTargetGoal(Mob p_26064_, Class<T> p_26065_, boolean p_26066_, boolean p_26067_)
```

**Parameters:**

- `p_26064_` (`Mob`)
- `p_26065_` (`Class<T>`)
- `p_26066_` (`boolean`)
- `p_26067_` (`boolean`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### NearestAttackableTargetGoal

```java
public NearestAttackableTargetGoal(Mob p_26053_, Class<T> p_26054_, int p_26055_, boolean p_26056_, boolean p_26057_, Predicate<LivingEntity> p_26058_)
```

**Parameters:**

- `p_26053_` (`Mob`)
- `p_26054_` (`Class<T>`)
- `p_26055_` (`int`)
- `p_26056_` (`boolean`)
- `p_26057_` (`boolean`)
- `p_26058_` (`Predicate<LivingEntity>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### canUse

```java
public boolean canUse()
```

**Returns:** `boolean`

### getTargetSearchArea

```java
protected AABB getTargetSearchArea(double p_26069_)
```

**Parameters:**

- `p_26069_` (`double`)

**Returns:** `protected AABB`

### findTarget

```java
protected void findTarget()
```

**Returns:** `protected void`

### start

```java
public void start()
```

### setTarget

```java
public void setTarget(LivingEntity p_26071_)
```

**Parameters:**

- `p_26071_` (`LivingEntity`)

**Returns:** `public void`
