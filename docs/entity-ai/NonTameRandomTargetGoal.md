# NonTameRandomTargetGoal

**Package:** `net.minecraft.world.entity.ai.goal.target`
**Type:** class<T extends LivingEntity> extends NearestAttackableTargetGoal<T>

## Methods

### NonTameRandomTargetGoal

```java
public NonTameRandomTargetGoal(TamableAnimal p_26097_, Class<T> p_26098_, boolean p_26099_, Predicate<LivingEntity> p_26100_)
```

**Parameters:**

- `p_26097_` (`TamableAnimal`)
- `p_26098_` (`Class<T>`)
- `p_26099_` (`boolean`)
- `p_26100_` (`Predicate<LivingEntity>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### canUse

```java
public boolean canUse()
```

**Returns:** `boolean`

### canContinueToUse

```java
public boolean canContinueToUse()
```

**Returns:** `boolean`
