# PoiCompetitorScan

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class

## Methods

### create

```java
public static BehaviorControl<Villager> create()
```

**Returns:** `public static BehaviorControl<Villager>`

### selectWinner

```java
private static Villager selectWinner(Villager p_23725_, Villager p_23726_)
```

**Parameters:**

- `p_23725_` (`Villager`)
- `p_23726_` (`Villager`)

**Returns:** `private static Villager`

### competesForSameJobsite

```java
private static boolean competesForSameJobsite(GlobalPos p_217330_, Holder<PoiType> p_217331_, Villager p_217332_)
```

**Parameters:**

- `p_217330_` (`GlobalPos`)
- `p_217331_` (`Holder<PoiType>`)
- `p_217332_` (`Villager`)

**Returns:** `private static boolean`

### hasMatchingProfession

```java
private static boolean hasMatchingProfession(Holder<PoiType> p_217334_, VillagerProfession p_217335_)
```

**Parameters:**

- `p_217334_` (`Holder<PoiType>`)
- `p_217335_` (`VillagerProfession`)

**Returns:** `private static boolean`
