# RandomLookAround

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class
**Extends:** `Behavior<Mob>`

## Methods

### RandomLookAround

```java
public RandomLookAround(IntProvider p_248963_, float p_251826_, float p_251456_, float p_249962_)
```

**Parameters:**

- `p_248963_` (`IntProvider`)
- `p_251826_` (`float`)
- `p_251456_` (`float`)
- `p_249962_` (`float`)

**Returns:** `public`

### IllegalArgumentException

```java
throw new IllegalArgumentException("Minimum pitch is larger than maximum pitch! " + p_251456_ + " > " + p_249962_)
```

**Parameters:**

- `p_249962_` (`"Minimum pitch is larger than maximum pitch! " + p_251456_ + " > " +`)

**Returns:** `throw new`

### start

```java
protected void start(ServerLevel p_250941_, Mob p_248765_, long p_251801_)
```

**Parameters:**

- `p_250941_` (`ServerLevel`)
- `p_248765_` (`Mob`)
- `p_251801_` (`long`)

**Returns:** `protected void`
