# RangedAttackGoal

**Package:** `net.minecraft.world.entity.ai.goal`
**Type:** class
**Extends:** `Goal`

## Methods

### RangedAttackGoal

```java
public RangedAttackGoal(RangedAttackMob p_25768_, double p_25769_, int p_25770_, float p_25771_)
```

**Parameters:**

- `p_25768_` (`RangedAttackMob`)
- `p_25769_` (`double`)
- `p_25770_` (`int`)
- `p_25771_` (`float`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### RangedAttackGoal

```java
public RangedAttackGoal(RangedAttackMob p_25773_, double p_25774_, int p_25775_, int p_25776_, float p_25777_)
```

**Parameters:**

- `p_25773_` (`RangedAttackMob`)
- `p_25774_` (`double`)
- `p_25775_` (`int`)
- `p_25776_` (`int`)
- `p_25777_` (`float`)

**Returns:** `public`

### IllegalArgumentException

```java
throw new IllegalArgumentException("ArrowAttackGoal requires Mob implements RangedAttackMob")
```

**Parameters:**

- `RangedAttackMob"` (`"ArrowAttackGoal requires Mob implements`)

**Returns:** `throw new`

### canUse

```java
public boolean canUse()
```

**Returns:** `boolean`

### canContinueToUse

```java
public boolean canContinueToUse()
```

**Returns:** `boolean`

### stop

```java
public void stop()
```

### requiresUpdateEveryTick

```java
public boolean requiresUpdateEveryTick()
```

**Returns:** `boolean`

### tick

```java
public void tick()
```
