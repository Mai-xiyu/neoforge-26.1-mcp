# RangedBowAttackGoal

**Package:** `net.minecraft.world.entity.ai.goal`
**Type:** class<T extends net.minecraft.world.entity.Mob & RangedAttackMob>
**Extends:** `Goal`

## Methods

### RangedBowAttackGoal

```java
RangedAttackMob> RangedBowAttackGoal(M p_25792_, double p_25793_, int p_25794_, float p_25795_)
```

**Parameters:**

- `p_25792_` (`M`)
- `p_25793_` (`double`)
- `p_25794_` (`int`)
- `p_25795_` (`float`)

**Returns:** `RangedAttackMob>`

### RangedBowAttackGoal

```java
public RangedBowAttackGoal(T p_25792_, double p_25793_, int p_25794_, float p_25795_)
```

**Parameters:**

- `p_25792_` (`T`)
- `p_25793_` (`double`)
- `p_25794_` (`int`)
- `p_25795_` (`float`)

**Returns:** `public`

### setMinAttackInterval

```java
public void setMinAttackInterval(int p_25798_)
```

**Parameters:**

- `p_25798_` (`int`)

**Returns:** `public void`

### canUse

```java
public boolean canUse()
```

**Returns:** `boolean`

### isHoldingBow

```java
protected boolean isHoldingBow()
```

**Returns:** `protected boolean`

### canContinueToUse

```java
public boolean canContinueToUse()
```

**Returns:** `boolean`

### start

```java
public void start()
```

### stop

```java
public void stop()
```

### requiresUpdateEveryTick

```java
public boolean requiresUpdateEveryTick()
```

**Returns:** `boolean`

### tick

```java
public void tick()
```
