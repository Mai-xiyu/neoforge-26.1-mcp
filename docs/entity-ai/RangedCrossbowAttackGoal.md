# RangedCrossbowAttackGoal

**Package:** `net.minecraft.world.entity.ai.goal`
**Type:** class<T extends net.minecraft.world.entity.Mob & CrossbowAttackMob>
**Extends:** `Goal`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `PATHFINDING_DELAY_RANGE` | `UniformInt` |  |

## Methods

### RangedCrossbowAttackGoal

```java
CrossbowAttackMob> RangedCrossbowAttackGoal(M p_25814_, double p_25815_, float p_25816_)
```

**Parameters:**

- `p_25814_` (`M`)
- `p_25815_` (`double`)
- `p_25816_` (`float`)

**Returns:** `CrossbowAttackMob>`

### RangedCrossbowAttackGoal

```java
public RangedCrossbowAttackGoal(T p_25814_, double p_25815_, float p_25816_)
```

**Parameters:**

- `p_25814_` (`T`)
- `p_25815_` (`double`)
- `p_25816_` (`float`)

**Returns:** `public`

### canUse

```java
public boolean canUse()
```

**Returns:** `boolean`

### isHoldingCrossbow

```java
private boolean isHoldingCrossbow()
```

**Returns:** `private boolean`

### canContinueToUse

```java
public boolean canContinueToUse()
```

**Returns:** `boolean`

### isValidTarget

```java
private boolean isValidTarget()
```

**Returns:** `private boolean`

### stop

```java
public void stop()
```

### requiresUpdateEveryTick

```java
public boolean requiresUpdateEveryTick()
```

**Returns:** `boolean`

### tick

```java
public void tick()
```

### canRun

```java
private boolean canRun()
```

**Returns:** `private boolean`
