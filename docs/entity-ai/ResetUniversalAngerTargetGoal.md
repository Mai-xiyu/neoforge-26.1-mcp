# ResetUniversalAngerTargetGoal

**Package:** `net.minecraft.world.entity.ai.goal.target`
**Type:** class<T extends Mob & NeutralMob>
**Extends:** `Goal`

## Methods

### ResetUniversalAngerTargetGoal

```java
public ResetUniversalAngerTargetGoal(T p_26121_, boolean p_26122_)
```

**Parameters:**

- `p_26121_` (`T`)
- `p_26122_` (`boolean`)

**Returns:** `public`

### canUse

```java
public boolean canUse()
```

**Returns:** `boolean`

### wasHurtByPlayer

```java
private boolean wasHurtByPlayer()
```

**Returns:** `private boolean`

### start

```java
public void start()
```

### getNearbyMobsOfSameType

```java
private List<? extends Mob> getNearbyMobsOfSameType()
```

**Returns:** `private List<? extends Mob>`
