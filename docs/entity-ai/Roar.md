# Roar

**Package:** `net.minecraft.world.entity.ai.behavior.warden`
**Type:** class
**Extends:** `Behavior<Warden>`

## Methods

### Roar

```java
public Roar()
```

**Returns:** `public`

### start

```java
protected void start(ServerLevel p_217580_, Warden p_217581_, long p_217582_)
```

**Parameters:**

- `p_217580_` (`ServerLevel`)
- `p_217581_` (`Warden`)
- `p_217582_` (`long`)

**Returns:** `protected void`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_217588_, Warden p_217589_, long p_217590_)
```

**Parameters:**

- `p_217588_` (`ServerLevel`)
- `p_217589_` (`Warden`)
- `p_217590_` (`long`)

**Returns:** `protected boolean`

### tick

```java
protected void tick(ServerLevel p_217596_, Warden p_217597_, long p_217598_)
```

**Parameters:**

- `p_217596_` (`ServerLevel`)
- `p_217597_` (`Warden`)
- `p_217598_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_217604_, Warden p_217605_, long p_217606_)
```

**Parameters:**

- `p_217604_` (`ServerLevel`)
- `p_217605_` (`Warden`)
- `p_217606_` (`long`)

**Returns:** `protected void`
