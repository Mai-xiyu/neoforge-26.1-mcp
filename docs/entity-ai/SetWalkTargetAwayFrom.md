# SetWalkTargetAwayFrom

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class

## Methods

### pos

```java
public static BehaviorControl<PathfinderMob> pos(MemoryModuleType<BlockPos> p_259330_, float p_259719_, int p_259965_, boolean p_259828_)
```

**Parameters:**

- `p_259330_` (`MemoryModuleType<BlockPos>`)
- `p_259719_` (`float`)
- `p_259965_` (`int`)
- `p_259828_` (`boolean`)

**Returns:** `public static BehaviorControl<PathfinderMob>`

### create

```java
return create()
```

**Returns:** `return`

### entity

```java
public static OneShot<PathfinderMob> entity(MemoryModuleType<? extends Entity> p_259598_, float p_260183_, int p_260077_, boolean p_259761_)
```

**Parameters:**

- `p_259598_` (`MemoryModuleType<? extends Entity>`)
- `p_260183_` (`float`)
- `p_260077_` (`int`)
- `p_259761_` (`boolean`)

**Returns:** `public static OneShot<PathfinderMob>`

### create

```java
return create()
```

**Returns:** `return`

### create

```java
<T> private static <T> OneShot<PathfinderMob> create(MemoryModuleType<T> p_260057_, float p_259672_, int p_259866_, boolean p_259232_, Function<T, Vec3> p_259355_)
```

**Parameters:**

- `p_260057_` (`MemoryModuleType<T>`)
- `p_259672_` (`float`)
- `p_259866_` (`int`)
- `p_259232_` (`boolean`)
- `p_259355_` (`Function<T, Vec3>`)

**Returns:** `private static <T> OneShot<PathfinderMob>`
