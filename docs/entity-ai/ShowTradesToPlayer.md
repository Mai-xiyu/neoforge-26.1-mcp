# ShowTradesToPlayer

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class
**Extends:** `Behavior<Villager>`

## Methods

### ShowTradesToPlayer

```java
public ShowTradesToPlayer(int p_24096_, int p_24097_)
```

**Parameters:**

- `p_24096_` (`int`)
- `p_24097_` (`int`)

**Returns:** `public`

### checkExtraStartConditions

```java
public boolean checkExtraStartConditions(ServerLevel p_24106_, Villager p_24107_)
```

**Parameters:**

- `p_24106_` (`ServerLevel`)
- `p_24107_` (`Villager`)

**Returns:** `public boolean`

### canStillUse

```java
public boolean canStillUse(ServerLevel p_24109_, Villager p_24110_, long p_24111_)
```

**Parameters:**

- `p_24109_` (`ServerLevel`)
- `p_24110_` (`Villager`)
- `p_24111_` (`long`)

**Returns:** `public boolean`

### start

```java
public void start(ServerLevel p_24124_, Villager p_24125_, long p_24126_)
```

**Parameters:**

- `p_24124_` (`ServerLevel`)
- `p_24125_` (`Villager`)
- `p_24126_` (`long`)

**Returns:** `public void`

### tick

```java
public void tick(ServerLevel p_24134_, Villager p_24135_, long p_24136_)
```

**Parameters:**

- `p_24134_` (`ServerLevel`)
- `p_24135_` (`Villager`)
- `p_24136_` (`long`)

**Returns:** `public void`

### clearHeldItem

```java
 clearHeldItem()
```

**Returns:** ``

### stop

```java
public void stop(ServerLevel p_24144_, Villager p_24145_, long p_24146_)
```

**Parameters:**

- `p_24144_` (`ServerLevel`)
- `p_24145_` (`Villager`)
- `p_24146_` (`long`)

**Returns:** `public void`

### clearHeldItem

```java
 clearHeldItem()
```

**Returns:** ``

### findItemsToDisplay

```java
private void findItemsToDisplay(LivingEntity p_24113_, Villager p_24114_)
```

**Parameters:**

- `p_24113_` (`LivingEntity`)
- `p_24114_` (`Villager`)

**Returns:** `private void`

### displayFirstItem

```java
private void displayFirstItem(Villager p_24116_)
```

**Parameters:**

- `p_24116_` (`Villager`)

**Returns:** `private void`

### updateDisplayItems

```java
private void updateDisplayItems(Villager p_24128_)
```

**Parameters:**

- `p_24128_` (`Villager`)

**Returns:** `private void`

### playerItemStackMatchesCostOfOffer

```java
private boolean playerItemStackMatchesCostOfOffer(MerchantOffer p_24118_)
```

**Parameters:**

- `p_24118_` (`MerchantOffer`)

**Returns:** `private boolean`

### clearHeldItem

```java
private static void clearHeldItem(Villager p_182374_)
```

**Parameters:**

- `p_182374_` (`Villager`)

**Returns:** `private static void`

### displayAsHeldItem

```java
private static void displayAsHeldItem(Villager p_182371_, ItemStack p_182372_)
```

**Parameters:**

- `p_182371_` (`Villager`)
- `p_182372_` (`ItemStack`)

**Returns:** `private static void`

### lookAtTarget

```java
private LivingEntity lookAtTarget(Villager p_24138_)
```

**Parameters:**

- `p_24138_` (`Villager`)

**Returns:** `private LivingEntity`

### displayCyclingItems

```java
private void displayCyclingItems(Villager p_24148_)
```

**Parameters:**

- `p_24148_` (`Villager`)

**Returns:** `private void`
