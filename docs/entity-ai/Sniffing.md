# Sniffing

**Package:** `net.minecraft.world.entity.ai.behavior.warden`
**Type:** class<E extends Warden> extends Behavior<E>

## Methods

### Sniffing

```java
public Sniffing(int p_217647_)
```

**Parameters:**

- `p_217647_` (`int`)

**Returns:** `public`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_217653_, E p_217654_, long p_217655_)
```

**Parameters:**

- `p_217653_` (`ServerLevel`)
- `p_217654_` (`E`)
- `p_217655_` (`long`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_217664_, E p_217665_, long p_217666_)
```

**Parameters:**

- `p_217664_` (`ServerLevel`)
- `p_217665_` (`E`)
- `p_217666_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_217672_, E p_217673_, long p_217674_)
```

**Parameters:**

- `p_217672_` (`ServerLevel`)
- `p_217673_` (`E`)
- `p_217674_` (`long`)

**Returns:** `protected void`
