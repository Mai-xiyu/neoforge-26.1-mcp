# SonicBoom

**Package:** `net.minecraft.world.entity.ai.behavior.warden`
**Type:** class
**Extends:** `Behavior<Warden>`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `COOLDOWN` | `int` |  |

## Methods

### SonicBoom

```java
public SonicBoom()
```

**Returns:** `public`

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_217692_, Warden p_217693_)
```

**Parameters:**

- `p_217692_` (`ServerLevel`)
- `p_217693_` (`Warden`)

**Returns:** `protected boolean`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_217695_, Warden p_217696_, long p_217697_)
```

**Parameters:**

- `p_217695_` (`ServerLevel`)
- `p_217696_` (`Warden`)
- `p_217697_` (`long`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_217713_, Warden p_217714_, long p_217715_)
```

**Parameters:**

- `p_217713_` (`ServerLevel`)
- `p_217714_` (`Warden`)
- `p_217715_` (`long`)

**Returns:** `protected void`

### tick

```java
protected void tick(ServerLevel p_217724_, Warden p_217725_, long p_217726_)
```

**Parameters:**

- `p_217724_` (`ServerLevel`)
- `p_217725_` (`Warden`)
- `p_217726_` (`long`)

**Returns:** `protected void`

### stop

```java
protected void stop(ServerLevel p_217732_, Warden p_217733_, long p_217734_)
```

**Parameters:**

- `p_217732_` (`ServerLevel`)
- `p_217733_` (`Warden`)
- `p_217734_` (`long`)

**Returns:** `protected void`

### setCooldown

```java
 setCooldown()
```

**Returns:** ``

### setCooldown

```java
public static void setCooldown(LivingEntity p_217699_, int p_217700_)
```

**Parameters:**

- `p_217699_` (`LivingEntity`)
- `p_217700_` (`int`)

**Returns:** `public static void`
