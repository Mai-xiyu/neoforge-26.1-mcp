# Swim

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class
**Extends:** `Behavior<Mob>`

## Methods

### Swim

```java
public Swim(float p_24383_)
```

**Parameters:**

- `p_24383_` (`float`)

**Returns:** `public`

### shouldSwim

```java
public static boolean shouldSwim(Mob p_316787_)
```

**Parameters:**

- `p_316787_` (`Mob`)

**Returns:** `public static boolean`

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_24388_, Mob p_24389_)
```

**Parameters:**

- `p_24388_` (`ServerLevel`)
- `p_24389_` (`Mob`)

**Returns:** `protected boolean`

### shouldSwim

```java
return shouldSwim()
```

**Returns:** `return`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_24391_, Mob p_24392_, long p_24393_)
```

**Parameters:**

- `p_24391_` (`ServerLevel`)
- `p_24392_` (`Mob`)
- `p_24393_` (`long`)

**Returns:** `protected boolean`

### tick

```java
protected void tick(ServerLevel p_24399_, Mob p_24400_, long p_24401_)
```

**Parameters:**

- `p_24399_` (`ServerLevel`)
- `p_24400_` (`Mob`)
- `p_24401_` (`long`)

**Returns:** `protected void`
