# VillagerPanicTrigger

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class
**Extends:** `Behavior<Villager>`

## Methods

### VillagerPanicTrigger

```java
public VillagerPanicTrigger()
```

**Returns:** `public`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_24684_, Villager p_24685_, long p_24686_)
```

**Parameters:**

- `p_24684_` (`ServerLevel`)
- `p_24685_` (`Villager`)
- `p_24686_` (`long`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_24694_, Villager p_24695_, long p_24696_)
```

**Parameters:**

- `p_24694_` (`ServerLevel`)
- `p_24695_` (`Villager`)
- `p_24696_` (`long`)

**Returns:** `protected void`

### tick

```java
protected void tick(ServerLevel p_24700_, Villager p_24701_, long p_24702_)
```

**Parameters:**

- `p_24700_` (`ServerLevel`)
- `p_24701_` (`Villager`)
- `p_24702_` (`long`)

**Returns:** `protected void`

### hasHostile

```java
public static boolean hasHostile(LivingEntity p_24688_)
```

**Parameters:**

- `p_24688_` (`LivingEntity`)

**Returns:** `public static boolean`

### isHurt

```java
public static boolean isHurt(LivingEntity p_24698_)
```

**Parameters:**

- `p_24698_` (`LivingEntity`)

**Returns:** `public static boolean`
