# WalkTarget

**Package:** `net.minecraft.world.entity.ai.memory`
**Type:** class

## Methods

### WalkTarget

```java
public WalkTarget(BlockPos p_26417_, float p_26418_, int p_26419_)
```

**Parameters:**

- `p_26417_` (`BlockPos`)
- `p_26418_` (`float`)
- `p_26419_` (`int`)

**Returns:** `public`

### WalkTarget

```java
public WalkTarget(Vec3 p_26413_, float p_26414_, int p_26415_)
```

**Parameters:**

- `p_26413_` (`Vec3`)
- `p_26414_` (`float`)
- `p_26415_` (`int`)

**Returns:** `public`

### WalkTarget

```java
public WalkTarget(Entity p_148209_, float p_148210_, int p_148211_)
```

**Parameters:**

- `p_148209_` (`Entity`)
- `p_148210_` (`float`)
- `p_148211_` (`int`)

**Returns:** `public`

### WalkTarget

```java
public WalkTarget(PositionTracker p_26409_, float p_26410_, int p_26411_)
```

**Parameters:**

- `p_26409_` (`PositionTracker`)
- `p_26410_` (`float`)
- `p_26411_` (`int`)

**Returns:** `public`

### getTarget

```java
public PositionTracker getTarget()
```

**Returns:** `public PositionTracker`

### getSpeedModifier

```java
public float getSpeedModifier()
```

**Returns:** `public float`

### getCloseEnoughDist

```java
public int getCloseEnoughDist()
```

**Returns:** `public int`
