# WorkAtPoi

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class
**Extends:** `Behavior<Villager>`

## Methods

### WorkAtPoi

```java
public WorkAtPoi()
```

**Returns:** `public`

### checkExtraStartConditions

```java
protected boolean checkExtraStartConditions(ServerLevel p_24827_, Villager p_24828_)
```

**Parameters:**

- `p_24827_` (`ServerLevel`)
- `p_24828_` (`Villager`)

**Returns:** `protected boolean`

### start

```java
protected void start(ServerLevel p_24816_, Villager p_24817_, long p_24818_)
```

**Parameters:**

- `p_24816_` (`ServerLevel`)
- `p_24817_` (`Villager`)
- `p_24818_` (`long`)

**Returns:** `protected void`

### useWorkstation

```java
protected void useWorkstation(ServerLevel p_24813_, Villager p_24814_)
```

**Parameters:**

- `p_24813_` (`ServerLevel`)
- `p_24814_` (`Villager`)

**Returns:** `protected void`

### canStillUse

```java
protected boolean canStillUse(ServerLevel p_24830_, Villager p_24831_, long p_24832_)
```

**Parameters:**

- `p_24830_` (`ServerLevel`)
- `p_24831_` (`Villager`)
- `p_24832_` (`long`)

**Returns:** `protected boolean`
