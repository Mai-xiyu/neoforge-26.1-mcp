# YieldJobSite

**Package:** `net.minecraft.world.entity.ai.behavior`
**Type:** class

## Methods

### create

```java
public static BehaviorControl<Villager> create(float p_259768_)
```

**Parameters:**

- `p_259768_` (`float`)

**Returns:** `public static BehaviorControl<Villager>`

### nearbyWantsJobsite

```java
private static boolean nearbyWantsJobsite(Holder<PoiType> p_217511_, Villager p_217512_, BlockPos p_217513_)
```

**Parameters:**

- `p_217511_` (`Holder<PoiType>`)
- `p_217512_` (`Villager`)
- `p_217513_` (`BlockPos`)

**Returns:** `private static boolean`

### canReachPos

```java
private static boolean canReachPos(PathfinderMob p_260080_, BlockPos p_259875_, PoiType p_259606_)
```

**Parameters:**

- `p_260080_` (`PathfinderMob`)
- `p_259875_` (`BlockPos`)
- `p_259606_` (`PoiType`)

**Returns:** `private static boolean`
