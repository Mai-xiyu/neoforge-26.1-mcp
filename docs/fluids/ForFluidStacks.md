# ForFluidStacks

**Package:** `net.neoforged.neoforge.fluids.crafting.display`
**Type:** interface<T> extends DisplayContentsFactory<T>

## Methods

### forStack

```java
default T forStack(Holder<Fluid> fluid)
```

**Parameters:**

- `fluid` (`Holder<Fluid>`)

**Returns:** `T`

### forStack

```java
default T forStack(Fluid fluid)
```

**Parameters:**

- `fluid` (`Fluid`)

**Returns:** `T`

### forStack

```java
T forStack(FluidStack fluid)
```

**Parameters:**

- `fluid` (`FluidStack`)

**Returns:** `T`
