# GameTestBatchFactory

**Package:** `net.minecraft.gametest.framework`
**Type:** class

## Methods

### fromTestFunction

```java
public static Collection<GameTestBatch> fromTestFunction(Collection<TestFunction> p_320519_, ServerLevel p_319999_)
```

**Parameters:**

- `p_320519_` (`Collection<TestFunction>`)
- `p_319999_` (`ServerLevel`)

**Returns:** `public static Collection<GameTestBatch>`

### toGameTestInfo

```java
public static GameTestInfo toGameTestInfo(TestFunction p_320432_, int p_320796_, ServerLevel p_320136_)
```

**Parameters:**

- `p_320432_` (`TestFunction`)
- `p_320796_` (`int`)
- `p_320136_` (`ServerLevel`)

**Returns:** `public static GameTestInfo`

### fromGameTestInfo

```java
public static GameTestRunner.GameTestBatcher fromGameTestInfo()
```

**Returns:** `public static GameTestRunner.GameTestBatcher`

### fromGameTestInfo

```java
return fromGameTestInfo()
```

**Returns:** `return`

### fromGameTestInfo

```java
public static GameTestRunner.GameTestBatcher fromGameTestInfo(int p_352220_)
```

**Parameters:**

- `p_352220_` (`int`)

**Returns:** `public static GameTestRunner.GameTestBatcher`

### toGameTestBatch

```java
public static GameTestBatch toGameTestBatch(Collection<GameTestInfo> p_352081_, String p_320417_, long p_326505_)
```

**Parameters:**

- `p_352081_` (`Collection<GameTestInfo>`)
- `p_320417_` (`String`)
- `p_326505_` (`long`)

**Returns:** `public static GameTestBatch`

### GameTestBatch

```java
return new GameTestBatch(p_320417_ + ":" + p_326505_)
```

**Parameters:**

- `p_326505_` (`p_320417_ + ":" +`)

**Returns:** `return new`
