# CartographyTableScreen

**Package:** `net.minecraft.client.gui.screens.inventory`
**Type:** class
**Extends:** `AbstractContainerScreen<CartographyTableMenu>`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### CartographyTableScreen

```java
public CartographyTableScreen(CartographyTableMenu p_98349_, Inventory p_98350_, Component p_98351_)
```

**Parameters:**

- `p_98349_` (`CartographyTableMenu`)
- `p_98350_` (`Inventory`)
- `p_98351_` (`Component`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### render

```java
public void render(GuiGraphics p_281331_, int p_281706_, int p_282996_, float p_283037_)
```

**Parameters:**

- `p_281331_` (`GuiGraphics`)
- `p_281706_` (`int`)
- `p_282996_` (`int`)
- `p_283037_` (`float`)

### renderBg

```java
protected void renderBg(GuiGraphics p_282101_, float p_282697_, int p_282380_, int p_282327_)
```

**Parameters:**

- `p_282101_` (`GuiGraphics`)
- `p_282697_` (`float`)
- `p_282380_` (`int`)
- `p_282327_` (`int`)

### renderResultingMap

```java
private void renderResultingMap(GuiGraphics p_282167_, MapId p_323466_, MapItemSavedData p_282045_, boolean p_282086_, boolean p_283531_, boolean p_282645_, boolean p_281646_)
```

**Parameters:**

- `p_282167_` (`GuiGraphics`)
- `p_323466_` (`MapId`)
- `p_282045_` (`MapItemSavedData`)
- `p_282086_` (`boolean`)
- `p_283531_` (`boolean`)
- `p_282645_` (`boolean`)
- `p_281646_` (`boolean`)

**Returns:** `private void`

### renderMap

```java
private void renderMap(GuiGraphics p_282298_, MapId p_324470_, MapItemSavedData p_282897_, int p_281632_, int p_282115_, float p_283388_)
```

**Parameters:**

- `p_282298_` (`GuiGraphics`)
- `p_324470_` (`MapId`)
- `p_282897_` (`MapItemSavedData`)
- `p_281632_` (`int`)
- `p_282115_` (`int`)
- `p_283388_` (`float`)

**Returns:** `private void`
