# JigsawBlockEditScreen

**Package:** `net.minecraft.client.gui.screens.inventory`
**Type:** class
**Extends:** `Screen`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### JigsawBlockEditScreen

```java
public JigsawBlockEditScreen(JigsawBlockEntity p_98949_)
```

**Parameters:**

- `p_98949_` (`JigsawBlockEntity`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### onDone

```java
private void onDone()
```

**Returns:** `private void`

### onCancel

```java
private void onCancel()
```

**Returns:** `private void`

### sendToServer

```java
private void sendToServer()
```

**Returns:** `private void`

### parseAsInt

```java
private int parseAsInt(String p_309106_)
```

**Parameters:**

- `p_309106_` (`String`)

**Returns:** `private int`

### sendGenerate

```java
private void sendGenerate()
```

**Returns:** `private void`

### onClose

```java
public void onClose()
```

### init

```java
protected void init()
```

### updateMessage

```java
protected void updateMessage()
```

### applyValue

```java
protected void applyValue()
```

### setInitialFocus

```java
protected void setInitialFocus()
```

### renderBackground

```java
public void renderBackground(GuiGraphics p_334016_, int p_333942_, int p_334006_, float p_333770_)
```

**Parameters:**

- `p_334016_` (`GuiGraphics`)
- `p_333942_` (`int`)
- `p_334006_` (`int`)
- `p_333770_` (`float`)

### isValidResourceLocation

```java
public static boolean isValidResourceLocation(String p_352366_)
```

**Parameters:**

- `p_352366_` (`String`)

**Returns:** `public static boolean`

### updateValidity

```java
private void updateValidity()
```

**Returns:** `private void`

### resize

```java
public void resize(Minecraft p_98960_, int p_98961_, int p_98962_)
```

**Parameters:**

- `p_98960_` (`Minecraft`)
- `p_98961_` (`int`)
- `p_98962_` (`int`)

### keyPressed

```java
public boolean keyPressed(int p_98951_, int p_98952_, int p_98953_)
```

**Parameters:**

- `p_98951_` (`int`)
- `p_98952_` (`int`)
- `p_98953_` (`int`)

**Returns:** `boolean`

### render

```java
public void render(GuiGraphics p_282514_, int p_98956_, int p_98957_, float p_98958_)
```

**Parameters:**

- `p_282514_` (`GuiGraphics`)
- `p_98956_` (`int`)
- `p_98957_` (`int`)
- `p_98958_` (`float`)
