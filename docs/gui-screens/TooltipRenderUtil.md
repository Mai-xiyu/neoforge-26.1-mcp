# TooltipRenderUtil

**Package:** `net.minecraft.client.gui.screens.inventory.tooltip`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `MOUSE_OFFSET` | `int` |  |
| `PADDING_LEFT` | `int` |  |
| `PADDING_RIGHT` | `int` |  |
| `PADDING_TOP` | `int` |  |
| `PADDING_BOTTOM` | `int` |  |

## Methods

### renderTooltipBackground

```java
public static void renderTooltipBackground(GuiGraphics p_282666_, int p_281901_, int p_281846_, int p_281559_, int p_283336_, int p_283422_)
```

**Parameters:**

- `p_282666_` (`GuiGraphics`)
- `p_281901_` (`int`)
- `p_281846_` (`int`)
- `p_281559_` (`int`)
- `p_283336_` (`int`)
- `p_283422_` (`int`)

**Returns:** `public static void`

### renderTooltipBackground

```java
 renderTooltipBackground()
```

**Returns:** ``

### renderTooltipBackground

```java
Allow specifying colors for the inner border gradient and a gradient instead of a single color for the background and outer border
    public static void renderTooltipBackground(GuiGraphics p_282666_, int p_281901_, int p_281846_, int p_281559_, int p_283336_, int p_283422_, int backgroundTop, int backgroundBottom, int borderTop, int borderBottom)
```

**Parameters:**

- `p_282666_` (`GuiGraphics`)
- `p_281901_` (`int`)
- `p_281846_` (`int`)
- `p_281559_` (`int`)
- `p_283336_` (`int`)
- `p_283422_` (`int`)
- `backgroundTop` (`int`)
- `backgroundBottom` (`int`)
- `borderTop` (`int`)
- `borderBottom` (`int`)

**Returns:** `Allow specifying colors for the inner border gradient and a gradient instead of a single color for the background and outer border
    public static void`

### renderHorizontalLine

```java
 renderHorizontalLine(j - 1)
```

**Parameters:**

- `1` (`j -`)

**Returns:** ``

### renderHorizontalLine

```java
 renderHorizontalLine(j + l)
```

**Parameters:**

- `l` (`j +`)

**Returns:** ``

### renderRectangle

```java
 renderRectangle()
```

**Returns:** ``

### renderVerticalLineGradient

```java
 renderVerticalLineGradient(i - 1)
```

**Parameters:**

- `1` (`i -`)

**Returns:** ``

### renderVerticalLineGradient

```java
 renderVerticalLineGradient(i + k)
```

**Parameters:**

- `k` (`i +`)

**Returns:** ``

### renderFrameGradient

```java
 renderFrameGradient(j + 1)
```

**Parameters:**

- `1` (`j +`)

**Returns:** ``

### renderFrameGradient

```java
private static void renderFrameGradient(GuiGraphics p_282000_, int p_282055_, int p_281580_, int p_283284_, int p_282599_, int p_283432_, int p_282907_, int p_283153_)
```

**Parameters:**

- `p_282000_` (`GuiGraphics`)
- `p_282055_` (`int`)
- `p_281580_` (`int`)
- `p_283284_` (`int`)
- `p_282599_` (`int`)
- `p_283432_` (`int`)
- `p_282907_` (`int`)
- `p_283153_` (`int`)

**Returns:** `private static void`

### renderVerticalLineGradient

```java
 renderVerticalLineGradient(p_282599_ - 2)
```

**Parameters:**

- `2` (`p_282599_ -`)

**Returns:** ``

### renderVerticalLineGradient

```java
 renderVerticalLineGradient(p_282055_ + p_283284_ - 1, p_282599_ - 2)
```

**Parameters:**

- `1` (`p_282055_ + p_283284_ -`)
- `2` (`p_282599_ -`)

**Returns:** ``

### renderHorizontalLine

```java
 renderHorizontalLine(p_281580_ - 1)
```

**Parameters:**

- `1` (`p_281580_ -`)

**Returns:** ``

### renderHorizontalLine

```java
 renderHorizontalLine(p_281580_ - 1 + p_282599_ - 1)
```

**Parameters:**

- `1` (`p_281580_ - 1 + p_282599_ -`)

**Returns:** ``

### renderVerticalLine

```java
private static void renderVerticalLine(GuiGraphics p_281270_, int p_281928_, int p_281561_, int p_283155_, int p_282552_, int p_282221_)
```

**Parameters:**

- `p_281270_` (`GuiGraphics`)
- `p_281928_` (`int`)
- `p_281561_` (`int`)
- `p_283155_` (`int`)
- `p_282552_` (`int`)
- `p_282221_` (`int`)

**Returns:** `private static void`

### renderVerticalLineGradient

```java
private static void renderVerticalLineGradient(GuiGraphics p_282478_, int p_282583_, int p_283262_, int p_283161_, int p_283322_, int p_282624_, int p_282756_)
```

**Parameters:**

- `p_282478_` (`GuiGraphics`)
- `p_282583_` (`int`)
- `p_283262_` (`int`)
- `p_283161_` (`int`)
- `p_283322_` (`int`)
- `p_282624_` (`int`)
- `p_282756_` (`int`)

**Returns:** `private static void`

### renderHorizontalLine

```java
private static void renderHorizontalLine(GuiGraphics p_282981_, int p_282028_, int p_282141_, int p_281771_, int p_282734_, int p_281979_)
```

**Parameters:**

- `p_282981_` (`GuiGraphics`)
- `p_282028_` (`int`)
- `p_282141_` (`int`)
- `p_281771_` (`int`)
- `p_282734_` (`int`)
- `p_281979_` (`int`)

**Returns:** `private static void`

### renderRectangle

```java
 renderRectangle()
```

**Returns:** ``

### renderRectangle

```java
Allow specifying a gradient instead of a single color for the background
    private static void renderRectangle(GuiGraphics p_281392_, int p_282294_, int p_283353_, int p_282640_, int p_281964_, int p_283211_, int p_282349_, int colorTo)
```

**Parameters:**

- `p_281392_` (`GuiGraphics`)
- `p_282294_` (`int`)
- `p_283353_` (`int`)
- `p_282640_` (`int`)
- `p_281964_` (`int`)
- `p_283211_` (`int`)
- `p_282349_` (`int`)
- `colorTo` (`int`)

**Returns:** `Allow specifying a gradient instead of a single color for the background
    private static void`
