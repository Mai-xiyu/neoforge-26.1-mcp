# ChatReportScreen

**Package:** `net.minecraft.client.gui.screens.reporting`
**Type:** class
**Extends:** `AbstractReportScreen<ChatReport.Builder>`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### ChatReportScreen

```java
private ChatReportScreen(Screen p_253839_, ReportingContext p_254386_, ChatReport.Builder p_299987_)
```

**Parameters:**

- `p_253839_` (`Screen`)
- `p_254386_` (`ReportingContext`)
- `p_299987_` (`ChatReport.Builder`)

**Returns:** `private`

### super

```java
 super()
```

**Returns:** ``

### ChatReportScreen

```java
public ChatReportScreen(Screen p_239116_, ReportingContext p_239117_, UUID p_239118_)
```

**Parameters:**

- `p_239116_` (`Screen`)
- `p_239117_` (`ReportingContext`)
- `p_239118_` (`UUID`)

**Returns:** `public`

### ChatReportScreen

```java
public ChatReportScreen(Screen p_254505_, ReportingContext p_254531_, ChatReport p_299964_)
```

**Parameters:**

- `p_254505_` (`Screen`)
- `p_254531_` (`ReportingContext`)
- `p_299964_` (`ChatReport`)

**Returns:** `public`

### addContent

```java
protected void addContent()
```

### onReportChanged

```java
protected void onReportChanged()
```

### mouseReleased

```java
public boolean mouseReleased(double p_239350_, double p_239351_, int p_239352_)
```

**Parameters:**

- `p_239350_` (`double`)
- `p_239351_` (`double`)
- `p_239352_` (`int`)

**Returns:** `boolean`
