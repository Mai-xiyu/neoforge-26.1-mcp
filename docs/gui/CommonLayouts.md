# CommonLayouts

**Package:** `net.minecraft.client.gui.layouts`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### CommonLayouts

```java
private CommonLayouts()
```

**Returns:** `private`

### labeledElement

```java
public static Layout labeledElement(Font p_300005_, LayoutElement p_299827_, Component p_299870_)
```

**Parameters:**

- `p_300005_` (`Font`)
- `p_299827_` (`LayoutElement`)
- `p_299870_` (`Component`)

**Returns:** `public static Layout`

### labeledElement

```java
return labeledElement(p_300036_ -> { })
```

**Parameters:**

- `}` (`p_300036_ -> {`)

**Returns:** `return`

### labeledElement

```java
public static Layout labeledElement(Font p_300013_, LayoutElement p_299865_, Component p_300008_, Consumer<LayoutSettings> p_299888_)
```

**Parameters:**

- `p_300013_` (`Font`)
- `p_299865_` (`LayoutElement`)
- `p_300008_` (`Component`)
- `p_299888_` (`Consumer<LayoutSettings>`)

**Returns:** `public static Layout`
