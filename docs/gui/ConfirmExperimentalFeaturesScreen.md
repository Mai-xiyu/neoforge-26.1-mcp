# ConfirmExperimentalFeaturesScreen

**Package:** `net.minecraft.client.gui.screens.worldselection`
**Type:** class
**Extends:** `Screen`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### ConfirmExperimentalFeaturesScreen

```java
public ConfirmExperimentalFeaturesScreen(Collection<Pack> p_252011_, BooleanConsumer p_250152_)
```

**Parameters:**

- `p_252011_` (`Collection<Pack>`)
- `p_250152_` (`BooleanConsumer`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getNarrationMessage

```java
public Component getNarrationMessage()
```

**Returns:** `Component`

### init

```java
protected void init()
```

### repositionElements

```java
protected void repositionElements()
```

### onClose

```java
public void onClose()
```

### DetailsScreen

```java
 DetailsScreen()
```

**Returns:** ``

### super

```java
 super()
```

**Returns:** ``

### init

```java
protected void init()
```

### repositionElements

```java
protected void repositionElements()
```

### onClose

```java
public void onClose()
```

### PackList

```java
public PackList(Minecraft p_249776_, Collection<Pack> p_251183_)
```

**Parameters:**

- `p_249776_` (`Minecraft`)
- `p_251183_` (`Collection<Pack>`)

**Returns:** `public`

### getRowWidth

```java
public int getRowWidth()
```

**Returns:** `int`

### PackListEntry

```java
 PackListEntry(Component p_250724_, Component p_248883_, MultiLineLabel p_250949_)
```

**Parameters:**

- `p_250724_` (`Component`)
- `p_248883_` (`Component`)
- `p_250949_` (`MultiLineLabel`)

**Returns:** ``

### render

```java
public void render(GuiGraphics p_282199_, int p_282727_, int p_283089_, int p_283116_, int p_281268_, int p_283038_, int p_283070_, int p_282448_, boolean p_281417_, float p_283226_)
```

**Parameters:**

- `p_282199_` (`GuiGraphics`)
- `p_282727_` (`int`)
- `p_283089_` (`int`)
- `p_283116_` (`int`)
- `p_281268_` (`int`)
- `p_283038_` (`int`)
- `p_283070_` (`int`)
- `p_282448_` (`int`)
- `p_281417_` (`boolean`)
- `p_283226_` (`float`)

### getNarration

```java
public Component getNarration()
```

**Returns:** `Component`
