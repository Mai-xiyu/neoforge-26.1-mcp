# ConfirmLinkScreen

**Package:** `net.minecraft.client.gui.screens`
**Type:** class
**Extends:** `ConfirmScreen`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### ConfirmLinkScreen

```java
public ConfirmLinkScreen(BooleanConsumer p_95631_, String p_95632_, boolean p_95633_)
```

**Parameters:**

- `p_95631_` (`BooleanConsumer`)
- `p_95632_` (`String`)
- `p_95633_` (`boolean`)

**Returns:** `public`

### ConfirmLinkScreen

```java
public ConfirmLinkScreen(BooleanConsumer p_238329_, Component p_238330_, String p_238331_, boolean p_238332_)
```

**Parameters:**

- `p_238329_` (`BooleanConsumer`)
- `p_238330_` (`Component`)
- `p_238331_` (`String`)
- `p_238332_` (`boolean`)

**Returns:** `public`

### ConfirmLinkScreen

```java
public ConfirmLinkScreen(BooleanConsumer p_352448_, Component p_352313_, URI p_352270_, boolean p_352104_)
```

**Parameters:**

- `p_352448_` (`BooleanConsumer`)
- `p_352313_` (`Component`)
- `p_352270_` (`URI`)
- `p_352104_` (`boolean`)

**Returns:** `public`

### ConfirmLinkScreen

```java
public ConfirmLinkScreen(BooleanConsumer p_352145_, Component p_352090_, Component p_352169_, URI p_352197_, Component p_352365_, boolean p_352117_)
```

**Parameters:**

- `p_352145_` (`BooleanConsumer`)
- `p_352090_` (`Component`)
- `p_352169_` (`Component`)
- `p_352197_` (`URI`)
- `p_352365_` (`Component`)
- `p_352117_` (`boolean`)

**Returns:** `public`

### ConfirmLinkScreen

```java
public ConfirmLinkScreen(BooleanConsumer p_240191_, Component p_240192_, Component p_240193_, String p_240194_, Component p_240195_, boolean p_240196_)
```

**Parameters:**

- `p_240191_` (`BooleanConsumer`)
- `p_240192_` (`Component`)
- `p_240193_` (`Component`)
- `p_240194_` (`String`)
- `p_240195_` (`Component`)
- `p_240196_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### confirmMessage

```java
protected static MutableComponent confirmMessage(boolean p_239180_, String p_239181_)
```

**Parameters:**

- `p_239180_` (`boolean`)
- `p_239181_` (`String`)

**Returns:** `protected static MutableComponent`

### confirmMessage

```java
protected static MutableComponent confirmMessage(boolean p_240014_)
```

**Parameters:**

- `p_240014_` (`boolean`)

**Returns:** `protected static MutableComponent`

### addButtons

```java
protected void addButtons(int p_169243_)
```

**Parameters:**

- `p_169243_` (`int`)

### copyToClipboard

```java
public void copyToClipboard()
```

**Returns:** `public void`

### render

```java
public void render(GuiGraphics p_281548_, int p_281671_, int p_283205_, float p_283628_)
```

**Parameters:**

- `p_281548_` (`GuiGraphics`)
- `p_281671_` (`int`)
- `p_283205_` (`int`)
- `p_283628_` (`float`)

### confirmLinkNow

```java
public static void confirmLinkNow(Screen p_350478_, String p_350522_, boolean p_350852_)
```

**Parameters:**

- `p_350478_` (`Screen`)
- `p_350522_` (`String`)
- `p_350852_` (`boolean`)

**Returns:** `public static void`

### confirmLinkNow

```java
public static void confirmLinkNow(Screen p_352415_, URI p_352168_, boolean p_352122_)
```

**Parameters:**

- `p_352415_` (`Screen`)
- `p_352168_` (`URI`)
- `p_352122_` (`boolean`)

**Returns:** `public static void`

### confirmLinkNow

```java
public static void confirmLinkNow(Screen p_352190_, URI p_352392_)
```

**Parameters:**

- `p_352190_` (`Screen`)
- `p_352392_` (`URI`)

**Returns:** `public static void`

### confirmLinkNow

```java
 confirmLinkNow()
```

**Returns:** ``

### confirmLinkNow

```java
public static void confirmLinkNow(Screen p_275593_, String p_275417_)
```

**Parameters:**

- `p_275593_` (`Screen`)
- `p_275417_` (`String`)

**Returns:** `public static void`

### confirmLinkNow

```java
 confirmLinkNow()
```

**Returns:** ``

### confirmLink

```java
public static Button.OnPress confirmLink(Screen p_350304_, String p_350370_, boolean p_350962_)
```

**Parameters:**

- `p_350304_` (`Screen`)
- `p_350370_` (`String`)
- `p_350962_` (`boolean`)

**Returns:** `public static Button.OnPress`

### confirmLinkNow

```java
> confirmLinkNow()
```

**Returns:** `>`

### confirmLink

```java
public static Button.OnPress confirmLink(Screen p_352068_, URI p_352436_, boolean p_352216_)
```

**Parameters:**

- `p_352068_` (`Screen`)
- `p_352436_` (`URI`)
- `p_352216_` (`boolean`)

**Returns:** `public static Button.OnPress`

### confirmLinkNow

```java
> confirmLinkNow()
```

**Returns:** `>`

### confirmLink

```java
public static Button.OnPress confirmLink(Screen p_275326_, String p_275241_)
```

**Parameters:**

- `p_275326_` (`Screen`)
- `p_275241_` (`String`)

**Returns:** `public static Button.OnPress`

### confirmLink

```java
return confirmLink()
```

**Returns:** `return`

### confirmLink

```java
public static Button.OnPress confirmLink(Screen p_352385_, URI p_352416_)
```

**Parameters:**

- `p_352385_` (`Screen`)
- `p_352416_` (`URI`)

**Returns:** `public static Button.OnPress`

### confirmLink

```java
return confirmLink()
```

**Returns:** `return`
