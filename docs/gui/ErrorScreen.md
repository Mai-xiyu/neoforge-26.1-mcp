# ErrorScreen

**Package:** `net.minecraft.client.gui.screens`
**Type:** class
**Extends:** `Screen`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### ErrorScreen

```java
public ErrorScreen(Component p_96049_, Component p_96050_)
```

**Parameters:**

- `p_96049_` (`Component`)
- `p_96050_` (`Component`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### init

```java
protected void init()
```

### render

```java
public void render(GuiGraphics p_281469_, int p_96053_, int p_96054_, float p_96055_)
```

**Parameters:**

- `p_281469_` (`GuiGraphics`)
- `p_96053_` (`int`)
- `p_96054_` (`int`)
- `p_96055_` (`float`)

### renderBackground

```java
public void renderBackground(GuiGraphics p_294505_, int p_295605_, int p_295169_, float p_296050_)
```

**Parameters:**

- `p_294505_` (`GuiGraphics`)
- `p_295605_` (`int`)
- `p_295169_` (`int`)
- `p_296050_` (`float`)

### shouldCloseOnEsc

```java
public boolean shouldCloseOnEsc()
```

**Returns:** `boolean`
