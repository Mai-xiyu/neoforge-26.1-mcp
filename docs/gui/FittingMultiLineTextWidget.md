# FittingMultiLineTextWidget

**Package:** `net.minecraft.client.gui.components`
**Type:** class
**Extends:** `AbstractScrollWidget`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### FittingMultiLineTextWidget

```java
public FittingMultiLineTextWidget(int p_289785_, int p_289777_, int p_289760_, int p_289801_, Component p_289788_, Font p_289781_)
```

**Parameters:**

- `p_289785_` (`int`)
- `p_289777_` (`int`)
- `p_289760_` (`int`)
- `p_289801_` (`int`)
- `p_289788_` (`Component`)
- `p_289781_` (`Font`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### setColor

```java
public FittingMultiLineTextWidget setColor(int p_289780_)
```

**Parameters:**

- `p_289780_` (`int`)

**Returns:** `public FittingMultiLineTextWidget`

### setWidth

```java
public void setWidth(int p_289765_)
```

**Parameters:**

- `p_289765_` (`int`)

### getInnerHeight

```java
protected int getInnerHeight()
```

**Returns:** `int`

### scrollRate

```java
protected double scrollRate()
```

**Returns:** `double`

### renderBackground

```java
protected void renderBackground(GuiGraphics p_289758_)
```

**Parameters:**

- `p_289758_` (`GuiGraphics`)

### renderWidget

```java
public void renderWidget(GuiGraphics p_289802_, int p_289778_, int p_289798_, float p_289804_)
```

**Parameters:**

- `p_289802_` (`GuiGraphics`)
- `p_289778_` (`int`)
- `p_289798_` (`int`)
- `p_289804_` (`float`)

### showingScrollBar

```java
public boolean showingScrollBar()
```

**Returns:** `public boolean`

### renderContents

```java
protected void renderContents(GuiGraphics p_289766_, int p_289790_, int p_289786_, float p_289767_)
```

**Parameters:**

- `p_289766_` (`GuiGraphics`)
- `p_289790_` (`int`)
- `p_289786_` (`int`)
- `p_289767_` (`float`)

### updateWidgetNarration

```java
protected void updateWidgetNarration(NarrationElementOutput p_289784_)
```

**Parameters:**

- `p_289784_` (`NarrationElementOutput`)
