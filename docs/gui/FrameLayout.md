# FrameLayout

**Package:** `net.minecraft.client.gui.layouts`
**Type:** class
**Extends:** `AbstractLayout`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### FrameLayout

```java
public FrameLayout()
```

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### FrameLayout

```java
public FrameLayout(int p_270073_, int p_270705_)
```

**Parameters:**

- `p_270073_` (`int`)
- `p_270705_` (`int`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### FrameLayout

```java
public FrameLayout(int p_265719_, int p_265042_, int p_265587_, int p_265682_)
```

**Parameters:**

- `p_265719_` (`int`)
- `p_265042_` (`int`)
- `p_265587_` (`int`)
- `p_265682_` (`int`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### setMinDimensions

```java
public FrameLayout setMinDimensions(int p_265169_, int p_265616_)
```

**Parameters:**

- `p_265169_` (`int`)
- `p_265616_` (`int`)

**Returns:** `public FrameLayout`

### setMinHeight

```java
public FrameLayout setMinHeight(int p_265646_)
```

**Parameters:**

- `p_265646_` (`int`)

**Returns:** `public FrameLayout`

### setMinWidth

```java
public FrameLayout setMinWidth(int p_265764_)
```

**Parameters:**

- `p_265764_` (`int`)

**Returns:** `public FrameLayout`

### newChildLayoutSettings

```java
public LayoutSettings newChildLayoutSettings()
```

**Returns:** `public LayoutSettings`

### defaultChildLayoutSetting

```java
public LayoutSettings defaultChildLayoutSetting()
```

**Returns:** `public LayoutSettings`

### arrangeElements

```java
public void arrangeElements()
```

### addChild

```java
<T extends LayoutElement> public <T extends LayoutElement> T addChild(T p_265071_)
```

**Parameters:**

- `p_265071_` (`T`)

**Returns:** `public <T extends LayoutElement> T`

### addChild

```java
<T extends LayoutElement> public <T extends LayoutElement> T addChild(T p_265386_, LayoutSettings p_265532_)
```

**Parameters:**

- `p_265386_` (`T`)
- `p_265532_` (`LayoutSettings`)

**Returns:** `public <T extends LayoutElement> T`

### addChild

```java
<T extends LayoutElement> public <T extends LayoutElement> T addChild(T p_294629_, Consumer<LayoutSettings> p_294957_)
```

**Parameters:**

- `p_294629_` (`T`)
- `p_294957_` (`Consumer<LayoutSettings>`)

**Returns:** `public <T extends LayoutElement> T`

### visitChildren

```java
public void visitChildren(Consumer<LayoutElement> p_265070_)
```

**Parameters:**

- `p_265070_` (`Consumer<LayoutElement>`)

### centerInRectangle

```java
public static void centerInRectangle(LayoutElement p_265197_, int p_265518_, int p_265334_, int p_265540_, int p_265632_)
```

**Parameters:**

- `p_265197_` (`LayoutElement`)
- `p_265518_` (`int`)
- `p_265334_` (`int`)
- `p_265540_` (`int`)
- `p_265632_` (`int`)

**Returns:** `public static void`

### alignInRectangle

```java
 alignInRectangle()
```

**Returns:** ``

### centerInRectangle

```java
public static void centerInRectangle(LayoutElement p_268229_, ScreenRectangle p_268113_)
```

**Parameters:**

- `p_268229_` (`LayoutElement`)
- `p_268113_` (`ScreenRectangle`)

**Returns:** `public static void`

### alignInRectangle

```java
public static void alignInRectangle(LayoutElement p_275320_, ScreenRectangle p_275389_, float p_275607_, float p_275662_)
```

**Parameters:**

- `p_275320_` (`LayoutElement`)
- `p_275389_` (`ScreenRectangle`)
- `p_275607_` (`float`)
- `p_275662_` (`float`)

**Returns:** `public static void`

### alignInRectangle

```java
public static void alignInRectangle(LayoutElement p_265662_, int p_265497_, int p_265030_, int p_265535_, int p_265427_, float p_265271_, float p_265365_)
```

**Parameters:**

- `p_265662_` (`LayoutElement`)
- `p_265497_` (`int`)
- `p_265030_` (`int`)
- `p_265535_` (`int`)
- `p_265427_` (`int`)
- `p_265271_` (`float`)
- `p_265365_` (`float`)

**Returns:** `public static void`

### alignInDimension

```java
public static void alignInDimension(int p_265164_, int p_265100_, int p_265351_, Consumer<Integer> p_265614_, float p_265428_)
```

**Parameters:**

- `p_265164_` (`int`)
- `p_265100_` (`int`)
- `p_265351_` (`int`)
- `p_265614_` (`Consumer<Integer>`)
- `p_265428_` (`float`)

**Returns:** `public static void`

### ChildContainer

```java
protected ChildContainer(LayoutElement p_265667_, LayoutSettings p_265430_)
```

**Parameters:**

- `p_265667_` (`LayoutElement`)
- `p_265430_` (`LayoutSettings`)

**Returns:** `protected`

### super

```java
 super()
```

**Returns:** ``
