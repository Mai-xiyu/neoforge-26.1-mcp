# Gui

**Package:** `net.minecraft.client.gui`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### Gui

```java
public Gui(Minecraft p_232355_)
```

**Parameters:**

- `p_232355_` (`Minecraft`)

**Returns:** `public`

### resetTitleTimes

```java
public void resetTitleTimes()
```

**Returns:** `public void`

### render

```java
public void render(GuiGraphics p_282884_, DeltaTracker p_348630_)
```

**Parameters:**

- `p_282884_` (`GuiGraphics`)
- `p_348630_` (`DeltaTracker`)

**Returns:** `public void`

### renderCameraOverlays

```java
private void renderCameraOverlays(GuiGraphics p_316735_, DeltaTracker p_348538_)
```

**Parameters:**

- `p_316735_` (`GuiGraphics`)
- `p_348538_` (`DeltaTracker`)

**Returns:** `private void`

### renderSleepOverlay

```java
private void renderSleepOverlay(GuiGraphics p_316466_, DeltaTracker p_348533_)
```

**Parameters:**

- `p_316466_` (`GuiGraphics`)
- `p_348533_` (`DeltaTracker`)

**Returns:** `private void`

### renderOverlayMessage

```java
private void renderOverlayMessage(GuiGraphics p_316291_, DeltaTracker p_348653_)
```

**Parameters:**

- `p_316291_` (`GuiGraphics`)
- `p_348653_` (`DeltaTracker`)

**Returns:** `private void`

### renderTitle

```java
private void renderTitle(GuiGraphics p_316629_, DeltaTracker p_348613_)
```

**Parameters:**

- `p_316629_` (`GuiGraphics`)
- `p_348613_` (`DeltaTracker`)

**Returns:** `private void`

### renderChat

```java
private void renderChat(GuiGraphics p_316307_, DeltaTracker p_348631_)
```

**Parameters:**

- `p_316307_` (`GuiGraphics`)
- `p_348631_` (`DeltaTracker`)

**Returns:** `private void`

### renderScoreboardSidebar

```java
private void renderScoreboardSidebar(GuiGraphics p_316834_, DeltaTracker p_348514_)
```

**Parameters:**

- `p_316834_` (`GuiGraphics`)
- `p_348514_` (`DeltaTracker`)

**Returns:** `private void`

### renderTabList

```java
private void renderTabList(GuiGraphics p_316182_, DeltaTracker p_348611_)
```

**Parameters:**

- `p_316182_` (`GuiGraphics`)
- `p_348611_` (`DeltaTracker`)

**Returns:** `private void`

### renderCrosshair

```java
private void renderCrosshair(GuiGraphics p_282828_, DeltaTracker p_348625_)
```

**Parameters:**

- `p_282828_` (`GuiGraphics`)
- `p_348625_` (`DeltaTracker`)

**Returns:** `private void`

### canRenderCrosshairForSpectator

```java
private boolean canRenderCrosshairForSpectator(HitResult p_93025_)
```

**Parameters:**

- `p_93025_` (`HitResult`)

**Returns:** `private boolean`

### renderEffects

```java
private void renderEffects(GuiGraphics p_282812_, DeltaTracker p_348654_)
```

**Parameters:**

- `p_282812_` (`GuiGraphics`)
- `p_348654_` (`DeltaTracker`)

**Returns:** `private void`

### renderHotbarAndDecorations ⚠️ *Deprecated*

```java
Split up into different layers
    private void renderHotbarAndDecorations(GuiGraphics p_316628_, DeltaTracker p_348543_)
```

**Parameters:**

- `p_316628_` (`GuiGraphics`)
- `p_348543_` (`DeltaTracker`)

**Returns:** `Split up into different layers
    private void`

### renderHotbar

```java
 renderHotbar()
```

**Returns:** ``

### maybeRenderJumpMeter

```java
 maybeRenderJumpMeter()
```

**Returns:** ``

### maybeRenderExperienceBar

```java
 maybeRenderExperienceBar()
```

**Returns:** ``

### maybeRenderPlayerHealth

```java
 maybeRenderPlayerHealth()
```

**Returns:** ``

### maybeRenderVehicleHealth

```java
 maybeRenderVehicleHealth()
```

**Returns:** ``

### maybeRenderSelectedItemName

```java
 maybeRenderSelectedItemName()
```

**Returns:** ``

### maybeRenderSpectatorTooltip

```java
 maybeRenderSpectatorTooltip()
```

**Returns:** ``

### renderHotbar

```java
private void renderHotbar(GuiGraphics p_316628_, DeltaTracker p_348543_)
```

**Parameters:**

- `p_316628_` (`GuiGraphics`)
- `p_348543_` (`DeltaTracker`)

**Returns:** `private void`

### maybeRenderJumpMeter

```java
private void maybeRenderJumpMeter(GuiGraphics p_316628_, DeltaTracker p_348543_)
```

**Parameters:**

- `p_316628_` (`GuiGraphics`)
- `p_348543_` (`DeltaTracker`)

**Returns:** `private void`

### maybeRenderExperienceBar

```java
private void maybeRenderExperienceBar(GuiGraphics p_316628_, DeltaTracker p_348543_)
```

**Parameters:**

- `p_316628_` (`GuiGraphics`)
- `p_348543_` (`DeltaTracker`)

**Returns:** `private void`

### maybeRenderPlayerHealth

```java
private void maybeRenderPlayerHealth(GuiGraphics p_316628_, DeltaTracker p_348543_)
```

**Parameters:**

- `p_316628_` (`GuiGraphics`)
- `p_348543_` (`DeltaTracker`)

**Returns:** `private void`

### maybeRenderVehicleHealth

```java
private void maybeRenderVehicleHealth(GuiGraphics p_316628_, DeltaTracker p_348543_)
```

**Parameters:**

- `p_316628_` (`GuiGraphics`)
- `p_348543_` (`DeltaTracker`)

**Returns:** `private void`

### maybeRenderSelectedItemName

```java
private void maybeRenderSelectedItemName(GuiGraphics p_316628_, DeltaTracker p_348543_)
```

**Parameters:**

- `p_316628_` (`GuiGraphics`)
- `p_348543_` (`DeltaTracker`)

**Returns:** `private void`

### maybeRenderSpectatorTooltip

```java
private void maybeRenderSpectatorTooltip(GuiGraphics p_316628_, DeltaTracker p_348543_)
```

**Parameters:**

- `p_316628_` (`GuiGraphics`)
- `p_348543_` (`DeltaTracker`)

**Returns:** `private void`

### renderItemHotbar

```java
private void renderItemHotbar(GuiGraphics p_316896_, DeltaTracker p_348464_)
```

**Parameters:**

- `p_316896_` (`GuiGraphics`)
- `p_348464_` (`DeltaTracker`)

**Returns:** `private void`

### renderJumpMeter

```java
private void renderJumpMeter(PlayerRideableJumping p_282774_, GuiGraphics p_282939_, int p_283351_)
```

**Parameters:**

- `p_282774_` (`PlayerRideableJumping`)
- `p_282939_` (`GuiGraphics`)
- `p_283351_` (`int`)

**Returns:** `private void`

### renderExperienceBar

```java
private void renderExperienceBar(GuiGraphics p_281906_, int p_282731_)
```

**Parameters:**

- `p_281906_` (`GuiGraphics`)
- `p_282731_` (`int`)

**Returns:** `private void`

### renderExperienceLevel

```java
private void renderExperienceLevel(GuiGraphics p_320582_, DeltaTracker p_348622_)
```

**Parameters:**

- `p_320582_` (`GuiGraphics`)
- `p_348622_` (`DeltaTracker`)

**Returns:** `private void`

### isExperienceBarVisible

```java
private boolean isExperienceBarVisible()
```

**Returns:** `private boolean`

### renderSelectedItemName

```java
private void renderSelectedItemName(GuiGraphics p_283501_)
```

**Parameters:**

- `p_283501_` (`GuiGraphics`)

**Returns:** `private void`

### renderSelectedItemName

```java
 renderSelectedItemName()
```

**Returns:** ``

### renderSelectedItemName

```java
public void renderSelectedItemName(GuiGraphics p_283501_, int yShift)
```

**Parameters:**

- `p_283501_` (`GuiGraphics`)
- `yShift` (`int`)

**Returns:** `public void`

### renderDemoOverlay

```java
private void renderDemoOverlay(GuiGraphics p_281825_, DeltaTracker p_348679_)
```

**Parameters:**

- `p_281825_` (`GuiGraphics`)
- `p_348679_` (`DeltaTracker`)

**Returns:** `private void`

### displayScoreboardSidebar

```java
private void displayScoreboardSidebar(GuiGraphics p_282008_, Objective p_283455_)
```

**Parameters:**

- `p_282008_` (`GuiGraphics`)
- `p_283455_` (`Objective`)

**Returns:** `private void`

### DisplayEntry

```java
record DisplayEntry(Component name, Component score, int scoreWidth)
```

**Parameters:**

- `name` (`Component`)
- `score` (`Component`)
- `scoreWidth` (`int`)

**Returns:** `record`

### DisplayEntry

```java
return new DisplayEntry()
```

**Returns:** `return new`

### getVehicleMaxHearts

```java
private int getVehicleMaxHearts(LivingEntity p_93023_)
```

**Parameters:**

- `p_93023_` (`LivingEntity`)

**Returns:** `private int`

### getVisibleVehicleHeartRows

```java
private int getVisibleVehicleHeartRows(int p_93013_)
```

**Parameters:**

- `p_93013_` (`int`)

**Returns:** `private int`

### renderPlayerHealth ⚠️ *Deprecated*

```java
Split up into different layers
    private void renderPlayerHealth(GuiGraphics p_283143_)
```

**Parameters:**

- `p_283143_` (`GuiGraphics`)

**Returns:** `Split up into different layers
    private void`

### renderHealthLevel

```java
 renderHealthLevel()
```

**Returns:** ``

### renderArmorLevel

```java
 renderArmorLevel()
```

**Returns:** ``

### renderFoodLevel

```java
 renderFoodLevel()
```

**Returns:** ``

### renderAirLevel

```java
 renderAirLevel()
```

**Returns:** ``

### renderHealthLevel

```java
private void renderHealthLevel(GuiGraphics p_283143_)
```

**Parameters:**

- `p_283143_` (`GuiGraphics`)

**Returns:** `private void`

### renderArmorLevel

```java
private void renderArmorLevel(GuiGraphics p_283143_)
```

**Parameters:**

- `p_283143_` (`GuiGraphics`)

**Returns:** `private void`

### renderFoodLevel

```java
private void renderFoodLevel(GuiGraphics p_283143_)
```

**Parameters:**

- `p_283143_` (`GuiGraphics`)

**Returns:** `private void`

### renderAirLevel

```java
private void renderAirLevel(GuiGraphics p_283143_)
```

**Parameters:**

- `p_283143_` (`GuiGraphics`)

**Returns:** `private void`

### renderArmor

```java
private static void renderArmor(GuiGraphics p_335393_, Player p_335672_, int p_335452_, int p_335846_, int p_335778_, int p_335859_)
```

**Parameters:**

- `p_335393_` (`GuiGraphics`)
- `p_335672_` (`Player`)
- `p_335452_` (`int`)
- `p_335846_` (`int`)
- `p_335778_` (`int`)
- `p_335859_` (`int`)

**Returns:** `private static void`

### renderHearts

```java
private void renderHearts(GuiGraphics p_282497_, Player p_168690_, int p_168691_, int p_168692_, int p_168693_, int p_168694_, float p_168695_, int p_168696_, int p_168697_, int p_168698_, boolean p_168699_)
```

**Parameters:**

- `p_282497_` (`GuiGraphics`)
- `p_168690_` (`Player`)
- `p_168691_` (`int`)
- `p_168692_` (`int`)
- `p_168693_` (`int`)
- `p_168694_` (`int`)
- `p_168695_` (`float`)
- `p_168696_` (`int`)
- `p_168697_` (`int`)
- `p_168698_` (`int`)
- `p_168699_` (`boolean`)

**Returns:** `private void`

### renderHeart

```java
private void renderHeart(GuiGraphics p_283024_, Gui.HeartType p_281393_, int p_283636_, int p_283279_, boolean p_283440_, boolean p_282496_, boolean p_294129_)
```

**Parameters:**

- `p_283024_` (`GuiGraphics`)
- `p_281393_` (`Gui.HeartType`)
- `p_283636_` (`int`)
- `p_283279_` (`int`)
- `p_283440_` (`boolean`)
- `p_282496_` (`boolean`)
- `p_294129_` (`boolean`)

**Returns:** `private void`

### renderFood

```java
private void renderFood(GuiGraphics p_335615_, Player p_336082_, int p_335399_, int p_335589_)
```

**Parameters:**

- `p_335615_` (`GuiGraphics`)
- `p_336082_` (`Player`)
- `p_335399_` (`int`)
- `p_335589_` (`int`)

**Returns:** `private void`

### renderVehicleHealth

```java
private void renderVehicleHealth(GuiGraphics p_283368_)
```

**Parameters:**

- `p_283368_` (`GuiGraphics`)

**Returns:** `private void`

### renderTextureOverlay

```java
private void renderTextureOverlay(GuiGraphics p_282304_, ResourceLocation p_281622_, float p_281504_)
```

**Parameters:**

- `p_282304_` (`GuiGraphics`)
- `p_281622_` (`ResourceLocation`)
- `p_281504_` (`float`)

**Returns:** `private void`

### renderSpyglassOverlay

```java
private void renderSpyglassOverlay(GuiGraphics p_282069_, float p_283442_)
```

**Parameters:**

- `p_282069_` (`GuiGraphics`)
- `p_283442_` (`float`)

**Returns:** `private void`

### updateVignetteBrightness

```java
private void updateVignetteBrightness(Entity p_93021_)
```

**Parameters:**

- `p_93021_` (`Entity`)

**Returns:** `private void`

### renderVignette

```java
private void renderVignette(GuiGraphics p_283063_, Entity p_283439_)
```

**Parameters:**

- `p_283063_` (`GuiGraphics`)
- `p_283439_` (`Entity`)

**Returns:** `private void`

### renderPortalOverlay

```java
private void renderPortalOverlay(GuiGraphics p_283375_, float p_283296_)
```

**Parameters:**

- `p_283375_` (`GuiGraphics`)
- `p_283296_` (`float`)

**Returns:** `private void`

### renderSlot

```java
private void renderSlot(GuiGraphics p_283283_, int p_283213_, int p_281301_, DeltaTracker p_348541_, Player p_283644_, ItemStack p_283317_, int p_283261_)
```

**Parameters:**

- `p_283283_` (`GuiGraphics`)
- `p_283213_` (`int`)
- `p_281301_` (`int`)
- `p_348541_` (`DeltaTracker`)
- `p_283644_` (`Player`)
- `p_283317_` (`ItemStack`)
- `p_283261_` (`int`)

**Returns:** `private void`

### tick

```java
public void tick(boolean p_193833_)
```

**Parameters:**

- `p_193833_` (`boolean`)

**Returns:** `public void`

### tick

```java
private void tick()
```

**Returns:** `private void`

### tickAutosaveIndicator

```java
private void tickAutosaveIndicator()
```

**Returns:** `private void`

### setNowPlaying

```java
public void setNowPlaying(Component p_93056_)
```

**Parameters:**

- `p_93056_` (`Component`)

**Returns:** `public void`

### setOverlayMessage

```java
public void setOverlayMessage(Component p_93064_, boolean p_93065_)
```

**Parameters:**

- `p_93064_` (`Component`)
- `p_93065_` (`boolean`)

**Returns:** `public void`

### setChatDisabledByPlayerShown

```java
public void setChatDisabledByPlayerShown(boolean p_238398_)
```

**Parameters:**

- `p_238398_` (`boolean`)

**Returns:** `public void`

### isShowingChatDisabledByPlayer

```java
public boolean isShowingChatDisabledByPlayer()
```

**Returns:** `public boolean`

### setTimes

```java
public void setTimes(int p_168685_, int p_168686_, int p_168687_)
```

**Parameters:**

- `p_168685_` (`int`)
- `p_168686_` (`int`)
- `p_168687_` (`int`)

**Returns:** `public void`

### setSubtitle

```java
public void setSubtitle(Component p_168712_)
```

**Parameters:**

- `p_168712_` (`Component`)

**Returns:** `public void`

### setTitle

```java
public void setTitle(Component p_168715_)
```

**Parameters:**

- `p_168715_` (`Component`)

**Returns:** `public void`

### clear

```java
public void clear()
```

**Returns:** `public void`

### getChat

```java
public ChatComponent getChat()
```

**Returns:** `public ChatComponent`

### getGuiTicks

```java
public int getGuiTicks()
```

**Returns:** `public int`

### getFont

```java
public Font getFont()
```

**Returns:** `public Font`

### getSpectatorGui

```java
public SpectatorGui getSpectatorGui()
```

**Returns:** `public SpectatorGui`

### getTabList

```java
public PlayerTabOverlay getTabList()
```

**Returns:** `public PlayerTabOverlay`

### onDisconnected

```java
public void onDisconnected()
```

**Returns:** `public void`

### getBossOverlay

```java
public BossHealthOverlay getBossOverlay()
```

**Returns:** `public BossHealthOverlay`

### getDebugOverlay

```java
public DebugScreenOverlay getDebugOverlay()
```

**Returns:** `public DebugScreenOverlay`

### clearCache

```java
public void clearCache()
```

**Returns:** `public void`

### renderSavingIndicator

```java
public void renderSavingIndicator(GuiGraphics p_282761_, DeltaTracker p_348592_)
```

**Parameters:**

- `p_282761_` (`GuiGraphics`)
- `p_348592_` (`DeltaTracker`)

**Returns:** `public void`

### initModdedOverlays

```java
.jetbrains.annotations.ApiStatus.Internal
    public void initModdedOverlays()
```

**Returns:** `.jetbrains.annotations.ApiStatus.Internal
    public void`

### getLayerCount

```java
public int getLayerCount()
```

**Returns:** `public int`

### HeartType

```java
private HeartType(ResourceLocation p_294435_, ResourceLocation p_294438_, ResourceLocation p_295036_, ResourceLocation p_295439_, ResourceLocation p_296249_, ResourceLocation p_295479_, ResourceLocation p_296219_, ResourceLocation p_296437_)
```

**Parameters:**

- `p_294435_` (`ResourceLocation`)
- `p_294438_` (`ResourceLocation`)
- `p_295036_` (`ResourceLocation`)
- `p_295439_` (`ResourceLocation`)
- `p_296249_` (`ResourceLocation`)
- `p_295479_` (`ResourceLocation`)
- `p_296219_` (`ResourceLocation`)
- `p_296437_` (`ResourceLocation`)

**Returns:** `private`

### getSprite

```java
public ResourceLocation getSprite(boolean p_295909_, boolean p_295387_, boolean p_294486_)
```

**Parameters:**

- `p_295909_` (`boolean`)
- `p_295387_` (`boolean`)
- `p_294486_` (`boolean`)

**Returns:** `public ResourceLocation`

### forPlayer

```java
static Gui.HeartType forPlayer(Player p_168733_)
```

**Parameters:**

- `p_168733_` (`Player`)

**Returns:** `static Gui.HeartType`

### getExtensionInfo

```java
public static net.neoforged.fml.common.asm.enumextension.ExtensionInfo getExtensionInfo()
```

**Returns:** `public static net.neoforged.fml.common.asm.enumextension.ExtensionInfo`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `HeartType` | enum |  |
