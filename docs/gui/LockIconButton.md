# LockIconButton

**Package:** `net.minecraft.client.gui.components`
**Type:** class
**Extends:** `Button`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### LockIconButton

```java
public LockIconButton(int p_94299_, int p_94300_, Button.OnPress p_94301_)
```

**Parameters:**

- `p_94299_` (`int`)
- `p_94300_` (`int`)
- `p_94301_` (`Button.OnPress`)

**Returns:** `public`

### createNarrationMessage

```java
protected MutableComponent createNarrationMessage()
```

**Returns:** `MutableComponent`

### isLocked

```java
public boolean isLocked()
```

**Returns:** `public boolean`

### setLocked

```java
public void setLocked(boolean p_94310_)
```

**Parameters:**

- `p_94310_` (`boolean`)

**Returns:** `public void`

### renderWidget

```java
public void renderWidget(GuiGraphics p_282701_, int p_282638_, int p_283565_, float p_282549_)
```

**Parameters:**

- `p_282701_` (`GuiGraphics`)
- `p_282638_` (`int`)
- `p_283565_` (`int`)
- `p_282549_` (`float`)

### Icon

```java
private Icon(ResourceLocation p_294269_)
```

**Parameters:**

- `p_294269_` (`ResourceLocation`)

**Returns:** `private`
