# NameReportScreen

**Package:** `net.minecraft.client.gui.screens.reporting`
**Type:** class
**Extends:** `AbstractReportScreen<NameReport.Builder>`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### NameReportScreen

```java
private NameReportScreen(Screen p_299832_, ReportingContext p_300026_, NameReport.Builder p_299866_)
```

**Parameters:**

- `p_299832_` (`Screen`)
- `p_300026_` (`ReportingContext`)
- `p_299866_` (`NameReport.Builder`)

**Returns:** `private`

### super

```java
 super()
```

**Returns:** ``

### NameReportScreen

```java
public NameReportScreen(Screen p_299947_, ReportingContext p_299966_, UUID p_299912_, String p_299935_)
```

**Parameters:**

- `p_299947_` (`Screen`)
- `p_299966_` (`ReportingContext`)
- `p_299912_` (`UUID`)
- `p_299935_` (`String`)

**Returns:** `public`

### NameReportScreen

```java
public NameReportScreen(Screen p_299861_, ReportingContext p_300015_, NameReport p_299994_)
```

**Parameters:**

- `p_299861_` (`Screen`)
- `p_300015_` (`ReportingContext`)
- `p_299994_` (`NameReport`)

**Returns:** `public`

### addContent

```java
protected void addContent()
```

### mouseReleased

```java
public boolean mouseReleased(double p_299979_, double p_299840_, int p_299849_)
```

**Parameters:**

- `p_299979_` (`double`)
- `p_299840_` (`double`)
- `p_299849_` (`int`)

**Returns:** `boolean`
