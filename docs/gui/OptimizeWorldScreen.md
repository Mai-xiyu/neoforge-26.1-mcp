# OptimizeWorldScreen

**Package:** `net.minecraft.client.gui.screens.worldselection`
**Type:** class
**Extends:** `Screen`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### create

```java
public static OptimizeWorldScreen create(Minecraft p_101316_, BooleanConsumer p_101317_, DataFixer p_101318_, LevelStorageSource.LevelStorageAccess p_101319_, boolean p_101320_)
```

**Parameters:**

- `p_101316_` (`Minecraft`)
- `p_101317_` (`BooleanConsumer`)
- `p_101318_` (`DataFixer`)
- `p_101319_` (`LevelStorageSource.LevelStorageAccess`)
- `p_101320_` (`boolean`)

**Returns:** `OptimizeWorldScreen`

### OptimizeWorldScreen

```java
private OptimizeWorldScreen(BooleanConsumer p_251295_, DataFixer p_250489_, LevelStorageSource.LevelStorageAccess p_248781_, LevelSettings p_251180_, boolean p_250358_, RegistryAccess p_323621_)
```

**Parameters:**

- `p_251295_` (`BooleanConsumer`)
- `p_250489_` (`DataFixer`)
- `p_248781_` (`LevelStorageSource.LevelStorageAccess`)
- `p_251180_` (`LevelSettings`)
- `p_250358_` (`boolean`)
- `p_323621_` (`RegistryAccess`)

**Returns:** `private`

### init

```java
protected void init()
```

### tick

```java
public void tick()
```

### onClose

```java
public void onClose()
```

### removed

```java
public void removed()
```

### render

```java
public void render(GuiGraphics p_281829_, int p_101312_, int p_101313_, float p_101314_)
```

**Parameters:**

- `p_281829_` (`GuiGraphics`)
- `p_101312_` (`int`)
- `p_101313_` (`int`)
- `p_101314_` (`float`)
