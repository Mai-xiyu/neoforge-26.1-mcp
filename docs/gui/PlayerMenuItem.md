# PlayerMenuItem

**Package:** `net.minecraft.client.gui.spectator`
**Type:** class
**Implements:** `SpectatorMenuItem`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### PlayerMenuItem

```java
public PlayerMenuItem(GameProfile p_101756_)
```

**Parameters:**

- `p_101756_` (`GameProfile`)

**Returns:** `public`

### selectItem

```java
public void selectItem(SpectatorMenu p_101762_)
```

**Parameters:**

- `p_101762_` (`SpectatorMenu`)

### getName

```java
public Component getName()
```

**Returns:** `Component`

### renderIcon

```java
public void renderIcon(GuiGraphics p_282282_, float p_282686_, int p_282849_)
```

**Parameters:**

- `p_282282_` (`GuiGraphics`)
- `p_282686_` (`float`)
- `p_282849_` (`int`)

### isEnabled

```java
public boolean isEnabled()
```

**Returns:** `boolean`
