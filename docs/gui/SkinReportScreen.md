# SkinReportScreen

**Package:** `net.minecraft.client.gui.screens.reporting`
**Type:** class
**Extends:** `AbstractReportScreen<SkinReport.Builder>`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### SkinReportScreen

```java
private SkinReportScreen(Screen p_299943_, ReportingContext p_299995_, SkinReport.Builder p_299886_)
```

**Parameters:**

- `p_299943_` (`Screen`)
- `p_299995_` (`ReportingContext`)
- `p_299886_` (`SkinReport.Builder`)

**Returns:** `private`

### super

```java
 super()
```

**Returns:** ``

### SkinReportScreen

```java
public SkinReportScreen(Screen p_299906_, ReportingContext p_299981_, UUID p_299970_, Supplier<PlayerSkin> p_299980_)
```

**Parameters:**

- `p_299906_` (`Screen`)
- `p_299981_` (`ReportingContext`)
- `p_299970_` (`UUID`)
- `p_299980_` (`Supplier<PlayerSkin>`)

**Returns:** `public`

### SkinReportScreen

```java
public SkinReportScreen(Screen p_299996_, ReportingContext p_299971_, SkinReport p_299899_)
```

**Parameters:**

- `p_299996_` (`Screen`)
- `p_299971_` (`ReportingContext`)
- `p_299899_` (`SkinReport`)

**Returns:** `public`

### addContent

```java
protected void addContent()
```

### onReportChanged

```java
protected void onReportChanged()
```

### mouseReleased

```java
public boolean mouseReleased(double p_299874_, double p_299850_, int p_299923_)
```

**Parameters:**

- `p_299874_` (`double`)
- `p_299850_` (`double`)
- `p_299923_` (`int`)

**Returns:** `boolean`
