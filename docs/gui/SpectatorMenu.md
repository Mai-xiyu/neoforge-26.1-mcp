# SpectatorMenu

**Package:** `net.minecraft.client.gui.spectator`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `EMPTY_SLOT` | `SpectatorMenuItem` |  |

## Methods

### selectItem

```java
public void selectItem(SpectatorMenu p_101812_)
```

**Parameters:**

- `p_101812_` (`SpectatorMenu`)

### getName

```java
public Component getName()
```

**Returns:** `Component`

### renderIcon

```java
public void renderIcon(GuiGraphics p_283652_, float p_101809_, int p_101810_)
```

**Parameters:**

- `p_283652_` (`GuiGraphics`)
- `p_101809_` (`float`)
- `p_101810_` (`int`)

### isEnabled

```java
public boolean isEnabled()
```

**Returns:** `boolean`

### SpectatorMenu

```java
public SpectatorMenu(SpectatorMenuListener p_101785_)
```

**Parameters:**

- `p_101785_` (`SpectatorMenuListener`)

**Returns:** `public`

### getItem

```java
public SpectatorMenuItem getItem(int p_101788_)
```

**Parameters:**

- `p_101788_` (`int`)

**Returns:** `public SpectatorMenuItem`

### getItems

```java
public List<SpectatorMenuItem> getItems()
```

**Returns:** `public List<SpectatorMenuItem>`

### getSelectedItem

```java
public SpectatorMenuItem getSelectedItem()
```

**Returns:** `public SpectatorMenuItem`

### getSelectedCategory

```java
public SpectatorMenuCategory getSelectedCategory()
```

**Returns:** `public SpectatorMenuCategory`

### selectSlot

```java
public void selectSlot(int p_101798_)
```

**Parameters:**

- `p_101798_` (`int`)

**Returns:** `public void`

### exit

```java
public void exit()
```

**Returns:** `public void`

### getSelectedSlot

```java
public int getSelectedSlot()
```

**Returns:** `public int`

### selectCategory

```java
public void selectCategory(SpectatorMenuCategory p_101795_)
```

**Parameters:**

- `p_101795_` (`SpectatorMenuCategory`)

**Returns:** `public void`

### getCurrentPage

```java
public SpectatorPage getCurrentPage()
```

**Returns:** `public SpectatorPage`

### selectItem

```java
public void selectItem(SpectatorMenu p_101823_)
```

**Parameters:**

- `p_101823_` (`SpectatorMenu`)

### getName

```java
public Component getName()
```

**Returns:** `Component`

### renderIcon

```java
public void renderIcon(GuiGraphics p_283113_, float p_282295_, int p_282511_)
```

**Parameters:**

- `p_283113_` (`GuiGraphics`)
- `p_282295_` (`float`)
- `p_282511_` (`int`)

### isEnabled

```java
public boolean isEnabled()
```

**Returns:** `boolean`

### ScrollMenuItem

```java
public ScrollMenuItem(int p_101829_, boolean p_101830_)
```

**Parameters:**

- `p_101829_` (`int`)
- `p_101830_` (`boolean`)

**Returns:** `public`

### selectItem

```java
public void selectItem(SpectatorMenu p_101836_)
```

**Parameters:**

- `p_101836_` (`SpectatorMenu`)

### getName

```java
public Component getName()
```

**Returns:** `Component`

### renderIcon

```java
public void renderIcon(GuiGraphics p_281376_, float p_282065_, int p_282653_)
```

**Parameters:**

- `p_281376_` (`GuiGraphics`)
- `p_282065_` (`float`)
- `p_282653_` (`int`)

### isEnabled

```java
public boolean isEnabled()
```

**Returns:** `boolean`
