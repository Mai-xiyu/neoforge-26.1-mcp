# TeleportToPlayerMenuCategory

**Package:** `net.minecraft.client.gui.spectator.categories`
**Type:** class
**Implements:** `SpectatorMenuCategory`, `SpectatorMenuItem`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### TeleportToPlayerMenuCategory

```java
public TeleportToPlayerMenuCategory()
```

**Returns:** `public`

### TeleportToPlayerMenuCategory

```java
public TeleportToPlayerMenuCategory(Collection<PlayerInfo> p_101861_)
```

**Parameters:**

- `p_101861_` (`Collection<PlayerInfo>`)

**Returns:** `public`

### getItems

```java
public List<SpectatorMenuItem> getItems()
```

**Returns:** `List<SpectatorMenuItem>`

### getPrompt

```java
public Component getPrompt()
```

**Returns:** `Component`

### selectItem

```java
public void selectItem(SpectatorMenu p_101868_)
```

**Parameters:**

- `p_101868_` (`SpectatorMenu`)

### getName

```java
public Component getName()
```

**Returns:** `Component`

### renderIcon

```java
public void renderIcon(GuiGraphics p_281992_, float p_281684_, int p_281889_)
```

**Parameters:**

- `p_281992_` (`GuiGraphics`)
- `p_281684_` (`float`)
- `p_281889_` (`int`)

### isEnabled

```java
public boolean isEnabled()
```

**Returns:** `boolean`
