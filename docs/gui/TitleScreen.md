# TitleScreen

**Package:** `net.minecraft.client.gui.screens`
**Type:** class
**Extends:** `Screen`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### TitleScreen

```java
public TitleScreen()
```

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### TitleScreen

```java
public TitleScreen(boolean p_96733_)
```

**Parameters:**

- `p_96733_` (`boolean`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### TitleScreen

```java
public TitleScreen(boolean p_265779_, LogoRenderer p_265067_)
```

**Parameters:**

- `p_265779_` (`boolean`)
- `p_265067_` (`LogoRenderer`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### realmsNotificationsEnabled

```java
private boolean realmsNotificationsEnabled()
```

**Returns:** `private boolean`

### tick

```java
public void tick()
```

### preloadResources

```java
public static CompletableFuture<Void> preloadResources(TextureManager p_96755_, Executor p_96756_)
```

**Parameters:**

- `p_96755_` (`TextureManager`)
- `p_96756_` (`Executor`)

**Returns:** `public static CompletableFuture<Void>`

### isPauseScreen

```java
public boolean isPauseScreen()
```

**Returns:** `boolean`

### shouldCloseOnEsc

```java
public boolean shouldCloseOnEsc()
```

**Returns:** `boolean`

### init

```java
protected void init()
```

### createNormalMenuOptions

```java
private void createNormalMenuOptions(int p_96764_, int p_96765_)
```

**Parameters:**

- `p_96764_` (`int`)
- `p_96765_` (`int`)

**Returns:** `private void`

### createDemoMenuOptions

```java
private void createDemoMenuOptions(int p_96773_, int p_96774_)
```

**Parameters:**

- `p_96773_` (`int`)
- `p_96774_` (`int`)

**Returns:** `private void`

### checkDemoWorldPresence

```java
private boolean checkDemoWorldPresence()
```

**Returns:** `private boolean`

### render

```java
public void render(GuiGraphics p_282860_, int p_281753_, int p_283539_, float p_282628_)
```

**Parameters:**

- `p_282860_` (`GuiGraphics`)
- `p_281753_` (`int`)
- `p_283539_` (`int`)
- `p_282628_` (`float`)

### fadeWidgets

```java
private void fadeWidgets(float p_341352_)
```

**Parameters:**

- `p_341352_` (`float`)

**Returns:** `private void`

### renderBackground

```java
public void renderBackground(GuiGraphics p_294355_, int p_295208_, int p_294981_, float p_294740_)
```

**Parameters:**

- `p_294355_` (`GuiGraphics`)
- `p_295208_` (`int`)
- `p_294981_` (`int`)
- `p_294740_` (`float`)

### renderPanorama

```java
protected void renderPanorama(GuiGraphics p_330491_, float p_331140_)
```

**Parameters:**

- `p_330491_` (`GuiGraphics`)
- `p_331140_` (`float`)

### mouseClicked

```java
public boolean mouseClicked(double p_96735_, double p_96736_, int p_96737_)
```

**Parameters:**

- `p_96735_` (`double`)
- `p_96736_` (`double`)
- `p_96737_` (`int`)

**Returns:** `boolean`

### removed

```java
public void removed()
```

### added

```java
public void added()
```

### confirmDemo

```java
private void confirmDemo(boolean p_96778_)
```

**Parameters:**

- `p_96778_` (`boolean`)

**Returns:** `private void`
