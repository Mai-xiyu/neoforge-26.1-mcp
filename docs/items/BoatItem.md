# BoatItem

**Package:** `net.minecraft.world.item`
**Type:** class
**Extends:** `Item`

## Methods

### BoatItem

```java
public BoatItem(boolean p_220013_, Boat.Type p_220014_, Item.Properties p_220015_)
```

**Parameters:**

- `p_220013_` (`boolean`)
- `p_220014_` (`Boat.Type`)
- `p_220015_` (`Item.Properties`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### use

```java
public InteractionResultHolder<ItemStack> use(Level p_40622_, Player p_40623_, InteractionHand p_40624_)
```

**Parameters:**

- `p_40622_` (`Level`)
- `p_40623_` (`Player`)
- `p_40624_` (`InteractionHand`)

**Returns:** `InteractionResultHolder<ItemStack>`

### getBoat

```java
private Boat getBoat(Level p_220017_, HitResult p_220018_, ItemStack p_305852_, Player p_306127_)
```

**Parameters:**

- `p_220017_` (`Level`)
- `p_220018_` (`HitResult`)
- `p_305852_` (`ItemStack`)
- `p_306127_` (`Player`)

**Returns:** `private Boat`
