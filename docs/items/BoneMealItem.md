# BoneMealItem

**Package:** `net.minecraft.world.item`
**Type:** class
**Extends:** `Item`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `GRASS_SPREAD_WIDTH` | `int` |  |
| `GRASS_SPREAD_HEIGHT` | `int` |  |
| `GRASS_COUNT_MULTIPLIER` | `int` |  |

## Methods

### BoneMealItem

```java
public BoneMealItem(Item.Properties p_40626_)
```

**Parameters:**

- `p_40626_` (`Item.Properties`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### useOn

```java
public InteractionResult useOn(UseOnContext p_40637_)
```

**Parameters:**

- `p_40637_` (`UseOnContext`)

**Returns:** `InteractionResult`

### growCrop ⚠️ *Deprecated*

```java
Hand version
    public static boolean growCrop(ItemStack p_40628_, Level p_40629_, BlockPos p_40630_)
```

**Parameters:**

- `p_40628_` (`ItemStack`)
- `p_40629_` (`Level`)
- `p_40630_` (`BlockPos`)

**Returns:** `Hand version
    public static boolean`

### applyBonemeal

```java
return applyBonemeal()
```

**Returns:** `return`

### applyBonemeal

```java
public static boolean applyBonemeal(ItemStack p_40628_, Level p_40629_, BlockPos p_40630_, net.minecraft.world.entity.player.Player player)
```

**Parameters:**

- `p_40628_` (`ItemStack`)
- `p_40629_` (`Level`)
- `p_40630_` (`BlockPos`)
- `player` (`net.minecraft.world.entity.player.Player`)

**Returns:** `public static boolean`

### growWaterPlant

```java
public static boolean growWaterPlant(ItemStack p_40632_, Level p_40633_, BlockPos p_40634_, Direction p_40635_)
```

**Parameters:**

- `p_40632_` (`ItemStack`)
- `p_40633_` (`Level`)
- `p_40634_` (`BlockPos`)
- `p_40635_` (`Direction`)

**Returns:** `public static boolean`

### addGrowthParticles

```java
public static void addGrowthParticles(LevelAccessor p_40639_, BlockPos p_40640_, int p_40641_)
```

**Parameters:**

- `p_40639_` (`LevelAccessor`)
- `p_40640_` (`BlockPos`)
- `p_40641_` (`int`)

**Returns:** `public static void`
