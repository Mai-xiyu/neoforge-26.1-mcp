# CompassItem

**Package:** `net.minecraft.world.item`
**Type:** class
**Extends:** `Item`

## Methods

### CompassItem

```java
public CompassItem(Item.Properties p_40718_)
```

**Parameters:**

- `p_40718_` (`Item.Properties`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getSpawnPosition

```java
public static GlobalPos getSpawnPosition(Level p_220020_)
```

**Parameters:**

- `p_220020_` (`Level`)

**Returns:** `GlobalPos`

### isFoil

```java
public boolean isFoil(ItemStack p_40739_)
```

**Parameters:**

- `p_40739_` (`ItemStack`)

**Returns:** `boolean`

### inventoryTick

```java
public void inventoryTick(ItemStack p_40720_, Level p_40721_, Entity p_40722_, int p_40723_, boolean p_40724_)
```

**Parameters:**

- `p_40720_` (`ItemStack`)
- `p_40721_` (`Level`)
- `p_40722_` (`Entity`)
- `p_40723_` (`int`)
- `p_40724_` (`boolean`)

### useOn

```java
public InteractionResult useOn(UseOnContext p_40726_)
```

**Parameters:**

- `p_40726_` (`UseOnContext`)

**Returns:** `InteractionResult`

### getDescriptionId

```java
public String getDescriptionId(ItemStack p_40741_)
```

**Parameters:**

- `p_40741_` (`ItemStack`)

**Returns:** `String`
