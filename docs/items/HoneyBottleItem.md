# HoneyBottleItem

**Package:** `net.minecraft.world.item`
**Type:** class
**Extends:** `Item`

## Methods

### HoneyBottleItem

```java
public HoneyBottleItem(Item.Properties p_41346_)
```

**Parameters:**

- `p_41346_` (`Item.Properties`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### finishUsingItem

```java
public ItemStack finishUsingItem(ItemStack p_41348_, Level p_41349_, LivingEntity p_41350_)
```

**Parameters:**

- `p_41348_` (`ItemStack`)
- `p_41349_` (`Level`)
- `p_41350_` (`LivingEntity`)

**Returns:** `ItemStack`

### ItemStack

```java
return new ItemStack()
```

**Returns:** `return new`

### getUseDuration

```java
public int getUseDuration(ItemStack p_41360_, LivingEntity p_345163_)
```

**Parameters:**

- `p_41360_` (`ItemStack`)
- `p_345163_` (`LivingEntity`)

**Returns:** `int`

### getUseAnimation

```java
public UseAnim getUseAnimation(ItemStack p_41358_)
```

**Parameters:**

- `p_41358_` (`ItemStack`)

**Returns:** `UseAnim`

### getDrinkingSound

```java
public SoundEvent getDrinkingSound()
```

**Returns:** `SoundEvent`

### getEatingSound

```java
public SoundEvent getEatingSound()
```

**Returns:** `SoundEvent`

### use

```java
public InteractionResultHolder<ItemStack> use(Level p_41352_, Player p_41353_, InteractionHand p_41354_)
```

**Parameters:**

- `p_41352_` (`Level`)
- `p_41353_` (`Player`)
- `p_41354_` (`InteractionHand`)

**Returns:** `InteractionResultHolder<ItemStack>`
