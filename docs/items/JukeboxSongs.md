# JukeboxSongs

**Package:** `net.minecraft.world.item`
**Type:** interface

## Methods

### create

```java
private static ResourceKey<JukeboxSong> create(String p_350505_)
```

**Parameters:**

- `p_350505_` (`String`)

**Returns:** `private static ResourceKey<JukeboxSong>`

### register

```java
private static void register(BootstrapContext<JukeboxSong> p_350719_, ResourceKey<JukeboxSong> p_350460_, Holder.Reference<SoundEvent> p_350456_, int p_350314_, int p_350919_)
```

**Parameters:**

- `p_350719_` (`BootstrapContext<JukeboxSong>`)
- `p_350460_` (`ResourceKey<JukeboxSong>`)
- `p_350456_` (`Holder.Reference<SoundEvent>`)
- `p_350314_` (`int`)
- `p_350919_` (`int`)

**Returns:** `private static void`

### bootstrap

```java
static void bootstrap(BootstrapContext<JukeboxSong> p_350269_)
```

**Parameters:**

- `p_350269_` (`BootstrapContext<JukeboxSong>`)

**Returns:** `static void`

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``
