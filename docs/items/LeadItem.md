# LeadItem

**Package:** `net.minecraft.world.item`
**Type:** class
**Extends:** `Item`

## Methods

### LeadItem

```java
public LeadItem(Item.Properties p_42828_)
```

**Parameters:**

- `p_42828_` (`Item.Properties`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### useOn

```java
public InteractionResult useOn(UseOnContext p_42834_)
```

**Parameters:**

- `p_42834_` (`UseOnContext`)

**Returns:** `InteractionResult`

### bindPlayerMobs

```java
 bindPlayerMobs()
```

**Returns:** ``

### bindPlayerMobs

```java
public static InteractionResult bindPlayerMobs(Player p_42830_, Level p_42831_, BlockPos p_42832_)
```

**Parameters:**

- `p_42830_` (`Player`)
- `p_42831_` (`Level`)
- `p_42832_` (`BlockPos`)

**Returns:** `public static InteractionResult`

### leashableInArea

```java
public static List<Leashable> leashableInArea(Level p_353033_, BlockPos p_353031_, Predicate<Leashable> p_353062_)
```

**Parameters:**

- `p_353033_` (`Level`)
- `p_353031_` (`BlockPos`)
- `p_353062_` (`Predicate<Leashable>`)

**Returns:** `public static List<Leashable>`
