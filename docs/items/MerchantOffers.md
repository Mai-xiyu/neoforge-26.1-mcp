# MerchantOffers

**Package:** `net.minecraft.world.item.trading`
**Type:** class
**Extends:** `ArrayList<MerchantOffer>`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `Codec<MerchantOffers>` |  |
| `STREAM_CODEC` | `StreamCodec<RegistryFriendlyByteBuf, MerchantOffers>` |  |

## Methods

### MerchantOffers

```java
public MerchantOffers()
```

**Returns:** `public`

### MerchantOffers

```java
private MerchantOffers(int p_220323_)
```

**Parameters:**

- `p_220323_` (`int`)

**Returns:** `private`

### super

```java
 super()
```

**Returns:** ``

### MerchantOffers

```java
private MerchantOffers(Collection<MerchantOffer> p_323676_)
```

**Parameters:**

- `p_323676_` (`Collection<MerchantOffer>`)

**Returns:** `private`

### super

```java
 super()
```

**Returns:** ``

### getRecipeFor

```java
public MerchantOffer getRecipeFor(ItemStack p_45390_, ItemStack p_45391_, int p_45392_)
```

**Parameters:**

- `p_45390_` (`ItemStack`)
- `p_45391_` (`ItemStack`)
- `p_45392_` (`int`)

**Returns:** `MerchantOffer`

### copy

```java
public MerchantOffers copy()
```

**Returns:** `public MerchantOffers`
