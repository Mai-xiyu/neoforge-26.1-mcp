# MobBucketItem

**Package:** `net.minecraft.world.item`
**Type:** class
**Extends:** `BucketItem`

## Methods

### MobBucketItem

```java
public MobBucketItem(EntityType<?> p_151137_, Fluid p_151138_, SoundEvent p_151139_, Item.Properties p_151140_)
```

**Parameters:**

- `p_151137_` (`EntityType<?>`)
- `p_151138_` (`Fluid`)
- `p_151139_` (`SoundEvent`)
- `p_151140_` (`Item.Properties`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### checkExtraContent

```java
public void checkExtraContent(Player p_151146_, Level p_151147_, ItemStack p_151148_, BlockPos p_151149_)
```

**Parameters:**

- `p_151146_` (`Player`)
- `p_151147_` (`Level`)
- `p_151148_` (`ItemStack`)
- `p_151149_` (`BlockPos`)

### playEmptySound

```java
protected void playEmptySound(Player p_151151_, LevelAccessor p_151152_, BlockPos p_151153_)
```

**Parameters:**

- `p_151151_` (`Player`)
- `p_151152_` (`LevelAccessor`)
- `p_151153_` (`BlockPos`)

### spawn

```java
private void spawn(ServerLevel p_151142_, ItemStack p_151143_, BlockPos p_151144_)
```

**Parameters:**

- `p_151142_` (`ServerLevel`)
- `p_151143_` (`ItemStack`)
- `p_151144_` (`BlockPos`)

**Returns:** `private void`

### appendHoverText

```java
public void appendHoverText(ItemStack p_151155_, Item.TooltipContext p_339675_, List<Component> p_151157_, TooltipFlag p_151158_)
```

**Parameters:**

- `p_151155_` (`ItemStack`)
- `p_339675_` (`Item.TooltipContext`)
- `p_151157_` (`List<Component>`)
- `p_151158_` (`TooltipFlag`)
