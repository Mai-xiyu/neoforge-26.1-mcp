# TrimPatterns

**Package:** `net.minecraft.world.item.armortrim`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `SENTRY` | `ResourceKey<TrimPattern>` |  |
| `DUNE` | `ResourceKey<TrimPattern>` |  |
| `COAST` | `ResourceKey<TrimPattern>` |  |
| `WILD` | `ResourceKey<TrimPattern>` |  |
| `WARD` | `ResourceKey<TrimPattern>` |  |
| `EYE` | `ResourceKey<TrimPattern>` |  |
| `VEX` | `ResourceKey<TrimPattern>` |  |
| `TIDE` | `ResourceKey<TrimPattern>` |  |
| `SNOUT` | `ResourceKey<TrimPattern>` |  |
| `RIB` | `ResourceKey<TrimPattern>` |  |
| `SPIRE` | `ResourceKey<TrimPattern>` |  |
| `WAYFINDER` | `ResourceKey<TrimPattern>` |  |
| `SHAPER` | `ResourceKey<TrimPattern>` |  |
| `SILENCE` | `ResourceKey<TrimPattern>` |  |
| `RAISER` | `ResourceKey<TrimPattern>` |  |
| `HOST` | `ResourceKey<TrimPattern>` |  |
| `FLOW` | `ResourceKey<TrimPattern>` |  |
| `BOLT` | `ResourceKey<TrimPattern>` |  |

## Methods

### bootstrap

```java
public static void bootstrap(BootstrapContext<TrimPattern> p_321567_)
```

**Parameters:**

- `p_321567_` (`BootstrapContext<TrimPattern>`)

**Returns:** `public static void`

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### register

```java
 register()
```

**Returns:** ``

### getFromTemplate

```java
public static Optional<Holder.Reference<TrimPattern>> getFromTemplate(HolderLookup.Provider p_335972_, ItemStack p_267001_)
```

**Parameters:**

- `p_335972_` (`HolderLookup.Provider`)
- `p_267001_` (`ItemStack`)

**Returns:** `public static Optional<Holder.Reference<TrimPattern>>`

### register

```java
public static void register(BootstrapContext<TrimPattern> p_321509_, Item p_267097_, ResourceKey<TrimPattern> p_267079_)
```

**Parameters:**

- `p_321509_` (`BootstrapContext<TrimPattern>`)
- `p_267097_` (`Item`)
- `p_267079_` (`ResourceKey<TrimPattern>`)

**Returns:** `public static void`

### registryKey

```java
private static ResourceKey<TrimPattern> registryKey(String p_266889_)
```

**Parameters:**

- `p_266889_` (`String`)

**Returns:** `private static ResourceKey<TrimPattern>`
