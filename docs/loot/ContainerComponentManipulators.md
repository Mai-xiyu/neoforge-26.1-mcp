# ContainerComponentManipulators

**Package:** `net.minecraft.world.level.storage.loot`
**Type:** interface

## Methods

### type

```java
public DataComponentType<ItemContainerContents> type()
```

**Returns:** `DataComponentType<ItemContainerContents>`

### getContents

```java
public Stream<ItemStack> getContents(ItemContainerContents p_340963_)
```

**Parameters:**

- `p_340963_` (`ItemContainerContents`)

**Returns:** `public Stream<ItemStack>`

### empty

```java
public ItemContainerContents empty()
```

**Returns:** `public ItemContainerContents`

### setContents

```java
public ItemContainerContents setContents(ItemContainerContents p_341159_, Stream<ItemStack> p_340907_)
```

**Parameters:**

- `p_341159_` (`ItemContainerContents`)
- `p_340907_` (`Stream<ItemStack>`)

**Returns:** `public ItemContainerContents`

### type

```java
public DataComponentType<BundleContents> type()
```

**Returns:** `DataComponentType<BundleContents>`

### empty

```java
public BundleContents empty()
```

**Returns:** `public BundleContents`

### getContents

```java
public Stream<ItemStack> getContents(BundleContents p_341255_)
```

**Parameters:**

- `p_341255_` (`BundleContents`)

**Returns:** `public Stream<ItemStack>`

### setContents

```java
public BundleContents setContents(BundleContents p_341134_, Stream<ItemStack> p_341127_)
```

**Parameters:**

- `p_341134_` (`BundleContents`)
- `p_341127_` (`Stream<ItemStack>`)

**Returns:** `public BundleContents`

### type

```java
public DataComponentType<ChargedProjectiles> type()
```

**Returns:** `DataComponentType<ChargedProjectiles>`

### empty

```java
public ChargedProjectiles empty()
```

**Returns:** `public ChargedProjectiles`

### getContents

```java
public Stream<ItemStack> getContents(ChargedProjectiles p_341086_)
```

**Parameters:**

- `p_341086_` (`ChargedProjectiles`)

**Returns:** `public Stream<ItemStack>`

### setContents

```java
public ChargedProjectiles setContents(ChargedProjectiles p_341319_, Stream<ItemStack> p_341213_)
```

**Parameters:**

- `p_341319_` (`ChargedProjectiles`)
- `p_341213_` (`Stream<ItemStack>`)

**Returns:** `public ChargedProjectiles`
