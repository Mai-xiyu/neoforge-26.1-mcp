# Axis

**Package:** `com.mojang.math`
**Type:** interface
**Annotations:** `@FunctionalInterface`

## Methods

### of

```java
static Axis of(Vector3f p_254398_)
```

**Parameters:**

- `p_254398_` (`Vector3f`)

**Returns:** `static Axis`

### rotation

```java
Quaternionf rotation(float p_254545_)
```

**Parameters:**

- `p_254545_` (`float`)

**Returns:** `Quaternionf`

### rotationDegrees

```java
default Quaternionf rotationDegrees(float p_253800_)
```

**Parameters:**

- `p_253800_` (`float`)

**Returns:** `default Quaternionf`
