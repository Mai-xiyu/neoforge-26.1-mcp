# MatrixUtil

**Package:** `com.mojang.math`
**Type:** class

## Methods

### MatrixUtil

```java
private MatrixUtil()
```

**Returns:** `private`

### mulComponentWise

```java
public static Matrix4f mulComponentWise(Matrix4f p_254173_, float p_253864_)
```

**Parameters:**

- `p_254173_` (`Matrix4f`)
- `p_253864_` (`float`)

**Returns:** `public static Matrix4f`

### approxGivensQuat

```java
private static GivensParameters approxGivensQuat(float p_276275_, float p_276276_, float p_276282_)
```

**Parameters:**

- `p_276275_` (`float`)
- `p_276276_` (`float`)
- `p_276282_` (`float`)

**Returns:** `private static GivensParameters`

### qrGivensQuat

```java
private static GivensParameters qrGivensQuat(float p_253897_, float p_254413_)
```

**Parameters:**

- `p_253897_` (`float`)
- `p_254413_` (`float`)

**Returns:** `private static GivensParameters`

### similarityTransform

```java
private static void similarityTransform(Matrix3f p_276319_, Matrix3f p_276263_)
```

**Parameters:**

- `p_276319_` (`Matrix3f`)
- `p_276263_` (`Matrix3f`)

**Returns:** `private static void`

### stepJacobi

```java
private static void stepJacobi(Matrix3f p_276262_, Matrix3f p_276279_, Quaternionf p_276314_, Quaternionf p_276299_)
```

**Parameters:**

- `p_276262_` (`Matrix3f`)
- `p_276279_` (`Matrix3f`)
- `p_276314_` (`Quaternionf`)
- `p_276299_` (`Quaternionf`)

**Returns:** `private static void`

### similarityTransform

```java
 similarityTransform()
```

**Returns:** ``

### similarityTransform

```java
 similarityTransform()
```

**Returns:** ``

### similarityTransform

```java
 similarityTransform()
```

**Returns:** ``

### eigenvalueJacobi

```java
public static Quaternionf eigenvalueJacobi(Matrix3f p_276278_, int p_276269_)
```

**Parameters:**

- `p_276278_` (`Matrix3f`)
- `p_276269_` (`int`)

**Returns:** `public static Quaternionf`

### stepJacobi

```java
 stepJacobi()
```

**Returns:** ``

### svdDecompose

```java
public static Triple<Quaternionf, Vector3f, Quaternionf> svdDecompose(Matrix3f p_253947_)
```

**Parameters:**

- `p_253947_` (`Matrix3f`)

**Returns:** `public static Triple<Quaternionf, Vector3f, Quaternionf>`

### isPureTranslation

```java
public static boolean isPureTranslation(Matrix4f p_324331_)
```

**Parameters:**

- `p_324331_` (`Matrix4f`)

**Returns:** `public static boolean`

### isOrthonormal

```java
public static boolean isOrthonormal(Matrix4f p_323931_)
```

**Parameters:**

- `p_323931_` (`Matrix4f`)

**Returns:** `public static boolean`
