# OctahedralGroup

**Package:** `com.mojang.math`
**Type:** enum
**Implements:** `StringRepresentable`

## Methods

### ROT_90_REF_Z_POS

```java
, ROT_90_REF_Z_POS()
```

**Returns:** `,`

### OctahedralGroup

```java
private OctahedralGroup(String p_56513_, SymmetricGroup3 p_56514_, boolean p_56515_, boolean p_56516_, boolean p_56517_)
```

**Parameters:**

- `p_56513_` (`String`)
- `p_56514_` (`SymmetricGroup3`)
- `p_56515_` (`boolean`)
- `p_56516_` (`boolean`)
- `p_56517_` (`boolean`)

**Returns:** `private`

### packInversions

```java
private BooleanList packInversions()
```

**Returns:** `private BooleanList`

### BooleanArrayList

```java
return new BooleanArrayList(new boolean[]{this.invertX)
```

**Parameters:**

- `boolean[]{this.invertX` (`new`)

**Returns:** `return new`

### compose

```java
public OctahedralGroup compose(OctahedralGroup p_56522_)
```

**Parameters:**

- `p_56522_` (`OctahedralGroup`)

**Returns:** `public OctahedralGroup`

### inverse

```java
public OctahedralGroup inverse()
```

**Returns:** `public OctahedralGroup`

### transformation

```java
public Matrix3f transformation()
```

**Returns:** `public Matrix3f`

### Matrix3f

```java
return new Matrix3f()
```

**Returns:** `return new`

### toString

```java
public String toString()
```

**Returns:** `String`

### getSerializedName

```java
public String getSerializedName()
```

**Returns:** `String`

### rotate

```java
public Direction rotate(Direction p_56529_)
```

**Parameters:**

- `p_56529_` (`Direction`)

**Returns:** `public Direction`

### inverts

```java
public boolean inverts(Direction.Axis p_56527_)
```

**Parameters:**

- `p_56527_` (`Direction.Axis`)

**Returns:** `public boolean`

### rotate

```java
public FrontAndTop rotate(FrontAndTop p_56531_)
```

**Parameters:**

- `p_56531_` (`FrontAndTop`)

**Returns:** `public FrontAndTop`
