# LoomMenu

**Package:** `net.minecraft.world.inventory`
**Type:** class
**Extends:** `AbstractContainerMenu`

## Methods

### setChanged

```java
public void setChanged()
```

### setChanged

```java
public void setChanged()
```

### LoomMenu

```java
public LoomMenu(int p_39856_, Inventory p_39857_)
```

**Parameters:**

- `p_39856_` (`int`)
- `p_39857_` (`Inventory`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### LoomMenu

```java
public LoomMenu(int p_39859_, Inventory p_39860_, ContainerLevelAccess p_39861_)
```

**Parameters:**

- `p_39859_` (`int`)
- `p_39860_` (`Inventory`)
- `p_39861_` (`ContainerLevelAccess`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### mayPlace

```java
public boolean mayPlace(ItemStack p_39918_)
```

**Parameters:**

- `p_39918_` (`ItemStack`)

**Returns:** `boolean`

### mayPlace

```java
public boolean mayPlace(ItemStack p_39927_)
```

**Parameters:**

- `p_39927_` (`ItemStack`)

**Returns:** `boolean`

### mayPlace

```java
public boolean mayPlace(ItemStack p_39936_)
```

**Parameters:**

- `p_39936_` (`ItemStack`)

**Returns:** `boolean`

### mayPlace

```java
public boolean mayPlace(ItemStack p_39950_)
```

**Parameters:**

- `p_39950_` (`ItemStack`)

**Returns:** `boolean`

### onTake

```java
public void onTake(Player p_150617_, ItemStack p_150618_)
```

**Parameters:**

- `p_150617_` (`Player`)
- `p_150618_` (`ItemStack`)

### stillValid

```java
public boolean stillValid(Player p_39865_)
```

**Parameters:**

- `p_39865_` (`Player`)

**Returns:** `boolean`

### stillValid

```java
return stillValid()
```

**Returns:** `return`

### clickMenuButton

```java
public boolean clickMenuButton(Player p_39867_, int p_39868_)
```

**Parameters:**

- `p_39867_` (`Player`)
- `p_39868_` (`int`)

**Returns:** `boolean`

### getSelectablePatterns

```java
private List<Holder<BannerPattern>> getSelectablePatterns(ItemStack p_219994_)
```

**Parameters:**

- `p_219994_` (`ItemStack`)

**Returns:** `private List<Holder<BannerPattern>>`

### isValidPatternIndex

```java
private boolean isValidPatternIndex(int p_242850_)
```

**Parameters:**

- `p_242850_` (`int`)

**Returns:** `private boolean`

### slotsChanged

```java
public void slotsChanged(Container p_39863_)
```

**Parameters:**

- `p_39863_` (`Container`)

### getSelectablePatterns

```java
public List<Holder<BannerPattern>> getSelectablePatterns()
```

**Returns:** `public List<Holder<BannerPattern>>`

### getSelectedBannerPatternIndex

```java
public int getSelectedBannerPatternIndex()
```

**Returns:** `public int`

### registerUpdateListener

```java
public void registerUpdateListener(Runnable p_39879_)
```

**Parameters:**

- `p_39879_` (`Runnable`)

**Returns:** `public void`

### quickMoveStack

```java
public ItemStack quickMoveStack(Player p_39883_, int p_39884_)
```

**Parameters:**

- `p_39883_` (`Player`)
- `p_39884_` (`int`)

**Returns:** `ItemStack`

### removed

```java
public void removed(Player p_39881_)
```

**Parameters:**

- `p_39881_` (`Player`)

### setupResultSlot

```java
private void setupResultSlot(Holder<BannerPattern> p_219992_)
```

**Parameters:**

- `p_219992_` (`Holder<BannerPattern>`)

**Returns:** `private void`

### getBannerSlot

```java
public Slot getBannerSlot()
```

**Returns:** `public Slot`

### getDyeSlot

```java
public Slot getDyeSlot()
```

**Returns:** `public Slot`

### getPatternSlot

```java
public Slot getPatternSlot()
```

**Returns:** `public Slot`

### getResultSlot

```java
public Slot getResultSlot()
```

**Returns:** `public Slot`
