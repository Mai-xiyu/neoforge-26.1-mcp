# PlayerEnderChestContainer

**Package:** `net.minecraft.world.inventory`
**Type:** class
**Extends:** `SimpleContainer`

## Methods

### PlayerEnderChestContainer

```java
public PlayerEnderChestContainer()
```

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### setActiveChest

```java
public void setActiveChest(EnderChestBlockEntity p_40106_)
```

**Parameters:**

- `p_40106_` (`EnderChestBlockEntity`)

**Returns:** `public void`

### isActiveChest

```java
public boolean isActiveChest(EnderChestBlockEntity p_150634_)
```

**Parameters:**

- `p_150634_` (`EnderChestBlockEntity`)

**Returns:** `public boolean`

### fromTag

```java
public void fromTag(ListTag p_40108_, HolderLookup.Provider p_330977_)
```

**Parameters:**

- `p_40108_` (`ListTag`)
- `p_330977_` (`HolderLookup.Provider`)

### createTag

```java
public ListTag createTag(HolderLookup.Provider p_330801_)
```

**Parameters:**

- `p_330801_` (`HolderLookup.Provider`)

**Returns:** `ListTag`

### stillValid

```java
public boolean stillValid(Player p_40104_)
```

**Parameters:**

- `p_40104_` (`Player`)

**Returns:** `boolean`

### startOpen

```java
public void startOpen(Player p_40112_)
```

**Parameters:**

- `p_40112_` (`Player`)

### stopOpen

```java
public void stopOpen(Player p_40110_)
```

**Parameters:**

- `p_40110_` (`Player`)
