# SlotRanges

**Package:** `net.minecraft.world.inventory`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `Codec<SlotRange>` |  |

## Methods

### addSingleSlot

```java
 addSingleSlot()
```

**Returns:** ``

### addSlotRange

```java
 addSlotRange()
```

**Returns:** ``

### addSlotRange

```java
 addSlotRange()
```

**Returns:** ``

### addSlotRange

```java
 addSlotRange()
```

**Returns:** ``

### addSlotRange

```java
 addSlotRange()
```

**Returns:** ``

### addSlotRange

```java
 addSlotRange()
```

**Returns:** ``

### addSlotRange

```java
 addSlotRange()
```

**Returns:** ``

### addSingleSlot

```java
 addSingleSlot()
```

**Returns:** ``

### addSingleSlot

```java
 addSingleSlot()
```

**Returns:** ``

### addSingleSlot

```java
 addSingleSlot()
```

**Returns:** ``

### addSlots

```java
 addSlots()
```

**Returns:** ``

### addSingleSlot

```java
 addSingleSlot()
```

**Returns:** ``

### addSingleSlot

```java
 addSingleSlot()
```

**Returns:** ``

### addSingleSlot

```java
 addSingleSlot()
```

**Returns:** ``

### addSingleSlot

```java
 addSingleSlot()
```

**Returns:** ``

### addSingleSlot

```java
 addSingleSlot()
```

**Returns:** ``

### addSlots

```java
 addSlots()
```

**Returns:** ``

### addSingleSlot

```java
 addSingleSlot()
```

**Returns:** ``

### addSingleSlot

```java
 addSingleSlot()
```

**Returns:** ``

### addSingleSlot

```java
 addSingleSlot()
```

**Returns:** ``

### addSlotRange

```java
 addSlotRange()
```

**Returns:** ``

### create

```java
private static SlotRange create(String p_332740_, int p_332819_)
```

**Parameters:**

- `p_332740_` (`String`)
- `p_332819_` (`int`)

**Returns:** `private static SlotRange`

### create

```java
private static SlotRange create(String p_332797_, IntList p_332739_)
```

**Parameters:**

- `p_332797_` (`String`)
- `p_332739_` (`IntList`)

**Returns:** `private static SlotRange`

### create

```java
private static SlotRange create(String p_332671_, int[]... p_332804_)
```

**Parameters:**

- `p_332671_` (`String`)
- `p_332804_` (`int[]...`)

**Returns:** `private static SlotRange`

### addSingleSlot

```java
private static void addSingleSlot(List<SlotRange> p_332716_, String p_332683_, int p_332766_)
```

**Parameters:**

- `p_332716_` (`List<SlotRange>`)
- `p_332683_` (`String`)
- `p_332766_` (`int`)

**Returns:** `private static void`

### addSlotRange

```java
private static void addSlotRange(List<SlotRange> p_332790_, String p_332813_, int p_332763_, int p_332721_)
```

**Parameters:**

- `p_332790_` (`List<SlotRange>`)
- `p_332813_` (`String`)
- `p_332763_` (`int`)
- `p_332721_` (`int`)

**Returns:** `private static void`

### addSlots

```java
private static void addSlots(List<SlotRange> p_332784_, String p_332702_, int[]... p_332789_)
```

**Parameters:**

- `p_332784_` (`List<SlotRange>`)
- `p_332702_` (`String`)
- `p_332789_` (`int[]...`)

**Returns:** `private static void`

### nameToIds

```java
public static SlotRange nameToIds(String p_332718_)
```

**Parameters:**

- `p_332718_` (`String`)

**Returns:** `SlotRange`

### allNames

```java
public static Stream<String> allNames()
```

**Returns:** `public static Stream<String>`

### singleSlotNames

```java
public static Stream<String> singleSlotNames()
```

**Returns:** `public static Stream<String>`
