# Optionull

**Package:** `net.minecraft`
**Type:** class

## Methods

### map

```java
public static <T, R> R map(T p_270441_, Function<T, R> p_270332_)
```

**Parameters:**

- `p_270441_` (`T`)
- `p_270332_` (`Function<T, R>`)

**Returns:** `R`

### mapOrDefault

```java
<T, R> public static <T, R> R mapOrDefault(T p_270215_, Function<T, R> p_270557_, R p_270839_)
```

**Parameters:**

- `p_270215_` (`T`)
- `p_270557_` (`Function<T, R>`)
- `p_270839_` (`R`)

**Returns:** `public static <T, R> R`

### mapOrElse

```java
<T, R> public static <T, R> R mapOrElse(T p_270820_, Function<T, R> p_270536_, Supplier<R> p_270756_)
```

**Parameters:**

- `p_270820_` (`T`)
- `p_270536_` (`Function<T, R>`)
- `p_270756_` (`Supplier<R>`)

**Returns:** `public static <T, R> R`

### first

```java
public static <T> T first(Collection<T> p_270346_)
```

**Parameters:**

- `p_270346_` (`Collection<T>`)

**Returns:** `T`

### firstOrDefault

```java
<T> public static <T> T firstOrDefault(Collection<T> p_270625_, T p_270960_)
```

**Parameters:**

- `p_270625_` (`Collection<T>`)
- `p_270960_` (`T`)

**Returns:** `public static <T> T`

### firstOrElse

```java
<T> public static <T> T firstOrElse(Collection<T> p_270529_, Supplier<T> p_270239_)
```

**Parameters:**

- `p_270529_` (`Collection<T>`)
- `p_270239_` (`Supplier<T>`)

**Returns:** `public static <T> T`

### isNullOrEmpty

```java
<T> public static <T> boolean isNullOrEmpty(T[] p_270794_)
```

**Parameters:**

- `p_270794_` (`T[]`)

**Returns:** `public static <T> boolean`

### isNullOrEmpty

```java
public static boolean isNullOrEmpty(boolean[] p_270403_)
```

**Parameters:**

- `p_270403_` (`boolean[]`)

**Returns:** `public static boolean`

### isNullOrEmpty

```java
public static boolean isNullOrEmpty(byte[] p_270775_)
```

**Parameters:**

- `p_270775_` (`byte[]`)

**Returns:** `public static boolean`

### isNullOrEmpty

```java
public static boolean isNullOrEmpty(char[] p_270512_)
```

**Parameters:**

- `p_270512_` (`char[]`)

**Returns:** `public static boolean`

### isNullOrEmpty

```java
public static boolean isNullOrEmpty(short[] p_270712_)
```

**Parameters:**

- `p_270712_` (`short[]`)

**Returns:** `public static boolean`

### isNullOrEmpty

```java
public static boolean isNullOrEmpty(int[] p_270127_)
```

**Parameters:**

- `p_270127_` (`int[]`)

**Returns:** `public static boolean`

### isNullOrEmpty

```java
public static boolean isNullOrEmpty(long[] p_270148_)
```

**Parameters:**

- `p_270148_` (`long[]`)

**Returns:** `public static boolean`

### isNullOrEmpty

```java
public static boolean isNullOrEmpty(float[] p_270428_)
```

**Parameters:**

- `p_270428_` (`float[]`)

**Returns:** `public static boolean`

### isNullOrEmpty

```java
public static boolean isNullOrEmpty(double[] p_270373_)
```

**Parameters:**

- `p_270373_` (`double[]`)

**Returns:** `public static boolean`
