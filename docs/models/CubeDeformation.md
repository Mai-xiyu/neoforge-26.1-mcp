# CubeDeformation

**Package:** `net.minecraft.client.model.geom.builders`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `NONE` | `CubeDeformation` |  |

## Methods

### CubeDeformation

```java
public CubeDeformation(float p_171466_, float p_171467_, float p_171468_)
```

**Parameters:**

- `p_171466_` (`float`)
- `p_171467_` (`float`)
- `p_171468_` (`float`)

**Returns:** `public`

### CubeDeformation

```java
public CubeDeformation(float p_171464_)
```

**Parameters:**

- `p_171464_` (`float`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### extend

```java
public CubeDeformation extend(float p_171470_)
```

**Parameters:**

- `p_171470_` (`float`)

**Returns:** `public CubeDeformation`

### CubeDeformation

```java
return new CubeDeformation(this.growX + p_171470_, this.growY + p_171470_, this.growZ + p_171470_)
```

**Parameters:**

- `p_171470_` (`this.growX +`)
- `p_171470_` (`this.growY +`)
- `p_171470_` (`this.growZ +`)

**Returns:** `return new`

### extend

```java
public CubeDeformation extend(float p_171472_, float p_171473_, float p_171474_)
```

**Parameters:**

- `p_171472_` (`float`)
- `p_171473_` (`float`)
- `p_171474_` (`float`)

**Returns:** `public CubeDeformation`

### CubeDeformation

```java
return new CubeDeformation(this.growX + p_171472_, this.growY + p_171473_, this.growZ + p_171474_)
```

**Parameters:**

- `p_171472_` (`this.growX +`)
- `p_171473_` (`this.growY +`)
- `p_171474_` (`this.growZ +`)

**Returns:** `return new`
