# LayerDefinition

**Package:** `net.minecraft.client.model.geom.builders`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### LayerDefinition

```java
private LayerDefinition(MeshDefinition p_171562_, MaterialDefinition p_171563_)
```

**Parameters:**

- `p_171562_` (`MeshDefinition`)
- `p_171563_` (`MaterialDefinition`)

**Returns:** `private`

### bakeRoot

```java
public ModelPart bakeRoot()
```

**Returns:** `public ModelPart`

### create

```java
public static LayerDefinition create(MeshDefinition p_171566_, int p_171567_, int p_171568_)
```

**Parameters:**

- `p_171566_` (`MeshDefinition`)
- `p_171567_` (`int`)
- `p_171568_` (`int`)

**Returns:** `public static LayerDefinition`
