# LayerDefinitions

**Package:** `net.minecraft.client.model.geom`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `OUTER_ARMOR_DEFORMATION` | `CubeDeformation` |  |
| `INNER_ARMOR_DEFORMATION` | `CubeDeformation` |  |

## Methods

### createRoots

```java
public static Map<ModelLayerLocation, LayerDefinition> createRoots()
```

**Returns:** `public static Map<ModelLayerLocation, LayerDefinition>`

### IllegalStateException

```java
throw new IllegalStateException("Missing layer definitions: " + list)
```

**Parameters:**

- `list` (`"Missing layer definitions: " +`)

**Returns:** `throw new`
