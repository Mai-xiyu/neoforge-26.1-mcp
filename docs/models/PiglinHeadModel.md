# PiglinHeadModel

**Package:** `net.minecraft.client.model`
**Type:** class
**Extends:** `SkullModelBase`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### PiglinHeadModel

```java
public PiglinHeadModel(ModelPart p_261926_)
```

**Parameters:**

- `p_261926_` (`ModelPart`)

**Returns:** `public`

### createHeadModel

```java
public static MeshDefinition createHeadModel()
```

**Returns:** `public static MeshDefinition`

### setupAnim

```java
public void setupAnim(float p_261561_, float p_261750_, float p_261549_)
```

**Parameters:**

- `p_261561_` (`float`)
- `p_261750_` (`float`)
- `p_261549_` (`float`)

### renderToBuffer

```java
public void renderToBuffer(PoseStack p_261809_, VertexConsumer p_261744_, int p_261711_, int p_262032_, int p_350881_)
```

**Parameters:**

- `p_261809_` (`PoseStack`)
- `p_261744_` (`VertexConsumer`)
- `p_261711_` (`int`)
- `p_262032_` (`int`)
- `p_350881_` (`int`)
