# PiglinModel

**Package:** `net.minecraft.client.model`
**Type:** class<T extends Mob> extends PlayerModel<T>
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `rightEar` | `ModelPart` |  |

## Methods

### PiglinModel

```java
public PiglinModel(ModelPart p_170810_)
```

**Parameters:**

- `p_170810_` (`ModelPart`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### createMesh

```java
public static MeshDefinition createMesh(CubeDeformation p_170812_)
```

**Parameters:**

- `p_170812_` (`CubeDeformation`)

**Returns:** `public static MeshDefinition`

### addHead

```java
 addHead()
```

**Returns:** ``

### addHead

```java
public static void addHead(CubeDeformation p_262174_, MeshDefinition p_262011_)
```

**Parameters:**

- `p_262174_` (`CubeDeformation`)
- `p_262011_` (`MeshDefinition`)

**Returns:** `public static void`

### setupAnim

```java
public void setupAnim(T p_103366_, float p_103367_, float p_103368_, float p_103369_, float p_103370_, float p_103371_)
```

**Parameters:**

- `p_103366_` (`T`)
- `p_103367_` (`float`)
- `p_103368_` (`float`)
- `p_103369_` (`float`)
- `p_103370_` (`float`)
- `p_103371_` (`float`)

**Returns:** `public void`

### setupAttackAnimation

```java
protected void setupAttackAnimation(T p_103363_, float p_103364_)
```

**Parameters:**

- `p_103363_` (`T`)
- `p_103364_` (`float`)

**Returns:** `protected void`

### holdWeaponHigh

```java
private void holdWeaponHigh(T p_103361_)
```

**Parameters:**

- `p_103361_` (`T`)

**Returns:** `private void`
