# RaftModel

**Package:** `net.minecraft.client.model`
**Type:** class
**Extends:** `ListModel<Boat>`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### RaftModel

```java
public RaftModel(ModelPart p_251383_)
```

**Parameters:**

- `p_251383_` (`ModelPart`)

**Returns:** `public`

### createPartsBuilder

```java
protected Builder<ModelPart> createPartsBuilder(ModelPart p_250773_)
```

**Parameters:**

- `p_250773_` (`ModelPart`)

**Returns:** `protected Builder<ModelPart>`

### createChildren

```java
public static void createChildren(PartDefinition p_250262_)
```

**Parameters:**

- `p_250262_` (`PartDefinition`)

**Returns:** `public static void`

### createBodyModel

```java
public static LayerDefinition createBodyModel()
```

**Returns:** `public static LayerDefinition`

### createChildren

```java
 createChildren()
```

**Returns:** ``

### setupAnim

```java
public void setupAnim(Boat p_249733_, float p_249202_, float p_252219_, float p_249366_, float p_249759_, float p_250286_)
```

**Parameters:**

- `p_249733_` (`Boat`)
- `p_249202_` (`float`)
- `p_252219_` (`float`)
- `p_249366_` (`float`)
- `p_249759_` (`float`)
- `p_250286_` (`float`)

**Returns:** `public void`

### animatePaddle

```java
 animatePaddle()
```

**Returns:** ``

### animatePaddle

```java
 animatePaddle()
```

**Returns:** ``

### parts

```java
public ImmutableList<ModelPart> parts()
```

**Returns:** `public ImmutableList<ModelPart>`

### animatePaddle

```java
private static void animatePaddle(Boat p_250792_, int p_249947_, ModelPart p_248943_, float p_251990_)
```

**Parameters:**

- `p_250792_` (`Boat`)
- `p_249947_` (`int`)
- `p_248943_` (`ModelPart`)
- `p_251990_` (`float`)

**Returns:** `private static void`
