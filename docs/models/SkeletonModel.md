# SkeletonModel

**Package:** `net.minecraft.client.model`
**Type:** class<T extends Mob & RangedAttackMob> extends HumanoidModel<T>
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### SkeletonModel

```java
public SkeletonModel(ModelPart p_170941_)
```

**Parameters:**

- `p_170941_` (`ModelPart`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### createBodyLayer

```java
public static LayerDefinition createBodyLayer()
```

**Returns:** `public static LayerDefinition`

### createDefaultSkeletonMesh

```java
 createDefaultSkeletonMesh()
```

**Returns:** ``

### createDefaultSkeletonMesh

```java
protected static void createDefaultSkeletonMesh(PartDefinition p_331700_)
```

**Parameters:**

- `p_331700_` (`PartDefinition`)

**Returns:** `protected static void`

### prepareMobModel

```java
public void prepareMobModel(T p_103793_, float p_103794_, float p_103795_, float p_103796_)
```

**Parameters:**

- `p_103793_` (`T`)
- `p_103794_` (`float`)
- `p_103795_` (`float`)
- `p_103796_` (`float`)

**Returns:** `public void`

### setupAnim

```java
public void setupAnim(T p_103798_, float p_103799_, float p_103800_, float p_103801_, float p_103802_, float p_103803_)
```

**Parameters:**

- `p_103798_` (`T`)
- `p_103799_` (`float`)
- `p_103800_` (`float`)
- `p_103801_` (`float`)
- `p_103802_` (`float`)
- `p_103803_` (`float`)

**Returns:** `public void`

### translateToHand

```java
public void translateToHand(HumanoidArm p_103778_, PoseStack p_103779_)
```

**Parameters:**

- `p_103778_` (`HumanoidArm`)
- `p_103779_` (`PoseStack`)
