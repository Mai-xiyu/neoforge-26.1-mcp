# SnifferModel

**Package:** `net.minecraft.client.model`
**Type:** class<T extends Sniffer> extends AgeableHierarchicalModel<T>
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### SnifferModel

```java
public SnifferModel(ModelPart p_272867_)
```

**Parameters:**

- `p_272867_` (`ModelPart`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### createBodyLayer

```java
public static LayerDefinition createBodyLayer()
```

**Returns:** `public static LayerDefinition`

### setupAnim

```java
public void setupAnim(T p_273213_, float p_273252_, float p_273344_, float p_272633_, float p_272774_, float p_273206_)
```

**Parameters:**

- `p_273213_` (`T`)
- `p_273252_` (`float`)
- `p_273344_` (`float`)
- `p_272633_` (`float`)
- `p_272774_` (`float`)
- `p_273206_` (`float`)

**Returns:** `public void`

### root

```java
public ModelPart root()
```

**Returns:** `ModelPart`
