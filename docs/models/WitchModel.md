# WitchModel

**Package:** `net.minecraft.client.model`
**Type:** class<T extends Entity> extends VillagerModel<T>
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### WitchModel

```java
public WitchModel(ModelPart p_171055_)
```

**Parameters:**

- `p_171055_` (`ModelPart`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### createBodyLayer

```java
public static LayerDefinition createBodyLayer()
```

**Returns:** `public static LayerDefinition`

### setupAnim

```java
public void setupAnim(T p_104067_, float p_104068_, float p_104069_, float p_104070_, float p_104071_, float p_104072_)
```

**Parameters:**

- `p_104067_` (`T`)
- `p_104068_` (`float`)
- `p_104069_` (`float`)
- `p_104070_` (`float`)
- `p_104071_` (`float`)
- `p_104072_` (`float`)

### getNose

```java
public ModelPart getNose()
```

**Returns:** `public ModelPart`

### setHoldingItem

```java
public void setHoldingItem(boolean p_104075_)
```

**Parameters:**

- `p_104075_` (`boolean`)

**Returns:** `public void`
