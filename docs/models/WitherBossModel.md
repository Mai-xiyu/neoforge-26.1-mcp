# WitherBossModel

**Package:** `net.minecraft.client.model`
**Type:** class<T extends WitherBoss> extends HierarchicalModel<T>
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### WitherBossModel

```java
public WitherBossModel(ModelPart p_171070_)
```

**Parameters:**

- `p_171070_` (`ModelPart`)

**Returns:** `public`

### createBodyLayer

```java
public static LayerDefinition createBodyLayer(CubeDeformation p_171076_)
```

**Parameters:**

- `p_171076_` (`CubeDeformation`)

**Returns:** `public static LayerDefinition`

### root

```java
public ModelPart root()
```

**Returns:** `ModelPart`

### setupAnim

```java
public void setupAnim(T p_104100_, float p_104101_, float p_104102_, float p_104103_, float p_104104_, float p_104105_)
```

**Parameters:**

- `p_104100_` (`T`)
- `p_104101_` (`float`)
- `p_104102_` (`float`)
- `p_104103_` (`float`)
- `p_104104_` (`float`)
- `p_104105_` (`float`)

**Returns:** `public void`

### prepareMobModel

```java
public void prepareMobModel(T p_104095_, float p_104096_, float p_104097_, float p_104098_)
```

**Parameters:**

- `p_104095_` (`T`)
- `p_104096_` (`float`)
- `p_104097_` (`float`)
- `p_104098_` (`float`)

**Returns:** `public void`

### setupHeadRotation

```java
 setupHeadRotation()
```

**Returns:** ``

### setupHeadRotation

```java
 setupHeadRotation()
```

**Returns:** ``

### setupHeadRotation

```java
<T extends WitherBoss> private static <T extends WitherBoss> void setupHeadRotation(T p_171072_, ModelPart p_171073_, int p_171074_)
```

**Parameters:**

- `p_171072_` (`T`)
- `p_171073_` (`ModelPart`)
- `p_171074_` (`int`)

**Returns:** `private static <T extends WitherBoss> void`
