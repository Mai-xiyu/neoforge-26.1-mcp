# WolfModel

**Package:** `net.minecraft.client.model`
**Type:** class<T extends Wolf> extends ColorableAgeableListModel<T>
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### WolfModel

```java
public WolfModel(ModelPart p_171087_)
```

**Parameters:**

- `p_171087_` (`ModelPart`)

**Returns:** `public`

### createMeshDefinition

```java
public static MeshDefinition createMeshDefinition(CubeDeformation p_316269_)
```

**Parameters:**

- `p_316269_` (`CubeDeformation`)

**Returns:** `public static MeshDefinition`

### headParts

```java
protected Iterable<ModelPart> headParts()
```

**Returns:** `Iterable<ModelPart>`

### bodyParts

```java
protected Iterable<ModelPart> bodyParts()
```

**Returns:** `Iterable<ModelPart>`

### prepareMobModel

```java
public void prepareMobModel(T p_104132_, float p_104133_, float p_104134_, float p_104135_)
```

**Parameters:**

- `p_104132_` (`T`)
- `p_104133_` (`float`)
- `p_104134_` (`float`)
- `p_104135_` (`float`)

**Returns:** `public void`

### setupAnim

```java
public void setupAnim(T p_104137_, float p_104138_, float p_104139_, float p_104140_, float p_104141_, float p_104142_)
```

**Parameters:**

- `p_104137_` (`T`)
- `p_104138_` (`float`)
- `p_104139_` (`float`)
- `p_104140_` (`float`)
- `p_104141_` (`float`)
- `p_104142_` (`float`)

**Returns:** `public void`
