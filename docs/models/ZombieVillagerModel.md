# ZombieVillagerModel

**Package:** `net.minecraft.client.model`
**Type:** class<T extends Zombie> extends HumanoidModel<T>
**Implements:** `VillagerHeadModel`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### ZombieVillagerModel

```java
public ZombieVillagerModel(ModelPart p_171092_)
```

**Parameters:**

- `p_171092_` (`ModelPart`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### createBodyLayer

```java
public static LayerDefinition createBodyLayer()
```

**Returns:** `public static LayerDefinition`

### createArmorLayer

```java
public static LayerDefinition createArmorLayer(CubeDeformation p_171094_)
```

**Parameters:**

- `p_171094_` (`CubeDeformation`)

**Returns:** `public static LayerDefinition`

### setupAnim

```java
public void setupAnim(T p_104175_, float p_104176_, float p_104177_, float p_104178_, float p_104179_, float p_104180_)
```

**Parameters:**

- `p_104175_` (`T`)
- `p_104176_` (`float`)
- `p_104177_` (`float`)
- `p_104178_` (`float`)
- `p_104179_` (`float`)
- `p_104180_` (`float`)

**Returns:** `public void`

### hatVisible

```java
public void hatVisible(boolean p_104182_)
```

**Parameters:**

- `p_104182_` (`boolean`)
