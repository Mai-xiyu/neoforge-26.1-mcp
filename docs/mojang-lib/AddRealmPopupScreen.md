# AddRealmPopupScreen

**Package:** `com.mojang.realmsclient.gui.screens`
**Type:** class
**Extends:** `RealmsScreen`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### AddRealmPopupScreen

```java
public AddRealmPopupScreen(Screen p_344774_, boolean p_345980_)
```

**Parameters:**

- `p_344774_` (`Screen`)
- `p_345980_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### updateCarouselImages

```java
public static void updateCarouselImages(ResourceManager p_346428_)
```

**Parameters:**

- `p_346428_` (`ResourceManager`)

**Returns:** `public static void`

### init

```java
protected void init()
```

### tick

```java
public void tick()
```

### render

```java
public void render(GuiGraphics p_345725_, int p_345936_, int p_345896_, float p_345975_)
```

**Parameters:**

- `p_345725_` (`GuiGraphics`)
- `p_345936_` (`int`)
- `p_345896_` (`int`)
- `p_345975_` (`float`)

### renderDiamond

```java
 renderDiamond()
```

**Returns:** ``

### renderDiamond

```java
public static void renderDiamond(GuiGraphics p_344936_, Button p_345090_)
```

**Parameters:**

- `p_344936_` (`GuiGraphics`)
- `p_345090_` (`Button`)

**Returns:** `public static void`

### renderBackground

```java
public void renderBackground(GuiGraphics p_346027_, int p_345796_, int p_345649_, float p_345603_)
```

**Parameters:**

- `p_346027_` (`GuiGraphics`)
- `p_345796_` (`int`)
- `p_345649_` (`int`)
- `p_345603_` (`float`)

### left

```java
private int left()
```

**Returns:** `private int`

### top

```java
private int top()
```

**Returns:** `private int`

### right

```java
private int right()
```

**Returns:** `private int`

### bottom

```java
private int bottom()
```

**Returns:** `private int`

### onClose

```java
public void onClose()
```
