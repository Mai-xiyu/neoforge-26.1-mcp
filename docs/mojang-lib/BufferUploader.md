# BufferUploader

**Package:** `com.mojang.blaze3d.vertex`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### reset

```java
public static void reset()
```

**Returns:** `public static void`

### invalidate

```java
 invalidate()
```

**Returns:** ``

### invalidate

```java
public static void invalidate()
```

**Returns:** `public static void`

### drawWithShader

```java
public static void drawWithShader(MeshData p_350434_)
```

**Parameters:**

- `p_350434_` (`MeshData`)

**Returns:** `public static void`

### _drawWithShader

```java
 _drawWithShader()
```

**Returns:** ``

### _drawWithShader

```java
private static void _drawWithShader(MeshData p_350373_)
```

**Parameters:**

- `p_350373_` (`MeshData`)

**Returns:** `private static void`

### draw

```java
public static void draw(MeshData p_350302_)
```

**Parameters:**

- `p_350302_` (`MeshData`)

**Returns:** `public static void`

### upload

```java
private static VertexBuffer upload(MeshData p_350970_)
```

**Parameters:**

- `p_350970_` (`MeshData`)

**Returns:** `private static VertexBuffer`

### bindImmediateBuffer

```java
private static VertexBuffer bindImmediateBuffer(VertexFormat p_231207_)
```

**Parameters:**

- `p_231207_` (`VertexFormat`)

**Returns:** `private static VertexBuffer`

### bindImmediateBuffer

```java
 bindImmediateBuffer()
```

**Returns:** ``

### bindImmediateBuffer

```java
private static void bindImmediateBuffer(VertexBuffer p_231205_)
```

**Parameters:**

- `p_231205_` (`VertexBuffer`)

**Returns:** `private static void`
