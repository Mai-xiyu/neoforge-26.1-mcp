# MacosUtil

**Package:** `com.mojang.blaze3d.platform`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### exitNativeFullscreen

```java
public static void exitNativeFullscreen(long p_182518_)
```

**Parameters:**

- `p_182518_` (`long`)

**Returns:** `public static void`

### clearResizableBit

```java
public static void clearResizableBit(long p_304542_)
```

**Parameters:**

- `p_304542_` (`long`)

**Returns:** `public static void`

### getNsWindow

```java
private static Optional<NSObject> getNsWindow(long p_182522_)
```

**Parameters:**

- `p_182522_` (`long`)

**Returns:** `private static Optional<NSObject>`

### isInNativeFullscreen

```java
private static boolean isInNativeFullscreen(NSObject p_304820_)
```

**Parameters:**

- `p_304820_` (`NSObject`)

**Returns:** `private static boolean`

### getStyleMask

```java
private static long getStyleMask(NSObject p_304421_)
```

**Parameters:**

- `p_304421_` (`NSObject`)

**Returns:** `private static long`

### toggleNativeFullscreen

```java
private static void toggleNativeFullscreen(NSObject p_182524_)
```

**Parameters:**

- `p_182524_` (`NSObject`)

**Returns:** `private static void`

### loadIcon

```java
public static void loadIcon(IoSupplier<InputStream> p_250929_)
```

**Parameters:**

- `p_250929_` (`IoSupplier<InputStream>`)

**Returns:** `public static void`
