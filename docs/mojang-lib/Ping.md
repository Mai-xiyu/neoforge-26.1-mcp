# Ping

**Package:** `com.mojang.realmsclient.client`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### ping

```java
public static List<RegionPingResult> ping(Ping.Region[]... p_87131_)
```

**Parameters:**

- `p_87131_` (`Ping.Region[]...`)

**Returns:** `public static List<RegionPingResult>`

### ping

```java
 ping()
```

**Returns:** ``

### ping

```java
private static int ping(String p_87127_)
```

**Parameters:**

- `p_87127_` (`String`)

**Returns:** `private static int`

### now

```java
private static long now()
```

**Returns:** `private static long`

### pingAllRegions

```java
public static List<RegionPingResult> pingAllRegions()
```

**Returns:** `public static List<RegionPingResult>`

### SA_EAST_1

```java
, SA_EAST_1()
```

**Returns:** `,`

### Region

```java
private Region(String p_87148_, String p_87149_)
```

**Parameters:**

- `p_87148_` (`String`)
- `p_87149_` (`String`)

**Returns:** `private`
