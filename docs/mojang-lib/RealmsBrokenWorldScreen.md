# RealmsBrokenWorldScreen

**Package:** `com.mojang.realmsclient.gui.screens`
**Type:** class
**Extends:** `RealmsScreen`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### RealmsBrokenWorldScreen

```java
public RealmsBrokenWorldScreen(Screen p_88296_, long p_88298_, boolean p_88299_)
```

**Parameters:**

- `p_88296_` (`Screen`)
- `p_88298_` (`long`)
- `p_88299_` (`boolean`)

**Returns:** `public`

### init

```java
public void init()
```

### getNarrationMessage

```java
public Component getNarrationMessage()
```

**Returns:** `Component`

### addButtons

```java
private void addButtons()
```

**Returns:** `private void`

### tick

```java
public void tick()
```

### render

```java
public void render(GuiGraphics p_282934_, int p_88317_, int p_88318_, float p_88319_)
```

**Parameters:**

- `p_282934_` (`GuiGraphics`)
- `p_88317_` (`int`)
- `p_88318_` (`int`)
- `p_88319_` (`float`)

### getFramePositionX

```java
private int getFramePositionX(int p_88302_)
```

**Parameters:**

- `p_88302_` (`int`)

**Returns:** `private int`

### fetchServerData

```java
private void fetchServerData(long p_88314_)
```

**Parameters:**

- `p_88314_` (`long`)

**Returns:** `private void`

### doSwitchOrReset

```java
public void doSwitchOrReset()
```

**Returns:** `public void`

### downloadWorld

```java
private void downloadWorld(int p_88336_)
```

**Parameters:**

- `p_88336_` (`int`)

**Returns:** `private void`

### onClose

```java
public void onClose()
```

### isMinigame

```java
private boolean isMinigame()
```

**Returns:** `private boolean`

### drawSlotFrame

```java
private void drawSlotFrame(GuiGraphics p_281929_, int p_283393_, int p_281553_, int p_283523_, int p_282823_, boolean p_283032_, String p_283498_, int p_283330_, long p_283588_, String p_282484_, boolean p_282283_)
```

**Parameters:**

- `p_281929_` (`GuiGraphics`)
- `p_283393_` (`int`)
- `p_281553_` (`int`)
- `p_283523_` (`int`)
- `p_282823_` (`int`)
- `p_283032_` (`boolean`)
- `p_283498_` (`String`)
- `p_283330_` (`int`)
- `p_283588_` (`long`)
- `p_282484_` (`String`)
- `p_282283_` (`boolean`)

**Returns:** `private void`
