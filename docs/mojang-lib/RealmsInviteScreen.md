# RealmsInviteScreen

**Package:** `com.mojang.realmsclient.gui.screens`
**Type:** class
**Extends:** `RealmsScreen`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### RealmsInviteScreen

```java
public RealmsInviteScreen(RealmsConfigureWorldScreen p_88703_, Screen p_88704_, RealmsServer p_88705_)
```

**Parameters:**

- `p_88703_` (`RealmsConfigureWorldScreen`)
- `p_88704_` (`Screen`)
- `p_88705_` (`RealmsServer`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### init

```java
public void init()
```

### repositionElements

```java
protected void repositionElements()
```

### setInitialFocus

```java
protected void setInitialFocus()
```

### onInvite

```java
private void onInvite()
```

**Returns:** `private void`

### showMessage

```java
private void showMessage(Component p_289685_)
```

**Parameters:**

- `p_289685_` (`Component`)

**Returns:** `private void`

### onClose

```java
public void onClose()
```

### render

```java
public void render(GuiGraphics p_282206_, int p_283415_, int p_282016_, float p_283011_)
```

**Parameters:**

- `p_282206_` (`GuiGraphics`)
- `p_283415_` (`int`)
- `p_282016_` (`int`)
- `p_283011_` (`float`)
