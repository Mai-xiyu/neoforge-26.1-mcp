# RealmsLongRunningMcoTaskScreen

**Package:** `com.mojang.realmsclient.gui.screens`
**Type:** class
**Extends:** `RealmsScreen`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### RealmsLongRunningMcoTaskScreen

```java
public RealmsLongRunningMcoTaskScreen(Screen p_88777_, LongRunningTask[]... p_305868_)
```

**Parameters:**

- `p_88777_` (`Screen`)
- `p_305868_` (`LongRunningTask[]...`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### IllegalArgumentException

```java
throw new IllegalArgumentException("No tasks added")
```

**Parameters:**

- `added"` (`"No tasks`)

**Returns:** `throw new`

### tick

```java
public void tick()
```

### keyPressed

```java
public boolean keyPressed(int p_88781_, int p_88782_, int p_88783_)
```

**Parameters:**

- `p_88781_` (`int`)
- `p_88782_` (`int`)
- `p_88783_` (`int`)

**Returns:** `boolean`

### init

```java
public void init()
```

### repositionElements

```java
protected void repositionElements()
```

### cancel

```java
protected void cancel()
```

**Returns:** `protected void`

### setTitle

```java
public void setTitle(Component p_88797_)
```

**Parameters:**

- `p_88797_` (`Component`)

**Returns:** `public void`
