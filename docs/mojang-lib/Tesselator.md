# Tesselator

**Package:** `com.mojang.blaze3d.vertex`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### init

```java
public static void init()
```

**Returns:** `public static void`

### IllegalStateException

```java
throw new IllegalStateException("Tesselator has already been initialized")
```

**Parameters:**

- `initialized"` (`"Tesselator has already been`)

**Returns:** `throw new`

### getInstance

```java
public static Tesselator getInstance()
```

**Returns:** `public static Tesselator`

### IllegalStateException

```java
throw new IllegalStateException("Tesselator has not been initialized")
```

**Parameters:**

- `initialized"` (`"Tesselator has not been`)

**Returns:** `throw new`

### Tesselator

```java
public Tesselator(int p_85912_)
```

**Parameters:**

- `p_85912_` (`int`)

**Returns:** `public`

### Tesselator

```java
public Tesselator()
```

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### begin

```java
public BufferBuilder begin(VertexFormat.Mode p_350571_, VertexFormat p_350837_)
```

**Parameters:**

- `p_350571_` (`VertexFormat.Mode`)
- `p_350837_` (`VertexFormat`)

**Returns:** `public BufferBuilder`

### BufferBuilder

```java
return new BufferBuilder()
```

**Returns:** `return new`

### clear

```java
public void clear()
```

**Returns:** `public void`
