# TextureUtil

**Package:** `com.mojang.blaze3d.platform`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@DontObfuscate`, `@OnlyIn(Dist.CLIENT)`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `MIN_MIPMAP_LEVEL` | `int` |  |

## Methods

### generateTextureId

```java
public static int generateTextureId()
```

**Returns:** `public static int`

### releaseTextureId

```java
public static void releaseTextureId(int p_85282_)
```

**Parameters:**

- `p_85282_` (`int`)

**Returns:** `public static void`

### prepareImage

```java
public static void prepareImage(int p_85284_, int p_85285_, int p_85286_)
```

**Parameters:**

- `p_85284_` (`int`)
- `p_85285_` (`int`)
- `p_85286_` (`int`)

**Returns:** `public static void`

### prepareImage

```java
 prepareImage()
```

**Returns:** ``

### prepareImage

```java
public static void prepareImage(NativeImage.InternalGlFormat p_85293_, int p_85294_, int p_85295_, int p_85296_)
```

**Parameters:**

- `p_85293_` (`NativeImage.InternalGlFormat`)
- `p_85294_` (`int`)
- `p_85295_` (`int`)
- `p_85296_` (`int`)

**Returns:** `public static void`

### prepareImage

```java
 prepareImage()
```

**Returns:** ``

### prepareImage

```java
public static void prepareImage(int p_85288_, int p_85289_, int p_85290_, int p_85291_)
```

**Parameters:**

- `p_85288_` (`int`)
- `p_85289_` (`int`)
- `p_85290_` (`int`)
- `p_85291_` (`int`)

**Returns:** `public static void`

### prepareImage

```java
 prepareImage()
```

**Returns:** ``

### prepareImage

```java
public static void prepareImage(NativeImage.InternalGlFormat p_85298_, int p_85299_, int p_85300_, int p_85301_, int p_85302_)
```

**Parameters:**

- `p_85298_` (`NativeImage.InternalGlFormat`)
- `p_85299_` (`int`)
- `p_85300_` (`int`)
- `p_85301_` (`int`)
- `p_85302_` (`int`)

**Returns:** `public static void`

### bind

```java
 bind()
```

**Returns:** ``

### bind

```java
private static void bind(int p_85310_)
```

**Parameters:**

- `p_85310_` (`int`)

**Returns:** `private static void`

### readResource

```java
public static ByteBuffer readResource(InputStream p_85304_)
```

**Parameters:**

- `p_85304_` (`InputStream`)

**Returns:** `public static ByteBuffer`

### readResource

```java
private static ByteBuffer readResource(ReadableByteChannel p_273208_, int p_273297_)
```

**Parameters:**

- `p_273208_` (`ReadableByteChannel`)
- `p_273297_` (`int`)

**Returns:** `private static ByteBuffer`

### writeAsPNG

```java
public static void writeAsPNG(Path p_261923_, String p_262070_, int p_261655_, int p_261576_, int p_261966_, int p_261775_)
```

**Parameters:**

- `p_261923_` (`Path`)
- `p_262070_` (`String`)
- `p_261655_` (`int`)
- `p_261576_` (`int`)
- `p_261966_` (`int`)
- `p_261775_` (`int`)

**Returns:** `public static void`

### writeAsPNG

```java
 writeAsPNG()
```

**Returns:** ``

### writeAsPNG

```java
public static void writeAsPNG(Path p_285286_, String p_285408_, int p_285400_, int p_285244_, int p_285373_, int p_285206_, IntUnaryOperator p_284988_)
```

**Parameters:**

- `p_285286_` (`Path`)
- `p_285408_` (`String`)
- `p_285400_` (`int`)
- `p_285244_` (`int`)
- `p_285373_` (`int`)
- `p_285206_` (`int`)
- `p_284988_` (`IntUnaryOperator`)

**Returns:** `public static void`

### bind

```java
 bind()
```

**Returns:** ``

### getDebugTexturePath

```java
public static Path getDebugTexturePath(Path p_262015_)
```

**Parameters:**

- `p_262015_` (`Path`)

**Returns:** `public static Path`

### getDebugTexturePath

```java
public static Path getDebugTexturePath()
```

**Returns:** `public static Path`
