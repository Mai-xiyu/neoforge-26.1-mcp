# VertexMultiConsumer

**Package:** `com.mojang.blaze3d.vertex`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### create

```java
public static VertexConsumer create()
```

**Returns:** `public static VertexConsumer`

### IllegalArgumentException

```java
throw new IllegalArgumentException()
```

**Returns:** `throw new`

### create

```java
public static VertexConsumer create(VertexConsumer p_167062_)
```

**Parameters:**

- `p_167062_` (`VertexConsumer`)

**Returns:** `public static VertexConsumer`

### create

```java
public static VertexConsumer create(VertexConsumer p_86169_, VertexConsumer p_86170_)
```

**Parameters:**

- `p_86169_` (`VertexConsumer`)
- `p_86170_` (`VertexConsumer`)

**Returns:** `public static VertexConsumer`

### create

```java
public static VertexConsumer create(VertexConsumer[]... p_167064_)
```

**Parameters:**

- `p_167064_` (`VertexConsumer[]...`)

**Returns:** `public static VertexConsumer`

### Double

```java
public Double(VertexConsumer p_86174_, VertexConsumer p_86175_)
```

**Parameters:**

- `p_86174_` (`VertexConsumer`)
- `p_86175_` (`VertexConsumer`)

**Returns:** `public`

### IllegalArgumentException

```java
throw new IllegalArgumentException("Duplicate delegates")
```

**Parameters:**

- `delegates"` (`"Duplicate`)

**Returns:** `throw new`

### addVertex

```java
public VertexConsumer addVertex(float p_350863_, float p_350291_, float p_350475_)
```

**Parameters:**

- `p_350863_` (`float`)
- `p_350291_` (`float`)
- `p_350475_` (`float`)

**Returns:** `VertexConsumer`

### setColor

```java
public VertexConsumer setColor(int p_350709_, int p_350378_, int p_350552_, int p_350453_)
```

**Parameters:**

- `p_350709_` (`int`)
- `p_350378_` (`int`)
- `p_350552_` (`int`)
- `p_350453_` (`int`)

**Returns:** `VertexConsumer`

### setUv

```java
public VertexConsumer setUv(float p_350403_, float p_350914_)
```

**Parameters:**

- `p_350403_` (`float`)
- `p_350914_` (`float`)

**Returns:** `VertexConsumer`

### setUv1

```java
public VertexConsumer setUv1(int p_350402_, int p_351027_)
```

**Parameters:**

- `p_350402_` (`int`)
- `p_351027_` (`int`)

**Returns:** `VertexConsumer`

### setUv2

```java
public VertexConsumer setUv2(int p_350512_, int p_350829_)
```

**Parameters:**

- `p_350512_` (`int`)
- `p_350829_` (`int`)

**Returns:** `VertexConsumer`

### setNormal

```java
public VertexConsumer setNormal(float p_350811_, float p_350770_, float p_350296_)
```

**Parameters:**

- `p_350811_` (`float`)
- `p_350770_` (`float`)
- `p_350296_` (`float`)

**Returns:** `VertexConsumer`

### addVertex

```java
public void addVertex(float p_350971_, float p_350483_, float p_351043_, int p_350835_, float p_350590_, float p_350299_, int p_350687_, int p_350641_, float p_350742_, float p_350591_, float p_350324_)
```

**Parameters:**

- `p_350971_` (`float`)
- `p_350483_` (`float`)
- `p_351043_` (`float`)
- `p_350835_` (`int`)
- `p_350590_` (`float`)
- `p_350299_` (`float`)
- `p_350687_` (`int`)
- `p_350641_` (`int`)
- `p_350742_` (`float`)
- `p_350591_` (`float`)
- `p_350324_` (`float`)

### Multiple

```java
 Multiple(VertexConsumer[] delegates)
```

**Parameters:**

- `delegates` (`VertexConsumer[]`)

**Returns:** ``

### IllegalArgumentException

```java
throw new IllegalArgumentException("Duplicate delegates")
```

**Parameters:**

- `delegates"` (`"Duplicate`)

**Returns:** `throw new`

### forEach

```java
private void forEach(Consumer<VertexConsumer> p_167145_)
```

**Parameters:**

- `p_167145_` (`Consumer<VertexConsumer>`)

**Returns:** `private void`

### addVertex

```java
public VertexConsumer addVertex(float p_350626_, float p_351046_, float p_351003_)
```

**Parameters:**

- `p_350626_` (`float`)
- `p_351046_` (`float`)
- `p_351003_` (`float`)

**Returns:** `VertexConsumer`

### setColor

```java
public VertexConsumer setColor(int p_167130_, int p_167131_, int p_167132_, int p_167133_)
```

**Parameters:**

- `p_167130_` (`int`)
- `p_167131_` (`int`)
- `p_167132_` (`int`)
- `p_167133_` (`int`)

**Returns:** `VertexConsumer`

### setUv

```java
public VertexConsumer setUv(float p_167084_, float p_167085_)
```

**Parameters:**

- `p_167084_` (`float`)
- `p_167085_` (`float`)

**Returns:** `VertexConsumer`

### setUv1

```java
public VertexConsumer setUv1(int p_350622_, int p_350367_)
```

**Parameters:**

- `p_350622_` (`int`)
- `p_350367_` (`int`)

**Returns:** `VertexConsumer`

### setUv2

```java
public VertexConsumer setUv2(int p_350498_, int p_350436_)
```

**Parameters:**

- `p_350498_` (`int`)
- `p_350436_` (`int`)

**Returns:** `VertexConsumer`

### setNormal

```java
public VertexConsumer setNormal(float p_167147_, float p_167148_, float p_167149_)
```

**Parameters:**

- `p_167147_` (`float`)
- `p_167148_` (`float`)
- `p_167149_` (`float`)

**Returns:** `VertexConsumer`

### addVertex

```java
public void addVertex(float p_350950_, float p_350670_, float p_350366_, int p_350619_, float p_350871_, float p_350850_, int p_350499_, int p_350318_, float p_350404_, float p_350988_, float p_350991_)
```

**Parameters:**

- `p_350950_` (`float`)
- `p_350670_` (`float`)
- `p_350366_` (`float`)
- `p_350619_` (`int`)
- `p_350871_` (`float`)
- `p_350850_` (`float`)
- `p_350499_` (`int`)
- `p_350318_` (`int`)
- `p_350404_` (`float`)
- `p_350988_` (`float`)
- `p_350991_` (`float`)
