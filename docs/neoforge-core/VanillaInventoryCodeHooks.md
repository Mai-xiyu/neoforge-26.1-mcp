# VanillaInventoryCodeHooks

**Package:** `net.neoforged.neoforge.transfer.item`
**Type:** class
**Annotations:** `@ApiStatus`

## Methods

### extractHook

```java
public static boolean extractHook(Hopper dest, ResourceHandler<ItemResource> handler)
```

**Parameters:**

- `dest` (`Hopper`)
- `handler` (`ResourceHandler<ItemResource>`)

**Returns:** `boolean`

### insertHook

```java
public static boolean insertHook(HopperBlockEntity hopper, ResourceHandler<ItemResource> itemHandler)
```

**Parameters:**

- `hopper` (`HopperBlockEntity`)
- `itemHandler` (`ResourceHandler<ItemResource>`)

**Returns:** `boolean`

### getEntityContainerOrHandler

```java
public static ContainerOrHandler getEntityContainerOrHandler(Level level, double x, double y, double z, Direction side)
```

**Parameters:**

- `level` (`Level`)
- `x` (`double`)
- `y` (`double`)
- `z` (`double`)
- `side` (`Direction`)

**Returns:** `public static ContainerOrHandler`

### ContainerOrHandler

```java
return new ContainerOrHandler()
```

**Returns:** `return new`

### ContainerOrHandler

```java
t in the entity predicate above.
                return new ContainerOrHandler()
```

**Returns:** `t in the entity predicate above.
                return new`
