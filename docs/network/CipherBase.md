# CipherBase

**Package:** `net.minecraft.network`
**Type:** class

## Methods

### CipherBase

```java
protected CipherBase(Cipher p_129403_)
```

**Parameters:**

- `p_129403_` (`Cipher`)

**Returns:** `protected`

### bufToByte

```java
private byte[] bufToByte(ByteBuf p_129405_)
```

**Parameters:**

- `p_129405_` (`ByteBuf`)

**Returns:** `private byte[]`

### decipher

```java
protected ByteBuf decipher(ChannelHandlerContext p_129410_, ByteBuf p_129411_)
```

**Parameters:**

- `p_129410_` (`ChannelHandlerContext`)
- `p_129411_` (`ByteBuf`)

**Returns:** `protected ByteBuf`

### encipher

```java
protected void encipher(ByteBuf p_129407_, ByteBuf p_129408_)
```

**Parameters:**

- `p_129407_` (`ByteBuf`)
- `p_129408_` (`ByteBuf`)

**Returns:** `protected void`
