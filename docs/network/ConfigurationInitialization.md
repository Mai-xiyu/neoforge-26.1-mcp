# ConfigurationInitialization

**Package:** `net.neoforged.neoforge.network`
**Type:** class
**Annotations:** `@EventBusSubscriber(modid = "neoforge", bus = EventBusSubscriber.Bus.MOD)`, `@ApiStatus`

## Methods

### configureEarlyTasks

```java
public static void configureEarlyTasks(ServerConfigurationPacketListener listener, Consumer<ConfigurationTask> tasks)
```

**Parameters:**

- `listener` (`ServerConfigurationPacketListener`)
- `tasks` (`Consumer<ConfigurationTask>`)
