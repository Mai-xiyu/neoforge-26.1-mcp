# FixedFormat

**Package:** `net.minecraft.network.chat.numbers`
**Type:** class
**Implements:** `NumberFormat`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `TYPE` | `NumberFormatType<FixedFormat>` |  |

## Methods

### mapCodec

```java
public MapCodec<FixedFormat> mapCodec()
```

**Returns:** `MapCodec<FixedFormat>`

### streamCodec

```java
public StreamCodec<RegistryFriendlyByteBuf, FixedFormat> streamCodec()
```

**Returns:** `StreamCodec<RegistryFriendlyByteBuf, FixedFormat>`

### FixedFormat

```java
public FixedFormat(Component p_313808_)
```

**Parameters:**

- `p_313808_` (`Component`)

**Returns:** `public`

### format

```java
public MutableComponent format(int p_313721_)
```

**Parameters:**

- `p_313721_` (`int`)

**Returns:** `MutableComponent`

### type

```java
public NumberFormatType<FixedFormat> type()
```

**Returns:** `NumberFormatType<FixedFormat>`
