# HandlerThread

**Package:** `net.neoforged.neoforge.network.registration`
**Type:** enum

## Description

Used by `PayloadRegistrar` to declare the default handling thread for registered `IPayloadHandler`s.
