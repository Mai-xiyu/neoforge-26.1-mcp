# RegistryFriendlyByteBuf

**Package:** `net.minecraft.network`
**Type:** class
**Extends:** `FriendlyByteBuf`

## Methods

### RegistryFriendlyByteBuf

```java
public public RegistryFriendlyByteBuf(ByteBuf p_320951_, RegistryAccess p_319803_)
```

**Parameters:**

- `p_320951_` (`ByteBuf`)
- `p_319803_` (`RegistryAccess`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### RegistryFriendlyByteBuf

```java
public RegistryFriendlyByteBuf(ByteBuf p_320951_, RegistryAccess p_319803_, net.neoforged.neoforge.network.connection.ConnectionType connectionType)
```

**Parameters:**

- `p_320951_` (`ByteBuf`)
- `p_319803_` (`RegistryAccess`)
- `connectionType` (`net.neoforged.neoforge.network.connection.ConnectionType`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getConnectionType

```java
public net.neoforged.neoforge.network.connection.ConnectionType getConnectionType()
```

**Returns:** `public net.neoforged.neoforge.network.connection.ConnectionType`

### registryAccess

```java
public RegistryAccess registryAccess()
```

**Returns:** `public RegistryAccess`

### decorator

```java
public static Function<ByteBuf, RegistryFriendlyByteBuf> decorator(RegistryAccess p_320166_, net.neoforged.neoforge.network.connection.ConnectionType connectionType)
```

**Parameters:**

- `p_320166_` (`RegistryAccess`)
- `connectionType` (`net.neoforged.neoforge.network.connection.ConnectionType`)

**Returns:** `public static Function<ByteBuf, RegistryFriendlyByteBuf>`

### RegistryFriendlyByteBuf

```java
> new RegistryFriendlyByteBuf()
```

**Returns:** `> new`

### decorator

```java
public static Function<ByteBuf, RegistryFriendlyByteBuf> decorator(RegistryAccess p_320166_)
```

**Parameters:**

- `p_320166_` (`RegistryAccess`)

**Returns:** `Function<ByteBuf, RegistryFriendlyByteBuf>`

### RegistryFriendlyByteBuf

```java
> new RegistryFriendlyByteBuf()
```

**Returns:** `> new`
