# Utf8String

**Package:** `net.minecraft.network`
**Type:** class

## Methods

### read

```java
public static String read(ByteBuf p_295677_, int p_295190_)
```

**Parameters:**

- `p_295677_` (`ByteBuf`)
- `p_295190_` (`int`)

**Returns:** `public static String`

### DecoderException

```java
throw new DecoderException("The received encoded string buffer length is less than zero! Weird string!")
```

**Parameters:**

- `string!"` (`"The received encoded string buffer length is less than zero! Weird`)

**Returns:** `throw new`

### DecoderException

```java
throw new DecoderException("Not enough bytes in buffer, expected " + j + ", but got " + k)
```

**Parameters:**

- `buffer` (`"Not enough bytes in`)
- `"` (`expected " + j +`)
- `k` (`but got " +`)

**Returns:** `throw new`

### write

```java
public static void write(ByteBuf p_295811_, CharSequence p_295702_, int p_295310_)
```

**Parameters:**

- `p_295811_` (`ByteBuf`)
- `p_295702_` (`CharSequence`)
- `p_295310_` (`int`)

**Returns:** `public static void`
