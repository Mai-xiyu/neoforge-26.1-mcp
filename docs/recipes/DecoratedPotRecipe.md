# DecoratedPotRecipe

**Package:** `net.minecraft.world.item.crafting`
**Type:** class
**Extends:** `CustomRecipe`

## Methods

### DecoratedPotRecipe

```java
public DecoratedPotRecipe(CraftingBookCategory p_273056_)
```

**Parameters:**

- `p_273056_` (`CraftingBookCategory`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### matches

```java
public boolean matches(CraftingInput p_344915_, Level p_272812_)
```

**Parameters:**

- `p_344915_` (`CraftingInput`)
- `p_272812_` (`Level`)

**Returns:** `public boolean`

### assemble

```java
public ItemStack assemble(CraftingInput p_345761_, HolderLookup.Provider p_335840_)
```

**Parameters:**

- `p_345761_` (`CraftingInput`)
- `p_335840_` (`HolderLookup.Provider`)

**Returns:** `public ItemStack`

### canCraftInDimensions

```java
public boolean canCraftInDimensions(int p_273734_, int p_273516_)
```

**Parameters:**

- `p_273734_` (`int`)
- `p_273516_` (`int`)

**Returns:** `boolean`

### getSerializer

```java
public RecipeSerializer<?> getSerializer()
```

**Returns:** `RecipeSerializer<?>`
