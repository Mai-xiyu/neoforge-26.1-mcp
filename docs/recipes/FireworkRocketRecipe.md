# FireworkRocketRecipe

**Package:** `net.minecraft.world.item.crafting`
**Type:** class
**Extends:** `CustomRecipe`

## Methods

### FireworkRocketRecipe

```java
public FireworkRocketRecipe(CraftingBookCategory p_250134_)
```

**Parameters:**

- `p_250134_` (`CraftingBookCategory`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### matches

```java
public boolean matches(CraftingInput p_345559_, Level p_43855_)
```

**Parameters:**

- `p_345559_` (`CraftingInput`)
- `p_43855_` (`Level`)

**Returns:** `public boolean`

### assemble

```java
public ItemStack assemble(CraftingInput p_345921_, HolderLookup.Provider p_335560_)
```

**Parameters:**

- `p_345921_` (`CraftingInput`)
- `p_335560_` (`HolderLookup.Provider`)

**Returns:** `public ItemStack`

### canCraftInDimensions

```java
public boolean canCraftInDimensions(int p_43844_, int p_43845_)
```

**Parameters:**

- `p_43844_` (`int`)
- `p_43845_` (`int`)

**Returns:** `boolean`

### getResultItem

```java
public ItemStack getResultItem(HolderLookup.Provider p_335481_)
```

**Parameters:**

- `p_335481_` (`HolderLookup.Provider`)

**Returns:** `ItemStack`

### ItemStack

```java
return new ItemStack()
```

**Returns:** `return new`

### getSerializer

```java
public RecipeSerializer<?> getSerializer()
```

**Returns:** `RecipeSerializer<?>`
