# FireworkStarRecipe

**Package:** `net.minecraft.world.item.crafting`
**Type:** class
**Extends:** `CustomRecipe`

## Methods

### FireworkStarRecipe

```java
public FireworkStarRecipe(CraftingBookCategory p_251577_)
```

**Parameters:**

- `p_251577_` (`CraftingBookCategory`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### matches

```java
public boolean matches(CraftingInput p_344993_, Level p_43896_)
```

**Parameters:**

- `p_344993_` (`CraftingInput`)
- `p_43896_` (`Level`)

**Returns:** `public boolean`

### assemble

```java
public ItemStack assemble(CraftingInput p_346206_, HolderLookup.Provider p_335498_)
```

**Parameters:**

- `p_346206_` (`CraftingInput`)
- `p_335498_` (`HolderLookup.Provider`)

**Returns:** `public ItemStack`

### canCraftInDimensions

```java
public boolean canCraftInDimensions(int p_43885_, int p_43886_)
```

**Parameters:**

- `p_43885_` (`int`)
- `p_43886_` (`int`)

**Returns:** `boolean`

### getResultItem

```java
public ItemStack getResultItem(HolderLookup.Provider p_335423_)
```

**Parameters:**

- `p_335423_` (`HolderLookup.Provider`)

**Returns:** `ItemStack`

### ItemStack

```java
return new ItemStack()
```

**Returns:** `return new`

### getSerializer

```java
public RecipeSerializer<?> getSerializer()
```

**Returns:** `RecipeSerializer<?>`
