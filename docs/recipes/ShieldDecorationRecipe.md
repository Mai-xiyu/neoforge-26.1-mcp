# ShieldDecorationRecipe

**Package:** `net.minecraft.world.item.crafting`
**Type:** class
**Extends:** `CustomRecipe`

## Methods

### ShieldDecorationRecipe

```java
public ShieldDecorationRecipe(CraftingBookCategory p_251065_)
```

**Parameters:**

- `p_251065_` (`CraftingBookCategory`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### matches

```java
public boolean matches(CraftingInput p_344969_, Level p_44309_)
```

**Parameters:**

- `p_344969_` (`CraftingInput`)
- `p_44309_` (`Level`)

**Returns:** `public boolean`

### assemble

```java
public ItemStack assemble(CraftingInput p_345351_, HolderLookup.Provider p_335895_)
```

**Parameters:**

- `p_345351_` (`CraftingInput`)
- `p_335895_` (`HolderLookup.Provider`)

**Returns:** `public ItemStack`

### canCraftInDimensions

```java
public boolean canCraftInDimensions(int p_44298_, int p_44299_)
```

**Parameters:**

- `p_44298_` (`int`)
- `p_44299_` (`int`)

**Returns:** `boolean`

### getSerializer

```java
public RecipeSerializer<?> getSerializer()
```

**Returns:** `RecipeSerializer<?>`
