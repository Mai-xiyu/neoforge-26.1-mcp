# ShulkerBoxColoring

**Package:** `net.minecraft.world.item.crafting`
**Type:** class
**Extends:** `CustomRecipe`

## Methods

### ShulkerBoxColoring

```java
public ShulkerBoxColoring(CraftingBookCategory p_250756_)
```

**Parameters:**

- `p_250756_` (`CraftingBookCategory`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### matches

```java
public boolean matches(CraftingInput p_345624_, Level p_44325_)
```

**Parameters:**

- `p_345624_` (`CraftingInput`)
- `p_44325_` (`Level`)

**Returns:** `public boolean`

### assemble

```java
public ItemStack assemble(CraftingInput p_346433_, HolderLookup.Provider p_335717_)
```

**Parameters:**

- `p_346433_` (`CraftingInput`)
- `p_335717_` (`HolderLookup.Provider`)

**Returns:** `public ItemStack`

### canCraftInDimensions

```java
public boolean canCraftInDimensions(int p_44314_, int p_44315_)
```

**Parameters:**

- `p_44314_` (`int`)
- `p_44315_` (`int`)

**Returns:** `boolean`

### getSerializer

```java
public RecipeSerializer<?> getSerializer()
```

**Returns:** `RecipeSerializer<?>`
