# SuspiciousStewRecipe

**Package:** `net.minecraft.world.item.crafting`
**Type:** class
**Extends:** `CustomRecipe`

## Methods

### SuspiciousStewRecipe

```java
public SuspiciousStewRecipe(CraftingBookCategory p_250392_)
```

**Parameters:**

- `p_250392_` (`CraftingBookCategory`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### matches

```java
public boolean matches(CraftingInput p_345878_, Level p_44500_)
```

**Parameters:**

- `p_345878_` (`CraftingInput`)
- `p_44500_` (`Level`)

**Returns:** `public boolean`

### assemble

```java
public ItemStack assemble(CraftingInput p_346220_, HolderLookup.Provider p_336034_)
```

**Parameters:**

- `p_346220_` (`CraftingInput`)
- `p_336034_` (`HolderLookup.Provider`)

**Returns:** `public ItemStack`

### canCraftInDimensions

```java
public boolean canCraftInDimensions(int p_44489_, int p_44490_)
```

**Parameters:**

- `p_44489_` (`int`)
- `p_44490_` (`int`)

**Returns:** `boolean`

### getSerializer

```java
public RecipeSerializer<?> getSerializer()
```

**Returns:** `RecipeSerializer<?>`
