# TippedArrowRecipe

**Package:** `net.minecraft.world.item.crafting`
**Type:** class
**Extends:** `CustomRecipe`

## Methods

### TippedArrowRecipe

```java
public TippedArrowRecipe(CraftingBookCategory p_252163_)
```

**Parameters:**

- `p_252163_` (`CraftingBookCategory`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### matches

```java
public boolean matches(CraftingInput p_345143_, Level p_44516_)
```

**Parameters:**

- `p_345143_` (`CraftingInput`)
- `p_44516_` (`Level`)

**Returns:** `public boolean`

### assemble

```java
public ItemStack assemble(CraftingInput p_345174_, HolderLookup.Provider p_335792_)
```

**Parameters:**

- `p_345174_` (`CraftingInput`)
- `p_335792_` (`HolderLookup.Provider`)

**Returns:** `public ItemStack`

### canCraftInDimensions

```java
public boolean canCraftInDimensions(int p_44505_, int p_44506_)
```

**Parameters:**

- `p_44505_` (`int`)
- `p_44506_` (`int`)

**Returns:** `boolean`

### getSerializer

```java
public RecipeSerializer<?> getSerializer()
```

**Returns:** `RecipeSerializer<?>`
