# NeoForgeDataMaps

**Package:** `net.neoforged.neoforge.registries.datamaps.builtin`
**Type:** class

## Description

Holds all `DataMapType data maps` provided by NeoForge.



These data maps are usually replacements for vanilla in-code maps, and are optionally
synced so that mods can use them on the client side.

## Methods

### id

```java
private static ResourceLocation id(String name)
```

**Parameters:**

- `name` (`String`)

**Returns:** `private static ResourceLocation`
