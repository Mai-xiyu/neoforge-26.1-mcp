# BeeStingerLayer

**Package:** `net.minecraft.client.renderer.entity.layers`
**Type:** class<T extends LivingEntity, M extends PlayerModel<T>> extends StuckInBodyLayer<T, M>
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### BeeStingerLayer

```java
public BeeStingerLayer(LivingEntityRenderer<T, M> p_116580_)
```

**Parameters:**

- `p_116580_` (`LivingEntityRenderer<T, M>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### numStuck

```java
protected int numStuck(T p_116582_)
```

**Parameters:**

- `p_116582_` (`T`)

**Returns:** `int`

### renderStuckItem

```java
protected void renderStuckItem(PoseStack p_116584_, MultiBufferSource p_116585_, int p_116586_, Entity p_116587_, float p_116588_, float p_116589_, float p_116590_, float p_116591_)
```

**Parameters:**

- `p_116584_` (`PoseStack`)
- `p_116585_` (`MultiBufferSource`)
- `p_116586_` (`int`)
- `p_116587_` (`Entity`)
- `p_116588_` (`float`)
- `p_116589_` (`float`)
- `p_116590_` (`float`)
- `p_116591_` (`float`)

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
private static void vertex(VertexConsumer p_254470_, PoseStack.Pose p_324141_, float p_253749_, int p_254520_, float p_254099_, float p_253914_, int p_254168_)
```

**Parameters:**

- `p_254470_` (`VertexConsumer`)
- `p_324141_` (`PoseStack.Pose`)
- `p_253749_` (`float`)
- `p_254520_` (`int`)
- `p_254099_` (`float`)
- `p_253914_` (`float`)
- `p_254168_` (`int`)

**Returns:** `private static void`
