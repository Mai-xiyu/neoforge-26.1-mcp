# BreezeWindLayer

**Package:** `net.minecraft.client.renderer.entity.layers`
**Type:** class
**Extends:** `RenderLayer<Breeze, BreezeModel<Breeze>>`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### BreezeWindLayer

```java
public BreezeWindLayer(EntityRendererProvider.Context p_350777_, RenderLayerParent<Breeze, BreezeModel<Breeze>> p_312625_)
```

**Parameters:**

- `p_350777_` (`EntityRendererProvider.Context`)
- `p_312625_` (`RenderLayerParent<Breeze, BreezeModel<Breeze>>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### render

```java
public void render(PoseStack p_312822_, MultiBufferSource p_312869_, int p_311783_, Breeze p_312046_, float p_312170_, float p_311773_, float p_312428_, float p_312287_, float p_312118_, float p_312531_)
```

**Parameters:**

- `p_312822_` (`PoseStack`)
- `p_312869_` (`MultiBufferSource`)
- `p_311783_` (`int`)
- `p_312046_` (`Breeze`)
- `p_312170_` (`float`)
- `p_311773_` (`float`)
- `p_312428_` (`float`)
- `p_312287_` (`float`)
- `p_312118_` (`float`)
- `p_312531_` (`float`)

**Returns:** `public void`

### xOffset

```java
private float xOffset(float p_312086_)
```

**Parameters:**

- `p_312086_` (`float`)

**Returns:** `private float`
