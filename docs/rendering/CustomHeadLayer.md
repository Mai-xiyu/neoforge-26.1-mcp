# CustomHeadLayer

**Package:** `net.minecraft.client.renderer.entity.layers`
**Type:** class<T extends LivingEntity, M extends EntityModel<T> & HeadedModel> extends RenderLayer<T, M>
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### CustomHeadLayer

```java
public CustomHeadLayer(RenderLayerParent<T, M> p_234829_, EntityModelSet p_234830_, ItemInHandRenderer p_234831_)
```

**Parameters:**

- `p_234829_` (`RenderLayerParent<T, M>`)
- `p_234830_` (`EntityModelSet`)
- `p_234831_` (`ItemInHandRenderer`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### CustomHeadLayer

```java
public CustomHeadLayer(RenderLayerParent<T, M> p_234822_, EntityModelSet p_234823_, float p_234824_, float p_234825_, float p_234826_, ItemInHandRenderer p_234827_)
```

**Parameters:**

- `p_234822_` (`RenderLayerParent<T, M>`)
- `p_234823_` (`EntityModelSet`)
- `p_234824_` (`float`)
- `p_234825_` (`float`)
- `p_234826_` (`float`)
- `p_234827_` (`ItemInHandRenderer`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### render

```java
public void render(PoseStack p_116731_, MultiBufferSource p_116732_, int p_116733_, T p_116734_, float p_116735_, float p_116736_, float p_116737_, float p_116738_, float p_116739_, float p_116740_)
```

**Parameters:**

- `p_116731_` (`PoseStack`)
- `p_116732_` (`MultiBufferSource`)
- `p_116733_` (`int`)
- `p_116734_` (`T`)
- `p_116735_` (`float`)
- `p_116736_` (`float`)
- `p_116737_` (`float`)
- `p_116738_` (`float`)
- `p_116739_` (`float`)
- `p_116740_` (`float`)

**Returns:** `public void`

### translateToHead

```java
 translateToHead()
```

**Returns:** ``

### translateToHead

```java
public static void translateToHead(PoseStack p_174484_, boolean p_174485_)
```

**Parameters:**

- `p_174484_` (`PoseStack`)
- `p_174485_` (`boolean`)

**Returns:** `public static void`
