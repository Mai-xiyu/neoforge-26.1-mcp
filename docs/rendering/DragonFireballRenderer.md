# DragonFireballRenderer

**Package:** `net.minecraft.client.renderer.entity`
**Type:** class
**Extends:** `EntityRenderer<DragonFireball>`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### DragonFireballRenderer

```java
public DragonFireballRenderer(EntityRendererProvider.Context p_173962_)
```

**Parameters:**

- `p_173962_` (`EntityRendererProvider.Context`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getBlockLightLevel

```java
protected int getBlockLightLevel(DragonFireball p_114087_, BlockPos p_114088_)
```

**Parameters:**

- `p_114087_` (`DragonFireball`)
- `p_114088_` (`BlockPos`)

**Returns:** `protected int`

### render

```java
public void render(DragonFireball p_114080_, float p_114081_, float p_114082_, PoseStack p_114083_, MultiBufferSource p_114084_, int p_114085_)
```

**Parameters:**

- `p_114080_` (`DragonFireball`)
- `p_114081_` (`float`)
- `p_114082_` (`float`)
- `p_114083_` (`PoseStack`)
- `p_114084_` (`MultiBufferSource`)
- `p_114085_` (`int`)

**Returns:** `public void`

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
private static void vertex(VertexConsumer p_254095_, PoseStack.Pose p_324420_, int p_253829_, float p_253995_, int p_254031_, int p_253641_, int p_254243_)
```

**Parameters:**

- `p_254095_` (`VertexConsumer`)
- `p_324420_` (`PoseStack.Pose`)
- `p_253829_` (`int`)
- `p_253995_` (`float`)
- `p_254031_` (`int`)
- `p_253641_` (`int`)
- `p_254243_` (`int`)

**Returns:** `private static void`

### getTextureLocation

```java
public ResourceLocation getTextureLocation(DragonFireball p_114078_)
```

**Parameters:**

- `p_114078_` (`DragonFireball`)

**Returns:** `public ResourceLocation`
