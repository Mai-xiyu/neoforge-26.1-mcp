# ExperienceOrbRenderer

**Package:** `net.minecraft.client.renderer.entity`
**Type:** class
**Extends:** `EntityRenderer<ExperienceOrb>`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### ExperienceOrbRenderer

```java
public ExperienceOrbRenderer(EntityRendererProvider.Context p_174110_)
```

**Parameters:**

- `p_174110_` (`EntityRendererProvider.Context`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getBlockLightLevel

```java
protected int getBlockLightLevel(ExperienceOrb p_114606_, BlockPos p_114607_)
```

**Parameters:**

- `p_114606_` (`ExperienceOrb`)
- `p_114607_` (`BlockPos`)

**Returns:** `protected int`

### render

```java
public void render(ExperienceOrb p_114599_, float p_114600_, float p_114601_, PoseStack p_114602_, MultiBufferSource p_114603_, int p_114604_)
```

**Parameters:**

- `p_114599_` (`ExperienceOrb`)
- `p_114600_` (`float`)
- `p_114601_` (`float`)
- `p_114602_` (`PoseStack`)
- `p_114603_` (`MultiBufferSource`)
- `p_114604_` (`int`)

**Returns:** `public void`

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
private static void vertex(VertexConsumer p_254515_, PoseStack.Pose p_324046_, float p_253952_, float p_254066_, int p_254283_, int p_254566_, int p_253882_, float p_254434_, float p_254223_, int p_254372_)
```

**Parameters:**

- `p_254515_` (`VertexConsumer`)
- `p_324046_` (`PoseStack.Pose`)
- `p_253952_` (`float`)
- `p_254066_` (`float`)
- `p_254283_` (`int`)
- `p_254566_` (`int`)
- `p_253882_` (`int`)
- `p_254434_` (`float`)
- `p_254223_` (`float`)
- `p_254372_` (`int`)

**Returns:** `private static void`

### getTextureLocation

```java
public ResourceLocation getTextureLocation(ExperienceOrb p_114597_)
```

**Parameters:**

- `p_114597_` (`ExperienceOrb`)

**Returns:** `public ResourceLocation`
