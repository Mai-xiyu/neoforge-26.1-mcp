# FishingHookRenderer

**Package:** `net.minecraft.client.renderer.entity`
**Type:** class
**Extends:** `EntityRenderer<FishingHook>`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### FishingHookRenderer

```java
public FishingHookRenderer(EntityRendererProvider.Context p_174117_)
```

**Parameters:**

- `p_174117_` (`EntityRendererProvider.Context`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### render

```java
public void render(FishingHook p_114705_, float p_114706_, float p_114707_, PoseStack p_114708_, MultiBufferSource p_114709_, int p_114710_)
```

**Parameters:**

- `p_114705_` (`FishingHook`)
- `p_114706_` (`float`)
- `p_114707_` (`float`)
- `p_114708_` (`PoseStack`)
- `p_114709_` (`MultiBufferSource`)
- `p_114710_` (`int`)

**Returns:** `public void`

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### getPlayerHandPos

```java
private Vec3 getPlayerHandPos(Player p_340935_, float p_340872_, float p_341261_)
```

**Parameters:**

- `p_340935_` (`Player`)
- `p_340872_` (`float`)
- `p_341261_` (`float`)

**Returns:** `private Vec3`

### fraction

```java
private static float fraction(int p_114691_, int p_114692_)
```

**Parameters:**

- `p_114691_` (`int`)
- `p_114692_` (`int`)

**Returns:** `private static float`

### vertex

```java
private static void vertex(VertexConsumer p_254464_, PoseStack.Pose p_323724_, int p_254296_, float p_253632_, int p_254132_, int p_254171_, int p_254026_)
```

**Parameters:**

- `p_254464_` (`VertexConsumer`)
- `p_323724_` (`PoseStack.Pose`)
- `p_254296_` (`int`)
- `p_253632_` (`float`)
- `p_254132_` (`int`)
- `p_254171_` (`int`)
- `p_254026_` (`int`)

**Returns:** `private static void`

### stringVertex

```java
private static void stringVertex(float p_174119_, float p_174120_, float p_174121_, VertexConsumer p_174122_, PoseStack.Pose p_174123_, float p_174124_, float p_174125_)
```

**Parameters:**

- `p_174119_` (`float`)
- `p_174120_` (`float`)
- `p_174121_` (`float`)
- `p_174122_` (`VertexConsumer`)
- `p_174123_` (`PoseStack.Pose`)
- `p_174124_` (`float`)
- `p_174125_` (`float`)

**Returns:** `private static void`

### getTextureLocation

```java
public ResourceLocation getTextureLocation(FishingHook p_114703_)
```

**Parameters:**

- `p_114703_` (`FishingHook`)

**Returns:** `public ResourceLocation`
