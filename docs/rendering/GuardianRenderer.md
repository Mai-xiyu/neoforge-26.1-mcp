# GuardianRenderer

**Package:** `net.minecraft.client.renderer.entity`
**Type:** class
**Extends:** `MobRenderer<Guardian, GuardianModel>`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### GuardianRenderer

```java
public GuardianRenderer(EntityRendererProvider.Context p_174159_)
```

**Parameters:**

- `p_174159_` (`EntityRendererProvider.Context`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### GuardianRenderer

```java
protected GuardianRenderer(EntityRendererProvider.Context p_174161_, float p_174162_, ModelLayerLocation p_174163_)
```

**Parameters:**

- `p_174161_` (`EntityRendererProvider.Context`)
- `p_174162_` (`float`)
- `p_174163_` (`ModelLayerLocation`)

**Returns:** `protected`

### shouldRender

```java
public boolean shouldRender(Guardian p_114836_, Frustum p_114837_, double p_114838_, double p_114839_, double p_114840_)
```

**Parameters:**

- `p_114836_` (`Guardian`)
- `p_114837_` (`Frustum`)
- `p_114838_` (`double`)
- `p_114839_` (`double`)
- `p_114840_` (`double`)

**Returns:** `public boolean`

### getPosition

```java
private Vec3 getPosition(LivingEntity p_114803_, double p_114804_, float p_114805_)
```

**Parameters:**

- `p_114803_` (`LivingEntity`)
- `p_114804_` (`double`)
- `p_114805_` (`float`)

**Returns:** `private Vec3`

### Vec3

```java
return new Vec3()
```

**Returns:** `return new`

### render

```java
public void render(Guardian p_114829_, float p_114830_, float p_114831_, PoseStack p_114832_, MultiBufferSource p_114833_, int p_114834_)
```

**Parameters:**

- `p_114829_` (`Guardian`)
- `p_114830_` (`float`)
- `p_114831_` (`float`)
- `p_114832_` (`PoseStack`)
- `p_114833_` (`MultiBufferSource`)
- `p_114834_` (`int`)

**Returns:** `public void`

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex(f31 + 0.5F)
```

**Parameters:**

- `0.5F` (`f31 +`)

**Returns:** ``

### vertex

```java
 vertex(f31 + 0.5F)
```

**Parameters:**

- `0.5F` (`f31 +`)

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
 vertex()
```

**Returns:** ``

### vertex

```java
private static void vertex(VertexConsumer p_253637_, PoseStack.Pose p_323627_, float p_253994_, float p_254492_, float p_254474_, int p_254080_, int p_253655_, int p_254133_, float p_254233_, float p_253939_)
```

**Parameters:**

- `p_253637_` (`VertexConsumer`)
- `p_323627_` (`PoseStack.Pose`)
- `p_253994_` (`float`)
- `p_254492_` (`float`)
- `p_254474_` (`float`)
- `p_254080_` (`int`)
- `p_253655_` (`int`)
- `p_254133_` (`int`)
- `p_254233_` (`float`)
- `p_253939_` (`float`)

**Returns:** `private static void`

### getTextureLocation

```java
public ResourceLocation getTextureLocation(Guardian p_114827_)
```

**Parameters:**

- `p_114827_` (`Guardian`)

**Returns:** `public ResourceLocation`
