# HeightMapRenderer

**Package:** `net.minecraft.client.renderer.debug`
**Type:** class
**Implements:** `DebugRenderer.SimpleDebugRenderer`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### HeightMapRenderer

```java
public HeightMapRenderer(Minecraft p_113572_)
```

**Parameters:**

- `p_113572_` (`Minecraft`)

**Returns:** `public`

### render

```java
public void render(PoseStack p_113576_, MultiBufferSource p_113577_, double p_113578_, double p_113579_, double p_113580_)
```

**Parameters:**

- `p_113576_` (`PoseStack`)
- `p_113577_` (`MultiBufferSource`)
- `p_113578_` (`double`)
- `p_113579_` (`double`)
- `p_113580_` (`double`)

### getColor

```java
private Vector3f getColor(Heightmap.Types p_113574_)
```

**Parameters:**

- `p_113574_` (`Heightmap.Types`)

**Returns:** `private Vector3f`

### Vector3f

```java
> new Vector3f()
```

**Returns:** `> new`

### Vector3f

```java
> new Vector3f()
```

**Returns:** `> new`

### Vector3f

```java
> new Vector3f()
```

**Returns:** `> new`

### Vector3f

```java
> new Vector3f()
```

**Returns:** `> new`

### Vector3f

```java
> new Vector3f()
```

**Returns:** `> new`

### Vector3f

```java
> new Vector3f()
```

**Returns:** `> new`
