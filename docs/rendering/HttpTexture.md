# HttpTexture

**Package:** `net.minecraft.client.renderer.texture`
**Type:** class
**Extends:** `SimpleTexture`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### HttpTexture

```java
public HttpTexture(File p_118002_, String p_118003_, ResourceLocation p_118004_, boolean p_118005_, Runnable p_118006_)
```

**Parameters:**

- `p_118002_` (`File`)
- `p_118003_` (`String`)
- `p_118004_` (`ResourceLocation`)
- `p_118005_` (`boolean`)
- `p_118006_` (`Runnable`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### loadCallback

```java
private void loadCallback(NativeImage p_118011_)
```

**Parameters:**

- `p_118011_` (`NativeImage`)

**Returns:** `private void`

### upload

```java
private void upload(NativeImage p_118021_)
```

**Parameters:**

- `p_118021_` (`NativeImage`)

**Returns:** `private void`

### load

```java
public void load(ResourceManager p_118009_)
```

**Parameters:**

- `p_118009_` (`ResourceManager`)

### setNoAlpha

```java
 setNoAlpha()
```

**Returns:** ``

### doNotchTransparencyHack

```java
 doNotchTransparencyHack()
```

**Returns:** ``

### setNoAlpha

```java
 setNoAlpha()
```

**Returns:** ``

### setNoAlpha

```java
 setNoAlpha()
```

**Returns:** ``

### doNotchTransparencyHack

```java
private static void doNotchTransparencyHack(NativeImage p_118013_, int p_118014_, int p_118015_, int p_118016_, int p_118017_)
```

**Parameters:**

- `p_118013_` (`NativeImage`)
- `p_118014_` (`int`)
- `p_118015_` (`int`)
- `p_118016_` (`int`)
- `p_118017_` (`int`)

**Returns:** `private static void`

### setNoAlpha

```java
private static void setNoAlpha(NativeImage p_118023_, int p_118024_, int p_118025_, int p_118026_, int p_118027_)
```

**Parameters:**

- `p_118023_` (`NativeImage`)
- `p_118024_` (`int`)
- `p_118025_` (`int`)
- `p_118026_` (`int`)
- `p_118027_` (`int`)

**Returns:** `private static void`
