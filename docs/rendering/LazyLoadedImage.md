# LazyLoadedImage

**Package:** `net.minecraft.client.renderer.texture.atlas.sources`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### LazyLoadedImage

```java
public LazyLoadedImage(ResourceLocation p_267104_, Resource p_266995_, int p_266778_)
```

**Parameters:**

- `p_267104_` (`ResourceLocation`)
- `p_266995_` (`Resource`)
- `p_266778_` (`int`)

**Returns:** `public`

### get

```java
public NativeImage get()
```

**Returns:** `public NativeImage`

### synchronized

```java
 synchronized()
```

**Returns:** ``

### IOException

```java
throw new IOException("Failed to load image " + this.id)
```

**Parameters:**

- `this.id` (`"Failed to load image " +`)

**Returns:** `throw new`

### release

```java
public void release()
```

**Returns:** `public void`
