# LightSectionDebugRenderer

**Package:** `net.minecraft.client.renderer.debug`
**Type:** class
**Implements:** `DebugRenderer.SimpleDebugRenderer`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### LightSectionDebugRenderer

```java
public LightSectionDebugRenderer(Minecraft p_283340_, LightLayer p_283096_)
```

**Parameters:**

- `p_283340_` (`Minecraft`)
- `p_283096_` (`LightLayer`)

**Returns:** `public`

### render

```java
public void render(PoseStack p_281418_, MultiBufferSource p_282487_, double p_282164_, double p_282565_, double p_281615_)
```

**Parameters:**

- `p_281418_` (`PoseStack`)
- `p_282487_` (`MultiBufferSource`)
- `p_282164_` (`double`)
- `p_282565_` (`double`)
- `p_281615_` (`double`)

### renderEdges

```java
 renderEdges()
```

**Returns:** ``

### renderEdges

```java
 renderEdges()
```

**Returns:** ``

### renderFaces

```java
 renderFaces()
```

**Returns:** ``

### renderFaces

```java
 renderFaces()
```

**Returns:** ``

### renderFaces

```java
private static void renderFaces(PoseStack p_283088_, DiscreteVoxelShape p_281747_, SectionPos p_282941_, VertexConsumer p_283103_, double p_281419_, double p_282520_, double p_281976_, Vector4f p_282342_)
```

**Parameters:**

- `p_283088_` (`PoseStack`)
- `p_281747_` (`DiscreteVoxelShape`)
- `p_282941_` (`SectionPos`)
- `p_283103_` (`VertexConsumer`)
- `p_281419_` (`double`)
- `p_282520_` (`double`)
- `p_281976_` (`double`)
- `p_282342_` (`Vector4f`)

**Returns:** `private static void`

### renderFace

```java
 renderFace()
```

**Returns:** ``

### renderEdges

```java
private static void renderEdges(PoseStack p_282890_, DiscreteVoxelShape p_282950_, SectionPos p_281925_, MultiBufferSource p_281516_, double p_281554_, double p_283233_, double p_281690_, Vector4f p_282916_)
```

**Parameters:**

- `p_282890_` (`PoseStack`)
- `p_282950_` (`DiscreteVoxelShape`)
- `p_281925_` (`SectionPos`)
- `p_281516_` (`MultiBufferSource`)
- `p_281554_` (`double`)
- `p_283233_` (`double`)
- `p_281690_` (`double`)
- `p_282916_` (`Vector4f`)

**Returns:** `private static void`

### renderEdge

```java
 renderEdge()
```

**Returns:** ``

### renderFace

```java
private static void renderFace(PoseStack p_283612_, VertexConsumer p_281996_, Direction p_282340_, double p_281988_, double p_282440_, double p_282235_, int p_282751_, int p_282270_, int p_282159_, Vector4f p_283316_)
```

**Parameters:**

- `p_283612_` (`PoseStack`)
- `p_281996_` (`VertexConsumer`)
- `p_282340_` (`Direction`)
- `p_281988_` (`double`)
- `p_282440_` (`double`)
- `p_282235_` (`double`)
- `p_282751_` (`int`)
- `p_282270_` (`int`)
- `p_282159_` (`int`)
- `p_283316_` (`Vector4f`)

**Returns:** `private static void`

### renderEdge

```java
private static void renderEdge(PoseStack p_283045_, VertexConsumer p_282888_, double p_283424_, double p_283677_, double p_283390_, int p_281439_, int p_282106_, int p_282462_, int p_282216_, int p_281474_, int p_281542_, Vector4f p_283667_)
```

**Parameters:**

- `p_283045_` (`PoseStack`)
- `p_282888_` (`VertexConsumer`)
- `p_283424_` (`double`)
- `p_283677_` (`double`)
- `p_283390_` (`double`)
- `p_281439_` (`int`)
- `p_282106_` (`int`)
- `p_282462_` (`int`)
- `p_282216_` (`int`)
- `p_281474_` (`int`)
- `p_281542_` (`int`)
- `p_283667_` (`Vector4f`)

**Returns:** `private static void`

### SectionData

```java
 SectionData(LevelLightEngine p_283220_, SectionPos p_282370_, int p_282804_, LightLayer p_283151_)
```

**Parameters:**

- `p_283220_` (`LevelLightEngine`)
- `p_282370_` (`SectionPos`)
- `p_282804_` (`int`)
- `p_283151_` (`LightLayer`)

**Returns:** ``
