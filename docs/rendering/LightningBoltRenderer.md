# LightningBoltRenderer

**Package:** `net.minecraft.client.renderer.entity`
**Type:** class
**Extends:** `EntityRenderer<LightningBolt>`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### LightningBoltRenderer

```java
public LightningBoltRenderer(EntityRendererProvider.Context p_174286_)
```

**Parameters:**

- `p_174286_` (`EntityRendererProvider.Context`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### render

```java
public void render(LightningBolt p_115266_, float p_115267_, float p_115268_, PoseStack p_115269_, MultiBufferSource p_115270_, int p_115271_)
```

**Parameters:**

- `p_115266_` (`LightningBolt`)
- `p_115267_` (`float`)
- `p_115268_` (`float`)
- `p_115269_` (`PoseStack`)
- `p_115270_` (`MultiBufferSource`)
- `p_115271_` (`int`)

**Returns:** `public void`

### quad

```java
 quad()
```

**Returns:** ``

### quad

```java
 quad()
```

**Returns:** ``

### quad

```java
 quad()
```

**Returns:** ``

### quad

```java
 quad()
```

**Returns:** ``

### quad

```java
private static void quad(Matrix4f p_253966_, VertexConsumer p_115274_, float p_115275_, float p_115276_, int p_115277_, float p_115278_, float p_115279_, float p_115280_, float p_115281_, float p_115282_, float p_115283_, float p_115284_, boolean p_115285_, boolean p_115286_, boolean p_115287_, boolean p_115288_)
```

**Parameters:**

- `p_253966_` (`Matrix4f`)
- `p_115274_` (`VertexConsumer`)
- `p_115275_` (`float`)
- `p_115276_` (`float`)
- `p_115277_` (`int`)
- `p_115278_` (`float`)
- `p_115279_` (`float`)
- `p_115280_` (`float`)
- `p_115281_` (`float`)
- `p_115282_` (`float`)
- `p_115283_` (`float`)
- `p_115284_` (`float`)
- `p_115285_` (`boolean`)
- `p_115286_` (`boolean`)
- `p_115287_` (`boolean`)
- `p_115288_` (`boolean`)

**Returns:** `private static void`

### getTextureLocation

```java
public ResourceLocation getTextureLocation(LightningBolt p_115264_)
```

**Parameters:**

- `p_115264_` (`LightningBolt`)

**Returns:** `public ResourceLocation`
