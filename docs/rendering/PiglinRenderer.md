# PiglinRenderer

**Package:** `net.minecraft.client.renderer.entity`
**Type:** class
**Extends:** `HumanoidMobRenderer<Mob, PiglinModel<Mob>>`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### PiglinRenderer

```java
public PiglinRenderer(EntityRendererProvider.Context p_174344_, ModelLayerLocation p_174345_, ModelLayerLocation p_174346_, ModelLayerLocation p_174347_, boolean p_174348_)
```

**Parameters:**

- `p_174344_` (`EntityRendererProvider.Context`)
- `p_174345_` (`ModelLayerLocation`)
- `p_174346_` (`ModelLayerLocation`)
- `p_174347_` (`ModelLayerLocation`)
- `p_174348_` (`boolean`)

**Returns:** `public`

### createModel

```java
private static PiglinModel<Mob> createModel(EntityModelSet p_174350_, ModelLayerLocation p_174351_, boolean p_174352_)
```

**Parameters:**

- `p_174350_` (`EntityModelSet`)
- `p_174351_` (`ModelLayerLocation`)
- `p_174352_` (`boolean`)

**Returns:** `private static PiglinModel<Mob>`

### getTextureLocation

```java
public ResourceLocation getTextureLocation(Mob p_115708_)
```

**Parameters:**

- `p_115708_` (`Mob`)

**Returns:** `public ResourceLocation`

### isShaking

```java
protected boolean isShaking(Mob p_115712_)
```

**Parameters:**

- `p_115712_` (`Mob`)

**Returns:** `protected boolean`
