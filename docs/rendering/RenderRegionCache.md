# RenderRegionCache

**Package:** `net.minecraft.client.renderer.chunk`
**Type:** class
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### createRegion

```java
public RenderChunkRegion createRegion(Level p_200466_, SectionPos p_350879_)
```

**Parameters:**

- `p_200466_` (`Level`)
- `p_350879_` (`SectionPos`)

**Returns:** `RenderChunkRegion`

### createRegion

```java
return createRegion()
```

**Returns:** `return`

### createRegion

```java
public RenderChunkRegion createRegion(Level p_200466_, SectionPos p_350879_, boolean nullForEmpty)
```

**Parameters:**

- `p_200466_` (`Level`)
- `p_350879_` (`SectionPos`)
- `nullForEmpty` (`boolean`)

**Returns:** `RenderChunkRegion`

### RenderChunkRegion

```java
return new RenderChunkRegion()
```

**Returns:** `return new`

### getChunkInfo

```java
private RenderRegionCache.ChunkInfo getChunkInfo(Level p_350834_, int p_350803_, int p_350907_)
```

**Parameters:**

- `p_350834_` (`Level`)
- `p_350803_` (`int`)
- `p_350907_` (`int`)

**Returns:** `private RenderRegionCache.ChunkInfo`

### ChunkInfo

```java
 ChunkInfo(LevelChunk p_200479_)
```

**Parameters:**

- `p_200479_` (`LevelChunk`)

**Returns:** ``

### chunk

```java
public LevelChunk chunk()
```

**Returns:** `public LevelChunk`

### renderChunk

```java
public RenderChunk renderChunk()
```

**Returns:** `public RenderChunk`
