# SaddleLayer

**Package:** `net.minecraft.client.renderer.entity.layers`
**Type:** class<T extends Entity & Saddleable, M extends EntityModel<T>> extends RenderLayer<T, M>
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### SaddleLayer

```java
public SaddleLayer(RenderLayerParent<T, M> p_117390_, M p_117391_, ResourceLocation p_117392_)
```

**Parameters:**

- `p_117390_` (`RenderLayerParent<T, M>`)
- `p_117391_` (`M`)
- `p_117392_` (`ResourceLocation`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### render

```java
public void render(PoseStack p_117394_, MultiBufferSource p_117395_, int p_117396_, T p_117397_, float p_117398_, float p_117399_, float p_117400_, float p_117401_, float p_117402_, float p_117403_)
```

**Parameters:**

- `p_117394_` (`PoseStack`)
- `p_117395_` (`MultiBufferSource`)
- `p_117396_` (`int`)
- `p_117397_` (`T`)
- `p_117398_` (`float`)
- `p_117399_` (`float`)
- `p_117400_` (`float`)
- `p_117401_` (`float`)
- `p_117402_` (`float`)
- `p_117403_` (`float`)
