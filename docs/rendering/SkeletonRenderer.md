# SkeletonRenderer

**Package:** `net.minecraft.client.renderer.entity`
**Type:** class<T extends AbstractSkeleton> extends HumanoidMobRenderer<T, SkeletonModel<T>>
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### SkeletonRenderer

```java
public SkeletonRenderer(EntityRendererProvider.Context p_174380_)
```

**Parameters:**

- `p_174380_` (`EntityRendererProvider.Context`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### SkeletonRenderer

```java
public SkeletonRenderer(EntityRendererProvider.Context p_174382_, ModelLayerLocation p_174383_, ModelLayerLocation p_174384_, ModelLayerLocation p_174385_)
```

**Parameters:**

- `p_174382_` (`EntityRendererProvider.Context`)
- `p_174383_` (`ModelLayerLocation`)
- `p_174384_` (`ModelLayerLocation`)
- `p_174385_` (`ModelLayerLocation`)

**Returns:** `public`

### SkeletonRenderer

```java
public SkeletonRenderer(EntityRendererProvider.Context p_331294_, ModelLayerLocation p_330964_, ModelLayerLocation p_331594_, SkeletonModel<T> p_331796_)
```

**Parameters:**

- `p_331294_` (`EntityRendererProvider.Context`)
- `p_330964_` (`ModelLayerLocation`)
- `p_331594_` (`ModelLayerLocation`)
- `p_331796_` (`SkeletonModel<T>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getTextureLocation

```java
public ResourceLocation getTextureLocation(T p_115941_)
```

**Parameters:**

- `p_115941_` (`T`)

**Returns:** `public ResourceLocation`

### isShaking

```java
protected boolean isShaking(T p_174389_)
```

**Parameters:**

- `p_174389_` (`T`)

**Returns:** `protected boolean`
