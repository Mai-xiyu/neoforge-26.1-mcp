# SlimeOuterLayer

**Package:** `net.minecraft.client.renderer.entity.layers`
**Type:** class<T extends LivingEntity> extends RenderLayer<T, SlimeModel<T>>
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### SlimeOuterLayer

```java
public SlimeOuterLayer(RenderLayerParent<T, SlimeModel<T>> p_174536_, EntityModelSet p_174537_)
```

**Parameters:**

- `p_174536_` (`RenderLayerParent<T, SlimeModel<T>>`)
- `p_174537_` (`EntityModelSet`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### render

```java
public void render(PoseStack p_117470_, MultiBufferSource p_117471_, int p_117472_, T p_117473_, float p_117474_, float p_117475_, float p_117476_, float p_117477_, float p_117478_, float p_117479_)
```

**Parameters:**

- `p_117470_` (`PoseStack`)
- `p_117471_` (`MultiBufferSource`)
- `p_117472_` (`int`)
- `p_117473_` (`T`)
- `p_117474_` (`float`)
- `p_117475_` (`float`)
- `p_117476_` (`float`)
- `p_117477_` (`float`)
- `p_117478_` (`float`)
- `p_117479_` (`float`)

**Returns:** `public void`
