# SpriteCoordinateExpander

**Package:** `net.minecraft.client.renderer`
**Type:** class
**Implements:** `VertexConsumer`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### SpriteCoordinateExpander

```java
public SpriteCoordinateExpander(VertexConsumer p_110798_, TextureAtlasSprite p_110799_)
```

**Parameters:**

- `p_110798_` (`VertexConsumer`)
- `p_110799_` (`TextureAtlasSprite`)

**Returns:** `public`

### addVertex

```java
public VertexConsumer addVertex(float p_350653_, float p_350607_, float p_350347_)
```

**Parameters:**

- `p_350653_` (`float`)
- `p_350607_` (`float`)
- `p_350347_` (`float`)

**Returns:** `VertexConsumer`

### setColor

```java
public VertexConsumer setColor(int p_350639_, int p_350295_, int p_350400_, int p_350645_)
```

**Parameters:**

- `p_350639_` (`int`)
- `p_350295_` (`int`)
- `p_350400_` (`int`)
- `p_350645_` (`int`)

**Returns:** `VertexConsumer`

### setUv

```java
public VertexConsumer setUv(float p_350609_, float p_350452_)
```

**Parameters:**

- `p_350609_` (`float`)
- `p_350452_` (`float`)

**Returns:** `VertexConsumer`

### setUv1

```java
public VertexConsumer setUv1(int p_351031_, int p_351048_)
```

**Parameters:**

- `p_351031_` (`int`)
- `p_351048_` (`int`)

**Returns:** `VertexConsumer`

### setUv2

```java
public VertexConsumer setUv2(int p_350272_, int p_350390_)
```

**Parameters:**

- `p_350272_` (`int`)
- `p_350390_` (`int`)

**Returns:** `VertexConsumer`

### setNormal

```java
public VertexConsumer setNormal(float p_350659_, float p_350518_, float p_350663_)
```

**Parameters:**

- `p_350659_` (`float`)
- `p_350518_` (`float`)
- `p_350663_` (`float`)

**Returns:** `VertexConsumer`

### addVertex

```java
public void addVertex(float p_350724_, float p_350634_, float p_350513_, int p_350392_, float p_350322_, float p_350843_, int p_350477_, int p_350406_, float p_350536_, float p_350625_, float p_350888_)
```

**Parameters:**

- `p_350724_` (`float`)
- `p_350634_` (`float`)
- `p_350513_` (`float`)
- `p_350392_` (`int`)
- `p_350322_` (`float`)
- `p_350843_` (`float`)
- `p_350477_` (`int`)
- `p_350406_` (`int`)
- `p_350536_` (`float`)
- `p_350625_` (`float`)
- `p_350888_` (`float`)
