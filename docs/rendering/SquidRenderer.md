# SquidRenderer

**Package:** `net.minecraft.client.renderer.entity`
**Type:** class<T extends Squid> extends MobRenderer<T, SquidModel<T>>
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### SquidRenderer

```java
public SquidRenderer(EntityRendererProvider.Context p_174406_, SquidModel<T> p_174407_)
```

**Parameters:**

- `p_174406_` (`EntityRendererProvider.Context`)
- `p_174407_` (`SquidModel<T>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getTextureLocation

```java
public ResourceLocation getTextureLocation(T p_116030_)
```

**Parameters:**

- `p_116030_` (`T`)

**Returns:** `public ResourceLocation`

### setupRotations

```java
protected void setupRotations(T p_116035_, PoseStack p_116036_, float p_116037_, float p_116038_, float p_116039_, float p_319853_)
```

**Parameters:**

- `p_116035_` (`T`)
- `p_116036_` (`PoseStack`)
- `p_116037_` (`float`)
- `p_116038_` (`float`)
- `p_116039_` (`float`)
- `p_319853_` (`float`)

**Returns:** `protected void`

### getBob

```java
protected float getBob(T p_116032_, float p_116033_)
```

**Parameters:**

- `p_116032_` (`T`)
- `p_116033_` (`float`)

**Returns:** `protected float`
