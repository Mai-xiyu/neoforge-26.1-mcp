# VillageSectionsDebugRenderer

**Package:** `net.minecraft.client.renderer.debug`
**Type:** class
**Implements:** `DebugRenderer.SimpleDebugRenderer`
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### VillageSectionsDebugRenderer

```java
 VillageSectionsDebugRenderer()
```

**Returns:** ``

### clear

```java
public void clear()
```

### setVillageSection

```java
public void setVillageSection(SectionPos p_113710_)
```

**Parameters:**

- `p_113710_` (`SectionPos`)

**Returns:** `public void`

### setNotVillageSection

```java
public void setNotVillageSection(SectionPos p_113712_)
```

**Parameters:**

- `p_113712_` (`SectionPos`)

**Returns:** `public void`

### render

```java
public void render(PoseStack p_113701_, MultiBufferSource p_113702_, double p_113703_, double p_113704_, double p_113705_)
```

**Parameters:**

- `p_113701_` (`PoseStack`)
- `p_113702_` (`MultiBufferSource`)
- `p_113703_` (`double`)
- `p_113704_` (`double`)
- `p_113705_` (`double`)

### highlightVillageSection

```java
 highlightVillageSection()
```

**Returns:** ``

### highlightVillageSection

```java
private static void highlightVillageSection(PoseStack p_270832_, MultiBufferSource p_270443_, SectionPos p_271021_)
```

**Parameters:**

- `p_270832_` (`PoseStack`)
- `p_270443_` (`MultiBufferSource`)
- `p_271021_` (`SectionPos`)

**Returns:** `private static void`
