# VillagerProfessionLayer

**Package:** `net.minecraft.client.renderer.entity.layers`
**Type:** class<T extends LivingEntity & VillagerDataHolder, M extends EntityModel<T> & VillagerHeadModel> extends RenderLayer<T, M>
**Side:** 🖥️ Client
**Annotations:** `@OnlyIn(Dist.CLIENT)`

## Methods

### VillagerProfessionLayer

```java
public VillagerProfessionLayer(RenderLayerParent<T, M> p_174550_, ResourceManager p_174551_, String p_174552_)
```

**Parameters:**

- `p_174550_` (`RenderLayerParent<T, M>`)
- `p_174551_` (`ResourceManager`)
- `p_174552_` (`String`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### render

```java
public void render(PoseStack p_117646_, MultiBufferSource p_117647_, int p_117648_, T p_117649_, float p_117650_, float p_117651_, float p_117652_, float p_117653_, float p_117654_, float p_117655_)
```

**Parameters:**

- `p_117646_` (`PoseStack`)
- `p_117647_` (`MultiBufferSource`)
- `p_117648_` (`int`)
- `p_117649_` (`T`)
- `p_117650_` (`float`)
- `p_117651_` (`float`)
- `p_117652_` (`float`)
- `p_117653_` (`float`)
- `p_117654_` (`float`)
- `p_117655_` (`float`)

**Returns:** `public void`

### renderColoredCutoutModel

```java
 renderColoredCutoutModel()
```

**Returns:** ``

### renderColoredCutoutModel

```java
 renderColoredCutoutModel()
```

**Returns:** ``

### renderColoredCutoutModel

```java
 renderColoredCutoutModel()
```

**Returns:** ``

### getResourceLocation

```java
private ResourceLocation getResourceLocation(String p_117669_, ResourceLocation p_117670_)
```

**Parameters:**

- `p_117669_` (`String`)
- `p_117670_` (`ResourceLocation`)

**Returns:** `private ResourceLocation`

### getHatData

```java
<K> public <K> VillagerMetaDataSection.Hat getHatData(Object2ObjectMap<K, VillagerMetaDataSection.Hat> p_117659_, String p_117660_, DefaultedRegistry<K> p_117661_, K p_117662_)
```

**Parameters:**

- `p_117659_` (`Object2ObjectMap<K, VillagerMetaDataSection.Hat>`)
- `p_117660_` (`String`)
- `p_117661_` (`DefaultedRegistry<K>`)
- `p_117662_` (`K`)

**Returns:** `public <K> VillagerMetaDataSection.Hat`
