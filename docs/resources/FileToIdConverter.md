# FileToIdConverter

**Package:** `net.minecraft.resources`
**Type:** class

## Methods

### FileToIdConverter

```java
public FileToIdConverter(String p_248876_, String p_251478_)
```

**Parameters:**

- `p_248876_` (`String`)
- `p_251478_` (`String`)

**Returns:** `public`

### json

```java
public static FileToIdConverter json(String p_248754_)
```

**Parameters:**

- `p_248754_` (`String`)

**Returns:** `public static FileToIdConverter`

### FileToIdConverter

```java
return new FileToIdConverter()
```

**Returns:** `return new`

### idToFile

```java
public ResourceLocation idToFile(ResourceLocation p_251878_)
```

**Parameters:**

- `p_251878_` (`ResourceLocation`)

**Returns:** `public ResourceLocation`

### fileToId

```java
public ResourceLocation fileToId(ResourceLocation p_249595_)
```

**Parameters:**

- `p_249595_` (`ResourceLocation`)

**Returns:** `public ResourceLocation`

### listMatchingResources

```java
public Map<ResourceLocation, Resource> listMatchingResources(ResourceManager p_252045_)
```

**Parameters:**

- `p_252045_` (`ResourceManager`)

**Returns:** `public Map<ResourceLocation, Resource>`

### listMatchingResourceStacks

```java
public Map<ResourceLocation, List<Resource>> listMatchingResourceStacks(ResourceManager p_249881_)
```

**Parameters:**

- `p_249881_` (`ResourceManager`)

**Returns:** `public Map<ResourceLocation, List<Resource>>`

### listMatchingResourcesFromNamespace

```java
public Map<ResourceLocation, Resource> listMatchingResourcesFromNamespace(ResourceManager manager, String namespace)
```

**Parameters:**

- `manager` (`ResourceManager`)
- `namespace` (`String`)

**Returns:** `Map<ResourceLocation, Resource>`

### listMatchingResourceStacksFromNamespace

```java
public Map<ResourceLocation, List<Resource>> listMatchingResourceStacksFromNamespace(ResourceManager manager, String namespace)
```

**Parameters:**

- `manager` (`ResourceManager`)
- `namespace` (`String`)

**Returns:** `Map<ResourceLocation, List<Resource>>`
