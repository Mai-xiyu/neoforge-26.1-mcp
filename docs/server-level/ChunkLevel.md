# ChunkLevel

**Package:** `net.minecraft.server.level`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `RADIUS_AROUND_FULL_CHUNK` | `int` |  |
| `MAX_LEVEL` | `int` |  |

## Methods

### generationStatus

```java
public static ChunkStatus generationStatus(int p_287738_)
```

**Parameters:**

- `p_287738_` (`int`)

**Returns:** `ChunkStatus`

### getStatusAroundFullChunk

```java
return getStatusAroundFullChunk(p_287738_ - 33)
```

**Parameters:**

- `33` (`p_287738_ -`)

**Returns:** `return`

### getStatusAroundFullChunk

```java
public static ChunkStatus getStatusAroundFullChunk(int p_347593_, ChunkStatus p_347451_)
```

**Parameters:**

- `p_347593_` (`int`)
- `p_347451_` (`ChunkStatus`)

**Returns:** `ChunkStatus`

### getStatusAroundFullChunk

```java
public static ChunkStatus getStatusAroundFullChunk(int p_347662_)
```

**Parameters:**

- `p_347662_` (`int`)

**Returns:** `public static ChunkStatus`

### getStatusAroundFullChunk

```java
return getStatusAroundFullChunk()
```

**Returns:** `return`

### byStatus

```java
public static int byStatus(ChunkStatus p_331231_)
```

**Parameters:**

- `p_331231_` (`ChunkStatus`)

**Returns:** `public static int`

### fullStatus

```java
public static FullChunkStatus fullStatus(int p_287750_)
```

**Parameters:**

- `p_287750_` (`int`)

**Returns:** `public static FullChunkStatus`

### byStatus

```java
public static int byStatus(FullChunkStatus p_287601_)
```

**Parameters:**

- `p_287601_` (`FullChunkStatus`)

**Returns:** `public static int`

### isEntityTicking

```java
public static boolean isEntityTicking(int p_287767_)
```

**Parameters:**

- `p_287767_` (`int`)

**Returns:** `public static boolean`

### isBlockTicking

```java
public static boolean isBlockTicking(int p_287696_)
```

**Parameters:**

- `p_287696_` (`int`)

**Returns:** `public static boolean`

### isLoaded

```java
public static boolean isLoaded(int p_287635_)
```

**Parameters:**

- `p_287635_` (`int`)

**Returns:** `public static boolean`
