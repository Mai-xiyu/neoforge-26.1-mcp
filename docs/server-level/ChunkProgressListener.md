# ChunkProgressListener

**Package:** `net.minecraft.server.level.progress`
**Type:** interface

## Methods

### updateSpawnPos

```java
void updateSpawnPos(ChunkPos p_9617_)
```

**Parameters:**

- `p_9617_` (`ChunkPos`)

### onStatusChange

```java
void onStatusChange(ChunkPos p_9618_, ChunkStatus p_330739_)
```

**Parameters:**

- `p_9618_` (`ChunkPos`)
- `p_330739_` (`ChunkStatus`)

### start

```java
void start()
```

### stop

```java
void stop()
```

### calculateDiameter

```java
static int calculateDiameter(int p_320268_)
```

**Parameters:**

- `p_320268_` (`int`)

**Returns:** `static int`
