# LoggerChunkProgressListener

**Package:** `net.minecraft.server.level.progress`
**Type:** class
**Implements:** `ChunkProgressListener`

## Methods

### LoggerChunkProgressListener

```java
private LoggerChunkProgressListener(int p_9629_)
```

**Parameters:**

- `p_9629_` (`int`)

**Returns:** `private`

### createFromGameruleRadius

```java
public static LoggerChunkProgressListener createFromGameruleRadius(int p_319913_)
```

**Parameters:**

- `p_319913_` (`int`)

**Returns:** `public static LoggerChunkProgressListener`

### create

```java
public static LoggerChunkProgressListener create(int p_320293_)
```

**Parameters:**

- `p_320293_` (`int`)

**Returns:** `public static LoggerChunkProgressListener`

### LoggerChunkProgressListener

```java
return new LoggerChunkProgressListener(i * i)
```

**Parameters:**

- `i` (`i *`)

**Returns:** `return new`

### createCompleted

```java
public static LoggerChunkProgressListener createCompleted()
```

**Returns:** `public static LoggerChunkProgressListener`

### LoggerChunkProgressListener

```java
return new LoggerChunkProgressListener()
```

**Returns:** `return new`

### updateSpawnPos

```java
public void updateSpawnPos(ChunkPos p_9631_)
```

**Parameters:**

- `p_9631_` (`ChunkPos`)

### onStatusChange

```java
public void onStatusChange(ChunkPos p_9633_, ChunkStatus p_332174_)
```

**Parameters:**

- `p_9633_` (`ChunkPos`)
- `p_332174_` (`ChunkStatus`)

### start

```java
public void start()
```

### stop

```java
public void stop()
```

### getProgress

```java
public int getProgress()
```

**Returns:** `public int`
