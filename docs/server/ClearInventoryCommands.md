# ClearInventoryCommands

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_214421_, CommandBuildContext p_214422_)
```

**Parameters:**

- `p_214421_` (`CommandDispatcher<CommandSourceStack>`)
- `p_214422_` (`CommandBuildContext`)

**Returns:** `public static void`

### clearUnlimited

```java
private static int clearUnlimited(CommandSourceStack p_332708_, Collection<ServerPlayer> p_332807_, Predicate<ItemStack> p_332684_)
```

**Parameters:**

- `p_332708_` (`CommandSourceStack`)
- `p_332807_` (`Collection<ServerPlayer>`)
- `p_332684_` (`Predicate<ItemStack>`)

**Returns:** `private static int`

### clearInventory

```java
return clearInventory()
```

**Returns:** `return`

### clearInventory

```java
private static int clearInventory(CommandSourceStack p_136706_, Collection<ServerPlayer> p_136707_, Predicate<ItemStack> p_136708_, int p_136709_)
```

**Parameters:**

- `p_136706_` (`CommandSourceStack`)
- `p_136707_` (`Collection<ServerPlayer>`)
- `p_136708_` (`Predicate<ItemStack>`)
- `p_136709_` (`int`)

**Returns:** `private static int`
