# DebugConfigCommand

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_294998_)
```

**Parameters:**

- `p_294998_` (`CommandDispatcher<CommandSourceStack>`)

**Returns:** `public static void`

### getUuidsInConfig

```java
private static Iterable<String> getUuidsInConfig(MinecraftServer p_294210_)
```

**Parameters:**

- `p_294210_` (`MinecraftServer`)

**Returns:** `private static Iterable<String>`

### config

```java
private static int config(CommandSourceStack p_295666_, ServerPlayer p_294477_)
```

**Parameters:**

- `p_295666_` (`CommandSourceStack`)
- `p_294477_` (`ServerPlayer`)

**Returns:** `private static int`

### unconfig

```java
private static int unconfig(CommandSourceStack p_294216_, UUID p_296468_)
```

**Parameters:**

- `p_294216_` (`CommandSourceStack`)
- `p_296468_` (`UUID`)

**Returns:** `private static int`
