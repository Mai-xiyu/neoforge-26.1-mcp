# DebugMobSpawningCommand

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_180111_)
```

**Parameters:**

- `p_180111_` (`CommandDispatcher<CommandSourceStack>`)

**Returns:** `public static void`

### spawnMobs

```java
private static int spawnMobs(CommandSourceStack p_180115_, MobCategory p_180116_, BlockPos p_180117_)
```

**Parameters:**

- `p_180115_` (`CommandSourceStack`)
- `p_180116_` (`MobCategory`)
- `p_180117_` (`BlockPos`)

**Returns:** `private static int`
