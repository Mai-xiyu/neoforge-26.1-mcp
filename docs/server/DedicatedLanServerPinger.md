# DedicatedLanServerPinger

**Package:** `net.neoforged.neoforge.server.dedicated`
**Type:** class
**Extends:** `Thread`
**Side:** 🖧 Server

## Methods

### DedicatedLanServerPinger

```java
public DedicatedLanServerPinger(String motd, String address)
```

**Parameters:**

- `motd` (`String`)
- `address` (`String`)

**Returns:** `public`

### run

```java
public void run()
```

### sleep

```java
 sleep()
```

**Returns:** ``

### interrupt

```java
public void interrupt()
```

### createPingString

```java
private static String createPingString(String motd, String address)
```

**Parameters:**

- `motd` (`String`)
- `address` (`String`)

**Returns:** `private static String`
