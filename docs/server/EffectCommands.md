# EffectCommands

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_136954_, CommandBuildContext p_251610_)
```

**Parameters:**

- `p_136954_` (`CommandDispatcher<CommandSourceStack>`)
- `p_251610_` (`CommandBuildContext`)

**Returns:** `public static void`

### giveEffect

```java
private static int giveEffect(CommandSourceStack p_250553_, Collection<? extends Entity> p_250411_, Holder<MobEffect> p_249495_, Integer p_249652_, int p_251498_, boolean p_249944_)
```

**Parameters:**

- `p_250553_` (`CommandSourceStack`)
- `p_250411_` (`Collection<? extends Entity>`)
- `p_249495_` (`Holder<MobEffect>`)
- `p_249652_` (`Integer`)
- `p_251498_` (`int`)
- `p_249944_` (`boolean`)

**Returns:** `private static int`

### clearEffects

```java
private static int clearEffects(CommandSourceStack p_136960_, Collection<? extends Entity> p_136961_)
```

**Parameters:**

- `p_136960_` (`CommandSourceStack`)
- `p_136961_` (`Collection<? extends Entity>`)

**Returns:** `private static int`

### clearEffect

```java
private static int clearEffect(CommandSourceStack p_250069_, Collection<? extends Entity> p_248561_, Holder<MobEffect> p_249198_)
```

**Parameters:**

- `p_250069_` (`CommandSourceStack`)
- `p_248561_` (`Collection<? extends Entity>`)
- `p_249198_` (`Holder<MobEffect>`)

**Returns:** `private static int`
