# Main

**Package:** `net.minecraft.server`
**Type:** class
**Side:** 🖧 Server

## Methods

### main

```java
public static void main(String[] p_129699_)
```

**Parameters:**

- `p_129699_` (`String[]`)

### writePidFile

```java
 writePidFile()
```

**Returns:** ``

### run

```java
public void run()
```

### writePidFile

```java
private static void writePidFile(Path p_270192_)
```

**Parameters:**

- `p_270192_` (`Path`)

**Returns:** `private static void`

### UncheckedIOException

```java
throw new UncheckedIOException()
```

**Returns:** `throw new`

### loadOrCreateConfig

```java
private static WorldLoader.InitConfig loadOrCreateConfig(DedicatedServerProperties p_248563_, Dynamic<?> p_307444_, boolean p_249093_, PackRepository p_251069_)
```

**Parameters:**

- `p_248563_` (`DedicatedServerProperties`)
- `p_307444_` (`Dynamic<?>`)
- `p_249093_` (`boolean`)
- `p_251069_` (`PackRepository`)

**Returns:** `private static WorldLoader.InitConfig`

### forceUpgrade

```java
private static void forceUpgrade(LevelStorageSource.LevelStorageAccess p_195489_, DataFixer p_195490_, boolean p_195491_, BooleanSupplier p_195492_, RegistryAccess p_323503_, boolean p_321704_)
```

**Parameters:**

- `p_195489_` (`LevelStorageSource.LevelStorageAccess`)
- `p_195490_` (`DataFixer`)
- `p_195491_` (`boolean`)
- `p_195492_` (`BooleanSupplier`)
- `p_323503_` (`RegistryAccess`)
- `p_321704_` (`boolean`)

**Returns:** `private static void`
