# MinecraftServerGui

**Package:** `net.minecraft.server.gui`
**Type:** class
**Extends:** `JComponent`
**Side:** 🖧 Server

## Methods

### showFrameFor

```java
public static MinecraftServerGui showFrameFor(DedicatedServer p_139922_)
```

**Parameters:**

- `p_139922_` (`DedicatedServer`)

**Returns:** `public static MinecraftServerGui`

### windowClosing

```java
public void windowClosing(WindowEvent p_139944_)
```

**Parameters:**

- `p_139944_` (`WindowEvent`)

### MinecraftServerGui

```java
private MinecraftServerGui(DedicatedServer p_139907_)
```

**Parameters:**

- `p_139907_` (`DedicatedServer`)

**Returns:** `private`

### addFinalizer

```java
public void addFinalizer(Runnable p_139910_)
```

**Parameters:**

- `p_139910_` (`Runnable`)

**Returns:** `public void`

### buildInfoPanel

```java
private JComponent buildInfoPanel()
```

**Returns:** `private JComponent`

### buildPlayerPanel

```java
private JComponent buildPlayerPanel()
```

**Returns:** `private JComponent`

### buildChatPanel

```java
private JComponent buildChatPanel()
```

**Returns:** `private JComponent`

### focusGained

```java
public void focusGained(FocusEvent p_139949_)
```

**Parameters:**

- `p_139949_` (`FocusEvent`)

### start

```java
public void start()
```

**Returns:** `public void`

### close

```java
public void close()
```

**Returns:** `public void`

### runFinalizers

```java
void runFinalizers()
```

### print

```java
public void print(JTextArea p_139915_, JScrollPane p_139916_, String p_139917_)
```

**Parameters:**

- `p_139915_` (`JTextArea`)
- `p_139916_` (`JScrollPane`)
- `p_139917_` (`String`)

**Returns:** `public void`
