# OpCommand

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_138080_)
```

**Parameters:**

- `p_138080_` (`CommandDispatcher<CommandSourceStack>`)

**Returns:** `public static void`

### opPlayers

```java
private static int opPlayers(CommandSourceStack p_138089_, Collection<GameProfile> p_138090_)
```

**Parameters:**

- `p_138089_` (`CommandSourceStack`)
- `p_138090_` (`Collection<GameProfile>`)

**Returns:** `private static int`
