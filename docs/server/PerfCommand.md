# PerfCommand

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_180438_)
```

**Parameters:**

- `p_180438_` (`CommandDispatcher<CommandSourceStack>`)

**Returns:** `public static void`

### startProfilingDedicatedServer

```java
private static int startProfilingDedicatedServer(CommandSourceStack p_180442_)
```

**Parameters:**

- `p_180442_` (`CommandSourceStack`)

**Returns:** `private static int`

### whenStopped

```java
> whenStopped()
```

**Returns:** `>`

### saveResults

```java
> saveResults()
```

**Returns:** `>`

### stopProfilingDedicatedServer

```java
private static int stopProfilingDedicatedServer(CommandSourceStack p_180457_)
```

**Parameters:**

- `p_180457_` (`CommandSourceStack`)

**Returns:** `private static int`

### saveResults

```java
private static void saveResults(CommandSourceStack p_180447_, Path p_180448_, MinecraftServer p_180449_)
```

**Parameters:**

- `p_180447_` (`CommandSourceStack`)
- `p_180448_` (`Path`)
- `p_180449_` (`MinecraftServer`)

**Returns:** `private static void`

### whenStopped

```java
private static void whenStopped(CommandSourceStack p_180444_, ProfileResults p_180445_)
```

**Parameters:**

- `p_180444_` (`CommandSourceStack`)
- `p_180445_` (`ProfileResults`)

**Returns:** `private static void`
