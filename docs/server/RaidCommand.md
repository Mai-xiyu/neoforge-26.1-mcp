# RaidCommand

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_180469_, CommandBuildContext p_323666_)
```

**Parameters:**

- `p_180469_` (`CommandDispatcher<CommandSourceStack>`)
- `p_323666_` (`CommandBuildContext`)

**Returns:** `public static void`

### glow

```java
private static int glow(CommandSourceStack p_180473_)
```

**Parameters:**

- `p_180473_` (`CommandSourceStack`)

**Returns:** `private static int`

### setRaidOmenLevel

```java
private static int setRaidOmenLevel(CommandSourceStack p_180475_, int p_180476_)
```

**Parameters:**

- `p_180475_` (`CommandSourceStack`)
- `p_180476_` (`int`)

**Returns:** `private static int`

### spawnLeader

```java
private static int spawnLeader(CommandSourceStack p_180483_)
```

**Parameters:**

- `p_180483_` (`CommandSourceStack`)

**Returns:** `private static int`

### playSound

```java
private static int playSound(CommandSourceStack p_180478_, Component p_180479_)
```

**Parameters:**

- `p_180478_` (`CommandSourceStack`)
- `p_180479_` (`Component`)

**Returns:** `private static int`

### start

```java
private static int start(CommandSourceStack p_180485_, int p_180486_)
```

**Parameters:**

- `p_180485_` (`CommandSourceStack`)
- `p_180486_` (`int`)

**Returns:** `private static int`

### stop

```java
private static int stop(CommandSourceStack p_180490_)
```

**Parameters:**

- `p_180490_` (`CommandSourceStack`)

**Returns:** `private static int`

### check

```java
private static int check(CommandSourceStack p_180494_)
```

**Parameters:**

- `p_180494_` (`CommandSourceStack`)

**Returns:** `private static int`
