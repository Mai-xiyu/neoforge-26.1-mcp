# RecipeCommand

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_138201_)
```

**Parameters:**

- `p_138201_` (`CommandDispatcher<CommandSourceStack>`)

**Returns:** `public static void`

### giveRecipes

```java
private static int giveRecipes(CommandSourceStack p_138207_, Collection<ServerPlayer> p_138208_, Collection<RecipeHolder<?>> p_138209_)
```

**Parameters:**

- `p_138207_` (`CommandSourceStack`)
- `p_138208_` (`Collection<ServerPlayer>`)
- `p_138209_` (`Collection<RecipeHolder<?>>`)

**Returns:** `private static int`

### takeRecipes

```java
private static int takeRecipes(CommandSourceStack p_138213_, Collection<ServerPlayer> p_138214_, Collection<RecipeHolder<?>> p_138215_)
```

**Parameters:**

- `p_138213_` (`CommandSourceStack`)
- `p_138214_` (`Collection<ServerPlayer>`)
- `p_138215_` (`Collection<RecipeHolder<?>>`)

**Returns:** `private static int`
