# ReloadCommand

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### reloadPacks

```java
public static void reloadPacks(Collection<String> p_138236_, CommandSourceStack p_138237_)
```

**Parameters:**

- `p_138236_` (`Collection<String>`)
- `p_138237_` (`CommandSourceStack`)

**Returns:** `public static void`

### discoverNewPacks

```java
private static Collection<String> discoverNewPacks(PackRepository p_138223_, WorldData p_138224_, Collection<String> p_138225_)
```

**Parameters:**

- `p_138223_` (`PackRepository`)
- `p_138224_` (`WorldData`)
- `p_138225_` (`Collection<String>`)

**Returns:** `private static Collection<String>`

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_138227_)
```

**Parameters:**

- `p_138227_` (`CommandDispatcher<CommandSourceStack>`)

**Returns:** `public static void`

### reloadPacks

```java
 reloadPacks()
```

**Returns:** ``
