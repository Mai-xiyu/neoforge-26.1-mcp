# ReturnCommand

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static <T extends ExecutionCommandSource<T>> void register(CommandDispatcher<T> p_282091_)
```

**Parameters:**

- `p_282091_` (`CommandDispatcher<T>`)

**Returns:** `public static <T extends ExecutionCommandSource<T>> void`

### run

```java
public void run(T p_309679_, ContextChain<T> p_309539_, ChainModifiers p_309649_, ExecutionControl<T> p_309599_)
```

**Parameters:**

- `p_309679_` (`T`)
- `p_309539_` (`ContextChain<T>`)
- `p_309649_` (`ChainModifiers`)
- `p_309599_` (`ExecutionControl<T>`)

**Returns:** `public void`

### apply

```java
public void apply(T p_309535_, List<T> p_305925_, ContextChain<T> p_305969_, ChainModifiers p_309676_, ExecutionControl<T> p_305873_)
```

**Parameters:**

- `p_309535_` (`T`)
- `p_305925_` (`List<T>`)
- `p_305969_` (`ContextChain<T>`)
- `p_309676_` (`ChainModifiers`)
- `p_305873_` (`ExecutionControl<T>`)

**Returns:** `public void`

### run

```java
public void run(T p_309669_, ContextChain<T> p_306161_, ChainModifiers p_309665_, ExecutionControl<T> p_306298_)
```

**Parameters:**

- `p_309669_` (`T`)
- `p_306161_` (`ContextChain<T>`)
- `p_309665_` (`ChainModifiers`)
- `p_306298_` (`ExecutionControl<T>`)

**Returns:** `public void`
