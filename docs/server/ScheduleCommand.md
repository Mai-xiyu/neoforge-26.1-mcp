# ScheduleCommand

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_138420_)
```

**Parameters:**

- `p_138420_` (`CommandDispatcher<CommandSourceStack>`)

**Returns:** `public static void`

### schedule

```java
private static int schedule(CommandSourceStack p_138429_, Pair<ResourceLocation, Either<CommandFunction<CommandSourceStack>, Collection<CommandFunction<CommandSourceStack>>>> p_138430_, int p_138431_, boolean p_138432_)
```

**Parameters:**

- `p_138429_` (`CommandSourceStack`)
- `p_138430_` (`Pair<ResourceLocation, Either<CommandFunction<CommandSourceStack>, Collection<CommandFunction<CommandSourceStack>>>>`)
- `p_138431_` (`int`)
- `p_138432_` (`boolean`)

**Returns:** `private static int`

### remove

```java
private static int remove(CommandSourceStack p_138434_, String p_138435_)
```

**Parameters:**

- `p_138434_` (`CommandSourceStack`)
- `p_138435_` (`String`)

**Returns:** `private static int`
