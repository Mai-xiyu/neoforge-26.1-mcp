# TickCommand

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_309001_)
```

**Parameters:**

- `p_309001_` (`CommandDispatcher<CommandSourceStack>`)

**Returns:** `public static void`

### nanosToMilisString

```java
private static String nanosToMilisString(long p_308883_)
```

**Parameters:**

- `p_308883_` (`long`)

**Returns:** `private static String`

### setTickingRate

```java
private static int setTickingRate(CommandSourceStack p_309112_, float p_309104_)
```

**Parameters:**

- `p_309112_` (`CommandSourceStack`)
- `p_309104_` (`float`)

**Returns:** `private static int`

### tickQuery

```java
private static int tickQuery(CommandSourceStack p_309091_)
```

**Parameters:**

- `p_309091_` (`CommandSourceStack`)

**Returns:** `private static int`

### sprint

```java
private static int sprint(CommandSourceStack p_309049_, int p_308892_)
```

**Parameters:**

- `p_309049_` (`CommandSourceStack`)
- `p_308892_` (`int`)

**Returns:** `private static int`

### setFreeze

```java
private static int setFreeze(CommandSourceStack p_309056_, boolean p_309141_)
```

**Parameters:**

- `p_309056_` (`CommandSourceStack`)
- `p_309141_` (`boolean`)

**Returns:** `private static int`

### step

```java
private static int step(CommandSourceStack p_309024_, int p_309080_)
```

**Parameters:**

- `p_309024_` (`CommandSourceStack`)
- `p_309080_` (`int`)

**Returns:** `private static int`

### stopStepping

```java
private static int stopStepping(CommandSourceStack p_308875_)
```

**Parameters:**

- `p_308875_` (`CommandSourceStack`)

**Returns:** `private static int`

### stopSprinting

```java
private static int stopSprinting(CommandSourceStack p_308870_)
```

**Parameters:**

- `p_308870_` (`CommandSourceStack`)

**Returns:** `private static int`
