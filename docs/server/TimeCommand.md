# TimeCommand

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_139072_)
```

**Parameters:**

- `p_139072_` (`CommandDispatcher<CommandSourceStack>`)

**Returns:** `public static void`

### getDayTime

```java
private static int getDayTime(ServerLevel p_139070_)
```

**Parameters:**

- `p_139070_` (`ServerLevel`)

**Returns:** `private static int`

### queryTime

```java
private static int queryTime(CommandSourceStack p_139088_, int p_139089_)
```

**Parameters:**

- `p_139088_` (`CommandSourceStack`)
- `p_139089_` (`int`)

**Returns:** `private static int`

### setTime

```java
public static int setTime(CommandSourceStack p_139078_, int p_139079_)
```

**Parameters:**

- `p_139078_` (`CommandSourceStack`)
- `p_139079_` (`int`)

**Returns:** `public static int`

### addTime

```java
public static int addTime(CommandSourceStack p_139083_, int p_139084_)
```

**Parameters:**

- `p_139083_` (`CommandSourceStack`)
- `p_139084_` (`int`)

**Returns:** `public static int`
