# TitleCommand

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_139103_, CommandBuildContext p_324289_)
```

**Parameters:**

- `p_139103_` (`CommandDispatcher<CommandSourceStack>`)
- `p_324289_` (`CommandBuildContext`)

**Returns:** `public static void`

### clearTitle

```java
private static int clearTitle(CommandSourceStack p_139109_, Collection<ServerPlayer> p_139110_)
```

**Parameters:**

- `p_139109_` (`CommandSourceStack`)
- `p_139110_` (`Collection<ServerPlayer>`)

**Returns:** `private static int`

### resetTitle

```java
private static int resetTitle(CommandSourceStack p_139125_, Collection<ServerPlayer> p_139126_)
```

**Parameters:**

- `p_139125_` (`CommandSourceStack`)
- `p_139126_` (`Collection<ServerPlayer>`)

**Returns:** `private static int`

### showTitle

```java
private static int showTitle(CommandSourceStack p_142781_, Collection<ServerPlayer> p_142782_, Component p_142783_, String p_142784_, Function<Component, Packet<?>> p_142785_)
```

**Parameters:**

- `p_142781_` (`CommandSourceStack`)
- `p_142782_` (`Collection<ServerPlayer>`)
- `p_142783_` (`Component`)
- `p_142784_` (`String`)
- `p_142785_` (`Function<Component, Packet<?>>`)

**Returns:** `private static int`

### setTimes

```java
private static int setTimes(CommandSourceStack p_139112_, Collection<ServerPlayer> p_139113_, int p_139114_, int p_139115_, int p_139116_)
```

**Parameters:**

- `p_139112_` (`CommandSourceStack`)
- `p_139113_` (`Collection<ServerPlayer>`)
- `p_139114_` (`int`)
- `p_139115_` (`int`)
- `p_139116_` (`int`)

**Returns:** `private static int`
