# UserBanListEntry

**Package:** `net.minecraft.server.players`
**Type:** class
**Extends:** `BanListEntry<GameProfile>`
**Side:** 🖧 Server

## Methods

### UserBanListEntry

```java
public UserBanListEntry(GameProfile p_11436_)
```

**Parameters:**

- `p_11436_` (`GameProfile`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### UserBanListEntry

```java
public UserBanListEntry(GameProfile p_11438_, Date p_11439_, String p_11440_, Date p_11441_, String p_11442_)
```

**Parameters:**

- `p_11438_` (`GameProfile`)
- `p_11439_` (`Date`)
- `p_11440_` (`String`)
- `p_11441_` (`Date`)
- `p_11442_` (`String`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### UserBanListEntry

```java
public UserBanListEntry(JsonObject p_11434_)
```

**Parameters:**

- `p_11434_` (`JsonObject`)

**Returns:** `public`

### serialize

```java
protected void serialize(JsonObject p_11444_)
```

**Parameters:**

- `p_11444_` (`JsonObject`)

### getDisplayName

```java
public Component getDisplayName()
```

**Returns:** `Component`
