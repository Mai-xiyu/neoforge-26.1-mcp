# WhitelistCommand

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_139202_)
```

**Parameters:**

- `p_139202_` (`CommandDispatcher<CommandSourceStack>`)

**Returns:** `public static void`

### reload

```java
private static int reload(CommandSourceStack p_139209_)
```

**Parameters:**

- `p_139209_` (`CommandSourceStack`)

**Returns:** `private static int`

### addPlayers

```java
private static int addPlayers(CommandSourceStack p_139211_, Collection<GameProfile> p_139212_)
```

**Parameters:**

- `p_139211_` (`CommandSourceStack`)
- `p_139212_` (`Collection<GameProfile>`)

**Returns:** `private static int`

### removePlayers

```java
private static int removePlayers(CommandSourceStack p_139221_, Collection<GameProfile> p_139222_)
```

**Parameters:**

- `p_139221_` (`CommandSourceStack`)
- `p_139222_` (`Collection<GameProfile>`)

**Returns:** `private static int`

### enableWhitelist

```java
private static int enableWhitelist(CommandSourceStack p_139219_)
```

**Parameters:**

- `p_139219_` (`CommandSourceStack`)

**Returns:** `private static int`

### disableWhitelist

```java
private static int disableWhitelist(CommandSourceStack p_139226_)
```

**Parameters:**

- `p_139226_` (`CommandSourceStack`)

**Returns:** `private static int`

### showList

```java
private static int showList(CommandSourceStack p_139230_)
```

**Parameters:**

- `p_139230_` (`CommandSourceStack`)

**Returns:** `private static int`
