# WorldBorderCommand

**Package:** `net.minecraft.server.commands`
**Type:** class
**Side:** 🖧 Server

## Methods

### register

```java
public static void register(CommandDispatcher<CommandSourceStack> p_139247_)
```

**Parameters:**

- `p_139247_` (`CommandDispatcher<CommandSourceStack>`)

**Returns:** `public static void`

### setDamageBuffer

```java
private static int setDamageBuffer(CommandSourceStack p_139257_, float p_139258_)
```

**Parameters:**

- `p_139257_` (`CommandSourceStack`)
- `p_139258_` (`float`)

**Returns:** `private static int`

### setDamageAmount

```java
private static int setDamageAmount(CommandSourceStack p_139270_, float p_139271_)
```

**Parameters:**

- `p_139270_` (`CommandSourceStack`)
- `p_139271_` (`float`)

**Returns:** `private static int`

### setWarningTime

```java
private static int setWarningTime(CommandSourceStack p_139260_, int p_139261_)
```

**Parameters:**

- `p_139260_` (`CommandSourceStack`)
- `p_139261_` (`int`)

**Returns:** `private static int`

### setWarningDistance

```java
private static int setWarningDistance(CommandSourceStack p_139273_, int p_139274_)
```

**Parameters:**

- `p_139273_` (`CommandSourceStack`)
- `p_139274_` (`int`)

**Returns:** `private static int`

### getSize

```java
private static int getSize(CommandSourceStack p_139251_)
```

**Parameters:**

- `p_139251_` (`CommandSourceStack`)

**Returns:** `private static int`

### setCenter

```java
private static int setCenter(CommandSourceStack p_139263_, Vec2 p_139264_)
```

**Parameters:**

- `p_139263_` (`CommandSourceStack`)
- `p_139264_` (`Vec2`)

**Returns:** `private static int`

### setSize

```java
private static int setSize(CommandSourceStack p_139253_, double p_139254_, long p_139255_)
```

**Parameters:**

- `p_139253_` (`CommandSourceStack`)
- `p_139254_` (`double`)
- `p_139255_` (`long`)

**Returns:** `private static int`
