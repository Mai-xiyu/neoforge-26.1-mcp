# RecipeBook

**Package:** `net.minecraft.stats`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `known` | `Set<ResourceLocation>` |  |
| `highlight` | `Set<ResourceLocation>` |  |

## Methods

### copyOverData

```java
public void copyOverData(RecipeBook p_12686_)
```

**Parameters:**

- `p_12686_` (`RecipeBook`)

**Returns:** `public void`

### add

```java
public void add(RecipeHolder<?> p_300937_)
```

**Parameters:**

- `p_300937_` (`RecipeHolder<?>`)

**Returns:** `public void`

### add

```java
protected void add(ResourceLocation p_12703_)
```

**Parameters:**

- `p_12703_` (`ResourceLocation`)

**Returns:** `protected void`

### contains

```java
public boolean contains(RecipeHolder<?> p_300981_)
```

**Parameters:**

- `p_300981_` (`RecipeHolder<?>`)

**Returns:** `public boolean`

### contains

```java
public boolean contains(ResourceLocation p_12712_)
```

**Parameters:**

- `p_12712_` (`ResourceLocation`)

**Returns:** `public boolean`

### remove

```java
public void remove(RecipeHolder<?> p_301170_)
```

**Parameters:**

- `p_301170_` (`RecipeHolder<?>`)

**Returns:** `public void`

### remove

```java
protected void remove(ResourceLocation p_12716_)
```

**Parameters:**

- `p_12716_` (`ResourceLocation`)

**Returns:** `protected void`

### willHighlight

```java
public boolean willHighlight(RecipeHolder<?> p_300856_)
```

**Parameters:**

- `p_300856_` (`RecipeHolder<?>`)

**Returns:** `public boolean`

### removeHighlight

```java
public void removeHighlight(RecipeHolder<?> p_300963_)
```

**Parameters:**

- `p_300963_` (`RecipeHolder<?>`)

**Returns:** `public void`

### addHighlight

```java
public void addHighlight(RecipeHolder<?> p_300907_)
```

**Parameters:**

- `p_300907_` (`RecipeHolder<?>`)

**Returns:** `public void`

### addHighlight

```java
protected void addHighlight(ResourceLocation p_12720_)
```

**Parameters:**

- `p_12720_` (`ResourceLocation`)

**Returns:** `protected void`

### isOpen

```java
public boolean isOpen(RecipeBookType p_12692_)
```

**Parameters:**

- `p_12692_` (`RecipeBookType`)

**Returns:** `public boolean`

### setOpen

```java
public void setOpen(RecipeBookType p_12694_, boolean p_12695_)
```

**Parameters:**

- `p_12694_` (`RecipeBookType`)
- `p_12695_` (`boolean`)

**Returns:** `public void`

### isFiltering

```java
public boolean isFiltering(RecipeBookMenu<?, ?> p_12690_)
```

**Parameters:**

- `p_12690_` (`RecipeBookMenu<?, ?>`)

**Returns:** `public boolean`

### isFiltering

```java
public boolean isFiltering(RecipeBookType p_12705_)
```

**Parameters:**

- `p_12705_` (`RecipeBookType`)

**Returns:** `public boolean`

### setFiltering

```java
public void setFiltering(RecipeBookType p_12707_, boolean p_12708_)
```

**Parameters:**

- `p_12707_` (`RecipeBookType`)
- `p_12708_` (`boolean`)

**Returns:** `public void`

### setBookSettings

```java
public void setBookSettings(RecipeBookSettings p_12688_)
```

**Parameters:**

- `p_12688_` (`RecipeBookSettings`)

**Returns:** `public void`

### getBookSettings

```java
public RecipeBookSettings getBookSettings()
```

**Returns:** `public RecipeBookSettings`

### setBookSetting

```java
public void setBookSetting(RecipeBookType p_12697_, boolean p_12698_, boolean p_12699_)
```

**Parameters:**

- `p_12697_` (`RecipeBookType`)
- `p_12698_` (`boolean`)
- `p_12699_` (`boolean`)

**Returns:** `public void`
