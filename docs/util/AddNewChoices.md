# AddNewChoices

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### AddNewChoices

```java
public AddNewChoices(Schema p_14628_, String p_14629_, TypeReference p_14630_)
```

**Parameters:**

- `p_14628_` (`Schema`)
- `p_14629_` (`String`)
- `p_14630_` (`TypeReference`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
public TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### cap

```java
<K> private <K> TypeRewriteRule cap(TaggedChoiceType<K> p_14639_, TaggedChoiceType<?> p_14640_)
```

**Parameters:**

- `p_14639_` (`TaggedChoiceType<K>`)
- `p_14640_` (`TaggedChoiceType<?>`)

**Returns:** `private <K> TypeRewriteRule`

### IllegalStateException

```java
throw new IllegalStateException("Could not inject: key type is not the same")
```

**Parameters:**

- `same"` (`"Could not inject: key type is not the`)

**Returns:** `throw new`
