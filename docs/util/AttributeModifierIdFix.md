# AttributeModifierIdFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### AttributeModifierIdFix

```java
public AttributeModifierIdFix(Schema p_350353_)
```

**Parameters:**

- `p_350353_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### fixModifiersTypeWrapper

```java
private static Stream<Dynamic<?>> fixModifiersTypeWrapper(Stream<?> p_350501_)
```

**Parameters:**

- `p_350501_` (`Stream<?>`)

**Returns:** `private static Stream<Dynamic<?>>`

### fixModifiers

```java
private static Stream<Dynamic<?>> fixModifiers(Stream<Dynamic<?>> p_350772_)
```

**Parameters:**

- `p_350772_` (`Stream<Dynamic<?>>`)

**Returns:** `private static Stream<Dynamic<?>>`

### convertModifierForEntity

```java
private static Dynamic<?> convertModifierForEntity(Dynamic<?> p_350703_)
```

**Parameters:**

- `p_350703_` (`Dynamic<?>`)

**Returns:** `private static Dynamic<?>`

### fixItemStackComponents

```java
private static Dynamic<?> fixItemStackComponents(Dynamic<?> p_350317_)
```

**Parameters:**

- `p_350317_` (`Dynamic<?>`)

**Returns:** `private static Dynamic<?>`

### fixAttribute

```java
private static Dynamic<?> fixAttribute(Dynamic<?> p_350325_)
```

**Parameters:**

- `p_350325_` (`Dynamic<?>`)

**Returns:** `private static Dynamic<?>`

### fixEntity

```java
private static Typed<?> fixEntity(Typed<?> p_351052_)
```

**Parameters:**

- `p_351052_` (`Typed<?>`)

**Returns:** `private static Typed<?>`

### uuidFromIntArray

```java
public static UUID uuidFromIntArray(int[] p_350729_)
```

**Parameters:**

- `p_350729_` (`int[]`)

**Returns:** `UUID`
