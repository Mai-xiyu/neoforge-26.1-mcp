# BitStorageAlignFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### BitStorageAlignFix

```java
public BitStorageAlignFix(Schema p_14736_)
```

**Parameters:**

- `p_14736_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### updateHeightmaps

```java
private Typed<?> updateHeightmaps(Typed<?> p_14763_)
```

**Parameters:**

- `p_14763_` (`Typed<?>`)

**Returns:** `private Typed<?>`

### updateSections

```java
private static Typed<?> updateSections(OpticFinder<?> p_14751_, OpticFinder<?> p_14752_, OpticFinder<List<Pair<String, Dynamic<?>>>> p_14753_, Typed<?> p_14754_)
```

**Parameters:**

- `p_14751_` (`OpticFinder<?>`)
- `p_14752_` (`OpticFinder<?>`)
- `p_14753_` (`OpticFinder<List<Pair<String, Dynamic<?>>>>`)
- `p_14754_` (`Typed<?>`)

**Returns:** `private static Typed<?>`

### updateBitStorage

```java
private static Dynamic<?> updateBitStorage(Dynamic<?> p_14777_, Dynamic<?> p_14778_, int p_14779_, int p_14780_)
```

**Parameters:**

- `p_14777_` (`Dynamic<?>`)
- `p_14778_` (`Dynamic<?>`)
- `p_14779_` (`int`)
- `p_14780_` (`int`)

**Returns:** `private static Dynamic<?>`

### addPadding

```java
public static long[] addPadding(int p_14738_, int p_14739_, long[] p_14740_)
```

**Parameters:**

- `p_14738_` (`int`)
- `p_14739_` (`int`)
- `p_14740_` (`long[]`)

**Returns:** `public static long[]`
