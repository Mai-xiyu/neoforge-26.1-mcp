# BlockEntitySignDoubleSidedEditableTextFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `NamedEntityFix`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `FILTERED_CORRECT` | `String` |  |

## Methods

### BlockEntitySignDoubleSidedEditableTextFix

```java
public BlockEntitySignDoubleSidedEditableTextFix(Schema p_277789_, String p_278061_, String p_277403_)
```

**Parameters:**

- `p_277789_` (`Schema`)
- `p_278061_` (`String`)
- `p_277403_` (`String`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### fixTag

```java
<T> private static <T> Dynamic<T> fixTag(Dynamic<T> p_278110_)
```

**Parameters:**

- `p_278110_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### fixFrontTextTag

```java
<T> private static <T> Dynamic<T> fixFrontTextTag(Dynamic<T> p_295403_)
```

**Parameters:**

- `p_295403_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### getLines

```java
<T> private static <T> Stream<Optional<Dynamic<T>>> getLines(Dynamic<T> p_295400_, String p_294843_)
```

**Parameters:**

- `p_295400_` (`Dynamic<T>`)
- `p_294843_` (`String`)

**Returns:** `private static <T> Stream<Optional<Dynamic<T>>>`

### createDefaultText

```java
<T> private static <T> Dynamic<T> createDefaultText(Dynamic<T> p_294259_)
```

**Parameters:**

- `p_294259_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### createEmptyLines

```java
<T> private static <T> Dynamic<T> createEmptyLines(Dynamic<T> p_294420_)
```

**Parameters:**

- `p_294420_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### fix

```java
protected Typed<?> fix(Typed<?> p_277962_)
```

**Parameters:**

- `p_277962_` (`Typed<?>`)

**Returns:** `Typed<?>`
