# ChunkBedBlockEntityInjecterFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### ChunkBedBlockEntityInjecterFix

```java
public ChunkBedBlockEntityInjecterFix(Schema p_184825_, boolean p_184826_)
```

**Parameters:**

- `p_184825_` (`Schema`)
- `p_184826_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
public TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### IllegalStateException

```java
throw new IllegalStateException("Tile entity type is not a list type.")
```

**Parameters:**

- `type."` (`"Tile entity type is not a list`)

**Returns:** `throw new`

### cap

```java
<TE> private <TE> TypeRewriteRule cap(Type<?> p_184834_, ListType<TE> p_184835_)
```

**Parameters:**

- `p_184834_` (`Type<?>`)
- `p_184835_` (`ListType<TE>`)

**Returns:** `private <TE> TypeRewriteRule`
