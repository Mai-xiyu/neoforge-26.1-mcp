# ChunkHeightAndBiomeFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `DATAFIXER_CONTEXT_TAG` | `String` |  |
| `BLOCKS_PER_SECTION` | `int` |  |
| `DEFAULT_BIOME` | `String` |  |

## Methods

### ChunkHeightAndBiomeFix

```java
public ChunkHeightAndBiomeFix(Schema p_184863_)
```

**Parameters:**

- `p_184863_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### predictChunkStatusBeforeSurface

```java
private Dynamic<?> predictChunkStatusBeforeSurface(Dynamic<?> p_184904_, Set<String> p_184905_)
```

**Parameters:**

- `p_184904_` (`Dynamic<?>`)
- `p_184905_` (`Set<String>`)

**Returns:** `private Dynamic<?>`

### getBiomeContainers

```java
private static Dynamic<?>[] getBiomeContainers(Dynamic<?> p_184907_, boolean p_184908_, int p_184909_, MutableBoolean p_184910_)
```

**Parameters:**

- `p_184907_` (`Dynamic<?>`)
- `p_184908_` (`boolean`)
- `p_184909_` (`int`)
- `p_184910_` (`MutableBoolean`)

**Returns:** `private static Dynamic<?>[]`

### getOldBiome

```java
private static int getOldBiome(int[] p_184949_, int p_184950_)
```

**Parameters:**

- `p_184949_` (`int[]`)
- `p_184950_` (`int`)

**Returns:** `private static int`

### updateChunkTag

```java
private static Dynamic<?> updateChunkTag(Dynamic<?> p_184912_, boolean p_184913_, boolean p_184914_, boolean p_184915_, Supplier<ChunkProtoTickListFix.PoorMansPalettedContainer> p_184916_)
```

**Parameters:**

- `p_184912_` (`Dynamic<?>`)
- `p_184913_` (`boolean`)
- `p_184914_` (`boolean`)
- `p_184915_` (`boolean`)
- `p_184916_` (`Supplier<ChunkProtoTickListFix.PoorMansPalettedContainer>`)

**Returns:** `private static Dynamic<?>`

### updateCarvingMasks

```java
return updateCarvingMasks()
```

**Returns:** `return`

### updateCarvingMasks

```java
return updateCarvingMasks()
```

**Returns:** `return`

### shiftUpgradeData

```java
<T> private static <T> Dynamic<T> shiftUpgradeData(Dynamic<T> p_196591_)
```

**Parameters:**

- `p_196591_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### updateCarvingMasks

```java
private static Dynamic<?> updateCarvingMasks(Dynamic<?> p_184888_, int p_184889_, int p_184890_)
```

**Parameters:**

- `p_184888_` (`Dynamic<?>`)
- `p_184889_` (`int`)
- `p_184890_` (`int`)

**Returns:** `private static Dynamic<?>`

### addPaddingEntries

```java
private static Dynamic<?> addPaddingEntries(Dynamic<?> p_184901_, String p_184902_)
```

**Parameters:**

- `p_184901_` (`Dynamic<?>`)
- `p_184902_` (`String`)

**Returns:** `private static Dynamic<?>`

### updateHeightmaps

```java
private static Dynamic<?> updateHeightmaps(Dynamic<?> p_184886_)
```

**Parameters:**

- `p_184886_` (`Dynamic<?>`)

**Returns:** `private static Dynamic<?>`

### getFixedHeightmap

```java
private static Dynamic<?> getFixedHeightmap(Dynamic<?> p_184957_)
```

**Parameters:**

- `p_184957_` (`Dynamic<?>`)

**Returns:** `private static Dynamic<?>`

### makeBiomeContainer

```java
private static Dynamic<?> makeBiomeContainer(Dynamic<?> p_184895_, Int2IntFunction p_184896_)
```

**Parameters:**

- `p_184895_` (`Dynamic<?>`)
- `p_184896_` (`Int2IntFunction`)

**Returns:** `private static Dynamic<?>`

### makePalettedContainer

```java
return makePalettedContainer()
```

**Returns:** `return`

### makePalettedContainer

```java
return makePalettedContainer()
```

**Returns:** `return`

### makePalettedContainer

```java
private static Dynamic<?> makePalettedContainer(Dynamic<?> p_184970_)
```

**Parameters:**

- `p_184970_` (`Dynamic<?>`)

**Returns:** `private static Dynamic<?>`

### makePalettedContainer

```java
private static Dynamic<?> makePalettedContainer(Dynamic<?> p_184892_, Dynamic<?> p_184893_)
```

**Parameters:**

- `p_184892_` (`Dynamic<?>`)
- `p_184893_` (`Dynamic<?>`)

**Returns:** `private static Dynamic<?>`

### makeOptimizedPalettedContainer

```java
private static Dynamic<?> makeOptimizedPalettedContainer(Dynamic<?> p_184959_, Dynamic<?> p_184960_)
```

**Parameters:**

- `p_184959_` (`Dynamic<?>`)
- `p_184960_` (`Dynamic<?>`)

**Returns:** `private static Dynamic<?>`

### makePalettedContainer

```java
return makePalettedContainer()
```

**Returns:** `return`

### makePalettedContainer

```java
return makePalettedContainer()
```

**Returns:** `return`

### padPaletteEntries

```java
private static Dynamic<?> padPaletteEntries(Dynamic<?> p_196593_, Dynamic<?> p_196594_, List<Dynamic<?>> p_196595_)
```

**Parameters:**

- `p_196593_` (`Dynamic<?>`)
- `p_196594_` (`Dynamic<?>`)
- `p_196595_` (`List<Dynamic<?>>`)

**Returns:** `private static Dynamic<?>`

### ceillog2

```java
public static int ceillog2(int p_184866_)
```

**Parameters:**

- `p_184866_` (`int`)

**Returns:** `public static int`
