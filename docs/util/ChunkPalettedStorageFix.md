# ChunkPalettedStorageFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### mapSkull

```java
 mapSkull()
```

**Returns:** ``

### mapSkull

```java
 mapSkull()
```

**Returns:** ``

### mapSkull

```java
 mapSkull()
```

**Returns:** ``

### mapSkull

```java
 mapSkull()
```

**Returns:** ``

### mapSkull

```java
 mapSkull()
```

**Returns:** ``

### mapSkull

```java
 mapSkull()
```

**Returns:** ``

### mapDoor

```java
 mapDoor()
```

**Returns:** ``

### mapDoor

```java
 mapDoor()
```

**Returns:** ``

### mapDoor

```java
 mapDoor()
```

**Returns:** ``

### mapDoor

```java
 mapDoor()
```

**Returns:** ``

### mapDoor

```java
 mapDoor()
```

**Returns:** ``

### mapDoor

```java
 mapDoor()
```

**Returns:** ``

### mapDoor

```java
 mapDoor()
```

**Returns:** ``

### ChunkPalettedStorageFix

```java
public ChunkPalettedStorageFix(Schema p_15058_, boolean p_15059_)
```

**Parameters:**

- `p_15058_` (`Schema`)
- `p_15059_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### mapSkull

```java
private static void mapSkull(Map<String, Dynamic<?>> p_15078_, int p_15079_, String p_15080_, String p_15081_)
```

**Parameters:**

- `p_15078_` (`Map<String, Dynamic<?>>`)
- `p_15079_` (`int`)
- `p_15080_` (`String`)
- `p_15081_` (`String`)

**Returns:** `private static void`

### mapDoor

```java
private static void mapDoor(Map<String, Dynamic<?>> p_15083_, String p_15084_, int p_15085_)
```

**Parameters:**

- `p_15083_` (`Map<String, Dynamic<?>>`)
- `p_15084_` (`String`)
- `p_15085_` (`int`)

**Returns:** `private static void`

### addBeds

```java
private static void addBeds(Map<String, Dynamic<?>> p_15074_, int p_15075_, String p_15076_)
```

**Parameters:**

- `p_15074_` (`Map<String, Dynamic<?>>`)
- `p_15075_` (`int`)
- `p_15076_` (`String`)

**Returns:** `private static void`

### addBanners

```java
private static void addBanners(Map<String, Dynamic<?>> p_15097_, int p_15098_, String p_15099_)
```

**Parameters:**

- `p_15097_` (`Map<String, Dynamic<?>>`)
- `p_15098_` (`int`)
- `p_15099_` (`String`)

**Returns:** `private static void`

### getName

```java
public static String getName(Dynamic<?> p_15065_)
```

**Parameters:**

- `p_15065_` (`Dynamic<?>`)

**Returns:** `public static String`

### getProperty

```java
public static String getProperty(Dynamic<?> p_15067_, String p_15068_)
```

**Parameters:**

- `p_15067_` (`Dynamic<?>`)
- `p_15068_` (`String`)

**Returns:** `public static String`

### idFor

```java
public static int idFor(CrudeIncrementalIntIdentityHashBiMap<Dynamic<?>> p_15062_, Dynamic<?> p_15063_)
```

**Parameters:**

- `p_15062_` (`CrudeIncrementalIntIdentityHashBiMap<Dynamic<?>>`)
- `p_15063_` (`Dynamic<?>`)

**Returns:** `public static int`

### fix

```java
private Dynamic<?> fix(Dynamic<?> p_15093_)
```

**Parameters:**

- `p_15093_` (`Dynamic<?>`)

**Returns:** `private Dynamic<?>`

### makeRule

```java
public TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### getSideMask

```java
public static int getSideMask(boolean p_15087_, boolean p_15088_, boolean p_15089_, boolean p_15090_)
```

**Parameters:**

- `p_15087_` (`boolean`)
- `p_15088_` (`boolean`)
- `p_15089_` (`boolean`)
- `p_15090_` (`boolean`)

**Returns:** `public static int`

### DataLayer

```java
public DataLayer()
```

**Returns:** `public`

### DataLayer

```java
public DataLayer(byte[] p_15132_)
```

**Parameters:**

- `p_15132_` (`byte[]`)

**Returns:** `public`

### IllegalArgumentException

```java
throw new IllegalArgumentException("ChunkNibbleArrays should be 2048 bytes not: " + p_15132_.length)
```

**Parameters:**

- `p_15132_.length` (`"ChunkNibbleArrays should be 2048 bytes not: " +`)

**Returns:** `throw new`

### get

```java
public int get(int p_15136_, int p_15137_, int p_15138_)
```

**Parameters:**

- `p_15136_` (`int`)
- `p_15137_` (`int`)
- `p_15138_` (`int`)

**Returns:** `public int`

### isFirst

```java
private boolean isFirst(int p_15134_)
```

**Parameters:**

- `p_15134_` (`int`)

**Returns:** `private boolean`

### getPosition

```java
private int getPosition(int p_15140_)
```

**Parameters:**

- `p_15140_` (`int`)

**Returns:** `private int`

### EAST

```java
, EAST()
```

**Returns:** `,`

### Direction

```java
private Direction(ChunkPalettedStorageFix.Direction.AxisDirection p_15154_, ChunkPalettedStorageFix.Direction.Axis p_15155_)
```

**Parameters:**

- `p_15154_` (`ChunkPalettedStorageFix.Direction.AxisDirection`)
- `p_15155_` (`ChunkPalettedStorageFix.Direction.Axis`)

**Returns:** `private`

### getAxisDirection

```java
public ChunkPalettedStorageFix.Direction.AxisDirection getAxisDirection()
```

**Returns:** `public ChunkPalettedStorageFix.Direction.AxisDirection`

### getAxis

```java
public ChunkPalettedStorageFix.Direction.Axis getAxis()
```

**Returns:** `public ChunkPalettedStorageFix.Direction.Axis`

### NEGATIVE

```java
, NEGATIVE()
```

**Returns:** `,`

### AxisDirection

```java
private AxisDirection(int p_15180_)
```

**Parameters:**

- `p_15180_` (`int`)

**Returns:** `private`

### getStep

```java
public int getStep()
```

**Returns:** `public int`

### Section

```java
public Section(Dynamic<?> p_15195_)
```

**Parameters:**

- `p_15195_` (`Dynamic<?>`)

**Returns:** `public`

### getBlock

```java
public Dynamic<?> getBlock(int p_15198_)
```

**Parameters:**

- `p_15198_` (`int`)

**Returns:** `public Dynamic<?>`

### setBlock

```java
public void setBlock(int p_15203_, Dynamic<?> p_15204_)
```

**Parameters:**

- `p_15203_` (`int`)
- `p_15204_` (`Dynamic<?>`)

**Returns:** `public void`

### upgrade

```java
public int upgrade(int p_15210_)
```

**Parameters:**

- `p_15210_` (`int`)

**Returns:** `public int`

### addFix

```java
private void addFix(int p_15200_, int p_15201_)
```

**Parameters:**

- `p_15200_` (`int`)
- `p_15201_` (`int`)

**Returns:** `private void`

### write

```java
public Dynamic<?> write()
```

**Returns:** `public Dynamic<?>`

### UpgradeChunk

```java
public UpgradeChunk(Dynamic<?> p_15222_)
```

**Parameters:**

- `p_15222_` (`Dynamic<?>`)

**Returns:** `public`

### relative

```java
public static int relative(int p_15227_, ChunkPalettedStorageFix.Direction p_15228_)
```

**Parameters:**

- `p_15227_` (`int`)
- `p_15228_` (`ChunkPalettedStorageFix.Direction`)

**Returns:** `public static int`

### setBlock

```java
private void setBlock(int p_15230_, Dynamic<?> p_15231_)
```

**Parameters:**

- `p_15230_` (`int`)
- `p_15231_` (`Dynamic<?>`)

**Returns:** `private void`

### getBlock

```java
public Dynamic<?> getBlock(int p_15225_)
```

**Parameters:**

- `p_15225_` (`int`)

**Returns:** `public Dynamic<?>`

### write

```java
public Dynamic<?> write()
```

**Returns:** `public Dynamic<?>`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `Direction` | enum |  |
| `Axis` | enum |  |
| `AxisDirection` | enum |  |
