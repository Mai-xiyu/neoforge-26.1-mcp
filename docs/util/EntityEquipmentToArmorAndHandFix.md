# EntityEquipmentToArmorAndHandFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### EntityEquipmentToArmorAndHandFix

```java
public EntityEquipmentToArmorAndHandFix(Schema p_15417_, boolean p_15418_)
```

**Parameters:**

- `p_15417_` (`Schema`)
- `p_15418_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
public TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### cap

```java
<IS> private <IS> TypeRewriteRule cap(Type<IS> p_15427_)
```

**Parameters:**

- `p_15427_` (`Type<IS>`)

**Returns:** `private <IS> TypeRewriteRule`
