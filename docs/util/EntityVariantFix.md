# EntityVariantFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `NamedEntityFix`

## Methods

### EntityVariantFix

```java
public EntityVariantFix(Schema p_216623_, String p_216624_, TypeReference p_216625_, String p_216626_, String p_216627_, IntFunction<String> p_216628_)
```

**Parameters:**

- `p_216623_` (`Schema`)
- `p_216624_` (`String`)
- `p_216625_` (`TypeReference`)
- `p_216626_` (`String`)
- `p_216627_` (`String`)
- `p_216628_` (`IntFunction<String>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### updateAndRename

```java
<T> private static <T> Dynamic<T> updateAndRename(Dynamic<T> p_216637_, String p_216638_, String p_216639_, Function<Dynamic<T>, Dynamic<T>> p_216640_)
```

**Parameters:**

- `p_216637_` (`Dynamic<T>`)
- `p_216638_` (`String`)
- `p_216639_` (`String`)
- `p_216640_` (`Function<Dynamic<T>, Dynamic<T>>`)

**Returns:** `private static <T> Dynamic<T>`

### fix

```java
protected Typed<?> fix(Typed<?> p_216630_)
```

**Parameters:**

- `p_216630_` (`Typed<?>`)

**Returns:** `Typed<?>`
