# FeatureFlagRemoveFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### FeatureFlagRemoveFix

```java
public FeatureFlagRemoveFix(Schema p_277930_, String p_277628_, Set<String> p_277886_)
```

**Parameters:**

- `p_277930_` (`Schema`)
- `p_277628_` (`String`)
- `p_277886_` (`Set<String>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### fixTag

```java
<T> private <T> Dynamic<T> fixTag(Dynamic<T> p_277583_)
```

**Parameters:**

- `p_277583_` (`Dynamic<T>`)

**Returns:** `private <T> Dynamic<T>`
