# ForcePoiRebuild

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### ForcePoiRebuild

```java
public ForcePoiRebuild(Schema p_15821_, boolean p_15822_)
```

**Parameters:**

- `p_15821_` (`Schema`)
- `p_15822_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### IllegalStateException

```java
throw new IllegalStateException("Poi type is not what was expected.")
```

**Parameters:**

- `expected."` (`"Poi type is not what was`)

**Returns:** `throw new`

### cap

```java
<T> private static <T> Dynamic<T> cap(Dynamic<T> p_15826_)
```

**Parameters:**

- `p_15826_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`
