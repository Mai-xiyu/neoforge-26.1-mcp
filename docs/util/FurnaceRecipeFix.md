# FurnaceRecipeFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### FurnaceRecipeFix

```java
public FurnaceRecipeFix(Schema p_15837_, boolean p_15838_)
```

**Parameters:**

- `p_15837_` (`Schema`)
- `p_15838_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### cap

```java
<R> private <R> TypeRewriteRule cap(Type<R> p_15850_)
```

**Parameters:**

- `p_15850_` (`Type<R>`)

**Returns:** `private <R> TypeRewriteRule`

### updateFurnaceContents

```java
<R> private <R> Typed<?> updateFurnaceContents(Type<R> p_15852_, Type<Pair<Either<Pair<List<Pair<R, Integer>>, Dynamic<?>>, Unit>, Dynamic<?>>> p_15853_, Typed<?> p_15854_)
```

**Parameters:**

- `p_15852_` (`Type<R>`)
- `p_15853_` (`Type<Pair<Either<Pair<List<Pair<R, Integer>>, Dynamic<?>>, Unit>, Dynamic<?>>>`)
- `p_15854_` (`Typed<?>`)

**Returns:** `private <R> Typed<?>`
