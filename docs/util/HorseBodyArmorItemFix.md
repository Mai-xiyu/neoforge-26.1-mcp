# HorseBodyArmorItemFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `NamedEntityWriteReadFix`

## Methods

### HorseBodyArmorItemFix

```java
public HorseBodyArmorItemFix(Schema p_323857_, String p_324552_, String p_324464_, boolean p_342009_)
```

**Parameters:**

- `p_323857_` (`Schema`)
- `p_324552_` (`String`)
- `p_324464_` (`String`)
- `p_342009_` (`boolean`)

**Returns:** `public`

### super

```java
 super("Horse armor fix for " + p_324552_)
```

**Parameters:**

- `p_324552_` (`"Horse armor fix for " +`)

**Returns:** ``

### fix

```java
protected <T> Dynamic<T> fix(Dynamic<T> p_324321_)
```

**Parameters:**

- `p_324321_` (`Dynamic<T>`)

**Returns:** `Dynamic<T>`
