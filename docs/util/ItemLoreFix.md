# ItemLoreFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### ItemLoreFix

```java
public ItemLoreFix(Schema p_15958_, boolean p_15959_)
```

**Parameters:**

- `p_15958_` (`Schema`)
- `p_15959_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### fixLoreList

```java
<T> private static <T> Stream<Dynamic<T>> fixLoreList(Stream<Dynamic<T>> p_15970_)
```

**Parameters:**

- `p_15970_` (`Stream<Dynamic<T>>`)

**Returns:** `private static <T> Stream<Dynamic<T>>`
