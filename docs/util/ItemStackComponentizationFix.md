# ItemStackComponentizationFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### ItemStackComponentizationFix

```java
public ItemStackComponentizationFix(Schema p_331673_)
```

**Parameters:**

- `p_331673_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### fixItemStack

```java
private static void fixItemStack(ItemStackComponentizationFix.ItemStackData p_332167_, Dynamic<?> p_330498_)
```

**Parameters:**

- `p_332167_` (`ItemStackComponentizationFix.ItemStackData`)
- `p_330498_` (`Dynamic<?>`)

**Returns:** `private static void`

### fixAdventureModeChecks

```java
 fixAdventureModeChecks()
```

**Returns:** ``

### fixAttributeModifiers

```java
 fixAttributeModifiers()
```

**Returns:** ``

### fixPotionContents

```java
 fixPotionContents()
```

**Returns:** ``

### fixWritableBook

```java
 fixWritableBook()
```

**Returns:** ``

### fixWrittenBook

```java
 fixWrittenBook()
```

**Returns:** ``

### fixBucketedMobData

```java
 fixBucketedMobData()
```

**Returns:** ``

### fixLodestoneTracker

```java
 fixLodestoneTracker()
```

**Returns:** ``

### fixFireworkRocket

```java
 fixFireworkRocket()
```

**Returns:** ``

### fixFireworkStar

```java
 fixFireworkStar()
```

**Returns:** ``

### fixBlockStateTag

```java
private static Dynamic<?> fixBlockStateTag(Dynamic<?> p_332829_)
```

**Parameters:**

- `p_332829_` (`Dynamic<?>`)

**Returns:** `private static Dynamic<?>`

### fixDisplay

```java
private static Dynamic<?> fixDisplay(ItemStackComponentizationFix.ItemStackData p_331302_, Dynamic<?> p_331703_, int p_331793_)
```

**Parameters:**

- `p_331302_` (`ItemStackComponentizationFix.ItemStackData`)
- `p_331703_` (`Dynamic<?>`)
- `p_331793_` (`int`)

**Returns:** `private static Dynamic<?>`

### fixBlockEntityTag

```java
<T> private static <T> Dynamic<T> fixBlockEntityTag(ItemStackComponentizationFix.ItemStackData p_330721_, Dynamic<T> p_331427_, String p_331421_)
```

**Parameters:**

- `p_330721_` (`ItemStackComponentizationFix.ItemStackData`)
- `p_331427_` (`Dynamic<T>`)
- `p_331421_` (`String`)

**Returns:** `private static <T> Dynamic<T>`

### fixEnchantments

```java
private static void fixEnchantments(ItemStackComponentizationFix.ItemStackData p_331903_, Dynamic<?> p_330744_, String p_331654_, String p_331804_, boolean p_331955_)
```

**Parameters:**

- `p_331903_` (`ItemStackComponentizationFix.ItemStackData`)
- `p_330744_` (`Dynamic<?>`)
- `p_331654_` (`String`)
- `p_331804_` (`String`)
- `p_331955_` (`boolean`)

**Returns:** `private static void`

### parseEnchantment

```java
private static Optional<Pair<String, Integer>> parseEnchantment(Dynamic<?> p_332205_)
```

**Parameters:**

- `p_332205_` (`Dynamic<?>`)

**Returns:** `private static Optional<Pair<String, Integer>>`

### fixAdventureModeChecks

```java
private static void fixAdventureModeChecks(ItemStackComponentizationFix.ItemStackData p_332034_, Dynamic<?> p_332091_, int p_331042_)
```

**Parameters:**

- `p_332034_` (`ItemStackComponentizationFix.ItemStackData`)
- `p_332091_` (`Dynamic<?>`)
- `p_331042_` (`int`)

**Returns:** `private static void`

### fixBlockStatePredicates

```java
private static void fixBlockStatePredicates(ItemStackComponentizationFix.ItemStackData p_331487_, Dynamic<?> p_331061_, String p_330321_, String p_332110_, boolean p_332166_)
```

**Parameters:**

- `p_331487_` (`ItemStackComponentizationFix.ItemStackData`)
- `p_331061_` (`Dynamic<?>`)
- `p_330321_` (`String`)
- `p_332110_` (`String`)
- `p_332166_` (`boolean`)

**Returns:** `private static void`

### fixBlockStatePredicate

```java
private static Dynamic<?> fixBlockStatePredicate(Dynamic<?> p_331862_, String p_332123_)
```

**Parameters:**

- `p_331862_` (`Dynamic<?>`)
- `p_332123_` (`String`)

**Returns:** `private static Dynamic<?>`

### fixAttributeModifiers

```java
private static void fixAttributeModifiers(ItemStackComponentizationFix.ItemStackData p_330771_, Dynamic<?> p_331387_, int p_330887_)
```

**Parameters:**

- `p_330771_` (`ItemStackComponentizationFix.ItemStackData`)
- `p_331387_` (`Dynamic<?>`)
- `p_330887_` (`int`)

**Returns:** `private static void`

### fixAttributeModifier

```java
private static Dynamic<?> fixAttributeModifier(Dynamic<?> p_331035_)
```

**Parameters:**

- `p_331035_` (`Dynamic<?>`)

**Returns:** `private static Dynamic<?>`

### fixMapDecoration

```java
private static Pair<Dynamic<?>, Dynamic<?>> fixMapDecoration(Dynamic<?> p_332095_)
```

**Parameters:**

- `p_332095_` (`Dynamic<?>`)

**Returns:** `private static Pair<Dynamic<?>, Dynamic<?>>`

### fixMapDecorationType

```java
private static String fixMapDecorationType(int p_330596_)
```

**Parameters:**

- `p_330596_` (`int`)

**Returns:** `private static String`

### fixPotionContents

```java
private static void fixPotionContents(ItemStackComponentizationFix.ItemStackData p_330477_, Dynamic<?> p_330723_)
```

**Parameters:**

- `p_330477_` (`ItemStackComponentizationFix.ItemStackData`)
- `p_330723_` (`Dynamic<?>`)

**Returns:** `private static void`

### fixWritableBook

```java
private static void fixWritableBook(ItemStackComponentizationFix.ItemStackData p_330306_, Dynamic<?> p_331455_)
```

**Parameters:**

- `p_330306_` (`ItemStackComponentizationFix.ItemStackData`)
- `p_331455_` (`Dynamic<?>`)

**Returns:** `private static void`

### fixWrittenBook

```java
private static void fixWrittenBook(ItemStackComponentizationFix.ItemStackData p_330209_, Dynamic<?> p_332113_)
```

**Parameters:**

- `p_330209_` (`ItemStackComponentizationFix.ItemStackData`)
- `p_332113_` (`Dynamic<?>`)

**Returns:** `private static void`

### createFilteredText

```java
private static Dynamic<?> createFilteredText(Dynamic<?> p_331589_, String p_330423_, Optional<String> p_330385_)
```

**Parameters:**

- `p_331589_` (`Dynamic<?>`)
- `p_330423_` (`String`)
- `p_330385_` (`Optional<String>`)

**Returns:** `private static Dynamic<?>`

### fixBucketedMobData

```java
private static void fixBucketedMobData(ItemStackComponentizationFix.ItemStackData p_331570_, Dynamic<?> p_330855_)
```

**Parameters:**

- `p_331570_` (`ItemStackComponentizationFix.ItemStackData`)
- `p_330855_` (`Dynamic<?>`)

**Returns:** `private static void`

### fixLodestoneTracker

```java
private static void fixLodestoneTracker(ItemStackComponentizationFix.ItemStackData p_332824_, Dynamic<?> p_332755_)
```

**Parameters:**

- `p_332824_` (`ItemStackComponentizationFix.ItemStackData`)
- `p_332755_` (`Dynamic<?>`)

**Returns:** `private static void`

### fixFireworkStar

```java
private static void fixFireworkStar(ItemStackComponentizationFix.ItemStackData p_330447_)
```

**Parameters:**

- `p_330447_` (`ItemStackComponentizationFix.ItemStackData`)

**Returns:** `private static void`

### fixFireworkRocket

```java
private static void fixFireworkRocket(ItemStackComponentizationFix.ItemStackData p_330858_)
```

**Parameters:**

- `p_330858_` (`ItemStackComponentizationFix.ItemStackData`)

**Returns:** `private static void`

### fixFireworkExplosion

```java
private static Dynamic<?> fixFireworkExplosion(Dynamic<?> p_332063_)
```

**Parameters:**

- `p_332063_` (`Dynamic<?>`)

**Returns:** `private static Dynamic<?>`

### fixProfile

```java
public static Dynamic<?> fixProfile(Dynamic<?> p_330375_)
```

**Parameters:**

- `p_330375_` (`Dynamic<?>`)

**Returns:** `public static Dynamic<?>`

### isValidPlayerName

```java
private static boolean isValidPlayerName(String p_332666_)
```

**Parameters:**

- `p_332666_` (`String`)

**Returns:** `private static boolean`

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### fixItemStack

```java
 fixItemStack()
```

**Returns:** ``

### ItemStackData

```java
private ItemStackData(String p_330523_, int p_331147_, Dynamic<?> p_331630_)
```

**Parameters:**

- `p_330523_` (`String`)
- `p_331147_` (`int`)
- `p_331630_` (`Dynamic<?>`)

**Returns:** `private`

### read

```java
public static Optional<ItemStackComponentizationFix.ItemStackData> read(Dynamic<?> p_330273_)
```

**Parameters:**

- `p_330273_` (`Dynamic<?>`)

**Returns:** `public static Optional<ItemStackComponentizationFix.ItemStackData>`

### removeTag

```java
public OptionalDynamic<?> removeTag(String p_330602_)
```

**Parameters:**

- `p_330602_` (`String`)

**Returns:** `public OptionalDynamic<?>`

### setComponent

```java
public void setComponent(String p_330861_, Dynamic<?> p_331399_)
```

**Parameters:**

- `p_330861_` (`String`)
- `p_331399_` (`Dynamic<?>`)

**Returns:** `public void`

### setComponent

```java
public void setComponent(String p_331000_, OptionalDynamic<?> p_332145_)
```

**Parameters:**

- `p_331000_` (`String`)
- `p_332145_` (`OptionalDynamic<?>`)

**Returns:** `public void`

### moveTagInto

```java
public Dynamic<?> moveTagInto(String p_330240_, Dynamic<?> p_330972_, String p_331818_)
```

**Parameters:**

- `p_330240_` (`String`)
- `p_330972_` (`Dynamic<?>`)
- `p_331818_` (`String`)

**Returns:** `public Dynamic<?>`

### moveTagToComponent

```java
public void moveTagToComponent(String p_331198_, String p_330485_, Dynamic<?> p_330981_)
```

**Parameters:**

- `p_331198_` (`String`)
- `p_330485_` (`String`)
- `p_330981_` (`Dynamic<?>`)

**Returns:** `public void`

### moveTagToComponent

```java
public void moveTagToComponent(String p_330961_, String p_330509_)
```

**Parameters:**

- `p_330961_` (`String`)
- `p_330509_` (`String`)

**Returns:** `public void`

### fixSubTag

```java
public void fixSubTag(String p_330776_, boolean p_330566_, UnaryOperator<Dynamic<?>> p_330246_)
```

**Parameters:**

- `p_330776_` (`String`)
- `p_330566_` (`boolean`)
- `p_330246_` (`UnaryOperator<Dynamic<?>>`)

**Returns:** `public void`

### write

```java
public Dynamic<?> write()
```

**Returns:** `public Dynamic<?>`

### mergeRemainder

```java
return mergeRemainder()
```

**Returns:** `return`

### mergeRemainder

```java
<T> private static <T> Dynamic<T> mergeRemainder(Dynamic<T> p_331175_, Dynamic<?> p_330435_)
```

**Parameters:**

- `p_331175_` (`Dynamic<T>`)
- `p_330435_` (`Dynamic<?>`)

**Returns:** `private static <T> Dynamic<T>`

### is

```java
public boolean is(String p_330700_)
```

**Parameters:**

- `p_330700_` (`String`)

**Returns:** `public boolean`

### is

```java
public boolean is(Set<String> p_330784_)
```

**Parameters:**

- `p_330784_` (`Set<String>`)

**Returns:** `public boolean`

### hasComponent

```java
public boolean hasComponent(String p_332677_)
```

**Parameters:**

- `p_332677_` (`String`)

**Returns:** `public boolean`
