# JsonEventLogReader

**Package:** `net.minecraft.util.eventlog`
**Type:** interface<T>
**Extends:** `Closeable`

## Methods

### create

```java
<T> static <T> JsonEventLogReader<T> create(Codec<T> p_261600_, Reader p_261836_)
```

**Parameters:**

- `p_261600_` (`Codec<T>`)
- `p_261836_` (`Reader`)

**Returns:** `static <T> JsonEventLogReader<T>`

### next

```java
public T next()
```

**Returns:** `T`

### IOException

```java
throw new IOException()
```

**Returns:** `throw new`

### close

```java
public void close()
```

### next

```java
T next()
```

**Returns:** `T`
