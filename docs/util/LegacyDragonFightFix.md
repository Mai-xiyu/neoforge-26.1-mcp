# LegacyDragonFightFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### LegacyDragonFightFix

```java
public LegacyDragonFightFix(Schema p_289761_)
```

**Parameters:**

- `p_289761_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### fixDragonFight

```java
<T> private static <T> Dynamic<T> fixDragonFight(Dynamic<T> p_325915_)
```

**Parameters:**

- `p_325915_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`
