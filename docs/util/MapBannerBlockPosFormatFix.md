# MapBannerBlockPosFormatFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### MapBannerBlockPosFormatFix

```java
public MapBannerBlockPosFormatFix(Schema p_326011_)
```

**Parameters:**

- `p_326011_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### fixMapSavedData

```java
<T> private static <T> Dynamic<T> fixMapSavedData(Dynamic<T> p_326259_)
```

**Parameters:**

- `p_326259_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`
