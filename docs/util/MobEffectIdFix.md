# MobEffectIdFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### MobEffectIdFix

```java
public MobEffectIdFix(Schema p_298197_)
```

**Parameters:**

- `p_298197_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### getAndConvertMobEffectId

```java
<T> private static <T> Optional<Dynamic<T>> getAndConvertMobEffectId(Dynamic<T> p_299296_, String p_298445_)
```

**Parameters:**

- `p_299296_` (`Dynamic<T>`)
- `p_298445_` (`String`)

**Returns:** `private static <T> Optional<Dynamic<T>>`

### updateMobEffectIdField

```java
<T> private static <T> Dynamic<T> updateMobEffectIdField(Dynamic<T> p_298948_, String p_299234_, Dynamic<T> p_298832_, String p_298487_)
```

**Parameters:**

- `p_298948_` (`Dynamic<T>`)
- `p_299234_` (`String`)
- `p_298832_` (`Dynamic<T>`)
- `p_298487_` (`String`)

**Returns:** `private static <T> Dynamic<T>`

### updateMobEffectIdField

```java
<T> private static <T> Dynamic<T> updateMobEffectIdField(Dynamic<T> p_299001_, String p_298420_, String p_299179_)
```

**Parameters:**

- `p_299001_` (`Dynamic<T>`)
- `p_298420_` (`String`)
- `p_299179_` (`String`)

**Returns:** `private static <T> Dynamic<T>`

### updateMobEffectIdField

```java
return updateMobEffectIdField()
```

**Returns:** `return`

### updateMobEffectIdFieldConsideringForge

```java
<T> private static <T> Dynamic<T> updateMobEffectIdFieldConsideringForge(Dynamic<T> p_298948_, String p_299234_, Dynamic<T> p_298832_, String p_298487_, String forgeFieldId)
```

**Parameters:**

- `p_298948_` (`Dynamic<T>`)
- `p_299234_` (`String`)
- `p_298832_` (`Dynamic<T>`)
- `p_298487_` (`String`)
- `forgeFieldId` (`String`)

**Returns:** `private static <T> Dynamic<T>`

### updateMobEffectIdField

```java
return updateMobEffectIdField()
```

**Returns:** `return`

### setFieldIfPresent

```java
<T> private static <T> Dynamic<T> setFieldIfPresent(Dynamic<T> dynamic, String s, Optional<Dynamic<T>> optional)
```

**Parameters:**

- `dynamic` (`Dynamic<T>`)
- `s` (`String`)
- `optional` (`Optional<Dynamic<T>>`)

**Returns:** `private static <T> Dynamic<T>`

### updateMobEffectInstance

```java
<T> private static <T> Dynamic<T> updateMobEffectInstance(Dynamic<T> p_298320_)
```

**Parameters:**

- `p_298320_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### updateMobEffectInstanceList

```java
<T> private static <T> Dynamic<T> updateMobEffectInstanceList(Dynamic<T> p_299048_, String p_298254_, String p_298643_)
```

**Parameters:**

- `p_299048_` (`Dynamic<T>`)
- `p_298254_` (`String`)
- `p_298643_` (`String`)

**Returns:** `private static <T> Dynamic<T>`

### updateSuspiciousStewEntry

```java
<T> private static <T> Dynamic<T> updateSuspiciousStewEntry(Dynamic<T> p_298902_, Dynamic<T> p_299113_)
```

**Parameters:**

- `p_298902_` (`Dynamic<T>`)
- `p_299113_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### updateSuspiciousStewEntry

```java
<T> private static <T> Dynamic<T> updateSuspiciousStewEntry(Dynamic<T> p_298873_)
```

**Parameters:**

- `p_298873_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### updateSuspiciousStewEntry

```java
return updateSuspiciousStewEntry()
```

**Returns:** `return`

### updateNamedChoice

```java
private Typed<?> updateNamedChoice(Typed<?> p_298304_, TypeReference p_298928_, String p_298718_, Function<Dynamic<?>, Dynamic<?>> p_298931_)
```

**Parameters:**

- `p_298304_` (`Typed<?>`)
- `p_298928_` (`TypeReference`)
- `p_298718_` (`String`)
- `p_298931_` (`Function<Dynamic<?>, Dynamic<?>>`)

**Returns:** `private Typed<?>`

### blockEntityFixer

```java
private TypeRewriteRule blockEntityFixer()
```

**Returns:** `private TypeRewriteRule`

### updateMobEffectIdField

```java
return updateMobEffectIdField()
```

**Returns:** `return`

### fixMooshroomTag

```java
<T> private static <T> Dynamic<T> fixMooshroomTag(Dynamic<T> p_298470_)
```

**Parameters:**

- `p_298470_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### fixArrowTag

```java
<T> private static <T> Dynamic<T> fixArrowTag(Dynamic<T> p_299026_)
```

**Parameters:**

- `p_299026_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### updateMobEffectInstanceList

```java
return updateMobEffectInstanceList()
```

**Returns:** `return`

### fixAreaEffectCloudTag

```java
<T> private static <T> Dynamic<T> fixAreaEffectCloudTag(Dynamic<T> p_298539_)
```

**Parameters:**

- `p_298539_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### updateMobEffectInstanceList

```java
return updateMobEffectInstanceList()
```

**Returns:** `return`

### updateLivingEntityTag

```java
private static Dynamic<?> updateLivingEntityTag(Dynamic<?> p_299145_)
```

**Parameters:**

- `p_299145_` (`Dynamic<?>`)

**Returns:** `private static Dynamic<?>`

### updateMobEffectInstanceList

```java
return updateMobEffectInstanceList()
```

**Returns:** `return`

### entityFixer

```java
private TypeRewriteRule entityFixer()
```

**Returns:** `private TypeRewriteRule`

### playerFixer

```java
private TypeRewriteRule playerFixer()
```

**Returns:** `private TypeRewriteRule`

### fixSuspiciousStewTag

```java
<T> private static <T> Dynamic<T> fixSuspiciousStewTag(Dynamic<T> p_298546_)
```

**Parameters:**

- `p_298546_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### itemStackFixer

```java
private TypeRewriteRule itemStackFixer()
```

**Returns:** `private TypeRewriteRule`

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`
