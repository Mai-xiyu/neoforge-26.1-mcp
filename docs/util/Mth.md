# Mth

**Package:** `net.minecraft.util`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `PI` | `float` |  |
| `HALF_PI` | `float` |  |
| `TWO_PI` | `float` |  |
| `DEG_TO_RAD` | `float` |  |
| `RAD_TO_DEG` | `float` |  |
| `EPSILON` | `float` |  |
| `SQRT_OF_TWO` | `float` |  |
| `Y_AXIS` | `Vector3f` |  |
| `X_AXIS` | `Vector3f` |  |
| `Z_AXIS` | `Vector3f` |  |

## Methods

### sin

```java
public static float sin(float p_14032_)
```

**Parameters:**

- `p_14032_` (`float`)

**Returns:** `public static float`

### cos

```java
public static float cos(float p_14090_)
```

**Parameters:**

- `p_14090_` (`float`)

**Returns:** `public static float`

### sqrt

```java
public static float sqrt(float p_14117_)
```

**Parameters:**

- `p_14117_` (`float`)

**Returns:** `public static float`

### floor

```java
public static int floor(float p_14144_)
```

**Parameters:**

- `p_14144_` (`float`)

**Returns:** `public static int`

### floor

```java
public static int floor(double p_14108_)
```

**Parameters:**

- `p_14108_` (`double`)

**Returns:** `public static int`

### lfloor

```java
public static long lfloor(double p_14135_)
```

**Parameters:**

- `p_14135_` (`double`)

**Returns:** `public static long`

### abs

```java
public static float abs(float p_14155_)
```

**Parameters:**

- `p_14155_` (`float`)

**Returns:** `public static float`

### abs

```java
public static int abs(int p_14041_)
```

**Parameters:**

- `p_14041_` (`int`)

**Returns:** `public static int`

### ceil

```java
public static int ceil(float p_14168_)
```

**Parameters:**

- `p_14168_` (`float`)

**Returns:** `public static int`

### ceil

```java
public static int ceil(double p_14166_)
```

**Parameters:**

- `p_14166_` (`double`)

**Returns:** `public static int`

### clamp

```java
public static int clamp(int p_14046_, int p_14047_, int p_14048_)
```

**Parameters:**

- `p_14046_` (`int`)
- `p_14047_` (`int`)
- `p_14048_` (`int`)

**Returns:** `public static int`

### clamp

```java
public static long clamp(long p_298408_, long p_298924_, long p_298326_)
```

**Parameters:**

- `p_298408_` (`long`)
- `p_298924_` (`long`)
- `p_298326_` (`long`)

**Returns:** `public static long`

### clamp

```java
public static float clamp(float p_14037_, float p_14038_, float p_14039_)
```

**Parameters:**

- `p_14037_` (`float`)
- `p_14038_` (`float`)
- `p_14039_` (`float`)

**Returns:** `public static float`

### clamp

```java
public static double clamp(double p_14009_, double p_14010_, double p_14011_)
```

**Parameters:**

- `p_14009_` (`double`)
- `p_14010_` (`double`)
- `p_14011_` (`double`)

**Returns:** `public static double`

### clampedLerp

```java
public static double clampedLerp(double p_14086_, double p_14087_, double p_14088_)
```

**Parameters:**

- `p_14086_` (`double`)
- `p_14087_` (`double`)
- `p_14088_` (`double`)

**Returns:** `public static double`

### clampedLerp

```java
public static float clampedLerp(float p_144921_, float p_144922_, float p_144923_)
```

**Parameters:**

- `p_144921_` (`float`)
- `p_144922_` (`float`)
- `p_144923_` (`float`)

**Returns:** `public static float`

### absMax

```java
public static double absMax(double p_14006_, double p_14007_)
```

**Parameters:**

- `p_14006_` (`double`)
- `p_14007_` (`double`)

**Returns:** `public static double`

### floorDiv

```java
public static int floorDiv(int p_14043_, int p_14044_)
```

**Parameters:**

- `p_14043_` (`int`)
- `p_14044_` (`int`)

**Returns:** `public static int`

### nextInt

```java
public static int nextInt(RandomSource p_216272_, int p_216273_, int p_216274_)
```

**Parameters:**

- `p_216272_` (`RandomSource`)
- `p_216273_` (`int`)
- `p_216274_` (`int`)

**Returns:** `public static int`

### nextFloat

```java
public static float nextFloat(RandomSource p_216268_, float p_216269_, float p_216270_)
```

**Parameters:**

- `p_216268_` (`RandomSource`)
- `p_216269_` (`float`)
- `p_216270_` (`float`)

**Returns:** `public static float`

### nextDouble

```java
public static double nextDouble(RandomSource p_216264_, double p_216265_, double p_216266_)
```

**Parameters:**

- `p_216264_` (`RandomSource`)
- `p_216265_` (`double`)
- `p_216266_` (`double`)

**Returns:** `public static double`

### equal

```java
public static boolean equal(float p_14034_, float p_14035_)
```

**Parameters:**

- `p_14034_` (`float`)
- `p_14035_` (`float`)

**Returns:** `public static boolean`

### equal

```java
public static boolean equal(double p_14083_, double p_14084_)
```

**Parameters:**

- `p_14083_` (`double`)
- `p_14084_` (`double`)

**Returns:** `public static boolean`

### positiveModulo

```java
public static int positiveModulo(int p_14101_, int p_14102_)
```

**Parameters:**

- `p_14101_` (`int`)
- `p_14102_` (`int`)

**Returns:** `public static int`

### positiveModulo

```java
public static float positiveModulo(float p_14092_, float p_14093_)
```

**Parameters:**

- `p_14092_` (`float`)
- `p_14093_` (`float`)

**Returns:** `public static float`

### positiveModulo

```java
public static double positiveModulo(double p_14110_, double p_14111_)
```

**Parameters:**

- `p_14110_` (`double`)
- `p_14111_` (`double`)

**Returns:** `public static double`

### isMultipleOf

```java
public static boolean isMultipleOf(int p_265754_, int p_265543_)
```

**Parameters:**

- `p_265754_` (`int`)
- `p_265543_` (`int`)

**Returns:** `public static boolean`

### wrapDegrees

```java
public static int wrapDegrees(int p_14099_)
```

**Parameters:**

- `p_14099_` (`int`)

**Returns:** `public static int`

### wrapDegrees

```java
public static float wrapDegrees(float p_14178_)
```

**Parameters:**

- `p_14178_` (`float`)

**Returns:** `public static float`

### wrapDegrees

```java
public static double wrapDegrees(double p_14176_)
```

**Parameters:**

- `p_14176_` (`double`)

**Returns:** `public static double`

### degreesDifference

```java
public static float degreesDifference(float p_14119_, float p_14120_)
```

**Parameters:**

- `p_14119_` (`float`)
- `p_14120_` (`float`)

**Returns:** `public static float`

### wrapDegrees

```java
return wrapDegrees(p_14120_ - p_14119_)
```

**Parameters:**

- `p_14119_` (`p_14120_ -`)

**Returns:** `return`

### degreesDifferenceAbs

```java
public static float degreesDifferenceAbs(float p_14146_, float p_14147_)
```

**Parameters:**

- `p_14146_` (`float`)
- `p_14147_` (`float`)

**Returns:** `public static float`

### rotateIfNecessary

```java
public static float rotateIfNecessary(float p_14095_, float p_14096_, float p_14097_)
```

**Parameters:**

- `p_14095_` (`float`)
- `p_14096_` (`float`)
- `p_14097_` (`float`)

**Returns:** `public static float`

### approach

```java
public static float approach(float p_14122_, float p_14123_, float p_14124_)
```

**Parameters:**

- `p_14122_` (`float`)
- `p_14123_` (`float`)
- `p_14124_` (`float`)

**Returns:** `public static float`

### approachDegrees

```java
public static float approachDegrees(float p_14149_, float p_14150_, float p_14151_)
```

**Parameters:**

- `p_14149_` (`float`)
- `p_14150_` (`float`)
- `p_14151_` (`float`)

**Returns:** `public static float`

### approach

```java
return approach(p_14149_ + f)
```

**Parameters:**

- `f` (`p_14149_ +`)

**Returns:** `return`

### getInt

```java
public static int getInt(String p_14060_, int p_14061_)
```

**Parameters:**

- `p_14060_` (`String`)
- `p_14061_` (`int`)

**Returns:** `public static int`

### smallestEncompassingPowerOfTwo

```java
public static int smallestEncompassingPowerOfTwo(int p_14126_)
```

**Parameters:**

- `p_14126_` (`int`)

**Returns:** `public static int`

### isPowerOfTwo

```java
public static boolean isPowerOfTwo(int p_14153_)
```

**Parameters:**

- `p_14153_` (`int`)

**Returns:** `public static boolean`

### ceillog2

```java
public static int ceillog2(int p_14164_)
```

**Parameters:**

- `p_14164_` (`int`)

**Returns:** `public static int`

### log2

```java
public static int log2(int p_14174_)
```

**Parameters:**

- `p_14174_` (`int`)

**Returns:** `public static int`

### color

```java
public static int color(float p_14160_, float p_14161_, float p_14162_)
```

**Parameters:**

- `p_14160_` (`float`)
- `p_14161_` (`float`)
- `p_14162_` (`float`)

**Returns:** `public static int`

### frac

```java
public static float frac(float p_14188_)
```

**Parameters:**

- `p_14188_` (`float`)

**Returns:** `public static float`

### frac

```java
public static double frac(double p_14186_)
```

**Parameters:**

- `p_14186_` (`double`)

**Returns:** `public static double`

### getSeed

```java
public static long getSeed(Vec3i p_14058_)
```

**Parameters:**

- `p_14058_` (`Vec3i`)

**Returns:** `long`

### getSeed

```java
public static long getSeed(int p_14131_, int p_14132_, int p_14133_)
```

**Parameters:**

- `p_14131_` (`int`)
- `p_14132_` (`int`)
- `p_14133_` (`int`)

**Returns:** `long`

### createInsecureUUID

```java
public static UUID createInsecureUUID(RandomSource p_216262_)
```

**Parameters:**

- `p_216262_` (`RandomSource`)

**Returns:** `public static UUID`

### UUID

```java
return new UUID()
```

**Returns:** `return new`

### createInsecureUUID

```java
public static UUID createInsecureUUID()
```

**Returns:** `public static UUID`

### createInsecureUUID

```java
return createInsecureUUID()
```

**Returns:** `return`

### inverseLerp

```java
public static double inverseLerp(double p_14113_, double p_14114_, double p_14115_)
```

**Parameters:**

- `p_14113_` (`double`)
- `p_14114_` (`double`)
- `p_14115_` (`double`)

**Returns:** `public static double`

### inverseLerp

```java
public static float inverseLerp(float p_184656_, float p_184657_, float p_184658_)
```

**Parameters:**

- `p_184656_` (`float`)
- `p_184657_` (`float`)
- `p_184658_` (`float`)

**Returns:** `public static float`

### rayIntersectsAABB

```java
public static boolean rayIntersectsAABB(Vec3 p_144889_, Vec3 p_144890_, AABB p_144891_)
```

**Parameters:**

- `p_144889_` (`Vec3`)
- `p_144890_` (`Vec3`)
- `p_144891_` (`AABB`)

**Returns:** `public static boolean`

### atan2

```java
public static double atan2(double p_14137_, double p_14138_)
```

**Parameters:**

- `p_14137_` (`double`)
- `p_14138_` (`double`)

**Returns:** `public static double`

### invSqrt

```java
public static float invSqrt(float p_265060_)
```

**Parameters:**

- `p_265060_` (`float`)

**Returns:** `public static float`

### invSqrt

```java
public static double invSqrt(double p_265088_)
```

**Parameters:**

- `p_265088_` (`double`)

**Returns:** `public static double`

### fastInvSqrt

```java
public static double fastInvSqrt(double p_14194_)
```

**Parameters:**

- `p_14194_` (`double`)

**Returns:** `double`

### fastInvCubeRoot

```java
public static float fastInvCubeRoot(float p_14200_)
```

**Parameters:**

- `p_14200_` (`float`)

**Returns:** `public static float`

### hsvToRgb

```java
public static int hsvToRgb(float p_14170_, float p_14171_, float p_14172_)
```

**Parameters:**

- `p_14170_` (`float`)
- `p_14171_` (`float`)
- `p_14172_` (`float`)

**Returns:** `public static int`

### hsvToArgb

```java
return hsvToArgb()
```

**Returns:** `return`

### hsvToArgb

```java
public static int hsvToArgb(float p_348601_, float p_348572_, float p_348491_, int p_348531_)
```

**Parameters:**

- `p_348601_` (`float`)
- `p_348572_` (`float`)
- `p_348491_` (`float`)
- `p_348531_` (`int`)

**Returns:** `public static int`

### RuntimeException

```java
throw new RuntimeException("Something went wrong when converting from HSV to RGB. Input was " + p_348601_ + ", " + p_348572_ + ", " + p_348491_)
```

**Parameters:**

- `"` (`"Something went wrong when converting from HSV to RGB. Input was " + p_348601_ +`)
- `"` (`" + p_348572_ +`)
- `p_348491_` (`" +`)

**Returns:** `throw new`

### murmurHash3Mixer

```java
public static int murmurHash3Mixer(int p_14184_)
```

**Parameters:**

- `p_14184_` (`int`)

**Returns:** `public static int`

### binarySearch

```java
public static int binarySearch(int p_14050_, int p_14051_, IntPredicate p_14052_)
```

**Parameters:**

- `p_14050_` (`int`)
- `p_14051_` (`int`)
- `p_14052_` (`IntPredicate`)

**Returns:** `public static int`

### lerpInt

```java
public static int lerpInt(float p_270245_, int p_270597_, int p_270301_)
```

**Parameters:**

- `p_270245_` (`float`)
- `p_270597_` (`int`)
- `p_270301_` (`int`)

**Returns:** `public static int`

### lerpDiscrete

```java
public static int lerpDiscrete(float p_299072_, int p_298417_, int p_299185_)
```

**Parameters:**

- `p_299072_` (`float`)
- `p_298417_` (`int`)
- `p_299185_` (`int`)

**Returns:** `public static int`

### lerp

```java
public static float lerp(float p_14180_, float p_14181_, float p_14182_)
```

**Parameters:**

- `p_14180_` (`float`)
- `p_14181_` (`float`)
- `p_14182_` (`float`)

**Returns:** `public static float`

### lerp

```java
public static double lerp(double p_14140_, double p_14141_, double p_14142_)
```

**Parameters:**

- `p_14140_` (`double`)
- `p_14141_` (`double`)
- `p_14142_` (`double`)

**Returns:** `public static double`

### lerp2

```java
public static double lerp2(double p_14013_, double p_14014_, double p_14015_, double p_14016_, double p_14017_, double p_14018_)
```

**Parameters:**

- `p_14013_` (`double`)
- `p_14014_` (`double`)
- `p_14015_` (`double`)
- `p_14016_` (`double`)
- `p_14017_` (`double`)
- `p_14018_` (`double`)

**Returns:** `public static double`

### lerp3

```java
public static double lerp3(double p_14020_, double p_14021_, double p_14022_, double p_14023_, double p_14024_, double p_14025_, double p_14026_, double p_14027_, double p_14028_, double p_14029_, double p_14030_)
```

**Parameters:**

- `p_14020_` (`double`)
- `p_14021_` (`double`)
- `p_14022_` (`double`)
- `p_14023_` (`double`)
- `p_14024_` (`double`)
- `p_14025_` (`double`)
- `p_14026_` (`double`)
- `p_14027_` (`double`)
- `p_14028_` (`double`)
- `p_14029_` (`double`)
- `p_14030_` (`double`)

**Returns:** `public static double`

### catmullrom

```java
public static float catmullrom(float p_216245_, float p_216246_, float p_216247_, float p_216248_, float p_216249_)
```

**Parameters:**

- `p_216245_` (`float`)
- `p_216246_` (`float`)
- `p_216247_` (`float`)
- `p_216248_` (`float`)
- `p_216249_` (`float`)

**Returns:** `public static float`

### smoothstep

```java
public static double smoothstep(double p_14198_)
```

**Parameters:**

- `p_14198_` (`double`)

**Returns:** `public static double`

### smoothstepDerivative

```java
public static double smoothstepDerivative(double p_144947_)
```

**Parameters:**

- `p_144947_` (`double`)

**Returns:** `public static double`

### sign

```java
public static int sign(double p_14206_)
```

**Parameters:**

- `p_14206_` (`double`)

**Returns:** `public static int`

### rotLerp

```java
public static float rotLerp(float p_14190_, float p_14191_, float p_14192_)
```

**Parameters:**

- `p_14190_` (`float`)
- `p_14191_` (`float`)
- `p_14192_` (`float`)

**Returns:** `public static float`

### rotLerp

```java
public static double rotLerp(double p_296301_, double p_294715_, double p_295496_)
```

**Parameters:**

- `p_296301_` (`double`)
- `p_294715_` (`double`)
- `p_295496_` (`double`)

**Returns:** `public static double`

### triangleWave

```java
public static float triangleWave(float p_14157_, float p_14158_)
```

**Parameters:**

- `p_14157_` (`float`)
- `p_14158_` (`float`)

**Returns:** `public static float`

### square

```java
public static float square(float p_14208_)
```

**Parameters:**

- `p_14208_` (`float`)

**Returns:** `public static float`

### square

```java
public static double square(double p_144953_)
```

**Parameters:**

- `p_144953_` (`double`)

**Returns:** `public static double`

### square

```java
public static int square(int p_144945_)
```

**Parameters:**

- `p_144945_` (`int`)

**Returns:** `public static int`

### square

```java
public static long square(long p_184644_)
```

**Parameters:**

- `p_184644_` (`long`)

**Returns:** `public static long`

### clampedMap

```java
public static double clampedMap(double p_144852_, double p_144853_, double p_144854_, double p_144855_, double p_144856_)
```

**Parameters:**

- `p_144852_` (`double`)
- `p_144853_` (`double`)
- `p_144854_` (`double`)
- `p_144855_` (`double`)
- `p_144856_` (`double`)

**Returns:** `public static double`

### clampedMap

```java
public static float clampedMap(float p_184632_, float p_184633_, float p_184634_, float p_184635_, float p_184636_)
```

**Parameters:**

- `p_184632_` (`float`)
- `p_184633_` (`float`)
- `p_184634_` (`float`)
- `p_184635_` (`float`)
- `p_184636_` (`float`)

**Returns:** `public static float`

### map

```java
public static double map(double p_144915_, double p_144916_, double p_144917_, double p_144918_, double p_144919_)
```

**Parameters:**

- `p_144915_` (`double`)
- `p_144916_` (`double`)
- `p_144917_` (`double`)
- `p_144918_` (`double`)
- `p_144919_` (`double`)

**Returns:** `public static double`

### map

```java
public static float map(float p_184638_, float p_184639_, float p_184640_, float p_184641_, float p_184642_)
```

**Parameters:**

- `p_184638_` (`float`)
- `p_184639_` (`float`)
- `p_184640_` (`float`)
- `p_184641_` (`float`)
- `p_184642_` (`float`)

**Returns:** `public static float`

### wobble

```java
public static double wobble(double p_144955_)
```

**Parameters:**

- `p_144955_` (`double`)

**Returns:** `public static double`

### roundToward

```java
public static int roundToward(int p_144942_, int p_144943_)
```

**Parameters:**

- `p_144942_` (`int`)
- `p_144943_` (`int`)

**Returns:** `public static int`

### positiveCeilDiv

```java
public static int positiveCeilDiv(int p_184653_, int p_184654_)
```

**Parameters:**

- `p_184653_` (`int`)
- `p_184654_` (`int`)

**Returns:** `public static int`

### randomBetweenInclusive

```java
public static int randomBetweenInclusive(RandomSource p_216288_, int p_216289_, int p_216290_)
```

**Parameters:**

- `p_216288_` (`RandomSource`)
- `p_216289_` (`int`)
- `p_216290_` (`int`)

**Returns:** `public static int`

### randomBetween

```java
public static float randomBetween(RandomSource p_216284_, float p_216285_, float p_216286_)
```

**Parameters:**

- `p_216284_` (`RandomSource`)
- `p_216285_` (`float`)
- `p_216286_` (`float`)

**Returns:** `public static float`

### normal

```java
public static float normal(RandomSource p_216292_, float p_216293_, float p_216294_)
```

**Parameters:**

- `p_216292_` (`RandomSource`)
- `p_216293_` (`float`)
- `p_216294_` (`float`)

**Returns:** `public static float`

### lengthSquared

```java
public static double lengthSquared(double p_211590_, double p_211591_)
```

**Parameters:**

- `p_211590_` (`double`)
- `p_211591_` (`double`)

**Returns:** `public static double`

### length

```java
public static double length(double p_184646_, double p_184647_)
```

**Parameters:**

- `p_184646_` (`double`)
- `p_184647_` (`double`)

**Returns:** `public static double`

### lengthSquared

```java
public static double lengthSquared(double p_211593_, double p_211594_, double p_211595_)
```

**Parameters:**

- `p_211593_` (`double`)
- `p_211594_` (`double`)
- `p_211595_` (`double`)

**Returns:** `public static double`

### length

```java
public static double length(double p_184649_, double p_184650_, double p_184651_)
```

**Parameters:**

- `p_184649_` (`double`)
- `p_184650_` (`double`)
- `p_184651_` (`double`)

**Returns:** `public static double`

### lengthSquared

```java
public static float lengthSquared(float p_350890_, float p_350831_, float p_350696_)
```

**Parameters:**

- `p_350890_` (`float`)
- `p_350831_` (`float`)
- `p_350696_` (`float`)

**Returns:** `public static float`

### quantize

```java
public static int quantize(double p_184629_, int p_184630_)
```

**Parameters:**

- `p_184629_` (`double`)
- `p_184630_` (`int`)

**Returns:** `public static int`

### outFromOrigin

```java
public static IntStream outFromOrigin(int p_216296_, int p_216297_, int p_216298_)
```

**Parameters:**

- `p_216296_` (`int`)
- `p_216297_` (`int`)
- `p_216298_` (`int`)

**Returns:** `public static IntStream`

### outFromOrigin

```java
return outFromOrigin()
```

**Returns:** `return`

### outFromOrigin

```java
public static IntStream outFromOrigin(int p_216251_, int p_216252_, int p_216253_, int p_216254_)
```

**Parameters:**

- `p_216251_` (`int`)
- `p_216252_` (`int`)
- `p_216253_` (`int`)
- `p_216254_` (`int`)

**Returns:** `public static IntStream`

### rotationAroundAxis

```java
public static Quaternionf rotationAroundAxis(Vector3f p_304469_, Quaternionf p_304737_, Quaternionf p_304773_)
```

**Parameters:**

- `p_304469_` (`Vector3f`)
- `p_304737_` (`Quaternionf`)
- `p_304773_` (`Quaternionf`)

**Returns:** `public static Quaternionf`

### mulAndTruncate

```java
public static int mulAndTruncate(Fraction p_339645_, int p_339636_)
```

**Parameters:**

- `p_339645_` (`Fraction`)
- `p_339636_` (`int`)

**Returns:** `public static int`
