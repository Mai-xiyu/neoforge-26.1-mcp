# NativeModuleLister

**Package:** `net.minecraft.util`
**Type:** class

## Methods

### listModules

```java
public static List<NativeModuleLister.NativeModuleInfo> listModules()
```

**Returns:** `public static List<NativeModuleLister.NativeModuleInfo>`

### tryGetVersion

```java
private static Optional<NativeModuleLister.NativeModuleVersion> tryGetVersion(String p_184674_)
```

**Parameters:**

- `p_184674_` (`String`)

**Returns:** `private static Optional<NativeModuleLister.NativeModuleVersion>`

### Win32Exception

```java
throw new Win32Exception()
```

**Returns:** `throw new`

### langTableKey

```java
private static String langTableKey(String p_184676_, int p_184677_, int p_184678_)
```

**Parameters:**

- `p_184676_` (`String`)
- `p_184677_` (`int`)
- `p_184678_` (`int`)

**Returns:** `private static String`

### findLangAndCodepage

```java
private static OptionalInt findLangAndCodepage(int[] p_184682_)
```

**Parameters:**

- `p_184682_` (`int[]`)

**Returns:** `private static OptionalInt`

### queryVersionValue

```java
private static Pointer queryVersionValue(Pointer p_184670_, String p_184671_, IntByReference p_184672_)
```

**Parameters:**

- `p_184670_` (`Pointer`)
- `p_184671_` (`String`)
- `p_184672_` (`IntByReference`)

**Returns:** `private static Pointer`

### UnsupportedOperationException

```java
throw new UnsupportedOperationException("Can't get version value " + p_184671_)
```

**Parameters:**

- `p_184671_` (`"Can't get version value " +`)

**Returns:** `throw new`

### queryVersionString

```java
private static String queryVersionString(Pointer p_184687_, String p_184688_, IntByReference p_184689_)
```

**Parameters:**

- `p_184687_` (`Pointer`)
- `p_184688_` (`String`)
- `p_184689_` (`IntByReference`)

**Returns:** `private static String`

### String

```java
return new String()
```

**Returns:** `return new`

### addCrashSection

```java
public static void addCrashSection(CrashReportCategory p_184680_)
```

**Parameters:**

- `p_184680_` (`CrashReportCategory`)

**Returns:** `public static void`

### NativeModuleInfo

```java
public NativeModuleInfo(String p_184693_, Optional<NativeModuleLister.NativeModuleVersion> p_184694_)
```

**Parameters:**

- `p_184693_` (`String`)
- `p_184694_` (`Optional<NativeModuleLister.NativeModuleVersion>`)

**Returns:** `public`

### toString

```java
public String toString()
```

**Returns:** `String`

### NativeModuleVersion

```java
public NativeModuleVersion(String p_184702_, String p_184703_, String p_184704_)
```

**Parameters:**

- `p_184702_` (`String`)
- `p_184703_` (`String`)
- `p_184704_` (`String`)

**Returns:** `public`

### toString

```java
public String toString()
```

**Returns:** `String`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `NativeModuleInfo` | class |  |
| `NativeModuleVersion` | class |  |
