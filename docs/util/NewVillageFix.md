# NewVillageFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### NewVillageFix

```java
public NewVillageFix(Schema p_16476_, boolean p_16477_)
```

**Parameters:**

- `p_16476_` (`Schema`)
- `p_16477_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### cap

```java
<SF> private <SF> TypeRewriteRule cap(CompoundListType<String, SF> p_16499_)
```

**Parameters:**

- `p_16499_` (`CompoundListType<String, SF>`)

**Returns:** `private <SF> TypeRewriteRule`
