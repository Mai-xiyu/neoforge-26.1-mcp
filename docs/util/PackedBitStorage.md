# PackedBitStorage

**Package:** `net.minecraft.util.datafix`
**Type:** class

## Methods

### PackedBitStorage

```java
public PackedBitStorage(int p_14555_, int p_14556_)
```

**Parameters:**

- `p_14555_` (`int`)
- `p_14556_` (`int`)

**Returns:** `public`

### PackedBitStorage

```java
public PackedBitStorage(int p_14558_, int p_14559_, long[] p_14560_)
```

**Parameters:**

- `p_14558_` (`int`)
- `p_14559_` (`int`)
- `p_14560_` (`long[]`)

**Returns:** `public`

### IllegalArgumentException

```java
throw new IllegalArgumentException("Invalid length given for storage, got: " + p_14560_.length + " but expected: " + i)
```

**Parameters:**

- `storage` (`"Invalid length given for`)
- `i` (`got: " + p_14560_.length + " but expected: " +`)

**Returns:** `throw new`

### set

```java
public void set(int p_14565_, int p_14566_)
```

**Parameters:**

- `p_14565_` (`int`)
- `p_14566_` (`int`)

**Returns:** `public void`

### get

```java
public int get(int p_14563_)
```

**Parameters:**

- `p_14563_` (`int`)

**Returns:** `public int`

### getRaw

```java
public long[] getRaw()
```

**Returns:** `public long[]`

### getBits

```java
public int getBits()
```

**Returns:** `public int`
