# ParticleUnflatteningFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### ParticleUnflatteningFix

```java
public ParticleUnflatteningFix(Schema p_340896_)
```

**Parameters:**

- `p_340896_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### fix

```java
<T> private <T> Dynamic<T> fix(Dynamic<T> p_341243_)
```

**Parameters:**

- `p_341243_` (`Dynamic<T>`)

**Returns:** `private <T> Dynamic<T>`

### updateItem

```java
<T> private <T> Dynamic<T> updateItem(Dynamic<T> p_340826_, String p_340894_)
```

**Parameters:**

- `p_340826_` (`Dynamic<T>`)
- `p_340894_` (`String`)

**Returns:** `private <T> Dynamic<T>`

### updateBlock

```java
<T> private <T> Dynamic<T> updateBlock(Dynamic<T> p_341247_, String p_341412_)
```

**Parameters:**

- `p_341247_` (`Dynamic<T>`)
- `p_341412_` (`String`)

**Returns:** `private <T> Dynamic<T>`

### parseBlockProperties

```java
<T> private static <T> Map<Dynamic<T>, Dynamic<T>> parseBlockProperties(Dynamic<T> p_341040_, String p_341381_)
```

**Parameters:**

- `p_341040_` (`Dynamic<T>`)
- `p_341381_` (`String`)

**Returns:** `private static <T> Map<Dynamic<T>, Dynamic<T>>`

### readVector

```java
<T> private static <T> Dynamic<T> readVector(Dynamic<T> p_340849_, StringReader p_340969_)
```

**Parameters:**

- `p_340849_` (`Dynamic<T>`)
- `p_340969_` (`StringReader`)

**Returns:** `private static <T> Dynamic<T>`

### updateDust

```java
<T> private <T> Dynamic<T> updateDust(Dynamic<T> p_340805_, String p_340908_)
```

**Parameters:**

- `p_340805_` (`Dynamic<T>`)
- `p_340908_` (`String`)

**Returns:** `private <T> Dynamic<T>`

### updateDustTransition

```java
<T> private <T> Dynamic<T> updateDustTransition(Dynamic<T> p_341240_, String p_341296_)
```

**Parameters:**

- `p_341240_` (`Dynamic<T>`)
- `p_341296_` (`String`)

**Returns:** `private <T> Dynamic<T>`

### updateSculkCharge

```java
<T> private <T> Dynamic<T> updateSculkCharge(Dynamic<T> p_340987_, String p_340846_)
```

**Parameters:**

- `p_340987_` (`Dynamic<T>`)
- `p_340846_` (`String`)

**Returns:** `private <T> Dynamic<T>`

### updateVibration

```java
<T> private <T> Dynamic<T> updateVibration(Dynamic<T> p_340954_, String p_341325_)
```

**Parameters:**

- `p_340954_` (`Dynamic<T>`)
- `p_341325_` (`String`)

**Returns:** `private <T> Dynamic<T>`

### updateShriek

```java
<T> private <T> Dynamic<T> updateShriek(Dynamic<T> p_340938_, String p_341343_)
```

**Parameters:**

- `p_340938_` (`Dynamic<T>`)
- `p_341343_` (`String`)

**Returns:** `private <T> Dynamic<T>`
