# ParticleUtils

**Package:** `net.minecraft.util`
**Type:** class

## Methods

### spawnParticlesOnBlockFaces

```java
public static void spawnParticlesOnBlockFaces(Level p_216314_, BlockPos p_216315_, ParticleOptions p_216316_, IntProvider p_216317_)
```

**Parameters:**

- `p_216314_` (`Level`)
- `p_216315_` (`BlockPos`)
- `p_216316_` (`ParticleOptions`)
- `p_216317_` (`IntProvider`)

**Returns:** `public static void`

### spawnParticlesOnBlockFace

```java
public static void spawnParticlesOnBlockFace(Level p_216319_, BlockPos p_216320_, ParticleOptions p_216321_, IntProvider p_216322_, Direction p_216323_, Supplier<Vec3> p_216324_, double p_216325_)
```

**Parameters:**

- `p_216319_` (`Level`)
- `p_216320_` (`BlockPos`)
- `p_216321_` (`ParticleOptions`)
- `p_216322_` (`IntProvider`)
- `p_216323_` (`Direction`)
- `p_216324_` (`Supplier<Vec3>`)
- `p_216325_` (`double`)

**Returns:** `public static void`

### getRandomSpeedRanges

```java
private static Vec3 getRandomSpeedRanges(RandomSource p_216303_)
```

**Parameters:**

- `p_216303_` (`RandomSource`)

**Returns:** `private static Vec3`

### spawnParticlesAlongAxis

```java
public static void spawnParticlesAlongAxis(Direction.Axis p_144968_, Level p_144969_, BlockPos p_144970_, double p_144971_, ParticleOptions p_144972_, UniformInt p_144973_)
```

**Parameters:**

- `p_144968_` (`Direction.Axis`)
- `p_144969_` (`Level`)
- `p_144970_` (`BlockPos`)
- `p_144971_` (`double`)
- `p_144972_` (`ParticleOptions`)
- `p_144973_` (`UniformInt`)

**Returns:** `public static void`

### spawnParticleOnFace

```java
public static void spawnParticleOnFace(Level p_216307_, BlockPos p_216308_, Direction p_216309_, ParticleOptions p_216310_, Vec3 p_216311_, double p_216312_)
```

**Parameters:**

- `p_216307_` (`Level`)
- `p_216308_` (`BlockPos`)
- `p_216309_` (`Direction`)
- `p_216310_` (`ParticleOptions`)
- `p_216311_` (`Vec3`)
- `p_216312_` (`double`)

**Returns:** `public static void`

### spawnParticleBelow

```java
public static void spawnParticleBelow(Level p_273159_, BlockPos p_273452_, RandomSource p_273538_, ParticleOptions p_273419_)
```

**Parameters:**

- `p_273159_` (`Level`)
- `p_273452_` (`BlockPos`)
- `p_273538_` (`RandomSource`)
- `p_273419_` (`ParticleOptions`)

**Returns:** `public static void`

### spawnParticleInBlock

```java
public static void spawnParticleInBlock(LevelAccessor p_316553_, BlockPos p_316882_, int p_316412_, ParticleOptions p_316308_)
```

**Parameters:**

- `p_316553_` (`LevelAccessor`)
- `p_316882_` (`BlockPos`)
- `p_316412_` (`int`)
- `p_316308_` (`ParticleOptions`)

**Returns:** `public static void`

### spawnParticles

```java
 spawnParticles()
```

**Returns:** ``

### spawnParticles

```java
public static void spawnParticles(LevelAccessor p_316549_, BlockPos p_316335_, int p_316207_, double p_316351_, double p_316421_, boolean p_316563_, ParticleOptions p_316241_)
```

**Parameters:**

- `p_316549_` (`LevelAccessor`)
- `p_316335_` (`BlockPos`)
- `p_316207_` (`int`)
- `p_316351_` (`double`)
- `p_316421_` (`double`)
- `p_316563_` (`boolean`)
- `p_316241_` (`ParticleOptions`)

**Returns:** `public static void`

### spawnSmashAttackParticles

```java
public static void spawnSmashAttackParticles(LevelAccessor p_338535_, BlockPos p_338284_, int p_338684_)
```

**Parameters:**

- `p_338535_` (`LevelAccessor`)
- `p_338284_` (`BlockPos`)
- `p_338684_` (`int`)

**Returns:** `public static void`
