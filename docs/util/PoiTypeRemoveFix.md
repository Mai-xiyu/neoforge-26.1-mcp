# PoiTypeRemoveFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `AbstractPoiSectionFix`

## Methods

### PoiTypeRemoveFix

```java
public PoiTypeRemoveFix(Schema p_216701_, String p_216702_, Predicate<String> p_216703_)
```

**Parameters:**

- `p_216701_` (`Schema`)
- `p_216702_` (`String`)
- `p_216703_` (`Predicate<String>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### processRecords

```java
protected <T> Stream<Dynamic<T>> processRecords(Stream<Dynamic<T>> p_216707_)
```

**Parameters:**

- `p_216707_` (`Stream<Dynamic<T>>`)

**Returns:** `Stream<Dynamic<T>>`

### shouldKeepRecord

```java
<T> private <T> boolean shouldKeepRecord(Dynamic<T> p_216705_)
```

**Parameters:**

- `p_216705_` (`Dynamic<T>`)

**Returns:** `private <T> boolean`
