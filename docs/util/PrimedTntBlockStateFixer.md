# PrimedTntBlockStateFixer

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `NamedEntityWriteReadFix`

## Methods

### PrimedTntBlockStateFixer

```java
public PrimedTntBlockStateFixer(Schema p_307306_)
```

**Parameters:**

- `p_307306_` (`Schema`)

**Returns:** `public`

### super

```java
 super("PrimedTnt BlockState fixer")
```

**Parameters:**

- `fixer"` (`"PrimedTnt BlockState`)

**Returns:** ``

### renameFuse

```java
<T> private static <T> Dynamic<T> renameFuse(Dynamic<T> p_307631_)
```

**Parameters:**

- `p_307631_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### insertBlockState

```java
<T> private static <T> Dynamic<T> insertBlockState(Dynamic<T> p_307568_)
```

**Parameters:**

- `p_307568_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### fix

```java
protected <T> Dynamic<T> fix(Dynamic<T> p_307650_)
```

**Parameters:**

- `p_307650_` (`Dynamic<T>`)

**Returns:** `Dynamic<T>`
