# ProjectileStoredWeaponFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### ProjectileStoredWeaponFix

```java
public ProjectileStoredWeaponFix(Schema p_345491_)
```

**Parameters:**

- `p_345491_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### fixChoice

```java
private Function<Typed<?>, Typed<?>> fixChoice(String p_344880_)
```

**Parameters:**

- `p_344880_` (`String`)

**Returns:** `private Function<Typed<?>, Typed<?>>`

### fixChoiceCap

```java
return fixChoiceCap()
```

**Returns:** `return`

### fixChoiceCap

```java
<T> private static <T> Function<Typed<?>, Typed<?>> fixChoiceCap(String p_345540_, Type<?> p_346235_, Type<T> p_346079_)
```

**Parameters:**

- `p_345540_` (`String`)
- `p_346235_` (`Type<?>`)
- `p_346079_` (`Type<T>`)

**Returns:** `private static <T> Function<Typed<?>, Typed<?>>`
