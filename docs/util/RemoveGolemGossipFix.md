# RemoveGolemGossipFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `NamedEntityFix`

## Methods

### RemoveGolemGossipFix

```java
public RemoveGolemGossipFix(Schema p_16823_, boolean p_16824_)
```

**Parameters:**

- `p_16823_` (`Schema`)
- `p_16824_` (`boolean`)

**Returns:** `public`

### super

```java
 super("Remove Golem Gossip Fix")
```

**Parameters:**

- `Fix"` (`"Remove Golem Gossip`)

**Returns:** ``

### fix

```java
protected Typed<?> fix(Typed<?> p_16826_)
```

**Parameters:**

- `p_16826_` (`Typed<?>`)

**Returns:** `Typed<?>`

### fixValue

```java
private static Dynamic<?> fixValue(Dynamic<?> p_16828_)
```

**Parameters:**

- `p_16828_` (`Dynamic<?>`)

**Returns:** `private static Dynamic<?>`
