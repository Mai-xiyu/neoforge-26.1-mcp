# ReorganizePoi

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### ReorganizePoi

```java
public ReorganizePoi(Schema p_16853_, boolean p_16854_)
```

**Parameters:**

- `p_16853_` (`Schema`)
- `p_16854_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### IllegalStateException

```java
throw new IllegalStateException("Poi type is not what was expected.")
```

**Parameters:**

- `expected."` (`"Poi type is not what was`)

**Returns:** `throw new`

### cap

```java
<T> private static <T> Dynamic<T> cap(Dynamic<T> p_16858_)
```

**Parameters:**

- `p_16858_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`
