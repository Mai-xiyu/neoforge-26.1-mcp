# ResourceLocationParseRule

**Package:** `net.minecraft.util.parsing.packrat.commands`
**Type:** class
**Implements:** `Rule<StringReader`, `ResourceLocation>`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `INSTANCE` | `Rule<StringReader, ResourceLocation>` |  |

## Methods

### ResourceLocationParseRule

```java
private ResourceLocationParseRule()
```

**Returns:** `private`

### parse

```java
public Optional<ResourceLocation> parse(ParseState<StringReader> p_335633_)
```

**Parameters:**

- `p_335633_` (`ParseState<StringReader>`)

**Returns:** `Optional<ResourceLocation>`
