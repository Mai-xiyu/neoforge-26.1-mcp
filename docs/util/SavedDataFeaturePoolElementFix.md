# SavedDataFeaturePoolElementFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### SavedDataFeaturePoolElementFix

```java
public SavedDataFeaturePoolElementFix(Schema p_145646_)
```

**Parameters:**

- `p_145646_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
public TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### fixTag

```java
<T> private static <T> Dynamic<T> fixTag(Dynamic<T> p_145663_)
```

**Parameters:**

- `p_145663_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### updateChildren

```java
<T> private static <T> Dynamic<T> updateChildren(Dynamic<T> p_145665_)
```

**Parameters:**

- `p_145665_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### updateChildren

```java
private static Stream<? extends Dynamic<?>> updateChildren(Stream<? extends Dynamic<?>> p_145661_)
```

**Parameters:**

- `p_145661_` (`Stream<? extends Dynamic<?>>`)

**Returns:** `private static Stream<? extends Dynamic<?>>`

### get

```java
<T> private static <T> OptionalDynamic<T> get(Dynamic<T> p_145650_, String[]... p_145651_)
```

**Parameters:**

- `p_145650_` (`Dynamic<T>`)
- `p_145651_` (`String[]...`)

**Returns:** `private static <T> OptionalDynamic<T>`

### IllegalArgumentException

```java
throw new IllegalArgumentException("Missing path")
```

**Parameters:**

- `path"` (`"Missing`)

**Returns:** `throw new`

### fixFeature

```java
protected static Dynamic<?> fixFeature(Dynamic<?> p_145648_)
```

**Parameters:**

- `p_145648_` (`Dynamic<?>`)

**Returns:** `Dynamic<?>`

### getReplacement

```java
private static Optional<String> getReplacement(String p_145653_, String p_145654_, String p_145655_, String p_145656_, String p_145657_, String p_145658_, String p_145659_)
```

**Parameters:**

- `p_145653_` (`String`)
- `p_145654_` (`String`)
- `p_145655_` (`String`)
- `p_145656_` (`String`)
- `p_145657_` (`String`)
- `p_145658_` (`String`)
- `p_145659_` (`String`)

**Returns:** `private static Optional<String>`
