# SegmentedAnglePrecision

**Package:** `net.minecraft.util`
**Type:** class

## Methods

### SegmentedAnglePrecision

```java
public SegmentedAnglePrecision(int p_265275_)
```

**Parameters:**

- `p_265275_` (`int`)

**Returns:** `public`

### IllegalArgumentException

```java
throw new IllegalArgumentException("Precision cannot be less than 2 bits")
```

**Parameters:**

- `bits"` (`"Precision cannot be less than 2`)

**Returns:** `throw new`

### IllegalArgumentException

```java
throw new IllegalArgumentException("Precision cannot be greater than 30 bits")
```

**Parameters:**

- `bits"` (`"Precision cannot be greater than 30`)

**Returns:** `throw new`

### isSameAxis

```java
public boolean isSameAxis(int p_265505_, int p_265708_)
```

**Parameters:**

- `p_265505_` (`int`)
- `p_265708_` (`int`)

**Returns:** `public boolean`

### fromDirection

```java
public int fromDirection(Direction p_265731_)
```

**Parameters:**

- `p_265731_` (`Direction`)

**Returns:** `public int`

### fromDegreesWithTurns

```java
public int fromDegreesWithTurns(float p_265346_)
```

**Parameters:**

- `p_265346_` (`float`)

**Returns:** `public int`

### fromDegrees

```java
public int fromDegrees(float p_265688_)
```

**Parameters:**

- `p_265688_` (`float`)

**Returns:** `public int`

### toDegreesWithTurns

```java
public float toDegreesWithTurns(int p_265278_)
```

**Parameters:**

- `p_265278_` (`int`)

**Returns:** `public float`

### toDegrees

```java
public float toDegrees(int p_265623_)
```

**Parameters:**

- `p_265623_` (`int`)

**Returns:** `public float`

### normalize

```java
public int normalize(int p_265542_)
```

**Parameters:**

- `p_265542_` (`int`)

**Returns:** `public int`

### getMask

```java
public int getMask()
```

**Returns:** `public int`
