# SimpleBitStorage

**Package:** `net.minecraft.util`
**Type:** class
**Implements:** `BitStorage`

## Methods

### SimpleBitStorage

```java
public SimpleBitStorage(int p_198164_, int p_198165_, int[] p_198166_)
```

**Parameters:**

- `p_198164_` (`int`)
- `p_198165_` (`int`)
- `p_198166_` (`int[]`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### SimpleBitStorage

```java
public SimpleBitStorage(int p_184717_, int p_184718_)
```

**Parameters:**

- `p_184717_` (`int`)
- `p_184718_` (`int`)

**Returns:** `public`

### SimpleBitStorage

```java
public SimpleBitStorage(int p_184724_, int p_184725_, long[] p_184726_)
```

**Parameters:**

- `p_184724_` (`int`)
- `p_184725_` (`int`)
- `p_184726_` (`long[]`)

**Returns:** `public`

### cellIndex

```java
private int cellIndex(int p_184740_)
```

**Parameters:**

- `p_184740_` (`int`)

**Returns:** `private int`

### getAndSet

```java
public int getAndSet(int p_184731_, int p_184732_)
```

**Parameters:**

- `p_184731_` (`int`)
- `p_184732_` (`int`)

**Returns:** `int`

### set

```java
public void set(int p_184742_, int p_184743_)
```

**Parameters:**

- `p_184742_` (`int`)
- `p_184743_` (`int`)

### get

```java
public int get(int p_184729_)
```

**Parameters:**

- `p_184729_` (`int`)

**Returns:** `int`

### getRaw

```java
public long[] getRaw()
```

**Returns:** `long[]`

### getSize

```java
public int getSize()
```

**Returns:** `int`

### getBits

```java
public int getBits()
```

**Returns:** `int`

### getAll

```java
public void getAll(IntConsumer p_184734_)
```

**Parameters:**

- `p_184734_` (`IntConsumer`)

### unpack

```java
public void unpack(int[] p_198168_)
```

**Parameters:**

- `p_198168_` (`int[]`)

### copy

```java
public BitStorage copy()
```

**Returns:** `BitStorage`

### InitializationException

```java
 InitializationException(String p_184746_)
```

**Parameters:**

- `p_184746_` (`String`)

**Returns:** ``

### super

```java
 super()
```

**Returns:** ``

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `InitializationException` | class |  |
