# SortedArraySet

**Package:** `net.minecraft.util`
**Type:** class<T> extends AbstractSet<T>

## Methods

### SortedArraySet

```java
private SortedArraySet(int p_14244_, Comparator<T> p_14245_)
```

**Parameters:**

- `p_14244_` (`int`)
- `p_14245_` (`Comparator<T>`)

**Returns:** `private`

### create

```java
public static <T extends Comparable<T>> SortedArraySet<T> create()
```

**Returns:** `public static <T extends Comparable<T>> SortedArraySet<T>`

### create

```java
return create()
```

**Returns:** `return`

### create

```java
public static <T extends Comparable<T>> SortedArraySet<T> create(int p_14247_)
```

**Parameters:**

- `p_14247_` (`int`)

**Returns:** `public static <T extends Comparable<T>> SortedArraySet<T>`

### create

```java
<T> public static <T> SortedArraySet<T> create(Comparator<T> p_144977_)
```

**Parameters:**

- `p_144977_` (`Comparator<T>`)

**Returns:** `public static <T> SortedArraySet<T>`

### create

```java
return create()
```

**Returns:** `return`

### create

```java
<T> public static <T> SortedArraySet<T> create(Comparator<T> p_144979_, int p_144980_)
```

**Parameters:**

- `p_144979_` (`Comparator<T>`)
- `p_144980_` (`int`)

**Returns:** `public static <T> SortedArraySet<T>`

### castRawArray

```java
<T> private static <T> T[] castRawArray(Object[] p_14259_)
```

**Parameters:**

- `p_14259_` (`Object[]`)

**Returns:** `private static <T> T[]`

### findIndex

```java
private int findIndex(T p_14270_)
```

**Parameters:**

- `p_14270_` (`T`)

**Returns:** `private int`

### getInsertionPosition

```java
private static int getInsertionPosition(int p_14264_)
```

**Parameters:**

- `p_14264_` (`int`)

**Returns:** `private static int`

### add

```java
public boolean add(T p_14261_)
```

**Parameters:**

- `p_14261_` (`T`)

**Returns:** `boolean`

### grow

```java
private void grow(int p_14268_)
```

**Parameters:**

- `p_14268_` (`int`)

**Returns:** `private void`

### addInternal

```java
private void addInternal(T p_14256_, int p_14257_)
```

**Parameters:**

- `p_14256_` (`T`)
- `p_14257_` (`int`)

**Returns:** `private void`

### removeInternal

```java
void removeInternal(int p_14275_)
```

**Parameters:**

- `p_14275_` (`int`)

### getInternal

```java
private T getInternal(int p_14277_)
```

**Parameters:**

- `p_14277_` (`int`)

**Returns:** `private T`

### addOrGet

```java
public T addOrGet(T p_14254_)
```

**Parameters:**

- `p_14254_` (`T`)

**Returns:** `public T`

### remove

```java
public boolean remove(Object p_14282_)
```

**Parameters:**

- `p_14282_` (`Object`)

**Returns:** `boolean`

### get

```java
public T get(T p_144982_)
```

**Parameters:**

- `p_144982_` (`T`)

**Returns:** `T`

### first

```java
public T first()
```

**Returns:** `public T`

### last

```java
public T last()
```

**Returns:** `public T`

### contains

```java
public boolean contains(Object p_14273_)
```

**Parameters:**

- `p_14273_` (`Object`)

**Returns:** `boolean`

### iterator

```java
public Iterator<T> iterator()
```

**Returns:** `Iterator<T>`

### size

```java
public int size()
```

**Returns:** `int`

### toArray

```java
public Object[] toArray()
```

**Returns:** `Object[]`

### toArray

```java
public <U> U[] toArray(U[] p_14286_)
```

**Parameters:**

- `p_14286_` (`U[]`)

**Returns:** `U[]`

### clear

```java
public void clear()
```

### equals

```java
public boolean equals(Object p_14279_)
```

**Parameters:**

- `p_14279_` (`Object`)

**Returns:** `boolean`

### hasNext

```java
public boolean hasNext()
```

**Returns:** `boolean`

### next

```java
public T next()
```

**Returns:** `T`

### NoSuchElementException

```java
throw new NoSuchElementException()
```

**Returns:** `throw new`

### remove

```java
public void remove()
```

### IllegalStateException

```java
throw new IllegalStateException()
```

**Returns:** `throw new`
