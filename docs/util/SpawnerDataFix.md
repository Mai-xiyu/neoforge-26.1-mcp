# SpawnerDataFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### SpawnerDataFix

```java
public SpawnerDataFix(Schema p_185133_)
```

**Parameters:**

- `p_185133_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### wrapEntityToSpawnData

```java
<T> private <T> Typed<T> wrapEntityToSpawnData(Type<T> p_185141_, Typed<?> p_185142_)
```

**Parameters:**

- `p_185141_` (`Type<T>`)
- `p_185142_` (`Typed<?>`)

**Returns:** `private <T> Typed<T>`

### wrapSpawnPotentialsToWeightedEntries

```java
<T> private <T> Typed<T> wrapSpawnPotentialsToWeightedEntries(Type<T> p_185147_, Typed<?> p_185148_)
```

**Parameters:**

- `p_185147_` (`Type<T>`)
- `p_185148_` (`Typed<?>`)

**Returns:** `private <T> Typed<T>`
