# StatsRenameFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### StatsRenameFix

```java
public StatsRenameFix(Schema p_145705_, String p_145706_, Map<String, String> p_145707_)
```

**Parameters:**

- `p_145705_` (`Schema`)
- `p_145706_` (`String`)
- `p_145707_` (`Map<String, String>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### createCriteriaRule

```java
private TypeRewriteRule createCriteriaRule()
```

**Returns:** `private TypeRewriteRule`

### IllegalStateException

```java
throw new IllegalStateException("Failed to find custom criterion type variant")
```

**Parameters:**

- `variant"` (`"Failed to find custom criterion type`)

**Returns:** `throw new`

### createStatRule

```java
private TypeRewriteRule createStatRule()
```

**Returns:** `private TypeRewriteRule`
