# StringDecomposer

**Package:** `net.minecraft.util`
**Type:** class

## Methods

### feedChar

```java
private static boolean feedChar(Style p_14333_, FormattedCharSink p_14334_, int p_14335_, char p_14336_)
```

**Parameters:**

- `p_14333_` (`Style`)
- `p_14334_` (`FormattedCharSink`)
- `p_14335_` (`int`)
- `p_14336_` (`char`)

**Returns:** `private static boolean`

### iterate

```java
public static boolean iterate(String p_14318_, Style p_14319_, FormattedCharSink p_14320_)
```

**Parameters:**

- `p_14318_` (`String`)
- `p_14319_` (`Style`)
- `p_14320_` (`FormattedCharSink`)

**Returns:** `public static boolean`

### iterateBackwards

```java
public static boolean iterateBackwards(String p_14338_, Style p_14339_, FormattedCharSink p_14340_)
```

**Parameters:**

- `p_14338_` (`String`)
- `p_14339_` (`Style`)
- `p_14340_` (`FormattedCharSink`)

**Returns:** `public static boolean`

### iterateFormatted

```java
public static boolean iterateFormatted(String p_14347_, Style p_14348_, FormattedCharSink p_14349_)
```

**Parameters:**

- `p_14347_` (`String`)
- `p_14348_` (`Style`)
- `p_14349_` (`FormattedCharSink`)

**Returns:** `public static boolean`

### iterateFormatted

```java
return iterateFormatted()
```

**Returns:** `return`

### iterateFormatted

```java
public static boolean iterateFormatted(String p_14307_, int p_14308_, Style p_14309_, FormattedCharSink p_14310_)
```

**Parameters:**

- `p_14307_` (`String`)
- `p_14308_` (`int`)
- `p_14309_` (`Style`)
- `p_14310_` (`FormattedCharSink`)

**Returns:** `public static boolean`

### iterateFormatted

```java
return iterateFormatted()
```

**Returns:** `return`

### iterateFormatted

```java
public static boolean iterateFormatted(String p_14312_, int p_14313_, Style p_14314_, Style p_14315_, FormattedCharSink p_14316_)
```

**Parameters:**

- `p_14312_` (`String`)
- `p_14313_` (`int`)
- `p_14314_` (`Style`)
- `p_14315_` (`Style`)
- `p_14316_` (`FormattedCharSink`)

**Returns:** `public static boolean`

### iterateFormatted

```java
public static boolean iterateFormatted(FormattedText p_14329_, Style p_14330_, FormattedCharSink p_14331_)
```

**Parameters:**

- `p_14329_` (`FormattedText`)
- `p_14330_` (`Style`)
- `p_14331_` (`FormattedCharSink`)

**Returns:** `public static boolean`

### filterBrokenSurrogates

```java
public static String filterBrokenSurrogates(String p_14305_)
```

**Parameters:**

- `p_14305_` (`String`)

**Returns:** `public static String`

### getPlainText

```java
public static String getPlainText(FormattedText p_14327_)
```

**Parameters:**

- `p_14327_` (`FormattedText`)

**Returns:** `public static String`
