# StringUtil

**Package:** `net.minecraft.util`
**Type:** class

## Methods

### formatTickDuration

```java
public static String formatTickDuration(int p_14405_, float p_314711_)
```

**Parameters:**

- `p_14405_` (`int`)
- `p_314711_` (`float`)

**Returns:** `public static String`

### stripColor

```java
public static String stripColor(String p_14407_)
```

**Parameters:**

- `p_14407_` (`String`)

**Returns:** `public static String`

### isNullOrEmpty

```java
public static boolean isNullOrEmpty(String p_14409_)
```

**Parameters:**

- `p_14409_` (`String`)

**Returns:** `public static boolean`

### truncateStringIfNecessary

```java
public static String truncateStringIfNecessary(String p_144999_, int p_145000_, boolean p_145001_)
```

**Parameters:**

- `p_144999_` (`String`)
- `p_145000_` (`int`)
- `p_145001_` (`boolean`)

**Returns:** `public static String`

### lineCount

```java
public static int lineCount(String p_145003_)
```

**Parameters:**

- `p_145003_` (`String`)

**Returns:** `public static int`

### endsWithNewLine

```java
public static boolean endsWithNewLine(String p_145005_)
```

**Parameters:**

- `p_145005_` (`String`)

**Returns:** `public static boolean`

### trimChatMessage

```java
public static String trimChatMessage(String p_216470_)
```

**Parameters:**

- `p_216470_` (`String`)

**Returns:** `public static String`

### truncateStringIfNecessary

```java
return truncateStringIfNecessary()
```

**Returns:** `return`

### isAllowedChatCharacter

```java
public static boolean isAllowedChatCharacter(char p_330599_)
```

**Parameters:**

- `p_330599_` (`char`)

**Returns:** `public static boolean`

### isValidPlayerName

```java
public static boolean isValidPlayerName(String p_330229_)
```

**Parameters:**

- `p_330229_` (`String`)

**Returns:** `public static boolean`

### filterText

```java
public static String filterText(String p_330272_)
```

**Parameters:**

- `p_330272_` (`String`)

**Returns:** `public static String`

### filterText

```java
return filterText()
```

**Returns:** `return`

### filterText

```java
public static String filterText(String p_332058_, boolean p_330572_)
```

**Parameters:**

- `p_332058_` (`String`)
- `p_330572_` (`boolean`)

**Returns:** `public static String`

### isWhitespace

```java
public static boolean isWhitespace(int p_331212_)
```

**Parameters:**

- `p_331212_` (`int`)

**Returns:** `public static boolean`

### isBlank

```java
public static boolean isBlank(String p_330852_)
```

**Parameters:**

- `p_330852_` (`String`)

**Returns:** `public static boolean`
