# StructureReferenceCountFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### StructureReferenceCountFix

```java
public StructureReferenceCountFix(Schema p_16961_, boolean p_16962_)
```

**Parameters:**

- `p_16961_` (`Schema`)
- `p_16962_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### setCountToAtLeastOne

```java
<T> private static <T> Dynamic<T> setCountToAtLeastOne(Dynamic<T> p_16966_)
```

**Parameters:**

- `p_16966_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`
