# TeamDisplayNameFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### TeamDisplayNameFix

```java
public TeamDisplayNameFix(Schema p_17001_, boolean p_17002_)
```

**Parameters:**

- `p_17001_` (`Schema`)
- `p_17002_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### IllegalStateException

```java
throw new IllegalStateException("Team type is not what was expected.")
```

**Parameters:**

- `expected."` (`"Team type is not what was`)

**Returns:** `throw new`
