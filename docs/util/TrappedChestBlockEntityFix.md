# TrappedChestBlockEntityFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### TrappedChestBlockEntityFix

```java
public TrappedChestBlockEntityFix(Schema p_17018_, boolean p_17019_)
```

**Parameters:**

- `p_17018_` (`Schema`)
- `p_17019_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
public TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### IllegalStateException

```java
throw new IllegalStateException("Tile entity type is not a list type.")
```

**Parameters:**

- `type."` (`"Tile entity type is not a list`)

**Returns:** `throw new`

### IllegalStateException

```java
throw new IllegalStateException("Expecting sections to be a list.")
```

**Parameters:**

- `list."` (`"Expecting sections to be a`)

**Returns:** `throw new`

### TrappedChestSection

```java
public TrappedChestSection(Typed<?> p_17050_, Schema p_17051_)
```

**Parameters:**

- `p_17050_` (`Typed<?>`)
- `p_17051_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### skippable

```java
protected boolean skippable()
```

**Returns:** `boolean`

### isTrappedChest

```java
public boolean isTrappedChest(int p_17054_)
```

**Parameters:**

- `p_17054_` (`int`)

**Returns:** `public boolean`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `TrappedChestSection` | class |  |
