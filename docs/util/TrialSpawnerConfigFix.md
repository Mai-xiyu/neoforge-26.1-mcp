# TrialSpawnerConfigFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `NamedEntityWriteReadFix`

## Methods

### TrialSpawnerConfigFix

```java
public TrialSpawnerConfigFix(Schema p_338323_)
```

**Parameters:**

- `p_338323_` (`Schema`)

**Returns:** `public`

### super

```java
 super("Trial Spawner config tag fixer")
```

**Parameters:**

- `fixer"` (`"Trial Spawner config tag`)

**Returns:** ``

### moveToConfigTag

```java
<T> private static <T> Dynamic<T> moveToConfigTag(Dynamic<T> p_338221_)
```

**Parameters:**

- `p_338221_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### fix

```java
protected <T> Dynamic<T> fix(Dynamic<T> p_338437_)
```

**Parameters:**

- `p_338437_` (`Dynamic<T>`)

**Returns:** `Dynamic<T>`

### moveToConfigTag

```java
return moveToConfigTag()
```

**Returns:** `return`
