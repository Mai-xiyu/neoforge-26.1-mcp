# UniformFloat

**Package:** `net.minecraft.util.valueproviders`
**Type:** class
**Extends:** `FloatProvider`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<UniformFloat>` |  |

## Methods

### UniformFloat

```java
private UniformFloat(float p_146595_, float p_146596_)
```

**Parameters:**

- `p_146595_` (`float`)
- `p_146596_` (`float`)

**Returns:** `private`

### of

```java
public static UniformFloat of(float p_146606_, float p_146607_)
```

**Parameters:**

- `p_146606_` (`float`)
- `p_146607_` (`float`)

**Returns:** `public static UniformFloat`

### IllegalArgumentException

```java
throw new IllegalArgumentException("Max must exceed min")
```

**Parameters:**

- `min"` (`"Max must exceed`)

**Returns:** `throw new`

### UniformFloat

```java
return new UniformFloat()
```

**Returns:** `return new`

### sample

```java
public float sample(RandomSource p_216866_)
```

**Parameters:**

- `p_216866_` (`RandomSource`)

**Returns:** `float`

### getMinValue

```java
public float getMinValue()
```

**Returns:** `float`

### getMaxValue

```java
public float getMaxValue()
```

**Returns:** `float`

### getType

```java
public FloatProviderType<?> getType()
```

**Returns:** `FloatProviderType<?>`

### toString

```java
public String toString()
```

**Returns:** `String`
