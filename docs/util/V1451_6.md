# V1451_6

**Package:** `net.minecraft.util.datafix.schemas`
**Type:** class
**Extends:** `NamespacedSchema`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `SPECIAL_OBJECTIVE_MARKER` | `String` |  |
| `UNPACK_OBJECTIVE_ID` | `HookFunction` |  |
| `REPACK_OBJECTIVE_ID` | `HookFunction` |  |

## Methods

### apply

```java
public <T> T apply(DynamicOps<T> p_181096_, T p_181097_)
```

**Parameters:**

- `p_181096_` (`DynamicOps<T>`)
- `p_181097_` (`T`)

**Returns:** `T`

### apply

```java
public <T> T apply(DynamicOps<T> p_181105_, T p_181106_)
```

**Parameters:**

- `p_181105_` (`DynamicOps<T>`)
- `p_181106_` (`T`)

**Returns:** `T`

### V1451_6

```java
public V1451_6(int p_17532_, Schema p_17533_)
```

**Parameters:**

- `p_17532_` (`int`)
- `p_17533_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### registerTypes

```java
public void registerTypes(Schema p_17540_, Map<String, Supplier<TypeTemplate>> p_17541_, Map<String, Supplier<TypeTemplate>> p_17542_)
```

**Parameters:**

- `p_17540_` (`Schema`)
- `p_17541_` (`Map<String, Supplier<TypeTemplate>>`)
- `p_17542_` (`Map<String, Supplier<TypeTemplate>>`)

### createCriterionTypes

```java
protected static Map<String, Supplier<TypeTemplate>> createCriterionTypes(Schema p_181078_)
```

**Parameters:**

- `p_181078_` (`Schema`)

**Returns:** `protected static Map<String, Supplier<TypeTemplate>>`

### packNamespacedWithDot

```java
public static String packNamespacedWithDot(String p_294923_)
```

**Parameters:**

- `p_294923_` (`String`)

**Returns:** `public static String`
