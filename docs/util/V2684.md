# V2684

**Package:** `net.minecraft.util.datafix.schemas`
**Type:** class
**Extends:** `NamespacedSchema`

## Methods

### V2684

```java
public V2684(int p_145856_, Schema p_145857_)
```

**Parameters:**

- `p_145856_` (`int`)
- `p_145857_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### registerTypes

```java
public void registerTypes(Schema p_216760_, Map<String, Supplier<TypeTemplate>> p_216761_, Map<String, Supplier<TypeTemplate>> p_216762_)
```

**Parameters:**

- `p_216760_` (`Schema`)
- `p_216761_` (`Map<String, Supplier<TypeTemplate>>`)
- `p_216762_` (`Map<String, Supplier<TypeTemplate>>`)

### registerBlockEntities

```java
public Map<String, Supplier<TypeTemplate>> registerBlockEntities(Schema p_145859_)
```

**Parameters:**

- `p_145859_` (`Schema`)

**Returns:** `Map<String, Supplier<TypeTemplate>>`
