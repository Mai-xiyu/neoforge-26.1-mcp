# V704

**Package:** `net.minecraft.util.datafix.schemas`
**Type:** class
**Extends:** `Schema`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `ITEM_TO_BLOCKENTITY` | `Map<String, String>` |  |

## Methods

### apply

```java
public <T> T apply(DynamicOps<T> p_18070_, T p_18071_)
```

**Parameters:**

- `p_18070_` (`DynamicOps<T>`)
- `p_18071_` (`T`)

**Returns:** `T`

### V704

```java
public V704(int p_18036_, Schema p_18037_)
```

**Parameters:**

- `p_18036_` (`int`)
- `p_18037_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### registerInventory

```java
protected static void registerInventory(Schema p_18044_, Map<String, Supplier<TypeTemplate>> p_18045_, String p_18046_)
```

**Parameters:**

- `p_18044_` (`Schema`)
- `p_18045_` (`Map<String, Supplier<TypeTemplate>>`)
- `p_18046_` (`String`)

**Returns:** `protected static void`

### getChoiceType

```java
public Type<?> getChoiceType(TypeReference p_18060_, String p_18061_)
```

**Parameters:**

- `p_18060_` (`TypeReference`)
- `p_18061_` (`String`)

**Returns:** `Type<?>`

### registerBlockEntities

```java
public Map<String, Supplier<TypeTemplate>> registerBlockEntities(Schema p_18063_)
```

**Parameters:**

- `p_18063_` (`Schema`)

**Returns:** `Map<String, Supplier<TypeTemplate>>`

### registerInventory

```java
 registerInventory()
```

**Returns:** ``

### registerInventory

```java
 registerInventory()
```

**Returns:** ``

### registerInventory

```java
 registerInventory()
```

**Returns:** ``

### registerInventory

```java
 registerInventory()
```

**Returns:** ``

### registerInventory

```java
 registerInventory()
```

**Returns:** ``

### registerInventory

```java
 registerInventory()
```

**Returns:** ``

### registerTypes

```java
public void registerTypes(Schema p_18065_, Map<String, Supplier<TypeTemplate>> p_18066_, Map<String, Supplier<TypeTemplate>> p_18067_)
```

**Parameters:**

- `p_18065_` (`Schema`)
- `p_18066_` (`Map<String, Supplier<TypeTemplate>>`)
- `p_18067_` (`Map<String, Supplier<TypeTemplate>>`)
