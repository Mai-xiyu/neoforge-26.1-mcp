# V705

**Package:** `net.minecraft.util.datafix.schemas`
**Type:** class
**Extends:** `NamespacedSchema`

## Methods

### apply

```java
public <T> T apply(DynamicOps<T> p_18167_, T p_18168_)
```

**Parameters:**

- `p_18167_` (`DynamicOps<T>`)
- `p_18168_` (`T`)

**Returns:** `T`

### V705

```java
public V705(int p_18075_, Schema p_18076_)
```

**Parameters:**

- `p_18075_` (`int`)
- `p_18076_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### registerMob

```java
protected static void registerMob(Schema p_18083_, Map<String, Supplier<TypeTemplate>> p_18084_, String p_18085_)
```

**Parameters:**

- `p_18083_` (`Schema`)
- `p_18084_` (`Map<String, Supplier<TypeTemplate>>`)
- `p_18085_` (`String`)

**Returns:** `protected static void`

### registerThrowableProjectile

```java
protected static void registerThrowableProjectile(Schema p_18094_, Map<String, Supplier<TypeTemplate>> p_18095_, String p_18096_)
```

**Parameters:**

- `p_18094_` (`Schema`)
- `p_18095_` (`Map<String, Supplier<TypeTemplate>>`)
- `p_18096_` (`String`)

**Returns:** `protected static void`

### registerEntities

```java
public Map<String, Supplier<TypeTemplate>> registerEntities(Schema p_18148_)
```

**Parameters:**

- `p_18148_` (`Schema`)

**Returns:** `Map<String, Supplier<TypeTemplate>>`

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerThrowableProjectile

```java
 registerThrowableProjectile()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerThrowableProjectile

```java
 registerThrowableProjectile()
```

**Returns:** ``

### registerThrowableProjectile

```java
 registerThrowableProjectile()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerThrowableProjectile

```java
 registerThrowableProjectile()
```

**Returns:** ``

### registerThrowableProjectile

```java
 registerThrowableProjectile()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerThrowableProjectile

```java
 registerThrowableProjectile()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerThrowableProjectile

```java
 registerThrowableProjectile()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerTypes

```java
public void registerTypes(Schema p_18150_, Map<String, Supplier<TypeTemplate>> p_18151_, Map<String, Supplier<TypeTemplate>> p_18152_)
```

**Parameters:**

- `p_18150_` (`Schema`)
- `p_18151_` (`Map<String, Supplier<TypeTemplate>>`)
- `p_18152_` (`Map<String, Supplier<TypeTemplate>>`)
