# V99

**Package:** `net.minecraft.util.datafix.schemas`
**Type:** class
**Extends:** `Schema`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `ITEM_TO_ENTITY` | `Map<String, String>` |  |
| `ADD_NAMES` | `HookFunction` |  |

## Methods

### apply

```java
public <T> T apply(DynamicOps<T> p_18312_, T p_18313_)
```

**Parameters:**

- `p_18312_` (`DynamicOps<T>`)
- `p_18313_` (`T`)

**Returns:** `T`

### V99

```java
public V99(int p_18185_, Schema p_18186_)
```

**Parameters:**

- `p_18185_` (`int`)
- `p_18186_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### equipment

```java
protected static TypeTemplate equipment(Schema p_18189_)
```

**Parameters:**

- `p_18189_` (`Schema`)

**Returns:** `protected static TypeTemplate`

### registerMob

```java
protected static void registerMob(Schema p_18194_, Map<String, Supplier<TypeTemplate>> p_18195_, String p_18196_)
```

**Parameters:**

- `p_18194_` (`Schema`)
- `p_18195_` (`Map<String, Supplier<TypeTemplate>>`)
- `p_18196_` (`String`)

**Returns:** `protected static void`

### registerThrowableProjectile

```java
protected static void registerThrowableProjectile(Schema p_18225_, Map<String, Supplier<TypeTemplate>> p_18226_, String p_18227_)
```

**Parameters:**

- `p_18225_` (`Schema`)
- `p_18226_` (`Map<String, Supplier<TypeTemplate>>`)
- `p_18227_` (`String`)

**Returns:** `protected static void`

### registerMinecart

```java
protected static void registerMinecart(Schema p_18237_, Map<String, Supplier<TypeTemplate>> p_18238_, String p_18239_)
```

**Parameters:**

- `p_18237_` (`Schema`)
- `p_18238_` (`Map<String, Supplier<TypeTemplate>>`)
- `p_18239_` (`String`)

**Returns:** `protected static void`

### registerInventory

```java
protected static void registerInventory(Schema p_18247_, Map<String, Supplier<TypeTemplate>> p_18248_, String p_18249_)
```

**Parameters:**

- `p_18247_` (`Schema`)
- `p_18248_` (`Map<String, Supplier<TypeTemplate>>`)
- `p_18249_` (`String`)

**Returns:** `protected static void`

### registerEntities

```java
public Map<String, Supplier<TypeTemplate>> registerEntities(Schema p_18305_)
```

**Parameters:**

- `p_18305_` (`Schema`)

**Returns:** `Map<String, Supplier<TypeTemplate>>`

### registerThrowableProjectile

```java
 registerThrowableProjectile()
```

**Returns:** ``

### registerThrowableProjectile

```java
 registerThrowableProjectile()
```

**Returns:** ``

### registerThrowableProjectile

```java
 registerThrowableProjectile()
```

**Returns:** ``

### registerThrowableProjectile

```java
 registerThrowableProjectile()
```

**Returns:** ``

### registerThrowableProjectile

```java
 registerThrowableProjectile()
```

**Returns:** ``

### registerThrowableProjectile

```java
 registerThrowableProjectile()
```

**Returns:** ``

### registerThrowableProjectile

```java
 registerThrowableProjectile()
```

**Returns:** ``

### registerMinecart

```java
 registerMinecart()
```

**Returns:** ``

### registerMinecart

```java
 registerMinecart()
```

**Returns:** ``

### registerMinecart

```java
 registerMinecart()
```

**Returns:** ``

### registerMinecart

```java
 registerMinecart()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerMob

```java
 registerMob()
```

**Returns:** ``

### registerBlockEntities

```java
public Map<String, Supplier<TypeTemplate>> registerBlockEntities(Schema p_18303_)
```

**Parameters:**

- `p_18303_` (`Schema`)

**Returns:** `Map<String, Supplier<TypeTemplate>>`

### registerInventory

```java
 registerInventory()
```

**Returns:** ``

### registerInventory

```java
 registerInventory()
```

**Returns:** ``

### registerInventory

```java
 registerInventory()
```

**Returns:** ``

### registerInventory

```java
 registerInventory()
```

**Returns:** ``

### registerInventory

```java
 registerInventory()
```

**Returns:** ``

### registerInventory

```java
 registerInventory()
```

**Returns:** ``

### registerTypes

```java
public void registerTypes(Schema p_18307_, Map<String, Supplier<TypeTemplate>> p_18308_, Map<String, Supplier<TypeTemplate>> p_18309_)
```

**Parameters:**

- `p_18307_` (`Schema`)
- `p_18308_` (`Map<String, Supplier<TypeTemplate>>`)
- `p_18309_` (`Map<String, Supplier<TypeTemplate>>`)

### addNames

```java
<T> protected static <T> T addNames(Dynamic<T> p_18206_, Map<String, String> p_18207_, Map<String, String> p_332665_)
```

**Parameters:**

- `p_18206_` (`Dynamic<T>`)
- `p_18207_` (`Map<String, String>`)
- `p_332665_` (`Map<String, String>`)

**Returns:** `protected static <T> T`
