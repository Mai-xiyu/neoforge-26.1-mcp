# WallPropertyFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### WallPropertyFix

```java
public WallPropertyFix(Schema p_17154_, boolean p_17155_)
```

**Parameters:**

- `p_17154_` (`Schema`)
- `p_17155_` (`boolean`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
public TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### mapProperty

```java
private static String mapProperty(String p_17164_)
```

**Parameters:**

- `p_17164_` (`String`)

**Returns:** `private static String`

### fixWallProperty

```java
<T> private static <T> Dynamic<T> fixWallProperty(Dynamic<T> p_17161_, String p_17162_)
```

**Parameters:**

- `p_17161_` (`Dynamic<T>`)
- `p_17162_` (`String`)

**Returns:** `private static <T> Dynamic<T>`

### upgradeBlockStateTag

```java
<T> private static <T> Dynamic<T> upgradeBlockStateTag(Dynamic<T> p_17159_)
```

**Parameters:**

- `p_17159_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### fixWallProperty

```java
return fixWallProperty()
```

**Returns:** `return`
