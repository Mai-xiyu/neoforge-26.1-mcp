# WorldGenSettingsFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `DataFix`

## Methods

### WorldGenSettingsFix

```java
public WorldGenSettingsFix(Schema p_17173_)
```

**Parameters:**

- `p_17173_` (`Schema`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### makeRule

```java
protected TypeRewriteRule makeRule()
```

**Returns:** `TypeRewriteRule`

### noise

```java
<T> private static <T> Dynamic<T> noise(long p_17175_, DynamicLike<T> p_17176_, Dynamic<T> p_17177_, Dynamic<T> p_17178_)
```

**Parameters:**

- `p_17175_` (`long`)
- `p_17176_` (`DynamicLike<T>`)
- `p_17177_` (`Dynamic<T>`)
- `p_17178_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### vanillaBiomeSource

```java
<T> private static <T> Dynamic<T> vanillaBiomeSource(Dynamic<T> p_17196_, long p_17197_, boolean p_17198_, boolean p_17199_)
```

**Parameters:**

- `p_17196_` (`Dynamic<T>`)
- `p_17197_` (`long`)
- `p_17198_` (`boolean`)
- `p_17199_` (`boolean`)

**Returns:** `private static <T> Dynamic<T>`

### fix

```java
<T> private static <T> Dynamic<T> fix(Dynamic<T> p_17186_)
```

**Parameters:**

- `p_17186_` (`Dynamic<T>`)

**Returns:** `private static <T> Dynamic<T>`

### defaultOverworld

```java
<T> protected static <T> Dynamic<T> defaultOverworld(Dynamic<T> p_17188_, long p_17189_)
```

**Parameters:**

- `p_17188_` (`Dynamic<T>`)
- `p_17189_` (`long`)

**Returns:** `protected static <T> Dynamic<T>`

### vanillaLevels

```java
<T> protected static <T> T vanillaLevels(Dynamic<T> p_17191_, long p_17192_, Dynamic<T> p_17193_, boolean p_17194_)
```

**Parameters:**

- `p_17191_` (`Dynamic<T>`)
- `p_17192_` (`long`)
- `p_17193_` (`Dynamic<T>`)
- `p_17194_` (`boolean`)

**Returns:** `protected static <T> T`

### fixFlatStructures

```java
<T> private static <T> Map<Dynamic<T>, Dynamic<T>> fixFlatStructures(DynamicOps<T> p_17218_, OptionalDynamic<T> p_17219_)
```

**Parameters:**

- `p_17218_` (`DynamicOps<T>`)
- `p_17219_` (`OptionalDynamic<T>`)

**Returns:** `private static <T> Map<Dynamic<T>, Dynamic<T>>`

### setSpacing

```java
 setSpacing()
```

**Returns:** ``

### setSpacing

```java
 setSpacing()
```

**Returns:** ``

### setSpacing

```java
 setSpacing()
```

**Returns:** ``

### setSpacing

```java
 setSpacing()
```

**Returns:** ``

### setSpacing

```java
 setSpacing()
```

**Returns:** ``

### setSpacing

```java
 setSpacing()
```

**Returns:** ``

### setSpacing

```java
 setSpacing()
```

**Returns:** ``

### setSpacing

```java
 setSpacing()
```

**Returns:** ``

### setSpacing

```java
 setSpacing()
```

**Returns:** ``

### getInt

```java
private static int getInt(String p_17229_, int p_17230_)
```

**Parameters:**

- `p_17229_` (`String`)
- `p_17230_` (`int`)

**Returns:** `private static int`

### getInt

```java
private static int getInt(String p_17232_, int p_17233_, int p_17234_)
```

**Parameters:**

- `p_17232_` (`String`)
- `p_17233_` (`int`)
- `p_17234_` (`int`)

**Returns:** `private static int`

### setSpacing

```java
private static void setSpacing(Map<String, WorldGenSettingsFix.StructureFeatureConfiguration> p_17236_, String p_17237_, String p_17238_, int p_17239_)
```

**Parameters:**

- `p_17236_` (`Map<String, WorldGenSettingsFix.StructureFeatureConfiguration>`)
- `p_17237_` (`String`)
- `p_17238_` (`String`)
- `p_17239_` (`int`)

**Returns:** `private static void`

### StructureFeatureConfiguration

```java
public StructureFeatureConfiguration(int p_17271_, int p_17272_, int p_17273_)
```

**Parameters:**

- `p_17271_` (`int`)
- `p_17272_` (`int`)
- `p_17273_` (`int`)

**Returns:** `public`

### serialize

```java
<T> public <T> Dynamic<T> serialize(DynamicOps<T> p_17277_)
```

**Parameters:**

- `p_17277_` (`DynamicOps<T>`)

**Returns:** `public <T> Dynamic<T>`
