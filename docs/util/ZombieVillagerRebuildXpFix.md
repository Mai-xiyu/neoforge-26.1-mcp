# ZombieVillagerRebuildXpFix

**Package:** `net.minecraft.util.datafix.fixes`
**Type:** class
**Extends:** `NamedEntityFix`

## Methods

### ZombieVillagerRebuildXpFix

```java
public ZombieVillagerRebuildXpFix(Schema p_17298_, boolean p_17299_)
```

**Parameters:**

- `p_17298_` (`Schema`)
- `p_17299_` (`boolean`)

**Returns:** `public`

### super

```java
 super("Zombie Villager XP rebuild")
```

**Parameters:**

- `rebuild"` (`"Zombie Villager XP`)

**Returns:** ``

### fix

```java
protected Typed<?> fix(Typed<?> p_17301_)
```

**Parameters:**

- `p_17301_` (`Typed<?>`)

**Returns:** `Typed<?>`
