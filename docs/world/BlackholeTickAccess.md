# BlackholeTickAccess

**Package:** `net.minecraft.world.ticks`
**Type:** class

## Methods

### schedule

```java
public void schedule(ScheduledTick<Object> p_193149_)
```

**Parameters:**

- `p_193149_` (`ScheduledTick<Object>`)

### hasScheduledTick

```java
public boolean hasScheduledTick(BlockPos p_193151_, Object p_193152_)
```

**Parameters:**

- `p_193151_` (`BlockPos`)
- `p_193152_` (`Object`)

**Returns:** `boolean`

### count

```java
public int count()
```

**Returns:** `int`

### schedule

```java
public void schedule(ScheduledTick<Object> p_193156_)
```

**Parameters:**

- `p_193156_` (`ScheduledTick<Object>`)

### hasScheduledTick

```java
public boolean hasScheduledTick(BlockPos p_193158_, Object p_193159_)
```

**Parameters:**

- `p_193158_` (`BlockPos`)
- `p_193159_` (`Object`)

**Returns:** `boolean`

### willTickThisTick

```java
public boolean willTickThisTick(BlockPos p_193161_, Object p_193162_)
```

**Parameters:**

- `p_193161_` (`BlockPos`)
- `p_193162_` (`Object`)

**Returns:** `boolean`

### count

```java
public int count()
```

**Returns:** `int`

### emptyContainer

```java
<T> public static <T> TickContainerAccess<T> emptyContainer()
```

**Returns:** `public static <T> TickContainerAccess<T>`

### emptyLevelList

```java
<T> public static <T> LevelTickAccess<T> emptyLevelList()
```

**Returns:** `public static <T> LevelTickAccess<T>`
