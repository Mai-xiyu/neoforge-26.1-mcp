# BlockCollisions

**Package:** `net.minecraft.world.level`
**Type:** class<T> extends AbstractIterator<T>

## Methods

### BlockCollisions

```java
public BlockCollisions(CollisionGetter p_286817_, Entity p_286246_, AABB p_286624_, boolean p_286354_, BiFunction<BlockPos.MutableBlockPos, VoxelShape, T> p_286303_)
```

**Parameters:**

- `p_286817_` (`CollisionGetter`)
- `p_286246_` (`Entity`)
- `p_286624_` (`AABB`)
- `p_286354_` (`boolean`)
- `p_286303_` (`BiFunction<BlockPos.MutableBlockPos, VoxelShape, T>`)

**Returns:** `public`

### computeNext

```java
protected T computeNext()
```

**Returns:** `T`
