# DragonRespawnAnimation

**Package:** `net.minecraft.world.level.dimension.end`
**Type:** enum

## Methods

### tick

```java
public void tick(ServerLevel p_64017_, EndDragonFight p_64018_, List<EndCrystal> p_64019_, int p_64020_, BlockPos p_64021_)
```

**Parameters:**

- `p_64017_` (`ServerLevel`)
- `p_64018_` (`EndDragonFight`)
- `p_64019_` (`List<EndCrystal>`)
- `p_64020_` (`int`)
- `p_64021_` (`BlockPos`)

### tick

```java
public void tick(ServerLevel p_64026_, EndDragonFight p_64027_, List<EndCrystal> p_64028_, int p_64029_, BlockPos p_64030_)
```

**Parameters:**

- `p_64026_` (`ServerLevel`)
- `p_64027_` (`EndDragonFight`)
- `p_64028_` (`List<EndCrystal>`)
- `p_64029_` (`int`)
- `p_64030_` (`BlockPos`)

### tick

```java
public void tick(ServerLevel p_64035_, EndDragonFight p_64036_, List<EndCrystal> p_64037_, int p_64038_, BlockPos p_64039_)
```

**Parameters:**

- `p_64035_` (`ServerLevel`)
- `p_64036_` (`EndDragonFight`)
- `p_64037_` (`List<EndCrystal>`)
- `p_64038_` (`int`)
- `p_64039_` (`BlockPos`)

### tick

```java
public void tick(ServerLevel p_64044_, EndDragonFight p_64045_, List<EndCrystal> p_64046_, int p_64047_, BlockPos p_64048_)
```

**Parameters:**

- `p_64044_` (`ServerLevel`)
- `p_64045_` (`EndDragonFight`)
- `p_64046_` (`List<EndCrystal>`)
- `p_64047_` (`int`)
- `p_64048_` (`BlockPos`)

### tick

```java
public void tick(ServerLevel p_64053_, EndDragonFight p_64054_, List<EndCrystal> p_64055_, int p_64056_, BlockPos p_64057_)
```

**Parameters:**

- `p_64053_` (`ServerLevel`)
- `p_64054_` (`EndDragonFight`)
- `p_64055_` (`List<EndCrystal>`)
- `p_64056_` (`int`)
- `p_64057_` (`BlockPos`)

### tick

```java
public abstract void tick(ServerLevel p_64005_, EndDragonFight p_64006_, List<EndCrystal> p_64007_, int p_64008_, BlockPos p_64009_)
```

**Parameters:**

- `p_64005_` (`ServerLevel`)
- `p_64006_` (`EndDragonFight`)
- `p_64007_` (`List<EndCrystal>`)
- `p_64008_` (`int`)
- `p_64009_` (`BlockPos`)

**Returns:** `public abstract void`
