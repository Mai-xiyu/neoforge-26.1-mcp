# EntityCollisionContext

**Package:** `net.minecraft.world.phys.shapes`
**Type:** class
**Implements:** `CollisionContext`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `EMPTY` | `CollisionContext` |  |

## Methods

### isAbove

```java
public boolean isAbove(VoxelShape p_82898_, BlockPos p_82899_, boolean p_82900_)
```

**Parameters:**

- `p_82898_` (`VoxelShape`)
- `p_82899_` (`BlockPos`)
- `p_82900_` (`boolean`)

**Returns:** `boolean`

### EntityCollisionContext

```java
protected EntityCollisionContext(boolean p_198916_, double p_198917_, ItemStack p_198918_, Predicate<FluidState> p_198919_, Entity p_198920_)
```

**Parameters:**

- `p_198916_` (`boolean`)
- `p_198917_` (`double`)
- `p_198918_` (`ItemStack`)
- `p_198919_` (`Predicate<FluidState>`)
- `p_198920_` (`Entity`)

**Returns:** `protected`

### EntityCollisionContext

```java
protected protected EntityCollisionContext(Entity p_82872_)
```

**Parameters:**

- `p_82872_` (`Entity`)

**Returns:** `protected`

### isHoldingItem

```java
public boolean isHoldingItem(Item p_82879_)
```

**Parameters:**

- `p_82879_` (`Item`)

**Returns:** `boolean`

### canStandOnFluid

```java
public boolean canStandOnFluid(FluidState p_205115_, FluidState p_205116_)
```

**Parameters:**

- `p_205115_` (`FluidState`)
- `p_205116_` (`FluidState`)

**Returns:** `boolean`

### isDescending

```java
public boolean isDescending()
```

**Returns:** `boolean`

### isAbove

```java
public boolean isAbove(VoxelShape p_82886_, BlockPos p_82887_, boolean p_82888_)
```

**Parameters:**

- `p_82886_` (`VoxelShape`)
- `p_82887_` (`BlockPos`)
- `p_82888_` (`boolean`)

**Returns:** `boolean`

### getEntity

```java
public Entity getEntity()
```

**Returns:** `Entity`
