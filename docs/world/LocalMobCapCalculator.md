# LocalMobCapCalculator

**Package:** `net.minecraft.world.level`
**Type:** class

## Methods

### LocalMobCapCalculator

```java
public LocalMobCapCalculator(ChunkMap p_186501_)
```

**Parameters:**

- `p_186501_` (`ChunkMap`)

**Returns:** `public`

### getPlayersNear

```java
private List<ServerPlayer> getPlayersNear(ChunkPos p_186508_)
```

**Parameters:**

- `p_186508_` (`ChunkPos`)

**Returns:** `private List<ServerPlayer>`

### addMob

```java
public void addMob(ChunkPos p_186513_, MobCategory p_186514_)
```

**Parameters:**

- `p_186513_` (`ChunkPos`)
- `p_186514_` (`MobCategory`)

**Returns:** `public void`

### canSpawn

```java
public boolean canSpawn(MobCategory p_186505_, ChunkPos p_186506_)
```

**Parameters:**

- `p_186505_` (`MobCategory`)
- `p_186506_` (`ChunkPos`)

**Returns:** `public boolean`

### add

```java
public void add(MobCategory p_186518_)
```

**Parameters:**

- `p_186518_` (`MobCategory`)

**Returns:** `public void`

### canSpawn

```java
public boolean canSpawn(MobCategory p_186523_)
```

**Parameters:**

- `p_186523_` (`MobCategory`)

**Returns:** `public boolean`
