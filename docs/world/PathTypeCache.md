# PathTypeCache

**Package:** `net.minecraft.world.level.pathfinder`
**Type:** class

## Methods

### getOrCompute

```java
public PathType getOrCompute(BlockGetter p_330930_, BlockPos p_331162_)
```

**Parameters:**

- `p_330930_` (`BlockGetter`)
- `p_331162_` (`BlockPos`)

**Returns:** `public PathType`

### compute

```java
private PathType compute(BlockGetter p_330773_, BlockPos p_330311_, int p_330671_, long p_332065_)
```

**Parameters:**

- `p_330773_` (`BlockGetter`)
- `p_330311_` (`BlockPos`)
- `p_330671_` (`int`)
- `p_332065_` (`long`)

**Returns:** `private PathType`

### invalidate

```java
public void invalidate(BlockPos p_331507_)
```

**Parameters:**

- `p_331507_` (`BlockPos`)

**Returns:** `public void`

### index

```java
private static int index(long p_332203_)
```

**Parameters:**

- `p_332203_` (`long`)

**Returns:** `private static int`
