# PathfindingContext

**Package:** `net.minecraft.world.level.pathfinder`
**Type:** class

## Methods

### PathfindingContext

```java
public PathfindingContext(CollisionGetter p_331783_, Mob p_331698_)
```

**Parameters:**

- `p_331783_` (`CollisionGetter`)
- `p_331698_` (`Mob`)

**Returns:** `public`

### getPathTypeFromState

```java
public PathType getPathTypeFromState(int p_331972_, int p_330358_, int p_330334_)
```

**Parameters:**

- `p_331972_` (`int`)
- `p_330358_` (`int`)
- `p_330334_` (`int`)

**Returns:** `public PathType`

### getBlockState

```java
public BlockState getBlockState(BlockPos p_330575_)
```

**Parameters:**

- `p_330575_` (`BlockPos`)

**Returns:** `public BlockState`

### level

```java
public CollisionGetter level()
```

**Returns:** `public CollisionGetter`

### mobPosition

```java
public BlockPos mobPosition()
```

**Returns:** `public BlockPos`

### currentEvalPos

```java
BlockPos currentEvalPos()
```

**Returns:** `BlockPos`
