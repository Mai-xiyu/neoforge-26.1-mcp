# TickAccess

**Package:** `net.minecraft.world.ticks`
**Type:** interface<T>

## Methods

### schedule

```java
void schedule(ScheduledTick<T> p_193428_)
```

**Parameters:**

- `p_193428_` (`ScheduledTick<T>`)

### hasScheduledTick

```java
boolean hasScheduledTick(BlockPos p_193429_, T p_193430_)
```

**Parameters:**

- `p_193429_` (`BlockPos`)
- `p_193430_` (`T`)

**Returns:** `boolean`

### count

```java
int count()
```

**Returns:** `int`
