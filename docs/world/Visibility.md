# Visibility

**Package:** `net.minecraft.world.level.entity`
**Type:** enum

## Methods

### TICKING

```java
, TICKING()
```

**Returns:** `,`

### Visibility

```java
private Visibility(boolean p_157689_, boolean p_157690_)
```

**Parameters:**

- `p_157689_` (`boolean`)
- `p_157690_` (`boolean`)

**Returns:** `private`

### isTicking

```java
public boolean isTicking()
```

**Returns:** `public boolean`

### isAccessible

```java
public boolean isAccessible()
```

**Returns:** `public boolean`

### fromFullChunkStatus

```java
public static Visibility fromFullChunkStatus(FullChunkStatus p_287651_)
```

**Parameters:**

- `p_287651_` (`FullChunkStatus`)

**Returns:** `public static Visibility`
