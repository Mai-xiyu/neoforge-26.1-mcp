# AlterGroundDecorator

**Package:** `net.minecraft.world.level.levelgen.feature.treedecorators`
**Type:** class
**Extends:** `TreeDecorator`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<AlterGroundDecorator>` |  |

## Methods

### AlterGroundDecorator

```java
public AlterGroundDecorator(BlockStateProvider p_69306_)
```

**Parameters:**

- `p_69306_` (`BlockStateProvider`)

**Returns:** `public`

### type

```java
protected TreeDecoratorType<?> type()
```

**Returns:** `TreeDecoratorType<?>`

### place

```java
public void place(TreeDecorator.Context p_225969_)
```

**Parameters:**

- `p_225969_` (`TreeDecorator.Context`)

### placeCircle

```java
private void placeCircle(TreeDecorator.Context p_225971_, BlockPos p_225972_)
```

**Parameters:**

- `p_225971_` (`TreeDecorator.Context`)
- `p_225972_` (`BlockPos`)

**Returns:** `private void`

### placeCircle

```java
 placeCircle()
```

**Returns:** ``

### placeCircle

```java
private void placeCircle(TreeDecorator.Context p_225971_, BlockPos p_225972_, net.neoforged.neoforge.event.level.AlterGroundEvent.StateProvider eventProvider)
```

**Parameters:**

- `p_225971_` (`TreeDecorator.Context`)
- `p_225972_` (`BlockPos`)
- `eventProvider` (`net.neoforged.neoforge.event.level.AlterGroundEvent.StateProvider`)

**Returns:** `private void`

### placeBlockAt

```java
private void placeBlockAt(TreeDecorator.Context p_225974_, BlockPos p_225975_)
```

**Parameters:**

- `p_225974_` (`TreeDecorator.Context`)
- `p_225975_` (`BlockPos`)

**Returns:** `private void`

### placeCircle

```java
 placeCircle()
```

**Returns:** ``

### placeBlockAt

```java
private void placeBlockAt(TreeDecorator.Context p_225974_, BlockPos p_225975_, net.neoforged.neoforge.event.level.AlterGroundEvent.StateProvider eventProvider)
```

**Parameters:**

- `p_225974_` (`TreeDecorator.Context`)
- `p_225975_` (`BlockPos`)
- `eventProvider` (`net.neoforged.neoforge.event.level.AlterGroundEvent.StateProvider`)

**Returns:** `private void`
