# BasaltPillarFeature

**Package:** `net.minecraft.world.level.levelgen.feature`
**Type:** class
**Extends:** `Feature<NoneFeatureConfiguration>`

## Methods

### BasaltPillarFeature

```java
public BasaltPillarFeature(Codec<NoneFeatureConfiguration> p_65190_)
```

**Parameters:**

- `p_65190_` (`Codec<NoneFeatureConfiguration>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### place

```java
public boolean place(FeaturePlaceContext<NoneFeatureConfiguration> p_159446_)
```

**Parameters:**

- `p_159446_` (`FeaturePlaceContext<NoneFeatureConfiguration>`)

**Returns:** `boolean`

### placeBaseHangOff

```java
private void placeBaseHangOff(LevelAccessor p_224937_, RandomSource p_224938_, BlockPos p_224939_)
```

**Parameters:**

- `p_224937_` (`LevelAccessor`)
- `p_224938_` (`RandomSource`)
- `p_224939_` (`BlockPos`)

**Returns:** `private void`

### placeHangOff

```java
private boolean placeHangOff(LevelAccessor p_224941_, RandomSource p_224942_, BlockPos p_224943_)
```

**Parameters:**

- `p_224941_` (`LevelAccessor`)
- `p_224942_` (`RandomSource`)
- `p_224943_` (`BlockPos`)

**Returns:** `private boolean`
