# BendingTrunkPlacer

**Package:** `net.minecraft.world.level.levelgen.feature.trunkplacers`
**Type:** class
**Extends:** `TrunkPlacer`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<BendingTrunkPlacer>` |  |

## Methods

### BendingTrunkPlacer

```java
public BendingTrunkPlacer(int p_161770_, int p_161771_, int p_161772_, int p_161773_, IntProvider p_161774_)
```

**Parameters:**

- `p_161770_` (`int`)
- `p_161771_` (`int`)
- `p_161772_` (`int`)
- `p_161773_` (`int`)
- `p_161774_` (`IntProvider`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### type

```java
protected TrunkPlacerType<?> type()
```

**Returns:** `TrunkPlacerType<?>`

### placeTrunk

```java
public List<FoliagePlacer.FoliageAttachment> placeTrunk(LevelSimulatedReader p_226079_, BiConsumer<BlockPos, BlockState> p_226080_, RandomSource p_226081_, int p_226082_, BlockPos p_226083_, TreeConfiguration p_226084_)
```

**Parameters:**

- `p_226079_` (`LevelSimulatedReader`)
- `p_226080_` (`BiConsumer<BlockPos, BlockState>`)
- `p_226081_` (`RandomSource`)
- `p_226082_` (`int`)
- `p_226083_` (`BlockPos`)
- `p_226084_` (`TreeConfiguration`)

**Returns:** `List<FoliagePlacer.FoliageAttachment>`

### setDirtAt

```java
 setDirtAt()
```

**Returns:** ``
