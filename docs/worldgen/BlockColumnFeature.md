# BlockColumnFeature

**Package:** `net.minecraft.world.level.levelgen.feature`
**Type:** class
**Extends:** `Feature<BlockColumnConfiguration>`

## Methods

### BlockColumnFeature

```java
public BlockColumnFeature(Codec<BlockColumnConfiguration> p_190789_)
```

**Parameters:**

- `p_190789_` (`Codec<BlockColumnConfiguration>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### place

```java
public boolean place(FeaturePlaceContext<BlockColumnConfiguration> p_190791_)
```

**Parameters:**

- `p_190791_` (`FeaturePlaceContext<BlockColumnConfiguration>`)

**Returns:** `boolean`

### truncate

```java
private static void truncate(int[] p_190793_, int p_190794_, int p_190795_, boolean p_190796_)
```

**Parameters:**

- `p_190793_` (`int[]`)
- `p_190794_` (`int`)
- `p_190795_` (`int`)
- `p_190796_` (`boolean`)

**Returns:** `private static void`
