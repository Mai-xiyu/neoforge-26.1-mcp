# BlockPredicateType

**Package:** `net.minecraft.world.level.levelgen.blockpredicates`
**Type:** interface<P extends BlockPredicate>

## Methods

### codec

```java
MapCodec<P> codec()
```

**Returns:** `MapCodec<P>`

### register

```java
<P extends BlockPredicate> private static <P extends BlockPredicate> BlockPredicateType<P> register(String p_190450_, MapCodec<P> p_338619_)
```

**Parameters:**

- `p_190450_` (`String`)
- `p_338619_` (`MapCodec<P>`)

**Returns:** `private static <P extends BlockPredicate> BlockPredicateType<P>`
