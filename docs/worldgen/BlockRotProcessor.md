# BlockRotProcessor

**Package:** `net.minecraft.world.level.levelgen.structure.templatesystem`
**Type:** class
**Extends:** `StructureProcessor`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<BlockRotProcessor>` |  |

## Methods

### BlockRotProcessor

```java
public BlockRotProcessor(HolderSet<Block> p_255622_, float p_256468_)
```

**Parameters:**

- `p_255622_` (`HolderSet<Block>`)
- `p_256468_` (`float`)

**Returns:** `public`

### BlockRotProcessor

```java
public BlockRotProcessor(float p_74078_)
```

**Parameters:**

- `p_74078_` (`float`)

**Returns:** `public`

### BlockRotProcessor

```java
private BlockRotProcessor(Optional<HolderSet<Block>> p_230284_, float p_230285_)
```

**Parameters:**

- `p_230284_` (`Optional<HolderSet<Block>>`)
- `p_230285_` (`float`)

**Returns:** `private`

### processBlock

```java
public StructureTemplate.StructureBlockInfo processBlock(LevelReader p_74081_, BlockPos p_74082_, BlockPos p_74083_, StructureTemplate.StructureBlockInfo p_74084_, StructureTemplate.StructureBlockInfo p_74085_, StructurePlaceSettings p_74086_)
```

**Parameters:**

- `p_74081_` (`LevelReader`)
- `p_74082_` (`BlockPos`)
- `p_74083_` (`BlockPos`)
- `p_74084_` (`StructureTemplate.StructureBlockInfo`)
- `p_74085_` (`StructureTemplate.StructureBlockInfo`)
- `p_74086_` (`StructurePlaceSettings`)

**Returns:** `StructureTemplate.StructureBlockInfo`

### getType

```java
protected StructureProcessorType<?> getType()
```

**Returns:** `StructureProcessorType<?>`
