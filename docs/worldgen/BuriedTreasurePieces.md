# BuriedTreasurePieces

**Package:** `net.minecraft.world.level.levelgen.structure.structures`
**Type:** class

## Methods

### BuriedTreasurePiece

```java
public BuriedTreasurePiece(BlockPos p_227366_)
```

**Parameters:**

- `p_227366_` (`BlockPos`)

**Returns:** `public`

### BuriedTreasurePiece

```java
public BuriedTreasurePiece(CompoundTag p_227368_)
```

**Parameters:**

- `p_227368_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### addAdditionalSaveData

```java
protected void addAdditionalSaveData(StructurePieceSerializationContext p_227378_, CompoundTag p_227379_)
```

**Parameters:**

- `p_227378_` (`StructurePieceSerializationContext`)
- `p_227379_` (`CompoundTag`)

### postProcess

```java
public void postProcess(WorldGenLevel p_227370_, StructureManager p_227371_, ChunkGenerator p_227372_, RandomSource p_227373_, BoundingBox p_227374_, ChunkPos p_227375_, BlockPos p_227376_)
```

**Parameters:**

- `p_227370_` (`WorldGenLevel`)
- `p_227371_` (`StructureManager`)
- `p_227372_` (`ChunkGenerator`)
- `p_227373_` (`RandomSource`)
- `p_227374_` (`BoundingBox`)
- `p_227375_` (`ChunkPos`)
- `p_227376_` (`BlockPos`)

### isLiquid

```java
private boolean isLiquid(BlockState p_227381_)
```

**Parameters:**

- `p_227381_` (`BlockState`)

**Returns:** `private boolean`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `BuriedTreasurePiece` | class |  |
