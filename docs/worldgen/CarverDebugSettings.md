# CarverDebugSettings

**Package:** `net.minecraft.world.level.levelgen.carver`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `DEFAULT` | `CarverDebugSettings` |  |
| `CODEC` | `Codec<CarverDebugSettings>` |  |

## Methods

### of

```java
public static CarverDebugSettings of(boolean p_159140_, BlockState p_159141_, BlockState p_159142_, BlockState p_159143_, BlockState p_159144_)
```

**Parameters:**

- `p_159140_` (`boolean`)
- `p_159141_` (`BlockState`)
- `p_159142_` (`BlockState`)
- `p_159143_` (`BlockState`)
- `p_159144_` (`BlockState`)

**Returns:** `public static CarverDebugSettings`

### CarverDebugSettings

```java
return new CarverDebugSettings()
```

**Returns:** `return new`

### of

```java
public static CarverDebugSettings of(BlockState p_159130_, BlockState p_159131_, BlockState p_159132_, BlockState p_159133_)
```

**Parameters:**

- `p_159130_` (`BlockState`)
- `p_159131_` (`BlockState`)
- `p_159132_` (`BlockState`)
- `p_159133_` (`BlockState`)

**Returns:** `public static CarverDebugSettings`

### CarverDebugSettings

```java
return new CarverDebugSettings()
```

**Returns:** `return new`

### of

```java
public static CarverDebugSettings of(boolean p_159137_, BlockState p_159138_)
```

**Parameters:**

- `p_159137_` (`boolean`)
- `p_159138_` (`BlockState`)

**Returns:** `public static CarverDebugSettings`

### CarverDebugSettings

```java
private CarverDebugSettings(boolean p_159123_, BlockState p_159124_, BlockState p_159125_, BlockState p_159126_, BlockState p_159127_)
```

**Parameters:**

- `p_159123_` (`boolean`)
- `p_159124_` (`BlockState`)
- `p_159125_` (`BlockState`)
- `p_159126_` (`BlockState`)
- `p_159127_` (`BlockState`)

**Returns:** `private`

### isDebugMode

```java
public boolean isDebugMode()
```

**Returns:** `public boolean`

### getAirState

```java
public BlockState getAirState()
```

**Returns:** `public BlockState`

### getWaterState

```java
public BlockState getWaterState()
```

**Returns:** `public BlockState`

### getLavaState

```java
public BlockState getLavaState()
```

**Returns:** `public BlockState`

### getBarrierState

```java
public BlockState getBarrierState()
```

**Returns:** `public BlockState`
