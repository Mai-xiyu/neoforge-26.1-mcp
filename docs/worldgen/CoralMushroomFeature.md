# CoralMushroomFeature

**Package:** `net.minecraft.world.level.levelgen.feature`
**Type:** class
**Extends:** `CoralFeature`

## Methods

### CoralMushroomFeature

```java
public CoralMushroomFeature(Codec<NoneFeatureConfiguration> p_65452_)
```

**Parameters:**

- `p_65452_` (`Codec<NoneFeatureConfiguration>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### placeFeature

```java
protected boolean placeFeature(LevelAccessor p_224982_, RandomSource p_224983_, BlockPos p_224984_, BlockState p_224985_)
```

**Parameters:**

- `p_224982_` (`LevelAccessor`)
- `p_224983_` (`RandomSource`)
- `p_224984_` (`BlockPos`)
- `p_224985_` (`BlockState`)

**Returns:** `boolean`
