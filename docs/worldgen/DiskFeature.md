# DiskFeature

**Package:** `net.minecraft.world.level.levelgen.feature`
**Type:** class
**Extends:** `Feature<DiskConfiguration>`

## Methods

### DiskFeature

```java
public DiskFeature(Codec<DiskConfiguration> p_224992_)
```

**Parameters:**

- `p_224992_` (`Codec<DiskConfiguration>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### place

```java
public boolean place(FeaturePlaceContext<DiskConfiguration> p_224994_)
```

**Parameters:**

- `p_224994_` (`FeaturePlaceContext<DiskConfiguration>`)

**Returns:** `boolean`

### placeColumn

```java
protected boolean placeColumn(DiskConfiguration p_224996_, WorldGenLevel p_224997_, RandomSource p_224998_, int p_224999_, int p_225000_, BlockPos.MutableBlockPos p_225001_)
```

**Parameters:**

- `p_224996_` (`DiskConfiguration`)
- `p_224997_` (`WorldGenLevel`)
- `p_224998_` (`RandomSource`)
- `p_224999_` (`int`)
- `p_225000_` (`int`)
- `p_225001_` (`BlockPos.MutableBlockPos`)

**Returns:** `protected boolean`
