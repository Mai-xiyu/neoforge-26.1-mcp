# EndCityPieces

**Package:** `net.minecraft.world.level.levelgen.structure.structures`
**Type:** class

## Methods

### init

```java
public void init()
```

### generate

```java
public boolean generate(StructureTemplateManager p_227456_, int p_227457_, EndCityPieces.EndCityPiece p_227458_, BlockPos p_227459_, List<StructurePiece> p_227460_, RandomSource p_227461_)
```

**Parameters:**

- `p_227456_` (`StructureTemplateManager`)
- `p_227457_` (`int`)
- `p_227458_` (`EndCityPieces.EndCityPiece`)
- `p_227459_` (`BlockPos`)
- `p_227460_` (`List<StructurePiece>`)
- `p_227461_` (`RandomSource`)

**Returns:** `boolean`

### init

```java
public void init()
```

### generate

```java
public boolean generate(StructureTemplateManager p_227465_, int p_227466_, EndCityPieces.EndCityPiece p_227467_, BlockPos p_227468_, List<StructurePiece> p_227469_, RandomSource p_227470_)
```

**Parameters:**

- `p_227465_` (`StructureTemplateManager`)
- `p_227466_` (`int`)
- `p_227467_` (`EndCityPieces.EndCityPiece`)
- `p_227468_` (`BlockPos`)
- `p_227469_` (`List<StructurePiece>`)
- `p_227470_` (`RandomSource`)

**Returns:** `boolean`

### init

```java
public void init()
```

### generate

```java
public boolean generate(StructureTemplateManager p_227475_, int p_227476_, EndCityPieces.EndCityPiece p_227477_, BlockPos p_227478_, List<StructurePiece> p_227479_, RandomSource p_227480_)
```

**Parameters:**

- `p_227475_` (`StructureTemplateManager`)
- `p_227476_` (`int`)
- `p_227477_` (`EndCityPieces.EndCityPiece`)
- `p_227478_` (`BlockPos`)
- `p_227479_` (`List<StructurePiece>`)
- `p_227480_` (`RandomSource`)

**Returns:** `boolean`

### init

```java
public void init()
```

### generate

```java
public boolean generate(StructureTemplateManager p_227484_, int p_227485_, EndCityPieces.EndCityPiece p_227486_, BlockPos p_227487_, List<StructurePiece> p_227488_, RandomSource p_227489_)
```

**Parameters:**

- `p_227484_` (`StructureTemplateManager`)
- `p_227485_` (`int`)
- `p_227486_` (`EndCityPieces.EndCityPiece`)
- `p_227487_` (`BlockPos`)
- `p_227488_` (`List<StructurePiece>`)
- `p_227489_` (`RandomSource`)

**Returns:** `boolean`

### addPiece

```java
static EndCityPieces.EndCityPiece addPiece(StructureTemplateManager p_227430_, EndCityPieces.EndCityPiece p_227431_, BlockPos p_227432_, String p_227433_, Rotation p_227434_, boolean p_227435_)
```

**Parameters:**

- `p_227430_` (`StructureTemplateManager`)
- `p_227431_` (`EndCityPieces.EndCityPiece`)
- `p_227432_` (`BlockPos`)
- `p_227433_` (`String`)
- `p_227434_` (`Rotation`)
- `p_227435_` (`boolean`)

**Returns:** `static EndCityPieces.EndCityPiece`

### startHouseTower

```java
public static void startHouseTower(StructureTemplateManager p_227445_, BlockPos p_227446_, Rotation p_227447_, List<StructurePiece> p_227448_, RandomSource p_227449_)
```

**Parameters:**

- `p_227445_` (`StructureTemplateManager`)
- `p_227446_` (`BlockPos`)
- `p_227447_` (`Rotation`)
- `p_227448_` (`List<StructurePiece>`)
- `p_227449_` (`RandomSource`)

**Returns:** `public static void`

### recursiveChildren

```java
 recursiveChildren()
```

**Returns:** ``

### addHelper

```java
static EndCityPieces.EndCityPiece addHelper(List<StructurePiece> p_227451_, EndCityPieces.EndCityPiece p_227452_)
```

**Parameters:**

- `p_227451_` (`List<StructurePiece>`)
- `p_227452_` (`EndCityPieces.EndCityPiece`)

**Returns:** `static EndCityPieces.EndCityPiece`

### recursiveChildren

```java
static boolean recursiveChildren(StructureTemplateManager p_227437_, EndCityPieces.SectionGenerator p_227438_, int p_227439_, EndCityPieces.EndCityPiece p_227440_, BlockPos p_227441_, List<StructurePiece> p_227442_, RandomSource p_227443_)
```

**Parameters:**

- `p_227437_` (`StructureTemplateManager`)
- `p_227438_` (`EndCityPieces.SectionGenerator`)
- `p_227439_` (`int`)
- `p_227440_` (`EndCityPieces.EndCityPiece`)
- `p_227441_` (`BlockPos`)
- `p_227442_` (`List<StructurePiece>`)
- `p_227443_` (`RandomSource`)

**Returns:** `static boolean`

### EndCityPiece

```java
public EndCityPiece(StructureTemplateManager p_227491_, String p_227492_, BlockPos p_227493_, Rotation p_227494_, boolean p_227495_)
```

**Parameters:**

- `p_227491_` (`StructureTemplateManager`)
- `p_227492_` (`String`)
- `p_227493_` (`BlockPos`)
- `p_227494_` (`Rotation`)
- `p_227495_` (`boolean`)

**Returns:** `public`

### EndCityPiece

```java
public EndCityPiece(StructureTemplateManager p_227497_, CompoundTag p_227498_)
```

**Parameters:**

- `p_227497_` (`StructureTemplateManager`)
- `p_227498_` (`CompoundTag`)

**Returns:** `public`

### makeSettings

```java
private static StructurePlaceSettings makeSettings(boolean p_227514_, Rotation p_227515_)
```

**Parameters:**

- `p_227514_` (`boolean`)
- `p_227515_` (`Rotation`)

**Returns:** `private static StructurePlaceSettings`

### makeTemplateLocation

```java
protected ResourceLocation makeTemplateLocation()
```

**Returns:** `ResourceLocation`

### makeResourceLocation

```java
return makeResourceLocation()
```

**Returns:** `return`

### makeResourceLocation

```java
private static ResourceLocation makeResourceLocation(String p_227503_)
```

**Parameters:**

- `p_227503_` (`String`)

**Returns:** `private static ResourceLocation`

### addAdditionalSaveData

```java
protected void addAdditionalSaveData(StructurePieceSerializationContext p_227500_, CompoundTag p_227501_)
```

**Parameters:**

- `p_227500_` (`StructurePieceSerializationContext`)
- `p_227501_` (`CompoundTag`)

### handleDataMarker

```java
protected void handleDataMarker(String p_227505_, BlockPos p_227506_, ServerLevelAccessor p_227507_, RandomSource p_227508_, BoundingBox p_227509_)
```

**Parameters:**

- `p_227505_` (`String`)
- `p_227506_` (`BlockPos`)
- `p_227507_` (`ServerLevelAccessor`)
- `p_227508_` (`RandomSource`)
- `p_227509_` (`BoundingBox`)

### init

```java
void init()
```

### generate

```java
boolean generate(StructureTemplateManager p_227517_, int p_227518_, EndCityPieces.EndCityPiece p_227519_, BlockPos p_227520_, List<StructurePiece> p_227521_, RandomSource p_227522_)
```

**Parameters:**

- `p_227517_` (`StructureTemplateManager`)
- `p_227518_` (`int`)
- `p_227519_` (`EndCityPieces.EndCityPiece`)
- `p_227520_` (`BlockPos`)
- `p_227521_` (`List<StructurePiece>`)
- `p_227522_` (`RandomSource`)

**Returns:** `boolean`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `EndCityPiece` | class |  |
