# FancyFoliagePlacer

**Package:** `net.minecraft.world.level.levelgen.feature.foliageplacers`
**Type:** class
**Extends:** `BlobFoliagePlacer`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<FancyFoliagePlacer>` |  |

## Methods

### FancyFoliagePlacer

```java
public FancyFoliagePlacer(IntProvider p_161397_, IntProvider p_161398_, int p_161399_)
```

**Parameters:**

- `p_161397_` (`IntProvider`)
- `p_161398_` (`IntProvider`)
- `p_161399_` (`int`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### type

```java
protected FoliagePlacerType<?> type()
```

**Returns:** `FoliagePlacerType<?>`

### createFoliage

```java
protected void createFoliage(LevelSimulatedReader p_225582_, FoliagePlacer.FoliageSetter p_273184_, RandomSource p_225584_, TreeConfiguration p_225585_, int p_225586_, FoliagePlacer.FoliageAttachment p_225587_, int p_225588_, int p_225589_, int p_225590_)
```

**Parameters:**

- `p_225582_` (`LevelSimulatedReader`)
- `p_273184_` (`FoliagePlacer.FoliageSetter`)
- `p_225584_` (`RandomSource`)
- `p_225585_` (`TreeConfiguration`)
- `p_225586_` (`int`)
- `p_225587_` (`FoliagePlacer.FoliageAttachment`)
- `p_225588_` (`int`)
- `p_225589_` (`int`)
- `p_225590_` (`int`)

### shouldSkipLocation

```java
protected boolean shouldSkipLocation(RandomSource p_225575_, int p_225576_, int p_225577_, int p_225578_, int p_225579_, boolean p_225580_)
```

**Parameters:**

- `p_225575_` (`RandomSource`)
- `p_225576_` (`int`)
- `p_225577_` (`int`)
- `p_225578_` (`int`)
- `p_225579_` (`int`)
- `p_225580_` (`boolean`)

**Returns:** `boolean`
