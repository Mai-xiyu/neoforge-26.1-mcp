# FeatureSizeType

**Package:** `net.minecraft.world.level.levelgen.feature.featuresize`
**Type:** class<P extends FeatureSize>

## Fields

| Name | Type | Description |
|------|------|-------------|
| `TWO_LAYERS_FEATURE_SIZE` | `FeatureSizeType<TwoLayersFeatureSize>` |  |
| `THREE_LAYERS_FEATURE_SIZE` | `FeatureSizeType<ThreeLayersFeatureSize>` |  |

## Methods

### register

```java
<P extends FeatureSize> private static <P extends FeatureSize> FeatureSizeType<P> register(String p_68304_, MapCodec<P> p_338491_)
```

**Parameters:**

- `p_68304_` (`String`)
- `p_338491_` (`MapCodec<P>`)

**Returns:** `private static <P extends FeatureSize> FeatureSizeType<P>`

### FeatureSizeType

```java
public FeatureSizeType(MapCodec<P> p_338873_)
```

**Parameters:**

- `p_338873_` (`MapCodec<P>`)

**Returns:** `public`

### codec

```java
public MapCodec<P> codec()
```

**Returns:** `public MapCodec<P>`
