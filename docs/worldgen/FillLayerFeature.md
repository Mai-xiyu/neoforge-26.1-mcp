# FillLayerFeature

**Package:** `net.minecraft.world.level.levelgen.feature`
**Type:** class
**Extends:** `Feature<LayerConfiguration>`

## Methods

### FillLayerFeature

```java
public FillLayerFeature(Codec<LayerConfiguration> p_65818_)
```

**Parameters:**

- `p_65818_` (`Codec<LayerConfiguration>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### place

```java
public boolean place(FeaturePlaceContext<LayerConfiguration> p_159780_)
```

**Parameters:**

- `p_159780_` (`FeaturePlaceContext<LayerConfiguration>`)

**Returns:** `boolean`
