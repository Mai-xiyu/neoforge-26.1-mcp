# ForkingTrunkPlacer

**Package:** `net.minecraft.world.level.levelgen.feature.trunkplacers`
**Type:** class
**Extends:** `TrunkPlacer`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<ForkingTrunkPlacer>` |  |

## Methods

### ForkingTrunkPlacer

```java
public ForkingTrunkPlacer(int p_70148_, int p_70149_, int p_70150_)
```

**Parameters:**

- `p_70148_` (`int`)
- `p_70149_` (`int`)
- `p_70150_` (`int`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### type

```java
protected TrunkPlacerType<?> type()
```

**Returns:** `TrunkPlacerType<?>`

### placeTrunk

```java
public List<FoliagePlacer.FoliageAttachment> placeTrunk(LevelSimulatedReader p_226116_, BiConsumer<BlockPos, BlockState> p_226117_, RandomSource p_226118_, int p_226119_, BlockPos p_226120_, TreeConfiguration p_226121_)
```

**Parameters:**

- `p_226116_` (`LevelSimulatedReader`)
- `p_226117_` (`BiConsumer<BlockPos, BlockState>`)
- `p_226118_` (`RandomSource`)
- `p_226119_` (`int`)
- `p_226120_` (`BlockPos`)
- `p_226121_` (`TreeConfiguration`)

**Returns:** `List<FoliagePlacer.FoliageAttachment>`
