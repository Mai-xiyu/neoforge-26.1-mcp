# HeightProviderType

**Package:** `net.minecraft.world.level.levelgen.heightproviders`
**Type:** interface<P extends HeightProvider>

## Methods

### codec

```java
MapCodec<P> codec()
```

**Returns:** `MapCodec<P>`

### register

```java
<P extends HeightProvider> private static <P extends HeightProvider> HeightProviderType<P> register(String p_161990_, MapCodec<P> p_338570_)
```

**Parameters:**

- `p_161990_` (`String`)
- `p_338570_` (`MapCodec<P>`)

**Returns:** `private static <P extends HeightProvider> HeightProviderType<P>`
