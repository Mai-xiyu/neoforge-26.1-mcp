# HugeFungusFeature

**Package:** `net.minecraft.world.level.levelgen.feature`
**Type:** class
**Extends:** `Feature<HugeFungusConfiguration>`

## Methods

### HugeFungusFeature

```java
public HugeFungusFeature(Codec<HugeFungusConfiguration> p_65922_)
```

**Parameters:**

- `p_65922_` (`Codec<HugeFungusConfiguration>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### place

```java
public boolean place(FeaturePlaceContext<HugeFungusConfiguration> p_159878_)
```

**Parameters:**

- `p_159878_` (`FeaturePlaceContext<HugeFungusConfiguration>`)

**Returns:** `boolean`

### isReplaceable

```java
private static boolean isReplaceable(WorldGenLevel p_285049_, BlockPos p_285309_, HugeFungusConfiguration p_284992_, boolean p_285162_)
```

**Parameters:**

- `p_285049_` (`WorldGenLevel`)
- `p_285309_` (`BlockPos`)
- `p_284992_` (`HugeFungusConfiguration`)
- `p_285162_` (`boolean`)

**Returns:** `private static boolean`

### placeStem

```java
private void placeStem(WorldGenLevel p_285364_, RandomSource p_285032_, HugeFungusConfiguration p_285198_, BlockPos p_285090_, int p_285249_, boolean p_285355_)
```

**Parameters:**

- `p_285364_` (`WorldGenLevel`)
- `p_285032_` (`RandomSource`)
- `p_285198_` (`HugeFungusConfiguration`)
- `p_285090_` (`BlockPos`)
- `p_285249_` (`int`)
- `p_285355_` (`boolean`)

**Returns:** `private void`

### placeHat

```java
private void placeHat(WorldGenLevel p_285200_, RandomSource p_285456_, HugeFungusConfiguration p_285146_, BlockPos p_285097_, int p_285156_, boolean p_285265_)
```

**Parameters:**

- `p_285200_` (`WorldGenLevel`)
- `p_285456_` (`RandomSource`)
- `p_285146_` (`HugeFungusConfiguration`)
- `p_285097_` (`BlockPos`)
- `p_285156_` (`int`)
- `p_285265_` (`boolean`)

**Returns:** `private void`

### placeHatBlock

```java
private void placeHatBlock(LevelAccessor p_225050_, RandomSource p_225051_, HugeFungusConfiguration p_225052_, BlockPos.MutableBlockPos p_225053_, float p_225054_, float p_225055_, float p_225056_)
```

**Parameters:**

- `p_225050_` (`LevelAccessor`)
- `p_225051_` (`RandomSource`)
- `p_225052_` (`HugeFungusConfiguration`)
- `p_225053_` (`BlockPos.MutableBlockPos`)
- `p_225054_` (`float`)
- `p_225055_` (`float`)
- `p_225056_` (`float`)

**Returns:** `private void`

### tryPlaceWeepingVines

```java
 tryPlaceWeepingVines()
```

**Returns:** ``

### placeHatDropBlock

```java
private void placeHatDropBlock(LevelAccessor p_225065_, RandomSource p_225066_, BlockPos p_225067_, BlockState p_225068_, boolean p_225069_)
```

**Parameters:**

- `p_225065_` (`LevelAccessor`)
- `p_225066_` (`RandomSource`)
- `p_225067_` (`BlockPos`)
- `p_225068_` (`BlockState`)
- `p_225069_` (`boolean`)

**Returns:** `private void`

### tryPlaceWeepingVines

```java
 tryPlaceWeepingVines()
```

**Returns:** ``

### tryPlaceWeepingVines

```java
private static void tryPlaceWeepingVines(BlockPos p_225071_, LevelAccessor p_225072_, RandomSource p_225073_)
```

**Parameters:**

- `p_225071_` (`BlockPos`)
- `p_225072_` (`LevelAccessor`)
- `p_225073_` (`RandomSource`)

**Returns:** `private static void`
