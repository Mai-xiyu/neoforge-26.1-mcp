# LeaveVineDecorator

**Package:** `net.minecraft.world.level.levelgen.feature.treedecorators`
**Type:** class
**Extends:** `TreeDecorator`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<LeaveVineDecorator>` |  |

## Methods

### type

```java
protected TreeDecoratorType<?> type()
```

**Returns:** `TreeDecoratorType<?>`

### LeaveVineDecorator

```java
public LeaveVineDecorator(float p_226031_)
```

**Parameters:**

- `p_226031_` (`float`)

**Returns:** `public`

### place

```java
public void place(TreeDecorator.Context p_226039_)
```

**Parameters:**

- `p_226039_` (`TreeDecorator.Context`)

### addHangingVine

```java
 addHangingVine()
```

**Returns:** ``

### addHangingVine

```java
 addHangingVine()
```

**Returns:** ``

### addHangingVine

```java
 addHangingVine()
```

**Returns:** ``

### addHangingVine

```java
 addHangingVine()
```

**Returns:** ``

### addHangingVine

```java
private static void addHangingVine(BlockPos p_226041_, BooleanProperty p_226042_, TreeDecorator.Context p_226043_)
```

**Parameters:**

- `p_226041_` (`BlockPos`)
- `p_226042_` (`BooleanProperty`)
- `p_226043_` (`TreeDecorator.Context`)

**Returns:** `private static void`
