# LegacyStructureDataHandler

**Package:** `net.minecraft.world.level.levelgen.structure`
**Type:** class

## Methods

### LegacyStructureDataHandler

```java
public LegacyStructureDataHandler(DimensionDataStorage p_71308_, List<String> p_71309_, List<String> p_71310_)
```

**Parameters:**

- `p_71308_` (`DimensionDataStorage`)
- `p_71309_` (`List<String>`)
- `p_71310_` (`List<String>`)

**Returns:** `public`

### removeIndex

```java
public void removeIndex(long p_71319_)
```

**Parameters:**

- `p_71319_` (`long`)

**Returns:** `public void`

### updateFromLegacy

```java
public CompoundTag updateFromLegacy(CompoundTag p_71327_)
```

**Parameters:**

- `p_71327_` (`CompoundTag`)

**Returns:** `public CompoundTag`

### hasLegacyStart

```java
private boolean hasLegacyStart(int p_71315_, int p_71316_, String p_71317_)
```

**Parameters:**

- `p_71315_` (`int`)
- `p_71316_` (`int`)
- `p_71317_` (`String`)

**Returns:** `private boolean`

### isUnhandledStructureStart

```java
private boolean isUnhandledStructureStart(int p_71312_, int p_71313_)
```

**Parameters:**

- `p_71312_` (`int`)
- `p_71313_` (`int`)

**Returns:** `private boolean`

### updateStructureStart

```java
private CompoundTag updateStructureStart(CompoundTag p_71329_, ChunkPos p_71330_)
```

**Parameters:**

- `p_71329_` (`CompoundTag`)
- `p_71330_` (`ChunkPos`)

**Returns:** `private CompoundTag`

### populateCaches

```java
private void populateCaches(DimensionDataStorage p_71321_)
```

**Parameters:**

- `p_71321_` (`DimensionDataStorage`)

**Returns:** `private void`

### getLegacyStructureHandler

```java
public static LegacyStructureDataHandler getLegacyStructureHandler(ResourceKey<Level> p_71332_, DimensionDataStorage p_71333_)
```

**Parameters:**

- `p_71332_` (`ResourceKey<Level>`)
- `p_71333_` (`DimensionDataStorage`)

**Returns:** `public static LegacyStructureDataHandler`

### LegacyStructureDataHandler

```java
return new LegacyStructureDataHandler()
```

**Returns:** `return new`

### LegacyStructureDataHandler

```java
return new LegacyStructureDataHandler()
```

**Returns:** `return new`
