# MegaJungleTrunkPlacer

**Package:** `net.minecraft.world.level.levelgen.feature.trunkplacers`
**Type:** class
**Extends:** `GiantTrunkPlacer`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<MegaJungleTrunkPlacer>` |  |

## Methods

### MegaJungleTrunkPlacer

```java
public MegaJungleTrunkPlacer(int p_70193_, int p_70194_, int p_70195_)
```

**Parameters:**

- `p_70193_` (`int`)
- `p_70194_` (`int`)
- `p_70195_` (`int`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### type

```java
protected TrunkPlacerType<?> type()
```

**Returns:** `TrunkPlacerType<?>`

### placeTrunk

```java
public List<FoliagePlacer.FoliageAttachment> placeTrunk(LevelSimulatedReader p_226140_, BiConsumer<BlockPos, BlockState> p_226141_, RandomSource p_226142_, int p_226143_, BlockPos p_226144_, TreeConfiguration p_226145_)
```

**Parameters:**

- `p_226140_` (`LevelSimulatedReader`)
- `p_226141_` (`BiConsumer<BlockPos, BlockState>`)
- `p_226142_` (`RandomSource`)
- `p_226143_` (`int`)
- `p_226144_` (`BlockPos`)
- `p_226145_` (`TreeConfiguration`)

**Returns:** `List<FoliagePlacer.FoliageAttachment>`
