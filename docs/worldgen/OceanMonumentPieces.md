# OceanMonumentPieces

**Package:** `net.minecraft.world.level.levelgen.structure.structures`
**Type:** class

## Methods

### OceanMonumentPieces

```java
private OceanMonumentPieces()
```

**Returns:** `private`

### fits

```java
public boolean fits(OceanMonumentPieces.RoomDefinition p_228592_)
```

**Parameters:**

- `p_228592_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `boolean`

### create

```java
public OceanMonumentPieces.OceanMonumentPiece create(Direction p_228594_, OceanMonumentPieces.RoomDefinition p_228595_, RandomSource p_228596_)
```

**Parameters:**

- `p_228594_` (`Direction`)
- `p_228595_` (`OceanMonumentPieces.RoomDefinition`)
- `p_228596_` (`RandomSource`)

**Returns:** `OceanMonumentPieces.OceanMonumentPiece`

### fits

```java
public boolean fits(OceanMonumentPieces.RoomDefinition p_228599_)
```

**Parameters:**

- `p_228599_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `boolean`

### create

```java
public OceanMonumentPieces.OceanMonumentPiece create(Direction p_228601_, OceanMonumentPieces.RoomDefinition p_228602_, RandomSource p_228603_)
```

**Parameters:**

- `p_228601_` (`Direction`)
- `p_228602_` (`OceanMonumentPieces.RoomDefinition`)
- `p_228603_` (`RandomSource`)

**Returns:** `OceanMonumentPieces.OceanMonumentPiece`

### fits

```java
public boolean fits(OceanMonumentPieces.RoomDefinition p_228606_)
```

**Parameters:**

- `p_228606_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `boolean`

### create

```java
public OceanMonumentPieces.OceanMonumentPiece create(Direction p_228608_, OceanMonumentPieces.RoomDefinition p_228609_, RandomSource p_228610_)
```

**Parameters:**

- `p_228608_` (`Direction`)
- `p_228609_` (`OceanMonumentPieces.RoomDefinition`)
- `p_228610_` (`RandomSource`)

**Returns:** `OceanMonumentPieces.OceanMonumentPiece`

### fits

```java
public boolean fits(OceanMonumentPieces.RoomDefinition p_228613_)
```

**Parameters:**

- `p_228613_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `boolean`

### create

```java
public OceanMonumentPieces.OceanMonumentPiece create(Direction p_228615_, OceanMonumentPieces.RoomDefinition p_228616_, RandomSource p_228617_)
```

**Parameters:**

- `p_228615_` (`Direction`)
- `p_228616_` (`OceanMonumentPieces.RoomDefinition`)
- `p_228617_` (`RandomSource`)

**Returns:** `OceanMonumentPieces.OceanMonumentPiece`

### fits

```java
public boolean fits(OceanMonumentPieces.RoomDefinition p_228620_)
```

**Parameters:**

- `p_228620_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `boolean`

### create

```java
public OceanMonumentPieces.OceanMonumentPiece create(Direction p_228622_, OceanMonumentPieces.RoomDefinition p_228623_, RandomSource p_228624_)
```

**Parameters:**

- `p_228622_` (`Direction`)
- `p_228623_` (`OceanMonumentPieces.RoomDefinition`)
- `p_228624_` (`RandomSource`)

**Returns:** `OceanMonumentPieces.OceanMonumentPiece`

### fits

```java
public boolean fits(OceanMonumentPieces.RoomDefinition p_228627_)
```

**Parameters:**

- `p_228627_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `boolean`

### create

```java
public OceanMonumentPieces.OceanMonumentPiece create(Direction p_228629_, OceanMonumentPieces.RoomDefinition p_228630_, RandomSource p_228631_)
```

**Parameters:**

- `p_228629_` (`Direction`)
- `p_228630_` (`OceanMonumentPieces.RoomDefinition`)
- `p_228631_` (`RandomSource`)

**Returns:** `OceanMonumentPieces.OceanMonumentPiece`

### fits

```java
public boolean fits(OceanMonumentPieces.RoomDefinition p_228634_)
```

**Parameters:**

- `p_228634_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `boolean`

### create

```java
public OceanMonumentPieces.OceanMonumentPiece create(Direction p_228636_, OceanMonumentPieces.RoomDefinition p_228637_, RandomSource p_228638_)
```

**Parameters:**

- `p_228636_` (`Direction`)
- `p_228637_` (`OceanMonumentPieces.RoomDefinition`)
- `p_228638_` (`RandomSource`)

**Returns:** `OceanMonumentPieces.OceanMonumentPiece`

### MonumentBuilding

```java
public MonumentBuilding(RandomSource p_228648_, int p_228649_, int p_228650_, Direction p_228651_)
```

**Parameters:**

- `p_228648_` (`RandomSource`)
- `p_228649_` (`int`)
- `p_228650_` (`int`)
- `p_228651_` (`Direction`)

**Returns:** `public`

### MonumentBuilding

```java
public MonumentBuilding(CompoundTag p_228653_)
```

**Parameters:**

- `p_228653_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### generateRoomGraph

```java
private List<OceanMonumentPieces.RoomDefinition> generateRoomGraph(RandomSource p_228673_)
```

**Parameters:**

- `p_228673_` (`RandomSource`)

**Returns:** `private List<OceanMonumentPieces.RoomDefinition>`

### postProcess

```java
public void postProcess(WorldGenLevel p_228659_, StructureManager p_228660_, ChunkGenerator p_228661_, RandomSource p_228662_, BoundingBox p_228663_, ChunkPos p_228664_, BlockPos p_228665_)
```

**Parameters:**

- `p_228659_` (`WorldGenLevel`)
- `p_228660_` (`StructureManager`)
- `p_228661_` (`ChunkGenerator`)
- `p_228662_` (`RandomSource`)
- `p_228663_` (`BoundingBox`)
- `p_228664_` (`ChunkPos`)
- `p_228665_` (`BlockPos`)

### generateWing

```java
private void generateWing(boolean p_228667_, int p_228668_, WorldGenLevel p_228669_, RandomSource p_228670_, BoundingBox p_228671_)
```

**Parameters:**

- `p_228667_` (`boolean`)
- `p_228668_` (`int`)
- `p_228669_` (`WorldGenLevel`)
- `p_228670_` (`RandomSource`)
- `p_228671_` (`BoundingBox`)

**Returns:** `private void`

### generateEntranceArchs

```java
private void generateEntranceArchs(WorldGenLevel p_228655_, RandomSource p_228656_, BoundingBox p_228657_)
```

**Parameters:**

- `p_228655_` (`WorldGenLevel`)
- `p_228656_` (`RandomSource`)
- `p_228657_` (`BoundingBox`)

**Returns:** `private void`

### generateEntranceWall

```java
private void generateEntranceWall(WorldGenLevel p_228675_, RandomSource p_228676_, BoundingBox p_228677_)
```

**Parameters:**

- `p_228675_` (`WorldGenLevel`)
- `p_228676_` (`RandomSource`)
- `p_228677_` (`BoundingBox`)

**Returns:** `private void`

### generateRoofPiece

```java
private void generateRoofPiece(WorldGenLevel p_228679_, RandomSource p_228680_, BoundingBox p_228681_)
```

**Parameters:**

- `p_228679_` (`WorldGenLevel`)
- `p_228680_` (`RandomSource`)
- `p_228681_` (`BoundingBox`)

**Returns:** `private void`

### generateLowerWall

```java
private void generateLowerWall(WorldGenLevel p_228683_, RandomSource p_228684_, BoundingBox p_228685_)
```

**Parameters:**

- `p_228683_` (`WorldGenLevel`)
- `p_228684_` (`RandomSource`)
- `p_228685_` (`BoundingBox`)

**Returns:** `private void`

### generateMiddleWall

```java
private void generateMiddleWall(WorldGenLevel p_228687_, RandomSource p_228688_, BoundingBox p_228689_)
```

**Parameters:**

- `p_228687_` (`WorldGenLevel`)
- `p_228688_` (`RandomSource`)
- `p_228689_` (`BoundingBox`)

**Returns:** `private void`

### generateUpperWall

```java
private void generateUpperWall(WorldGenLevel p_228691_, RandomSource p_228692_, BoundingBox p_228693_)
```

**Parameters:**

- `p_228691_` (`WorldGenLevel`)
- `p_228692_` (`RandomSource`)
- `p_228693_` (`BoundingBox`)

**Returns:** `private void`

### fits

```java
boolean fits(OceanMonumentPieces.RoomDefinition p_228694_)
```

**Parameters:**

- `p_228694_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `boolean`

### create

```java
OceanMonumentPieces.OceanMonumentPiece create(Direction p_228695_, OceanMonumentPieces.RoomDefinition p_228696_, RandomSource p_228697_)
```

**Parameters:**

- `p_228695_` (`Direction`)
- `p_228696_` (`OceanMonumentPieces.RoomDefinition`)
- `p_228697_` (`RandomSource`)

**Returns:** `OceanMonumentPieces.OceanMonumentPiece`

### OceanMonumentCoreRoom

```java
public OceanMonumentCoreRoom(Direction p_228699_, OceanMonumentPieces.RoomDefinition p_228700_)
```

**Parameters:**

- `p_228699_` (`Direction`)
- `p_228700_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### OceanMonumentCoreRoom

```java
public OceanMonumentCoreRoom(CompoundTag p_228702_)
```

**Parameters:**

- `p_228702_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### postProcess

```java
public void postProcess(WorldGenLevel p_228704_, StructureManager p_228705_, ChunkGenerator p_228706_, RandomSource p_228707_, BoundingBox p_228708_, ChunkPos p_228709_, BlockPos p_228710_)
```

**Parameters:**

- `p_228704_` (`WorldGenLevel`)
- `p_228705_` (`StructureManager`)
- `p_228706_` (`ChunkGenerator`)
- `p_228707_` (`RandomSource`)
- `p_228708_` (`BoundingBox`)
- `p_228709_` (`ChunkPos`)
- `p_228710_` (`BlockPos`)

### OceanMonumentDoubleXRoom

```java
public OceanMonumentDoubleXRoom(Direction p_228712_, OceanMonumentPieces.RoomDefinition p_228713_)
```

**Parameters:**

- `p_228712_` (`Direction`)
- `p_228713_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### OceanMonumentDoubleXRoom

```java
public OceanMonumentDoubleXRoom(CompoundTag p_228715_)
```

**Parameters:**

- `p_228715_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### postProcess

```java
public void postProcess(WorldGenLevel p_228717_, StructureManager p_228718_, ChunkGenerator p_228719_, RandomSource p_228720_, BoundingBox p_228721_, ChunkPos p_228722_, BlockPos p_228723_)
```

**Parameters:**

- `p_228717_` (`WorldGenLevel`)
- `p_228718_` (`StructureManager`)
- `p_228719_` (`ChunkGenerator`)
- `p_228720_` (`RandomSource`)
- `p_228721_` (`BoundingBox`)
- `p_228722_` (`ChunkPos`)
- `p_228723_` (`BlockPos`)

### OceanMonumentDoubleXYRoom

```java
public OceanMonumentDoubleXYRoom(Direction p_228725_, OceanMonumentPieces.RoomDefinition p_228726_)
```

**Parameters:**

- `p_228725_` (`Direction`)
- `p_228726_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### OceanMonumentDoubleXYRoom

```java
public OceanMonumentDoubleXYRoom(CompoundTag p_228728_)
```

**Parameters:**

- `p_228728_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### postProcess

```java
public void postProcess(WorldGenLevel p_228730_, StructureManager p_228731_, ChunkGenerator p_228732_, RandomSource p_228733_, BoundingBox p_228734_, ChunkPos p_228735_, BlockPos p_228736_)
```

**Parameters:**

- `p_228730_` (`WorldGenLevel`)
- `p_228731_` (`StructureManager`)
- `p_228732_` (`ChunkGenerator`)
- `p_228733_` (`RandomSource`)
- `p_228734_` (`BoundingBox`)
- `p_228735_` (`ChunkPos`)
- `p_228736_` (`BlockPos`)

### OceanMonumentDoubleYRoom

```java
public OceanMonumentDoubleYRoom(Direction p_228738_, OceanMonumentPieces.RoomDefinition p_228739_)
```

**Parameters:**

- `p_228738_` (`Direction`)
- `p_228739_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### OceanMonumentDoubleYRoom

```java
public OceanMonumentDoubleYRoom(CompoundTag p_228741_)
```

**Parameters:**

- `p_228741_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### postProcess

```java
public void postProcess(WorldGenLevel p_228743_, StructureManager p_228744_, ChunkGenerator p_228745_, RandomSource p_228746_, BoundingBox p_228747_, ChunkPos p_228748_, BlockPos p_228749_)
```

**Parameters:**

- `p_228743_` (`WorldGenLevel`)
- `p_228744_` (`StructureManager`)
- `p_228745_` (`ChunkGenerator`)
- `p_228746_` (`RandomSource`)
- `p_228747_` (`BoundingBox`)
- `p_228748_` (`ChunkPos`)
- `p_228749_` (`BlockPos`)

### OceanMonumentDoubleYZRoom

```java
public OceanMonumentDoubleYZRoom(Direction p_228751_, OceanMonumentPieces.RoomDefinition p_228752_)
```

**Parameters:**

- `p_228751_` (`Direction`)
- `p_228752_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### OceanMonumentDoubleYZRoom

```java
public OceanMonumentDoubleYZRoom(CompoundTag p_228754_)
```

**Parameters:**

- `p_228754_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### postProcess

```java
public void postProcess(WorldGenLevel p_228756_, StructureManager p_228757_, ChunkGenerator p_228758_, RandomSource p_228759_, BoundingBox p_228760_, ChunkPos p_228761_, BlockPos p_228762_)
```

**Parameters:**

- `p_228756_` (`WorldGenLevel`)
- `p_228757_` (`StructureManager`)
- `p_228758_` (`ChunkGenerator`)
- `p_228759_` (`RandomSource`)
- `p_228760_` (`BoundingBox`)
- `p_228761_` (`ChunkPos`)
- `p_228762_` (`BlockPos`)

### OceanMonumentDoubleZRoom

```java
public OceanMonumentDoubleZRoom(Direction p_228764_, OceanMonumentPieces.RoomDefinition p_228765_)
```

**Parameters:**

- `p_228764_` (`Direction`)
- `p_228765_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### OceanMonumentDoubleZRoom

```java
public OceanMonumentDoubleZRoom(CompoundTag p_228767_)
```

**Parameters:**

- `p_228767_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### postProcess

```java
public void postProcess(WorldGenLevel p_228769_, StructureManager p_228770_, ChunkGenerator p_228771_, RandomSource p_228772_, BoundingBox p_228773_, ChunkPos p_228774_, BlockPos p_228775_)
```

**Parameters:**

- `p_228769_` (`WorldGenLevel`)
- `p_228770_` (`StructureManager`)
- `p_228771_` (`ChunkGenerator`)
- `p_228772_` (`RandomSource`)
- `p_228773_` (`BoundingBox`)
- `p_228774_` (`ChunkPos`)
- `p_228775_` (`BlockPos`)

### OceanMonumentEntryRoom

```java
public OceanMonumentEntryRoom(Direction p_228777_, OceanMonumentPieces.RoomDefinition p_228778_)
```

**Parameters:**

- `p_228777_` (`Direction`)
- `p_228778_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### OceanMonumentEntryRoom

```java
public OceanMonumentEntryRoom(CompoundTag p_228780_)
```

**Parameters:**

- `p_228780_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### postProcess

```java
public void postProcess(WorldGenLevel p_228782_, StructureManager p_228783_, ChunkGenerator p_228784_, RandomSource p_228785_, BoundingBox p_228786_, ChunkPos p_228787_, BlockPos p_228788_)
```

**Parameters:**

- `p_228782_` (`WorldGenLevel`)
- `p_228783_` (`StructureManager`)
- `p_228784_` (`ChunkGenerator`)
- `p_228785_` (`RandomSource`)
- `p_228786_` (`BoundingBox`)
- `p_228787_` (`ChunkPos`)
- `p_228788_` (`BlockPos`)

### OceanMonumentPenthouse

```java
public OceanMonumentPenthouse(Direction p_228790_, BoundingBox p_228791_)
```

**Parameters:**

- `p_228790_` (`Direction`)
- `p_228791_` (`BoundingBox`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### OceanMonumentPenthouse

```java
public OceanMonumentPenthouse(CompoundTag p_228793_)
```

**Parameters:**

- `p_228793_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### postProcess

```java
public void postProcess(WorldGenLevel p_228795_, StructureManager p_228796_, ChunkGenerator p_228797_, RandomSource p_228798_, BoundingBox p_228799_, ChunkPos p_228800_, BlockPos p_228801_)
```

**Parameters:**

- `p_228795_` (`WorldGenLevel`)
- `p_228796_` (`StructureManager`)
- `p_228797_` (`ChunkGenerator`)
- `p_228798_` (`RandomSource`)
- `p_228799_` (`BoundingBox`)
- `p_228800_` (`ChunkPos`)
- `p_228801_` (`BlockPos`)

### getRoomIndex

```java
protected static int getRoomIndex(int p_228890_, int p_228891_, int p_228892_)
```

**Parameters:**

- `p_228890_` (`int`)
- `p_228891_` (`int`)
- `p_228892_` (`int`)

**Returns:** `protected static int`

### OceanMonumentPiece

```java
public OceanMonumentPiece(StructurePieceType p_228836_, Direction p_228837_, int p_228838_, BoundingBox p_228839_)
```

**Parameters:**

- `p_228836_` (`StructurePieceType`)
- `p_228837_` (`Direction`)
- `p_228838_` (`int`)
- `p_228839_` (`BoundingBox`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### OceanMonumentPiece

```java
protected OceanMonumentPiece(StructurePieceType p_228828_, int p_228829_, Direction p_228830_, OceanMonumentPieces.RoomDefinition p_228831_, int p_228832_, int p_228833_, int p_228834_)
```

**Parameters:**

- `p_228828_` (`StructurePieceType`)
- `p_228829_` (`int`)
- `p_228830_` (`Direction`)
- `p_228831_` (`OceanMonumentPieces.RoomDefinition`)
- `p_228832_` (`int`)
- `p_228833_` (`int`)
- `p_228834_` (`int`)

**Returns:** `protected`

### makeBoundingBox

```java
private static BoundingBox makeBoundingBox(Direction p_228875_, OceanMonumentPieces.RoomDefinition p_228876_, int p_228877_, int p_228878_, int p_228879_)
```

**Parameters:**

- `p_228875_` (`Direction`)
- `p_228876_` (`OceanMonumentPieces.RoomDefinition`)
- `p_228877_` (`int`)
- `p_228878_` (`int`)
- `p_228879_` (`int`)

**Returns:** `private static BoundingBox`

### OceanMonumentPiece

```java
public OceanMonumentPiece(StructurePieceType p_228841_, CompoundTag p_228842_)
```

**Parameters:**

- `p_228841_` (`StructurePieceType`)
- `p_228842_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### addAdditionalSaveData

```java
protected void addAdditionalSaveData(StructurePieceSerializationContext p_228872_, CompoundTag p_228873_)
```

**Parameters:**

- `p_228872_` (`StructurePieceSerializationContext`)
- `p_228873_` (`CompoundTag`)

### generateWaterBox

```java
protected void generateWaterBox(WorldGenLevel p_228881_, BoundingBox p_228882_, int p_228883_, int p_228884_, int p_228885_, int p_228886_, int p_228887_, int p_228888_)
```

**Parameters:**

- `p_228881_` (`WorldGenLevel`)
- `p_228882_` (`BoundingBox`)
- `p_228883_` (`int`)
- `p_228884_` (`int`)
- `p_228885_` (`int`)
- `p_228886_` (`int`)
- `p_228887_` (`int`)
- `p_228888_` (`int`)

**Returns:** `protected void`

### generateDefaultFloor

```java
protected void generateDefaultFloor(WorldGenLevel p_228860_, BoundingBox p_228861_, int p_228862_, int p_228863_, boolean p_228864_)
```

**Parameters:**

- `p_228860_` (`WorldGenLevel`)
- `p_228861_` (`BoundingBox`)
- `p_228862_` (`int`)
- `p_228863_` (`int`)
- `p_228864_` (`boolean`)

**Returns:** `protected void`

### generateBoxOnFillOnly

```java
protected void generateBoxOnFillOnly(WorldGenLevel p_228850_, BoundingBox p_228851_, int p_228852_, int p_228853_, int p_228854_, int p_228855_, int p_228856_, int p_228857_, BlockState p_228858_)
```

**Parameters:**

- `p_228850_` (`WorldGenLevel`)
- `p_228851_` (`BoundingBox`)
- `p_228852_` (`int`)
- `p_228853_` (`int`)
- `p_228854_` (`int`)
- `p_228855_` (`int`)
- `p_228856_` (`int`)
- `p_228857_` (`int`)
- `p_228858_` (`BlockState`)

**Returns:** `protected void`

### chunkIntersects

```java
protected boolean chunkIntersects(BoundingBox p_228866_, int p_228867_, int p_228868_, int p_228869_, int p_228870_)
```

**Parameters:**

- `p_228866_` (`BoundingBox`)
- `p_228867_` (`int`)
- `p_228868_` (`int`)
- `p_228869_` (`int`)
- `p_228870_` (`int`)

**Returns:** `protected boolean`

### spawnElder

```java
protected void spawnElder(WorldGenLevel p_251919_, BoundingBox p_248944_, int p_251311_, int p_249326_, int p_252095_)
```

**Parameters:**

- `p_251919_` (`WorldGenLevel`)
- `p_248944_` (`BoundingBox`)
- `p_251311_` (`int`)
- `p_249326_` (`int`)
- `p_252095_` (`int`)

**Returns:** `protected void`

### OceanMonumentSimpleRoom

```java
public OceanMonumentSimpleRoom(Direction p_228895_, OceanMonumentPieces.RoomDefinition p_228896_, RandomSource p_228897_)
```

**Parameters:**

- `p_228895_` (`Direction`)
- `p_228896_` (`OceanMonumentPieces.RoomDefinition`)
- `p_228897_` (`RandomSource`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### OceanMonumentSimpleRoom

```java
public OceanMonumentSimpleRoom(CompoundTag p_228899_)
```

**Parameters:**

- `p_228899_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### postProcess

```java
public void postProcess(WorldGenLevel p_228901_, StructureManager p_228902_, ChunkGenerator p_228903_, RandomSource p_228904_, BoundingBox p_228905_, ChunkPos p_228906_, BlockPos p_228907_)
```

**Parameters:**

- `p_228901_` (`WorldGenLevel`)
- `p_228902_` (`StructureManager`)
- `p_228903_` (`ChunkGenerator`)
- `p_228904_` (`RandomSource`)
- `p_228905_` (`BoundingBox`)
- `p_228906_` (`ChunkPos`)
- `p_228907_` (`BlockPos`)

### OceanMonumentSimpleTopRoom

```java
public OceanMonumentSimpleTopRoom(Direction p_228909_, OceanMonumentPieces.RoomDefinition p_228910_)
```

**Parameters:**

- `p_228909_` (`Direction`)
- `p_228910_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### OceanMonumentSimpleTopRoom

```java
public OceanMonumentSimpleTopRoom(CompoundTag p_228912_)
```

**Parameters:**

- `p_228912_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### postProcess

```java
public void postProcess(WorldGenLevel p_228914_, StructureManager p_228915_, ChunkGenerator p_228916_, RandomSource p_228917_, BoundingBox p_228918_, ChunkPos p_228919_, BlockPos p_228920_)
```

**Parameters:**

- `p_228914_` (`WorldGenLevel`)
- `p_228915_` (`StructureManager`)
- `p_228916_` (`ChunkGenerator`)
- `p_228917_` (`RandomSource`)
- `p_228918_` (`BoundingBox`)
- `p_228919_` (`ChunkPos`)
- `p_228920_` (`BlockPos`)

### OceanMonumentWingRoom

```java
public OceanMonumentWingRoom(Direction p_228923_, BoundingBox p_228924_, int p_228925_)
```

**Parameters:**

- `p_228923_` (`Direction`)
- `p_228924_` (`BoundingBox`)
- `p_228925_` (`int`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### OceanMonumentWingRoom

```java
public OceanMonumentWingRoom(CompoundTag p_228927_)
```

**Parameters:**

- `p_228927_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### postProcess

```java
public void postProcess(WorldGenLevel p_228929_, StructureManager p_228930_, ChunkGenerator p_228931_, RandomSource p_228932_, BoundingBox p_228933_, ChunkPos p_228934_, BlockPos p_228935_)
```

**Parameters:**

- `p_228929_` (`WorldGenLevel`)
- `p_228930_` (`StructureManager`)
- `p_228931_` (`ChunkGenerator`)
- `p_228932_` (`RandomSource`)
- `p_228933_` (`BoundingBox`)
- `p_228934_` (`ChunkPos`)
- `p_228935_` (`BlockPos`)

### RoomDefinition

```java
public RoomDefinition(int p_228943_)
```

**Parameters:**

- `p_228943_` (`int`)

**Returns:** `public`

### setConnection

```java
public void setConnection(Direction p_228948_, OceanMonumentPieces.RoomDefinition p_228949_)
```

**Parameters:**

- `p_228948_` (`Direction`)
- `p_228949_` (`OceanMonumentPieces.RoomDefinition`)

**Returns:** `public void`

### updateOpenings

```java
public void updateOpenings()
```

**Returns:** `public void`

### findSource

```java
public boolean findSource(int p_228946_)
```

**Parameters:**

- `p_228946_` (`int`)

**Returns:** `public boolean`

### isSpecial

```java
public boolean isSpecial()
```

**Returns:** `public boolean`

### countOpenings

```java
public int countOpenings()
```

**Returns:** `public int`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `MonumentBuilding` | class |  |
| `OceanMonumentCoreRoom` | class |  |
| `OceanMonumentDoubleXRoom` | class |  |
| `OceanMonumentDoubleXYRoom` | class |  |
| `OceanMonumentDoubleYRoom` | class |  |
| `OceanMonumentDoubleYZRoom` | class |  |
| `OceanMonumentDoubleZRoom` | class |  |
| `OceanMonumentEntryRoom` | class |  |
| `OceanMonumentPenthouse` | class |  |
| `OceanMonumentSimpleRoom` | class |  |
| `OceanMonumentSimpleTopRoom` | class |  |
| `OceanMonumentWingRoom` | class |  |
