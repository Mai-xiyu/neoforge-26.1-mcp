# PatrolSpawner

**Package:** `net.minecraft.world.level.levelgen`
**Type:** class
**Implements:** `CustomSpawner`

## Methods

### tick

```java
public int tick(ServerLevel p_64570_, boolean p_64571_, boolean p_64572_)
```

**Parameters:**

- `p_64570_` (`ServerLevel`)
- `p_64571_` (`boolean`)
- `p_64572_` (`boolean`)

**Returns:** `int`

### spawnPatrolMember

```java
private boolean spawnPatrolMember(ServerLevel p_224533_, BlockPos p_224534_, RandomSource p_224535_, boolean p_224536_)
```

**Parameters:**

- `p_224533_` (`ServerLevel`)
- `p_224534_` (`BlockPos`)
- `p_224535_` (`RandomSource`)
- `p_224536_` (`boolean`)

**Returns:** `private boolean`
