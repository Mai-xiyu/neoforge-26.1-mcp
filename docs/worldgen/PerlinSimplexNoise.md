# PerlinSimplexNoise

**Package:** `net.minecraft.world.level.levelgen.synth`
**Type:** class

## Methods

### PerlinSimplexNoise

```java
public PerlinSimplexNoise(RandomSource p_230546_, List<Integer> p_230547_)
```

**Parameters:**

- `p_230546_` (`RandomSource`)
- `p_230547_` (`List<Integer>`)

**Returns:** `public`

### PerlinSimplexNoise

```java
private PerlinSimplexNoise(RandomSource p_230543_, IntSortedSet p_230544_)
```

**Parameters:**

- `p_230543_` (`RandomSource`)
- `p_230544_` (`IntSortedSet`)

**Returns:** `private`

### IllegalArgumentException

```java
throw new IllegalArgumentException("Need some octaves!")
```

**Parameters:**

- `octaves!"` (`"Need some`)

**Returns:** `throw new`

### IllegalArgumentException

```java
throw new IllegalArgumentException("Total number of octaves needs to be >= 1")
```

**Parameters:**

- `1"` (`"Total number of octaves needs to be >=`)

**Returns:** `throw new`

### getValue

```java
public double getValue(double p_75450_, double p_75451_, boolean p_75452_)
```

**Parameters:**

- `p_75450_` (`double`)
- `p_75451_` (`double`)
- `p_75452_` (`boolean`)

**Returns:** `public double`
