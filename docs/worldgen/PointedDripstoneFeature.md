# PointedDripstoneFeature

**Package:** `net.minecraft.world.level.levelgen.feature`
**Type:** class
**Extends:** `Feature<PointedDripstoneConfiguration>`

## Methods

### PointedDripstoneFeature

```java
public PointedDripstoneFeature(Codec<PointedDripstoneConfiguration> p_191067_)
```

**Parameters:**

- `p_191067_` (`Codec<PointedDripstoneConfiguration>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### place

```java
public boolean place(FeaturePlaceContext<PointedDripstoneConfiguration> p_191078_)
```

**Parameters:**

- `p_191078_` (`FeaturePlaceContext<PointedDripstoneConfiguration>`)

**Returns:** `boolean`

### createPatchOfDripstoneBlocks

```java
 createPatchOfDripstoneBlocks()
```

**Returns:** ``

### getTipDirection

```java
private static Optional<Direction> getTipDirection(LevelAccessor p_225199_, BlockPos p_225200_, RandomSource p_225201_)
```

**Parameters:**

- `p_225199_` (`LevelAccessor`)
- `p_225200_` (`BlockPos`)
- `p_225201_` (`RandomSource`)

**Returns:** `private static Optional<Direction>`

### createPatchOfDripstoneBlocks

```java
private static void createPatchOfDripstoneBlocks(LevelAccessor p_225194_, RandomSource p_225195_, BlockPos p_225196_, PointedDripstoneConfiguration p_225197_)
```

**Parameters:**

- `p_225194_` (`LevelAccessor`)
- `p_225195_` (`RandomSource`)
- `p_225196_` (`BlockPos`)
- `p_225197_` (`PointedDripstoneConfiguration`)

**Returns:** `private static void`
