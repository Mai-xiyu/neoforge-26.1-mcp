# PoolAliasLookup

**Package:** `net.minecraft.world.level.levelgen.structure.pools.alias`
**Type:** interface
**Annotations:** `@FunctionalInterface`

## Methods

### lookup

```java
ResourceKey<StructureTemplatePool> lookup(ResourceKey<StructureTemplatePool> p_307512_)
```

**Parameters:**

- `p_307512_` (`ResourceKey<StructureTemplatePool>`)

**Returns:** `ResourceKey<StructureTemplatePool>`

### create

```java
static PoolAliasLookup create(List<PoolAliasBinding> p_307423_, BlockPos p_307208_, long p_307622_)
```

**Parameters:**

- `p_307423_` (`List<PoolAliasBinding>`)
- `p_307208_` (`BlockPos`)
- `p_307622_` (`long`)

**Returns:** `static PoolAliasLookup`
