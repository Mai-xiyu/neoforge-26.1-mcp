# ProcessorRule

**Package:** `net.minecraft.world.level.levelgen.structure.templatesystem`
**Type:** class

## Fields

| Name | Type | Description |
|------|------|-------------|
| `DEFAULT_BLOCK_ENTITY_MODIFIER` | `Passthrough` |  |
| `CODEC` | `Codec<ProcessorRule>` |  |

## Methods

### ProcessorRule

```java
public ProcessorRule(RuleTest p_74223_, RuleTest p_74224_, BlockState p_74225_)
```

**Parameters:**

- `p_74223_` (`RuleTest`)
- `p_74224_` (`RuleTest`)
- `p_74225_` (`BlockState`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### ProcessorRule

```java
public ProcessorRule(RuleTest p_74227_, RuleTest p_74228_, PosRuleTest p_74229_, BlockState p_74230_)
```

**Parameters:**

- `p_74227_` (`RuleTest`)
- `p_74228_` (`RuleTest`)
- `p_74229_` (`PosRuleTest`)
- `p_74230_` (`BlockState`)

**Returns:** `public`

### this

```java
 this()
```

**Returns:** ``

### ProcessorRule

```java
public ProcessorRule(RuleTest p_277678_, RuleTest p_277379_, PosRuleTest p_278018_, BlockState p_277412_, RuleBlockEntityModifier p_277808_)
```

**Parameters:**

- `p_277678_` (`RuleTest`)
- `p_277379_` (`RuleTest`)
- `p_278018_` (`PosRuleTest`)
- `p_277412_` (`BlockState`)
- `p_277808_` (`RuleBlockEntityModifier`)

**Returns:** `public`

### test

```java
public boolean test(BlockState p_230310_, BlockState p_230311_, BlockPos p_230312_, BlockPos p_230313_, BlockPos p_230314_, RandomSource p_230315_)
```

**Parameters:**

- `p_230310_` (`BlockState`)
- `p_230311_` (`BlockState`)
- `p_230312_` (`BlockPos`)
- `p_230313_` (`BlockPos`)
- `p_230314_` (`BlockPos`)
- `p_230315_` (`RandomSource`)

**Returns:** `public boolean`

### getOutputState

```java
public BlockState getOutputState()
```

**Returns:** `public BlockState`

### getOutputTag

```java
public CompoundTag getOutputTag(RandomSource p_277551_, CompoundTag p_277867_)
```

**Parameters:**

- `p_277551_` (`RandomSource`)
- `p_277867_` (`CompoundTag`)

**Returns:** `CompoundTag`
