# RarityFilter

**Package:** `net.minecraft.world.level.levelgen.placement`
**Type:** class
**Extends:** `PlacementFilter`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<RarityFilter>` |  |

## Methods

### RarityFilter

```java
private RarityFilter(int p_191899_)
```

**Parameters:**

- `p_191899_` (`int`)

**Returns:** `private`

### onAverageOnceEvery

```java
public static RarityFilter onAverageOnceEvery(int p_191901_)
```

**Parameters:**

- `p_191901_` (`int`)

**Returns:** `public static RarityFilter`

### RarityFilter

```java
return new RarityFilter()
```

**Returns:** `return new`

### shouldPlace

```java
protected boolean shouldPlace(PlacementContext p_226397_, RandomSource p_226398_, BlockPos p_226399_)
```

**Parameters:**

- `p_226397_` (`PlacementContext`)
- `p_226398_` (`RandomSource`)
- `p_226399_` (`BlockPos`)

**Returns:** `boolean`

### type

```java
public PlacementModifierType<?> type()
```

**Returns:** `PlacementModifierType<?>`
