# RootPlacerType

**Package:** `net.minecraft.world.level.levelgen.feature.rootplacers`
**Type:** class<P extends RootPlacer>

## Fields

| Name | Type | Description |
|------|------|-------------|
| `MANGROVE_ROOT_PLACER` | `RootPlacerType<MangroveRootPlacer>` |  |

## Methods

### register

```java
<P extends RootPlacer> private static <P extends RootPlacer> RootPlacerType<P> register(String p_225905_, MapCodec<P> p_338740_)
```

**Parameters:**

- `p_225905_` (`String`)
- `p_338740_` (`MapCodec<P>`)

**Returns:** `private static <P extends RootPlacer> RootPlacerType<P>`

### RootPlacerType

```java
public RootPlacerType(MapCodec<P> p_338455_)
```

**Parameters:**

- `p_338455_` (`MapCodec<P>`)

**Returns:** `public`

### codec

```java
public MapCodec<P> codec()
```

**Returns:** `public MapCodec<P>`
