# RuleBlockEntityModifierType

**Package:** `net.minecraft.world.level.levelgen.structure.templatesystem.rule.blockentity`
**Type:** interface<P extends RuleBlockEntityModifier>

## Methods

### codec

```java
MapCodec<P> codec()
```

**Returns:** `MapCodec<P>`

### register

```java
<P extends RuleBlockEntityModifier> private static <P extends RuleBlockEntityModifier> RuleBlockEntityModifierType<P> register(String p_277659_, MapCodec<P> p_338294_)
```

**Parameters:**

- `p_277659_` (`String`)
- `p_338294_` (`MapCodec<P>`)

**Returns:** `private static <P extends RuleBlockEntityModifier> RuleBlockEntityModifierType<P>`
