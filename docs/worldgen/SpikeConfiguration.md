# SpikeConfiguration

**Package:** `net.minecraft.world.level.levelgen.feature.configurations`
**Type:** class
**Implements:** `FeatureConfiguration`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `Codec<SpikeConfiguration>` |  |

## Methods

### SpikeConfiguration

```java
public SpikeConfiguration(boolean p_68105_, List<SpikeFeature.EndSpike> p_68106_, BlockPos p_68107_)
```

**Parameters:**

- `p_68105_` (`boolean`)
- `p_68106_` (`List<SpikeFeature.EndSpike>`)
- `p_68107_` (`BlockPos`)

**Returns:** `public`

### SpikeConfiguration

```java
private SpikeConfiguration(boolean p_68109_, List<SpikeFeature.EndSpike> p_68110_, Optional<BlockPos> p_68111_)
```

**Parameters:**

- `p_68109_` (`boolean`)
- `p_68110_` (`List<SpikeFeature.EndSpike>`)
- `p_68111_` (`Optional<BlockPos>`)

**Returns:** `private`

### isCrystalInvulnerable

```java
public boolean isCrystalInvulnerable()
```

**Returns:** `public boolean`

### getSpikes

```java
public List<SpikeFeature.EndSpike> getSpikes()
```

**Returns:** `public List<SpikeFeature.EndSpike>`

### getCrystalBeamTarget

```java
public BlockPos getCrystalBeamTarget()
```

**Returns:** `BlockPos`
