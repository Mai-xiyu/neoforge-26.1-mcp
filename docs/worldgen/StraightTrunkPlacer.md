# StraightTrunkPlacer

**Package:** `net.minecraft.world.level.levelgen.feature.trunkplacers`
**Type:** class
**Extends:** `TrunkPlacer`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<StraightTrunkPlacer>` |  |

## Methods

### StraightTrunkPlacer

```java
public StraightTrunkPlacer(int p_70248_, int p_70249_, int p_70250_)
```

**Parameters:**

- `p_70248_` (`int`)
- `p_70249_` (`int`)
- `p_70250_` (`int`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### type

```java
protected TrunkPlacerType<?> type()
```

**Returns:** `TrunkPlacerType<?>`

### placeTrunk

```java
public List<FoliagePlacer.FoliageAttachment> placeTrunk(LevelSimulatedReader p_226147_, BiConsumer<BlockPos, BlockState> p_226148_, RandomSource p_226149_, int p_226150_, BlockPos p_226151_, TreeConfiguration p_226152_)
```

**Parameters:**

- `p_226147_` (`LevelSimulatedReader`)
- `p_226148_` (`BiConsumer<BlockPos, BlockState>`)
- `p_226149_` (`RandomSource`)
- `p_226150_` (`int`)
- `p_226151_` (`BlockPos`)
- `p_226152_` (`TreeConfiguration`)

**Returns:** `List<FoliagePlacer.FoliageAttachment>`
