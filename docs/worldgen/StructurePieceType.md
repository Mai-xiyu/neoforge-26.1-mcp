# StructurePieceType

**Package:** `net.minecraft.world.level.levelgen.structure.pieces`
**Type:** interface

## Methods

### load

```java
StructurePiece load(StructurePieceSerializationContext p_210161_, CompoundTag p_210162_)
```

**Parameters:**

- `p_210161_` (`StructurePieceSerializationContext`)
- `p_210162_` (`CompoundTag`)

**Returns:** `StructurePiece`

### setFullContextPieceId

```java
private static StructurePieceType setFullContextPieceId(StructurePieceType p_210159_, String p_210160_)
```

**Parameters:**

- `p_210159_` (`StructurePieceType`)
- `p_210160_` (`String`)

**Returns:** `private static StructurePieceType`

### setPieceId

```java
private static StructurePieceType setPieceId(StructurePieceType.ContextlessType p_210153_, String p_210154_)
```

**Parameters:**

- `p_210153_` (`StructurePieceType.ContextlessType`)
- `p_210154_` (`String`)

**Returns:** `private static StructurePieceType`

### setFullContextPieceId

```java
return setFullContextPieceId()
```

**Returns:** `return`

### setTemplatePieceId

```java
private static StructurePieceType setTemplatePieceId(StructurePieceType.StructureTemplateType p_210156_, String p_210157_)
```

**Parameters:**

- `p_210156_` (`StructurePieceType.StructureTemplateType`)
- `p_210157_` (`String`)

**Returns:** `private static StructurePieceType`

### setFullContextPieceId

```java
return setFullContextPieceId()
```

**Returns:** `return`

### load

```java
StructurePiece load(CompoundTag p_210167_)
```

**Parameters:**

- `p_210167_` (`CompoundTag`)

**Returns:** `StructurePiece`

### load

```java
default StructurePiece load(StructurePieceSerializationContext p_210164_, CompoundTag p_210165_)
```

**Parameters:**

- `p_210164_` (`StructurePieceSerializationContext`)
- `p_210165_` (`CompoundTag`)

**Returns:** `StructurePiece`

### load

```java
StructurePiece load(StructureTemplateManager p_226963_, CompoundTag p_226964_)
```

**Parameters:**

- `p_226963_` (`StructureTemplateManager`)
- `p_226964_` (`CompoundTag`)

**Returns:** `StructurePiece`

### load

```java
default StructurePiece load(StructurePieceSerializationContext p_210169_, CompoundTag p_210170_)
```

**Parameters:**

- `p_210169_` (`StructurePieceSerializationContext`)
- `p_210170_` (`CompoundTag`)

**Returns:** `StructurePiece`

## Inner Classes

| Name | Type | Description |
|------|------|-------------|
| `ContextlessType` | interface |  |
| `StructureTemplateType` | interface |  |
