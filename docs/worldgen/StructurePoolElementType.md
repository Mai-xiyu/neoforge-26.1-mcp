# StructurePoolElementType

**Package:** `net.minecraft.world.level.levelgen.structure.pools`
**Type:** interface<P extends StructurePoolElement>

## Methods

### codec

```java
MapCodec<P> codec()
```

**Returns:** `MapCodec<P>`

### register

```java
<P extends StructurePoolElement> static <P extends StructurePoolElement> StructurePoolElementType<P> register(String p_210551_, MapCodec<P> p_338276_)
```

**Parameters:**

- `p_210551_` (`String`)
- `p_338276_` (`MapCodec<P>`)

**Returns:** `static <P extends StructurePoolElement> StructurePoolElementType<P>`
