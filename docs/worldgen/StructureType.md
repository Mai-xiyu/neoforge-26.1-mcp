# StructureType

**Package:** `net.minecraft.world.level.levelgen.structure`
**Type:** interface<S extends Structure>

## Methods

### codec

```java
MapCodec<S> codec()
```

**Returns:** `MapCodec<S>`

### register

```java
<S extends Structure> private static <S extends Structure> StructureType<S> register(String p_226882_, MapCodec<S> p_338639_)
```

**Parameters:**

- `p_226882_` (`String`)
- `p_338639_` (`MapCodec<S>`)

**Returns:** `private static <S extends Structure> StructureType<S>`
