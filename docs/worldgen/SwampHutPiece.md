# SwampHutPiece

**Package:** `net.minecraft.world.level.levelgen.structure.structures`
**Type:** class
**Extends:** `ScatteredFeaturePiece`

## Methods

### SwampHutPiece

```java
public SwampHutPiece(RandomSource p_229952_, int p_229953_, int p_229954_)
```

**Parameters:**

- `p_229952_` (`RandomSource`)
- `p_229953_` (`int`)
- `p_229954_` (`int`)

**Returns:** `public`

### SwampHutPiece

```java
public SwampHutPiece(CompoundTag p_229956_)
```

**Parameters:**

- `p_229956_` (`CompoundTag`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### addAdditionalSaveData

```java
protected void addAdditionalSaveData(StructurePieceSerializationContext p_229969_, CompoundTag p_229970_)
```

**Parameters:**

- `p_229969_` (`StructurePieceSerializationContext`)
- `p_229970_` (`CompoundTag`)

### postProcess

```java
public void postProcess(WorldGenLevel p_229961_, StructureManager p_229962_, ChunkGenerator p_229963_, RandomSource p_229964_, BoundingBox p_229965_, ChunkPos p_229966_, BlockPos p_229967_)
```

**Parameters:**

- `p_229961_` (`WorldGenLevel`)
- `p_229962_` (`StructureManager`)
- `p_229963_` (`ChunkGenerator`)
- `p_229964_` (`RandomSource`)
- `p_229965_` (`BoundingBox`)
- `p_229966_` (`ChunkPos`)
- `p_229967_` (`BlockPos`)

### spawnCat

```java
private void spawnCat(ServerLevelAccessor p_229958_, BoundingBox p_229959_)
```

**Parameters:**

- `p_229958_` (`ServerLevelAccessor`)
- `p_229959_` (`BoundingBox`)

**Returns:** `private void`
