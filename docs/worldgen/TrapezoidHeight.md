# TrapezoidHeight

**Package:** `net.minecraft.world.level.levelgen.heightproviders`
**Type:** class
**Extends:** `HeightProvider`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<TrapezoidHeight>` |  |

## Methods

### TrapezoidHeight

```java
private TrapezoidHeight(VerticalAnchor p_162000_, VerticalAnchor p_162001_, int p_162002_)
```

**Parameters:**

- `p_162000_` (`VerticalAnchor`)
- `p_162001_` (`VerticalAnchor`)
- `p_162002_` (`int`)

**Returns:** `private`

### of

```java
public static TrapezoidHeight of(VerticalAnchor p_162010_, VerticalAnchor p_162011_, int p_162012_)
```

**Parameters:**

- `p_162010_` (`VerticalAnchor`)
- `p_162011_` (`VerticalAnchor`)
- `p_162012_` (`int`)

**Returns:** `public static TrapezoidHeight`

### TrapezoidHeight

```java
return new TrapezoidHeight()
```

**Returns:** `return new`

### of

```java
public static TrapezoidHeight of(VerticalAnchor p_162007_, VerticalAnchor p_162008_)
```

**Parameters:**

- `p_162007_` (`VerticalAnchor`)
- `p_162008_` (`VerticalAnchor`)

**Returns:** `public static TrapezoidHeight`

### of

```java
return of()
```

**Returns:** `return`

### sample

```java
public int sample(RandomSource p_226305_, WorldGenerationContext p_226306_)
```

**Parameters:**

- `p_226305_` (`RandomSource`)
- `p_226306_` (`WorldGenerationContext`)

**Returns:** `int`

### getType

```java
public HeightProviderType<?> getType()
```

**Returns:** `HeightProviderType<?>`

### toString

```java
public String toString()
```

**Returns:** `String`
