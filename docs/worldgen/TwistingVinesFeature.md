# TwistingVinesFeature

**Package:** `net.minecraft.world.level.levelgen.feature`
**Type:** class
**Extends:** `Feature<TwistingVinesConfig>`

## Methods

### TwistingVinesFeature

```java
public TwistingVinesFeature(Codec<TwistingVinesConfig> p_67292_)
```

**Parameters:**

- `p_67292_` (`Codec<TwistingVinesConfig>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### place

```java
public boolean place(FeaturePlaceContext<TwistingVinesConfig> p_160558_)
```

**Parameters:**

- `p_160558_` (`FeaturePlaceContext<TwistingVinesConfig>`)

**Returns:** `boolean`

### placeWeepingVinesColumn

```java
 placeWeepingVinesColumn()
```

**Returns:** ``

### findFirstAirBlockAboveGround

```java
private static boolean findFirstAirBlockAboveGround(LevelAccessor p_67294_, BlockPos.MutableBlockPos p_67295_)
```

**Parameters:**

- `p_67294_` (`LevelAccessor`)
- `p_67295_` (`BlockPos.MutableBlockPos`)

**Returns:** `private static boolean`

### placeWeepingVinesColumn

```java
public static void placeWeepingVinesColumn(LevelAccessor p_225301_, RandomSource p_225302_, BlockPos.MutableBlockPos p_225303_, int p_225304_, int p_225305_, int p_225306_)
```

**Parameters:**

- `p_225301_` (`LevelAccessor`)
- `p_225302_` (`RandomSource`)
- `p_225303_` (`BlockPos.MutableBlockPos`)
- `p_225304_` (`int`)
- `p_225305_` (`int`)
- `p_225306_` (`int`)

**Returns:** `public static void`

### isInvalidPlacementLocation

```java
private static boolean isInvalidPlacementLocation(LevelAccessor p_67297_, BlockPos p_67298_)
```

**Parameters:**

- `p_67297_` (`LevelAccessor`)
- `p_67298_` (`BlockPos`)

**Returns:** `private static boolean`
