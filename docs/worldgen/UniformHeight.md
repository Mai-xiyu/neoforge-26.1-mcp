# UniformHeight

**Package:** `net.minecraft.world.level.levelgen.heightproviders`
**Type:** class
**Extends:** `HeightProvider`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<UniformHeight>` |  |

## Methods

### UniformHeight

```java
private UniformHeight(VerticalAnchor p_162029_, VerticalAnchor p_162030_)
```

**Parameters:**

- `p_162029_` (`VerticalAnchor`)
- `p_162030_` (`VerticalAnchor`)

**Returns:** `private`

### of

```java
public static UniformHeight of(VerticalAnchor p_162035_, VerticalAnchor p_162036_)
```

**Parameters:**

- `p_162035_` (`VerticalAnchor`)
- `p_162036_` (`VerticalAnchor`)

**Returns:** `public static UniformHeight`

### UniformHeight

```java
return new UniformHeight()
```

**Returns:** `return new`

### sample

```java
public int sample(RandomSource p_226308_, WorldGenerationContext p_226309_)
```

**Parameters:**

- `p_226308_` (`RandomSource`)
- `p_226309_` (`WorldGenerationContext`)

**Returns:** `int`

### getType

```java
public HeightProviderType<?> getType()
```

**Returns:** `HeightProviderType<?>`

### toString

```java
public String toString()
```

**Returns:** `String`
