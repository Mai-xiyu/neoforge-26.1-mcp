# VeryBiasedToBottomHeight

**Package:** `net.minecraft.world.level.levelgen.heightproviders`
**Type:** class
**Extends:** `HeightProvider`

## Fields

| Name | Type | Description |
|------|------|-------------|
| `CODEC` | `MapCodec<VeryBiasedToBottomHeight>` |  |

## Methods

### VeryBiasedToBottomHeight

```java
private VeryBiasedToBottomHeight(VerticalAnchor p_162052_, VerticalAnchor p_162053_, int p_162054_)
```

**Parameters:**

- `p_162052_` (`VerticalAnchor`)
- `p_162053_` (`VerticalAnchor`)
- `p_162054_` (`int`)

**Returns:** `private`

### of

```java
public static VeryBiasedToBottomHeight of(VerticalAnchor p_162059_, VerticalAnchor p_162060_, int p_162061_)
```

**Parameters:**

- `p_162059_` (`VerticalAnchor`)
- `p_162060_` (`VerticalAnchor`)
- `p_162061_` (`int`)

**Returns:** `public static VeryBiasedToBottomHeight`

### VeryBiasedToBottomHeight

```java
return new VeryBiasedToBottomHeight()
```

**Returns:** `return new`

### sample

```java
public int sample(RandomSource p_226311_, WorldGenerationContext p_226312_)
```

**Parameters:**

- `p_226311_` (`RandomSource`)
- `p_226312_` (`WorldGenerationContext`)

**Returns:** `int`

### getType

```java
public HeightProviderType<?> getType()
```

**Returns:** `HeightProviderType<?>`

### toString

```java
public String toString()
```

**Returns:** `String`
