# WeepingVinesFeature

**Package:** `net.minecraft.world.level.levelgen.feature`
**Type:** class
**Extends:** `Feature<NoneFeatureConfiguration>`

## Methods

### WeepingVinesFeature

```java
public WeepingVinesFeature(Codec<NoneFeatureConfiguration> p_67375_)
```

**Parameters:**

- `p_67375_` (`Codec<NoneFeatureConfiguration>`)

**Returns:** `public`

### super

```java
 super()
```

**Returns:** ``

### place

```java
public boolean place(FeaturePlaceContext<NoneFeatureConfiguration> p_160661_)
```

**Parameters:**

- `p_160661_` (`FeaturePlaceContext<NoneFeatureConfiguration>`)

**Returns:** `boolean`

### placeRoofNetherWart

```java
private void placeRoofNetherWart(LevelAccessor p_225360_, RandomSource p_225361_, BlockPos p_225362_)
```

**Parameters:**

- `p_225360_` (`LevelAccessor`)
- `p_225361_` (`RandomSource`)
- `p_225362_` (`BlockPos`)

**Returns:** `private void`

### placeRoofWeepingVines

```java
private void placeRoofWeepingVines(LevelAccessor p_225364_, RandomSource p_225365_, BlockPos p_225366_)
```

**Parameters:**

- `p_225364_` (`LevelAccessor`)
- `p_225365_` (`RandomSource`)
- `p_225366_` (`BlockPos`)

**Returns:** `private void`

### placeWeepingVinesColumn

```java
 placeWeepingVinesColumn()
```

**Returns:** ``

### placeWeepingVinesColumn

```java
public static void placeWeepingVinesColumn(LevelAccessor p_225353_, RandomSource p_225354_, BlockPos.MutableBlockPos p_225355_, int p_225356_, int p_225357_, int p_225358_)
```

**Parameters:**

- `p_225353_` (`LevelAccessor`)
- `p_225354_` (`RandomSource`)
- `p_225355_` (`BlockPos.MutableBlockPos`)
- `p_225356_` (`int`)
- `p_225357_` (`int`)
- `p_225358_` (`int`)

**Returns:** `public static void`
